{
  "name": "@charts-plus/chart-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./presets": {
      "types": "./dist/presets.d.ts",
      "import": "./dist/presets.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npx tsc --build tsconfig.json && tsup src/index.ts src/presets.ts --format esm --sourcemap --minify --tsconfig tsconfig.json --external '@charts-plus/*' --external '@charts-plus/chart-render-webgpu' --external '@charts-plus/chart-render-canvas2d' --no-splitting"
  }
}
