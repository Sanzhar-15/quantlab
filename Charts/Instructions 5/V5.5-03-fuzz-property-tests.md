# V5.5 Prompt 03 - Fuzz + Property Tests

Goal
- Catch edge-case regressions in data handling and crosshair logic.

Constraints
- Must not introduce flaky tests.

Deliverables
- Fuzz tests for data integrity (out-of-order, duplicates, NaNs).
- Property tests for patchExisting + streaming + gaps.
- Crosshair stability under extreme zoom.

Steps
1) Add fuzz tests for ingestion policy.
2) Add property tests for patchExisting + streaming updates.
3) Add crosshair stability tests at extreme zoom.

Acceptance
- Fuzz tests are deterministic and stable.

Tests
- Unit test suite.
