# V5.1 Prompt 01 - Contracts, Gates, Checklist, State

Goal
- Lock V5.1 scope and invariants before any feature work.

Constraints
- Additive only; no breaking API changes.
- Preserve V1/V2 semantics and worker opt-in by import.
- Keep bundle budgets unchanged unless explicitly justified.

Deliverables
- New contract doc: `docs/16_v5_contract.md`.
- New perf gates doc: `docs/17_v5_perf_gates.md`.
- Update `docs/09_release_checklist.md` with V5.1 gates.
- Update `docs/11_current_state.md` with V5.1 scope/invariants.
- If any public options are added, update `docs/04_v2_api_contract.md` (additive note only).
- If any public options are added, update `docs/05_getting_started.md` usage notes.

Steps
1) Draft `docs/16_v5_contract.md` with:
   - Frozen API and semantic invariants.
   - Worker opt-in guarantee.
   - Overlay-only pointermove invariant.
   - Deterministic export requirement.
   - Data integrity rules (out-of-order, duplicates, NaN/Infinity).
2) Draft `docs/17_v5_perf_gates.md` with:
   - Input latency p95 thresholds.
   - Memory ceilings and sampling requirements.
   - Scenario D/E/F constraints and no LOD rebuild spikes.
3) Update `docs/09_release_checklist.md` to include V5.1 verification steps.
4) Update `docs/11_current_state.md` to reflect V5.1 contracts and gates.
5) If any new public options are introduced, add a brief additive note in `docs/04_v2_api_contract.md`.
6) If any new public options are introduced, update `docs/05_getting_started.md` examples or notes.

Acceptance
- Docs are consistent with V4 contracts and do not change runtime behavior.
- Checklist references the new V5.1 contract and gates.

Tests
- None (docs only).
