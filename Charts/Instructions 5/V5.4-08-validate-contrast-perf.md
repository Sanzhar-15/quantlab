# V5.4 Prompt 08 - Validation + Contrast + Perf

Goal
- Validate V5.4 visual changes without regressions.

Constraints
- No baseline updates unless changes are intentional and verified.

Deliverables
- Updated visual regression snapshots.
- Contrast checks for axis/grid/tooltip/focus.
- Perf run A–F to ensure no latency regressions.

Steps
1) Run visual regression for multi-pane, dense series, and tooltip states.
2) Add contrast checks for key text and grid elements.
3) Run perf harness A–F and verify latency gates.

Acceptance
- All visual snapshots stable.
- Contrast meets minimum thresholds.
- Perf gates green.

Tests
- `npm run test:playwright -- tests/visual.spec.ts`
- `npm run perf:check`
