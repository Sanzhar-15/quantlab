# V5.2 Prompt 06 - Incremental LOD Updates

Goal
- Update LOD only for affected buckets on append/updateLast/patch.

Constraints
- No full LOD rebuilds during streaming.

Deliverables
- Dirty-range tracking for LOD pyramid.
- Bounded LOD work per frame.

Steps
1) Add dirty-range tracking per series and per LOD level.
2) Update LOD buckets incrementally for append/updateLast/patch.
3) Record LOD work per frame in instrumentation.

Acceptance
- Scenario E shows no LOD rebuild spikes.
- LOD work per frame remains bounded.

Tests
- Perf scenario E + targeted unit tests for LOD updates.
