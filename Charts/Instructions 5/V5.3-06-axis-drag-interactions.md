# V5.3 Prompt 06 - Axis Drag + HandleScroll/HandleScale

Goal
- Add axis drag interactions and scroll/scale options parity.

Constraints
- Additive only.
- Overlay-only invariant must hold.

Deliverables
- Axis drag zoom on price axis.
- handleScroll/handleScale options (granular interaction control).

Steps
1) Add axis drag handling for price scale.
2) Add scroll/scale configuration flags.
3) Ensure interaction latency remains within gates.

Acceptance
- Axis drag behaves correctly with no regressions.

Tests
- Integration tests for axis drag + scroll/scale toggles.
