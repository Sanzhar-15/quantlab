# V5.3 Prompt 02 - Time Scale Options Parity

Goal
- Add full time scale option parity with LC.

Constraints
- Additive only; no regressions for existing defaults.

Deliverables
- Options: `timeVisible`, `secondsVisible`, `barSpacing`, `rightOffset`, `fitContent`, `fixLeftEdge`, `fixRightEdge`, `lockVisibleTimeRangeOnResize`, `tickMarkFormatter`.
- Updated docs and tests.

Steps
1) Extend time scale options with the parity fields.
2) Implement behavior while preserving default output.
3) Add tests for formatting and edge clamping.

Acceptance
- Time scale options behave as expected without regressions.

Tests
- Unit tests for time scale option behavior.
