# V5.5 Prompt 02 - Visual Regression Expansion

Goal
- Expand visual regression coverage to all V5 series/types and design states.

Constraints
- Visual snapshots must be deterministic.
- No baseline updates unless changes are intentional.

Deliverables
- Visual scenes for OHLC, histogram, baseline, yield/options, multi-pane, dense tooltip.
- Worker vs main parity visuals.

Steps
1) Add new visual scenes for all new series and chart types.
2) Add worker vs main rendering parity scenes.
3) Update snapshots only if changes are intended.

Acceptance
- Visual regressions cover all V5 additions.
- Worker and main rendering are visually identical.

Tests
- `npm run test:playwright -- tests/visual.spec.ts`
