# V5.3 Prompt 03 - Price Scale Options Parity

Goal
- Add full price scale option parity with LC.

Constraints
- Additive only; existing behavior must remain unchanged by default.

Deliverables
- Options: `scaleMargins`, `invertScale`, `alignLabels`, `entireTextOnly`, `minWidth`, `borderVisible`, `ticksVisible`, `priceFormat` (precision/minMove).
- Updated docs and tests.

Steps
1) Extend price scale options with parity fields.
2) Implement behavior without breaking existing visuals.
3) Add unit tests for new option behaviors.

Acceptance
- Price scale options behave correctly and remain stable under zoom.

Tests
- Unit tests for price scale option behaviors.
