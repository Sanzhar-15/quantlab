# V5.3 Prompt 09 - Chart Types: Yield Curve + Options

Goal
- Add yield curve and options chart types using the new horizontal scale abstraction.

Constraints
- Additive only; no changes to time-based charts.

Deliverables
- `createYieldCurveChart` and `createOptionsChart` (or equivalent config).
- Yield curve uses linear month spacing; options uses numeric X.

Steps
1) Implement yield curve chart type with month spacing.
2) Implement options chart type with numeric X scale.
3) Add crosshair + axis tick tests for non-time scales.
4) Update docs with usage examples.

Acceptance
- Yield curve and options charts render correctly.
- Time-based charts unaffected.

Tests
- Visual regression for both chart types.
