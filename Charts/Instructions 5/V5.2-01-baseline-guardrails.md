# V5.2 Prompt 01 - Baseline + Guardrails

Goal
- Confirm V5.1 instrumentation is stable and establish V5.2 baseline constraints.

Constraints
- No runtime behavior changes.
- No schema changes that break baseline comparisons.

Deliverables
- Verify perf harness outputs required metrics for A–F.
- Confirm input latency p95 thresholds are enforceable.

Steps
1) Run perf harness locally with `PERF_SKIP_COMPARE=1`.
2) Confirm new metrics are present (input latency + memory) without errors.
3) If any metric is missing, fix instrumentation before continuing.

Acceptance
- Perf harness outputs all required metrics for A–F.
- No baseline loading errors.

Tests
- `PERF_SKIP_COMPARE=1 npm run perf:check`
