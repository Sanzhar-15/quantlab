# V5.3 Prompt 17 - Validation + Docs

Goal
- Validate V5.3 features and update docs/examples.

Constraints
- No baseline updates unless changes are intentional and verified.

Deliverables
- Full perf run A–F with new features enabled.
- Visual regression suite updated.
- Docs updated with new APIs and examples.

Steps
1) Run perf harness A–F with new series types.
2) Update visual regression snapshots for new series.
3) Update `docs/11_current_state.md`, `docs/01_api_spec.md`, and `docs/04_v2_api_contract.md` if new public APIs were added.

Acceptance
- All gates green.
- Docs reflect new features.

Tests
- `npm run perf:check`
- `npm run test:playwright -- tests/visual.spec.ts`
