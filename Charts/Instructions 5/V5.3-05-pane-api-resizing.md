# V5.3 Prompt 05 - Pane API + Resizing

Goal
- Add pane API parity (setHeight, setStretchFactor, moveTo, preserveEmptyPane) and drag resizing.

Constraints
- Additive only; no regressions to existing pane layouts.

Deliverables
- Pane API methods + resize drag behavior.
- Docs + tests.

Steps
1) Add pane API methods and wiring.
2) Implement pane divider drag resizing (opt-in).
3) Add preserveEmptyPane behavior.

Acceptance
- Pane resizing works reliably and is stable under zoom/pan.

Tests
- Integration tests for pane ordering and resizing.
