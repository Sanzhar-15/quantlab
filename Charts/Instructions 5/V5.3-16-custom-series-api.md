# V5.3 Prompt 16 - Custom Series API

Goal
- Add a custom series renderer interface for extensibility.

Constraints
- Additive only.
- Must not regress existing series behavior.

Deliverables
- Custom series API surface.
- Example custom series in demo.

Steps
1) Define custom series renderer interface.
2) Wire rendering into the series pipeline.
3) Provide a basic example to validate integration.

Acceptance
- Custom series renders correctly and integrates with scales.

Tests
- Visual regression for custom series example.
