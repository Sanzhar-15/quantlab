# V5.2 Prompt 10 - Perf Validation + Visual Check

Goal
- Validate V5.2 improvements and update baselines if required.

Constraints
- No baseline updates unless improvements are intentional and verified.

Deliverables
- Full perf run A–F with V5.2 metrics.
- Visual regression checks for pan/zoom/streaming scenarios.

Steps
1) Run perf harness A–F with current code.
2) Compare results to V4/V5.1 baselines.
3) Update baselines only if improvements are intended and consistent.
4) Run visual regression scenes (pan cache, streaming, multi-pane).

Acceptance
- All V5.2 gates green.
- Visual checks stable.

Tests
- `npm run perf:check`
- `npm run test:playwright -- tests/visual.spec.ts`
