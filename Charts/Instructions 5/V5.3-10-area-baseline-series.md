# V5.3 Prompt 10 - Area + Baseline Series

Goal
- Implement area and baseline series (line-derived).

Constraints
- Must reuse line pipeline where possible.
- No regression to line series.
- Worker parity required or clean fallback when in worker mode.

Deliverables
- Area series with gradient fill.
- Baseline series with dual-color fill and baseline line.

Steps
1) Implement area rendering with top/bottom colors.
2) Implement baseline rendering with baseValue support.
3) Ensure crosshair markers behave consistently.
4) Ensure worker mode renders identically or falls back gracefully.

Acceptance
- Visual parity with reference defaults.
- Performance on A/B unchanged.

Tests
- Visual regression for area/baseline.
