# V5.3 Prompt 04 - Series Common Options Parity

Goal
- Add common series options parity (last value, price line, titles, animation).

Constraints
- Additive only; no regressions for existing line series.

Deliverables
- Options: `title`, `lastValueVisible`, `priceLineVisible`, `priceLineStyle`, `priceLineColor`, `priceLineSource`, optional last‑price animation.
- Docs + tests.

Steps
1) Extend series options to include parity fields.
2) Implement last value label + price line rendering.
3) Add optional last‑price animation behind a flag.

Acceptance
- Series labels and price lines match LC behavior.

Tests
- Visual regression for price line + last value label.
