# V5.3 Prompt 13 - OHLC Bar + Candlestick Rendering

Goal
- Implement OHLC bar and candlestick rendering with worker parity.

Constraints
- Must render identically in main and worker modes.
- No regressions to existing series.

Deliverables
- OHLC bar renderer.
- Candlestick renderer.
- Worker parity validated.

Steps
1) Implement OHLC bar rendering.
2) Implement candlestick rendering.
3) Validate worker vs main output consistency.

Acceptance
- Visual parity with reference in both modes.
- Performance within gates.

Tests
- Visual regression + worker/main comparison.
