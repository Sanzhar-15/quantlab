#!/bin/sh
set -e

# Quantlab: do NOT configure any external apt repositories or keys.
# Keep installation non-interactive and CI-safe.

update-desktop-database -q || true
update-mime-database /usr/share/mime || true
gtk-update-icon-cache -f -t /usr/share/icons/hicolor || true

exit 0
