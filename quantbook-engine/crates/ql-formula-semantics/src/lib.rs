//! `ql-formula-semantics` — Phase 0 stub. See ../../../.plans/_QUANTBOOK-v1-SPECIFICATION.md Part V.

#![allow(dead_code)]

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        // crate compiles; replace as Phase 0 work lands.
    }
}
