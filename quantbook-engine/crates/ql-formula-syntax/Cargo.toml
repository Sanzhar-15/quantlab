[package]
name = "ql-formula-syntax"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
ql-types  = { path = "../ql-types" }
# Phase 2A.11 audit M16: LexError + ParseError use thiserror::Error
# (consistent with QbookError / RuntimeError / LoadAndRecomputeError /
# NameTableError which already do).
thiserror = { workspace = true }
