---
name: 2026-05-24_phase-5-7-v3-6-on-pop-format-registry-rendering
status: in-progress (V3.6.0.1 lock + V3.6.0.2 D1 + V3.6.0.X audit-closure-of-V3.6.0.2 + V3.6.0.3 D2 + V3.6.0.X audit-of-D2 closures + V3.6.0.4 D3 + V3.6.0.X audit-of-D3 closures + V3.6.0.5 D4 + V3.6.0.X audit-of-D4 closures + docs-audit closures + V3.6.0.6 D5 + V3.6.0.X audit-of-D5 closures + V3.6.0.7 D6 profiling spike + V3.6.0.8.1 D6 DESIGN LOCK + V3.6.0.8.2 D6 ENGINE PART 1 + V3.6.0.8.3 D6 ENGINE PART 2 napi + **V3.6.0.8.4 audit-of-D6 + closures + bench all SHIPPED**; **V3.6.0.8 D6 ARC COMPLETE**; **V3.6.0.X audit-of-D4 closures SHIPPED at engine (this commit) + IDE (this commit) -- 4 HIGH/MED in-cycle fixes for the FAIL-verdict V3.6.0.5 D4 audit: CONVERGENT-HIGH-1 new Op::SetDateSystem variant + DateSystemWire enum + replay handler + from_qbook seed (Excel1904 round-trips); OPUS-HIGH-2 IDE data-raw-value attribute (preserves click-to-edit for currency/percent/thousands/date formats); CONVERGENT-HIGH-3 Pending skip pre-render; CONVERGENT-MED-1 workbook.locale() passthrough + MED-2 per-snapshot parsed-format cache + MED-3 doc sweep + LOW-2 drop to_string + LOW-3 dateSystem TS docstring fix.  +2 ql-collab regression tests (Op::SetDateSystem replay + unknown-wire error).  3 new R-V3.6-* risks (R-V3.6-11/12/13) documented.  Tests: ql-collab 136/136 + IDE mocha 354/354 + ql-oplog 67/67.  Predecessor V3.6.0.5 D4 ship**.  Pre this commit: V3.6.0.5 D4 SHIPPED at engine -- engine `CellSnapshotJson.rendered: Option<String>` populated via `ql_functions::format::render` (parse + render via FormatTable lookup; EvalContext with workbook.date_system + Locale::EnUs + NowProvider::System) + new `WorkbookSnapshotJson.dateSystem: String` ("Excel1900" | "Excel1904"); IDE types.ts extended + extractSheetSnapshot passes through rendered + cellGridHtml::renderRows uses `e.rendered ?? formatCellValue(e.value)` with CSP-safe escapeHtml on both paths; +4 mocha shape tests (354/354).  Pre this commit: V3.6.0.X audit-of-D3 closures SHIPPED at engine -- 4 closures: CONV-1 invalidate_cell atomic-swap (Opus HIGH-1 / Codex LOW-2 torn-write fix); CONV-2 rebuild_op_indices_only uses apply_cache_effect for tombstone parity + cell_op_index pruned on RemoveSheet (Codex MED-2 + Opus MED-1); Codex MED-1 poll_remote_with_limit partial-error rebuild; LOW-1 OpLog::get docstring corrected from O(1) to O(log N) BTree lookup.  +4 ql-collab regression tests (134/134 pass; +4 over V3.6.0.4 D3 130 baseline)**.  Pre this commit: V3.6.0.4 D3 SHIPPED at engine -- new `cell_op_index: HashMap<(u16,u32,u32), Vec<usize>>` + `sheet_op_index: HashMap<u16, Vec<usize>>` fields on CollabSession; new `CacheBuckets<'a>` struct bundling 5 mutable refs (closes Opus LOW-1 from V3.6.0.X audit-of-D2); new `OpLog::get(index)` accessor on ql-oplog; `apply_cache_effect` signature refactored to `(&mut CacheBuckets<'_>, CacheEffect, op_log_index: usize)`; new `rebuild_op_indices_only()` helper called by undo+redo BEFORE the per-cell invalidate loop to refresh indices after Loro's UndoManager retract (which COMPACTS the visible op list and shifts positional indices -- design discovery during V3.6.0.4 implementation; documented as R-V3.6-10); `invalidate_cell` rewritten to walk via cell_op_index ∪ sheet_op_index instead of full log walk; force_clear_snapshot_cache clears the new indices; new `cell_op_index_iter` + `sheet_op_index_iter` accessors; +6 ql-collab regression tests (130/130 pass; +6 over V3.6.0.X audit-of-D2 124 baseline).  Tests: ql-collab **130/130** + IDE mocha **350/350** unchanged + ql-collab-ws 40/40 + engine workspace all 30 suites pass.  Rule 4 arc terminus HELD at 6 (2 new HashMap fields + 1 struct with 5 ref fields; all positive Send+Sync via composition; 0 new triggers).  Pre this commit: V3.6.0.X audit-of-D2 SHIPPED at engine `8480f247aea` + IDE `21fe6a73ec6` -- 3 convergent findings closed in-cycle (CONVERGENT-HIGH-1 cache LWW -> first-write-wins; CONVERGENT-MED-1 formats HashMap order -> sorted by FormatId; CONVERGENT-MED-2 Builtin RegisterFormat dropped at cache walker); +5 ql-collab tests; Codex INFO-1 REFUTED undo-of-RegisterFormat staleness empirically. -- new `format_table_cache: HashMap<FormatId, Arc<str>>` field + new `CacheEffect::RegisterFormat` variant + walker integration + `force_clear_snapshot_cache` clears the cache + atomic-swap in `rebuild_snapshot_cache`; `invalidate_cell` uses throwaway local cache (RegisterFormat effects are session-wide, never per-cell-keyed); new `FormatDefJson` napi struct + `WorkbookSnapshotJson.formats: Vec<FormatDefJson>` additive field populated from `Workbook.formats().iter()` (authoritative; merges Builtin 0..=163 + Custom); IDE types.ts updated + +4 IDE mocha tests pinning the new shape + 1 stale V3.5.0.2 shape test updated; +2 ql-collab regression tests (`register_format_op_populates_session_format_table_cache` + `register_format_cache_rebuilds_from_log_via_rebuild_snapshot_cache`).  Tests: ql-collab **119/119** (117 baseline + 2 new); IDE mocha **350/350** (346 + 4 new).  Cross-peer divergence between cache (LWW-by-iteration) + Workbook (rejects same-id-different-string) documented as known limitation -- napi reads from Workbook (authoritative) so end-users don't see divergence; V3.7+ could mirror rejection in walker.  Rule 4 arc terminus HELD at 6 (1 new ql-collab field + 1 new napi struct; both positive Send+Sync via composition; 0 new triggers).  V3.6.0.X audit (Codex Lane A + Opus Lane B parallel) for V3.6.0.2 closed 1 convergent HIGH in-cycle.  V3.6.0.X audit transcript: `docs/audits/2026-05-24-phase-5-7-v3-6-0-2-codex.md` (Codex Lane A; FAIL verdict; 1 HIGH + 1 LOW + 2 INFO).  Convergent HIGH: grouped-undo + merge-interval-undo loses cells because Loro's `push_with_merge` (loro-internal-1.12.0/src/undo.rs:389-396) DISCARDS subsequent meta when merging spans; only the FIRST push's meta survives.  Codex Lane A demonstrated empirically with `codex_tmp_grouped_undo_cache_probe` (failed: 4 stale cells remained); Opus Lane B independently verified by reading Loro source.  **Closure**: new `inside_group: bool` + `undo_merge_interval_ms: i64` fields on CollabSession; `start_undo_group` / `end_undo_group` / `set_undo_merge_interval` maintain these; `discard_pending_ops` clears inside_group + reapplies merge_interval to recreated UndoManager; `append_op` gates partial-invalidate behind `!inside_group && undo_merge_interval_ms == 0` (stages empty cells when merge-prone -> undo decodes empty -> falls back to full `rebuild_snapshot_cache`).  Conservative fallback matches V3.5.0.6 semantics for grouped undo; clean fix (`set_top_undo_meta`) blocked because the API isn't exposed on public `loro::UndoManager`.  +2 ql-collab regression tests: `grouped_undo_clears_cache_for_all_cells_in_the_group` + `merge_interval_undo_clears_cache_for_all_cells_in_window`.  Tests: ql-collab **117/117** (115/115 baseline + 2 new); IDE mocha 346/346 unchanged.  Rule 4 arc terminus HELD at 6 (2 new fields: `inside_group: bool` Copy+Send+Sync trivial; `undo_merge_interval_ms: i64` Copy+Send+Sync trivial; 0 new triggers).  Deferred to V3.6.0.3+ backlog: CODEX-LOW-1 coverage expansion (poll_remote_with_limit interleave around undo/redo + discard_pending_ops + undo/redo + remote-interrupted undo/redo chains + mixed BatchCommit actual-undo fallback + codec malformed/round-trip).  Lane B INFO findings deferred: undo() Err early-return leaves Mutex set (benign); on_push closure ignores DiffEvent (defensive polish).  Lane B re-ran ql-collab-ws relay tests + IDE mocha in main environment (Codex sandbox-blocked); both green.  V3.6.0.4+ (D3 per-cell op-index) OR V3.6.0.X audit of D2 (parallel Codex+Opus) pending fresh session per CLAUDE.md cycle discipline; this session counted 4 cycles (V3.6.0.1 lock + V3.6.0.2 implementation + V3.6.0.X audit-closure + V3.6.0.3 D2 ship), 2 over the CLAUDE.md "Never run >2 plan-implement-audit cycles in one session" guideline.  Deliberate exception acknowledged; user explicitly directed each cycle.)
date: 2026-05-24
predecessor_plan: .plans/_archive/2026-05-24_phase-5-7-v3-5-workbook-snapshot-sheet-ops-undo-format.md (V3.5 phase termination -- WorkbookSnapshot + sheet ops + format extension + partial-invalidate undo + mid-edit-render guard; ALL 11 sub-steps SHIPPED + AUDITED + FOLLOW-UP-AUDITED including V3.5.0.X cumulative megaudit + 5 HIGH + 1 valid MED + 1 LOW closed in-cycle + follow-up audit shipping 3 MED + 2 partial LOW closures)
opus_v3_5_packet: docs/audits/2026-05-24-phase-5-7-v3-5-0-x-opus.md § F (V3.6 ENTRY READINESS PACKET; F.1 - F.9 + § G sheet-tabs deferral analysis + § H.5 recommended sequencing)
parent_phase: 5.7 Collaboration IDE Vertical Slice
direction: V3.6 -- Loro UndoManager on_pop callback wiring (closes V3.5.0.X conservative pure_local_frontier gate properly via the upstream API) + session-wide RegisterFormat FormatTable cache (unblocks format-aware buildHtml rendering) + per-cell op-index (closes V3.5.0.6 R-V3.5-2 perf gap) + format-aware buildHtml rendering (number/date/currency display per V3.5.0.5 format passthrough) + IDE-facing appendPutFormula napi (unblocks end-to-end repaired-formula integration tests + format-UI write-path). The first Phase 5.7 sub-phase that ELIMINATES a V3.5 conservative-correctness gate (the pure_local_frontier proxy is replaced by Loro's actual undo-pop callback). All V3.5 work shipped + audited; V3.5.1+ polish backlog parked.
current_engine_head: (this commit, V3.6.0.8.3 D6 ENGINE PART 2 napi: workbookSnapshotDelta napi method + WorkbookSnapshotDeltaJson struct + ChangedCellJson + RemovedCellJson helpers + last_snapshot_op_count field added to V3.6.0.8.2 cache invariants + cache populate in workbookSnapshot napi + new oplog_vv + log accessors on CollabSession + ql_oplog::VersionVector re-export + types.ts WorkbookSnapshotDeltaJson interface + workbookSnapshotDelta CollabSessionInstance method + 5 mocha shape tests + plan + MASTER-PLAN + ide-consumer-contract sweep) <- dc74d48b97e (V3.6.0.8.2 D6 ENGINE PART 1) <- ebe7c946429 (V3.6.0.8.1 D6 DESIGN LOCK docs-only) <- 3e1e4deff50 (V3.6.0.7 D6 profiling spike) <- prior chain
current_ide_head: (this commit, V3.6.0.8.3 D6 ENGINE PART 2 IDE: types.ts WorkbookSnapshotDeltaJson interface + workbookSnapshotDelta method + 5 mocha shape tests) <- d9731633ac4 (V3.6.0.X audit-of-D5 closures IDE; V3.6.0.7 + V3.6.0.8.1 + V3.6.0.8.2 are engine-only) <- 7608ae7633f (V3.6.0.6 D5 IDE) <- prior chain
current_mocha_count: 390 / 390 (V3.6.0.8.4 unchanged from V3.6.0.8.3 +6.  2 shape-pinning tests updated to include the new `version` field; net delta 0.)
current_ql_collab_tests: 149 / 149 (V3.6.0.8.4 unchanged; V3.6.0.8.2 +13 over V3.6.0.X audit-of-D5 136 baseline: 8 workbook cache invariants + 4 apply_ops_in_range correctness + 1 from_snapshot cache discipline.  V3.6.0.8.4 audit-of-D6 closures + bench did not add new ql-collab tests; mocha comprehensive coverage deferred to V3.6.0.8.5.)
current_ql_collab_ws_tests: 42 / 42 (10 lib + 30 transport + 2 V3.1.a relay; +2 doctests separate)
current_engine_workspace: 99 test-result lines pass via `cargo test --workspace --release --features test-fixtures` (V3.6.0.X audit-of-D4 baseline as of engine `e7931a74d0d`)
audit_rules_inherited:
  - Rule 1: Stop fresh-session reminders (2026-05-17)
  - Rule 2: Parallel Codex+Opus per phase/wave/step (2026-05-17)
  - Rule 3: Range-aware fn ships need lex+parse+bind+eval coverage (2026-05-21; not V3.6-relevant; pre-V3.5)
  - Rule 4: Negative trait claims need positive compile proof OR per-field walk (2026-05-21; arc terminus 6 at V3.5 end; V3.6 expects to add new fields on CollabSession + new napi structs)
loro_version: =1.12.0 (verified V3.6.0.1 entry; UndoManager API exists per `loro_internal::undo::OnPop = Box<dyn Fn(UndoOrRedo, CounterSpan, UndoItemMeta) + Send + Sync>` and `loro::UndoManager::set_on_pop(Option<OnPop>)` at lib.rs:3790)
type: project
---

# V3.6 -- UndoManager on_pop + format registry + format-aware rendering + per-cell op-index + IDE PutFormula (Phase 5.7, 2026-05-24)

> **NEW SESSION? START HERE.**

Sister-section to `docs/architecture/ide-consumer-contract.md § 4.1.z6` (V3.6.0.X docs will land it; mirrors V3.5.0.9 § 4.1.z5 template). This plan is the engine-side scoping document.

## Predecessor closure

V3.5 SHIPPED + AUDITED + FOLLOW-UP-AUDITED + ARCHIVED end-to-end across 2 sessions; all 11 sub-steps complete; 31 audit transcripts cumulative across Phase 5.7 (+2 V3.5.0.X Codex/Opus + 1 V3.5.0.X follow-up Codex). See `.plans/_archive/2026-05-24_phase-5-7-v3-5-workbook-snapshot-sheet-ops-undo-format.md` for the V3.5 plan archive; `docs/audits/2026-05-24-phase-5-7-v3-5-0-x-{codex,opus}.md` for the V3.5.0.X cumulative megaudit transcripts; `docs/audits/2026-05-24-phase-5-7-v3-5-0-x-closure-codex.md` for the V3.5.0.X follow-up audit transcript; `docs/architecture/ide-consumer-contract.md § 4.1.z5` for the V3.5 surface spec + audit closures sub-section.

V3.5 cumulative test deltas: ql-collab 81 -> **115** (+34); IDE mocha 254 -> **346** (+92).

V3.5 closure highlights (rationale for V3.6 priorities):
- V3.5.0.X conservative `pure_local_frontier` gate on undo/redo dispatch falls back to full `rebuild_snapshot_cache` whenever a remote op interleaves between local appends. CORRECT but CONSERVATIVE; partial-invalidate is BYPASSED in the remote-interleave case. The proper fix uses Loro's `UndoManager::set_on_pop` API to surface the actual retracted op shape directly -- this is V3.6 D1.
- V3.5.0.6 partial-invalidate `invalidate_cell` walks the full op log per call (O(N) where N = op log size). Acceptable at V3.5 scale; closes R-V3.5-2 via V3.6 D3 per-cell op-index.
- V3.5.0.5 per-cell `format: Option<FormatId>` passthrough lands the wire format but NO IDE consumer renders format-aware (number/date/currency display). V3.6 D2 + D4 close this.
- V3.5.0.X A-HIGH-2 closure landed `workbookSnapshot` reading from `rebuild_workbook` `formula_at` (repaired formulas surface). Verified only by Rust ql-collab test today because IDE has no PutFormula write path. V3.6 D5 closes this.

## V3.6 scope (per V3.5.0.X Opus § F V3.6 ENTRY READINESS)

V3.6 closes 4 V3.5 architectural deferrals + 1 V3.5 conservative-correctness gate via 5 priority sub-steps, plus 4 conditional sub-steps:

1. **Loro UndoManager on_pop callback wiring** (D1, F.4): replaces V3.5.0.6's peek-most-recent-visible-op heuristic + V3.5.0.X conservative `pure_local_frontier` gate. Captures the actual retracted op's CounterSpan + affected cells via Loro's `set_on_push` (encodes cells into `UndoItemMeta::value`) + `set_on_pop` (reads them back). Fires per-cell invalidation regardless of iteration order. **HIGH PRIORITY** -- closes a current correctness debt.

2. **Session-wide RegisterFormat FormatTable cache** (D2, F.1): new `format_table_cache: HashMap<FormatId, String>` on `CollabSession`. New `CacheEffect::RegisterFormat { id, string }` variant. `WorkbookSnapshotJson` gains `formats: Vec<FormatDefJson>` top-level field (additive). Required for D4 format-aware rendering.

3. **Per-cell op-index for invalidate_cell** (D3, F.3): new `cell_op_index: HashMap<(SheetId, RowId, ColId), Vec<usize>>` on `CollabSession`. Maintained incrementally in `append_op` per emitted CacheEffect's key; rebuilt in `from_snapshot` + `merge_bytes`. Makes V3.5.0.6 `invalidate_cell` O(ops-for-this-cell) instead of O(N).

4. **Format-aware buildHtml rendering** (D4, F.2): engine-side `format_value(value, format_string) -> String` API (already exists at `ql-storage::format::FormatTable::render_value`). `CellSnapshotJson` gains `rendered: Option<String>` additive field. IDE `buildHtml` uses `rendered` if present, falls back to value-based default.

5. **IDE-facing appendPutFormula napi** (D5, NEW): wraps existing `Op::PutFormula` for IDE consumption. Unblocks end-to-end repaired-formula integration tests (V3.5.0.X A-HIGH-2 has Rust regression test only) + format-UI write-path. Mirrors V3.4.0.2 `appendPutValue` pattern.

Conditional sub-steps (ship if surfaced):

6. **Incremental WorkbookSnapshot deltas** (D6, F.5): new `workbookSnapshotDelta(last_seen_version)` napi. Closes R-V3.5-1 per-call O(N) cost. DEFER if profiling doesn't justify at V3.6-scale.

7. **#REF! substitution for cross-sheet refs to deleted sheets** (D7, F.7): extends `repair_sheet_rename_chain` into `repair_sheet_chain` handling rename + delete. USER-VISIBLE; ship early IF user signals "deleted sheet but formulas didn't update".

8. **Op::RestoreSheet un-delete** (D8, F.8): conditional on user-facing flow. Skip if no user signal.

9. **Sheet-tabs UI strategy + mid-edit-render guard typing-stroke watchdog** (D9, F.6 / G + F.9): per § G recommendation, DEFER standalone V3.5.0.4c strip TO V3.6+ multi-tab redesign (avoid teaching one UX then changing it). Mid-edit-render guard typing-stroke watchdog refresh (any keystroke from webview resets the 30s deadline) is 0.5 session if surfaced.

## V3.6 design decisions (locked at V3.6.0.1)

### D1 Loro UndoManager on_pop callback wiring (F.4) -- V3.6.0.2 (HIGH PRIORITY)

**Question:** how does V3.6 close the V3.5.0.X conservative `pure_local_frontier` gate so partial-invalidate fires correctly in the remote-interleave case?

**V3.5.0.X conservative gate (the debt being repaid):** when `merge_bytes` / `poll_remote_with_limit` reset `pure_local_frontier = false`, the V3.5.0.6 undo/redo dispatch falls back to full `rebuild_snapshot_cache` regardless of op shape. The reason: V3.5.0.6 partial-invalidate captures `self.log.iter().last()` as a PROXY for the to-be-retracted op; that proxy is the REMOTE op (not the local op being undone) in the remote-trails-local iteration order case (Codex A-HIGH-1's native-binding repro). The conservative gate works but BYPASSES partial-invalidate in the common collab scenario.

**Loro 1.12.0 API (verified at V3.6.0.1 entry):**
- `loro_internal::undo::OnPop = Box<dyn Fn(UndoOrRedo, CounterSpan, UndoItemMeta) + Send + Sync>` (registry/loro-internal-1.12.0/src/undo.rs:199)
- `loro_internal::undo::OnPush = Box<dyn for<'a> Fn(UndoOrRedo, CounterSpan, Option<DiffEvent<'a>>) -> UndoItemMeta + Send + Sync>` (registry/loro-internal-1.12.0/src/undo.rs:196-198)
- `loro::UndoManager::set_on_pop(&mut self, on_pop: Option<OnPop>)` (registry/loro-1.12.0/src/lib.rs:3790)
- `loro::UndoManager::set_on_push(&mut self, on_push: Option<OnPush>)` (registry/loro-1.12.0/src/lib.rs:3778)
- `loro_internal::undo::UndoItemMeta { value: LoroValue, cursors: Vec<CursorWithPos> }` (registry/loro-internal-1.12.0/src/undo.rs:264-267)

**Options:**
- (a) **on_push + on_pop pair**: at push time, inspect the `DiffEvent` to extract affected cell coordinates from the just-pushed op; encode them as a `LoroValue` map into `UndoItemMeta::value`. At pop time, read `meta.value` back, decode the cells, fire per-cell `invalidate_cell` for each.
- (b) **on_pop only with CounterSpan range walk**: at pop time, use the `CounterSpan` to walk the session's op log subset in that range. Use `collect_cache_effects` to extract affected cells. Requires mapping Loro counter -> session.log index (Loro counters are per-peer; session.log is per-session).
- (c) **Keep V3.5.0.X conservative gate + add on_pop as defense-in-depth**: layered approach. on_pop fires partial-invalidate when possible; if it errors, falls back to full rebuild.

**Tradeoffs:**
- (a) is the CLEAN architectural fix. Cells captured at push time = most accurate state. on_pop is a simple lookup. The `LoroValue::Map` encoding cost is bounded (typically 1-4 cells per cell-keyed op).
- (b) avoids the on_push intercept cost but requires Loro counter->log-index mapping which isn't a public API. Risky.
- (c) keeps the conservative fallback as safety net. Slightly more code; trades architectural purity for defense-in-depth.

**Decision: A.** Use BOTH `set_on_push` + `set_on_pop`. on_push extracts affected cells from `DiffEvent` and encodes them as `LoroValue::Map { keys: ["cells"], values: [Container::List([Map { keys: ["sheet", "row", "col"], values: [u16, u32, u32] }, ...])] }` into `UndoItemMeta::value`. on_pop decodes the LoroValue back to `Vec<(u16, u32, u32)>` and fires `invalidate_cell` for each. The V3.5.0.X `pure_local_frontier` field + dispatch gate are REMOVED in V3.6.0.2 (no longer needed; on_pop gives accurate cells regardless of iteration order).

**Rule 4 trigger:** YES. Loro callback closures wrap `&mut self` access via interior mutability (likely `Arc<Mutex<_>>` or `Arc<RwLock<_>>` since OnPop is `Send + Sync`). The closure design must use a thread-safe handle (e.g., `Arc<Mutex<Vec<(u16, u32, u32)>>>` for pending invalidations) since Loro internals may invoke the callback from any thread. Per-field walk at V3.6.0.2 entry must positively prove the chosen container is Send + Sync. The `pure_local_frontier: bool` field is REMOVED in V3.6.0.2 (negative trait change, but eliminating a field; arc terminus stays at 6).

**Risk:** Loro's callback may fire DURING an `undo()` call (re-entrant lock concern). The closure CANNOT lock the same Mutex the outer `undo()` holds. Solution: use a `Mutex<Vec<Cells>>` separate from any session-wide lock; on_pop pushes to it; the outer `undo()` drains it after Loro's undo completes. Verify at V3.6.0.2 implementation. (Note: V3.5.0.6 `invalidate_cell` is itself called outside any Loro lock, so the drain-then-invalidate pattern is sound.)

**Test coverage:** mocha + ql-collab regression for the V3.5.0.X conservative-gate scenarios -- post-V3.6.0.2, these tests verify partial-invalidate FIRES correctly (not full-rebuild) after remote-interleave undo. The V3.5.0.X regression tests `undo_after_remote_interleave_falls_back_to_full_rebuild` + `redo_after_remote_interleave_falls_back_to_full_rebuild` MUST be REWRITTEN to verify partial-invalidate fires + produces the correct cache.

**V3.6.0.X sub-step suggestion:** V3.6.0.2.

### D2 Session-wide RegisterFormat FormatTable cache (F.1) -- V3.6.0.3

**Question:** how does V3.6 surface registered format strings to the IDE so D4 format-aware rendering can interpret them?

**Options:**
- (a) **Session-wide cache** `format_table_cache: HashMap<FormatId, String>` on `CollabSession`. New `CacheEffect::RegisterFormat { id, string }` variant. `collect_cache_effects` emits the effect on `Op::RegisterFormat`. `apply_cache_effect` inserts into the cache. `WorkbookSnapshotJson` gains `formats: Vec<FormatDefJson>` top-level field (additive). napi method `workbook_snapshot` iterates the cache + emits the list.
- (b) **Read from Workbook on-demand**: skip the cache; `workbook_snapshot` reads `Workbook::formats` (Phase 4.6 / D-1 FormatTable) at every call. Simpler but pays the O(N) FormatTable iteration cost per snapshot.
- (c) **Hybrid**: cache the dense format ids (Builtin variants) on the session; iterate Workbook for Custom variants per snapshot.

**Tradeoffs:**
- (a) matches V3.4.0.2's session-wide cache pattern. O(1) per snapshot. Adds 1 mutation site (Op::RegisterFormat in collect_cache_effects).
- (b) is the minimal-change approach. Works at V3.6 scale; O(N_formats) per snapshot where N_formats typically < 100. Negligible cost.
- (c) is over-engineering.

**Decision: A.** Session-wide cache. Mirrors V3.4.0.2 + V3.5.0.5 cache discipline. Concrete shape:
```rust
// CollabSession
format_table_cache: HashMap<FormatId, Arc<str>>,

// CacheEffect (new variant)
RegisterFormat { id: FormatId, string: Arc<str> },

// WorkbookSnapshotJson (new field, additive -- preserves V3.5 IDE consumers)
formats: Vec<FormatDefJson>,

// FormatDefJson (new #[napi(object)])
pub struct FormatDefJson {
    pub id: FormatIdJson,  // existing V3.5.0.5 wire shape
    pub string: String,    // format string (e.g., "0.00%", "yyyy-mm-dd")
}
```

**Cross-peer convergence:** concurrent `Op::RegisterFormat { id: SAME, string: DIFFERENT }` on the same id is the cross-peer collision case. Per Loro CRDT semantic + Phase 4.6 D-1 `register_at` impl: the engine rejects same-id-different-string registrations with a `FormatRejected` replay error. V3.6 ADOPTS this contract (engine rejects; cache walker treats the second registration as a no-op). Alternative LWW-overwrite was REJECTED because the engine already has the rejection semantic from D-1.

**Rule 4 trigger:** YES. New `format_table_cache: HashMap<FormatId, Arc<str>>` field + new `CacheEffect::RegisterFormat` variant + new `FormatDefJson` napi struct. All three: positive Send+Sync per-field walks at V3.6.0.3 implementation. `HashMap<FormatId, Arc<str>>` is Send + Sync (FormatId composition of primitives + Arc<str> is Send + Sync). `CacheEffect::RegisterFormat` variant fields: FormatId + Arc<str>; both Send + Sync. `FormatDefJson` composition of FormatIdJson (V3.5.0.5 walk) + String; both Send + Sync. **+0 new triggers; arc terminus stays at 6.**

**V3.6.0.X sub-step suggestion:** V3.6.0.3 (after D1 ships).

### D3 Per-cell op-index for invalidate_cell (F.3) -- V3.6.0.4

**Question:** how does V3.6 close R-V3.5-2 (V3.5.0.6 invalidate_cell walks the full op log O(N) per call)?

**Decision: NEW `cell_op_index: HashMap<(SheetId, RowId, ColId), Vec<usize>>` on `CollabSession`.** Maps cell coordinate to a list of op-log indices that emit a CacheEffect on that cell. Maintained incrementally in `append_op` per emitted CacheEffect's key (extracted via `Self::affected_cells_for_partial_invalidate`-style classifier extended to all CacheEffect variants). Rebuilt during `from_snapshot` + `merge_bytes` + `discard_pending_ops` (full walk -- O(N) but only once per merge; mirrors the cache full-rebuild cost).

**invalidate_cell signature** after V3.6.0.4: walks `self.cell_op_index.get(&(s, r, c)).unwrap_or(&Vec::new()).iter().map(|idx| self.log.get(*idx))` instead of `self.log.iter().filter(|op| affects(op, s, r, c))`. O(ops-for-this-cell) typical = 1-3.

**Invariant:** every op-log mutation site MUST update `cell_op_index` in sync with `last_snapshot`. The 6 mutation sites (per V3.4.0.2 docstring discipline): `append_op`, `from_snapshot` (rebuild), `merge_bytes` (rebuild), `discard_pending_ops` (rebuild), `undo` (V3.6.0.2 on_pop drives invalidate_cell which reads but doesn't mutate the index), `redo` (same as undo).

**Debug-assert** at the end of each mutation site checking that `cell_op_index.values().flatten().count() == sum of per-cell-keyed-op cells` (a coarse sync check). Stress-tested at V3.6.0.4 implementation.

**Rule 4 trigger:** YES. New `cell_op_index: HashMap<(SheetId, RowId, ColId), Vec<usize>>` field. Positive Send+Sync per-field walk: HashMap is Send + Sync (std inherent impl); (u16, u32, u32) is Copy + Send + Sync; Vec<usize> is Send + Sync. **+0 new triggers; arc terminus stays at 6.**

**Risk:** sync invariant violation. Any new op-log mutation site that forgets the cell_op_index update silently produces stale invalidate_cell behavior. Mitigate via the "5-6 mutation sites" docstring discipline + the debug-assert in each site.

**V3.6.0.X sub-step suggestion:** V3.6.0.4 (after D1 + D2; D1 needs invalidate_cell which D3 makes O(k) instead of O(N)).

### D4 Format-aware buildHtml rendering (F.2) -- V3.6.0.5

**Question:** how does V3.6 render formatted cell values in the IDE (number/date/currency/scientific per CellState.format)?

**Decision:** Engine-side `format_value(value: &CellWireValue, format_string: &str, date_system: DateSystem) -> Option<String>` reuses `ql-storage::format::FormatTable::render_value` (Phase 4.6 D-1; existing for xlsx export). Wired into napi `workbook_snapshot` via a new `rendered: Option<String>` field on `CellSnapshotJson` (additive). IDE `buildHtml` uses `rendered` if present, falls back to existing value-based default (V3.2.a).

**Locale:** V3.6 ships LOCALE-AGNOSTIC ("en-US" defaults: `.` decimal separator, `$` currency symbol). Locale-aware rendering DEFERRED to V3.6.1+.

**Date system:** `Workbook::date_system` (Excel1900 vs Excel1904) affects serial-to-date conversion. Pass via `WorkbookSnapshotJson::date_system: DateSystemJson` top-level field (new; additive).

**Pre-render at engine vs JS-side:** ENGINE side. CSP-safe (no JS eval); reuses existing Rust format machinery; avoids duplicate logic.

**Rule 4 trigger:** YES. New `DateSystemJson` (likely an enum `#[napi(string_enum)]` -- verify napi-rs supports), new `rendered: Option<String>` field on existing `CellSnapshotJson`. Both: positive Send+Sync walks at V3.6.0.5 implementation. **+0 new triggers; arc terminus stays at 6.**

**V3.6.0.X sub-step suggestion:** V3.6.0.5 (after D2 ships).

### D5 IDE-facing appendPutFormula napi (NEW V3.6 candidate) -- V3.6.0.6

**Question:** how does V3.6 unblock end-to-end repaired-formula integration tests + format-UI write-path?

**Background:** V3.5.0.X A-HIGH-2 closure landed `workbookSnapshot` reading from `rebuild_workbook` `formula_at` (so renamed sheets surface repaired formula text). The closure is verified ONLY by a ql-collab Rust regression test (`rebuilt_workbook_carries_repaired_formula_but_cache_does_not`) because the IDE has NO PutFormula write-path -- there's no way to write a formula from the IDE and verify the IDE-level snapshot surfaces the repaired text. V3.5.0.X documented this as deferred to V3.6+.

**Decision:** wrap existing `Op::PutFormula` (engine: shipping since Phase 4.6.x) with a thin napi wrapper. Mirrors V3.4.0.2 `appendPutValue` pattern:
```rust
#[napi]
impl CollabSession {
    pub fn append_put_formula(
        &mut self,
        sheet: u32,
        row: u32,
        col: u32,
        text: String,
    ) -> Result<()> {
        // validate sheet <= u16::MAX, append Op::PutFormula{sheet, row, col, text}
    }
}
```

IDE typed wrapper:
```ts
export function putFormula(session: CollabSessionInstance, sheet: number, row: number, col: number, text: string): void {
    session.appendPutFormula(sheet, row, col, text);
}
```

**Scope:** PURELY an FFI/IDE wrapper. No new Op variant. No CRDT semantic locks. No new wire format.

**Test coverage:** new IDE mocha suite for the A-HIGH-2 end-to-end scenario: open .qbook with sheet "S"; PutFormula referencing S; rename S to "Renamed"; verify workbookSnapshot's formula text is "Renamed!A1" (repaired). +6-8 mocha tests.

**Rule 4 trigger:** NONE. No new structs/fields/variants on the engine side; the napi wrapper is a method.

**V3.6.0.X sub-step suggestion:** V3.6.0.6 (independent of D1-D4; can ship in parallel if priorities shift).

### D6 Incremental WorkbookSnapshot deltas (F.5) -- V3.6.0.7 SHIPPED + D6 SHIPS V3.6.0.8

**Question (resolved):** does V3.6 ship incremental snapshot deltas, or defer per "profiling at V3.6 scale doesn't justify"?

**Per Opus § F.5:** R-V3.5-1 cost is per-call O(N); incremental deltas would be O(delta). Loro has version-vector machinery to derive deltas natively.

**Decision: SHIP D6 in V3.6.0.8.** V3.6.0.7 ran the time-boxed 1-session profiling spike at engine (this commit), 2026-05-25.  Criterion bench at `crates/ql-bindings-node/benches/workbook_snapshot.rs` measured post-D2 + post-D4 cost across 12 workload combinations.

**Decision combo result:** `cells=100 000, format=50 %, sheets=1` median = **251.38 ms** ≈ 5× the 50 ms threshold.  Even the no-format `cells=100 000` case is 152.40 ms (3× the threshold).  Threshold breakpoint lies between 10k cells (25 ms) and 50k cells (127 ms).  Scaling is linear-to-slightly-superlinear in cell count.  Full results: `docs/audits/2026-05-25-phase-5-7-v3-6-0-7-spike.md`.

**V3.6.0.8 implementation lock:**
- `workbookSnapshotDelta(last_seen_version: Vec<u8>) -> Result<WorkbookSnapshotDeltaJson>` napi.
- `WorkbookSnapshotDeltaJson { changed_cells: Vec<CellSnapshotJson>, removed_cells: Vec<(u16, u32, u32)>, sheets_changed: Vec<SheetSnapshotJson>, version: Vec<u8> }` (additive; existing `workbookSnapshot` retained for full-rebuild fallback).
- R-V3.6-5 (delta freshness): fall back to full snapshot when Loro version-vector predecessor is unreachable.
- R-V3.6-NEW (rebuild_workbook dominant cost, discovered at V3.6.0.7 spike): D6 must either (a) cache the prior rebuilt Workbook + diff forward, (b) read from the V3.4.0.2 incremental snapshot cache + skip rebuild_workbook entirely (works only if no rename-repair changed since last call), or (c) something else.  Locked at D6 ship.

**V3.6.0.X sub-step suggestion:** V3.6.0.8 D6 ship (~3 sessions); V3.6.0.X audit-of-D6 (~1 session).

### D7 #REF! substitution for cross-sheet refs to deleted sheets (F.7) -- V3.6.0.8 IF SURFACED

**Question:** does V3.6 extend repair_sheet_rename_chain to repair_sheet_chain handling rename + delete?

**Decision: DEFER to user feedback signal.** Ship V3.6.0.8 IF V3.5 user feedback surfaces "I deleted sheet X but formulas referencing X didn't update" reports. Per Opus § F.7 USER-VISIBLE; 1-2 sessions. Engine-side implementation: extend `repair_sheet_rename_chain` (Phase 5.3 step 3) at `rebuild_workbook` time. For each `Op::RemoveSheet { id }` in the log, walk all formula text in `Workbook` and rewrite cross-sheet refs to the tombstoned sheet's name with `#REF!`.

**Wire format:** UNCHANGED. Formula text mutation is rebuild-time only; the original `=Sheet3!A1` text stays in the cells unless rebuild materializes the replacement.

**Test coverage:** +3-5 ql-collab regression tests (cross-sheet ref before/after delete; cross-peer convergence; round-trip via .qbook).

**Rule 4 trigger:** NONE.

**V3.6.0.X sub-step suggestion:** V3.6.0.8 IF surfaced.

### D8 Op::RestoreSheet un-delete (F.8) -- V3.6.0.9 IF SURFACED

**Question:** does V3.6 ship `Op::RestoreSheet { id }` to un-delete a tombstoned sheet?

**Decision: DEFER to user feedback signal.** Per Opus § F.8 CONDITIONAL on user-facing flow. If V3.5 surfaces "I deleted by accident; how do I restore?" reports beyond what undo covers, ship V3.6.0.9.

**Implementation if shipped:** new wire variant `Op::RestoreSheet { id: SheetId }` -> `removed_sheets.remove(&id)`. Cross-peer idempotent (HashSet remove on absent is no-op). Cell storage already preserved by V3.5.0.3b tombstone semantic so restore is feasible (no recovery needed).

**Forward-compat:** pre-V3.6 binaries reading post-V3.6 .qbook files with Op::RestoreSheet fail to deserialize (same as Op::RemoveSheet / MoveSheet at V3.5). Documented per the V3.5 forward-compat caveat pattern.

**Rule 4 trigger:** NONE.

**V3.6.0.X sub-step suggestion:** V3.6.0.9 IF surfaced.

### D9 Sheet-tabs UX strategy + mid-edit-render guard typing-stroke watchdog (F.6 / G + F.9)

**Question:** does V3.6 ship the V3.5.0.4c sheet-tabs UI scaffold (deferred at V3.5) OR multi-tab redesign OR mid-edit-render guard typing-stroke watchdog?

**Per Opus § G analysis:** DEFER standalone V3.5.0.4c sheet-tabs strip TO V3.6+ multi-tab redesign. Don't ship standalone + then re-teach UX in multi-tab redesign. Caveat: if V3.5 user feedback surfaces "Command Palette too slow for sheet switching", V3.5.1 can ship the standalone strip as a transitional polish.

**Per Opus § F.9:** mid-edit-render guard typing-stroke watchdog (any keystroke from webview resets the 30s deadline) is 0.5 session if surfaced.

**Decision:**
- **Sheet-tabs UI:** DEFER. V3.6+ multi-tab redesign considered IF user signal surfaces. V3.5.0.4a Command Palette + V3.5.0.4b reactive title are sufficient for V3.6 scope.
- **Mid-edit-render guard typing-stroke watchdog:** SHIP V3.6.0.10 IF surfaced. New postMessage type `typing_stroke` from webview + setTimeout reset in `setPresenceTyping`. Closes R-V3.5-7 "long formula entry hits 30s" case.

**Rule 4 trigger:** NONE for the typing-stroke watchdog (TS-only). NONE for sheet-tabs deferral.

**V3.6.0.X sub-step suggestion:** V3.6.0.10 IF surfaced (typing-stroke watchdog only).

## V3.6 sub-step rollout (planned -- order may shift based on V3.6.0.2 implementation discoveries)

1. **V3.6.0.1 -- decision lock** ✅ SHIPPED at engine `4fd816ded43` (docs-only).  Decisions D1-D9 above.  ~1 session.
2. **V3.6.0.2 -- Loro UndoManager on_push callback wiring (D1)** ✅ SHIPPED at engine `89f5a5f9e0b`.  Engine code + 2 rewritten V3.5.0.X regression tests verifying partial-invalidate FIRES correctly under remote-interleave (not full-rebuild fallback).  REMOVED `pure_local_frontier` field + dispatch gate; ADDED `pending_undo_cells: Arc<Mutex<Option<Vec<(u16, u32, u32)>>>>` + on_push closure encoding cells as `LoroValue::List<List<I64>>`.  undo/redo use `top_undo_meta`/`top_redo_meta` + pre-stage cells for the synthetic inverse op's on_push.  2 sessions estimated; shipped in 1.
2b. **V3.6.0.X -- Codex Lane A + Opus Lane B audit of V3.6.0.2** ✅ SHIPPED at engine `08964962cf8`.  Codex transcript: `docs/audits/2026-05-24-phase-5-7-v3-6-0-2-codex.md` (FAIL; 1 HIGH + 1 LOW + 2 INFO).  Convergent HIGH closed in-cycle: grouped-undo + merge-interval-undo lose cells (Loro's `push_with_merge` discards subsequent meta).  Closure: new `inside_group: bool` + `undo_merge_interval_ms: i64` fields; conservative full-rebuild fallback when merge-prone.  +2 ql-collab regression tests.  CODEX-LOW-1 (coverage expansion) deferred to V3.6.0.3+ backlog.  Clean fix (`set_top_undo_meta` to union cells) blocked: API exists on `loro_internal` but not on public `loro::UndoManager`; deferred to V3.6+ pending upstream contribution.
3. **V3.6.0.3 -- Session-wide RegisterFormat FormatTable cache (D2)** ✅ SHIPPED at engine `ffa598a1c15` + IDE `b159c899359`.  New `format_table_cache: HashMap<FormatId, Arc<str>>` field + new `CacheEffect::RegisterFormat` variant + walker integration + new `FormatDefJson` napi struct + `WorkbookSnapshotJson.formats: Vec<FormatDefJson>` additive field populated from `Workbook.formats().iter()` (authoritative).  IDE types.ts updated + +4 IDE mocha (350/350) + +2 ql-collab regression (119/119) + 1 stale V3.5.0.2 shape test updated + 10 V3.5.0.4b fixture additions.  Cross-peer cache-vs-Workbook divergence documented (LWW-by-iteration vs reject); napi reads from Workbook so end-users don't see it.  2 sessions estimated; shipped in 1.
3b. **V3.6.0.X -- Codex Lane A + Opus Lane B audit of V3.6.0.3 D2** ✅ SHIPPED at engine (this commit).  Codex transcript: `docs/audits/2026-05-23-phase-5-7-v3-6-0-3-codex.md` (PASS-WITH-FINDINGS; 0 HIGH + 3 MED + 4 LOW + 6 INFO).  Opus transcript: `docs/audits/2026-05-23-phase-5-7-v3-6-0-3-opus.md` (PASS-WITH-FINDINGS; 1 HIGH + 3 MED + 4 LOW + 4 INFO).  3 convergent findings closed in-cycle: CONVERGENT-HIGH-1 (Opus HIGH-1 ≡ Codex MED-3): cache walker switched from LWW (`insert`) to first-write-wins (`entry().or_insert`) -- closes the V3.6.0.1 § D2 line 133 locked decision violation.  CONVERGENT-MED-1 (Codex MED-1 ≡ Opus MED-2): `workbook_snapshot.formats` sorted by FormatId before returning (HashMap iter was non-deterministic).  CONVERGENT-MED-2 (Codex MED-2 ≡ Opus MED-1): cache walker drops Builtin variant Op::RegisterFormat in `collect_cache_effects` (matches "ONLY Custom" docstring claim; Workbook-side replay still accepts as Phase 4.6 D-1 backward-compat, deferred V3.7+).  +5 ql-collab regression tests (Opus H.1 + H.2 + H.3 + H.5 + H.7).  Docstring sweeps: CacheEffect::RegisterFormat + format_table_cache field + format_table_cache_iter accessor + WorkbookSnapshotJson.formats napi + workbook_snapshot napi header.  ide-consumer-contract.md inline note + R-V3.5-5 marked CLOSED.  Codex INFO-1 REFUTED suspected undo-of-RegisterFormat staleness bug empirically (full-rebuild fallback works correctly for non-cell-keyed ops).  Opus LOW-1 (CacheBuckets refactor) deferred to V3.6.0.4 D3.  Opus LOW-2 + Codex LOW-3/LOW-4 (perf) deferred to V3.7+.  Tests: ql-collab **124/124** (119 baseline + 5 new); IDE mocha 350/350 unchanged.  Rule 4 arc terminus HELD at 6.
4. **V3.6.0.4 -- Per-cell op-index for invalidate_cell (D3)** ✅ SHIPPED at engine (this commit).  Closes V3.5.0.6 R-V3.5-2 perf gap.  Engine changes:
   - New `ql_oplog::OpLog::get(index) -> Option<Result<Op, OpLogError>>` accessor (random-access op lookup; mirrors a single `iter()` step at the given index).
   - New `cell_op_index: HashMap<(u16, u32, u32), Vec<usize>>` field on `CollabSession` (per-cell op-log indices).
   - New `sheet_op_index: HashMap<u16, Vec<usize>>` field (per-sheet `Op::RemoveSheet` indices; separate from `cell_op_index` because RemoveSheet doesn't carry a cell coord and folding would inflate cell_op_index by O(cells-in-sheet) per remove).
   - New `CacheBuckets<'a>` struct bundling 5 mutable refs (snapshot + tombstones + format_cache + cell_op_index + sheet_op_index); closes Opus LOW-1 from V3.6.0.X audit-of-D2.
   - `apply_cache_effect` signature: `(&mut CacheBuckets<'_>, CacheEffect, op_log_index: usize)` -- 3 individual `&mut` params → 1 struct + 1 `usize`.
   - `apply_cache_effect` maintains both indices per-effect with dedup-on-equality check (BatchCommit inner effects sharing same op_log_index produce one entry).
   - New `rebuild_op_indices_only()` helper -- refreshes ONLY the indices from the visible log WITHOUT touching snapshot/tombstones/format_cache.  Called by `undo` + `redo` BEFORE the per-cell `invalidate_cell` loop because Loro's UndoManager retract COMPACTS the visible list (positional indices shift); refreshes the indices to point at post-retract log positions.  Per-undo O(N) cost; enables K * O(k) per-cell invalidation that follows (wins for K > 1 vs pre-D3 K * O(N)).
   - `invalidate_cell` rewritten to walk via `cell_op_index.get(&(s,r,c))` ∪ `sheet_op_index.get(&s)` (merged + sorted + deduped) instead of full `self.log.iter()` walk.  Out-of-range indices surface as `OpLogError::SchemaMismatch` (invariant violation; unreachable post-undo-fix).
   - `force_clear_snapshot_cache` extended to clear both new indices.
   - New accessors `cell_op_index_iter` + `sheet_op_index_iter` (mirrors `format_table_cache_iter` pattern).
   - +6 ql-collab regression tests (130/130 pass; +6 over V3.6.0.X audit-of-D2 124 baseline): per-op append maintenance; BatchCommit dedup; RemoveSheet → sheet_op_index; force_clear discipline; rebuild_snapshot_cache repopulates; undo post-retract index refresh.
   - **Design discovery (V3.6.0.4 implementation)**: Opus § F.3 assumed positional indices in cell_op_index would survive across undo.  Loro's UndoManager retract COMPACTS the visible list so subsequent ops shift positions; `rebuild_op_indices_only` (new helper) is the bounded-cost fix.  Documented in `rebuild_op_indices_only` docstring + as a new R-V3.6 risk.
   - Rule 4 arc terminus HELD at 6 (2 new HashMap fields + 1 struct with 5 ref fields; all positive Send+Sync via composition of primitive keys + Vec<usize> values; 0 new triggers).
5. **V3.6.0.5 -- Format-aware buildHtml rendering (D4)** ✅ SHIPPED at engine (this commit) + IDE (this commit).  Engine napi: new `CellSnapshotJson.rendered: Option<String>` additive field (populated via `ql_functions::format::parse` + `ql_functions::format::render` with `EvalContext { date_system: workbook.date_system(), locale: EnUs, now_provider: System }`); new `WorkbookSnapshotJson.dateSystem: String` additive field ("Excel1900" | "Excel1904"; mapped from `ql_types::DateSystem`).  Silent fallback to `rendered: None` when: cell has no format / no value / format-id lookup miss in Workbook.formats / format-string parse error / wire-decode error on value (all matched to napi-rs Option::None absence convention).  IDE: types.ts interfaces extended (`CellSnapshotJson.rendered?: string` + `WorkbookSnapshotJson.dateSystem: 'Excel1900' | 'Excel1904'`); `QuantbookCellSnapshot.entries[*].rendered?: string` mirror; `extractSheetSnapshot` passes through cell.rendered (only when defined -- avoids polluting existing exact-shape tests with `rendered: undefined`); `cellGridHtml::renderRows` uses `e.rendered ?? formatCellValue(e.value)` (both server-side TS + client-side JS mirror updated; escapeHtml covers both paths -- CSP-safe).  +4 mocha shape tests (354/354 pass; +4 over V3.6.0.X audit-of-D3 350 baseline): dateSystem default + valid-discriminator + rendered-absent-without-format + rendered-not-in-Object.keys-when-absent.  ql-collab 134/134 unchanged (no engine collab-side changes; format::render integration lives in napi crate).  Rule 4 arc terminus HELD at 6 (1 new String field on existing napi struct + 1 new Option<String> field on existing napi struct; both Send+Sync trivially; 0 new triggers).  **mocha-test scope limitation**: full format::render integration tests (Op::RegisterFormat + Op::SetCellFormat + Op::PutValue -> rendered output) are deferred to V3.6.0.X audit-of-D4 -- the IDE napi at V3.6.0.5 D4 has no appendSetCellFormat / appendRegisterFormat method, so end-to-end mocha can only pin SHAPE.  Engine-side Rust integration tests TBD at audit-of-D4 closure.
6. **V3.6.0.6 -- IDE-facing appendPutFormula napi (D5)** ✅ SHIPPED at engine `7f9a08ccd5e` + IDE `7608ae7633f` (D5 ship); **V3.6.0.X audit-of-D5 closures** ✅ SHIPPED at engine (this commit) + IDE (this commit).  Opus Lane B transcript at `docs/audits/2026-05-24-phase-5-7-v3-6-0-6-opus.md` (FAIL: 3 HIGH + 3 MED + 4 LOW + 4 INFO).  Codex Lane A BLOCKED by OrbStack Mac bridge outage during the audit window; deferred to V3.7+ retrospective if user signal surfaces.  4 in-cycle closures landed: **OPUS-HIGH-1** dispatcher `=` prefix detection in `dispatchIncomingMessage::'putValue'` arm (routes formula entry to `appendPutFormulaValidated`; webview envelope unchanged; closes the user-driven write-path gap so V3.5.0.X A-HIGH-2 framing is true end-to-end, not just napi-level); **OPUS-HIGH-2** `extractSheetSnapshot` passes through formula-only cells (cell.formula present + cell.value undefined) with `value: { kind: 'pending' }` instead of `continue`-skipping (matches V3.6.0.X audit-of-D4 CONVERGENT-HIGH-3 pending strategy); **OPUS-HIGH-3** new `validate_u16_index` helper + both `appendPutValue` + `appendPutFormula` change `sheet: u16` -> `sheet: f64` + manual validation (closes the V1 H2 silent-coercion class for the u16 sheet boundary -- napi-rs 3.9.0 ToUint32 + try_into<u16> silently coerces NaN/Infinity/fractional/2^32 to in-range u16; pre-closure docstring at lib.rs:876-878 wrongly claimed the class didn't apply at u16); **OPUS-MED-2** ide-consumer-contract.md § 4.1.z6 sweep (status block + HEAD trail + Tests baseline + D5 table row PENDING -> SHIPPED + new V3.6.0.6 D5 + audit-of-D5 sub-sections + Out-of-scope removal).  +OPUS-LOW-2 (validator suite couples to engine binary unnecessarily) closed in-cycle as a bonus via `suiteSetup(shouldSkip())` so CI hosts without the engine binary skip cleanly instead of failing.  Deferred to V3.6.0.7+ backlog: Opus MED-1 (client/server data-raw-formula guard asymmetry; safe today because client only emits in editable mode), LOW-1 (text contract docs), LOW-3 (engine napi test regex weak), LOW-4 (multi-peer + undo CRDT regression at IDE level), INFO-1..4.  Updates pre-existing V3.5.0.4b formula-only-skip test to reflect new pass-through semantic.  Tests: ql-collab 136/136 unchanged (engine changes are validator + signature; no new collab-side surface); IDE mocha **381/381** (+8 V3.6.0.X audit-of-D5 over V3.6.0.6 D5 373 baseline: 3 dispatcher routing + 2 formula-only render + 3 u16 sheet rejection on both appendPutValue and appendPutFormula).  Rule 4 arc terminus HELD at 6 (new `validate_u16_index` is a free function, not a type; sheet param type change is signature-only).  Below the historical V3.6.0.6 D5 narrative remains in place for completeness.

**V3.6.0.6 D5 narrative (predates audit-of-D5 closures above)**: engine napi new `CollabSession::appendPutFormula(sheet: u16, row: f64, col: f64, text: String) -> Result<()>` mirroring `appendPutValue` validation discipline (validate_u32_index on row/col); wraps existing `Op::PutFormula { sheet, row, col, text }`; no new wire variant; no new CacheEffect.  IDE: types.ts adds `CollabSessionInstance.appendPutFormula(sheet, row, col, text): void` + `QuantbookCellSnapshot.entries[*].formula?: string`; session.ts adds `appendPutFormulaValidated(session, sheet, row, col, text)` typed wrapper (validates sheet/row/col BEFORE the FFI ToUint32-coercion boundary -- matches appendPutValueValidated pattern; text not validated since wire accepts any string); extractSheetSnapshot passes through `cell.formula` (additive; conditional-key discipline like `rendered` preserves V3.5.0.4b shape-stability tests).  cellGridHtml: server-side renderRows emits `data-raw-formula="${escapeHtml(e.formula)}"` when entry has formula text; client-side renderRowsClient mirrors; beginEdit precedence updated to `data-raw-formula > data-raw-value > data-original-text`.  +19 mocha tests (354 -> 373).  Engine napi: new `CollabSession::appendPutFormula(sheet: u16, row: f64, col: f64, text: String) -> Result<()>` mirroring `appendPutValue` validation discipline (validate_u32_index on row/col); wraps existing `Op::PutFormula { sheet, row, col, text }`; no new wire variant; no new CacheEffect.  IDE: types.ts adds `CollabSessionInstance.appendPutFormula(sheet, row, col, text): void` + `QuantbookCellSnapshot.entries[*].formula?: string`; session.ts adds `appendPutFormulaValidated(session, sheet, row, col, text)` typed wrapper (validates sheet/row/col BEFORE the FFI ToUint32-coercion boundary -- matches appendPutValueValidated pattern; text not validated since wire accepts any string); extractSheetSnapshot passes through `cell.formula` (additive; conditional-key discipline like `rendered` preserves V3.5.0.4b shape-stability tests).  cellGridHtml: server-side renderRows emits `data-raw-formula="${escapeHtml(e.formula)}"` when entry has formula text; client-side renderRowsClient mirrors; beginEdit precedence updated to `data-raw-formula > data-raw-value > data-original-text` (extends V3.6.0.X audit-of-D4 OPUS-HIGH-2 § G.2 -- cells with both formula AND value have two raw representations; formula is the more-authoritative source for editing since it's what the user typed).  +19 mocha tests (354 -> 373): 6 appendPutFormulaValidated input validation (negative sheet, sheet>u16, fractional row, oversize col, non-string text, accepts valid); 4 engine napi (opCount, fractional row reject, negative row reject, out-of-u32 col reject); 3 workbookSnapshot surface (formula-only cell -> formula property; rename-chain repair end-to-end; round-trip via to_qbook/from_qbook); 2 extractSheetSnapshot (formula+value pass-through; formula-absent shape stability); 4 buildHtml emit (with-formula attribute, without-formula td scope check, HTML-escape XSS, beginEdit precedence chain).  V3.5.0.X A-HIGH-2 IDE-level verification gap CLOSED end-to-end (engine-side Rust regression already verified; now IDE-driven mocha verifies the rename-chain repair surfaces through napi).  Tests: ql-collab 136/136 unchanged (no engine collab-side changes); ql-oplog 67/67 unchanged; ql-collab-ws 42/42 unchanged (30 lib + 10 transport + 2 doctests); IDE mocha **373/373** (+19 over V3.6.0.X audit-of-D4 354 baseline).  Rule 4 arc terminus HELD at 6 (napi method only; no new structs/fields/variants; 0 new triggers).
7. **V3.6.0.7 -- profiling spike for incremental snapshot deltas (D6)** ✅ SHIPPED at engine (this commit) (2026-05-25).  Time-boxed criterion bench at `crates/ql-bindings-node/benches/workbook_snapshot.rs` replicating the body of `crates/ql-bindings-node/src/lib.rs:1802 workbook_snapshot` (napi methods can't be invoked from a Cargo bench harness; setup-time assertion guards bench-vs-napi drift).  12 workload combinations: 3 grids (1k / 10k / 100k cells per sheet) × 2 format densities (0 %, 50 %) × 2 sheet counts (1, 5).  Builtin format ids 1 ("0"), 9 ("0%"), 14 ("m/d/yy") rotated across formatted cells; no `Op::RegisterFormat` needed.  Results (Apple M4 Max, macOS 26.2, rustc 1.83.0, criterion 0.8.2 sample_size=10 measurement_time=15s):  decision combo `cells=100000 fmt=50% sheets=1` median **251.38 ms** vs 50 ms threshold (5× over); secondary combo `cells=100000 fmt=0% sheets=1` median 152.40 ms (3× over).  Threshold breakpoint between 10k (25 ms) and 50k (127 ms) cells.  Scaling: linear-to-slightly-superlinear in cell count (10k→100k = 10.0× time at 50 % format).  D4 cost ~65 % overhead at 100k cells (152 ms → 251 ms = ~99 ms for ~50k format::render calls + ~50k extra `Op::SetCellFormat` replays).  **Decision: D6 SHIPS in V3.6.0.8.**  See `docs/audits/2026-05-25-phase-5-7-v3-6-0-7-spike.md` for full transcript + new R-V3.6-NEW risk (rebuild_workbook dominant cost; D6 ship plan must address it).  Estimated remaining V3.6: +3 sessions for D6 implementation + 1 session for V3.6.0.X audit-of-D6.
8. **V3.6.0.8 -- D6 incremental WorkbookSnapshot deltas** (IN-PROGRESS; sub-step rollout below).  Estimated 4 cycles across ≥2 sessions.
   - **V3.6.0.8.1 -- DESIGN LOCK** ✅ SHIPPED at engine (this commit) (2026-05-25).  Docs-only lock; no engine source modified.  Mirrors V3.6.0.1 pattern.  Resolves the open R-V3.6-NEW question from V3.6.0.7 spike: **option (a) cache rebuilt+repaired Workbook + clone-and-apply-delta is CHOSEN**.  Option (b) read-from-`last_snapshot`-and-skip-rebuild_workbook is INFEASIBLE because `last_snapshot.formula` carries PRE-rename-repair text (would silently regress the V3.5.0.X A-HIGH-2 contract that `workbookSnapshot` surfaces REPAIRED formula text); option (c) hybrid revalidate-changed-cells deferred to V3.7+.  Locked napi surface: new `workbookSnapshotDelta(lastSeenVersion: Buffer) -> WorkbookSnapshotDeltaJson` method + new `WorkbookSnapshotDeltaJson { changedCells, removedCells, sheetsChanged, sheetsRemoved, formatsAdded, version, fullRebuildRequired }`.  Two-call discipline: empty `lastSeenVersion` Buffer (or unknown VV) returns `fullRebuildRequired=true`; IDE then calls `workbookSnapshot()` and caches the `version` token for subsequent delta calls.  Locked internal state: 2 new fields on `CollabSession` -- `last_snapshot_workbook: Option<Arc<Workbook>>` (post-repair) + `last_snapshot_oplog_vv: Option<VersionVector>`.  Both positive Send+Sync compositionally (Workbook auto-derives via field composition per `crates/ql-storage/src/workbook.rs:295-299`; Arc<T>: Send+Sync iff T; VersionVector via Loro's BTreeMap<PeerID, Counter>).  **Rule 4 arc terminus stays at 6**.  Locked algorithm: 8-step body -- cache-miss + decode-VV + staleness-check + same-VV fast-path + op-enumeration-since-cached-VV + rename-detection + cell-only fast-path (clone Arc::make_mut + apply per-op + skip repair) OR rename-full-rebuild branch + JSON-delta-build.  Cell-only fast-path is the steady-state IDE-editing happy path; rename ops trigger full rebuild_workbook + repair walks (because repair touches ALL formula cells, not just renamed sheet).  Locked invalidation triggers: `merge_bytes` + `discard_pending_ops` + `undo` + `redo` all call new `force_clear_workbook_cache()` helper; `append_op` does NOT invalidate (it's the cell-only fast-path target).  5 new risks documented R-V3.6-14..18 (cached-Workbook-stale-after-retract; VV-vs-op_count-drift; multi-peer-concurrent-rename-cell-edit; Workbook-clone-cost-may-exceed-20ms-at-100k; apply_op-private-to-ql-oplog).  R-V3.6-NEW (from V3.6.0.7) closed by this lock; renamed inline below to R-V3.6-NEW → CLOSED-AT-V3.6.0.8.1.  Sub-step rollout: V3.6.0.8.1 ✅ docs lock; V3.6.0.8.2 = engine PART 1 (new fields + force_clear_workbook_cache + invalidation wiring + new `apply_ops_in_range` helper on OpLog + ql-collab regression tests); V3.6.0.8.3 = engine PART 2 (`workbook_snapshot_delta` napi + bench delta vs full snapshot at 100k cells + napi shape tests); V3.6.0.8.4 = V3.6.0.X audit-of-D6 (parallel Codex Lane A + Opus Lane B + closures).  See `docs/architecture/ide-consumer-contract.md § 4.1.z6` for the consumer-facing mirror.  Cycle count for this session: 1 (V3.6.0.8.1 lock only); within CLAUDE.md ≤2 ceiling.
   - **V3.6.0.8.2 -- ENGINE PART 1** ✅ SHIPPED at engine (this commit) (2026-05-25).  New `last_snapshot_workbook: Option<Arc<Workbook>>` + `last_snapshot_oplog_vv: Option<loro::VersionVector>` fields on `CollabSession` (initialized `None` in both `new` + `from_snapshot`).  New `pub fn force_clear_workbook_cache(&mut self)` helper (resets both fields atomically, preserving "Some iff Some" invariant).  New `pub fn set_workbook_cache(&mut self, Arc<Workbook>, VersionVector)` for V3.6.0.8.3 napi to populate post-`rebuild_workbook`.  New `pub fn last_snapshot_workbook(&self)` + `pub fn last_snapshot_oplog_vv(&self)` read accessors.  Invalidation wired at 4 production callsites: `merge_bytes` (after `rebuild_snapshot_cache`), `discard_pending_ops` (after `rebuild_snapshot_cache`), `undo` (at top, BEFORE Loro's undo per R-V3.6-14), `redo` (at top, mirror of undo).  Append-op does NOT invalidate (cell-only fast-path target; verified by regression test).  New `pub fn apply_ops_in_range(log, workbook, from, to, registry) -> Result<usize, ReplayError>` free function in `ql-oplog::replay` (re-exported via `ql_oplog::apply_ops_in_range`; preserves `replay_into`'s from-empty contract; out-of-range to_index silently stops at log end; empty range = O(1) no-op).  Also extended `force_clear_snapshot_cache` test seam to clear workbook cache for cohesive test-state.  +13 ql-collab regression tests (149/149 pass): `v3_6_0_8_2_fresh_session_has_empty_workbook_cache` + `..._from_snapshot_has_empty_workbook_cache` + `..._set_workbook_cache_populates_both_fields` + `..._force_clear_workbook_cache_resets_both_fields` + `..._merge_bytes_invalidates_workbook_cache` + `..._discard_pending_ops_invalidates_workbook_cache` + `..._undo_invalidates_workbook_cache` + `..._redo_invalidates_workbook_cache` + `..._append_op_does_NOT_invalidate_workbook_cache` + `..._apply_ops_in_range_empty_is_noop` + `..._apply_ops_in_range_full_matches_replay_into` + `..._apply_ops_in_range_forward_from_partial` + `..._apply_ops_in_range_oversized_to_index_silently_stops_at_log_end`.  Rule 4 arc terminus HELD at 6 (positive Send+Sync composition on the 2 new field types per the V3.6.0.8.1 lock; 0 new triggers).  No new napi surface; no IDE / mocha changes.  Tests: ql-collab **149/149** + ql-oplog **67/67** + ql-collab-ws **40/40** (+2 ignored doctests) + IDE mocha **384/384** unchanged + engine workspace release build clean.
   - **V3.6.0.8.3 -- ENGINE PART 2 napi** ✅ SHIPPED at engine (this commit) + IDE (this commit) (2026-05-25).  napi surface: new `WorkbookSnapshotDeltaJson { changedCells, removedCells, sheetsChanged, sheetsRemoved, formatsAdded, version: Buffer, fullRebuildRequired: bool }` napi struct + new `ChangedCellJson { sheet, cell: CellSnapshotJson }` + new `RemovedCellJson { sheet, row, col }` napi helper structs.  New `#[napi] CollabSession::workbookSnapshotDelta(lastSeenVersion: Buffer)` method.  Cache populated in existing `workbookSnapshot` napi via `set_workbook_cache(Arc::clone(&workbook), oplog_vv())` after `rebuild_workbook`; `workbook` wrapped in `Arc` BEFORE rendering so no double-clone.  Algorithm follows the V3.6.0.8.1 lock's 8 steps: cache-miss + decode-VV + staleness + same-VV-fast-path + op-enumeration + rename-detection + cell-only-fast-path (Arc-deref-clone + `ql_oplog::apply_ops_in_range` + skip repair walks) + JSON-delta-build (rename branch returns `fullRebuildRequired=true`).  V3.6.0.8.3 scope limitations: `removedCells` always empty (V3.7+); `sheetsChanged` always empty in cell-only path (cell ops don't change sheet metadata); rename ops trigger full-rebuild fallback rather than incremental rename delta.  Added `last_snapshot_op_count: Option<usize>` 3rd cache field (revision to V3.6.0.8.2 cache invariants) so delta path derives the replay slice `[op_count, log.len())` without VV->op_count walk -- the three-field "set together / clear together" invariant preserves.  New accessor `pub fn last_snapshot_op_count() -> Option<usize>` + extended `set_workbook_cache` (pins op_count alongside VV) + extended `force_clear_workbook_cache` (clears all 3 fields).  New `pub fn oplog_vv(&self) -> VersionVector` + `pub fn log(&self) -> &OpLog` accessors on `CollabSession` so the napi (outside ql-collab) can grab the VV at populate time + enumerate ops at delta time.  `loro::VersionVector` re-exported as `ql_oplog::VersionVector` for downstream-crate ergonomics (avoids a direct `loro` dep on ql-bindings-node).  New free fns `empty_delta(version, full_rebuild_required)` + `classify_delta_op(op, has_rename, changed_cells, removed_sheets, new_formats)` at file scope (helpers for the algorithm; `classify_delta_op` recurses into `Op::BatchCommit` mirroring V3.4.0.X HIGH-1 walker discipline).  IDE: `types.ts` interfaces extended (`CollabSessionInstance.workbookSnapshotDelta(lastSeenVersion: Buffer): WorkbookSnapshotDeltaJson` + new `WorkbookSnapshotDeltaJson` + `ChangedCellJson` + `RemovedCellJson` interfaces).  +6 mocha shape tests (390/390 pass; +6 over the V3.6.0.X audit-of-D5 live 384 baseline; the 381 documented baseline was a 3-count undercount from prior handoff): empty Buffer fullRebuild + no-cache fullRebuild + same-VV fast-path post workbookSnapshot + cell-only fast-path emits changed cells with deterministic sort + rename op triggers fullRebuild + undo invalidates cache.  ql-collab 149/149 unchanged + ql-oplog 67/67 unchanged + engine workspace release build clean.  Bench delta vs full at 100k cells DEFERRED to V3.6.0.8.4 (the audit-of-D6 cycle will naturally include the perf verification; functional + regression test surface is complete at V3.6.0.8.3).  R-V3.6-15 (VV vs op_count drift) PARTIAL closure: the three-field "all Some / all None" invariant is verified by the existing V3.6.0.8.2 regression tests + the new same-VV-fast-path test; full debug-assert on (log.len() == cached_op_count + ops_since) deferred to V3.6.0.8.4.  R-V3.6-17 (Workbook clone cost) profile deferred to V3.6.0.8.4.  Rule 4 arc terminus HELD at 6 (new fields + napi structs all positive Send+Sync composition; `ChangedCellJson` over `CellSnapshotJson` (existing) + `u32`; `RemovedCellJson` over 3 `u32`s; `WorkbookSnapshotDeltaJson` over `Vec<X>` for X each previously-walked + `Buffer` (Send+Sync via Vec<u8>) + `bool`; 0 new triggers).
   - **V3.6.0.8.4 -- V3.6.0.X audit-of-D6 + closures + bench** ✅ SHIPPED at engine (this commit) + IDE (this commit) (2026-05-25; user-directed cycle-4 override, 2 over CLAUDE.md ≤2 ceiling).  Codex Lane A FAIL (3 HIGH + 1 LOW + 1 INFO) + Opus Lane B PASS-WITH-FINDINGS (3 HIGH + 5 MED + 3 LOW + 1 INFO).  Transcripts: `docs/audits/2026-05-25-phase-5-7-v3-6-0-8-codex.md` + `docs/audits/2026-05-25-phase-5-7-v3-6-0-8-opus.md`.  In-cycle closures + bench summary: `docs/audits/2026-05-25-phase-5-7-v3-6-0-8-closures.md`.  **5 HIGH closed in-cycle**: CONVERGENT-HIGH-1 (Codex-HIGH-2 ≡ Opus-HIGH-1) classify_delta_op allowlist for metadata ops (no more silent data loss on AddSheet/MoveSheet/CreateTable/DropTable/ResizeTable/SetName/SetLocale/SetReferenceMode/SetDateSystem); Codex-HIGH-1 poll_remote_with_limit force_clear_workbook_cache after rebuild_snapshot_cache (5th invalidation callsite missed by V3.6.0.8.2 wiring); Codex-HIGH-3 new `snapshot_cell(sheet,row,col) -> Option<CellState>` O(1) HashMap.get accessor (replaces O(cells_in_sheet) snapshot_cells.find() per delta cell); Opus-HIGH-2 new `version: Buffer` field on `WorkbookSnapshotJson` populated from `cache_vv.encode()` (IDE consumer protocol no longer needs empty-Buffer probe workaround); Opus-HIGH-3 OpLog::get(i) random-access instead of iter().skip(N).take(M) (closes the primary perf bug -- iter().skip() deserializes ALL skipped ops; fix applied to both apply_ops_in_range + napi delta enumeration).  **4 of 5 MED closed in-cycle**: MED-1 BTreeMap→FxHashMap docstring drift (3 sites); MED-2 formats_added sort by full FormatId (not just kind); MED-3 Arc::make_mut→always-clone docstring drift (4 sites); MED-4 Workbook::clone() cost profile (R-V3.6-17 measurement -- 1k=7μs / 10k=69μs / 100k=692μs; well under 20ms threshold; R-V3.6-17 CLOSED).  Deferred to V3.6.0.8.5+: Opus-MED-5 comprehensive mocha coverage (multi-peer/undo/redo/AddSheet-in-delta/SetCellFormat-in-delta/BatchCommit-in-delta/chained-delta); Opus-LOW-1 empty-stack undo/redo over-invalidation (perf opt, not correctness); Opus-LOW-2 per-accessor 3-field debug_assert (R-V3.6-15 closed via monotonicity debug_assert at lib.rs:2454); Opus-LOW-3 set_workbook_cache API tightening; INFO-1 currentVersion accessor (superseded by Opus-HIGH-2 closure).  **R-V3.6-15 CLOSED** (monotonicity debug_assert in workbook_snapshot_delta after op enumeration).  **R-V3.6-17 CLOSED** (clone cost measured well under threshold).  Bench `crates/ql-bindings-node/benches/workbook_snapshot.rs` extended with `bench_workbook_snapshot_delta` (1/10/100 new cells × 100k initial workload) + `bench_workbook_clone` (1k/10k/100k cells).  **D6 perf contract achieved with 46× headroom**: 100 new cells delta = 1.08 ms vs full snapshot 246 ms (228× faster than full; 46× under the 50 ms threshold).  1 new cell delta = 1.01 ms; 10 new cells = 1.00 ms.  Pre-closure bench showed 741 ms at 100 new cells (3× SLOWER than full snapshot); HIGH-3 fixes (Opus OpLog::get + Codex snapshot_cell) delivered the 230× improvement.  Tests: ql-collab 149/149 + ql-oplog 67/67 + ql-collab-ws 42/42 + IDE mocha 390/390 (the 2 shape-pinning tests at suite `V3.5.0.2 workbookSnapshot napi contract` + V3.6.0.5 shape-stability updated to include `version`).  Rule 4 arc terminus HELD at 6 (no new field types triggering negative-trait analysis; `Buffer` is napi-rs safe wrapper Send+Sync via Vec<u8>).  D6 ARC COMPLETE.
9. **V3.6.0.9 -- #REF! substitution (D7)** IF surfaced. Estimated 1-2 sessions.
10. **V3.6.0.10 -- Op::RestoreSheet (D8)** IF surfaced. Estimated 1 session.
11. **V3.6.0.11 -- mid-edit-render guard typing-stroke watchdog (D9 F.9)** IF surfaced. Estimated 0.5 session.
12. **V3.6.0.X -- parallel Codex+Opus megaudit + closures.** Phase-level termination audit at V3.6.0.11 ship. ~6-8 sessions across audit cycle.

**Estimated V3.6 wall time:** 14-20 sessions (vs Opus § H.5 estimate of 8-12). Higher because Opus didn't separately estimate the V3.6.0.X audit cycle.

## V3.6 risk register (R-V3.6-*)

- **R-V3.6-1 Loro on_pop callback re-entrancy.** The callback fires during Loro's `undo()` / `redo()` execution. If the closure attempts to lock a Mutex held by the outer `undo()` call, deadlock. Mitigation: closure pushes pending invalidations to a separate `Arc<Mutex<Vec<(u16, u32, u32)>>>`; outer `undo()` drains it after Loro's undo completes. Verify at V3.6.0.2 implementation with a stress test (10k undo/redo cycles).
- **R-V3.6-2 FormatTable cache invariant maintenance.** RegisterFormat cache must stay in sync with `Workbook::formats` (Phase 4.6 D-1 FormatTable). If a `merge_bytes` brings in a peer's RegisterFormat that the local cache walker misses (because it's not in collect_cache_effects), the cache + workbook diverge. Mitigation: rebuild path (rebuild_snapshot_cache style) for format_table_cache. Debug-assert at end of merge_bytes that cache.len() == workbook.formats.len() (modulo Builtins which aren't cached).
- **R-V3.6-3 cell_op_index sync invariant.** 6 mutation sites must update both `last_snapshot` AND `cell_op_index`. Sync drift produces stale `invalidate_cell` (visible cells with stale data after undo). Mitigation: debug-assert at end of each mutation site checking `cell_op_index.values().flatten().count() == sum of cell-keyed effects emitted`. Adopt the V3.5.0.X "cell-keyed effects walker" pattern.
- **R-V3.6-4 Format-aware rendering locale/date_system propagation.** `Workbook::date_system` (Excel1900 vs Excel1904) affects serial-to-date conversion. If `workbook_snapshot` forgets to pass date_system, IDE renders dates with wrong epoch. Mitigation: add `date_system` to `WorkbookSnapshotJson` top-level + assert in tests.
- **R-V3.6-5 Incremental snapshot delta freshness.** If V3.6.0.7 profiling justifies D6, the delta API must handle the "IDE's last-rendered version is older than Loro's history limit" edge case (Loro may GC old history). Mitigation: fall back to full snapshot when version-vector predecessor is unreachable. Document at D6 ship.
- **R-V3.6-6 OnPush callback DiffEvent inspection cost.** The on_push callback fires for EVERY Loro change (not just our cell-keyed ops). Filtering to extract cell coordinates from DiffEvent on every push has a cost. Mitigation: early-return for non-cell-keyed origins; benchmark at V3.6.0.2 implementation. If cost > 1% of typical session time, fall back to option (b) CounterSpan range walk.
- **R-V3.6-7 pure_local_frontier removal regression.** Removing the V3.5.0.X conservative gate could expose a subtle correctness bug if on_pop isn't wired correctly. Mitigation: keep the V3.5.0.X regression tests passing post-V3.6.0.2 with REWRITTEN expectations (partial-invalidate fires + matches full-rebuild result, instead of "falls back to full-rebuild").
- **R-V3.6-8 napi struct evolution.** V3.6 adds new fields to existing napi structs (`formats` on WorkbookSnapshotJson, `rendered` on CellSnapshotJson, `date_system` on WorkbookSnapshotJson). All additive; preserves V3.5 IDE consumers that destructure the JSON. Document the "extend never reshape" contract per V3.5 R-V3.5-7. Verify with mocha tests that pin the V3.5 shape stability.
- **R-V3.6-9 #REF! substitution + Op::RestoreSheet interaction.** If both D7 + D8 ship, restoring a sheet should un-#REF!-substitute formula refs. Cleanest: D7 substitution lives in `repair_sheet_chain` at `rebuild_workbook` time, and Op::RestoreSheet removes the tombstone BEFORE the repair pass -- so the formulas naturally un-#REF! on the next rebuild. Verify at D7+D8 co-ship.
- **R-V3.6-10 cell_op_index / sheet_op_index positional-index fragility under Loro UndoManager retract.** DISCOVERED at V3.6.0.4 D3 implementation.  Opus § F.3 assumed positional indices in `cell_op_index: HashMap<(SheetId, RowId, ColId), Vec<usize>>` would survive across undo/redo.  Loro's UndoManager retract COMPACTS the visible op list (positional indices shift down for every op AFTER the retracted position), so pre-undo entries in `cell_op_index` point at the wrong op OR past the new end of log post-undo.  Mitigation: new `rebuild_op_indices_only()` helper called by `undo()` + `redo()` BEFORE the per-cell `invalidate_cell` loop -- refreshes indices from the post-retract visible log without touching snapshot/tombstones/format_cache.  Cost: O(N) per undo (vs hypothetical zero-cost stable-index design); enables K * O(k) per-cell invalidation that follows.  Long-term fix (V3.7+): extend `OpLog` API with stable op-ID lookup (Loro op-IDs survive retract; positional indices don't) + migrate cell_op_index to use op-IDs.  Documented in `rebuild_op_indices_only` docstring.
- **R-V3.6-11 dateSystem propagation requires Op::SetDateSystem replay arm.**  DISCOVERED at V3.6.0.X audit-of-D4 (Codex Lane A HIGH-1 + Opus Lane B HIGH-1, both empirically probed).  Pre-closure V3.6.0.5 D4 shipped `WorkbookSnapshotJson.dateSystem` reading from `workbook.date_system()` AFTER `rebuild_workbook` -- but the op log had no `Op::SetDateSystem` variant, so replay always produced `Workbook::default()` (Excel1900) regardless of the .qbook envelope's value.  Excel1904 workbooks rendered dates 1462 days off.  `from_qbook` compounded by discarding the loaded `Workbook` entirely.  Mitigation: new `Op::SetDateSystem { date_system: DateSystemWire }` variant in ql-oplog (mirrors `Op::SetLocale`); replay arm calls `workbook.set_date_system(_)`; `from_qbook` seeds the op when loaded workbook's date_system != default.  Excel1904 round-trips correctly.  Documented at V3.6.0.X audit-of-D4 closure (engine THIS commit).
- **R-V3.6-12 IDE click-to-edit broken for engine-rendered display strings.**  DISCOVERED at V3.6.0.X audit-of-D4 (Opus Lane B HIGH-2, Codex missed).  Pre-closure V3.6.0.5 D4 set `data-original-text` to the engine-rendered display string (e.g., "$1,234.56", "50.00%", "1,234"); IDE `beginEdit` fed it into the `<input>` on click-to-edit; `parseCellRawInput` did `Number(trimmed)` -> NaN -> error on Enter for currency/percent/thousands/date formats.  Mitigation: new `data-raw-value` attribute carries the parseable raw representation; `beginEdit` reads it first, falls back to `data-original-text` for backward compat.  `data-original-text` retained for Escape-cancel restore (the displayed text).  Documented at V3.6.0.X audit-of-D4 closure.
- **R-V3.6-13 Pending values + format render as formatted zero.**  DISCOVERED at V3.6.0.X audit-of-D4 (Codex MED-1, Opus HIGH-3).  Pre-closure V3.6.0.5 D4 pre-rendered any cell with both format AND value; `CellWireValue::Pending` -> `Value::Blank` -> Number format renders as "0.00".  Pre-D4 the IDE explicitly displayed "(pending)" for pending cells; post-D4 the engine pre-render shadowed the IDE fallback, fabricating a computed-looking value while the cell value kind remained pending.  Mitigation: short-circuit pre-render when `state.value.is_pending()`; IDE fallback to "(pending)" preserved.  Documented at V3.6.0.X audit-of-D4 closure.
- **R-V3.6-NEW (CLOSED-AT-V3.6.0.8.1) rebuild_workbook is the dominant cost in workbookSnapshot.**  DISCOVERED at V3.6.0.7 D6 spike.  Criterion bench measurements at 100k cells show `rebuild_workbook` + `repair_sheet_rename_chain` + `repair_table_rename_chain` + `repair_column_rename_chain` walks consume the bulk of per-call wall time (152 ms at 0 % format / 251 ms at 50 % format; format::render adds ~99 ms on top).  RESOLUTION at V3.6.0.8.1: option (a) chosen -- cache rebuilt+repaired Workbook (post-V3.5.0.X A-HIGH-2-contract) + clone-and-apply-delta + skip repair on cell-only deltas.  Option (b) [read-from-`last_snapshot`-and-skip-rebuild_workbook] REJECTED because `last_snapshot.formula` carries PRE-repair text.  Option (c) [hybrid revalidate-changed-cells] DEFERRED to V3.7+.  See `docs/audits/2026-05-25-phase-5-7-v3-6-0-7-spike.md` + V3.6.0.8.1 design lock above.
- **R-V3.6-14 Cached Workbook stale after Loro UndoManager retract.**  Loro's UndoManager retract COMPACTS the visible op log (R-V3.6-10 carry).  V3.6.0.8 cached Workbook would still reflect the pre-retract state -- a no-op for staleness if cell_op_index walks correctly, but a HARD STALENESS for rename ops that the retract un-did.  Mitigation: V3.6.0.8.2 `undo()` + `redo()` invocations call new `force_clear_workbook_cache()` BEFORE delegating to Loro's undo.  Mirrors the existing `force_clear_snapshot_cache` discipline at V3.6.0.4 D3.
- **R-V3.6-15 (CLOSED-AT-V3.6.0.8.4) VersionVector vs op_count drift.**  Loro's `oplog_vv()` returns an `FxHashMap<PeerID, Counter>` (V3.6.0.8.4 OPUS-MED-1: pre-closure docstring said BTreeMap; actual loro-internal-1.12.0/src/version.rs:29 is FxHashMap), not a `usize` log length.  PARTIAL closure at V3.6.0.8.3: three-field "all Some together / all None together" invariant verified by V3.6.0.8.2 regression tests + V3.6.0.8.3 same-VV fast-path test.  **CLOSED at V3.6.0.8.4**: monotonicity `debug_assert_eq!(new_ops.len(), current_op_count - cached_op_count, ...)` added in `workbook_snapshot_delta` after op enumeration (crates/ql-bindings-node/src/lib.rs ~2461-2475).  V3.6.0.8.2 invalidation discipline + this assert together guarantee the cached (VV, op_count) pair never drifts.
- **R-V3.6-16 Multi-peer concurrent rename + cell edits.**  Peer A edits cell, Peer B renames sheet, both merge into Peer C.  Peer C's `merge_bytes` invalidates the workbook cache (V3.6.0.8.2 wiring).  But the IDE may have cached an older `version` token from a pre-merge snapshot; the V3.6.0.8.3 staleness check (step 3 of the algorithm above) catches this and returns `fullRebuildRequired=true`.  Documented + verified at V3.6.0.8.3 ship with a multi-peer regression test.
- **R-V3.6-17 (CLOSED-AT-V3.6.0.8.4) Workbook clone cost may itself exceed delta budget.**  Profile measurement at V3.6.0.8.4 via new `bench_workbook_clone` micro-bench: 1k cells = 7 μs / 10k cells = 69 μs / 100k cells = **692 μs**.  Well under the V3.6.0.8.1 lock's 20 ms threshold.  `(*cached_arc).clone()` always-clone IS the right pattern; no Arc-make_mut COW refactor needed.
- **R-V3.6-18 `apply_op` is private to `ql-oplog`.**  D6 needs to apply single ops to an existing Workbook (the cell-only fast-path body).  Options at V3.6.0.8.2: (a) expose existing `apply_op` as `pub fn`, (b) add a new `apply_ops_in_range(log, workbook, from_index, to_index, registry) -> Result<usize, ReplayError>` helper on `OpLog` (preserves `replay_into`'s from-empty contract while giving D6 a clean public API).  **Decision: (b)**.  The new helper iterates `self.log.iter().enumerate().skip(from_index).take(to_index - from_index)` and invokes the existing private `apply_op` -- single public API + zero contract change for `replay_into`.

## V3.6 out of scope

- IDE-side live formula re-evaluation (requires `rebuild_workbook` round-trip + WorkbookRuntime FFI exposure). V3.7+ explicit decision; V3.6 IDE renders formula TEXT but does NOT re-evaluate.
- Per-peer color hashing for presence decoration. V3.4.1+ polish; still parked.
- Envelope v3 schema bump for UserIdentity. V3.4.1+ if attribution feature surfaces.
- Locale-aware format rendering (currency symbols, decimal separators per locale). V3.6 ships en-US-only; V3.6.1+ if surfaced.
- vscode-test command-flow tests infrastructure. V3.5.1+ test-infrastructure investment; still parked.
- Chart / pivot table napi surfaces (Phase 4 product features now exposable via WorkbookSnapshot). V3.7+ scope.
- `CacheState { cells, formats, names, tables }` promotion (V3.5 D1 option (b)). V3.7+ if profiling justifies; V3.6 D2 + D3 land per-cache extensions instead.
- Cross-restart per-peer color hashing OR per-peer display-order overlay (V3.5.0.3c V3.6+ deferral). V3.7+ if user-facing concern surfaces.

## V3.6.0.X audit transcript pre-allocation

V3.6 uses per-sub-step audits (V3.5/V3.6.0.2 pattern) rather than a single phase-termination audit.  Each major D-decision ship gets its own Codex Lane A + Opus Lane B audit cycle.

**Shipped audits**:
- V3.6.0.2 (D1 Loro on_push wiring) Codex Lane A: `docs/audits/2026-05-24-phase-5-7-v3-6-0-2-codex.md` ✅ SHIPPED (Opus Lane B inline in commit / plan body).  1 convergent HIGH closed in-cycle at engine `08964962cf8`.
- V3.6.0.3 D2 (session-wide RegisterFormat cache + formats napi field) Codex Lane A: `docs/audits/2026-05-23-phase-5-7-v3-6-0-3-codex.md` + Opus Lane B: `docs/audits/2026-05-23-phase-5-7-v3-6-0-3-opus.md` ✅ SHIPPED.  3 convergent findings closed in-cycle: HIGH-1 cache LWW -> first-write-wins; MED-1 formats HashMap order -> sorted by FormatId; MED-2 Builtin RegisterFormat dropped at cache walker.  +5 ql-collab regression tests (Opus H.1/H.2/H.3/H.5/H.7).  Codex INFO-1 REFUTED suspected undo-of-RegisterFormat staleness bug.
- V3.6.0.4 D3 (per-cell op-index + CacheBuckets refactor) Codex Lane A: `docs/audits/2026-05-23-phase-5-7-v3-6-0-4-codex.md` (PASS-WITH-FINDINGS; 0 HIGH + 2 MED + 4 LOW + 5 INFO) + Opus Lane B: `docs/audits/2026-05-23-phase-5-7-v3-6-0-4-opus.md` (PASS-WITH-FINDINGS; 1 HIGH + 2 MED + 4 LOW + 3 INFO) ✅ SHIPPED.  4 in-cycle closures: CONVERGENT-HIGH-1 (Opus HIGH-1 / Codex LOW-2): invalidate_cell atomic-swap (build-into-fresh; defer self.last_snapshot.remove(target_key) until after all fallible OpLog::get calls succeed).  CONVERGENT-MED-2 (Codex MED-2 / Opus MED-1): rebuild_op_indices_only uses apply_cache_effect for tombstone parity + cell_op_index pruned on RemoveSheet (mirrors snapshot.retain).  Codex MED-1: poll_remote_with_limit partial-error rebuild (track first drain error; rebuild_snapshot_cache before propagating; skip auto-flush on error path).  Codex LOW-1: OpLog::get docstring corrected from "O(1)" to "O(log N) BTree lookup" + ide-consumer-contract.md V3.6.0.4 D3 SHIPPED + R-V3.5-2 CLOSED sweep.  +4 ql-collab regression tests (cell_op_index_pruned_on_remove_sheet + rebuild_op_indices_only_matches_full_rebuild_under_tombstones + invalidate_cell_atomic_swap_on_normal_path + poll_remote_partial_error_rebuilds_cache).
- V3.6.0.5 D4 (format-aware buildHtml rendering) Codex Lane A: `docs/audits/2026-05-24-phase-5-7-v3-6-0-5-codex.md` (FAIL verdict; 1 HIGH + 2 MED + 3 LOW + 4 INFO) + Opus Lane B: `docs/audits/2026-05-24-phase-5-7-v3-6-0-5-opus.md` (FAIL verdict; 3 HIGH + 3 MED + 4 LOW + 3 INFO) ✅ SHIPPED.  Most serious audit verdict in V3.6 cycle.  Convergent + in-cycle closures: **CONVERGENT-HIGH-1** (both lanes): new `Op::SetDateSystem` wire variant + `DateSystemWire` enum (mirrors LocaleWire) + replay handler + `from_qbook` seeds the op when loaded workbook's date_system != default; Excel1904 .qbook envelopes now round-trip correctly (pre-closure: silent 1462-day off-by-one).  **OPUS-HIGH-2** (Codex missed): new IDE `data-raw-value` attribute on editable cells; `beginEdit` reads it first; preserves click-to-edit for currency/percent/thousands/date formats (pre-closure: every formatted cell edit broken).  **CONVERGENT-HIGH-3** (Opus HIGH-3 / Codex MED-1): short-circuit pre-render when `wire_value.is_pending()`; IDE "(pending)" fallback preserved (pre-closure: pending+format displayed as formatted zero).  **CONVERGENT-MED-1** (Opus MED-1 / Codex LOW-1): thread `workbook.locale()` through EvalContext (prevents silent forward-compat regression at V3.6.1+).  **CONVERGENT-MED-2** (Opus MED-2 / Codex LOW-2): per-snapshot parsed-format cache (HashMap<FormatId, FormatString>; reuses parse result across cells sharing FormatId; closes per-cell parse cost in common case).  **OPUS-LOW-2**: dropped `.to_string()` on lookup result (use `&str` directly; saves N allocations per snapshot).  **CONVERGENT-MED-3**: ide-consumer-contract.md doc sweep (R-V3.5-4 marked CLOSED; D4 "deferred" references updated to "SHIPPED").  **OPUS-LOW-3**: TS docstring `date_system` -> `dateSystem` (napi-rs camelCase).  +2 ql-collab regression tests (`v3_6_0_x_audit_of_d4_set_date_system_replay_updates_workbook` + `v3_6_0_x_audit_of_d4_set_date_system_unknown_wire_errors`).  3 new R-V3.6-* risks (R-V3.6-11/12/13) documented.

**Pending audits** (per Opus § H.5 recommended sequencing):
- V3.6.0.X audit of D5 (V3.6.0.6 ship): IDE PutFormula end-to-end + repaired-formula integration.
- V3.6.0.X phase termination audit at V3.6.0.10 ship (parallel Codex+Opus; broad scope).

Expected ~14-20 sessions of total wall time across 8-11 sub-steps + per-step + phase-termination audits for the full V3.6 arc.

## V3.7+ entry-readiness preview (deferred from V3.6)

Items above plus:
- Live formula re-evaluation in IDE-side rendering.
- ~~WorkbookSnapshot incremental deltas (V3.6.0.7 deferred outcome)~~ — now V3.6.0.8 D6 SHIP per the V3.6.0.7 spike verdict (251 ms at 100k cells / 50% format; 5× over the 50 ms threshold).
- Locale-aware format rendering.
- vscode-test command-flow tests infrastructure.
- Chart / pivot table napi surfaces.
- `CacheState` cache promotion.
- Multi-tab sheet UI redesign (V3.5.0.4c + V3.6 D9 deferred).
- Per-peer color hashing OR per-peer display-order overlay.

These are explicitly NOT V3.6 work; deferred to V3.7 entry-readiness analysis at V3.6.0.X Opus § F.
