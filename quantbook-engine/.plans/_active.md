---
name: 2026-05-24_phase-5-7-v3-5-workbook-snapshot-sheet-ops-undo-format
status: in-progress (V3.5.0.1 lock `48be1878db5`; V3.5.0.2 WorkbookSnapshot napi engine `bbd129ed629` + IDE `299fed5eda9`; V3.5.0.3 D4 sheet-ops suite engine `74aa5039e04`+`9be3672ca01`+`7ec7877a018` + IDE `5ad934d087f`+`91a6b3870bd`+`e3642ef3458` (+ deep-audit closures engine `fa7541f1e8d`); V3.5.0.4a sheet management commands engine `1be56176fd2` + IDE `8fecc25c03f`; V3.5.0.4b cell-grid render migration engine `75963628c54` + IDE `0d4659e62d6`; V3.5.0.5 CellState format extension engine `175258f4dd0` + IDE `0b6a6481a14`; V3.5.0.6 partial-invalidate undo (D2) engine `1bd21296f31`; V3.5.0.7 mid-edit-render guard (D5 / R-V3.4-3 KNOWN-GAP CLOSED) engine `bf443957d3b` + IDE `650e7ea6c3e`; V3.5.0.9 docs § 4.1.z5 engine `1792a470e40`.  **V3.5.0.X megaudit IN PROGRESS** at engine (THIS commit; adds both Codex Lane A + Opus Lane B transcripts + plan body documents 5 HIGH closure paths + cross-lane convergence map + memory pointer updates).  Codex Lane A FAIL: 4 HIGH + 1 LOW.  Opus Lane B PASS-WITH-FINDINGS: 2 HIGH + 4 MEDIUM + 3 LOW + 1 INFO (M1 mocha-count claim FALSE -- verified 346 passing).  Cross-lane convergent: 1 HIGH (Codex A-HIGH-1 = Opus H2, partial-invalidate wrong-cell after remote interleaving).  **5 distinct HIGHs to close** (A-HIGH-1/2/3/4 + Opus H1 SetCellFormat tombstone gap) + 1 valid MED (Opus M2 docstring drift).  Estimated 4-8h closure cycle.  V3.5 CANNOT TERMINATE until HIGHs + Opus M2 close in-cycle.  Next session: execute closure plan documented in sub-step 10 body.)
date: 2026-05-24
predecessor_plan: .plans/_archive/2026-05-23_phase-5-7-v3-4-undo-persistence-presence.md (V3.4 phase termination -- undo/redo + .qbook persistence + presence integration; ALL 9 sub-steps SHIPPED + AUDITED including V3.4.0.X cumulative megaudit + closures)
predecessor_v3_4_0_x_audits: docs/audits/2026-05-24-phase-5-7-v3-4-0-x-{codex,opus}.md (V3.4.0.X audit; Opus § F V3.5 ENTRY READINESS is the basis for THIS entry plan)
parent_phase: 5.7 Collaboration IDE Vertical Slice
direction: V3.5 -- IDE-side `rebuild_workbook` consumption + full Op enum cache coverage (formats + names) + partial-invalidate undo strategy + richer sheet operations (rename/delete/reorder) + V3.4.0.5c mid-edit-render guard. The first Phase 5.7 sub-phase that exposes the full `Workbook` across the FFI boundary (V3.4 kept Workbook engine-side-only inside `to_qbook`); enables IDE-side formula rendering + format-aware cell display + sheet management UX. All pre-V3.5 blockers (V3.4.0.X HIGH-1+2+3 + V3.4.0.X MEDIUM-5 iteration-order doc) closed.
current_engine_head: (this commit) V3.5.0.X megaudit-in-progress docs -- both Codex Lane A + Opus Lane B transcripts committed (codex.md 111 lines / 22KB FAIL; opus.md 880 lines / 62KB PASS-WITH-FINDINGS).  Plan body sub-step 10 documents 5 distinct HIGH closure paths (4 Codex + 1 Opus) + cross-lane convergence map + Opus M2 closure.  No engine code changes; HIGHs + Opus M2 await close-in-cycle in the next session.  Predecessor `1792a470e40` (V3.5.0.9 docs § 4.1.z5).
current_ide_head: `650e7ea6c3e` (V3.5.0.7 IDE; unchanged -- audit found IDE-side HIGHs A-HIGH-3 + A-HIGH-4 that await closure in next session).
current_mocha_count: 346 / 346 (V3.5.0.X is audit-only at this point; IDE unchanged).  V3.5 cumulative +92 over V3.4.0.X 254 baseline.  WILL CHANGE in next session when HIGHs close (regression tests added per closure plan in sub-step 10).
current_ql_collab_tests: 106 / 106 (V3.5.0.X is audit-only at this point; ql-collab unchanged).  WILL CHANGE in next session when A-HIGH-1 closure adds the partial-invalidate-remote-interleaving regression test.
current_ql_collab_ws_tests: 42 / 42 (10 lib + 30 ws + 2 V3.1.a relay)
current_engine_workspace: 4472 / 0 baseline (will grow with V3.5 WorkbookSnapshot napi + sheet-ops napi + partial-invalidate undo + format cache tests)
audit_rules_inherited:
  - Rule 1: Stop fresh-session reminders (2026-05-17)
  - Rule 2: Parallel Codex+Opus per phase/wave/step (2026-05-17)
  - Rule 3: Range-aware fn ships need lex+parse+bind+eval coverage (2026-05-21; not V3.5-relevant; pre-V3.5)
  - Rule 4: Negative trait claims need positive compile proof OR per-field walk (2026-05-21; arc terminus 6, V3.5 expected to add 1+ new structs per D3 WorkbookSnapshot)
type: project
---

# V3.5 -- IDE-side `rebuild_workbook` consumption + full Op enum cache + sheet ops + partial-invalidate undo (Phase 5.7, 2026-05-24)

> **NEW SESSION? START HERE.**

Sister-section to `docs/architecture/ide-consumer-contract.md § 4.1.z5` (V3.5.0.9 docs will land it). This plan is the engine-side scoping document.

## Predecessor closure

V3.4 SHIPPED + AUDITED end-to-end across 2 sessions; all 9 sub-steps complete; 29 audit transcripts cumulative across Phase 5.7. See `.plans/_archive/2026-05-23_phase-5-7-v3-4-undo-persistence-presence.md` for the V3.4 plan archive; `docs/audits/2026-05-24-phase-5-7-v3-4-0-x-{codex,opus}.md` for the V3.4.0.X cumulative megaudit transcripts; `docs/architecture/ide-consumer-contract.md § 4.1.z4` for the V3.4 surface spec + audit closures sub-section.

V3.4 cumulative test deltas: ql-collab 76 -> **81** (+5); IDE mocha 181 -> **254** (+73 V3.4-specific).

## V3.5 scope (per V3.4.0.X Opus § F V3.5 ENTRY READINESS)

V3.5 is the first Phase 5.7 sub-phase that exposes the FULL Workbook across the FFI boundary. V3.4 kept Workbook engine-side-only (`to_qbook` materializes it internally; never crosses FFI). V3.5 surfaces it for IDE-side formula rendering, format-aware cell display, and sheet management UX.

Five major sub-areas:

1. **WorkbookSnapshot napi shape** (D3): expose a flattened, JSON-serializable view of the full Workbook for IDE rendering. NOT a 1:1 mirror of `ql-storage::Workbook` (whose API is large + tightly coupled). A flattened view: sheets + cells + names + formats + tables.

2. **Full Op enum cache coverage** (D1): extend `CellState` to carry format info (V3.4.0.2's value+formula -> V3.5's value+formula+format). RegisterFormat / SetName / RenameSheet land in their own session-wide caches.

3. **Partial-invalidate undo strategy** (D2): V3.4.0.2 always full-rebuilds on undo. V3.5 makes this incremental for cell-keyed ops: undo of `Op::PutValue { sheet, row, col, .. }` pops `(sheet, row, col)` from cache + replays only that cell's op-log subset. Full-rebuild stays for session-wide ops (SetName / RenameSheet).

4. **Richer sheet operations** (D4): `renameSheet(oldName, newName)` / `deleteSheet(name)` / `moveSheet(name, newIndex)` napi + IDE multi-sheet UX integration.

5. **R-V3.4-3 KNOWN-GAP closure** (D5 / V3.4.0.5c follow-up): host-level mid-edit-render guard. `presenceRepaintInFlight` boolean OR typing-counter checked in `tickPollRemote` before `render()` -- skip the render and re-queue for the next tick if mid-edit. Closes the host-driven `webview.html` rebuild during mid-edit `<input>` destroy hazard.

## V3.5 design decisions (locked at V3.5.0.1)

### D1 Op-enum cache shape extension

**Question:** how does `CellState` extend for richer Op variants?

**Options:**
- (a) Extend `CellState` with `format: Option<FormatId>` for cell-keyed format ops (SetCellFormat). RegisterFormat / SetName / RenameSheet land in SEPARATE session-wide HashMaps on `CollabSession`.
- (b) Promote `last_snapshot` to a richer `CacheState { cells, formats, names, tables }` with sub-maps per Op category. Each sub-map gets its own per-Op invalidation discipline.

**Tradeoffs:**
- (a) is minimal-divergence from V3.4.0.2; per-field LWW pattern carries cleanly; new fields = new positive Rule 4 walks but no architectural change.
- (b) is forward-compat for V3.6+ when cache may serve queries beyond `snapshot_cells` (e.g., `snapshot_formats`, `snapshot_names`). But V3.5 doesn't need (b) yet.

**Decision: A.** Extend `CellState` with `format: Option<FormatId>` for V3.5.0.5. Session-wide format/name/table state stays on `Workbook`; V3.5's WorkbookSnapshot napi (D3) surfaces them by flattening the Workbook. V3.6+ can promote to (b) if profiling shows the cache needs to serve more queries.

**Rule 4 trigger:** YES. `CellState` field walk must re-run when `format: Option<FormatId>` lands. `FormatId` is `enum { Builtin(u32), Custom(PeerId, u32) }` per D-1; both variants are `Send + Sync` (audited at D-1 megaudit step 8). Composition with `Option<T>` + the existing CellState fields preserves Send + Sync. Arc terminus stays at 6 if the walk is done correctly.

### D2 Partial-invalidate undo strategy

**Question:** undo's cache invalidation -- full-rebuild (V3.4.0.2 current) or partial-invalidate (V3.5+ goal)?

**Options:**
- (a) Always partial-invalidate. Undo of `Op::PutValue { sheet, row, col, .. }` pops `(sheet, row, col)` from cache + walks only the subset of ops touching that cell to reconstruct LWW. Same for PutFormula / ClearFormula. Session-wide ops (SetName etc.) trigger full-rebuild.
- (b) Always full-rebuild (current V3.4.0.2 behavior). Simple, slow at scale.
- (c) Profile-driven: full-rebuild until `op_count > N`, then switch. Adds complexity.

**Tradeoffs:**
- (a) is the V3.5 user-visible improvement (undo on a 100k-cell workbook becomes O(cell-op-history) instead of O(N)). Adds complexity in the cache-update path.
- Full-rebuild remains the fallback for any cache-cold or invariant-violation case.

**Decision: A** (partial-invalidate for cell-keyed ops; full-rebuild for session-wide ops). V3.5.0.6 implementation: new `invalidate_cell(&mut self, sheet, row, col)` method on CollabSession that walks `self.log.iter()` filtering by `(sheet, row, col)` + replays into a fresh `CellState`. `undo()` / `redo()` for cell-keyed Op variants call `invalidate_cell` only; non-cell-keyed Op undo falls back to `rebuild_snapshot_cache`.

**Risk:** the iteration-order invariant (V3.4.0.X Opus M5 closure) MUST hold for partial-invalidate's correctness. Pin a regression test that compares partial-invalidate result against full-rebuild on the same op log.

### D3 WorkbookSnapshot napi shape

**Question:** what shape does the napi expose for IDE-side rendering?

**Options:**
- (a) Mirror full `ql-storage::Workbook` API across FFI (sheets, cells, names, formats accessors). Large API surface; tightly coupled to internal types.
- (b) Flattened JSON-serializable snapshot: `WorkbookSnapshot { sheets: Vec<SheetSnapshot>, names: Vec<NamedRangeJson>, formats: Vec<FormatDefJson> }` where each SheetSnapshot has `{ id, name, cells: Vec<CellSnapshot> }`. JSON-friendly; mirrors V3.4.0.2 CellState pattern at higher level.
- (c) Lazy: cell-by-cell accessors (`get_cell(sheet, row, col)`) without snapshot. Avoids the large-payload cost but doesn't fit the V3.5 IDE renderer (which wants a full state to render at once).

**Tradeoffs:**
- (b) is the V3.5 sweet spot. Pre-flattened; IDE iterates once; CSP-safe data-block embedding.
- (a) couples engine internals to napi. Avoid.
- (c) doesn't support the IDE renderer's batch read pattern.

**Decision: B.** WorkbookSnapshot napi struct with `#[napi(object)]`. Field design:
```rust
#[napi(object)]
pub struct WorkbookSnapshotJson {
    pub sheets: Vec<SheetSnapshotJson>,
    pub names: Vec<NamedRangeJson>,
    pub formats: Vec<FormatDefJson>,
}
#[napi(object)]
pub struct SheetSnapshotJson {
    pub id: u16,
    pub name: String,
    pub cells: Vec<CellSnapshotJson>,
}
#[napi(object)]
pub struct CellSnapshotJson {
    pub row: u32,
    pub col: u32,
    pub value: Option<CellWireValueJson>,
    pub formula: Option<String>,
    pub format: Option<FormatIdJson>,
}
```
(Exact field shape may evolve at V3.5.0.2 implementation; the shape above is the entry plan.)

**Performance note:** large workbooks (100k+ cells) will produce large JSON. V3.6+ may add incremental snapshot deltas; V3.5 ships the full snapshot.

**Rule 4 trigger:** YES. 4 new `#[napi(object)]` structs (`WorkbookSnapshotJson`, `SheetSnapshotJson`, `CellSnapshotJson`, plus `NamedRangeJson` + `FormatDefJson` + `FormatIdJson` etc.). All field-by-field walks. Composition of primitives + Option<T> + Vec<T>. Arc terminus stays at 6 with positive Send + Sync walks.

### D4 Richer sheet operations

**Question:** which sheet operations does V3.5 surface?

**Required (from Opus § F item 7):**
- `renameSheet(oldName, newName)` -> emits `Op::RenameSheet`.
- `deleteSheet(name)` -> emits `Op::RemoveSheet` (engine: verify this exists per Phase 4.8.i.2 work; add if missing).
- `moveSheet(name, newIndex)` -> reordering.

**Decision: ALL THREE shipping at V3.5.0.3.** napi + IDE typed wrappers + IDE multi-sheet command integration (`quantlab.quantbookSheetRename` / `...SheetDelete` / `...SheetMove`).

**Pre-V3.5 engine audit:** verify `Op::RenameSheet` + `Op::RemoveSheet` + (any reorder Op) exist on `ql-oplog/src/op.rs`. If not, V3.5.0.3 adds them.

**IDE UX caveat:** sheet deletion is destructive; the IDE command MUST `showWarningMessage` with confirmation before emitting the op. Sheet rename is non-destructive but cross-references in formulas need `repair_sheet_rename_chain` from Phase 5.3 -- verify the V3.5 napi calls trigger repair correctly.

### D5 R-V3.4-3 mid-edit-render guard (V3.4.0.5c carry)

**Question:** how does V3.5 close the R-V3.4-3 KNOWN-GAP (host-driven `webview.html` rebuild during mid-edit `<input>` lifetime destroys the input)?

**Options:**
- (a) Boolean flag `presenceRepaintInFlight` set by dispatcher's `presenceUpdate` arm when `state.typing === true`, cleared on `state.typing === false`. `tickPollRemote` checks the flag before `render()`; if true, skip + re-queue.
- (b) Counter `activeEditCount` incremented on each `typing: true`, decremented on each `typing: false`. Allows multi-cell edit tracking (V3.6+).
- (c) Direct check: query the panel's webview state via `vscode.window.activeTextEditor`-style accessor. Requires new API surface.

**Tradeoffs:**
- (a) is the minimal V3.4.0.5c fix; matches the V3.4.0.1 D4 boolean-flag race-guard pattern (mirrors `activeInput` from V3.3.0.4).
- (b) is forward-compat for V3.6+ multi-cell edit scenarios.
- (c) is the cleanest but requires more design + new API.

**Decision: A.** Boolean flag `presenceRepaintInFlight` on `CellGridPanel`. Set by dispatcher when `presenceUpdate.state.typing === true`; cleared when `typing === false`. `tickPollRemote` at line ~474-492 in `cellGridPanel.ts` checks the flag before calling `this.render()` on `merged` ticks; if true, skip the render + log + re-queue (the next merged tick will fire render). The re-queue path avoids losing the merged ops -- the cache is updated by `pollRemote()`'s engine call BEFORE the render gate; only the visual repaint is deferred.

**Risk:** stale-presence + transport-disconnect could leave `presenceRepaintInFlight = true` forever, blocking all renders. Mitigation: re-init the flag to false on `CellGridPanel.show()` + on `attachTransport` lifecycle.

**Test coverage:** mocha pin for the host-level guard logic (separate from the webview script's `activeInput` guard which V3.3.0.4 covers).

## V3.5 sub-step rollout (planned)

1. **V3.5.0.1 -- decision lock** ✅ SHIPPED at engine (THIS commit, docs-only). Decisions D1-D5 above.
2. **V3.5.0.2 -- engine `WorkbookSnapshot` napi (D3)** ✅ SHIPPED at engine (THIS commit pair) + IDE (paired commit; minimal type+wrapper scaffold so mocha can pin the napi contract; V3.5.0.4 commands still pending).  4 new `#[napi(object)]` structs at engine FFI boundary: `CellValueJson` (discriminated union: kind + 4 optional payload fields number/boolean/text/error), `CellSnapshotJson` (row + col + Option<CellValueJson> value + Option<String> formula), `SheetSnapshotJson` (id u32 + name String + Vec<CellSnapshotJson> cells), `WorkbookSnapshotJson` (Vec<SheetSnapshotJson> sheets).  `From<CellWireValue> for CellValueJson` impl handles all 5 CellWireValue variants.  New napi method `CollabSession::workbook_snapshot() -> Result<WorkbookSnapshotJson>` routes through `inner.rebuild_workbook(&default_registry())` for sheet count + names + uses V3.3.0.3 incremental cache via `snapshot_cells(sheet_id)` for cell content (workbook DISCARDED after metadata extraction).  **DEFERRED to V3.5.0.5+**: `names: Vec<NamedRangeJson>` + `formats: Vec<FormatDefJson>` fields (additive; no V3.5.0.2 shape break).  IDE: types.ts adds 4 interfaces (`CellValueJson` / `CellSnapshotJson` / `SheetSnapshotJson` / `WorkbookSnapshotJson` with `?:` optional fields reflecting napi-rs `Option::None -> JS undefined` serialization) + `CollabSessionInstance.workbookSnapshot()` method.  session.ts adds typed wrapper `workbookSnapshot(session): WorkbookSnapshotJson`.  +11 mocha tests (254 -> 265): empty session / single addSheet / addSheet+PutValue / multi-sheet id-ordering / (row,col)-ascending sort / top-level JSON shape stability for V3.5.0.5 forward-extend / SheetSnapshotJson shape / CellSnapshotJson shape (absent formula key) / CellValueJson discriminator (non-active payload keys absent) / rebuild_workbook semantic (addSheet-only sheets appear) / round-trip via to_qbook+from_qbook preserves snapshot equality.  **Rule 4 arc terminus stays at 6** -- 4 new structs all positive Send+Sync walks (primitives / Option<T> / Vec<T> composition; `String` Send+Sync trivially); per-field walks documented in struct docstrings; 0 new triggers.  **Discovery**: napi-rs serializes `Rust Option<T>::None` as ABSENT JS property (not `null`); TS interfaces use `?:` shape + tests check `=== undefined` (not `=== null`).  IDE consumption V3.5.0.4 still pending (sheet rename/delete/move commands + UI integration; the typed wrapper alone is V3.5.0.2 test-infrastructure).
3. **V3.5.0.3 -- engine sheet operations napi (D4).** SPLIT into 3 sub-sub-steps per V3.5.0.3a scope discovery: only `Op::RenameSheet` exists today (Phase 4.6.C / 5.3); `Op::RemoveSheet` + `Op::MoveSheet` do NOT exist and adding them requires fresh wire-format + replay + CRDT semantic locks (concurrent delete + concurrent move are non-trivial design problems).
   - **V3.5.0.3a (renameSheet napi)** ✅ SHIPPED at engine (THIS commit pair) + IDE (paired commit).  Thin napi wrapper `CollabSession::rename_sheet(id: u32, new_name: String)` validates id <= u16::MAX + pre-rebuilds workbook to capture current sheet name (advisory `old_name` per Phase 5.3 step 2 Codex M2; recorded for debuggability + future strict-mode replay) + appends `Op::RenameSheet { id, old_name, new_name }`.  **Contract divergence from `WorkbookRuntime::rename_sheet`**: napi does NOT rewrite formula text; formula references to the old sheet name stay stale in the cache until next `workbookSnapshot` / `exportToQbook` call triggers `rebuild_workbook` + `repair_sheet_rename_chain` (Phase 5.3 step 3).  Acceptable for V3.5.0.3a scope because IDE flow always reads through workbookSnapshot.  IDE typed wrapper `renameSheet(session, id, newName)` + `CollabSessionInstance.renameSheet` interface method.  +8 mocha tests (265 -> 273): op-append; snapshot reflects new name; cells preserved; bad_argument on id > u16::MAX; bad_argument on non-existent sheet; multi-rename last-write-wins; cross-peer convergence via mergeBytes + Phase 5.3 repair chain; .qbook round-trip preserves rename.  **Per-call cost note**: O(N) replay per rename (for the advisory `old_name` lookup).  V3.5.0.6+ partial-invalidate undo may eliminate via a `current_sheet_name(id)` accessor.  Rule 4: no new structs; arc terminus stays at 6.
   - **V3.5.0.3b (deleteSheet napi + Op::RemoveSheet)** ✅ SHIPPED at engine (THIS commit pair) + IDE (paired commit).  New `Op::RemoveSheet { id: SheetId }` wire variant added to `crates/ql-oplog/src/op.rs` (serde-tagged additive; no `OPLOG_SCHEMA_VERSION` bump needed).  CRDT semantic lock chosen: **tombstone preserving id slot** (rejected hard-delete because it would shift subsequent sheet ids + break post-delete `Op::PutValue { sheet: N, .. }` references; rejected `#REF!` formula substitution -- defer to V3.6+ if user-feedback requires).  Engine impl: new `Workbook.removed_sheets: HashSet<SheetId>` field + `remove_sheet(id)` + `is_sheet_removed(id)` accessors in `crates/ql-storage/src/workbook.rs`.  New `apply_op` match arm in `crates/ql-oplog/src/replay.rs` for `Op::RemoveSheet` (calls `workbook.remove_sheet(id)`; idempotent via HashSet; out-of-range ids silently ignored for cross-peer cause-merge friendliness).  `apply_op` for cell-keyed ops (PutValue / PutFormula / ClearFormula) extended with `if workbook.is_sheet_removed(*sheet) { return Ok(()) }` short-circuit AFTER `validate_cell` -- writes to tombstoned sheets silently dropped per CRDT idempotency contract; concurrent {PutValue, RemoveSheet} ordering: PutValue first -> writes then tombstone, RemoveSheet first -> PutValue silently dropped; deterministic causal-merge order converges peers.  napi `CollabSession::delete_sheet(id: u32)` validates id <= u16::MAX + pre-rebuilds for existence check + appends `Op::RemoveSheet { id }`.  Already-tombstoned sheet is NOT a bad_argument (CRDT idempotency; second delete is replay-noop).  napi `workbook_snapshot` extended with `if workbook.is_sheet_removed(sheet_id) { continue }` filter so the IDE renderer omits tombstoned sheets.  IDE: `CollabSessionInstance.deleteSheet(id)` interface + `deleteSheet(session, id)` typed wrapper.  +11 mocha tests (273 -> 284): op-append; snapshot filters tombstones; cells on tombstoned sheet unreachable; writes to tombstoned sheet silent-dropped; double-delete idempotent; bad_argument id > u16::MAX; bad_argument non-existent sheet; cross-peer convergence via mergeBytes; id-stability after delete (sheet 2 cells survive delete-of-sheet-1); .qbook round-trip preserves tombstone; rename + delete co-op (rename doesn't affect tombstoned sheet).  Rule 4: no new Rust structs (Workbook just gains a `removed_sheets: HashSet<SheetId>` field); arc terminus stays at 6.  **V3.6+ deferrals**: formula `#REF!` substitution for cross-sheet refs to deleted sheets; `Op::RestoreSheet` un-delete; reclamation pass that compacts truly-orphaned sheet storage.
   - **V3.5.0.3c (moveSheet napi + Op::MoveSheet)** ✅ SHIPPED at engine (THIS commit pair) + IDE (paired commit).  V3.5.0.3 D4 SHEET-OPS SUITE COMPLETE (a + b + c).  CRDT semantic decision lock chosen at V3.5.0.3c entry: **option (a) display-order overlay** (id stays stable; mirrors V3.5.0.3b tombstone's id-stability strategy).  Rejected: (b) replay-time remapping table -- breaks "ops are immutable on the wire"; (c) IDE-side overlay -- engine is the right home for display order.  Engine impl: new `Op::MoveSheet { id: SheetId, new_index: u32 }` wire variant (serde-additive; no schema bump).  New `Workbook.sheet_display_order: Vec<SheetId>` field (defaults to `[0, 1, ..., sheet_count() - 1]` in append order via the existing `try_add_sheet_with_chunk_rows` hook updated to push the newly-assigned id).  New `Workbook::move_sheet(id, new_index)` + `sheet_display_order()` accessors.  `move_sheet` impl: linear-search current_pos; remove from current_pos; clamp new_index to `[0, len]`; insert -- idempotent if `id` not in order (cross-peer causal-merge-friendly); out-of-range new_index clamps to end.  New `apply_op` match arm for `Op::MoveSheet` calls `workbook.move_sheet(id, new_index)`.  napi `CollabSession::move_sheet(id: u32, new_index: u32)` validates id <= u16::MAX + pre-rebuilds for existence check + appends `Op::MoveSheet`.  napi `workbook_snapshot` extended to iterate `sheet_display_order().to_vec()` instead of `0..sheet_count()`; defensive fallback enumerates missing ids at end (resilient against future engine-internal bugs that bypass `try_add_sheet_with_chunk_rows`); tombstone filter applied AFTER display-order resolution (move-on-tombstoned-sheet silently applies; snapshot filters tombstones).  IDE: `CollabSessionInstance.moveSheet(id, newIndex)` interface + `moveSheet(session, id, newIndex)` typed wrapper.  +13 mocha tests (284 -> 297): op-append; snapshot reflects reorder; id stability (cells reachable by id after move); multi-move sequence; new_index out-of-range clamps; same-position no-op; bad_argument id > u16::MAX; bad_argument non-existent; cross-peer convergence via mergeBytes; move + delete interaction (tombstoned filtered post-move); move-tombstoned-sheet silent-apply; .qbook round-trip preserves display order; default-order regression pin (V3.5.0.3b compat).  Rule 4: no new Rust structs (Workbook gains a `Vec<SheetId>` field; primitive composition; Send + Sync trivial); arc terminus stays at 6.  **V3.6+ deferrals**: per-peer display-order overlay (users see different reorder preferences vs shared canonical order; V3.6+ if user-facing concern surfaces); display-order undo via `invalidate_cell`-style approach (V3.5.0.6 partial-invalidate undo work).
4. **V3.5.0.4 -- IDE WorkbookSnapshot consumption + sheet management UI commands.** SPLIT into 3 sub-sub-steps per scope discovery (~2-3 sessions total): 0.4a sheet management commands (uses V3.5.0.3 engine napi directly; self-contained); 0.4b cell-grid rendering migration to workbookSnapshot()-driven (touches existing render path); 0.4c sheet-tabs UI scaffold (optional polish; may defer to V3.5.1+).
   - **V3.5.0.4a (sheet management commands)** ✅ SHIPPED at engine (THIS commit; plan only) + IDE (paired commit; no engine changes -- 0.4a consumes V3.5.0.3 engine napi already shipped).  4 new vscode commands wrapping V3.5.0.3 sheet ops: `quantlab.quantbookSheetAdd` (showInputBox -> `addSheet(session, name.trim(), 1000)`); `quantlab.quantbookSheetRename` (showQuickPick over `workbookSnapshot.sheets` for source + showInputBox for newName seeded with current name + validates non-empty + validates different-from-current -> `renameSheet(session, id, newName.trim())`); `quantlab.quantbookSheetDelete` (showQuickPick for source + showWarningMessage `{ modal: true }` confirmation with destructive-warning copy -> `deleteSheet(session, id)`); `quantlab.quantbookSheetMove` (2-step picker: showQuickPick source + showQuickPick target position with adjacency labels -> `moveSheet(session, id, newIndex)`).  All 4 follow V3.4.0.4b Save As pattern: `panels[0]` arbitrary-pick from `CellGridPanel.activeLocalPanels()`; try/catch error handling with `showErrorMessage` + output channel logging.  2 new pure helpers in `cellGridLogic.ts`: `buildSheetManagementQuickPickItems(sheets, currentSheet)` (id+name labels, `(current)` annotation, mocha-testable) + `buildSheetMovePositionItems(sheets, sourceSheetId)` (position-picker with `(first)` / `(last)` / `(between Sheet X and Sheet Y)` adjacency labels per V3.5.0.3c display order).  package.json + package.nls.json registrations.  +16 mocha tests (297 -> 313): buildSheetManagementQuickPickItems (8: empty / single / multi-sheet / current-annotation / non-existent-current suppression / display-order preservation / name field exposure / special-chars in name) + buildSheetMovePositionItems (8: empty / two-sheets / three-with-middle-source-adjacency / current-position-marker / source-at-first / source-not-found / four-sheets-source-at-end / display-order-permutation-preserved).  **NO automatic panel re-render after sheet op** (V3.5.0.4a scope intentional): pollRemote tick (V3.2.c, 1s) picks up new state at next render via existing snapshot path; V3.5.0.4b will migrate render path to workbookSnapshot()-driven rendering which reflects sheet ops immediately.  **Command-layer wiring tests** (vscode command invocation) deferred to V3.5.1+ scope per V3.4.0.X Opus § F V3.5 ENTRY READINESS guidance (needs vscode-test integration; pure-helper tests cover the testable surface for V3.5.0.4a).  Rule 4: no new Rust structs; arc terminus stays at 6.
   - **V3.5.0.4b (cell-grid rendering migration to workbookSnapshot)** ✅ SHIPPED at engine (THIS commit; plan only) + IDE (paired commit; no engine changes).  V3.5.0.4b entry decision: **option (i) minimal-change** (pre-fetch full snapshot via workbookSnapshot but render only `target.sheet`'s cells; preserves panel-per-sheet V3.3.0.1 D2 semantic).  Rejected option (ii) render-all-sheets-with-active-focused at V3.5.0.4b scope (that's V3.5.0.4c's sheet-tabs concern).  **IDE impl**: `cellGridPanel.ts::render()` now calls `workbookSnapshot(this.session)` once, calls new `extractSheetSnapshot(snapshot, this.sheet)` to extract a `QuantbookCellSnapshot` shape, then passes to `buildHtml` unchanged (preserves V3.2.a html-building contract).  New pure helper `extractSheetSnapshot(snapshot, sheetId): QuantbookCellSnapshot | null` in `cellGridLogic.ts` bridges V3.5.0.2 CellSnapshotJson discriminated union -> V3.2.a QuantbookCellValue strict union (optional payload napi-rs Option::None -> mandatory payload qualified by `kind`); skips formula-only cells (V3.4.0.X MEDIUM-1 carry: matches `export_snapshot`'s `filter_map(state.value)` behavior); throws on unknown kind (binding-drift signal mirroring `snapshot_format_version !== 1` throw in `exportCellSnapshot`).  **Reactive panel title** (clears V3.3.0.X reactive-panel-title backlog): render() sets `this.panel.title` each call using `wbSnapshot.sheets.length` -- counts addSheet'd-but-empty sheets (was V3.3.0.X cache-based listSheets which only counted PutValue-populated sheets); also includes the active sheet's NAME from V3.5.0.3a renames.  show()-time title using listSheets stays as initial value (corrected within ms by render()).  **Sheet-not-found (tombstone race)**: if user runs V3.5.0.4a Delete Sheet on `this.sheet` while panel is open, extractSheetSnapshot returns null; render() falls back to empty entries + console.warn (No-Fallbacks tension acknowledged in docstring -- alternative is permanent red error banner; empty + warn is the lesser-evil here).  **Per-render cost**: workbookSnapshot is O(N) replay; render fires per onCommit (cell edit) + per pollRemote tick (~1s).  V3.4-scale sessions accept the cost; V3.6+ may add incremental snapshot deltas if profiling justifies.  +17 mocha tests (313 -> 330) across 2 suites: extractSheetSnapshot pure (11 tests) + extractSheetSnapshot via real workbookSnapshot session round-trip (6 tests including V3.5.0.4b-vs-V3.4.0.X path equality pin).  Rule 4: no new Rust structs; arc terminus stays at 6.
   - **V3.5.0.4c (sheet-tabs UI scaffold)** ⏭ PENDING (next session+; may defer to V3.5.1+).  Render `workbookSnapshot.sheets` as a horizontal tab strip atop the cell-grid panel.  Click switches the active sheet (panel-per-sheet still per V3.3.0.1 D2; or a single-panel multi-tab redesign -- decide at V3.5.0.4c entry).  Optional polish; if user signal is weak, can defer entirely.  Estimated 1-2 days if shipped.
5. **V3.5.0.5 -- engine `CellState` format extension (D1)** ✅ SHIPPED at engine (THIS commit) + IDE (paired commit; passthrough types + tests).  Scope narrowed per V3.5.0.5 entry decision: ship per-cell `Op::SetCellFormat` cache integration ONLY; defer session-wide `Op::RegisterFormat` FormatTable cache to V3.6+ (per-cell format is the minimum surface to unblock format-aware rendering; session-wide registry is a larger design).  **Engine impl**: `CellState` extended with `format: Option<FormatId>` field in `crates/ql-collab/src/session.rs` (full Rule 4 per-field walk in docstring: `FormatId = Builtin(u32) | Custom(PeerId, u32)`; `PeerId(pub u64)`; all primitives + Copy + Send + Sync trivially; arc terminus stays at 6).  New `CacheEffect::SetCellFormat { key, format: Option<FormatId> }` variant.  `collect_cache_effects` extended to emit the new variant on `Op::SetCellFormat` (converts wire `FormatIdWire` -> storage `FormatId` via `to_storage()`); BatchCommit recursion carries.  `apply_cache_effect` for the new variant: `format: Some(_)` -> `entry().or_default().format = Some(id)`; `format: None` -> `get_mut + skip-if-absent` (mirrors ClearFormula's ghost-entry-avoidance).  **Ghost-entry-avoidance extended** (V3.4.0.X MEDIUM-1 carry): the all-fields-None check now includes format -- `(value && formula && format).is_none()` triggers cache key removal.  Append_op docstring updated with V3.5.0.5 4-cell-keyed-variants note.  **napi impl**: new `#[napi(object)] FormatIdJson` struct in `crates/ql-bindings-node/src/lib.rs` (tagged-union mirror: `kind: String` + optional `builtin: u32` + optional `custom_peer: BigInt` + optional `custom_counter: u32`); `impl From<FormatId> for FormatIdJson` handles both variants losslessly (PeerId widened to BigInt via `BigInt::from(peer.0)`).  `CellSnapshotJson` extended with `format: Option<FormatIdJson>` field.  `workbook_snapshot` napi threads `state.format.map(FormatIdJson::from)` through.  Rule 4: `FormatIdJson` per-field walk documented (kind: String + Option<u32> + Option<BigInt(=Vec<u64>+sign)> + Option<u32>; all Send + Sync; 0 new triggers; arc terminus stays at 6).  **IDE impl**: types.ts adds `FormatIdJson` interface + `CellSnapshotJson.format?: FormatIdJson` optional field.  V3.5.0.5 scope is PASSTHROUGH only: `extractSheetSnapshot` deliberately DROPS format (V3.2.a QuantbookCellSnapshot has no format field; buildHtml not format-aware until V3.6+).  +12 ql-collab tests (81 -> 93): set_cell_format_writes_state_format / preserves_value_and_formula_per_field_lww / put_value_after_set_cell_format_preserves_format / lww_overwrites_previous_format / clear_cell_format_clears_state_format / format_only_clear_removes_cache_key / clear_format_on_never_set_cell_is_noop / cross_peer_set_cell_format_converges_via_merge_bytes / rebuild_snapshot_cache_round_trip_preserves_format / batch_commit_with_set_cell_format_recurses_into_cache / set_cell_format_custom_variant_via_cache / undo_preserves_format_lww_after_full_rebuild.  +7 IDE mocha (330 -> 337): CellSnapshotJson type-shape pins (3) + extractSheetSnapshot drops-format-V3.5.0.5-scope pins (2) + workbookSnapshot passthrough pins (2: no-SetCellFormat-ops -> format-undefined / napi-rs Option::None -> absent key).  **V3.6+ deferrals**: session-wide RegisterFormat FormatTable cache (would also surface in WorkbookSnapshotJson.formats: Vec<FormatDefJson>); format-aware buildHtml rendering (number format / date format / currency / etc.); IDE write-path (napi `appendSetCellFormat` + commands like quantbookSetCellFormat).
6. **V3.5.0.6 -- engine partial-invalidate undo (D2)** ✅ SHIPPED at engine (THIS commit).  IDE-side unchanged (engine-only sub-step).  **New `invalidate_cell(&mut self, sheet, row, col) -> Result<(), CollabSessionError>` method** on CollabSession (pub(crate)) drops the cell entry then walks the op log applying ONLY effects targeting `(sheet, row, col)` via the existing `collect_cache_effects` + `apply_cache_effect` helpers (BatchCommit recursion carries; ghost-entry-avoidance carries -- if the walk leaves all-fields-None, the entry stays removed).  **New `affected_cells_for_partial_invalidate(op) -> Option<Vec<(u16,u32,u32)>>` classifier**: returns `Some(deduped+sorted cells)` for cell-keyed ops (PutValue / PutFormula / ClearFormula / SetCellFormat) OR BatchCommit of only-cell-keyed inner ops; returns `None` for non-cell-keyed (AddSheet / RenameSheet / RemoveSheet / MoveSheet / RegisterFormat / table ops / mixed BatchCommit -- conservative fallback signal).  **undo/redo dispatch refactored**: captures the most-recent visible op BEFORE retract (undo) / AFTER append (redo); if log shrank/grew by exactly 1 AND the captured op classifies as cell-keyed, calls `invalidate_cell` per affected cell; otherwise falls back to `rebuild_snapshot_cache` (preserves V3.3.0.X HIGH-1 closure guarantee).  Loro UndoManager's "retract most-recent local op" + visible-log-shrink-by-1 (pinned by `undo_retracts_visible_op_from_op_log_len` test) means peek-most-recent-visible-op is correct WHEN no remote op landed after the local op; the shrinkage-by-exactly-1 check defensively falls back to full rebuild in the mixed-remote-ops case.  **+13 ql-collab tests (93 -> 106)**: invalidate_cell_matches_full_rebuild_for_single_put_value / preserves_unrelated_cells_byte_identical / ghost_entry_removal_after_full_clear / undo_partial_invalidate_matches_full_rebuild_{put_value,put_formula,clear_formula,set_cell_format} (4 ops; side-by-side equality) / undo_falls_back_to_full_rebuild_for_non_cell_keyed_op / undo_batch_commit_cell_keyed_uses_partial_invalidate / redo_partial_invalidate_matches_full_rebuild / undo_partial_invalidate_via_multiple_cells_in_batch / affected_cells_helper_returns_none_for_non_cell_keyed / affected_cells_helper_returns_dedup_sorted_for_cell_keyed.  **Cost story** (V3.5.0.6 ship): invalidate_cell walks the full log (O(N)) BUT only writes ONE HashMap entry (versus full rebuild's O(N) walk + O(cells) writes).  Real performance win (avoiding the full walk) needs a per-cell op-index that V3.5.0.6 does NOT add -- V3.6+ scope.  V3.5.0.6 ships the **architectural shape** (cleaner semantics: undo touches specific cells; foundation for V3.6+ incremental optimizations; cells unaffected by the undo retain their existing CellState entries).  **Side-by-side equality pin** (the critical correctness invariant): for every supported op shape, partial-invalidate post-undo cache EQUALS the forced-full-rebuild post-undo cache.  Rule 4: no new Rust structs; arc terminus stays at 6.  **V3.6+ deferral**: per-cell op-index (would make invalidate_cell O(ops-for-this-cell)); Loro UndoManager `on_pop` callback wiring (would surface retracted-op shape directly without peek-most-recent).
7. **V3.5.0.7 -- IDE mid-edit-render guard (D5 / R-V3.4-3 KNOWN-GAP closure)** ✅ SHIPPED at engine (THIS commit; plan + § 4.1.z4 R-V3.4-3 closure update) + IDE (paired commit; cellGridPanel.ts + cellGridLogic.ts + tests).  **R-V3.4-3 STATUS: DEFERRED (V3.4.0.5b) -> KNOWN-GAP (V3.4.0.X Opus M2) -> CLOSED (V3.5.0.7).**  **IDE impl**: new `_presenceRepaintInFlight: boolean` field on CellGridPanel (init false, defensive reinit on dispose via setPresenceTyping(false)).  New `setPresenceTyping(typing)` method atomically sets the flag + arms/clears a 30s `PRESENCE_TYPING_WATCHDOG_MS` setTimeout (stuck-true mitigation -- auto-clear if typing:false never arrives; covers panel-hung / window-closed-mid-edit / webview-crash edge cases; auto-clear logs to attachment's output channel).  DispatchDeps gains optional `onLocalTyping?(typing: boolean): void` callback (optional for backward compat with V3.4.0.X/V3.5.0.5 tests).  presenceUpdate dispatcher arm fires `deps.onLocalTyping?(s.typing)` AFTER `updatePresence` succeeds (shape + numeric validation failures + engine throws DON'T fire -- host flag stays in sync with engine state).  CellGridPanel.handleIncoming wires `onLocalTyping -> this.setPresenceTyping` with `_disposed` short-circuit.  **tickPollRemote skip**: `'merged'` branch checks `this._presenceRepaintInFlight` BEFORE `this.render()`; when true, logs `SKIPPING render (presenceRepaintInFlight; local user mid-edit). Will retry on next tick.` + returns.  Deferred render fires on next 1s pollRemote tick once typing:false arrives; merged op stays in engine cache so no data loss.  Dispose handler calls `setPresenceTyping(false)` to cancel any pending watchdog.  **+9 mocha tests (337 -> 346)**: presenceUpdate typing:true/false fires once; sequence true/false/true/false history; shape-validation failure (missing state) skips fire; shape-validation failure (typing missing) skips fire; numeric-validation failure (NaN row) skips fire; backward-compat (onLocalTyping omitted -- dispatcher doesn't crash); engine-throw via Proxy session (updatePresence throws -> NO fire + error reply); non-presenceUpdate types (putValue / unknown) don't fire onLocalTyping.  **Watchdog smoke** + **two-window live race verification** deferred to V3.5.0.9 § 4.1.z5 live smoke procedure (vscode-test integration scope per V3.4.0.X Opus § F V3.5 ENTRY READINESS).  Rule 4: no new Rust types; arc terminus stays at 6.
8. **V3.5.0.8 -- mocha + ql-collab tests** (~20-30 new): cell-keyed undo partial-invalidate matches full-rebuild; sheet rename/delete/move round-trip via WorkbookSnapshot; format-cache LWW + cross-peer convergence; mid-edit-render guard prevents `<input>` destruction; WorkbookSnapshot napi shape stability across V3.5 sub-step iterations.
9. **V3.5.0.9 -- docs** ✅ SHIPPED at engine (THIS commit; docs-only).  Inserted `ide-consumer-contract.md § 4.1.z5` (~350 lines; new section between § 4.1.z4 end and the existing `---` separator at line 1300).  Mirrors V3.4.0.7 § 4.1.z4 template structure: status line + engine + IDE HEAD trail (V3.5.0.1 -> 0.7); V3.5.0.1 decision lock table (5 D-decisions mapped to sub-steps); V3.5.0.2 WorkbookSnapshot napi (D3) full TS interface + per-call cost note + tombstone filter + Rule 4 walk; V3.5.0.3 D4 sheet-ops suite (a/b/c) with CRDT semantic locks documented + forward-compat caveat for pre-V3.5.0.3b binaries; V3.5.0.4 IDE consumption (0.4a commands + 0.4b render migration + reactive title + sheet-not-found tombstone-race fallback); V3.5.0.5 CellState.format extension (D1) per-field LWW + ghost-entry-avoidance extension + napi FormatIdJson + V3.5.0.5 IDE passthrough scope; V3.5.0.6 partial-invalidate undo (D2) with invalidate_cell + classifier + shrink-by-1 defensive fallback + cost story + side-by-side equality pin; V3.5.0.7 mid-edit-render guard (D5 / R-V3.4-3 KNOWN-GAP CLOSED) full closure record + watchdog + onLocalTyping fire semantic; 5 drift hazards for V3.6+ maintainers (CellState shape evolution; new cell-keyed Op variant integration; WorkbookSnapshot additivity; per-cell op-index design; UndoManager on_pop wiring; mid-edit-render guard watchdog tuning); V3.5 risk register R-V3.5-1..7 (WorkbookSnapshot O(N) cost; partial-invalidate per-cell-op-index deferral; sheet-tabs UX gap acceptance; format-aware rendering deferral; session-wide format registry deferral; cross-restart PeerId reuse closed at V3.4.0.4b; mid-edit-render guard watchdog correctness); 12 out-of-scope items; live smoke procedure steps 12-15 extending V3.4.0.7 steps 7-11 (sheet management commands smoke + cell-grid render migration + reactive title; two-window mid-edit-render guard verification + watchdog smoke; format passthrough devtools inspection).  Total file size 1300 -> 1648 lines.  **Live VS Code smoke remains user-action gap** as designed; procedure now exists for the user to execute at their leisure.  V3.5.0.X parallel megaudit can now run with the full V3.5 spec available as audit-baseline.
10. **V3.5.0.X -- parallel Codex+Opus megaudit + closures** 🚧 IN PROGRESS (Codex Lane A SHIPPED 2026-05-24 with **FAIL** verdict; Opus Lane B running at session-end).

    **Codex Lane A transcript**: `docs/audits/2026-05-24-phase-5-7-v3-5-0-x-codex.md` (111 lines / 22KB; verdict: **FAIL**, 4 HIGH + 1 LOW).  Codex's findings are NOT spurious -- A-HIGH-1 includes a NATIVE-BINDING REPRODUCTION (cache state diverged from a fresh full rebuild after undo + interleaved remote op).  V3.5 CANNOT TERMINATE until the 4 HIGHs close in-cycle.

    **Opus Lane B transcript**: `docs/audits/2026-05-24-phase-5-7-v3-5-0-x-opus.md` SHIPPED 2026-05-24 (880 lines / 62KB; verdict: **PASS-WITH-FINDINGS** -- 2 HIGH + 4 MEDIUM + 3 LOW + 1 INFO).  Landed mid-session-edit; documentation already integrated below.

    **Codex Lane A findings + closure paths**:

    - **A-HIGH-1: partial-invalidate undo/redo invalidates the WRONG cell after remote interleaving** (`crates/ql-collab/src/session.rs:2359` + `:2388`).  Native-binding reproduction in Codex transcript § 7.  Bug: V3.5.0.6 dispatch captures `self.log.iter().last()` BEFORE undo, then uses partial-invalidate whenever visible length shrank by exactly 1.  But if a remote op landed AFTER the local op being undone, the captured most-recent-visible op is the REMOTE op; partial-invalidate then targets the WRONG cell, leaving the undone local cell stale.  Symmetric risk in redo (capture-after grow-by-1).  **Closure**: short-term safe path is a conservative fallback to full `rebuild_snapshot_cache` whenever any merge/poll occurred since the last local append (i.e., track a "pure local frontier" boolean, reset on merge_bytes/pollRemote, gate partial-invalidate on it).  Longer-term: wire Loro UndoManager's `on_pop` callback to get the actual retracted op (V3.6+ deferral noted in § 4.1.z5).  Add regression test for the exact reproduction scenario.  **Estimated effort**: 1-2 hours.

    - **A-HIGH-2: `workbookSnapshot()` bypasses repaired formulas from rebuild_workbook** (`crates/ql-bindings-node/src/lib.rs:1523` + `:1580` + `crates/ql-collab/src/repair.rs:363`).  Bug: V3.5.0.2 docstring says stale formula references from renameSheet are resolved when workbookSnapshot triggers `rebuild_workbook -> repair_sheet_rename_chain`.  But the napi method DISCARDS the repaired Workbook (only uses it for sheet count + names) and serializes formula text from the UNREPAIRED `last_snapshot` cache.  § 4.1.z5 contract (line ~1392) is violated.  **Closure**: either (a) read formula text from the repaired Workbook in workbook_snapshot (preferred -- match the contract), OR (b) reduce the contract in § 4.1.z5 to "raw cache formulas; rename repair only affects toQbook output."  Option (a) closes the gap.  Need a regression test: rename a sheet referenced by a formula in another cell, then workbookSnapshot should surface the repaired formula text.  **Estimated effort**: 2-3 hours (formula extraction from repaired Workbook isn't trivial -- need a per-cell accessor that handles cell_format_overlay etc.).

    - **A-HIGH-3: sheet management commands mutate session without repainting panels** (`extensions/quantlab/src/commands/quantbookCommands.ts:503` + `:566` + `:627` + `:683`).  Bug: V3.5.0.4a's 4 commands (Add/Rename/Delete/Move Sheet) call the engine, toast, return.  Local panels have NO poll loop; `activeLocalPanels()` exposes no render callback.  § 4.1.z5 live smoke claims panel title/grid updates within ms -- FALSE.  Multi-sheet UX argument for deferring V3.5.0.4c sheet-tabs ("Command Palette + reactive title cover the UX") is undermined since reactive title doesn't actually fire after these commands.  **Closure**: minimal path -- after each successful sheet op, call `CellGridPanel.refreshAll()` (or equivalent panel-iteration helper).  Better path -- expose a targeted local-panel refresh by session.  Update the live smoke procedure in § 4.1.z5 if behavior actually deviates from the new closure.  Add a command-flow integration test (or panel-fixture test if vscode-test infrastructure exists).  **Estimated effort**: 1-2 hours.

    - **A-HIGH-4: mid-edit render skip loses the deferred repaint** (`extensions/quantlab/src/quantbook/cellGrid/cellGridPanel.ts:674` + `cellGridLogic.ts:58`).  Bug: V3.5.0.7's tickPollRemote 'merged' branch returns early when `_presenceRepaintInFlight` is true, but sets NO pending-render flag.  The remote blob was ALREADY drained into the engine cache by `pollRemote()` (which classifyPollTick called BEFORE the merged-branch).  Subsequent ticks call `classifyPollTick`, which returns `'idle'` when no NEW blobs arrived, and the idle branch does NOT render.  `setPresenceTyping(false)` does NOT render.  Watchdog auto-clear does NOT render.  **Therefore remote changes can stay invisible indefinitely after a guarded skip.**  R-V3.4-3 closure is INCOMPLETE.  § 4.1.z5 D5 guarantee (line ~1503) "deferred render fires on next 1s tick" is FALSE.  **Closure**: add a `_pendingRenderAfterTyping: boolean` flag set when a merged render is skipped.  Clear + render when (i) typing transitions false (`setPresenceTyping(false)`), (ii) watchdog auto-clears, OR (iii) at next tick before returning on idle.  Add a mocha test for skip -> typing-false -> render fires.  **Estimated effort**: 1-2 hours.

    - **A-LOW-1: TS comments still mention `null` for napi optional fields** (`extensions/quantlab/src/quantbook/types.ts:67` + `:85`).  Doc hygiene; interfaces correctly use `?:` and tests assert `=== undefined`.  Acceptable to defer to V3.5.1+ OR close in-cycle (10-minute fix: replace `null` wording with `absent`/`undefined` consistently in CellValueJson + CellSnapshotJson comments).

    **Opus Lane B findings + closure paths**:

    - **Opus-H1: `SetCellFormat` apply_op has NO tombstone silent-drop guard** (`crates/ql-oplog/src/replay.rs:728-757`).  V3.5.0.3b extended PutValue/PutFormula/ClearFormula handlers with `if workbook.is_sheet_removed(*sheet) { return Ok(()) }`; V3.5.0.5's new SetCellFormat handler MISSED this 4-line addition (V3.5.0.5 landed AFTER V3.5.0.3b but the cell-keyed-Op tombstone-guard sweep wasn't re-run).  Consequence: SetCellFormat on a tombstoned sheet writes to `format_overlay` -- invisible to workbook_snapshot (which filters tombstones) but VISIBLE to `list_sheets_from_cache` -> **phantom cache entry for tombstoned sheet**.  Also: a future V3.6+ `Op::RestoreSheet` would surface unexpected format mutations.  **Closure**: 4-line fix at `replay.rs:734` (after validate_cell, before format-overlay set/clear) + 1 ql-collab regression test pinning silent-drop behavior on tombstoned sheets.  **Estimated effort**: 15-30 min.

    - **Opus-H2: Partial-invalidate undo dispatch picks WRONG cell when remote cell-keyed op trails the retracted local op in iteration order** (`crates/ql-collab/src/session.rs:2338-2408`).  **CROSS-LANE CONVERGENT WITH CODEX A-HIGH-1.**  Same root cause; same closure path; one regression test covers both lanes' repros.

    - **Opus-M1 (FALSE POSITIVE): mocha count mismatch (spec=346, claimed actual=335)**.  REJECTED.  Verified directly via `node node_modules/mocha/bin/mocha.js out/test/quantbook-roundtrip.test.js --ui tdd ...` -> reports `346 passing (949ms)`.  Opus likely counted `test(...)` declarations via grep + missed mocha's dynamic test discovery (e.g., `for` loops generating `test()` calls inside `suite`).  No action; skip + note in audit-closures sub-section.

    - **Opus-M2: Docstring claims `_presenceRepaintInFlight` reset on `show()` + `attachTransport`; no such reset exists in code** (`extensions/quantlab/src/quantbook/cellGrid/cellGridPanel.ts:403-405`).  VERIFIED REAL.  The field-level docstring says "reset to `false` in {@link show} (defensive, before render)" but `show()` does NOT call setPresenceTyping(false).  The init (constructor `= false`) + dispose-time reset (line 252 `setPresenceTyping(false)`) DO exist; the show()-time reset claim is FALSE.  **Closure**: either (a) add `instance.setPresenceTyping(false)` in show() before the first render (defensive; matches docstring), OR (b) remove the show()-reset claim from the docstring (minimal).  Option (a) is the safer fix (~5 min) since defensive reinit matches the rest of the system (V3.4.0.X audit established the "reinit on show + attachTransport" pattern).

    - **Opus-M3: `quantbookCellGridSwitchSheet` + `quantbookCellGridRefresh` duplication risk** (`extensions/quantlab/package.json:191-202`).  DESIGN OPINION, not a bug.  V3.5.0.4a sheet management commands COULD subsume Switch/Refresh's roles but didn't (kept the V3.3.0.5 + V3.2.a.1 surface area).  Acceptable to defer; V3.6+ multi-tab redesign may consolidate.  Defer to V3.5.1+ backlog with "review during V3.6 sheet-tabs scoping" note.

    - **Opus-M4 (DOWNGRADED to LOW by Opus on re-read)**: `move_sheet` impl correctness.  Opus's TL;DR already self-downgraded.  No action.

    - **Opus-L1: dead-code arc in `affected_cells_for_partial_invalidate`** (`crates/ql-collab/src/session.rs:1205-1250`).  classifier can never return `Some(empty Vec)` because `collect_affected_cells_recursive` always pushes for cell-keyed leaves.  Minor.  Defer to V3.5.1+.

    - **Opus-L2: `buildSheetMovePositionItems` lists source's current position with `(current)` annotation** (`extensions/quantlab/src/quantbook/cellGrid/cellGridLogic.ts:622-653`).  Semantically a no-op move; user-confusing UX.  Engine accepts via idempotency.  Defer to V3.5.1+ UX polish.

    - **Opus-L3: V3.5.0.4b reactive title race** between construction + wireAttachment (`extensions/quantlab/src/quantbook/cellGrid/cellGridPanel.ts:544`).  Brief window where title shows wrong mode-tag.  Edge case.  Defer.

    - **Opus-I1: `Workbook::move_sheet` linear-search O(N)** (acceptable at typical sheet counts).  V3.6+ polish if needed.

    **Cross-lane convergence map**:

    | Convergence | Codex | Opus | Closure |
    |---|---|---|---|
    | Partial-invalidate wrong-cell after remote interleave | A-HIGH-1 (native binding repro) | H2 (B.4.b analysis) | CONVERGENT HIGH; one fix closes both |
    | SetCellFormat tombstone gap | (Codex missed) | H1 (B.6) | Opus-only HIGH; in-cycle |
    | workbookSnapshot bypasses repaired formulas | A-HIGH-2 | (Opus didn't flag) | Codex-only HIGH; in-cycle |
    | Sheet management commands don't repaint | A-HIGH-3 | (Opus didn't flag) | Codex-only HIGH; in-cycle |
    | Mid-edit render skip loses deferred repaint | A-HIGH-4 | (Opus didn't flag) | Codex-only HIGH; in-cycle |
    | _presenceRepaintInFlight docstring drift | (Codex didn't flag) | M2 | Opus-only MED; in-cycle (~5 min) |
    | TS comments mention `null` | A-LOW-1 | (Opus didn't flag) | Codex-only LOW; in-cycle or defer |

    **Total unique findings**: 5 HIGH (4 Codex + 1 Opus + 1 convergent that's already counted in both) + 1 valid MED (Opus M2) + 1 LOW + 4 minor (Opus L1+L2+L3+I1).  V3.5 CANNOT TERMINATE until all 5 HIGHs + Opus M2 close in-cycle.

    **V3.5.0.X exit criteria**:
    1. Opus transcript present + read
    2. All 4 Codex HIGHs closed in-cycle (or explicitly reclassified to V3.5.1+ with justification -- but A-HIGH-1's native-binding reproduction makes reclassification very hard to justify)
    3. § 4.1.z5 updated with audit-closures sub-section (mirrors V3.4.0.X closure pattern: post-fix closure record + LOW backlog list + Rule 4 arc terminus confirmation + R-V3.5-* reclassifications if any)
    4. MASTER-PLAN.md swept to reflect V3.5 ALL SHIPPED + AUDITED (only after exit criteria 1-3 above)
    5. Plan archived to `.plans/_archive/`; `.plans/_active.md` empty + ready for V3.6 entry plan

## V3.5 risk register

- **R-V3.5-1 WorkbookSnapshot napi serialization performance.** Large workbooks (100k+ cells) produce large JSON payloads at every snapshot call. V3.5 ships the full snapshot; V3.6+ may add incremental deltas. Document the per-call O(N) cost in the napi docstring. Mitigation: IDE callers should batch snapshot calls (don't call per-keystroke).
- **R-V3.5-2 Partial-invalidate undo correctness under causal reorder.** V3.5.0.6 partial-invalidate replays the op-log subset filtered by `(sheet, row, col)`. The replay MUST preserve Loro's causal-merge iteration order for the filtered subset. Regression test: build an op log with concurrent cross-peer writes on the same cell, undo a local write, compare partial-invalidate result against full-rebuild. If they diverge, partial-invalidate is broken.
- **R-V3.5-3 Sheet rename/delete/reorder Op semantics across peers.** Cross-peer convergence: two peers concurrently renaming the same sheet to different names -- which name wins? LWW per (op_index, peer_id) per Loro convention. Document the convergence behavior + add cross-peer mocha pin. Sheet deletion: if Peer A deletes sheet 3 + Peer B writes to sheet 3 concurrently, what happens at merge? Phase 5.3 conflict-resolution may have prior art; verify before V3.5.0.3 ship.
- **R-V3.5-4 Format ops cache invalidation.** SetCellFormat is per-cell (CellState.format extension); RegisterFormat is session-wide (separate cache OR Workbook state). The two have different invalidation triggers: SetCellFormat invalidates ONE cell entry; RegisterFormat invalidates EVERY cell using that FormatId. V3.5.0.5 must wire both correctly; cross-validation test required.
- **R-V3.5-5 Mid-edit-render guard false-positives.** If `presenceRepaintInFlight` gets stuck true (transport disconnect mid-typing; engine error mid-presenceUpdate), all subsequent renders are blocked indefinitely. Mitigation: reinit on `show()` + `attachTransport`; possibly also a watchdog timer that clears the flag after N seconds of no presence updates.
- **R-V3.5-6 Cross-restart UserIdentity** -- DEFERRED from V3.4.0.4b D5 to V3.4.1+. Envelope v3 + `user_identity: Option<UserIdentity>` field land IF user-facing "show my contributions" feature surfaces. V3.5 explicitly does NOT ship this; the PeerId-fresh-per-session contract is preserved.
- **R-V3.5-7 WorkbookSnapshot napi struct evolution.** V3.5 ships 6 new `#[napi(object)]` structs. Future field additions (V3.6+ for richer format / chart / pivot state) are API-breaking for IDE consumers that destructure the JSON shape. Mitigation: document the "extend never reshape" contract for these napi objects; consider `#[non_exhaustive]` if napi-rs supports it (verify -- the Opus L1 finding flagged this for `PresenceStateJson` too).

## V3.5 out of scope

- IDE-side `rebuild_workbook` ROUND-TRIP (Workbook -> mutate -> workbook -> back). V3.5 is read-only IDE-side consumption (WorkbookSnapshot is a snapshot, not a live handle). V3.6+ may add a live-Workbook surface if formula re-evaluation in the IDE needs it.
- Full Op enum cache for ALL session-wide ops. V3.5.0.5 ships SetCellFormat / RegisterFormat. SetName / RenameSheet / RemoveSheet / etc. continue to live on Workbook state and are surfaced via WorkbookSnapshot (D3) on demand; their cache invalidation is the full-rebuild path.
- Per-peer color hashing for presence decoration. V3.4.1+ polish.
- Envelope v3 schema bump for UserIdentity. V3.4.1+ if attribution feature surfaces (R-V3.5-6).
- WorkbookSnapshot incremental deltas. V3.6+ if profiling shows the full snapshot cost is too high.
- vscode-test command-flow tests (Opus V3.4.0.X recommendation). V3.5.1+ test-infrastructure investment.
- Threshold-based `sweepPresence` variant. Waits on engine `sweep_presence_with_threshold(secs)`; V3.4.1+ or V3.5.1+.
- `WorkbookRuntime::set_value` recompute integration (live formula re-eval on the IDE side). V3.6+ explicit decision; V3.5 IDE renders formula TEXT but does NOT re-evaluate formulas (the engine's recompute pass produces the cached values via `rebuild_workbook`).

## V3.5.0.X audit transcript pre-allocation

- Codex Lane A: `docs/audits/<YYYY-MM-DD>-phase-5-7-v3-5-0-x-codex.md`
- Opus Lane B: `docs/audits/<YYYY-MM-DD>-phase-5-7-v3-5-0-x-opus.md`

Expected ~16-18 hours of total wall time across 6-9 sessions for the full V3.5 arc.

## V3.6+ entry-readiness preview (deferred from V3.5)

Out-of-scope items above plus:
- Live formula re-evaluation in IDE-side rendering (requires `rebuild_workbook` round-trip + WorkbookRuntime exposure).
- WorkbookSnapshot incremental deltas.
- Per-peer color hashing presence polish.
- vscode-test command-flow tests infrastructure.
- Chart / pivot table napi surfaces (Phase 4 product features now exposable via WorkbookSnapshot).
- `CacheState { cells, formats, names, tables }` promotion (V3.5 D1 option (b) if profiling justifies).

These are explicitly NOT V3.5 work; deferred to V3.6 entry-readiness analysis at V3.5.0.X Opus § F.
