Reading additional input from stdin...
OpenAI Codex v0.130.0
--------
workdir: /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
model: gpt-5.5
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 019e246e-50f5-7151-9b59-6e8badd7744c
--------
user
Independent Codex pull-up review of Phase 4.7.B (W5-96) — unified FunctionFn ABI in ql-functions.

Read these files first (in order):
1. /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/docs/architecture/2026-05-14-array-formulas-and-spills.md — the design doc. Particularly section 6.2 ("Function ABI — UNIFY, dont extend").
2. /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/docs/audits/2026-05-14-phase-4.7-design-codex-review.txt — your prior review of the design (the design fix is in response to your HIGH-1 finding).
3. The W5-96 commit: run `git show afbaebce223 --stat` and `git log --format="%H %s" -1 afbaebce223` to confirm scope. Then inspect:
   - crates/ql-functions/src/registry.rs (the unification — main file)
   - crates/ql-functions/src/lib.rs (re-exports)
   - crates/ql-types/src/array.rs (W5-95 — ArrayValue dependency)

Working directory: /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
Branch: feat/quantbook-engine, HEAD afbaebce223 (W5-96).

The implementation claims:
- New types FunctionArg / FunctionReturn / FunctionContext / FunctionFn / RegisteredFn live in ql-functions.
- ArrayValue from ql-types is the array carrier (no cycle).
- Internal storage is one HashMap<name, RegisteredFn>; legacy lookup methods are filter views on the enum variant.
- Legacy register_* methods still work bit-for-bit (zero diff on the ~130 default_registry calls).
- New register_unified / lookup_unified / lookup_any for the array tier.
- Eval site (ql-exec/src/scalar.rs) NOT yet migrated; that is 4.7.G work.
- 2010 workspace tests passing, +11 new for the unified path.

Verify each claim against the actual code. For each section/claim:
- PASS / FLAG verdict with one-sentence justification.
- For each FLAG: HIGH/MEDIUM/LOW + concrete file.rs:line citation + suggested fix.

Specifically scrutinize:
1. Does FunctionArg::Range carry the SAME shape as the existing FnArg::Range? Drift would break the 4.7.G evaluator-side conversion.
2. Is FunctionContext flexible enough to evolve without breaking the FunctionFn fn-pointer type? (A fn pointer is invariant in its arg types; adding a field to FunctionContext is fine but adding a generic param would not be.)
3. Does the unified storage preserve EXACT semantics of all eight pre-W5-96 register_* and lookup_* methods? Any silent behavior change between pre- and post-W5-96 (e.g. names()/range_aware_names() iteration order) would surface as test instability later.
4. Is the lookup_any return type — Option<&RegisteredFn> — usable from the eval site that needs to call through the dispatch? Pattern-matching against a borrowed RegisteredFn with `Scalar(f) => f(args)` should work because the inner fn pointer is Copy; verify.
5. Are there any places elsewhere in the codebase that pattern-matched on the OLD three-HashMap structure (e.g., diagnostic tests reading internal fields) that this commit might have missed?
6. The pre-commit hygiene-hook child-process maxBuffer issue suggested the registry file grew a lot; is there dead code / unused legacy types that should be removed in this commit or filed for cleanup?

Optional but valuable:
7. Does the design doc section 6.2 still match the implementation, or did the implementation diverge in any way that should be back-ported to the doc?

Write your full review to stdout (the caller tee-s to a file).
codex
I’ll do this as a review: first read the design and prior audit, then inspect the target commit and current code with line references. Since the workspace is read-only here, I’ll report findings only.
exec
/bin/zsh -lc "sed -n '1,240p' docs/architecture/2026-05-14-array-formulas-and-spills.md" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
# Phase 4.7 — Array Formulas And Dynamic Spills (design)

**Date:** 2026-05-14
**Phase:** 4.7 (per `docs/MASTER-PLAN.md` §7)
**Status:** APPROVED post-Codex review (W5-94). Codex review at `docs/audits/2026-05-14-phase-4.7-design-codex-review.txt`: 5 HIGH + 4 MEDIUM + 3 LOW design findings, all folded into this doc.
**Pattern:** mirrors Phase 4.6 design doc (`2026-05-13-cross-sheet-references.md`).

## Codex review changelog

The original draft was substantively wrong on five HIGH points. This revision encodes the corrected design:

- **HIGH (§ 6.3):** "scalar context returns first cell" was not Excel canon. **Revised:** v1 returns `#CALC!` for array-in-scalar-context; proper implicit intersection / `@` deferred to Phase 4.9.
- **HIGH (§ 6 + § 6.2):** `ArrayFn` over `EvalResult` (defined in `ql-exec`) created a `ql-functions` → `ql-exec` reverse-dependency cycle. **Revised:** array value type moves to `ql-types`; function ABI unifies the four tiers into one dispatch shape.
- **HIGH (§ 10):** parallel `outgoing_spill_targets` adjacency + eager CellNode creation conflicts with the existing calcgraph scheduler. **Revised:** spill targets are PRODUCER ALIASES — at dependency-extraction time, a `CellRef` to a cell with `spill_target_anchor(c) == Some(anchor)` becomes an edge to the anchor's formula node, lazy and graph-shape-preserving.
- **HIGH (§ 8.3):** missing "always unregister old spill before re-eval" step. **Revised:** explicit unregister-then-eval invariant; even blocked re-eval clears the old footprint first.
- **HIGH (§ 12):** "no schema bump because computed overlay round-trips" was false — current save emits non-formula non-blank cells as user-overlay records, so spill targets reload as user blockers. **Revised:** save side SKIPS cells inside an active spill range (lookup via `spill_target_anchor`); load → recompute_all re-derives. No schema bump needed.
- **MEDIUM:** `SpillAnchorTable` map-only; overlay clearing in `Workbook` API. Binder returns `BindError`, not panic. Sub-phase order moved graph before runtime.
- **LOW:** `ErrorValue::Spill` already exists at `crates/ql-types/src/error.rs:38` — moved to "already in place." Terminology: anchor vs target vs Excel's `A1#` spill-range-ref syntax (Phase 4.9). Blocking example fixed.

## 1. Acceptance criteria (from MASTER-PLAN)

- **ARR-4-01** array literals parse and evaluate.
- **ARR-4-02** spill writes computed overlay only.
- **ARR-4-03** blocked spills return the right error (`#SPILL!`).
- **ARR-4-04** dynamic spill resizing invalidates dependents.

## 2. Current state — what already works, what's missing

### 2.1 Already in place

- **AST:** `Expr::Array(Vec<Vec<Expr>>)` shape-locked in `crates/ql-formula-syntax/src/ast.rs:63`. `Expr::Spill(Box<Expr>)` shape-locked at line 67 but is RESERVED for Excel's `A1#` spill-range-ref syntax (Phase 4.9), NOT for the runtime spill anchor. Parser doesn't construct either; binder rejects with `BindError::UnsupportedVariant`.
- **`ErrorValue::Spill`** at `crates/ql-types/src/error.rs:38` with canonical `"#SPILL!"` sigil. Round-trips through `CellWireValue::Error` wire format (verified by W5-93 sigil round-trip tests).
- **Storage:** `ColumnStore::computed_overlays: Vec<SparseOverlay>` already exists (Phase 3.5, W5-38). `Sheet::put_computed` / `clear_computed` API in place. Read cascade is `user → computed → base`.
- **Calcgraph:** `Node::Spill(SpillNode { anchor, shape_rows, shape_cols })` shape-locked in `ql-calcgraph/src/node.rs:107`. `add_spill_node` panics with "Phase 3+ feature" message. **NB: post-revision this node may not be needed at all** — spill targets aren't graph nodes (see § 10).
- **Functions:** registry has `ScalarFn` / `RangeAwareFn` / `ContextAwareFn` tiers in `crates/ql-functions/src/registry.rs`. **Will unify, not extend** (see § 6.2).
- **Op log + persistence:** stable. No new variants planned. NO schema bump.

### 2.2 Crate dependency map (relevant for § 6)

```
ql-types          (leaf)
  ↑
ql-functions      (depends on ql-types)
  ↑
ql-storage / ql-formula-syntax / ql-io / ql-oplog / ql-calcgraph
  ↑
ql-exec           (depends on all of the above)
```

`ql-functions` CANNOT import from `ql-exec` — that would close a cycle. Any type referenced by the function ABI must live in `ql-types` or `ql-functions` itself. **This is why the original draft's `ArrayFn(args: &[EvalResult])` design was wrong.**

### 2.3 Missing

- Lexer: `{` and `}` tokens; `,` and `;` as array-context separators.
- Parser: array-literal parsing rules.
- Printer: round-trip for `Expr::Array`.
- Binder: `Expr::Array` → `ExprPlan::Array` lowering with element-arity validation.
- Array value type in `ql-types` (so `ql-functions` can reference it).
- Unified function ABI in `ql-functions` (collapse `ScalarFn` / `RangeAwareFn` / `ContextAwareFn` into one dispatch shape; gain array support).
- Storage: spill-anchor table (anchor → shape lookup; target → anchor reverse).
- Workbook-layer `clear_spill_at(anchor)` (clears both the table entries AND the computed overlays at every target).
- Runtime: spill writeback (clear-old-first → eval → block-check → register-and-write).
- Calcgraph: producer-alias edges for spill targets (lazy, at dep-extraction time).
- Persistence: save-side skip for cells that are spill targets (lookup via `spill_target_anchor`).
- First array-returning functions: SEQUENCE, FILTER, TRANSPOSE.

## 3. Grammar

### 3.1 Array literal syntax

ASCII-only separators. Match Excel's en-US canonical syntax (deferred locale support is Phase 4.9):

```
array_literal := '{' array_row (';' array_row)* '}'
array_row    := array_cell (',' array_cell)*
array_cell   := literal | unary literal     // see § 3.3 for constraints
literal      := NUMBER | STRING | BOOL      // no nested refs in Phase 4.7
```

Horizontal: `{1, 2, 3}` → 1×3.
Vertical: `{1; 2; 3}` → 3×1.
2D: `{1, 2; 3, 4}` → 2×2.

### 3.2 Element-arity rule

All rows must have the same number of cells. Mismatched-row literals (e.g. `{1, 2; 3}`) error at parse time with `ParseError::ArrayRowArityMismatch { expected, found, row }`. **This is a Quantbook v1 subset** — Excel pads with `#N/A` for missing cells, and `{1,2;3}` evaluates as `{1,2;3,#N/A}`. Quantbook v1 chooses loud rejection over silent padding; the pad-with-`#N/A` variant is a Phase 4.10 polish item gated on test-corpus completeness.

### 3.3 Element kinds restricted in v1

Array cells in v1 — the allowed `array_cell` subset:

- `NUMBER` (positive or negative literal)
- `STRING` literal
- `BOOL` literal (`TRUE` / `FALSE`)
- `error literal` — `#NULL!`, `#DIV/0!`, `#VALUE!`, `#REF!`, `#NAME?`, `#NUM!`, `#N/A`, `#GETTING_DATA`, `#SPILL!`, `#CALC!` — matches IronCalc's `ArrayNode::Error` variant and is needed for `FILTER(_, _, #N/A)` etc.

NO nested CellRef / RangeRef / Function calls / Unary operators (other than the implicit sign on numeric literals). Codex MEDIUM finding: explicitly document this as a Quantbook v1 subset.

Phase 4.10 lifts to allow refs (`{A1, B1; A2, B2}`) once the array evaluator and dependency extraction prove stable.

**Rationale:** array literals with refs would require shape determination at bind/eval time, not parse time, AND would entangle the array-literal grammar with the dependency graph. Restricting v1 to constants keeps the parser simple and the test corpus closed.

## 4. AST

`Expr::Array(Vec<Vec<Expr>>)` is already shape-locked. The parser populates it directly. Each inner `Expr` is constrained to the kinds in § 3.3.

**`Expr::Spill(Box<Expr>)` terminology note** (Codex LOW): the existing shape-lock at `ast.rs:67` is RESERVED for Excel's `A1#` spill-range-ref SYNTAX (Phase 4.9), NOT the runtime spill anchor. Excel's `A1#` means "the spill range anchored at A1" — it's a reference operator, not an anchor marker. The runtime spill anchor (the formula cell at the top-left of a spilled range) lives in `Workbook::spill_anchors` (§ 7), not in the AST. Phase 4.7 does not construct `Expr::Spill`; that's Phase 4.9's job alongside `@`.

## 5. Binder — `ExprPlan::Array`

New variant:

```rust
ExprPlan::Array(Vec<Vec<ExprPlan>>)
```

Binder rule: `Expr::Array` → `ExprPlan::Array` element-wise. Each element binds to a `Number` / `Bool` / `String` / `Error` plan (no `Unary` — the lexer folds signs into literals).

**Codex MEDIUM:** `Expr::Array` is a PUBLIC AST variant. Tests + tools can construct invalid ASTs (e.g. ragged shapes via `Vec::push`). The binder MUST reject ragged ASTs with a clean error, not panic:

```rust
BindError::ArrayRowArityMismatch { expected: u32, found: u32, row: u32 }
```

This shadows the equivalent `ParseError` for the case where the AST was constructed directly. No `BindError::UnsupportedVariant` for `Expr::Array` itself — it's now supported.

## 6. Evaluator — REDESIGN per Codex HIGH

### 6.1 `ArrayValue` type — lives in `ql-types`

Codex HIGH: `EvalResult` in `ql-exec` cannot be referenced by `ql-functions` without closing a dependency cycle (`ql-functions` → `ql-types` only; `ql-exec` → `ql-functions`).

**Revised placement:** `ArrayValue` lives in `ql-types` alongside `Value` and `ErrorValue`:

```rust
// crates/ql-types/src/array.rs (new module)
#[derive(Clone, Debug, PartialEq)]
pub struct ArrayValue {
    pub rows: u32,
    pub cols: u32,
    pub cells: Vec<Value>,  // row-major, length = rows * cols
}

impl ArrayValue {
    pub fn at(&self, row: u32, col: u32) -> &Value { ... }
    pub fn first(&self) -> &Value { &self.cells[0] }
    pub fn is_degenerate(&self) -> bool { self.rows == 0 || self.cols == 0 }
}
```

`Value` itself stays scalar (no `Value::Array(...)` variant). Storage cells remain scalar by Excel canon — spilling MATERIALIZES arrays into per-cell scalars in the computed overlay. The array vs scalar distinction lives at the EVAL boundary via `EvalResult` (still in `ql-exec`).

### 6.2 Function ABI — UNIFY, don't extend

Codex HIGH: adding `ArrayFn` as a 4th parallel tier compounds the dispatch sprawl. Better: collapse the existing 3 tiers into ONE dispatch ABI that can carry scalar, range, array, and context.

**Revised dispatch shape** in `ql-functions`:

```rust
// crates/ql-functions/src/registry.rs (revised)
pub enum FunctionArg {
    Scalar(Value),
    Range { sheet: SheetId, range: Range, /* ... existing RangeAwareFn arg shape ... */ },
    Array(ArrayValue),  // new — array values from `Expr::Array` or another function call
}

pub struct FunctionContext<'a> {
    pub eval_ctx: &'a EvalContext,           // existing (date_system, locale, now)
    pub workbook: &'a dyn FunctionWorkbook,  // existing trait abstraction
    // Phase 4.7 doesn't change the context shape.
}

pub enum FunctionReturn {
    Scalar(Value),
    Array(ArrayValue),  // new — for SEQUENCE / FILTER / TRANSPOSE
}

pub type FunctionFn = fn(args: &[FunctionArg], ctx: &FunctionContext) -> FunctionReturn;
```

Migration:
- Existing `ScalarFn` / `RangeAwareFn` / `ContextAwareFn` registrations re-route to the unified `FunctionFn` via shim adapters. Adapters live in `ql-functions` itself; downstream callers stay unchanged in v1 by exposing the legacy `register_scalar` / `register_range_aware` / `register_context_aware` helpers that internally build the unified `FunctionFn`.
- New array-returning functions register directly with the unified ABI.
- Cleanup of the legacy tier names is a future polish item.

**Caller-side dispatch** in `ql-exec::eval`:

```rust
let arg_values: Vec<FunctionArg> = ...;  // materialize from ExprPlan args
let ret: FunctionReturn = (function.f)(&arg_values, &ctx);
let eval_result: EvalResult = match ret {
    FunctionReturn::Scalar(v) => EvalResult::Scalar(v),
    FunctionReturn::Array(a)  => EvalResult::Array(a),
};
```

`EvalResult` (still in `ql-exec`) wraps the ABI-side return at the eval boundary. No cycle.

### 6.3 Eval contexts — REDESIGN per Codex HIGH

The original "scalar context returns first cell" rule was **NOT Excel canon**. Excel's behavior:
- **Dynamic-array Excel (365+):** array-in-scalar-context spills. `@` explicitly opts back into legacy implicit-intersection.
- **Legacy Excel:** implicit intersection takes the cell at the row/col INTERSECTION of the calling formula's row/col with the array's row/col span. "First cell" is wrong in both modes.

**Revised v1 rule:** array-in-scalar-context produces `Value::Error(ErrorValue::Calc)`. Specifically:

- **Cell-boundary context** (the formula expression at a cell's root): array result → spill (§ 8).
- **Sub-expression scalar context** (binary operand, scalar function arg): array result → `#CALC!`.
- **Aggregate-arg context** (existing `BindContext::AggregateArg` from Phase 2B.4): array result is consumed as a 2D range of values, NOT as a single scalar. This already works for `RangeRef` and named ranges; extending to literal arrays just feeds the cells.

Test:
- `=SEQUENCE(3) + 1` (no spill — wrapped in `+1` binary op) → `#CALC!`. The `+1` operand context is scalar.
- `=SUM(SEQUENCE(3))` → 6. The SUM arg context is aggregate; SEQUENCE's 3×1 result feeds as 3 cells.
- `=SEQUENCE(3)` at cell A1 → spills to A1, A2, A3.

Implicit intersection / `@` / xlsx-canon scalarization is **Phase 4.9 work**. Documented at § 14.5.

## 7. Spill anchor data model (storage)

### 7.1 New `SpillAnchorTable` in `ql-storage`

```rust
pub struct SpillAnchorTable {
    /// Anchor (sheet, row, col) → shape (rows, cols).
    /// One entry per spilling formula.
    anchors: HashMap<(SheetId, RowId, ColId), SpillShape>,
    /// Reverse map: each spilled-into cell → its anchor.
    /// Used for fast "is this cell part of a spill?" lookup
    /// at user-write time.
    targets: HashMap<(SheetId, RowId, ColId), (SheetId, RowId, ColId)>,
}

pub struct SpillShape {
    pub rows: u32,
    pub cols: u32,
}
```

**Invariant:** `targets[(s, r, c)] = (anchor_s, anchor_r, anchor_c)` iff `(anchor_s, anchor_r, anchor_c)` exists in `anchors` AND `(r, c)` is inside the rectangle `(anchor_r..anchor_r+shape.rows, anchor_c..anchor_c+shape.cols)`.


exec
/bin/zsh -lc "sed -n '1,240p' docs/audits/2026-05-14-phase-4.7-design-codex-review.txt" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
Reading additional input from stdin...
OpenAI Codex v0.130.0
--------
workdir: /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
model: gpt-5.5
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 019e244a-d859-7361-81f7-fa07772f00b0
--------
user
Read this design doc fully before responding:
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/docs/architecture/2026-05-14-array-formulas-and-spills.md

Also read the prior phases design doc for the pattern:
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/docs/architecture/2026-05-13-cross-sheet-references.md

This is the Phase 4.7 design doc (Array Formulas + Dynamic Spills) for the Quantbook engine. It is the spec for ~13 sub-phases of implementation work. You are being asked for an independent design review BEFORE any code ships.

Working directory: /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
Branch: feat/quantbook-engine, HEAD b5bfc751cbc (post-W5-93).

For context, examine these existing files to verify the designs claims about current state:
- crates/ql-formula-syntax/src/ast.rs (Expr::Array, Expr::Spill shape-locks)
- crates/ql-storage/src/column.rs (computed_overlays existing)
- crates/ql-storage/src/sheet.rs (put_computed)
- crates/ql-calcgraph/src/node.rs (SpillNode shape-lock)
- crates/ql-calcgraph/src/graph.rs (existing edge model — for section 10.1 evaluation)
- crates/ql-exec/src/plan.rs (ExprPlan variants — for section 5)
- crates/ql-exec/src/eval/* (eval entry points — for section 6)
- crates/ql-functions/src/registry.rs (function-tier model — for section 6.2 / section 14.4)

Walk through each section 1-16 and return:
- A PASS / FLAG verdict per section with one-sentence justification.
- For each FLAG: HIGH/MEDIUM/LOW severity + concrete concern + suggested fix.
- Explicit answers to the 5 open questions in section 16.
- A "What design decisions are still wrong" punch list ordered HIGH → MEDIUM → LOW.

HIGH = architectural mistake that will require painful rework after implementation.
MEDIUM = inconsistency or unclear contract; closure-prefers-fixed before sub-phase 4.7.A.
LOW = nit / phrasing / future-polish hint.

Be independent. Do not anchor on my framing if the implementation reality (per the code you inspect) disagrees. Specifically scrutinize:
- The "scalar context returns first cell" rule (section 6.3). Is that actually Excel canon? Or is it #VALUE!? Or is it implicit-intersection?
- The "eager vs lazy" decision in section 14.2.
- The Option A parallel adjacency in section 10.1.
- The function-tier proliferation (section 6.2 / section 14.4).
- The "no schema bump" claim in section 12.

Write your full review to stdout (it gets tee-d to a file by the caller).
codex
I’ll read the two docs and the named source files first, then I’ll compare the spec against the implementation reality section by section. This is a review request, so I’ll lead with concrete findings and avoid making code changes.
exec
/bin/zsh -lc "sed -n '1,260p' docs/architecture/2026-05-14-array-formulas-and-spills.md" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
# Phase 4.7 — Array Formulas And Dynamic Spills (design)

**Date:** 2026-05-14
**Phase:** 4.7 (per `docs/MASTER-PLAN.md` §7)
**Status:** DRAFT for Codex review.
**Pattern:** mirrors Phase 4.6 design doc (`2026-05-13-cross-sheet-references.md`).

## 1. Acceptance criteria (from MASTER-PLAN)

- **ARR-4-01** array literals parse and evaluate.
- **ARR-4-02** spill writes computed overlay only.
- **ARR-4-03** blocked spills return the right error (`#SPILL!`).
- **ARR-4-04** dynamic spill resizing invalidates dependents.

## 2. Current state — what already works, what's missing

### 2.1 Already in place

- **AST:** `Expr::Array(Vec<Vec<Expr>>)` and `Expr::Spill(Box<Expr>)` are SHAPE-LOCKED in `crates/ql-formula-syntax/src/ast.rs:63-67`. Parser doesn't construct them; binder rejects with `BindError::UnsupportedVariant`.
- **Storage:** `ColumnStore::computed_overlays: Vec<SparseOverlay>` already exists (Phase 3.5, W5-38). `Sheet::put_computed` / `clear_computed` API in place. Read cascade is `user → computed → base`.
- **Calcgraph:** `Node::Spill(SpillNode { anchor, shape_rows, shape_cols })` shape-locked in `ql-calcgraph/src/node.rs:107`. `add_spill_node` panics with "Phase 3+ feature" message.
- **Functions:** registry has `ScalarFn` / `RangeAwareFn` / `ContextAwareFn` tiers. No array-returning function tier yet.
- **Op log + persistence:** stable. No new variants planned.

### 2.2 Missing

- Lexer: `{` and `}` tokens; `,` and `;` as array-context separators.
- Parser: array-literal parsing rules.
- Printer: round-trip for `Expr::Array`.
- Binder: `Expr::Array` → `ExprPlan::Array` lowering with element-arity validation.
- Evaluator: array result values; spill-array result distinction.
- Storage: spill-anchor table (anchor → shape lookup; target → anchor reverse).
- Runtime: spill writeback (computed overlay only); spill blocking.
- Calcgraph: spill invalidation edges (spill target → anchor formula).
- Function registry: array-returning function tier (or extend existing tier).
- First array-returning functions: SEQUENCE, FILTER (+ TRANSPOSE for completeness).

## 3. Grammar

### 3.1 Array literal syntax

ASCII-only separators. Match Excel's en-US canonical syntax (deferred locale support is Phase 4.9):

```
array_literal := '{' array_row (';' array_row)* '}'
array_row    := array_cell (',' array_cell)*
array_cell   := literal | unary literal     // see § 3.3 for constraints
literal      := NUMBER | STRING | BOOL      // no nested refs in Phase 4.7
```

Horizontal: `{1, 2, 3}` → 1×3.
Vertical: `{1; 2; 3}` → 3×1.
2D: `{1, 2; 3, 4}` → 2×2.

### 3.2 Element-arity rule

All rows must have the same number of cells. Mismatched-row literals (e.g. `{1, 2; 3}`) error at parse time with `ParseError::ArrayRowArityMismatch { expected, found, row }`. Matches Excel canon: Excel pads with `#N/A` but for our v1 we reject loudly — pad-with-N/A is a Phase 4.10 polish item.

### 3.3 Element kinds restricted in v1

Array cells in v1: NUMBER / STRING / BOOL / unary-minus-NUMBER only. NO nested CellRef / RangeRef / Function calls inside an array literal. This matches IronCalc's `ArrayNode` model (numbers/strings/booleans/errors). Phase 4.10 can lift to allow `{A1, B1; A2, B2}` if needed.

**Rationale:** array literals with refs would require shape determination at bind/eval time, not parse time. Restricting v1 to constants keeps the implementation tractable and the test corpus complete.

## 4. AST

`Expr::Array(Vec<Vec<Expr>>)` is already shape-locked. The parser populates it directly. Each inner `Expr` is constrained to the kinds in § 3.3.

`Expr::Spill(Box<Expr>)` stays shape-locked but unconstructed in Phase 4.7 — spilling is a RUNTIME behavior, not a syntactic node. (Excel's `@` implicit-intersection operator is Phase 4.9 work, NOT here.)

## 5. Binder — `ExprPlan::Array`

New variant:

```rust
ExprPlan::Array(Vec<Vec<ExprPlan>>)
```

Binder rule: `Expr::Array` → `ExprPlan::Array` element-wise. Each element binds to a `Number` / `Bool` / `String` / `Unary { op: Neg, .. }` plan. No `BindError::UnsupportedVariant` for the literal itself (it's now supported).

Element-arity already validated at parse time. The binder asserts it (defense in depth) and panics if violated — that'd be a parser bug, not user input.

## 6. Evaluator

### 6.1 `ArrayValue` type

New value type at the EVAL layer (NOT at `ql_types::Value`). Lives in `ql_exec::eval`:

```rust
pub enum EvalResult {
    Scalar(Value),
    Array(ArrayValue),
}

pub struct ArrayValue {
    pub rows: u32,
    pub cols: u32,
    pub cells: Vec<Value>,  // row-major, length = rows * cols
}
```

**Why not put `ArrayValue` in `ql_types::Value`?** Two reasons:
1. Cells in storage are scalar by Excel canon (`Value::Number`, `Value::Text`, etc.). Spilling MATERIALIZES arrays into per-cell scalars in the computed overlay.
2. A `Value` is what `Sheet::read` returns; promoting that to an enum-of-enum would touch every caller. Eval is the right boundary.

### 6.2 Function tier extension

`FunctionRegistry` gains a new tier:

```rust
pub type ArrayFn = fn(args: &[EvalResult], ctx: &EvalContext) -> EvalResult;
```

Registration: `register_array_fn(name, sig, f)`. Dispatched by name like the other tiers. Functions that ALWAYS return arrays (SEQUENCE, FILTER, UNIQUE, SORT, TRANSPOSE) register as `ArrayFn`. Functions that CAN return arrays based on input (e.g. an arithmetic op on arrays — Phase 4.7 doesn't do this; that's array-of-scalars broadcasting, Phase 4.10) stay scalar.

### 6.3 Eval contexts

- **Scalar context** (current default): `EvalResult::Array(_)` → first cell (`cells[0]`); rest discarded. Matches Excel's pre-365 implicit-intersection behavior. Used inside `Function::ScalarFn` argument lists.
- **Array context** (new): the formula at the cell BOUNDARY. Used by `WorkbookRuntime::set_formula` + recompute. If the eval result is `EvalResult::Array`, the runtime spills.

The eval entry point for the runtime call site distinguishes between scalar contexts (inside aggregate args, inside binary operators) and the cell-boundary array context. This is the "cell-boundary" notion that HyperFormula calls "vertex value" — only the top-level formula at a cell can spill.

## 7. Spill anchor data model (storage)

### 7.1 New `SpillAnchorTable` in `ql-storage`

```rust
pub struct SpillAnchorTable {
    /// Anchor (sheet, row, col) → shape (rows, cols).
    /// One entry per spilling formula.
    anchors: HashMap<(SheetId, RowId, ColId), SpillShape>,
    /// Reverse map: each spilled-into cell → its anchor.
    /// Used for fast "is this cell part of a spill?" lookup
    /// at user-write time.
    targets: HashMap<(SheetId, RowId, ColId), (SheetId, RowId, ColId)>,
}

pub struct SpillShape {
    pub rows: u32,
    pub cols: u32,
}
```

**Invariant:** `targets[(s, r, c)] = (anchor_s, anchor_r, anchor_c)` iff `(anchor_s, anchor_r, anchor_c)` exists in `anchors` AND `(r, c)` is inside the rectangle `(anchor_r..anchor_r+shape.rows, anchor_c..anchor_c+shape.cols)`.

The anchor cell itself is BOTH an anchor AND its own target (the (0,0) of the spill range). Both maps include it.

### 7.2 Workbook integration

`Workbook::spill_anchors: SpillAnchorTable` field. Accessors:

- `pub fn spill_anchors(&self) -> &SpillAnchorTable`
- `pub fn spill_anchor_at(&self, sheet, row, col) -> Option<&SpillShape>` (anchor lookup)
- `pub fn spill_target_anchor(&self, sheet, row, col) -> Option<(SheetId, RowId, ColId)>` (reverse lookup; the anchor that this cell belongs to, or `None`)

Mutators (low-level — production routes through runtime):
- `pub fn register_spill(&mut self, anchor, shape) -> Result<(), SpillBlockError>` — adds to both maps; errors if the range collides with an existing spill that doesn't share the same anchor.
- `pub fn unregister_spill(&mut self, anchor)` — removes the anchor + all its targets.

### 7.3 Persistence

`Workbook::spill_anchors` is RUNTIME-DERIVED state. NOT persisted. On `.qbook` load, `recompute_all` re-derives the table by re-evaluating each formula.

**No schema bump for Phase 4.7.** This is a property of the design — the computed overlay already round-trips correctly via the v4 envelope's per-sheet overlay; spill anchors are re-derived from formulas.

## 8. Spill writeback (runtime)

### 8.1 `WorkbookRuntime::set_formula` extension

Pre-W5-93 flow: parse → bind → eval (scalar context) → `put_computed` at the cell.

New flow (Phase 4.7):

1. Parse + bind formula.
2. Evaluate at cell-boundary context. Result is `EvalResult::Scalar(v)` or `EvalResult::Array(a)`.
3. If scalar: existing path. `put_computed` at the cell.
4. If array:
   a. Compute spill target rectangle `(row..row+a.rows, col..col+a.cols)`.
   b. Bounds-check: range must fit within `MAX_ROW` / `MAX_COLUMN`. Out-of-bounds → `#SPILL!` at anchor.
   c. **Blocking check (§ 9):** every cell in target range MUST be empty (no user value AND no other spill anchor's target). Else → `#SPILL!` at anchor.
   d. Register anchor: `workbook.spill_anchors.register_spill(anchor, shape)`.
   e. Write each array cell to its computed overlay via `put_computed`.
5. Op log: emit `Op::PutFormula { text }` only. The spill range is re-derived at replay time via the same eval path.

codex
The design doc’s section 6.2 now says the ABI should be unified in `ql-functions` with `FunctionArg`, `FunctionReturn`, and `ArrayValue` from `ql-types`; that is the contract I’m checking against. I’m going to pull the exact commit scope and then read the registry implementation with line numbers.
exec
/bin/zsh -lc 'git show afbaebce223 --stat' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc "nl -ba crates/ql-functions/src/registry.rs | sed -n '1,320p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-dJsFNFnz' (errno=Operation not permitted)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-OrEKPTfv' (errno=Operation not permitted)
commit afbaebce223962470b581bf171b469159762ae15
Author: steppen0mad <nurtaymergaliyev@gmail.com>
Date:   Thu May 14 03:51:40 2026 +0100

    W5-96 / Engine Phase 4.7.B — Unified FunctionFn ABI in ql-functions
    
    Second implementation sub-phase of Phase 4.7. Per design doc § 6.2 and
    Codex review HIGH-1: collapse the three parallel function-tier HashMaps
    (`ScalarFn`, `RangeAwareFn`, `ContextAwareFn`) into one unified storage
    keyed by name and valued by a `RegisteredFn` enum that tags the tier.
    Add a fourth `Unified` variant for array-returning functions (SEQUENCE
    / FILTER / TRANSPOSE — Phase 4.7.M/N).
    
    New public types in `ql-functions`:
    - `FunctionArg` — `Scalar(Value)` / `Range { values, rows, cols }` /
      `Array(ArrayValue)`. The unified ABI's arg vocabulary.
    - `FunctionReturn` — `Scalar(Value)` / `Array(ArrayValue)`. `is_array()`
      helper for the eval-site spill check.
    - `FunctionContext<'a>` — carries `&EvalContext`; struct-of-fields lets
      us add workbook-level state later without changing the ABI.
    - `FunctionFn = fn(&[FunctionArg], &FunctionContext) -> FunctionReturn` —
      the unified registration signature.
    - `RegisteredFn` — internal tagged-union: `Scalar` / `RangeAware` /
      `ContextAware` / `Unified`.
    
    Storage change in `FunctionRegistry`:
    - Three parallel `HashMap<&str, *Fn>` → one
      `HashMap<&str, RegisteredFn>`.
    - `insert_or_panic` helper consolidates duplicate-name validation
      across all `register_*` methods.
    
    Legacy public API preserved bit-for-bit:
    - `register` / `register_range_aware` / `register_context_aware` store
      the appropriate `RegisteredFn::*` variant.
    - `lookup` / `lookup_range_aware` / `lookup_context_aware` /
      `names` / `range_aware_names` / `context_aware_names` filter on
      the enum variant.
    - The ~130 existing `r.register("SUM", scalar_fns::sum)` calls in
      `default_registry` work unchanged — zero diff on call sites.
    
    New public API for the array-returning tier:
    - `register_unified(name, FunctionFn)` — register an array-aware fn.
    - `lookup_unified(name) -> Option<FunctionFn>` — variant-filtered lookup.
    - `lookup_any(name) -> Option<&RegisteredFn>` — return the tagged
      enum directly so the eval site can do one match instead of four
      sequential lookups. Eval-site migration deferred to W5-101
      (Phase 4.7.G) when array dispatch actually lands.
    - `unified_names()` — iterator over `Unified` names.
    
    Disjointness: pre-W5-96 the three tiers were enforced disjoint via six
    explicit `contains_key` asserts in each register method. The unified
    HashMap enforces this structurally — one name → one RegisteredFn — so
    registration collapses to a single duplicate-name panic.
    
    Eval dispatch (`ql-exec/src/scalar.rs`): NOT changed. Still calls
    `lookup_range_aware` → `lookup_context_aware` → `lookup` as three
    sequential filter-view lookups. Codex MEDIUM flagged "settle ABI
    before runtime work" — this commit is the ABI settle; the eval-site
    migration to one `match RegisteredFn` lands in W5-101 (Phase 4.7.G)
    when the array-eval path needs it.
    
    Tests (+11 from 1999 → 2010):
    - `register_unified` + `lookup_unified` round-trip with array return.
    - `lookup_unified` case-insensitivity.
    - Unified tier disjoint from other tiers (legacy filter views return
      None for Unified entries; `lookup_any` sees it).
    - All cross-tier duplicate-name registrations panic.
    - `unified_names` / `names_all` / `len` count unified entries.
    - `FunctionArg::Array` round-trips through a unified fn that echoes
      its first arg.
    - `FunctionReturn::is_array()` helper.
    - `lookup_any` returns the correct enum variant per tier.
    
    Existing 600+ tests in `ql-functions`, `ql-exec`, `ql-oplog`, `ql-io`
    pass unchanged.
    
    Gates (all 7 green):
    - fmt clean
    - clippy -D warnings clean
    - cargo test --workspace: 2010 passed / 0 failed
    - check-build-flags / check-cargo-lock-pins / check-multiversion-
      clones: OK
    - cargo audit: 1 allowed warning (atomic-polyfill, pre-existing)
    
    Per design plan: Codex pull-up follows this commit (the function-ABI
    unification was Codex's HIGH-1 finding; this is the first major
    milestone). After Codex sign-off, sub-phase W5-97 / 4.7.C — lexer
    brace tokens + error-literal token.
    
    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>

 quantbook-engine/crates/ql-functions/src/lib.rs    |   5 +-
 .../crates/ql-functions/src/registry.rs            | 604 +++++++++++++++------
 2 files changed, 455 insertions(+), 154 deletions(-)

 succeeded in 0ms:
     1	//! Function registry — name → implementation dispatch.
     2	//!
     3	//! Names are stored uppercase per Excel canon. Lookup is case-insensitive: the caller
     4	//! can pass `"sum"`, `"SUM"`, or `"Sum"` and get the same function.
     5	//!
     6	//! ## Storage shape (W5-96 / Phase 4.7.B unification)
     7	//!
     8	//! Pre-W5-96 the registry held three parallel `HashMap`s, one per function
     9	//! tier (`ScalarFn`, `RangeAwareFn`, `ContextAwareFn`). Phase 4.7 needs a
    10	//! fourth tier (array-returning functions for SEQUENCE / FILTER /
    11	//! TRANSPOSE), and Codex design review HIGH-1 flagged the proliferation:
    12	//! the right move at this point is to **unify** the dispatch table into
    13	//! one map keyed by name, valued by a `RegisteredFn` enum that tags the
    14	//! tier. Eval-site dispatch becomes one HashMap lookup + one `match`.
    15	//!
    16	//! This commit (W5-96) restructures STORAGE without changing the public
    17	//! `register_*` / `lookup_*` API surface: legacy callers still call
    18	//! `register("SUM", sum_fn)` and `lookup("SUM")` exactly as before. The
    19	//! lookup methods filter on the enum variant (e.g. `lookup_range_aware`
    20	//! returns `Some(raf)` only if the registered fn is
    21	//! `RegisteredFn::RangeAware(raf)`). Eval-site migration to a single
    22	//! match-on-enum lands in W5-101 (Phase 4.7.G) when the array-eval path
    23	//! actually needs it.
    24	//!
    25	//! New array-returning functions register via `register_unified(name,
    26	//! FunctionFn)` and are stored as `RegisteredFn::Unified(_)`. Eval-site
    27	//! dispatch in W5-101 will route them through a new `match` arm that
    28	//! returns `EvalResult::Array(_)` to the runtime.
    29	
    30	use std::collections::HashMap;
    31	
    32	use ql_types::{ArrayValue, EvalContext, Value};
    33	
    34	use crate::context_aware_fns::ContextAwareFn;
    35	use crate::range_aware_fns::RangeAwareFn;
    36	use crate::{date_fns, format, range_fns, scalar_fns, volatile};
    37	
    38	/// Function signature: pre-evaluated args → result Value.
    39	pub type ScalarFn = fn(&[Value]) -> Value;
    40	
    41	/// **W5-96 (Phase 4.7.B):** unified function-dispatch arg, used by the
    42	/// new array-returning function tier (`FunctionFn`). Covers all three
    43	/// shapes the eval site can produce:
    44	///
    45	/// - `Scalar(Value)` — single pre-evaluated value (analogous to a single
    46	///   slot in `&[Value]` for the legacy `ScalarFn`).
    47	/// - `Range { values, rows, cols }` — flat row-major iteration over a
    48	///   workbook range, with 2D shape preserved (matches `FnArg::Range`
    49	///   from the legacy `RangeAwareFn` contract).
    50	/// - `Array(ArrayValue)` — an explicit array value from `Expr::Array`
    51	///   literal or another function's return. Distinct from `Range`
    52	///   because `Range` carries workbook-range provenance (used by
    53	///   VLOOKUP/INDEX shape addressing) while `Array` is a free-floating
    54	///   2D value.
    55	///
    56	/// Conversion `FnArg → FunctionArg`:
    57	/// - `FnArg::Scalar(v)` → `FunctionArg::Scalar(v)`.
    58	/// - `FnArg::Range { values, rows, cols }` → `FunctionArg::Range { ... }`.
    59	///
    60	/// The eval site (W5-101 / Phase 4.7.G) materializes `FunctionArg` from
    61	/// `ExprPlan` arg positions before dispatching through `FunctionFn`.
    62	#[derive(Clone, Debug, PartialEq)]
    63	pub enum FunctionArg {
    64	    /// Single scalar value.
    65	    Scalar(Value),
    66	    /// 2D range with explicit shape; `values.len() == rows * cols`,
    67	    /// row-major iteration. Matches the existing `FnArg::Range` shape.
    68	    Range {
    69	        values: Vec<Value>,
    70	        rows: usize,
    71	        cols: usize,
    72	    },
    73	    /// Array value (from `Expr::Array` literal or another function's
    74	    /// `FunctionReturn::Array`).
    75	    Array(ArrayValue),
    76	}
    77	
    78	/// **W5-96 (Phase 4.7.B):** unified function return — either a scalar or
    79	/// an array. Arrays returned at the cell-boundary context spill; arrays
    80	/// returned in scalar context produce `Value::Error(ErrorValue::Calc)`
    81	/// (Phase 4.7.G enforces this at the eval site per design § 6.3).
    82	#[derive(Clone, Debug, PartialEq)]
    83	pub enum FunctionReturn {
    84	    Scalar(Value),
    85	    Array(ArrayValue),
    86	}
    87	
    88	impl FunctionReturn {
    89	    /// True if this is `Array(_)`. Helper for the eval-site spill check.
    90	    pub fn is_array(&self) -> bool {
    91	        matches!(self, FunctionReturn::Array(_))
    92	    }
    93	}
    94	
    95	/// **W5-96 (Phase 4.7.B):** unified function call context. Carries the
    96	/// `EvalContext` (date_system / locale / now_provider) that the legacy
    97	/// `ContextAwareFn` received as a bare reference. Struct-of-fields shape
    98	/// lets us add workbook-level state later (W5-101+) without changing the
    99	/// function ABI.
   100	pub struct FunctionContext<'a> {
   101	    pub eval_ctx: &'a EvalContext,
   102	}
   103	
   104	impl<'a> FunctionContext<'a> {
   105	    pub fn new(eval_ctx: &'a EvalContext) -> Self {
   106	        Self { eval_ctx }
   107	    }
   108	}
   109	
   110	/// **W5-96 (Phase 4.7.B):** unified function ABI. New array-returning
   111	/// functions (SEQUENCE, FILTER, TRANSPOSE — Phase 4.7.M/N) register
   112	/// through this signature.
   113	pub type FunctionFn = fn(&[FunctionArg], &FunctionContext) -> FunctionReturn;
   114	
   115	/// **W5-96 (Phase 4.7.B):** tagged-union of all four function-dispatch
   116	/// tiers, stored as the value in the unified `FunctionRegistry` HashMap.
   117	/// Pre-W5-96 the registry held three parallel HashMaps; this enum
   118	/// collapses them and adds the `Unified` variant for array fns.
   119	///
   120	/// Eval-site code in `ql-exec::scalar` currently routes through the
   121	/// legacy filter-views (`lookup_scalar` / `lookup_range_aware` /
   122	/// `lookup_context_aware`); W5-101 (Phase 4.7.G) migrates that site
   123	/// to a single `match` on `RegisteredFn`.
   124	#[derive(Clone, Debug)]
   125	pub enum RegisteredFn {
   126	    /// Legacy `fn(&[Value]) -> Value`. Most functions register here.
   127	    Scalar(ScalarFn),
   128	    /// Range-aware: `fn(&[FnArg]) -> Value`. Used by SUMIF / VLOOKUP /
   129	    /// INDEX etc. that need per-arg range-vs-scalar metadata.
   130	    RangeAware(RangeAwareFn),
   131	    /// Context-aware: `fn(&[Value], &EvalContext) -> Value`. Used by
   132	    /// DATE / NOW / TODAY / WEEKDAY etc. that need workbook EvalContext.
   133	    ContextAware(ContextAwareFn),
   134	    /// Unified ABI (W5-96+). New array-returning functions (Phase
   135	    /// 4.7.M/N) register here.
   136	    Unified(FunctionFn),
   137	}
   138	
   139	/// Phase 0 function registry. Built by `default_registry()` with the
   140	/// full built-in set (~130 entries by W5-93).
   141	///
   142	/// **W5-96 (Phase 4.7.B):** internal storage unified into one HashMap
   143	/// keyed by name, valued by `RegisteredFn`. Legacy `register_*` /
   144	/// `lookup_*` methods preserved as filters on the enum variant for
   145	/// backwards-compat (zero diff on the ~130 existing
   146	/// `r.register("SUM", scalar_fns::sum)` lines in `default_registry`).
   147	///
   148	/// **Disjointness:** a single name maps to ONE `RegisteredFn` (the
   149	/// HashMap enforces this naturally). The pre-W5-96 "cross-table
   150	/// disjointness" assertions become "name already registered" — same
   151	/// semantics, simpler implementation.
   152	#[derive(Clone, Debug)]
   153	pub struct FunctionRegistry {
   154	    fns: HashMap<&'static str, RegisteredFn>,
   155	}
   156	
   157	impl Default for FunctionRegistry {
   158	    fn default() -> Self {
   159	        Self::new()
   160	    }
   161	}
   162	
   163	impl FunctionRegistry {
   164	    /// Empty registry — caller adds functions via `register`. Use `default_registry()`
   165	    /// for the Phase 0 built-in set.
   166	    pub fn new() -> Self {
   167	        Self {
   168	            fns: HashMap::new(),
   169	        }
   170	    }
   171	
   172	    /// W5-96 internal: assert canonical-uppercase name + insert with
   173	    /// duplicate panic. Single source of truth for all `register_*`
   174	    /// methods. The pre-W5-96 cross-table disjointness checks collapse
   175	    /// to a single "duplicate" panic per the unified map invariant.
   176	    fn insert_or_panic(&mut self, name: &'static str, f: RegisteredFn, method: &'static str) {
   177	        assert!(!name.is_empty(), "{method}: name must not be empty");
   178	        assert!(
   179	            name.bytes().all(|b| !b.is_ascii_lowercase()),
   180	            "{method}: name {name:?} must be canonical upper-case; \
   181	             lookups uppercase the query, so a lower-case key is unreachable"
   182	        );
   183	        let prior = self.fns.insert(name, f);
   184	        assert!(
   185	            prior.is_none(),
   186	            "{method}: duplicate registration for {name:?} — silent override \
   187	             would let a typo replace a built-in"
   188	        );
   189	    }
   190	
   191	    /// Register a scalar function under `name`. Stored internally as
   192	    /// `RegisteredFn::Scalar(f)`.
   193	    ///
   194	    /// Phase 2A.7 audit M5: `name` MUST already be canonical upper-case. This is
   195	    /// asserted at registration time so a stray `register("sum", ...)` doesn't
   196	    /// silently create an unreachable entry (lookups uppercase the query, so a
   197	    /// lower-case key would never be found). Duplicate registrations also
   198	    /// panic — silent override would let a typo replace a built-in with a buggy
   199	    /// shim. Phase 2 has a closed default function set; if dynamic registration
   200	    /// ever becomes a real use case, swap this for `Result<(), RegisterError>`.
   201	    pub fn register(&mut self, name: &'static str, f: ScalarFn) {
   202	        self.insert_or_panic(name, RegisteredFn::Scalar(f), "FunctionRegistry::register");
   203	    }
   204	
   205	    /// W5-53: register a range-aware function under `name`. Stored
   206	    /// internally as `RegisteredFn::RangeAware(f)`. Same canonical-
   207	    /// uppercase requirement + duplicate panic as `register`.
   208	    pub fn register_range_aware(&mut self, name: &'static str, f: RangeAwareFn) {
   209	        self.insert_or_panic(
   210	            name,
   211	            RegisteredFn::RangeAware(f),
   212	            "FunctionRegistry::register_range_aware",
   213	        );
   214	    }
   215	
   216	    /// **W5-69 (Phase 4.5.A.0):** register a context-aware function. These
   217	    /// receive `&EvalContext` (date_system + locale + now_provider) in
   218	    /// addition to the standard `&[Value]` args. Stored internally as
   219	    /// `RegisteredFn::ContextAware(f)`.
   220	    pub fn register_context_aware(&mut self, name: &'static str, f: ContextAwareFn) {
   221	        self.insert_or_panic(
   222	            name,
   223	            RegisteredFn::ContextAware(f),
   224	            "FunctionRegistry::register_context_aware",
   225	        );
   226	    }
   227	
   228	    /// **W5-96 (Phase 4.7.B):** register a function under the unified
   229	    /// `FunctionFn` ABI. Used by new array-returning functions
   230	    /// (SEQUENCE, FILTER, TRANSPOSE — Phase 4.7.M/N). Stored as
   231	    /// `RegisteredFn::Unified(f)`.
   232	    pub fn register_unified(&mut self, name: &'static str, f: FunctionFn) {
   233	        self.insert_or_panic(
   234	            name,
   235	            RegisteredFn::Unified(f),
   236	            "FunctionRegistry::register_unified",
   237	        );
   238	    }
   239	
   240	    /// Case-insensitive lookup, returning the scalar function if and only
   241	    /// if the registered entry is `RegisteredFn::Scalar(_)`. Other tiers
   242	    /// (range-aware, context-aware, unified) return `None` here — the
   243	    /// caller must check the tier-specific lookup methods.
   244	    pub fn lookup(&self, name: &str) -> Option<ScalarFn> {
   245	        let upper = name.to_ascii_uppercase();
   246	        match self.fns.get(upper.as_str()) {
   247	            Some(RegisteredFn::Scalar(f)) => Some(*f),
   248	            _ => None,
   249	        }
   250	    }
   251	
   252	    /// W5-53: case-insensitive lookup, returning the range-aware fn iff
   253	    /// registered as `RegisteredFn::RangeAware(_)`. Callers should check
   254	    /// this BEFORE `lookup` — if a function is range-aware, the dispatch
   255	    /// must construct `Vec<FnArg>` rather than flattening to `Vec<Value>`.
   256	    pub fn lookup_range_aware(&self, name: &str) -> Option<RangeAwareFn> {
   257	        let upper = name.to_ascii_uppercase();
   258	        match self.fns.get(upper.as_str()) {
   259	            Some(RegisteredFn::RangeAware(f)) => Some(*f),
   260	            _ => None,
   261	        }
   262	    }
   263	
   264	    /// **W5-69 (Phase 4.5.A.0):** case-insensitive lookup, returning the
   265	    /// context-aware fn iff registered as `RegisteredFn::ContextAware(_)`.
   266	    /// Callers should check this AFTER `lookup_range_aware` but BEFORE
   267	    /// `lookup`. Dispatch order: `range_aware` → `context_aware` → `scalar`.
   268	    pub fn lookup_context_aware(&self, name: &str) -> Option<ContextAwareFn> {
   269	        let upper = name.to_ascii_uppercase();
   270	        match self.fns.get(upper.as_str()) {
   271	            Some(RegisteredFn::ContextAware(f)) => Some(*f),
   272	            _ => None,
   273	        }
   274	    }
   275	
   276	    /// **W5-96 (Phase 4.7.B):** case-insensitive lookup for the unified
   277	    /// ABI. Returns `Some(f)` iff the registered entry is
   278	    /// `RegisteredFn::Unified(_)`. Used by the eval-site array-dispatch
   279	    /// path (W5-101 / Phase 4.7.G).
   280	    pub fn lookup_unified(&self, name: &str) -> Option<FunctionFn> {
   281	        let upper = name.to_ascii_uppercase();
   282	        match self.fns.get(upper.as_str()) {
   283	            Some(RegisteredFn::Unified(f)) => Some(*f),
   284	            _ => None,
   285	        }
   286	    }
   287	
   288	    /// **W5-96 (Phase 4.7.B):** case-insensitive lookup returning the
   289	    /// `RegisteredFn` enum directly. Lets the eval site perform a
   290	    /// single match-on-tier rather than four sequential `lookup_*`
   291	    /// calls. Migrated callers (W5-101+) use this; pre-W5-96 callers
   292	    /// keep working through the tier-specific filter views above.
   293	    pub fn lookup_any(&self, name: &str) -> Option<&RegisteredFn> {
   294	        let upper = name.to_ascii_uppercase();
   295	        self.fns.get(upper.as_str())
   296	    }
   297	
   298	    /// Iterator over names registered as `RegisteredFn::Scalar(_)`.
   299	    /// **W5-96 (Phase 4.7.B):** previously was "names registered in the
   300	    /// scalar HashMap"; the unified storage means we filter by variant.
   301	    pub fn names(&self) -> impl Iterator<Item = &&'static str> + '_ {
   302	        self.fns
   303	            .iter()
   304	            .filter(|(_, v)| matches!(v, RegisteredFn::Scalar(_)))
   305	            .map(|(k, _)| k)
   306	    }
   307	
   308	    /// W5-65 (Phase 4.4.B; Codex MEDIUM 4 fix): names registered as
   309	    /// `RegisteredFn::RangeAware(_)`.
   310	    pub fn range_aware_names(&self) -> impl Iterator<Item = &&'static str> + '_ {
   311	        self.fns
   312	            .iter()
   313	            .filter(|(_, v)| matches!(v, RegisteredFn::RangeAware(_)))
   314	            .map(|(k, _)| k)
   315	    }
   316	
   317	    /// **W5-69 (Phase 4.5.A.0):** names registered as
   318	    /// `RegisteredFn::ContextAware(_)`.
   319	    pub fn context_aware_names(&self) -> impl Iterator<Item = &&'static str> + '_ {
   320	        self.fns

exec
/bin/zsh -lc "nl -ba crates/ql-functions/src/lib.rs | sed -n '1,220p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
     1	//! `ql-functions` — Quantbook built-in function library.
     2	//!
     3	//! Phase 0 W4-4 scope: 22 distinct functions (25 registry entries with aliases) covering
     4	//! the most common Excel formula primitives:
     5	//!
     6	//! - **Aggregates**: SUM, AVERAGE/AVG, COUNT, COUNTA, MIN, MAX, PRODUCT.
     7	//! - **Variance / stdev (Welford-backed for A6)**: VAR, VAR.S, VAR.P, STDEV, STDEV.S, STDEV.P.
     8	//! - **Logical**: IF, AND, OR, NOT, IFERROR.
     9	//! - **Math**: ABS, SQRT, ROUND, INT, MOD, POWER.
    10	//!
    11	//! All functions are `fn(&[Value]) -> Value` — pre-evaluated args, Excel-compatible
    12	//! coercion + error propagation. Range references get expanded to per-cell Values
    13	//! at the binder layer before reaching this module.
    14	//!
    15	//! ## A6 acceptance (NIST StRD numacc verification)
    16	//!
    17	//! VAR / VAR.S / STDEV / STDEV.S use Welford's online algorithm via
    18	//! `welford::WelfordState`. Verified against an analytic large-offset + small-variance
    19	//! dataset (`welford::tests::a6_acceptance_shape_lock_large_offset_small_variance`):
    20	//! mean accurate to ≥12 significant digits, variance to ≥8 — meets the A6 spec.
    21	//!
    22	//! Literal NIST numacc dataset import (W4-5 follow-up) will add data fixtures via a
    23	//! `Cargo`-tracked test asset; the Welford shape is locked here.
    24	
    25	pub mod context_aware_fns;
    26	pub mod date_fns;
    27	pub mod format;
    28	pub mod range_aware_fns;
    29	pub mod range_fns;
    30	pub mod registry;
    31	pub mod scalar_fns;
    32	pub mod volatile;
    33	pub mod welford;
    34	pub mod wildcard;
    35	
    36	pub use context_aware_fns::ContextAwareFn;
    37	pub use range_aware_fns::{FnArg, RangeAwareFn};
    38	pub use registry::{
    39	    default_registry, FunctionArg, FunctionContext, FunctionFn, FunctionRegistry, FunctionReturn,
    40	    RegisteredFn, ScalarFn,
    41	};
    42	pub use volatile::{clear_test_overrides, set_test_now_secs, set_test_rng_seed};
    43	pub use welford::{
    44	    mean, population_stdev, population_variance, sample_stdev, sample_variance, WelfordState,
    45	};

exec
/bin/zsh -lc "nl -ba crates/ql-types/src/array.rs | sed -n '1,240p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
     1	//! `ArrayValue` — a 2D array of `Value` cells.
     2	//!
     3	//! **Phase 4.7 sub-phase A (W5-95)** — first sub-phase of the array-formulas
     4	//! and dynamic-spills arc. Design doc:
     5	//! `docs/architecture/2026-05-14-array-formulas-and-spills.md` § 6.1.
     6	//!
     7	//! ## Why `ql-types` and not `ql-exec`?
     8	//!
     9	//! Codex design review (HIGH-1): the function-dispatch ABI in `ql-functions`
    10	//! must be able to PASS and RETURN array values. `ql-functions` depends only
    11	//! on `ql-types` — adding a `ql-functions` → `ql-exec` edge would close a
    12	//! cycle (since `ql-exec` already depends on `ql-functions`). So `ArrayValue`
    13	//! lives here, alongside `Value` and `ErrorValue`.
    14	//!
    15	//! ## What this is NOT
    16	//!
    17	//! - `ArrayValue` is NOT a `Value` variant. Storage cells stay scalar by
    18	//!   Excel canon — spilling MATERIALIZES arrays into per-cell scalars in
    19	//!   the computed overlay. `Value::Array(...)` would force every `read`
    20	//!   caller to handle the multi-cell case, which is wrong at that boundary.
    21	//! - `ArrayValue` is NOT an iterator-over-`Value`. It is a fixed-shape
    22	//!   container with row-major indexing.
    23	//!
    24	//! ## Invariants
    25	//!
    26	//! 1. **Length:** `cells.len() == (rows as usize) * (cols as usize)`. The
    27	//!    `new` constructor enforces this; convenience constructors `row`,
    28	//!    `column`, `singleton`, and `empty` build correct shapes by
    29	//!    construction.
    30	//! 2. **Row-major:** `cells[row * cols + col]` is the cell at logical
    31	//!    coordinate `(row, col)`. Iteration order in `cells.iter()` is
    32	//!    row-major.
    33	//! 3. **Degenerate shapes:** `rows == 0 || cols == 0` produces `cells: []`.
    34	//!    Degenerate arrays are valid containers (functions like `FILTER` with
    35	//!    an all-false mask produce them); the runtime spill path surfaces them
    36	//!    as `#CALC!` (NOT `#SPILL!`) per design § 9.1.
    37	//!
    38	//! ## Equality
    39	//!
    40	//! `PartialEq` only — `Value::Number(f64)` is not `Eq` (NaN). Test code
    41	//! that compares `ArrayValue`s composed of NaN should use bitwise
    42	//! comparison helpers, not `==`.
    43	
    44	use std::fmt;
    45	
    46	use crate::value::Value;
    47	
    48	/// A 2D array of `Value` cells in row-major layout.
    49	///
    50	/// Used at the EVAL boundary and in the unified function-dispatch ABI
    51	/// (`FunctionArg::Array` / `FunctionReturn::Array`, defined in
    52	/// `ql-functions`). Storage cells stay scalar — see module docs.
    53	#[derive(Clone, Debug, PartialEq, Default)]
    54	pub struct ArrayValue {
    55	    rows: u32,
    56	    cols: u32,
    57	    cells: Vec<Value>,
    58	}
    59	
    60	/// Errors emitted when constructing or operating on an `ArrayValue` with
    61	/// invalid arguments. These are PROGRAMMING errors (caller-side bugs);
    62	/// user-visible array errors (degenerate result, out-of-bounds spill, etc.)
    63	/// surface as `Value::Error(ErrorValue::*)` at the eval boundary, not here.
    64	#[derive(Clone, Debug, PartialEq, Eq)]
    65	pub enum ArrayShapeError {
    66	    /// Number of cells supplied doesn't match `rows * cols`.
    67	    CellCountMismatch {
    68	        rows: u32,
    69	        cols: u32,
    70	        expected: usize,
    71	        found: usize,
    72	    },
    73	
    74	    /// Coordinate passed to `at` / `at_mut` is outside the array bounds.
    75	    IndexOutOfBounds {
    76	        row: u32,
    77	        col: u32,
    78	        rows: u32,
    79	        cols: u32,
    80	    },
    81	}
    82	
    83	impl fmt::Display for ArrayShapeError {
    84	    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
    85	        match self {
    86	            Self::CellCountMismatch {
    87	                rows,
    88	                cols,
    89	                expected,
    90	                found,
    91	            } => write!(
    92	                f,
    93	                "ArrayValue construction: cell count {found} does not match \
    94	                 rows ({rows}) * cols ({cols}) = {expected}"
    95	            ),
    96	            Self::IndexOutOfBounds {
    97	                row,
    98	                col,
    99	                rows,
   100	                cols,
   101	            } => write!(
   102	                f,
   103	                "ArrayValue index out of bounds: ({row}, {col}) for shape ({rows}, {cols})"
   104	            ),
   105	        }
   106	    }
   107	}
   108	
   109	impl std::error::Error for ArrayShapeError {}
   110	
   111	impl ArrayValue {
   112	    /// Construct an array from `rows × cols` cells in row-major order.
   113	    ///
   114	    /// Returns `Err(CellCountMismatch)` if `cells.len() != rows * cols`.
   115	    /// For degenerate shapes (`rows == 0` or `cols == 0`), `cells` must
   116	    /// be empty.
   117	    pub fn new(rows: u32, cols: u32, cells: Vec<Value>) -> Result<Self, ArrayShapeError> {
   118	        let expected = (rows as usize).checked_mul(cols as usize).ok_or(
   119	            ArrayShapeError::CellCountMismatch {
   120	                rows,
   121	                cols,
   122	                expected: usize::MAX,
   123	                found: cells.len(),
   124	            },
   125	        )?;
   126	        if cells.len() != expected {
   127	            return Err(ArrayShapeError::CellCountMismatch {
   128	                rows,
   129	                cols,
   130	                expected,
   131	                found: cells.len(),
   132	            });
   133	        }
   134	        Ok(Self { rows, cols, cells })
   135	    }
   136	
   137	    /// 1×1 array wrapping a single value. Used by `FILTER`'s `if_empty`
   138	    /// path (a scalar fallback returned in array form).
   139	    pub fn singleton(value: Value) -> Self {
   140	        Self {
   141	            rows: 1,
   142	            cols: 1,
   143	            cells: vec![value],
   144	        }
   145	    }
   146	
   147	    /// 1×N horizontal vector. Convenience constructor.
   148	    pub fn row(values: Vec<Value>) -> Self {
   149	        let cols = values.len() as u32;
   150	        Self {
   151	            rows: 1,
   152	            cols,
   153	            cells: values,
   154	        }
   155	    }
   156	
   157	    /// N×1 vertical vector. Convenience constructor.
   158	    pub fn column(values: Vec<Value>) -> Self {
   159	        let rows = values.len() as u32;
   160	        Self {
   161	            rows,
   162	            cols: 1,
   163	            cells: values,
   164	        }
   165	    }
   166	
   167	    /// Degenerate (zero-area) array: `rows == 0 || cols == 0`, `cells` empty.
   168	    /// Returned by `FILTER` when no `include` cell is TRUE and no `if_empty`
   169	    /// is provided (the function itself then returns `#CALC!` instead of
   170	    /// this — see design § 9.1).
   171	    pub fn empty(rows: u32, cols: u32) -> Self {
   172	        debug_assert!(
   173	            rows == 0 || cols == 0,
   174	            "ArrayValue::empty: ({rows}, {cols}) is not degenerate"
   175	        );
   176	        Self {
   177	            rows,
   178	            cols,
   179	            cells: Vec::new(),
   180	        }
   181	    }
   182	
   183	    /// Number of rows. `u32` for symmetry with `RowId`.
   184	    #[inline]
   185	    pub fn rows(&self) -> u32 {
   186	        self.rows
   187	    }
   188	
   189	    /// Number of columns.
   190	    #[inline]
   191	    pub fn cols(&self) -> u32 {
   192	        self.cols
   193	    }
   194	
   195	    /// Total cell count (`rows * cols`). 0 if degenerate.
   196	    #[inline]
   197	    pub fn len(&self) -> usize {
   198	        self.cells.len()
   199	    }
   200	
   201	    /// True if `rows == 0` or `cols == 0`. Distinguished from `len() == 0`
   202	    /// only for clarity; the two are equivalent given the construction
   203	    /// invariant.
   204	    #[inline]
   205	    pub fn is_degenerate(&self) -> bool {
   206	        self.rows == 0 || self.cols == 0
   207	    }
   208	
   209	    /// True if degenerate OR `len() == 0`. Provided for the `Vec`-like
   210	    /// API symmetry; identical to `is_degenerate()` under the invariant.
   211	    #[inline]
   212	    pub fn is_empty(&self) -> bool {
   213	        self.cells.is_empty()
   214	    }
   215	
   216	    /// Borrow the cell at logical `(row, col)`. Returns `None` for
   217	    /// out-of-bounds. Use [`at`](Self::at) for the panicking variant.
   218	    #[inline]
   219	    pub fn get(&self, row: u32, col: u32) -> Option<&Value> {
   220	        if row >= self.rows || col >= self.cols {
   221	            return None;
   222	        }
   223	        Some(&self.cells[(row as usize) * (self.cols as usize) + (col as usize)])
   224	    }
   225	
   226	    /// Borrow the cell at logical `(row, col)`. Panics on out-of-bounds —
   227	    /// for the fallible variant use [`get`](Self::get).
   228	    #[inline]
   229	    pub fn at(&self, row: u32, col: u32) -> &Value {
   230	        self.get(row, col).unwrap_or_else(|| {
   231	            panic!(
   232	                "ArrayValue::at: ({row}, {col}) out of bounds for shape ({}, {})",
   233	                self.rows, self.cols
   234	            )
   235	        })
   236	    }
   237	
   238	    /// First cell — `cells[0]`. Panics if degenerate. Helper for callers
   239	    /// (e.g. tests) that need a single-cell view; the runtime's array →
   240	    /// scalar contract uses `#CALC!`, NOT `first()` (per design § 6.3).

exec
/bin/zsh -lc 'git log --format="%H %s" -1 afbaebce223' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-d4Mmw4ca' (errno=Operation not permitted)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-fxnQYJWm' (errno=Operation not permitted)
afbaebce223962470b581bf171b469159762ae15 W5-96 / Engine Phase 4.7.B — Unified FunctionFn ABI in ql-functions

exec
/bin/zsh -lc "nl -ba crates/ql-functions/src/registry.rs | sed -n '320,760p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   320	        self.fns
   321	            .iter()
   322	            .filter(|(_, v)| matches!(v, RegisteredFn::ContextAware(_)))
   323	            .map(|(k, _)| k)
   324	    }
   325	
   326	    /// **W5-96 (Phase 4.7.B):** names registered as
   327	    /// `RegisteredFn::Unified(_)`. For coverage walks that want to
   328	    /// see the array-returning function set explicitly.
   329	    pub fn unified_names(&self) -> impl Iterator<Item = &&'static str> + '_ {
   330	        self.fns
   331	            .iter()
   332	            .filter(|(_, v)| matches!(v, RegisteredFn::Unified(_)))
   333	            .map(|(k, _)| k)
   334	    }
   335	
   336	    /// W5-65 (Phase 4.4.B): convenience iterator over ALL registered
   337	    /// function names across all tiers. The unified storage makes this
   338	    /// trivial — `keys()` covers every entry without de-duplication.
   339	    pub fn names_all(&self) -> impl Iterator<Item = &&'static str> {
   340	        self.fns.keys()
   341	    }
   342	
   343	    pub fn len(&self) -> usize {
   344	        self.fns.len()
   345	    }
   346	
   347	    pub fn is_empty(&self) -> bool {
   348	        self.fns.is_empty()
   349	    }
   350	}
   351	
   352	/// Phase 0 default registry with the W4-4 built-in function set (22 functions).
   353	pub fn default_registry() -> FunctionRegistry {
   354	    let mut r = FunctionRegistry::new();
   355	
   356	    // Aggregates
   357	    r.register("SUM", scalar_fns::sum);
   358	    r.register("AVERAGE", scalar_fns::average);
   359	    r.register("AVG", scalar_fns::average); // Common alias; not Excel-canonical but
   360	                                            // user-friendly. Re-evaluate when Excel
   361	                                            // compat audit lands.
   362	    r.register("COUNT", scalar_fns::count);
   363	    r.register("COUNTA", scalar_fns::counta);
   364	    r.register("MIN", scalar_fns::min);
   365	    r.register("MAX", scalar_fns::max);
   366	    r.register("PRODUCT", scalar_fns::product);
   367	
   368	    // Variance + stdev (Welford-backed, A6 spec)
   369	    r.register("VAR", scalar_fns::var_s); // Excel alias for VAR.S in legacy mode.
   370	    r.register("VAR.S", scalar_fns::var_s);
   371	    r.register("VAR.P", scalar_fns::var_p);
   372	    r.register("STDEV", scalar_fns::stdev_s);
   373	    r.register("STDEV.S", scalar_fns::stdev_s);
   374	    r.register("STDEV.P", scalar_fns::stdev_p);
   375	
   376	    // Logical
   377	    r.register("IF", scalar_fns::r#if);
   378	    r.register("AND", scalar_fns::and);
   379	    r.register("OR", scalar_fns::or);
   380	    r.register("NOT", scalar_fns::not);
   381	    r.register("IFERROR", scalar_fns::iferror);
   382	
   383	    // Math
   384	    r.register("ABS", scalar_fns::abs);
   385	    r.register("SQRT", scalar_fns::sqrt);
   386	    r.register("ROUND", scalar_fns::round);
   387	    r.register("INT", scalar_fns::int);
   388	    r.register("MOD", scalar_fns::r#mod);
   389	    r.register("POWER", scalar_fns::power);
   390	
   391	    // AI reservation per CORR-06 / T4-D05 — returns Error(AINotAvailable). See
   392	    // `scalar_fns::ai` doc.
   393	    r.register("AI", scalar_fns::ai);
   394	
   395	    // Engine Phase 4.3 V1 (W5-46, 2026-05-13): function library wave 1
   396	    // batch — math + text + information. All scalar (per-cell, no
   397	    // range-arg machinery beyond what 3.6 already provides). Excel-
   398	    // canon error propagation and coercion.
   399	    r.register("ROUNDUP", scalar_fns::roundup);
   400	    r.register("ROUNDDOWN", scalar_fns::rounddown);
   401	    r.register("TRUNC", scalar_fns::trunc);
   402	    r.register("SIGN", scalar_fns::sign);
   403	    r.register("EXP", scalar_fns::exp);
   404	    r.register("LN", scalar_fns::ln);
   405	    r.register("LOG", scalar_fns::log);
   406	    r.register("LOG10", scalar_fns::log10);
   407	    r.register("PI", scalar_fns::pi);
   408	    r.register("DEGREES", scalar_fns::degrees);
   409	    r.register("RADIANS", scalar_fns::radians);
   410	    r.register("LEN", scalar_fns::len);
   411	    r.register("UPPER", scalar_fns::upper);
   412	    r.register("LOWER", scalar_fns::lower);
   413	    r.register("PROPER", scalar_fns::proper);
   414	    r.register("CLEAN", scalar_fns::clean);
   415	    r.register("TRIM", scalar_fns::trim);
   416	    r.register("ISNUMBER", scalar_fns::isnumber);
   417	    r.register("ISTEXT", scalar_fns::istext);
   418	    r.register("ISBLANK", scalar_fns::isblank);
   419	    r.register("ISLOGICAL", scalar_fns::islogical);
   420	    r.register("ISERROR", scalar_fns::iserror);
   421	    r.register("ISNA", scalar_fns::isna);
   422	    r.register("ISERR", scalar_fns::iserr);
   423	
   424	    // Engine Phase 4.3 V2 batch #5 — text functions wave 2 (W5-56).
   425	    // All scalar (existing ScalarFn contract). 1-based indices for
   426	    // FIND/SEARCH/MID/REPLACE; UTF-8 char-count semantics (UTF-16
   427	    // canon deferred to Phase 4.9).
   428	    r.register("LEFT", scalar_fns::left);
   429	    r.register("RIGHT", scalar_fns::right);
   430	    r.register("MID", scalar_fns::mid);
   431	    r.register("FIND", scalar_fns::find);
   432	    r.register("SEARCH", scalar_fns::search);
   433	    r.register("SUBSTITUTE", scalar_fns::substitute);
   434	    r.register("REPLACE", scalar_fns::replace_fn);
   435	    r.register("CONCATENATE", scalar_fns::concatenate);
   436	    r.register("REPT", scalar_fns::rept);
   437	    r.register("EXACT", scalar_fns::exact);
   438	
   439	    // Engine Phase 4.3 V2 batch #6 — math completion + hyperbolic
   440	    // trig (W5-57). All scalar. Math sign-rule canon for CEILING /
   441	    // FLOOR / MROUND; integer-domain for GCD / LCM / QUOTIENT.
   442	    r.register("CEILING", scalar_fns::ceiling);
   443	    r.register("FLOOR", scalar_fns::floor);
   444	    r.register("CEILING.MATH", scalar_fns::ceiling_math);
   445	    r.register("FLOOR.MATH", scalar_fns::floor_math);
   446	    r.register("MROUND", scalar_fns::mround);
   447	    r.register("ODD", scalar_fns::odd);
   448	    r.register("EVEN", scalar_fns::even);
   449	    r.register("QUOTIENT", scalar_fns::quotient);
   450	    r.register("GCD", scalar_fns::gcd);
   451	    r.register("LCM", scalar_fns::lcm);
   452	    r.register("SINH", scalar_fns::sinh);
   453	    r.register("COSH", scalar_fns::cosh);
   454	    r.register("TANH", scalar_fns::tanh);
   455	    r.register("ASINH", scalar_fns::asinh);
   456	    r.register("ACOSH", scalar_fns::acosh);
   457	    r.register("ATANH", scalar_fns::atanh);
   458	
   459	    // Engine Phase 4.3 V2 batch #1 (W5-51, 2026-05-13): trigonometry.
   460	    // All scalar, single-arg except ATAN2 (two args). Inputs/outputs
   461	    // in radians; pair with DEGREES/RADIANS for degree-mode math.
   462	    r.register("SIN", scalar_fns::sin);
   463	    r.register("COS", scalar_fns::cos);
   464	    r.register("TAN", scalar_fns::tan);
   465	    r.register("ASIN", scalar_fns::asin);
   466	    r.register("ACOS", scalar_fns::acos);
   467	    r.register("ATAN", scalar_fns::atan);
   468	    r.register("ATAN2", scalar_fns::atan2);
   469	
   470	    // Engine Phase 3.7 (W5-40, 2026-05-12): volatile functions. The
   471	    // `is_volatile_function` whitelist in `ql-exec::calcgraph_session`
   472	    // already covers these names; this registration is the executable
   473	    // half. RANDARRAY / INDIRECT / OFFSET / INFO / CELL stay deferred
   474	    // to the Phase 4.3 function library expansion.
   475	    // **W5-71 (Phase 4.5.A.2):** NOW/TODAY moved to the
   476	    // ContextAwareFn tier so they can read the workbook's date_system
   477	    // + locale-aware UTC offset from `EvalContext`. The legacy
   478	    // `volatile::now` / `volatile::today` scalar functions are kept in
   479	    // the source (deprecated callable API) but no longer registered.
   480	    r.register_context_aware("NOW", volatile::now_ctx);
   481	    r.register_context_aware("TODAY", volatile::today_ctx);
   482	    r.register("RAND", volatile::rand);
   483	    r.register("RANDBETWEEN", volatile::randbetween);
   484	
   485	    // **W5-72 (Phase 4.5.B wave 1):** date/time function library —
   486	    // foundational 8 (per W5-68 design § 5.1). DATE/YEAR/MONTH/DAY are
   487	    // ContextAwareFn (need workbook.date_system for serial interp).
   488	    // HOUR/MINUTE/SECOND/TIME are pure scalar (no date_system dep).
   489	    r.register_context_aware("DATE", date_fns::date_ctx);
   490	    r.register_context_aware("YEAR", date_fns::year_ctx);
   491	    r.register_context_aware("MONTH", date_fns::month_ctx);
   492	    r.register_context_aware("DAY", date_fns::day_ctx);
   493	    r.register("HOUR", date_fns::hour);
   494	    r.register("MINUTE", date_fns::minute);
   495	    r.register("SECOND", date_fns::second);
   496	    r.register("TIME", date_fns::time);
   497	
   498	    // **W5-73 (Phase 4.5.B wave 2):** date text parsing + month arithmetic +
   499	    // weekday. All ContextAwareFn (date_system aware; TIMEVALUE is locale-
   500	    // technically but consistent tier).
   501	    r.register_context_aware("DATEVALUE", date_fns::datevalue_ctx);
   502	    r.register_context_aware("TIMEVALUE", date_fns::timevalue_ctx);
   503	    r.register_context_aware("WEEKDAY", date_fns::weekday_ctx);
   504	    r.register_context_aware("EOMONTH", date_fns::eomonth_ctx);
   505	    r.register_context_aware("EDATE", date_fns::edate_ctx);
   506	
   507	    // **W5-74 (Phase 4.5.B wave 3, CLOSES V1 wave 18/18):** business-date
   508	    // + finance basics. DAYS is ScalarFn (pure subtraction); the rest
   509	    // are ContextAwareFn. Holidays arg unsupported in V1 (see GAP-F-09,
   510	    // GAP-F-10) — 3-arg NETWORKDAYS/WORKDAY returns #VALUE!.
   511	    r.register("DAYS", date_fns::days);
   512	    r.register_context_aware("NETWORKDAYS", date_fns::networkdays_ctx);
   513	    r.register_context_aware("WORKDAY", date_fns::workday_ctx);
   514	    r.register_context_aware("YEARFRAC", date_fns::yearfrac_ctx);
   515	
   516	    // **W5-75 (Phase 4.5.C V2 wave, 4 of 6):** date-arithmetic V2 fns.
   517	    // NETWORKDAYS.INTL + WORKDAY.INTL deferred (carry GAP-F-09/10
   518	    // tier-4 dependency).
   519	    r.register_context_aware("DATEDIF", date_fns::datedif_ctx);
   520	    r.register_context_aware("DAYS360", date_fns::days360_ctx);
   521	    r.register_context_aware("WEEKNUM", date_fns::weeknum_ctx);
   522	    r.register_context_aware("ISOWEEKNUM", date_fns::isoweeknum_ctx);
   523	
   524	    // **W5-83 (Phase 4.5.E):** `TEXT(value, format_string)` — format a
   525	    // value into a text string per the workbook's number-format grammar.
   526	    // ContextAwareFn because the renderer needs `DateSystem` for the
   527	    // serial→date conversion path. Parser failures + V2-deferred tokens
   528	    // surface as `#VALUE!`. Companion `format::render` shipped W5-78.
   529	    r.register_context_aware("TEXT", format::text_ctx);
   530	
   531	    // Engine Phase 4.3 V2 batch — range-aware (W5-53, GAP-F-05
   532	    // closure). These use the new `RangeAwareFn` table because the
   533	    // existing `ScalarFn = fn(&[Value]) -> Value` contract can't
   534	    // distinguish "this argument is a range" from "this argument is
   535	    // a scalar criteria". The dispatch in
   536	    // `ql-exec::scalar::eval_scalar_with_cache` checks the range-
   537	    // aware table first.
   538	    r.register_range_aware("SUMIF", range_fns::sumif);
   539	    r.register_range_aware("COUNTIF", range_fns::countif);
   540	
   541	    // Engine Phase 4.3 V2 batch #3 — lookup family (W5-54). Same
   542	    // RangeAwareFn dispatch as SUMIF/COUNTIF. MATCH/INDEX/VLOOKUP/
   543	    // HLOOKUP need 2D shape (rows, cols) on the range arg; CHOOSE
   544	    // takes only scalar args.
   545	    r.register_range_aware("MATCH", range_fns::r#match);
   546	    r.register_range_aware("INDEX", range_fns::index);
   547	    r.register_range_aware("VLOOKUP", range_fns::vlookup);
   548	    r.register_range_aware("HLOOKUP", range_fns::hlookup);
   549	    r.register_range_aware("CHOOSE", range_fns::choose);
   550	
   551	    // Engine Phase 4.3 V2 batch #4 — conditional-aggregate
   552	    // completion (W5-55). Multi-condition variants of SUMIF/COUNTIF
   553	    // + AVERAGEIF / AVERAGEIFS + SUMPRODUCT. All consume
   554	    // RangeAwareFn dispatch.
   555	    r.register_range_aware("AVERAGEIF", range_fns::averageif);
   556	    r.register_range_aware("SUMIFS", range_fns::sumifs);
   557	    r.register_range_aware("COUNTIFS", range_fns::countifs);
   558	    r.register_range_aware("AVERAGEIFS", range_fns::averageifs);
   559	    r.register_range_aware("SUMPRODUCT", range_fns::sumproduct);
   560	
   561	    // Engine Phase 4.3 V2 batch #7 — stats family (W5-58).
   562	    // Closes FN4-01 (100 functions). LARGE/SMALL are k-th order;
   563	    // RANK is 1-based with tie semantics; MEDIAN handles even-count
   564	    // averaging; MODE first-appearance tie-break + #N/A if no repeat.
   565	    r.register_range_aware("LARGE", range_fns::large);
   566	    r.register_range_aware("SMALL", range_fns::small);
   567	    r.register_range_aware("RANK", range_fns::rank);
   568	    r.register_range_aware("RANK.EQ", range_fns::rank); // Modern Excel alias
   569	    r.register_range_aware("RANK.AVG", range_fns::rank_avg);
   570	    r.register_range_aware("CONCAT", range_fns::concat);
   571	    r.register_range_aware("MEDIAN", range_fns::median);
   572	    r.register_range_aware("MODE", range_fns::mode);
   573	    r.register_range_aware("MODE.SNGL", range_fns::mode); // Modern Excel alias
   574	
   575	    r
   576	}
   577	
   578	#[cfg(test)]
   579	mod tests {
   580	    use super::*;
   581	
   582	    #[test]
   583	    fn empty_registry_lookup_none() {
   584	        let r = FunctionRegistry::new();
   585	        assert!(r.is_empty());
   586	        assert!(r.lookup("SUM").is_none());
   587	    }
   588	
   589	    #[test]
   590	    fn default_registry_has_expected_count() {
   591	        let r = default_registry();
   592	        // 61 entries — Phase 0 W4-4 (22 functions + 3 aliases = 25) +
   593	        // AI sentinel (1) + Phase 3.7 volatiles (NOW/TODAY/RAND/
   594	        // RANDBETWEEN = 4) + Phase 4.3 V1 wave 1 (ROUNDUP, ROUNDDOWN,
   595	        // TRUNC, SIGN, EXP, LN, LOG, LOG10, PI, DEGREES, RADIANS, LEN,
   596	        // UPPER, LOWER, TRIM, ISNUMBER, ISTEXT, ISBLANK, ISLOGICAL,
   597	        // ISERROR, ISNA, ISERR = 22) + Phase 4.3 V2 trig (W5-51:
   598	        // SIN, COS, TAN, ASIN, ACOS, ATAN, ATAN2 = 7) + Phase 4.3 V2
   599	        // range-aware (W5-53: SUMIF, COUNTIF = 2) + Phase 4.3 V2
   600	        // lookup family (W5-54: MATCH, INDEX, VLOOKUP, HLOOKUP,
   601	        // CHOOSE = 5) + Phase 4.3 V2 conditional-aggregate
   602	        // completion (W5-55: AVERAGEIF, SUMIFS, COUNTIFS, AVERAGEIFS,
   603	        // SUMPRODUCT = 5) + Phase 4.3 V2 text wave 2 (W5-56: LEFT,
   604	        // RIGHT, MID, FIND, SEARCH, SUBSTITUTE, REPLACE, CONCATENATE,
   605	        // REPT, EXACT = 10) + Phase 4.3 V2 math completion +
   606	        // hyperbolic trig (W5-57: CEILING, FLOOR, MROUND, ODD,
   607	        // EVEN, QUOTIENT, GCD, LCM, SINH, COSH, TANH, ASINH,
   608	        // ACOSH, ATANH = 14) + Phase 4.3 V2 stats family (W5-58:
   609	        // LARGE, SMALL, RANK, RANK.EQ alias, MEDIAN, MODE,
   610	        // MODE.SNGL alias = 7) + Phase 4.3 polish wave 1 (W5-61:
   611	        // PROPER, CLEAN, CEILING.MATH, FLOOR.MATH, RANK.AVG,
   612	        // CONCAT = 6) + Phase 4.5.B wave 1 (W5-72: DATE, YEAR,
   613	        // MONTH, DAY, HOUR, MINUTE, SECOND, TIME = 8) + Phase 4.5.B
   614	        // wave 2 (W5-73: DATEVALUE, TIMEVALUE, WEEKDAY, EOMONTH,
   615	        // EDATE = 5) + Phase 4.5.B wave 3 (W5-74: DAYS, NETWORKDAYS,
   616	        // WORKDAY, YEARFRAC = 4 — CLOSES V1 wave 18/18) + Phase 4.5.C
   617	        // V2 wave (W5-75: DATEDIF, DAYS360, WEEKNUM, ISOWEEKNUM = 4
   618	        // of 6; NETWORKDAYS.INTL + WORKDAY.INTL deferred to tier-4) +
   619	        // Phase 4.5.E (W5-83: TEXT = 1).
   620	        assert_eq!(r.len(), 130);
   621	    }
   622	
   623	    #[test]
   624	    fn range_aware_lookup_returns_registered_function() {
   625	        let r = default_registry();
   626	        assert!(r.lookup_range_aware("SUMIF").is_some());
   627	        assert!(r.lookup_range_aware("sumif").is_some()); // case-insensitive
   628	        assert!(r.lookup_range_aware("COUNTIF").is_some());
   629	        // SUM is NOT range-aware (uses the existing ScalarFn path).
   630	        assert!(r.lookup_range_aware("SUM").is_none());
   631	    }
   632	
   633	    #[test]
   634	    fn scalar_and_range_aware_tiers_are_disjoint() {
   635	        // W5-96: tier disjointness is now structurally enforced by the
   636	        // unified HashMap (one name → one RegisteredFn). The test still
   637	        // verifies that no name resolves through BOTH filter views.
   638	        let r = default_registry();
   639	        for name in r.names() {
   640	            assert!(
   641	                r.lookup_range_aware(name).is_none(),
   642	                "name {name:?} resolves as both Scalar and RangeAware"
   643	            );
   644	        }
   645	    }
   646	
   647	    #[test]
   648	    #[should_panic(expected = "duplicate registration")]
   649	    fn register_range_aware_with_existing_scalar_name_panics() {
   650	        let mut r = FunctionRegistry::new();
   651	        r.register("FOO", scalar_fns::sum);
   652	        r.register_range_aware("FOO", range_fns::sumif);
   653	    }
   654	
   655	    #[test]
   656	    #[should_panic(expected = "duplicate registration")]
   657	    fn duplicate_range_aware_registration_panics() {
   658	        let mut r = FunctionRegistry::new();
   659	        r.register_range_aware("SUMIF", range_fns::sumif);
   660	        r.register_range_aware("SUMIF", range_fns::sumif);
   661	    }
   662	
   663	    // ===== W5-69 Phase 4.5.A.0 — context-aware tier =====
   664	
   665	    // A trivial test fixture: a context-aware fn that returns the
   666	    // workbook's date-system as a Number (1900→1900.0, 1904→1904.0).
   667	    // Lets us verify dispatch + arg-shape without depending on any
   668	    // not-yet-implemented date function.
   669	    fn echo_date_system_year(args: &[Value], ctx: &ql_types::EvalContext) -> Value {
   670	        if !args.is_empty() {
   671	            return Value::Error(ql_types::ErrorValue::Value);
   672	        }
   673	        let n = match ctx.date_system {
   674	            ql_types::DateSystem::Excel1900 => 1900.0,
   675	            ql_types::DateSystem::Excel1904 => 1904.0,
   676	        };
   677	        Value::Number(n)
   678	    }
   679	
   680	    #[test]
   681	    fn context_aware_register_and_lookup_works() {
   682	        let mut r = FunctionRegistry::new();
   683	        r.register_context_aware("ECHO.DS", echo_date_system_year);
   684	        assert!(r.lookup_context_aware("ECHO.DS").is_some());
   685	        assert!(r.lookup_context_aware("echo.ds").is_some()); // case-insensitive
   686	        assert!(r.lookup("ECHO.DS").is_none()); // NOT in scalar table
   687	        assert!(r.lookup_range_aware("ECHO.DS").is_none()); // NOT in range-aware table
   688	    }
   689	
   690	    #[test]
   691	    fn context_aware_fn_receives_eval_context() {
   692	        let mut r = FunctionRegistry::new();
   693	        r.register_context_aware("ECHO.DS", echo_date_system_year);
   694	        let f = r.lookup_context_aware("ECHO.DS").expect("registered");
   695	        let ctx_1900 = ql_types::EvalContext::default();
   696	        assert_eq!(f(&[], &ctx_1900), Value::Number(1900.0));
   697	        let ctx_1904 = ql_types::EvalContext {
   698	            date_system: ql_types::DateSystem::Excel1904,
   699	            ..ql_types::EvalContext::default()
   700	        };
   701	        assert_eq!(f(&[], &ctx_1904), Value::Number(1904.0));
   702	    }
   703	
   704	    #[test]
   705	    fn context_aware_tier_disjoint_from_scalar() {
   706	        // W5-96: structurally enforced by the unified HashMap.
   707	        let r = default_registry();
   708	        for name in r.names() {
   709	            assert!(
   710	                r.lookup_context_aware(name).is_none(),
   711	                "{name:?} resolves as both Scalar and ContextAware"
   712	            );
   713	        }
   714	    }
   715	
   716	    #[test]
   717	    fn context_aware_tier_disjoint_from_range_aware() {
   718	        let r = default_registry();
   719	        for name in r.range_aware_names() {
   720	            assert!(
   721	                r.lookup_context_aware(name).is_none(),
   722	                "{name:?} resolves as both RangeAware and ContextAware"
   723	            );
   724	        }
   725	    }
   726	
   727	    #[test]
   728	    #[should_panic(expected = "duplicate registration")]
   729	    fn register_context_aware_with_existing_scalar_name_panics() {
   730	        let mut r = FunctionRegistry::new();
   731	        r.register("FOO", scalar_fns::sum);
   732	        r.register_context_aware("FOO", echo_date_system_year);
   733	    }
   734	
   735	    #[test]
   736	    #[should_panic(expected = "duplicate registration")]
   737	    fn register_context_aware_with_existing_range_aware_name_panics() {
   738	        let mut r = FunctionRegistry::new();
   739	        r.register_range_aware("FOO", range_fns::sumif);
   740	        r.register_context_aware("FOO", echo_date_system_year);
   741	    }
   742	
   743	    #[test]
   744	    #[should_panic(expected = "duplicate registration")]
   745	    fn register_scalar_with_existing_context_aware_name_panics() {
   746	        let mut r = FunctionRegistry::new();
   747	        r.register_context_aware("FOO", echo_date_system_year);
   748	        r.register("FOO", scalar_fns::sum);
   749	    }
   750	
   751	    #[test]
   752	    #[should_panic(expected = "duplicate registration")]
   753	    fn register_range_aware_with_existing_context_aware_name_panics() {
   754	        let mut r = FunctionRegistry::new();
   755	        r.register_context_aware("FOO", echo_date_system_year);
   756	        r.register_range_aware("FOO", range_fns::sumif);
   757	    }
   758	
   759	    #[test]
   760	    #[should_panic(expected = "duplicate registration")]

exec
/bin/zsh -lc "nl -ba crates/ql-functions/src/range_aware_fns.rs | sed -n '1,140p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
     1	//! Range-aware function dispatch (W5-53, GAP-F-05 closure).
     2	//!
     3	//! The base `ScalarFn = fn(&[Value]) -> Value` contract can't carry
     4	//! per-argument range-vs-scalar metadata. Functions like SUMIF /
     5	//! COUNTIF / VLOOKUP need to distinguish "this argument is a range
     6	//! (criteria_range, sum_range, lookup_table)" from "this argument is
     7	//! a single value (criteria, lookup_value)". Flattening every range
     8	//! into a `Vec<Value>` and concatenating before dispatch loses that
     9	//! structure.
    10	//!
    11	//! Phase 4.3 V2 (W5-53) introduces a parallel function-table tier
    12	//! that preserves the shape:
    13	//!
    14	//! ```rust,ignore
    15	//! pub enum FnArg {
    16	//!     Scalar(Value),
    17	//!     Range(Vec<Value>),
    18	//! }
    19	//! pub type RangeAwareFn = fn(&[FnArg]) -> Value;
    20	//! ```
    21	//!
    22	//! The `eval_scalar_with_cache` dispatch in `ql-exec::scalar` checks
    23	//! the range-aware table first; if a function is registered there,
    24	//! args are built as `Vec<FnArg>` (AggregateNameRef → `Range`, other
    25	//! plans → `Scalar(evaluated)`). Otherwise it falls through to the
    26	//! existing `ScalarFn` path with `Vec<Value>` flattening.
    27	//!
    28	//! Migration of existing functions to `RangeAwareFn` is NOT required
    29	//! — they keep working under `ScalarFn`. Only new functions that
    30	//! genuinely need per-arg range distinction use the new tier.
    31	
    32	use ql_types::{coercion, ErrorValue, Value};
    33	
    34	/// Cross-module ergonomic enum for "numeric coerce, with Blank-skip and
    35	/// Error-propagation surfaced as distinct match arms." Wraps the central
    36	/// `ql_types::coercion::to_number_strict_skip_blank` so callers can use a
    37	/// match on three variants instead of `Result<Option<f64>, ErrorValue>`.
    38	///
    39	/// **W5-64 (Phase 4.4.A consolidation):** Was previously duplicated in
    40	/// `scalar_fns.rs` and `range_fns.rs`. Codex audit MEDIUM 3: keep the central
    41	/// API neutral (Result/Option) but the function-aware enum stays in
    42	/// ql-functions where it belongs.
    43	///
    44	/// **W5-67 closure (Codex mega-audit LOW 2):** scoped to `pub(crate)` —
    45	/// internal cross-module helper, not part of the public API. External
    46	/// callers should use `ql_types::coercion::to_number_strict_skip_blank`
    47	/// directly.
    48	#[derive(Debug)]
    49	pub(crate) enum NumericArg {
    50	    Number(f64),
    51	    Skip,
    52	    Error(ErrorValue),
    53	}
    54	
    55	/// W5-64 (Phase 4.4.A): cross-module ergonomic wrapper around
    56	/// `coercion::to_number_strict_skip_blank`. Both `scalar_fns` and `range_fns`
    57	/// use this; the duplicate `coerce_numeric` previously in each module was
    58	/// removed.
    59	///
    60	/// **W5-67 closure (Codex mega-audit LOW 2):** scoped to `pub(crate)`.
    61	pub(crate) fn coerce_numeric(v: &Value) -> NumericArg {
    62	    match coercion::to_number_strict_skip_blank(v) {
    63	        Ok(Some(n)) => NumericArg::Number(n),
    64	        Ok(None) => NumericArg::Skip,
    65	        Err(e) => NumericArg::Error(e),
    66	    }
    67	}
    68	
    69	/// A single argument to a range-aware function. `Scalar` carries a
    70	/// pre-evaluated `Value`; `Range` carries the flat row-major
    71	/// iteration of a range reference (per `CellEnv::read_range`) PLUS
    72	/// its 2D shape (rows × cols).
    73	///
    74	/// Shape is required for 2D-addressing functions like VLOOKUP /
    75	/// HLOOKUP / INDEX, which compute `values[row * cols + col]`. 1D
    76	/// consumers (SUMIF, COUNTIF) ignore shape and iterate `values`
    77	/// flat — `rows * cols == values.len()` is the invariant the eval
    78	/// dispatch maintains.
    79	///
    80	/// `values` is a `Vec<Value>` rather than a slice because the
    81	/// producer (eval) owns the buffer. For very large ranges (whole-
    82	/// column refs over millions of cells) this is the same cost as the
    83	/// existing flattening path; range-aware functions get both the
    84	/// flat data and the structure.
    85	#[derive(Clone, Debug, PartialEq)]
    86	pub enum FnArg {
    87	    Scalar(Value),
    88	    Range {
    89	        values: Vec<Value>,
    90	        rows: usize,
    91	        cols: usize,
    92	    },
    93	}
    94	
    95	impl FnArg {
    96	    /// Construct a 1D `Range` (1 row × N cols, or N rows × 1 col —
    97	    /// caller decides which axis). Convenience for tests + 1D
    98	    /// consumers; uses `rows = 1, cols = values.len()` by default.
    99	    /// Production callers should construct the variant directly so
   100	    /// the shape reflects the source range.
   101	    #[cfg(test)]
   102	    pub fn range_1d(values: Vec<Value>) -> Self {
   103	        let cols = values.len();
   104	        FnArg::Range {
   105	            values,
   106	            rows: 1,
   107	            cols,
   108	        }
   109	    }
   110	
   111	    /// Construct a 2D `Range` with explicit shape.
   112	    #[cfg(test)]
   113	    pub fn range_2d(values: Vec<Value>, rows: usize, cols: usize) -> Self {
   114	        debug_assert_eq!(rows * cols, values.len(), "shape mismatch");
   115	        FnArg::Range { values, rows, cols }
   116	    }
   117	
   118	    /// Convenience for tests + scalar-only call sites: returns the
   119	    /// inner `Value` if this is a scalar arg, panics otherwise.
   120	    /// Production code should `match` on the variant explicitly.
   121	    #[cfg(test)]
   122	    pub fn unwrap_scalar(&self) -> &Value {
   123	        match self {
   124	            FnArg::Scalar(v) => v,
   125	            FnArg::Range { .. } => panic!("FnArg::unwrap_scalar called on a Range arg"),
   126	        }
   127	    }
   128	
   129	    /// Convenience for tests: returns the inner values if Range,
   130	    /// panics otherwise.
   131	    #[cfg(test)]
   132	    pub fn unwrap_range(&self) -> &[Value] {
   133	        match self {
   134	            FnArg::Range { values, .. } => values.as_slice(),
   135	            FnArg::Scalar(_) => panic!("FnArg::unwrap_range called on a Scalar arg"),
   136	        }
   137	    }
   138	}
   139	
   140	/// Signature for range-aware built-in functions. The evaluator

exec
/bin/zsh -lc "rg \"struct FunctionRegistry|\\.scalar|range_aware|context_aware|lookup_any|RegisteredFn|fns:\" -n crates tests docs | head -200" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
rg: tests: No such file or directory (os error 2)
docs/known-gaps.md:85:| ~~GAP-F-05~~ | ~~`ScalarFn` cannot carry range-vs-scalar argument metadata.~~ **CLOSED in Engine Phase 4.3 V2 (W5-53; refined W5-54).** Approach (a) — parallel `RangeAwareFn = fn(&[FnArg]) -> Value` table — chosen instead of full migration of the ~60 existing scalar entries. `FnArg` lives in `crates/ql-functions/src/range_aware_fns.rs`. **Final shape (W5-54 refactor):** `enum FnArg { Scalar(Value), Range { values: Vec<Value>, rows: usize, cols: usize } }` — `Range` is a struct variant carrying 2D shape, invariant `rows * cols == values.len()`. 1D consumers (SUMIF, COUNTIF) pattern-match `{ values, .. }` and ignore shape; 2D consumers (VLOOKUP, HLOOKUP, INDEX, MATCH) consume `rows`/`cols`. `FunctionRegistry` carries a second table `range_aware_fns: HashMap<&'static str, RangeAwareFn>` with `register_range_aware` / `lookup_range_aware`. Cross-table name collision panics at registration. Eval dispatch in `crates/ql-exec/src/scalar.rs::eval_scalar_with_cache` checks `lookup_range_aware` FIRST and constructs `Vec<FnArg>` from `ExprPlan` args (AggregateNameRef → `Range { values, rows, cols }` via `env.read_range_with_shape`, other plans → `Scalar` via recursive eval). SUMIF + COUNTIF shipped on this infra W5-53; SUMIFS / AVERAGEIF / SUMPRODUCT / VLOOKUP / HLOOKUP / MATCH / INDEX / CHOOSE + LARGE/SMALL/RANK/MEDIAN/MODE all live on it by W5-58. **W5-60 closure** added strict 2D shape validation in IFS family + SUMPRODUCT (`(rows, cols)` consensus, not flat length); `env.read_range_with_shape` distinguishes open-ended (clamp) from bounded (preserve+pad-with-Blank) ranges. 5 e2e tests in `workbook_runtime` + range-fn unit tests + 4 registry conflict/disjoint tests + extended `is_aggregate_function_lists_only_registered_aggregates` invariant test + W5-60 6 new 2D-shape regression tests. |
docs/process/audit-protocol.md:233:1. Register in `default_registry` via `register_range_aware`
docs/process/audit-protocol.md:249:### 7. Range-aware dispatch checks `lookup_range_aware` FIRST
docs/process/audit-protocol.md:253:if let Some(raf) = registry.lookup_range_aware(name) { /* build Vec<FnArg> */ }
docs/process/audit-protocol.md:259:**Always use `register_range_aware` for range-aware functions.** The registry's cross-table-collision panic catches the reverse (registering both scalar and range-aware with the same name).
docs/process/audit-protocol.md:263:The `names()` iterator on `FunctionRegistry` only walks the scalar `fns` table — it does NOT include range-aware names. Any code that enumerates the registry (e.g. to dump the function list) must walk both `fns` AND `range_aware_fns`. Currently no caller does this; this is a latent bug if any enumeration code is added.
docs/architecture/2026-05-14-array-formulas-and-spills.md:182:- Existing `ScalarFn` / `RangeAwareFn` / `ContextAwareFn` registrations re-route to the unified `FunctionFn` via shim adapters. Adapters live in `ql-functions` itself; downstream callers stay unchanged in v1 by exposing the legacy `register_scalar` / `register_range_aware` / `register_context_aware` helpers that internally build the unified `FunctionFn`.
docs/architecture/2026-05-14-array-formulas-and-spills.md:555:Per § 6.2: collapse the four tiers into one `FunctionFn` ABI. Legacy `register_scalar_fn` / `register_range_aware_fn` / `register_context_aware_fn` helpers preserved as shim adapters. No new tier.
docs/audits/2026-05-13-engine-session-final-handoff.md:72:**Decision:** Parallel `RangeAwareFn = fn(&[FnArg]) -> Value` table in `FunctionRegistry`. `enum FnArg { Scalar(Value), Range { values, rows, cols } }`. Eval dispatch checks `lookup_range_aware` FIRST, constructs `Vec<FnArg>` from `ExprPlan` args (AggregateNameRef → Range via `env.read_range_with_shape`, others → Scalar via recursive eval). The 60+ existing scalar functions stay under `ScalarFn` unchanged. NO migration.
docs/audits/2026-05-13-engine-session-final-handoff.md:117:- `crates/ql-functions/src/range_aware_fns.rs` (NEW): `FnArg` enum + `RangeAwareFn` type.
docs/audits/2026-05-13-engine-session-final-handoff.md:119:- `crates/ql-functions/src/registry.rs`: parallel `range_aware_fns` table + `register_range_aware` / `lookup_range_aware`.
docs/audits/2026-05-13-engine-session-final-handoff.md:120:- `crates/ql-exec/src/scalar.rs::eval_scalar_with_cache`: checks `lookup_range_aware` FIRST; builds `Vec<FnArg>` from `ExprPlan` args.
docs/audits/2026-05-13-engine-session-final-handoff.md:158:7. **`to_number_strict` rejects Inf/NaN at INPUT** (not at output via sanitize_f64). Affects trig fns: `Value::Number(f64::INFINITY)` returns `#NUM!` even for fns like `atan` whose math is well-defined for ±∞.
docs/audits/2026-05-13-engine-session-final-handoff.md:230:7. **Range-aware dispatch checks `lookup_range_aware` FIRST** — registering a range-aware function as scalar silently loses shape (per Codex).
docs/audits/2026-05-13-engine-session-final-handoff.md:248:5. **Phase 4.4 mega-audit closure** ✅ SHIPPED W5-67 — Codex + Sonnet parallel mega-audit caught 2 HIGH + 7 MEDIUM + 3 LOW. **Codex HIGH 1**: COUNT canon mismatch (provenance-aware semantics; relabeled as V1 divergence, GAP-F-06 filed). **Codex HIGH 2**: error-matrix overclaimed test coverage (each-row-has-test fixed to honest cited/`—` framing). **Sonnet S1**: HLOOKUP had zero tests (4 tests added). Plus: CHOOSE matrix tests (4), ROUNDUP binary-float divergence test, `to_int_arg` NaN/Inf hole closed, `ql-types::lib.rs` re-exports added, `to_text_for_arg` docstring corrected, `range_aware_fns::coerce_numeric` scoped to `pub(crate)`, AI tag ⚪ (not 🟡), stale comments fixed.
docs/architecture/2026-05-13-dates-times-formats.md:129:// ql-functions::context_aware_fns (new module)
docs/architecture/2026-05-13-dates-times-formats.md:139:`FunctionRegistry` gains a third table `context_aware_fns: HashMap<&'static str, ContextAwareFn>`. Eval dispatch precedence: `lookup_range_aware` FIRST → `lookup_context_aware` SECOND → `lookup` (scalar) LAST. The W5-65 `names_all()` becomes a 3-table chain.
docs/audits/2026-05-13-phase-4.4-megaudit-prompt.md:36:crates/ql-functions/src/range_aware_fns.rs            # cross-module NumericArg + coerce_numeric shim (W5-64)
docs/audits/2026-05-13-phase-4.4-megaudit-prompt.md:39:crates/ql-functions/src/registry.rs                   # range_aware_names() + names_all() (W5-65)
docs/audits/2026-05-13-phase-4.4-megaudit-prompt.md:71:   returns `Result<Option<f64>, ErrorValue>`. The `range_aware_fns` cross-
docs/audits/2026-05-13-phase-4.4-megaudit-prompt.md:180:The W5-65 commit added `range_aware_names()` and `names_all()` to the
docs/audits/2026-05-13-phase-4.4-megaudit-prompt.md:184:   `range_aware_fns.keys()`. Both directions enumerated?
docs/audits/2026-05-13-phase-4.4-megaudit-prompt.md:189:3. Is there a way for a function to be in `range_aware_fns` but NOT
docs/audits/2026-05-13-phase-4.4-megaudit-prompt.md:201:   workarounds in callers? (E.g., does `range_aware_fns::coerce_numeric`
docs/audits/2026-05-13-phase-4.4-megaudit-prompt.md:210:1. The `coerce_numeric` shim in `range_aware_fns` calls
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:28:- New types FunctionArg / FunctionReturn / FunctionContext / FunctionFn / RegisteredFn live in ql-functions.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:30:- Internal storage is one HashMap<name, RegisteredFn>; legacy lookup methods are filter views on the enum variant.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:32:- New register_unified / lookup_unified / lookup_any for the array tier.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:43:3. Does the unified storage preserve EXACT semantics of all eight pre-W5-96 register_* and lookup_* methods? Any silent behavior change between pre- and post-W5-96 (e.g. names()/range_aware_names() iteration order) would surface as test instability later.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:44:4. Is the lookup_any return type — Option<&RegisteredFn> — usable from the eval site that needs to call through the dispatch? Pattern-matching against a borrowed RegisteredFn with `Scalar(f) => f(args)` should work because the inner fn pointer is Copy; verify.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:238:- Existing `ScalarFn` / `RangeAwareFn` / `ContextAwareFn` registrations re-route to the unified `FunctionFn` via shim adapters. Adapters live in `ql-functions` itself; downstream callers stay unchanged in v1 by exposing the legacy `register_scalar` / `register_range_aware` / `register_context_aware` helpers that internally build the unified `FunctionFn`.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:562:    keyed by name and valued by a `RegisteredFn` enum that tags the tier.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:575:    - `RegisteredFn` — internal tagged-union: `Scalar` / `RangeAware` /
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:580:      `HashMap<&str, RegisteredFn>`.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:585:    - `register` / `register_range_aware` / `register_context_aware` store
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:586:      the appropriate `RegisteredFn::*` variant.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:587:    - `lookup` / `lookup_range_aware` / `lookup_context_aware` /
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:588:      `names` / `range_aware_names` / `context_aware_names` filter on
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:590:    - The ~130 existing `r.register("SUM", scalar_fns::sum)` calls in
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:596:    - `lookup_any(name) -> Option<&RegisteredFn>` — return the tagged
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:604:    HashMap enforces this structurally — one name → one RegisteredFn — so
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:608:    `lookup_range_aware` → `lookup_context_aware` → `lookup` as three
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:611:    migration to one `match RegisteredFn` lands in W5-101 (Phase 4.7.G)
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:618:      None for Unified entries; `lookup_any` sees it).
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:624:    - `lookup_any` returns the correct enum variant per tier.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:661:    13	//! one map keyed by name, valued by a `RegisteredFn` enum that tags the
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:667:    19	//! lookup methods filter on the enum variant (e.g. `lookup_range_aware`
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:669:    21	//! `RegisteredFn::RangeAware(raf)`). Eval-site migration to a single
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:674:    26	//! FunctionFn)` and are stored as `RegisteredFn::Unified(_)`. Eval-site
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:682:    34	use crate::context_aware_fns::ContextAwareFn;
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:683:    35	use crate::range_aware_fns::RangeAwareFn;
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:769:   121	/// legacy filter-views (`lookup_scalar` / `lookup_range_aware` /
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:770:   122	/// `lookup_context_aware`); W5-101 (Phase 4.7.G) migrates that site
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:771:   123	/// to a single `match` on `RegisteredFn`.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:773:   125	pub enum RegisteredFn {
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:791:   143	/// keyed by name, valued by `RegisteredFn`. Legacy `register_*` /
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:794:   146	/// `r.register("SUM", scalar_fns::sum)` lines in `default_registry`).
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:796:   148	/// **Disjointness:** a single name maps to ONE `RegisteredFn` (the
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:801:   153	pub struct FunctionRegistry {
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:802:   154	    fns: HashMap<&'static str, RegisteredFn>,
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:816:   168	            fns: HashMap::new(),
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:824:   176	    fn insert_or_panic(&mut self, name: &'static str, f: RegisteredFn, method: &'static str) {
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:840:   192	    /// `RegisteredFn::Scalar(f)`.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:850:   202	        self.insert_or_panic(name, RegisteredFn::Scalar(f), "FunctionRegistry::register");
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:854:   206	    /// internally as `RegisteredFn::RangeAware(f)`. Same canonical-
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:856:   208	    pub fn register_range_aware(&mut self, name: &'static str, f: RangeAwareFn) {
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:859:   211	            RegisteredFn::RangeAware(f),
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:860:   212	            "FunctionRegistry::register_range_aware",
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:867:   219	    /// `RegisteredFn::ContextAware(f)`.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:868:   220	    pub fn register_context_aware(&mut self, name: &'static str, f: ContextAwareFn) {
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:871:   223	            RegisteredFn::ContextAware(f),
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:872:   224	            "FunctionRegistry::register_context_aware",
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:879:   231	    /// `RegisteredFn::Unified(f)`.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:883:   235	            RegisteredFn::Unified(f),
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:889:   241	    /// if the registered entry is `RegisteredFn::Scalar(_)`. Other tiers
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:895:   247	            Some(RegisteredFn::Scalar(f)) => Some(*f),
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:901:   253	    /// registered as `RegisteredFn::RangeAware(_)`. Callers should check
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:904:   256	    pub fn lookup_range_aware(&self, name: &str) -> Option<RangeAwareFn> {
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:907:   259	            Some(RegisteredFn::RangeAware(f)) => Some(*f),
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:913:   265	    /// context-aware fn iff registered as `RegisteredFn::ContextAware(_)`.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:914:   266	    /// Callers should check this AFTER `lookup_range_aware` but BEFORE
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:915:   267	    /// `lookup`. Dispatch order: `range_aware` → `context_aware` → `scalar`.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:916:   268	    pub fn lookup_context_aware(&self, name: &str) -> Option<ContextAwareFn> {
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:919:   271	            Some(RegisteredFn::ContextAware(f)) => Some(*f),
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:926:   278	    /// `RegisteredFn::Unified(_)`. Used by the eval-site array-dispatch
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:931:   283	            Some(RegisteredFn::Unified(f)) => Some(*f),
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:937:   289	    /// `RegisteredFn` enum directly. Lets the eval site perform a
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:941:   293	    pub fn lookup_any(&self, name: &str) -> Option<&RegisteredFn> {
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:946:   298	    /// Iterator over names registered as `RegisteredFn::Scalar(_)`.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:952:   304	            .filter(|(_, v)| matches!(v, RegisteredFn::Scalar(_)))
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:957:   309	    /// `RegisteredFn::RangeAware(_)`.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:958:   310	    pub fn range_aware_names(&self) -> impl Iterator<Item = &&'static str> + '_ {
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:961:   313	            .filter(|(_, v)| matches!(v, RegisteredFn::RangeAware(_)))
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:966:   318	    /// `RegisteredFn::ContextAware(_)`.
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:967:   319	    pub fn context_aware_names(&self) -> impl Iterator<Item = &&'static str> + '_ {
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:997:    25	pub mod context_aware_fns;
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:1000:    28	pub mod range_aware_fns;
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:1008:    36	pub use context_aware_fns::ContextAwareFn;
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:1009:    37	pub use range_aware_fns::{FnArg, RangeAwareFn};
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:1012:    40	    RegisteredFn, ScalarFn,
docs/audits/2026-05-13-finalization-planning-claude.md:42:   - W5-53 RangeAwareFn dispatch goes through `lookup_range_aware` FIRST
docs/architecture/2026-05-13-coercion-matrix.md:20:1. **Existing coercion code is well-built but ad-hoc helpers have proliferated.** `ql-types::coercion` centralizes 5 public functions (`to_number_strict`, `to_number_lenient`, `to_logical`, `to_text_for_display`, `to_text_for_formula`, plus the `sanitize_f64` overflow leaf). However, three private helpers (`scalar_fns::coerce_numeric` (×2 — duplicated across `scalar_fns.rs` and `range_fns.rs`), `scalar_fns::coerce_text`, `scalar_fns::coerce_int_arg`) wrap the centralized fns to support function-arg patterns that the central module doesn't directly express (Blank-skip in aggregates, position/length integer extraction, text-result fallback). These helpers are correct individually but have drifted slightly between callers — e.g. the W5-60 audit caught that not all 2D-shape-aware fns handled `FnArg::Range` vs `FnArg::Scalar` consistently.
docs/architecture/2026-05-13-coercion-matrix.md:47:### 2.3 Private helper in `ql-functions::range_fns` (1 helper, DUPLICATE of `scalar_fns::coerce_numeric`)
docs/architecture/2026-05-13-coercion-matrix.md:54:- **`format_number_for_text`** (scalar_fns:1361): converts Number to text via integer-rendering-without-trailing-.0 rule. Used by CONCATENATE / CONCAT / FIND/SEARCH coercion paths.
docs/architecture/2026-05-13-coercion-matrix.md:55:- **`value_to_concat_text`** (range_fns:1411, W5-61): CONCAT-specific text coercion. Functionally equivalent to `coerce_text` but reimplemented inside range_fns to avoid cross-crate private import.
docs/architecture/2026-05-13-coercion-matrix.md:168:4. **(Codex MEDIUM 4 fix)** Coverage check: `crates/ql-functions/tests/coverage.rs` walks the registry's BOTH tables (scalar `fns` AND `range_aware_fns`) and asserts every registered name has at least one matrix-test entry. `FunctionRegistry::names()` currently exposes only scalar names (`registry.rs:115`); the coverage script either uses an extended API (`fn names_all()`) or directly inspects both tables. Without this, new fns landing in `range_aware_fns` (MATCH, VLOOKUP, COUNTIF, RANK, CONCAT, etc.) can slip through without matrix coverage.
docs/architecture/2026-05-13-coercion-matrix.md:245:- **MEDIUM 4 fix**: coverage check covers BOTH `fns` AND `range_aware_fns` tables, not just `FunctionRegistry::names()` (scalar-only).
crates/ql-ai/src/lib.rs:6://! sentinel TODAY; `ql-functions::scalar_fns::ai` dispatches to
crates/ql-functions/src/wildcard.rs:36://! - `range_fns::build_predicate` — when SUMIF/COUNTIF/SUMIFS/AVERAGEIF
crates/ql-functions/src/wildcard.rs:38://! - `scalar_fns::search` — wildcards in the find_text arg.
crates/ql-types/src/coercion.rs:142:/// `ql-functions::scalar_fns::format_number_for_text`. Phase 4.5 will replace this with a
docs/audits/2026-05-13-phase-4.5-megaudit-sonnet.md:180:reasons like `"W5-72; covered by date_fns::tests"`. The W5-62 Codex
docs/audits/2026-05-13-phase-4.5-megaudit-prompt.md:40:crates/ql-functions/src/context_aware_fns.rs           # W5-69 — ContextAwareFn type
docs/audits/2026-05-13-phase-4.5-megaudit-prompt.md:45:crates/ql-exec/src/scalar.rs                           # W5-69 — context_aware dispatch wiring
docs/audits/2026-05-13-phase-4.5-megaudit-prompt.md:75:1. **Dispatch order**: `eval_scalar_with_cache` checks range_aware FIRST, context_aware SECOND, scalar LAST. If a name lands in two tables by mistake, which wins? (Registry should panic at registration, but verify the runtime fallback.)
docs/audits/2026-05-13-phase-4.5-megaudit-prompt.md:76:2. **Test envs**: `MapEnv` and any benchmark env — do they use the default context? When a test calls `=DATE(2024,1,1)` against MapEnv, does it correctly route through context_aware and get Excel1900 semantics?
docs/audits/2026-05-13-phase-4.5-megaudit-prompt.md:160:`coverage.rs::EXPLICITLY_DEFERRED` keeps growing — 22 new entries from Phase 4.5 (8 wave 1 + 5 wave 2 + 4 wave 3 + 4 wave V2 + 1 schema entry?). Most reasons say "covered by date_fns::tests_waveX". The W5-62 Codex audit flagged that EXPLICITLY_DEFERRED is becoming a waiver list, not a coverage gate. Have we made it worse?
docs/audits/2026-05-13-session-final-megaudit-codex.txt:64:crates/ql-functions/src/range_aware_fns.rs        # FnArg infra
docs/audits/2026-05-13-session-final-megaudit-codex.txt:66:crates/ql-exec/src/scalar.rs                      # eval dispatch (lookup_range_aware FIRST)
docs/audits/2026-05-13-session-final-megaudit-codex.txt:546:**Decision:** Parallel `RangeAwareFn = fn(&[FnArg]) -> Value` table in `FunctionRegistry`. `enum FnArg { Scalar(Value), Range { values, rows, cols } }`. Eval dispatch checks `lookup_range_aware` FIRST, constructs `Vec<FnArg>` from `ExprPlan` args (AggregateNameRef → Range via `env.read_range_with_shape`, others → Scalar via recursive eval). The 60+ existing scalar functions stay under `ScalarFn` unchanged. NO migration.
docs/audits/2026-05-13-session-final-megaudit-codex.txt:591:- `crates/ql-functions/src/range_aware_fns.rs` (NEW): `FnArg` enum + `RangeAwareFn` type.
docs/audits/2026-05-13-session-final-megaudit-codex.txt:593:- `crates/ql-functions/src/registry.rs`: parallel `range_aware_fns` table + `register_range_aware` / `lookup_range_aware`.
docs/audits/2026-05-13-session-final-megaudit-codex.txt:594:- `crates/ql-exec/src/scalar.rs::eval_scalar_with_cache`: checks `lookup_range_aware` FIRST; builds `Vec<FnArg>` from `ExprPlan` args.
docs/audits/2026-05-13-session-final-megaudit-codex.txt:632:7. **`to_number_strict` rejects Inf/NaN at INPUT** (not at output via sanitize_f64). Affects trig fns: `Value::Number(f64::INFINITY)` returns `#NUM!` even for fns like `atan` whose math is well-defined for ±∞.
docs/audits/2026-05-13-session-final-megaudit-codex.txt:698:7. **Range-aware dispatch checks `lookup_range_aware` FIRST** — registering a range-aware function as scalar silently loses shape (per Codex).
docs/audits/2026-05-13-session-final-megaudit-codex.txt:1002:1. Register in `default_registry` via `register_range_aware`
docs/audits/2026-05-13-session-final-megaudit-codex.txt:1018:### 7. Range-aware dispatch checks `lookup_range_aware` FIRST
docs/audits/2026-05-13-session-final-megaudit-codex.txt:1022:if let Some(raf) = registry.lookup_range_aware(name) { /* build Vec<FnArg> */ }
docs/audits/2026-05-13-session-final-megaudit-codex.txt:1028:**Always use `register_range_aware` for range-aware functions.** The registry's cross-table-collision panic catches the reverse (registering both scalar and range-aware with the same name).
docs/audits/2026-05-13-session-final-megaudit-codex.txt:1032:The `names()` iterator on `FunctionRegistry` only walks the scalar `fns` table — it does NOT include range-aware names. Any code that enumerates the registry (e.g. to dump the function list) must walk both `fns` AND `range_aware_fns`. Currently no caller does this; this is a latent bug if any enumeration code is added.
docs/audits/2026-05-13-session-final-megaudit-codex.txt:1119:   - Range-aware dispatch checks `lookup_range_aware` FIRST; mis-registering as scalar silently loses shape
docs/audits/2026-05-13-session-final-megaudit-codex.txt:1282:crates/ql-functions/src/range_aware_fns.rs        # FnArg infra (W5-53)
docs/audits/2026-05-13-session-final-megaudit-codex.txt:1845:| ~~GAP-F-05~~ | ~~`ScalarFn` cannot carry range-vs-scalar argument metadata.~~ **CLOSED in Engine Phase 4.3 V2 (W5-53).** Approach (a) — parallel `RangeAwareFn = fn(&[FnArg]) -> Value` table — chosen instead of full migration of the ~60 existing scalar entries. `FnArg { Scalar(Value), Range(Vec<Value>) }` lives in `crates/ql-functions/src/range_aware_fns.rs`. `FunctionRegistry` carries a second table `range_aware_fns: HashMap<&'static str, RangeAwareFn>` with `register_range_aware` / `lookup_range_aware`. Cross-table name collision panics at registration. Eval dispatch in `crates/ql-exec/src/scalar.rs::eval_scalar_with_cache` checks `lookup_range_aware` FIRST and constructs `Vec<FnArg>` from `ExprPlan` args (AggregateNameRef → Range via env.read_range, other plans → Scalar via recursive eval). SUMIF + COUNTIF shipped on this infra in the same commit; SUMIFS / AVERAGEIF / SUMPRODUCT / VLOOKUP / HLOOKUP / MATCH / INDEX / CHOOSE are V2 follow-ups now safely unblocked. 5 e2e tests in `workbook_runtime` + 25 unit tests in `range_fns` + 4 registry conflict/disjoint tests + extended `is_aggregate_function_lists_only_registered_aggregates` invariant test. |
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2599:/bin/zsh -lc "nl -ba crates/ql-functions/src/range_aware_fns.rs | sed -n '1,220p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2773:   188	    // parallel `range_aware_fns` table. But the binder uses this list to
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2777:   192	    // eval-side dispatch routes correctly via `lookup_range_aware` first.
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2827:    10	use crate::range_aware_fns::RangeAwareFn;
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2835:    18	/// W5-53 adds a parallel `range_aware_fns` table for functions that
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2841:    24	pub struct FunctionRegistry {
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2842:    25	    fns: HashMap<&'static str, ScalarFn>,
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2843:    26	    range_aware_fns: HashMap<&'static str, RangeAwareFn>,
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2857:    40	            fns: HashMap::new(),
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2858:    41	            range_aware_fns: HashMap::new(),
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2892:    75	    /// cannot live in both `fns` and `range_aware_fns`.
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2893:    76	    pub fn register_range_aware(&mut self, name: &'static str, f: RangeAwareFn) {
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2896:    79	            "FunctionRegistry::register_range_aware: name must not be empty"
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2900:    83	            "FunctionRegistry::register_range_aware: name {name:?} must be canonical \
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2905:    88	            "FunctionRegistry::register_range_aware: {name:?} is already registered \
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2908:    91	        let prior = self.range_aware_fns.insert(name, f);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2911:    94	            "FunctionRegistry::register_range_aware: duplicate registration for {name:?}"
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2927:   110	    pub fn lookup_range_aware(&self, name: &str) -> Option<RangeAwareFn> {
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2929:   112	        self.range_aware_fns.get(upper.as_str()).copied()
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2937:   120	        self.fns.len() + self.range_aware_fns.len()
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2941:   124	        self.fns.is_empty() && self.range_aware_fns.is_empty()
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2950:   133	    r.register("SUM", scalar_fns::sum);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2951:   134	    r.register("AVERAGE", scalar_fns::average);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2952:   135	    r.register("AVG", scalar_fns::average); // Common alias; not Excel-canonical but
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2955:   138	    r.register("COUNT", scalar_fns::count);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2956:   139	    r.register("COUNTA", scalar_fns::counta);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2957:   140	    r.register("MIN", scalar_fns::min);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2958:   141	    r.register("MAX", scalar_fns::max);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2959:   142	    r.register("PRODUCT", scalar_fns::product);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2962:   145	    r.register("VAR", scalar_fns::var_s); // Excel alias for VAR.S in legacy mode.
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2963:   146	    r.register("VAR.S", scalar_fns::var_s);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2964:   147	    r.register("VAR.P", scalar_fns::var_p);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2965:   148	    r.register("STDEV", scalar_fns::stdev_s);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2966:   149	    r.register("STDEV.S", scalar_fns::stdev_s);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2967:   150	    r.register("STDEV.P", scalar_fns::stdev_p);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2970:   153	    r.register("IF", scalar_fns::r#if);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2971:   154	    r.register("AND", scalar_fns::and);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2972:   155	    r.register("OR", scalar_fns::or);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2973:   156	    r.register("NOT", scalar_fns::not);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2974:   157	    r.register("IFERROR", scalar_fns::iferror);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2977:   160	    r.register("ABS", scalar_fns::abs);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2978:   161	    r.register("SQRT", scalar_fns::sqrt);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2979:   162	    r.register("ROUND", scalar_fns::round);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2980:   163	    r.register("INT", scalar_fns::int);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2981:   164	    r.register("MOD", scalar_fns::r#mod);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2982:   165	    r.register("POWER", scalar_fns::power);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2985:   168	    // `scalar_fns::ai` doc.
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2986:   169	    r.register("AI", scalar_fns::ai);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2992:   175	    r.register("ROUNDUP", scalar_fns::roundup);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2993:   176	    r.register("ROUNDDOWN", scalar_fns::rounddown);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2994:   177	    r.register("TRUNC", scalar_fns::trunc);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2995:   178	    r.register("SIGN", scalar_fns::sign);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2996:   179	    r.register("EXP", scalar_fns::exp);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2997:   180	    r.register("LN", scalar_fns::ln);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2998:   181	    r.register("LOG", scalar_fns::log);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:2999:   182	    r.register("LOG10", scalar_fns::log10);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:3000:   183	    r.register("PI", scalar_fns::pi);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:3001:   184	    r.register("DEGREES", scalar_fns::degrees);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:3002:   185	    r.register("RADIANS", scalar_fns::radians);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:3003:   186	    r.register("LEN", scalar_fns::len);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:3004:   187	    r.register("UPPER", scalar_fns::upper);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:3005:   188	    r.register("LOWER", scalar_fns::lower);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:3006:   189	    r.register("TRIM", scalar_fns::trim);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:3007:   190	    r.register("ISNUMBER", scalar_fns::isnumber);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:3008:   191	    r.register("ISTEXT", scalar_fns::istext);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:3009:   192	    r.register("ISBLANK", scalar_fns::isblank);
docs/audits/2026-05-13-session-final-megaudit-codex.txt:3010:   193	    r.register("ISLOGICAL", scalar_fns::islogical);

exec
/bin/zsh -lc "rg \"names\\(\\)|range_aware_names\\(|context_aware_names\\(|names_all\\(|unified_names\\(\" -n crates tests" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 exited 2 in 0ms:
rg: tests: No such file or directory (os error 2)
crates/ql-storage/src/sheet.rs:378:        assert!(s.scoped_names().is_empty());
crates/ql-storage/src/sheet.rs:389:            s.scoped_names().lookup_ci("taxrate"),
crates/ql-storage/src/sheet.rs:392:        assert_eq!(s.scoped_names().len(), 1);
crates/ql-storage/src/sheet.rs:400:        assert_eq!(s.scoped_names().len(), 1);
crates/ql-storage/src/sheet.rs:402:        assert_eq!(s.scoped_names().len(), 0);
crates/ql-storage/src/sheet.rs:403:        assert!(s.scoped_names().lookup_ci("X").is_none());
crates/ql-storage/src/sheet.rs:425:        let r1 = s1.scoped_names().lookup_ci("Rate");
crates/ql-storage/src/sheet.rs:426:        let r2 = s2.scoped_names().lookup_ci("Rate");
crates/ql-exec/src/loader.rs:212:            loaded.names().lookup("TAXRATE"),
crates/ql-exec/src/calcgraph_session.rs:661:        // sheet-then-workbook scope chain fires; was `wb.names()`
crates/ql-storage/src/workbook.rs:762:        assert!(wb.names().lookup("MyName").is_none());
crates/ql-storage/src/workbook.rs:763:        assert!(wb.names().lookup_ci("MyName").is_none());
crates/ql-storage/src/workbook.rs:779:        assert!(wb.names().lookup("MIXEDCASE").is_some());
crates/ql-storage/src/workbook.rs:781:        assert!(wb.names().lookup("MixedCase").is_none());
crates/ql-storage/src/workbook.rs:783:        assert!(wb.names().lookup_ci("MixedCase").is_some());
crates/ql-storage/src/workbook.rs:784:        assert!(wb.names().lookup_ci("mixedcase").is_some());
crates/ql-storage/src/workbook.rs:789:        assert!(wb.names().lookup("ANOTHERNAME").is_some());
crates/ql-storage/src/workbook.rs:793:        assert!(wb.names().lookup_ci("MixedCase").is_none());
crates/ql-storage/src/workbook.rs:795:        assert!(wb.names().lookup_ci("anothername").is_none());
crates/ql-storage/src/workbook.rs:985:        assert!(wb.names().is_empty());
crates/ql-storage/src/workbook.rs:1005:        assert!(wb.names().lookup("TAXRATE").is_some());
crates/ql-storage/src/workbook.rs:1108:    fn validate_sheet_name_accepts_valid_names() {
crates/ql-oplog/src/replay.rs:573:            wb.names().lookup_ci("TaxRate"),
crates/ql-oplog/src/replay.rs:1078:            wb.sheet(0).unwrap().scoped_names().lookup_ci("Rate"),
crates/ql-oplog/src/replay.rs:1082:        assert!(wb.names().is_empty());
crates/ql-oplog/src/replay.rs:1103:            wb.names().lookup_ci("Rate"),
crates/ql-oplog/src/replay.rs:1107:        assert!(wb.sheet(0).unwrap().scoped_names().is_empty());
crates/ql-io/src/qbook_format.rs:1028:        for (name, target) in wb.names().iter() {
crates/ql-io/src/qbook_format.rs:1037:                for (name, target) in sheet.scoped_names().iter() {
crates/ql-io/src/qbook_format.rs:2416:        assert_eq!(loaded.names().len(), 4);
crates/ql-io/src/qbook_format.rs:2418:            loaded.names().lookup("TAXRATE"),
crates/ql-io/src/qbook_format.rs:2422:            loaded.names().lookup("ANCHORA1"),
crates/ql-io/src/qbook_format.rs:2426:            loaded.names().lookup("SALESRANGE"),
crates/ql-io/src/qbook_format.rs:2430:            loaded.names().lookup("PROFIT"),
crates/ql-io/src/qbook_format.rs:2838:        assert!(loaded.names().is_empty());
crates/ql-io/src/qbook_format.rs:3041:            loaded.names().lookup_ci("Rate"),
crates/ql-io/src/qbook_format.rs:3045:            loaded.sheet(s0).unwrap().scoped_names().lookup_ci("Rate"),
crates/ql-io/src/qbook_format.rs:3049:            loaded.sheet(s1).unwrap().scoped_names().lookup_ci("Bonus"),
crates/ql-io/src/qbook_format.rs:3056:            .scoped_names()
crates/ql-io/src/qbook_format.rs:3062:            .scoped_names()
crates/ql-io/src/qbook_format.rs:3094:            loaded.names().lookup_ci("Legacy"),
crates/ql-io/src/qbook_format.rs:3097:        assert!(loaded.sheet(0).unwrap().scoped_names().is_empty());
crates/ql-io/src/qbook_format.rs:3158:            loaded.names().lookup_ci("R"),
crates/ql-io/src/qbook_format.rs:3166:    fn load_rejects_envelope_with_duplicate_canonical_sheet_names() {
crates/ql-functions/tests/coverage.rs:3://! **Codex MEDIUM 4 fix:** the original `FunctionRegistry::names()` exposes
crates/ql-functions/tests/coverage.rs:8://! This test enumerates BOTH tables via the new `names_all()` API and
crates/ql-functions/tests/coverage.rs:267:    for name in reg.names_all() {
crates/ql-functions/tests/coverage.rs:293:        reg.names_all().map(|s| (*s).to_string()).collect();
crates/ql-oplog/tests/producer_replay_equivalence.rs:258:    let names_a = a.names();
crates/ql-oplog/tests/producer_replay_equivalence.rs:259:    let names_b = b.names();
crates/ql-oplog/tests/producer_replay_equivalence.rs:315:        let scoped_a = a.sheet(sheet_id).unwrap().scoped_names();
crates/ql-oplog/tests/producer_replay_equivalence.rs:316:        let scoped_b = b.sheet(sheet_id).unwrap().scoped_names();
crates/ql-oplog/tests/producer_replay_equivalence.rs:371:        replay_wb.names().lookup_ci("TaxRate"),
crates/ql-exec/src/plan.rs:275:/// `CalcgraphSession`) pass `self.workbook.names()` for `names` and
crates/ql-exec/src/plan.rs:470:/// Default empty-resolver. Used by the legacy `bind()` / `bind_with_names()`
crates/ql-exec/src/plan.rs:753:    fn bind_empty_sheet_resolver_rejects_all_sheet_names() {
crates/ql-exec/src/plan.rs:754:        // The legacy `bind()` / `bind_with_names()` entry points use an
crates/ql-functions/src/registry.rs:310:    pub fn range_aware_names(&self) -> impl Iterator<Item = &&'static str> + '_ {
crates/ql-functions/src/registry.rs:319:    pub fn context_aware_names(&self) -> impl Iterator<Item = &&'static str> + '_ {
crates/ql-functions/src/registry.rs:329:    pub fn unified_names(&self) -> impl Iterator<Item = &&'static str> + '_ {
crates/ql-functions/src/registry.rs:339:    pub fn names_all(&self) -> impl Iterator<Item = &&'static str> {
crates/ql-functions/src/registry.rs:639:        for name in r.names() {
crates/ql-functions/src/registry.rs:708:        for name in r.names() {
crates/ql-functions/src/registry.rs:719:        for name in r.range_aware_names() {
crates/ql-functions/src/registry.rs:773:        let all: Vec<&str> = r.names_all().copied().collect();
crates/ql-functions/src/registry.rs:805:        let names: Vec<&str> = r.context_aware_names().copied().collect();
crates/ql-functions/src/registry.rs:1010:        let names: Vec<&str> = r.unified_names().copied().collect();
crates/ql-functions/src/registry.rs:1021:        let all: std::collections::HashSet<&str> = r.names_all().copied().collect();
crates/ql-exec/src/env.rs:283:/// passed `self.workbook.names()` get workbook-only behavior (no chain
crates/ql-exec/src/env.rs:293:            if let Some(target) = sheet.scoped_names().lookup_ci(name) {
crates/ql-exec/src/env.rs:298:        named_target_to_resolved(self.names().lookup_ci(name)?)
crates/ql-exec/src/transaction.rs:216:        // sheet-then-workbook scope chain fires; was `self.workbook.names()`
crates/ql-exec/src/workbook_runtime.rs:495:        let name_gen = self.workbook.names().generation();
crates/ql-exec/src/workbook_runtime.rs:669:        self.workbook.names().would_accept(name)?;
crates/ql-exec/src/workbook_runtime.rs:722:        self.workbook.names().would_accept(name)?;
crates/ql-exec/src/workbook_runtime.rs:1526:        let name_gen = self.workbook.names().generation();
crates/ql-exec/src/workbook_runtime.rs:1533:        // (for `names()` inside the closure) while holding a mutable
crates/ql-exec/src/workbook_runtime.rs:2575:        let old_gen = wb.names().generation();
crates/ql-exec/src/workbook_runtime.rs:2579:            wb.names().generation() > old_gen,
crates/ql-exec/src/workbook_runtime.rs:3090:            wb.names().lookup_ci("TaxRate"),
crates/ql-exec/src/workbook_runtime.rs:3309:            replay_wb.names().lookup_ci("TaxRate"),
crates/ql-exec/src/workbook_runtime.rs:5355:            wb.sheet(s0).unwrap().scoped_names().lookup_ci("R"),
crates/ql-exec/src/workbook_runtime.rs:5359:        assert!(wb.names().is_empty());
crates/ql-exec/src/workbook_runtime.rs:5388:        assert!(wb.sheet(s0).unwrap().scoped_names().is_empty());
crates/ql-exec/tests/oplog_e2e.rs:63:    let names_a = a.names();
crates/ql-exec/tests/oplog_e2e.rs:64:    let names_b = b.names();
crates/ql-exec/tests/ide_simulation.rs:268:        reloaded_wb.names().lookup_ci("TaxRate"),

codex
A key implementation detail is already visible: `FunctionArg::Range` exactly mirrors legacy `FnArg::Range { values, rows, cols }`, so the future evaluator conversion is mechanically straightforward. I’m checking the public API and search results now for any callers/tests that assumed the old internal three-map layout.
exec
/bin/zsh -lc "nl -ba crates/ql-functions/src/registry.rs | sed -n '760,1060p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   760	    #[should_panic(expected = "duplicate registration")]
   761	    fn duplicate_context_aware_registration_panics() {
   762	        let mut r = FunctionRegistry::new();
   763	        r.register_context_aware("ECHO.DS", echo_date_system_year);
   764	        r.register_context_aware("ECHO.DS", echo_date_system_year);
   765	    }
   766	
   767	    #[test]
   768	    fn names_all_chains_all_three_tables() {
   769	        let mut r = FunctionRegistry::new();
   770	        r.register("SCALAR_FN", scalar_fns::sum);
   771	        r.register_range_aware("RANGE_FN", range_fns::sumif);
   772	        r.register_context_aware("CONTEXT_FN", echo_date_system_year);
   773	        let all: Vec<&str> = r.names_all().copied().collect();
   774	        assert!(all.contains(&"SCALAR_FN"));
   775	        assert!(all.contains(&"RANGE_FN"));
   776	        assert!(all.contains(&"CONTEXT_FN"));
   777	        assert_eq!(all.len(), 3);
   778	    }
   779	
   780	    #[test]
   781	    fn len_includes_context_aware_table() {
   782	        let mut r = FunctionRegistry::new();
   783	        r.register("S", scalar_fns::sum);
   784	        assert_eq!(r.len(), 1);
   785	        r.register_range_aware("RA", range_fns::sumif);
   786	        assert_eq!(r.len(), 2);
   787	        r.register_context_aware("CA", echo_date_system_year);
   788	        assert_eq!(r.len(), 3);
   789	    }
   790	
   791	    #[test]
   792	    fn is_empty_checks_all_three_tables() {
   793	        let mut r = FunctionRegistry::new();
   794	        assert!(r.is_empty());
   795	        r.register_context_aware("CA", echo_date_system_year);
   796	        assert!(!r.is_empty());
   797	    }
   798	
   799	    #[test]
   800	    fn context_aware_names_returns_only_context_aware() {
   801	        let mut r = FunctionRegistry::new();
   802	        r.register("S", scalar_fns::sum);
   803	        r.register_range_aware("RA", range_fns::sumif);
   804	        r.register_context_aware("CA", echo_date_system_year);
   805	        let names: Vec<&str> = r.context_aware_names().copied().collect();
   806	        assert_eq!(names, vec!["CA"]);
   807	    }
   808	
   809	    #[test]
   810	    fn ai_dispatches_to_not_available_sentinel() {
   811	        use ql_types::ErrorValue;
   812	        let r = default_registry();
   813	        let ai = r.lookup("AI").expect("AI is registered");
   814	        // AI ignores its args and returns the AINotAvailable error.
   815	        assert_eq!(ai(&[]), Value::Error(ErrorValue::AINotAvailable));
   816	        assert_eq!(
   817	            ai(&[Value::Number(1.0), Value::text("prompt")]),
   818	            Value::Error(ErrorValue::AINotAvailable)
   819	        );
   820	        // Case-insensitive lookup works.
   821	        let ai_lower = r.lookup("ai").expect("ai (lowercase) resolves");
   822	        assert_eq!(ai_lower(&[]), Value::Error(ErrorValue::AINotAvailable));
   823	    }
   824	
   825	    #[test]
   826	    fn lookup_is_case_insensitive() {
   827	        let r = default_registry();
   828	        let sum_upper = r.lookup("SUM").unwrap();
   829	        let sum_lower = r.lookup("sum").unwrap();
   830	        let sum_mixed = r.lookup("Sum").unwrap();
   831	        let result_upper = sum_upper(&[Value::Number(1.0), Value::Number(2.0)]);
   832	        let result_lower = sum_lower(&[Value::Number(1.0), Value::Number(2.0)]);
   833	        let result_mixed = sum_mixed(&[Value::Number(1.0), Value::Number(2.0)]);
   834	        assert_eq!(result_upper, Value::Number(3.0));
   835	        assert_eq!(result_lower, result_upper);
   836	        assert_eq!(result_mixed, result_upper);
   837	    }
   838	
   839	    #[test]
   840	    fn lookup_missing_returns_none() {
   841	        let r = default_registry();
   842	        assert!(r.lookup("FAKE_FN_NAME_XYZ").is_none());
   843	    }
   844	
   845	    #[test]
   846	    fn dotted_function_names_supported() {
   847	        // VAR.S — Excel uses dot in modern compatibility-friendly names.
   848	        let r = default_registry();
   849	        let var_s = r.lookup("VAR.S").unwrap();
   850	        let result = var_s(&[Value::Number(1.0), Value::Number(3.0)]);
   851	        assert_eq!(result, Value::Number(2.0));
   852	    }
   853	
   854	    // ===== Phase 2A.7 audit M5: register contract =====
   855	
   856	    #[test]
   857	    #[should_panic(expected = "canonical upper-case")]
   858	    fn register_lowercase_name_panics() {
   859	        let mut r = FunctionRegistry::new();
   860	        r.register("sum", scalar_fns::sum);
   861	    }
   862	
   863	    #[test]
   864	    #[should_panic(expected = "canonical upper-case")]
   865	    fn register_mixed_case_name_panics() {
   866	        let mut r = FunctionRegistry::new();
   867	        r.register("Sum", scalar_fns::sum);
   868	    }
   869	
   870	    #[test]
   871	    #[should_panic(expected = "duplicate registration")]
   872	    fn register_duplicate_name_panics() {
   873	        let mut r = FunctionRegistry::new();
   874	        r.register("SUM", scalar_fns::sum);
   875	        r.register("SUM", scalar_fns::sum);
   876	    }
   877	
   878	    #[test]
   879	    #[should_panic(expected = "name must not be empty")]
   880	    fn register_empty_name_panics() {
   881	        let mut r = FunctionRegistry::new();
   882	        r.register("", scalar_fns::sum);
   883	    }
   884	
   885	    #[test]
   886	    fn register_dotted_uppercase_name_ok() {
   887	        // Dotted Excel-canonical names (VAR.S, STDEV.P) are upper-case.
   888	        let mut r = FunctionRegistry::new();
   889	        r.register("CUSTOM.FN", scalar_fns::sum);
   890	        assert!(r.lookup("CUSTOM.FN").is_some());
   891	        // Case-insensitive lookup still works.
   892	        assert!(r.lookup("custom.fn").is_some());
   893	    }
   894	
   895	    #[test]
   896	    fn aliases_dispatch_to_same_fn() {
   897	        let r = default_registry();
   898	        // VAR and VAR.S — both map to sample variance.
   899	        let var = r.lookup("VAR").unwrap();
   900	        let var_s = r.lookup("VAR.S").unwrap();
   901	        let data = &[Value::Number(1.0), Value::Number(3.0)];
   902	        assert_eq!(var(data), var_s(data));
   903	    }
   904	
   905	    #[test]
   906	    fn end_to_end_sum_via_registry() {
   907	        let r = default_registry();
   908	        let sum = r.lookup("SUM").unwrap();
   909	        let result = sum(&[
   910	            Value::Number(10.0),
   911	            Value::Number(20.0),
   912	            Value::Number(30.0),
   913	        ]);
   914	        assert_eq!(result, Value::Number(60.0));
   915	    }
   916	
   917	    // ===== W5-96 (Phase 4.7.B) unified ABI =====
   918	
   919	    /// Fixture: a unified-tier function that returns a fixed 1×3
   920	    /// `ArrayValue` so we can verify the array-returning path
   921	    /// end-to-end through `register_unified` + `lookup_unified`.
   922	    fn fixed_sequence_3(_args: &[FunctionArg], _ctx: &FunctionContext) -> FunctionReturn {
   923	        FunctionReturn::Array(ArrayValue::row(vec![
   924	            Value::Number(1.0),
   925	            Value::Number(2.0),
   926	            Value::Number(3.0),
   927	        ]))
   928	    }
   929	
   930	    /// Fixture: a unified-tier function that echoes its first arg.
   931	    fn echo_first(args: &[FunctionArg], _ctx: &FunctionContext) -> FunctionReturn {
   932	        match args.first() {
   933	            Some(FunctionArg::Scalar(v)) => FunctionReturn::Scalar(v.clone()),
   934	            Some(FunctionArg::Array(a)) => FunctionReturn::Array(a.clone()),
   935	            _ => FunctionReturn::Scalar(Value::Error(ql_types::ErrorValue::Value)),
   936	        }
   937	    }
   938	
   939	    #[test]
   940	    fn register_unified_and_lookup_unified_round_trip() {
   941	        let mut r = FunctionRegistry::new();
   942	        r.register_unified("FIXED_SEQ", fixed_sequence_3);
   943	        let f = r.lookup_unified("FIXED_SEQ").expect("registered");
   944	        let ctx = ql_types::EvalContext::default();
   945	        let fctx = FunctionContext::new(&ctx);
   946	        let ret = f(&[], &fctx);
   947	        match ret {
   948	            FunctionReturn::Array(a) => {
   949	                assert_eq!(a.rows(), 1);
   950	                assert_eq!(a.cols(), 3);
   951	            }
   952	            FunctionReturn::Scalar(_) => panic!("expected array return"),
   953	        }
   954	    }
   955	
   956	    #[test]
   957	    fn lookup_unified_is_case_insensitive() {
   958	        let mut r = FunctionRegistry::new();
   959	        r.register_unified("FIXED_SEQ", fixed_sequence_3);
   960	        assert!(r.lookup_unified("fixed_seq").is_some());
   961	        assert!(r.lookup_unified("Fixed_Seq").is_some());
   962	    }
   963	
   964	    #[test]
   965	    fn unified_tier_disjoint_from_other_tiers() {
   966	        let mut r = FunctionRegistry::new();
   967	        r.register_unified("UFN", fixed_sequence_3);
   968	        // The legacy filter views must NOT return the unified fn.
   969	        assert!(r.lookup("UFN").is_none());
   970	        assert!(r.lookup_range_aware("UFN").is_none());
   971	        assert!(r.lookup_context_aware("UFN").is_none());
   972	        // But lookup_any does see it.
   973	        assert!(matches!(
   974	            r.lookup_any("UFN"),
   975	            Some(RegisteredFn::Unified(_))
   976	        ));
   977	    }
   978	
   979	    #[test]
   980	    #[should_panic(expected = "duplicate registration")]
   981	    fn register_unified_with_existing_scalar_name_panics() {
   982	        let mut r = FunctionRegistry::new();
   983	        r.register("FOO", scalar_fns::sum);
   984	        r.register_unified("FOO", fixed_sequence_3);
   985	    }
   986	
   987	    #[test]
   988	    #[should_panic(expected = "duplicate registration")]
   989	    fn register_scalar_with_existing_unified_name_panics() {
   990	        let mut r = FunctionRegistry::new();
   991	        r.register_unified("FOO", fixed_sequence_3);
   992	        r.register("FOO", scalar_fns::sum);
   993	    }
   994	
   995	    #[test]
   996	    #[should_panic(expected = "duplicate registration")]
   997	    fn duplicate_unified_registration_panics() {
   998	        let mut r = FunctionRegistry::new();
   999	        r.register_unified("UFN", fixed_sequence_3);
  1000	        r.register_unified("UFN", fixed_sequence_3);
  1001	    }
  1002	
  1003	    #[test]
  1004	    fn unified_names_returns_only_unified() {
  1005	        let mut r = FunctionRegistry::new();
  1006	        r.register("S", scalar_fns::sum);
  1007	        r.register_range_aware("RA", range_fns::sumif);
  1008	        r.register_context_aware("CA", echo_date_system_year);
  1009	        r.register_unified("UFN", fixed_sequence_3);
  1010	        let names: Vec<&str> = r.unified_names().copied().collect();
  1011	        assert_eq!(names, vec!["UFN"]);
  1012	    }
  1013	
  1014	    #[test]
  1015	    fn names_all_includes_unified() {
  1016	        let mut r = FunctionRegistry::new();
  1017	        r.register("S", scalar_fns::sum);
  1018	        r.register_range_aware("RA", range_fns::sumif);
  1019	        r.register_context_aware("CA", echo_date_system_year);
  1020	        r.register_unified("UFN", fixed_sequence_3);
  1021	        let all: std::collections::HashSet<&str> = r.names_all().copied().collect();
  1022	        assert!(all.contains("S"));
  1023	        assert!(all.contains("RA"));
  1024	        assert!(all.contains("CA"));
  1025	        assert!(all.contains("UFN"));
  1026	        assert_eq!(all.len(), 4);
  1027	        assert_eq!(r.len(), 4);
  1028	    }
  1029	
  1030	    #[test]
  1031	    fn function_arg_array_round_trips_through_unified() {
  1032	        // Verify that a unified fn can receive an ArrayValue arg and
  1033	        // return it unchanged. Closes the FunctionArg::Array surface.
  1034	        let mut r = FunctionRegistry::new();
  1035	        r.register_unified("ECHO", echo_first);
  1036	        let f = r.lookup_unified("ECHO").unwrap();
  1037	        let arr = ArrayValue::row(vec![Value::Number(7.0), Value::Number(8.0)]);
  1038	        let ctx = ql_types::EvalContext::default();
  1039	        let fctx = FunctionContext::new(&ctx);
  1040	        let ret = f(&[FunctionArg::Array(arr.clone())], &fctx);
  1041	        match ret {
  1042	            FunctionReturn::Array(out) => {
  1043	                assert_eq!(out, arr);
  1044	            }
  1045	            FunctionReturn::Scalar(_) => panic!("expected array round-trip"),
  1046	        }
  1047	    }
  1048	
  1049	    #[test]
  1050	    fn function_return_is_array_helper() {
  1051	        let arr = FunctionReturn::Array(ArrayValue::singleton(Value::Number(1.0)));
  1052	        let scalar = FunctionReturn::Scalar(Value::Number(1.0));
  1053	        assert!(arr.is_array());
  1054	        assert!(!scalar.is_array());
  1055	    }
  1056	
  1057	    #[test]
  1058	    fn lookup_any_returns_tagged_enum() {
  1059	        let mut r = FunctionRegistry::new();
  1060	        r.register("S", scalar_fns::sum);

exec
/bin/zsh -lc "nl -ba crates/ql-functions/tests/coverage.rs | sed -n '1,330p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
     1	//! Phase 4.4.B — Registry coverage check (W5-65).
     2	//!
     3	//! **Codex MEDIUM 4 fix:** the original `FunctionRegistry::names()` exposes
     4	//! only the scalar `fns` table. Without an explicit guardrail, range-aware
     5	//! functions (MATCH, VLOOKUP, COUNTIF, RANK, CONCAT, ...) can land in the
     6	//! registry without anyone noticing they lack matrix-test coverage.
     7	//!
     8	//! This test enumerates BOTH tables via the new `names_all()` API and
     9	//! asserts every registered name is either:
    10	//! 1. Listed in the expected-coverage set below, OR
    11	//! 2. Explicitly marked as "no matrix coverage required" with a reason.
    12	//!
    13	//! When a new function lands, this test FAILS until the author either:
    14	//! - Adds a matrix-test entry in `per_function_overrides.rs` (or another
    15	//!   matrix file) AND adds the name to `EXPECTED_COVERED` below, OR
    16	//! - Adds the name to `EXPLICITLY_DEFERRED` with a phase-target reason.
    17	
    18	use ql_functions::default_registry;
    19	
    20	/// Functions that have at least one matrix-test entry pinning their
    21	/// coercion / error / override behavior.
    22	///
    23	/// Kept alphabetically for diff-friendly maintenance. When you add a new
    24	/// matrix test, add the function name here. The list is the source of
    25	/// truth for "what's matrix-covered" — `per_function_overrides.rs` and
    26	/// `coercion_matrix.rs` are the actual tests.
    27	const EXPECTED_COVERED: &[&str] = &[
    28	    // Aggregates / stats (via type-pair matrix in ql-types + per-fn overrides)
    29	    "AVERAGE",
    30	    "COUNT",
    31	    "COUNTA",
    32	    "LARGE",
    33	    "MEDIAN",
    34	    "MODE",
    35	    "RANK",
    36	    "RANK.AVG",
    37	    "SMALL",
    38	    "SUM",
    39	    // Information predicates
    40	    "ISBLANK",
    41	    "ISERR",
    42	    "ISERROR",
    43	    "ISLOGICAL",
    44	    "ISNA",
    45	    "ISNUMBER",
    46	    "ISTEXT",
    47	    // Lookup family — W5-67 added HLOOKUP + CHOOSE (Sonnet mega-audit S1,
    48	    // Codex HIGH 2: previously claimed transitively-covered but no real
    49	    // test existed; now each path is explicitly pinned).
    50	    "CHOOSE",
    51	    "HLOOKUP",
    52	    "MATCH",
    53	    "VLOOKUP",
    54	    // Math divergence — W5-67 added ROUNDUP binary-float divergence test
    55	    // (Sonnet mega-audit MEDIUM-S5: divergence was doc-only, could regress
    56	    // silently).
    57	    "ROUNDUP",
    58	    "ROUNDDOWN",
    59	];
    60	
    61	/// Functions that intentionally lack matrix coverage today, with a stated
    62	/// reason. Each entry must reference either a known-gaps entry or a future
    63	/// phase. The matrix-coverage check is allowed to skip these, but the
    64	/// reason must remain accurate.
    65	const EXPLICITLY_DEFERRED: &[(&str, &str)] = &[
    66	    // -- Phase 4.4.B follow-on: these are covered by ql-types type-pair
    67	    // matrix (which exercises all 5 contexts × 9 input cases against the
    68	    // central coercion module), so per-fn overrides aren't strictly needed
    69	    // for SUMs/MINs/MAXs that have no overrides beyond defaults. They land
    70	    // in EXPECTED_COVERED in Phase 4.4.C if/when behavior diverges from
    71	    // default.
    72	    ("AVG", "alias of AVERAGE; covered transitively"),
    73	    (
    74	        "MIN",
    75	        "no override beyond default empty-range → 0; deferred to 4.4.C",
    76	    ),
    77	    (
    78	        "MAX",
    79	        "no override beyond default empty-range → 0; deferred to 4.4.C",
    80	    ),
    81	    (
    82	        "PRODUCT",
    83	        "no override beyond default error/blank handling; deferred",
    84	    ),
    85	    ("VAR", "Welford-backed; covered by NIST numacc tests"),
    86	    ("VAR.S", "Welford-backed; covered by NIST numacc tests"),
    87	    ("VAR.P", "Welford-backed; covered by NIST numacc tests"),
    88	    ("STDEV", "Welford-backed; covered by NIST numacc tests"),
    89	    ("STDEV.S", "Welford-backed; covered by NIST numacc tests"),
    90	    ("STDEV.P", "Welford-backed; covered by NIST numacc tests"),
    91	    // Logical
    92	    (
    93	        "IF",
    94	        "lazy eval gap GAP-F-02 / FN4-03; covered by error_precedence",
    95	    ),
    96	    ("IFERROR", "covered by error_precedence E2E"),
    97	    (
    98	        "AND",
    99	        "deferred — Phase 4.4.C will add logical override tests",
   100	    ),
   101	    (
   102	        "OR",
   103	        "deferred — Phase 4.4.C will add logical override tests",
   104	    ),
   105	    (
   106	        "NOT",
   107	        "deferred — Phase 4.4.C will add logical override tests",
   108	    ),
   109	    // Math (default coercion paths cover most; per-fn overrides deferred)
   110	    ("ABS", "default; deferred"),
   111	    ("SQRT", "domain error #NUM!; deferred"),
   112	    ("ROUND", "default; deferred"),
   113	    // (ROUNDUP / ROUNDDOWN moved to EXPECTED_COVERED in W5-67 — now have
   114	    // explicit binary-float divergence tests.)
   115	    ("INT", "default; deferred"),
   116	    ("TRUNC", "default; deferred"),
   117	    ("MOD", "div-zero handling; deferred"),
   118	    ("POWER", "domain errors; deferred"),
   119	    ("SIGN", "default; deferred"),
   120	    ("EXP", "domain errors; deferred"),
   121	    ("LN", "domain errors; deferred"),
   122	    ("LOG", "domain errors; deferred"),
   123	    ("LOG10", "domain errors; deferred"),
   124	    ("PI", "no args; deferred"),
   125	    ("DEGREES", "default; deferred"),
   126	    ("RADIANS", "default; deferred"),
   127	    // Trig
   128	    ("SIN", "default; deferred"),
   129	    ("COS", "default; deferred"),
   130	    ("TAN", "huge-finite at π/2 documented in matrix"),
   131	    ("ASIN", "domain error documented in matrix"),
   132	    ("ACOS", "domain error documented in matrix"),
   133	    ("ATAN", "default; deferred"),
   134	    ("ATAN2", "0,0 → #DIV/0! documented in matrix"),
   135	    ("SINH", "default; deferred"),
   136	    ("COSH", "default; deferred"),
   137	    ("TANH", "default; deferred"),
   138	    ("ASINH", "default; deferred"),
   139	    ("ACOSH", "domain error documented in matrix"),
   140	    ("ATANH", "domain error documented in matrix"),
   141	    // Math completion
   142	    ("CEILING", "sign-rule documented in matrix"),
   143	    ("FLOOR", "sign-rule documented in matrix"),
   144	    ("CEILING.MATH", "W5-61 polish; documented in matrix"),
   145	    ("FLOOR.MATH", "W5-61 polish; documented in matrix"),
   146	    ("MROUND", "W5-60 nonzero-multiple-zero fix in matrix"),
   147	    ("ODD", "default; deferred"),
   148	    ("EVEN", "default; deferred"),
   149	    ("QUOTIENT", "div-zero documented in matrix"),
   150	    ("GCD", "integer-domain documented in matrix"),
   151	    ("LCM", "integer-domain documented in matrix"),
   152	    // Text wave 2
   153	    ("LEN", "UTF-8 divergence documented in matrix"),
   154	    ("UPPER", "Unicode divergence documented in matrix"),
   155	    ("LOWER", "Unicode divergence documented in matrix"),
   156	    ("TRIM", "default; deferred"),
   157	    ("LEFT", "documented in matrix"),
   158	    ("RIGHT", "documented in matrix"),
   159	    ("MID", "documented in matrix"),
   160	    ("FIND", "case-SENSITIVE; documented in matrix"),
   161	    ("SEARCH", "W5-61 wildcards; documented in matrix"),
   162	    ("SUBSTITUTE", "default; deferred"),
   163	    ("REPLACE", "default; deferred"),
   164	    (
   165	        "CONCATENATE",
   166	        "documented in matrix; CONCAT is range-aware variant",
   167	    ),
   168	    ("REPT", "32K cap documented in matrix"),
   169	    ("EXACT", "default; deferred"),
   170	    ("PROPER", "W5-61 polish; documented in matrix"),
   171	    ("CLEAN", "W5-61 polish; documented in matrix"),
   172	    // Volatile
   173	    (
   174	        "NOW",
   175	        "non-deterministic; covered by volatile dedicated tests",
   176	    ),
   177	    (
   178	        "TODAY",
   179	        "non-deterministic; covered by volatile dedicated tests",
   180	    ),
   181	    (
   182	        "RAND",
   183	        "non-deterministic; deterministic-RNG-seed test exists",
   184	    ),
   185	    (
   186	        "RANDBETWEEN",
   187	        "non-deterministic; covered by volatile dedicated tests",
   188	    ),
   189	    // Phase 4.5.B wave 1 date/time fns (W5-72) — covered by
   190	    // `date_fns::tests` module (31 unit tests pinning Excel-canon
   191	    // edge cases including 1900-phantom day, month/day cascade,
   192	    // negative-arg #NUM!, etc). Deferred from per_function_overrides
   193	    // since the module-internal tests already do the coverage.
   194	    ("DATE", "W5-72; covered by date_fns::tests"),
   195	    ("YEAR", "W5-72; covered by date_fns::tests"),
   196	    ("MONTH", "W5-72; covered by date_fns::tests"),
   197	    ("DAY", "W5-72; covered by date_fns::tests"),
   198	    ("HOUR", "W5-72; covered by date_fns::tests"),
   199	    ("MINUTE", "W5-72; covered by date_fns::tests"),
   200	    ("SECOND", "W5-72; covered by date_fns::tests"),
   201	    ("TIME", "W5-72; covered by date_fns::tests"),
   202	    ("DATEVALUE", "W5-73; covered by date_fns::tests_wave2"),
   203	    ("TIMEVALUE", "W5-73; covered by date_fns::tests_wave2"),
   204	    ("WEEKDAY", "W5-73; covered by date_fns::tests_wave2"),
   205	    ("EOMONTH", "W5-73; covered by date_fns::tests_wave2"),
   206	    ("EDATE", "W5-73; covered by date_fns::tests_wave2"),
   207	    ("DAYS", "W5-74; covered by date_fns::tests_wave3"),
   208	    (
   209	        "NETWORKDAYS",
   210	        "W5-74; V1 no-holidays — GAP-F-09 pinned by tests_wave3",
   211	    ),
   212	    (
   213	        "WORKDAY",
   214	        "W5-74; V1 no-holidays — GAP-F-10 pinned by tests_wave3",
   215	    ),
   216	    (
   217	        "YEARFRAC",
   218	        "W5-74; V1 simplified 30/360 — GAP-F-11 pinned by tests_wave3",
   219	    ),
   220	    ("DATEDIF", "W5-75 V2; covered by date_fns::tests_wave_c"),
   221	    ("DAYS360", "W5-75 V2; covered by date_fns::tests_wave_c"),
   222	    ("WEEKNUM", "W5-75 V2; covered by date_fns::tests_wave_c"),
   223	    ("ISOWEEKNUM", "W5-75 V2; covered by date_fns::tests_wave_c"),
   224	    // Phase 4.5.E — TEXT() formatter
   225	    (
   226	        "TEXT",
   227	        "W5-83 Phase 4.5.E; covered by format::text_fn::tests + workbook_runtime end-to-end tests",
   228	    ),
   229	    // Range-aware
   230	    ("SUMIF", "wildcards + V1 anchoring divergence in matrix"),
   231	    ("COUNTIF", "wildcards covered in matrix"),
   232	    ("SUMIFS", "2D shape validation pinned in W5-60"),
   233	    ("COUNTIFS", "2D shape validation pinned in W5-60"),
   234	    ("AVERAGEIF", "V1 anchoring divergence in matrix"),
   235	    ("AVERAGEIFS", "2D shape validation pinned in W5-60"),
   236	    ("SUMPRODUCT", "2D shape validation pinned in W5-60"),
   237	    // (HLOOKIP moved to EXPECTED_COVERED in W5-67 — Sonnet mega-audit S1
   238	    // flagged that the prior "transitively covered by VLOOKUP" reason was
   239	    // false; HLOOKUP is a separate function body, not an alias.)
   240	    ("INDEX", "documented in matrix; array spill deferred to 4.7"),
   241	    // (CHOOSE moved to EXPECTED_COVERED in W5-67 — Codex mega-audit HIGH 2
   242	    // flagged that the "range-arg gotcha pinned" claim was about an older
   243	    // module unit test, not the W5-65 matrix substrate. Now has explicit
   244	    // matrix tests for each path.)
   245	    (
   246	        "RANK.EQ",
   247	        "alias of RANK; covered by per_function_overrides via RANK",
   248	    ),
   249	    (
   250	        "MODE.SNGL",
   251	        "alias of MODE; covered by per_function_overrides via MODE",
   252	    ),
   253	    ("CONCAT", "W5-61 polish; 32K cap pinned"),
   254	    // AI / future
   255	    ("AI", "sentinel returns #AI_NOT_AVAILABLE_V1; phase 6.6"),
   256	];
   257	
   258	#[test]
   259	fn every_registered_function_has_a_coverage_decision() {
   260	    let reg = default_registry();
   261	    let mut missing: Vec<String> = Vec::new();
   262	
   263	    let deferred_names: std::collections::HashSet<&str> =
   264	        EXPLICITLY_DEFERRED.iter().map(|(name, _)| *name).collect();
   265	    let covered_names: std::collections::HashSet<&str> = EXPECTED_COVERED.iter().copied().collect();
   266	
   267	    for name in reg.names_all() {
   268	        if covered_names.contains(*name) {
   269	            continue;
   270	        }
   271	        if deferred_names.contains(*name) {
   272	            continue;
   273	        }
   274	        missing.push((*name).to_string());
   275	    }
   276	
   277	    missing.sort();
   278	    assert!(
   279	        missing.is_empty(),
   280	        "Phase 4.4.B coverage gap — these registered functions have no \
   281	         matrix coverage decision (add to EXPECTED_COVERED or \
   282	         EXPLICITLY_DEFERRED in `crates/ql-functions/tests/coverage.rs`): \
   283	         {missing:?}"
   284	    );
   285	}
   286	
   287	#[test]
   288	fn no_stale_coverage_entries_for_unregistered_functions() {
   289	    // The flipside: if EXPECTED_COVERED or EXPLICITLY_DEFERRED names a
   290	    // function that's no longer registered (deleted, renamed), surface it.
   291	    let reg = default_registry();
   292	    let all_names: std::collections::HashSet<String> =
   293	        reg.names_all().map(|s| (*s).to_string()).collect();
   294	
   295	    let mut stale: Vec<&str> = Vec::new();
   296	    for name in EXPECTED_COVERED {
   297	        if !all_names.contains(*name) {
   298	            stale.push(name);
   299	        }
   300	    }
   301	    for (name, _reason) in EXPLICITLY_DEFERRED {
   302	        if !all_names.contains(*name) {
   303	            stale.push(name);
   304	        }
   305	    }
   306	    stale.sort();
   307	    assert!(
   308	        stale.is_empty(),
   309	        "Coverage list references unregistered function names: {stale:?}"
   310	    );
   311	}
   312	
   313	#[test]
   314	fn coverage_lists_are_disjoint() {
   315	    // A function should not appear in BOTH EXPECTED_COVERED and
   316	    // EXPLICITLY_DEFERRED — that would mask intent.
   317	    let covered: std::collections::HashSet<&str> = EXPECTED_COVERED.iter().copied().collect();
   318	    let mut overlap: Vec<&str> = Vec::new();
   319	    for (name, _) in EXPLICITLY_DEFERRED {
   320	        if covered.contains(*name) {
   321	            overlap.push(name);
   322	        }
   323	    }
   324	    overlap.sort();
   325	    assert!(
   326	        overlap.is_empty(),
   327	        "Functions appear in BOTH coverage lists: {overlap:?}"
   328	    );
   329	}

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/scalar.rs | sed -n '1,260p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
     1	//! Scalar evaluator — `eval_scalar(plan, env) -> Value`.
     2	//!
     3	//! Per-cell evaluation path. Used for:
     4	//! - Single-cell formulas (`=A1+B1` in cell C1).
     5	//! - Per-cell fallback inside a FormulaRegion when SIMD isn't applicable (text, error,
     6	//!   mixed types, non-arithmetic ops, division — see lower.rs H5 routing).
     7	//! - Tests, which is most of the W4-1 coverage.
     8	//!
     9	//! The SIMD hot path for OG-02 lives in `simd.rs` and bypasses this entirely —
    10	//! it consumes Arrow Float64Array slices directly via `multiversion`-dispatched
    11	//! kernels.
    12	//!
    13	//! ## Excel binary arithmetic semantics (Phase 2A.9 audit M1 — lenient coercion)
    14	//!
    15	//! For binary operators `+ - * / ^`:
    16	//! - If either operand is an `Error`, propagate that error (left operand wins).
    17	//! - Coerce both operands to f64 via `to_number_lenient` (Number → self;
    18	//!   Bool → 0/1; Blank → 0; Text that parses as a number → that number; Text
    19	//!   that doesn't parse → `#VALUE!`; Error → propagate). Phase 2A.9 audit M1
    20	//!   replaced the prior strict path that rejected ALL text.
    21	//! - Apply the f64 op. Sanitize result via `sanitize_f64` (NaN/Inf → `#NUM!`).
    22	//! - Special cases (Phase 2A.9 audit M3 / 2A.13 audit M3 companion):
    23	//!   - Division by zero → `#DIV/0!`.
    24	//!   - Negative base ^ non-integer exponent → `#NUM!`.
    25	//!   - `0^0` → `#NUM!`.
    26	//!   - `0^-n` (n > 0) → `#DIV/0!` (mathematically `1/0^n`).
    27	//!
    28	//! For `&` (concat): coerce both to text via `to_text_for_formula`; concat. Errors
    29	//! propagate.
    30	//!
    31	//! Comparison operators (`=`, `<>`, `<`, `>`, `<=`, `>=`) follow Excel-canon
    32	//! cross-type ordering (Phase 2A.9 audit M2): same-type pairs compare per
    33	//! their natural ordering; mixed-type pairs use type rank
    34	//! (`Number < Text < Boolean`). `Blank` coerces to `0` vs Number and `""`
    35	//! vs Text; for `Blank vs Boolean` the type-rank fallthrough applies (Blank
    36	//! ranks as Number, so `Blank < TRUE`). See `compare_values_excel`.
    37	//!
    38	//! Unary `-x` negates; `+x` no-ops; `x%` divides by 100.
    39	
    40	use ql_formula_syntax::Operator;
    41	use ql_functions::FunctionRegistry;
    42	use ql_types::{coercion, ErrorValue, Value};
    43	
    44	use crate::aggregate_cache::{AggregateCache, NoAggregateCache};
    45	use crate::env::CellEnv;
    46	use crate::plan::ExprPlan;
    47	
    48	/// Evaluate an `ExprPlan` against `env` to produce a `Value`. Total function — never
    49	/// panics on Excel semantics (NaN, div-by-zero, etc.). Panics only on internal
    50	/// programmer errors (e.g. malformed ExprPlan that bind() couldn't produce).
    51	///
    52	/// `Function` variants return `#NAME?` (`ErrorValue::Name`). To dispatch through a
    53	/// registry, use `eval_scalar_with_registry` instead.
    54	pub fn eval_scalar<E: CellEnv>(plan: &ExprPlan, env: &E) -> Value {
    55	    match plan {
    56	        ExprPlan::Number(n) => Value::number(*n),
    57	        ExprPlan::Bool(b) => Value::Boolean(*b),
    58	        ExprPlan::String(s) => Value::Text(s.clone()),
    59	        ExprPlan::CellRef {
    60	            sheet, row, col, ..
    61	        } => env.read_cell(*sheet, *row, *col),
    62	        ExprPlan::Binary { op, lhs, rhs } => {
    63	            let l = eval_scalar(lhs, env);
    64	            let r = eval_scalar(rhs, env);
    65	            eval_binary(*op, l, r)
    66	        }
    67	        ExprPlan::Unary { op, operand } => {
    68	            let v = eval_scalar(operand, env);
    69	            eval_unary(*op, v)
    70	        }
    71	        ExprPlan::Function { .. } => Value::Error(ErrorValue::Name),
    72	        // **Legacy no-registry path only.** Phase 3.6 / W5-39 added
    73	        // real aggregate-over-named-range evaluation, but ONLY in
    74	        // `eval_scalar_with_cache` (the cache-enabled entry point).
    75	        // This older `eval_scalar` function is kept for tests + the
    76	        // SIMD per-cell fallback path; it has no registry handle
    77	        // and intentionally surfaces `#CALC!` for any aggregate-
    78	        // named-range reference. Callers that want actual aggregate
    79	        // semantics must go through `eval_scalar_with_registry` or
    80	        // `eval_scalar_with_cache`.
    81	        ExprPlan::AggregateNameRef { .. } => Value::Error(ErrorValue::Calc),
    82	    }
    83	}
    84	
    85	/// Like `eval_scalar` but dispatches `Function` variants through a `FunctionRegistry`.
    86	/// Unknown function names return `#NAME?`. Args are evaluated left-to-right with this
    87	/// same function (recursive); error-args propagate naturally through the registered
    88	/// function's logic.
    89	pub fn eval_scalar_with_registry<E: CellEnv>(
    90	    plan: &ExprPlan,
    91	    env: &E,
    92	    registry: &FunctionRegistry,
    93	) -> Value {
    94	    // Phase 3.6: legacy entry point — no aggregate cache. Wraps the cached
    95	    // path with `NoAggregateCache` (zero-cost no-op impl). Callers that
    96	    // have a session-owned cache should call `eval_scalar_with_cache`.
    97	    eval_scalar_with_cache(plan, env, registry, &NoAggregateCache)
    98	}
    99	
   100	/// **Phase 3.6 (2026-05-12) — W5-39, AGG-3-01..04 entry point.** Like
   101	/// `eval_scalar_with_registry` but consults `cache` for aggregate-over-
   102	/// range evaluations. When the Function branch detects an aggregate fn
   103	/// with an `AggregateNameRef` arg, it:
   104	///
   105	/// 1. Looks up `(range, function_name)` in `cache`. Hit → returns the
   106	///    cached value without re-scanning the range.
   107	/// 2. Miss → materializes the range via `CellEnv::read_range`, calls
   108	///    the aggregator, stores the result in `cache`, returns it.
   109	///
   110	/// The cache is invalidated externally (via `CalcgraphSession::
   111	/// mark_dirty_from_cell_write` → `InMemAggregateCache::invalidate_at`)
   112	/// when a cell inside any cached range is written. The evaluator itself
   113	/// never invalidates — it only reads + stores.
   114	///
   115	/// Pass `&NoAggregateCache` for the legacy "always recompute" behavior.
   116	pub fn eval_scalar_with_cache<E: CellEnv>(
   117	    plan: &ExprPlan,
   118	    env: &E,
   119	    registry: &FunctionRegistry,
   120	    cache: &dyn AggregateCache,
   121	) -> Value {
   122	    match plan {
   123	        ExprPlan::Number(n) => Value::number(*n),
   124	        ExprPlan::Bool(b) => Value::Boolean(*b),
   125	        ExprPlan::String(s) => Value::Text(s.clone()),
   126	        ExprPlan::CellRef {
   127	            sheet, row, col, ..
   128	        } => env.read_cell(*sheet, *row, *col),
   129	        ExprPlan::Binary { op, lhs, rhs } => {
   130	            let l = eval_scalar_with_cache(lhs, env, registry, cache);
   131	            let r = eval_scalar_with_cache(rhs, env, registry, cache);
   132	            eval_binary(*op, l, r)
   133	        }
   134	        ExprPlan::Unary { op, operand } => {
   135	            let v = eval_scalar_with_cache(operand, env, registry, cache);
   136	            eval_unary(*op, v)
   137	        }
   138	        ExprPlan::Function { name, args } => {
   139	            // W5-53 (GAP-F-05 closure): check the range-aware table
   140	            // FIRST. Range-aware functions like SUMIF need per-arg
   141	            // range-vs-scalar metadata that the scalar `&[Value]`
   142	            // contract can't carry. If a function is registered as
   143	            // range-aware, build `Vec<FnArg>` with the correct
   144	            // variant per arg (`AggregateNameRef` → `Range(read_range)`,
   145	            // everything else → `Scalar(eval)`).
   146	            if let Some(raf) = registry.lookup_range_aware(name) {
   147	                use ql_functions::FnArg;
   148	                let mut fn_args: Vec<FnArg> = Vec::with_capacity(args.len());
   149	                for a in args {
   150	                    match a {
   151	                        ExprPlan::AggregateNameRef { range, .. } => {
   152	                            // W5-54: shape-aware Range so VLOOKUP /
   153	                            // HLOOKUP / INDEX can address by (row,
   154	                            // col). SUMIF / COUNTIF ignore shape.
   155	                            let (values, rows, cols) = env.read_range_with_shape(*range);
   156	                            fn_args.push(FnArg::Range { values, rows, cols });
   157	                        }
   158	                        other => {
   159	                            fn_args.push(FnArg::Scalar(eval_scalar_with_cache(
   160	                                other, env, registry, cache,
   161	                            )));
   162	                        }
   163	                    }
   164	                }
   165	                return raf(&fn_args);
   166	            }
   167	            // **W5-69 (Phase 4.5.A.0):** check the context-aware table
   168	            // SECOND. Date / locale / clock-aware functions (DATE, NOW,
   169	            // TODAY, WEEKDAY, ...) take an extra `&EvalContext` arg in
   170	            // addition to pre-evaluated `&[Value]`. Args are evaluated
   171	            // left-to-right exactly like the scalar path; only the
   172	            // function signature differs. EvalContext is sourced from
   173	            // `env.eval_context()` (default is Excel1900 + EnUs +
   174	            // System).
   175	            if let Some(caf) = registry.lookup_context_aware(name) {
   176	                let evaluated: Vec<Value> = args
   177	                    .iter()
   178	                    .map(|a| eval_scalar_with_cache(a, env, registry, cache))
   179	                    .collect();
   180	                return caf(&evaluated, env.eval_context());
   181	            }
   182	            match registry.lookup(name) {
   183	                Some(f) => {
   184	                    // Phase 3.6 (2026-05-12) — AGG-3-04 correctness + AGG-
   185	                    // 3-01 cache hit path. Detect aggregate-over-named-range
   186	                    // and route through the cache. The cache key is `(range,
   187	                    // function_name)` — the EXACT call shape, not just the
   188	                    // range — so `SUM(Sales)` and `AVERAGE(Sales)` are
   189	                    // separate entries.
   190	                    //
   191	                    // Single-AggregateNameRef-arg fast path: most aggregates
   192	                    // are called with a single named range (`=SUM(Sales)`).
   193	                    // We special-case that for cache use. Multi-range
   194	                    // aggregates (`SUM(Sales, Discount)`) fall back to no-
   195	                    // cache (compute on every call) — V1 limitation.
   196	                    let single_range_arg = match args.as_slice() {
   197	                        [ExprPlan::AggregateNameRef { range, .. }] => Some(*range),
   198	                        _ => None,
   199	                    };
   200	                    if let Some(range) = single_range_arg {
   201	                        if crate::plan::is_aggregate_function(name) {
   202	                            if let Some(cached) = cache.lookup_aggregate(range, name) {
   203	                                return cached;
   204	                            }
   205	                            let flat = env.read_range(range);
   206	                            let v = f(&flat);
   207	                            // Don't cache `Value::Error(_)` results — they
   208	                            // often signal env-level issues (missing sheet
   209	                            // → #REF!, empty range AVERAGE → #DIV/0!) that
   210	                            // a follow-up edit would resolve; better to
   211	                            // recompute than serve a stale error.
   212	                            if !matches!(v, Value::Error(_)) {
   213	                                cache.store_aggregate(range, name, v.clone());
   214	                            }
   215	                            return v;
   216	                        }
   217	                    }
   218	                    let has_range_arg = args
   219	                        .iter()
   220	                        .any(|a| matches!(a, ExprPlan::AggregateNameRef { .. }));
   221	                    if has_range_arg && crate::plan::is_aggregate_function(name) {
   222	                        // Multi-range or mixed aggregate args: materialize
   223	                        // every range and call the function. No cache use
   224	                        // (V2 may add multi-range cache keys).
   225	                        let mut flat: Vec<Value> = Vec::new();
   226	                        for a in args {
   227	                            match a {
   228	                                ExprPlan::AggregateNameRef { range, .. } => {
   229	                                    flat.extend(env.read_range(*range));
   230	                                }
   231	                                other => {
   232	                                    flat.push(eval_scalar_with_cache(other, env, registry, cache));
   233	                                }
   234	                            }
   235	                        }
   236	                        f(&flat)
   237	                    } else {
   238	                        let evaluated: Vec<Value> = args
   239	                            .iter()
   240	                            .map(|a| eval_scalar_with_cache(a, env, registry, cache))
   241	                            .collect();
   242	                        f(&evaluated)
   243	                    }
   244	                }
   245	                None => Value::Error(ErrorValue::Name),
   246	            }
   247	        }
   248	        // `AggregateNameRef` outside a Function context — the binder is
   249	        // supposed to surface `BindError::NamedRangeInScalarContext`
   250	        // before we ever evaluate. Defensive fallback: `#CALC!`.
   251	        ExprPlan::AggregateNameRef { .. } => Value::Error(ErrorValue::Calc),
   252	    }
   253	}
   254	
   255	fn eval_binary(op: Operator, lhs: Value, rhs: Value) -> Value {
   256	    // Error propagation comes first — Excel's left-error-wins rule.
   257	    if let Value::Error(e) = lhs {
   258	        return Value::Error(e);
   259	    }
   260	    if let Value::Error(e) = rhs {

exec
/bin/zsh -lc "rg \"pub struct EvalContext|struct EvalContext\" -n crates/ql-types/src && nl -ba crates/ql-types/src/context.rs | sed -n '1,160p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
crates/ql-types/src/eval_context.rs:136:pub struct EvalContext {
nl: crates/ql-types/src/context.rs: No such file or directory

exec
/bin/zsh -lc "nl -ba crates/ql-functions/src/registry.rs | sed -n '1056,1125p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1056	
  1057	    #[test]
  1058	    fn lookup_any_returns_tagged_enum() {
  1059	        let mut r = FunctionRegistry::new();
  1060	        r.register("S", scalar_fns::sum);
  1061	        r.register_range_aware("RA", range_fns::sumif);
  1062	        r.register_context_aware("CA", echo_date_system_year);
  1063	        r.register_unified("UFN", fixed_sequence_3);
  1064	
  1065	        assert!(matches!(r.lookup_any("S"), Some(RegisteredFn::Scalar(_))));
  1066	        assert!(matches!(
  1067	            r.lookup_any("RA"),
  1068	            Some(RegisteredFn::RangeAware(_))
  1069	        ));
  1070	        assert!(matches!(
  1071	            r.lookup_any("CA"),
  1072	            Some(RegisteredFn::ContextAware(_))
  1073	        ));
  1074	        assert!(matches!(
  1075	            r.lookup_any("UFN"),
  1076	            Some(RegisteredFn::Unified(_))
  1077	        ));
  1078	        assert!(r.lookup_any("missing").is_none());
  1079	    }
  1080	}

exec
/bin/zsh -lc "nl -ba crates/ql-types/src/eval_context.rs | sed -n '120,190p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   120	                        .map_err(|_| ErrorValue::Num)?
   121	                        .as_secs();
   122	                    Ok((secs, 0))
   123	                }
   124	            }
   125	        }
   126	    }
   127	}
   128	
   129	/// Bundle passed to context-aware functions alongside their `&[Value]` args.
   130	///
   131	/// **W5-69 scaffolding contract:** `EvalContext` is `Copy` (all three fields
   132	/// are `Copy` enums). The runtime supplies one at every eval site; the cheap
   133	/// default (`DEFAULT_EVAL_CONTEXT`) is sufficient until Sub-phase 4.5.A
   134	/// upgrades the runtime to read the workbook's actual date_system.
   135	#[derive(Clone, Copy, Debug, Eq, PartialEq, Default)]
   136	pub struct EvalContext {
   137	    pub date_system: DateSystem,
   138	    pub locale: Locale,
   139	    pub now_provider: NowProvider,
   140	}
   141	
   142	impl EvalContext {
   143	    /// Builder convenience: an `EvalContext` with everything default
   144	    /// (Excel1900 + EnUs + System). Same as `EvalContext::default()` but
   145	    /// usable as a `const` initializer.
   146	    pub const DEFAULT: EvalContext = EvalContext {
   147	        date_system: DateSystem::Excel1900,
   148	        locale: Locale::EnUs,
   149	        now_provider: NowProvider::System,
   150	    };
   151	
   152	    /// Test-friendly constructor: pin `unix_secs` + UTC offset.
   153	    pub fn for_test(unix_secs: u64, utc_offset_seconds: i32) -> EvalContext {
   154	        EvalContext {
   155	            date_system: DateSystem::Excel1900,
   156	            locale: Locale::EnUs,
   157	            now_provider: NowProvider::Test {
   158	                unix_secs,
   159	                utc_offset_seconds,
   160	            },
   161	        }
   162	    }
   163	}
   164	
   165	/// `'static` default context. Returned by `CellEnv::eval_context()`'s default
   166	/// impl, so callers without a workbook (tests, MapEnv, benches) get sensible
   167	/// behavior without plumbing.
   168	pub static DEFAULT_EVAL_CONTEXT: EvalContext = EvalContext::DEFAULT;
   169	
   170	#[cfg(test)]
   171	mod tests {
   172	    use super::*;
   173	
   174	    #[test]
   175	    fn default_is_excel1900_enus_system() {
   176	        let ctx = EvalContext::default();
   177	        assert_eq!(ctx.date_system, DateSystem::Excel1900);
   178	        assert_eq!(ctx.locale, Locale::EnUs);
   179	        assert_eq!(ctx.now_provider, NowProvider::System);
   180	    }
   181	
   182	    #[test]
   183	    fn static_default_matches_const() {
   184	        // DEFAULT_EVAL_CONTEXT static should equal EvalContext::DEFAULT.
   185	        assert_eq!(DEFAULT_EVAL_CONTEXT, EvalContext::DEFAULT);
   186	    }
   187	
   188	    #[test]
   189	    fn for_test_helper_pins_clock() {
   190	        let ctx = EvalContext::for_test(1_700_000_000, -28800);

exec
/bin/zsh -lc "nl -ba crates/ql-types/src/lib.rs | sed -n '1,120p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
     1	//! `ql-types` — Phase 0 minimum cell-value types for the Quantbook engine.
     2	//!
     3	//! Modules:
     4	//! - [`error`] — the 14-variant `ErrorValue` (Excel sigils + Quantbook-specific surfaces + AI reservation).
     5	//! - [`value`] — the 5-variant `Value` (`Blank | Number | Boolean | Text | Error`).
     6	//! - [`array`] — `ArrayValue`: a 2D row-major container of `Value` cells, used at the eval
     7	//!   boundary and in the unified function-dispatch ABI (Phase 4.7, W5-95).
     8	//! - [`coercion`] — Excel-compatible boundary functions: `to_number_{strict,lenient}`,
     9	//!   `to_logical`, `to_text_for_{display,formula,arg}`, `to_int_arg`,
    10	//!   `to_number_strict_skip_blank`, `format_number_for_arg`, `sanitize_f64`.
    11	//! - [`address`] — cell coordinate types: `SheetId`, `RowId`, `ColId`, `Address`, `Range`.
    12	//!
    13	//! Spec: `.plans/_QUANTBOOK-v1-SPECIFICATION.md` Part V §2 Week 2 Day 1-2.
    14	
    15	pub mod address;
    16	pub mod array;
    17	pub mod coercion;
    18	pub mod date;
    19	pub mod error;
    20	pub mod eval_context;
    21	pub mod value;
    22	
    23	pub use address::{Address, ColId, Range, RowId, SheetId, MAX_COLUMN, MAX_ROW};
    24	pub use array::{ArrayShapeError, ArrayValue};
    25	pub use coercion::{
    26	    format_number_for_arg, sanitize_f64, to_int_arg, to_logical, to_number_lenient,
    27	    to_number_strict, to_number_strict_skip_blank, to_text_for_arg, to_text_for_display,
    28	    to_text_for_formula,
    29	};
    30	pub use date::{
    31	    days_in_month, fraction_to_hms, hms_to_fraction, is_leap_year, serial_to_ymd, unix_days_to_ymd,
    32	    ymd_to_serial, ymd_to_unix_days, MAX_EXCEL_SERIAL_DAY, UNIX_EPOCH_AS_1900_SERIAL,
    33	    UNIX_EPOCH_AS_1904_SERIAL,
    34	};
    35	pub use error::{ErrorValue, ParseErrorValueError};
    36	pub use eval_context::{DateSystem, EvalContext, Locale, NowProvider, DEFAULT_EVAL_CONTEXT};
    37	pub use value::Value;
    38	
    39	#[cfg(test)]
    40	mod tests {
    41	    use super::*;
    42	
    43	    #[test]
    44	    fn reexports_compile() {
    45	        let _: Value = Value::Blank;
    46	        let _: ErrorValue = ErrorValue::Ref;
    47	        let _: Result<f64, ErrorValue> = sanitize_f64(1.5);
    48	        let _: Result<f64, ErrorValue> = to_number_strict(&Value::Blank);
    49	    }
    50	}

exec
/bin/zsh -lc "rg \"FunctionRegistry \\{|range_aware_fns:|context_aware_fns:|\\.fns\\b|lookup\\(|lookup_range_aware\\(|lookup_context_aware\\(\" -n crates/ql-* | head -300" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
crates/ql-exec/src/loader.rs:212:            loaded.names().lookup("TAXRATE"),
crates/ql-io/src/qbook_format.rs:2418:            loaded.names().lookup("TAXRATE"),
crates/ql-io/src/qbook_format.rs:2422:            loaded.names().lookup("ANCHORA1"),
crates/ql-io/src/qbook_format.rs:2426:            loaded.names().lookup("SALESRANGE"),
crates/ql-io/src/qbook_format.rs:2430:            loaded.names().lookup("PROFIT"),
crates/ql-io/src/qbook_format.rs:2515:            loaded.formats().lookup(ql_storage::FormatId(14)),
crates/ql-io/src/qbook_format.rs:2541:        assert_eq!(loaded.formats().lookup(id_a), Some("\"⚓\" #,##0.00"));
crates/ql-io/src/qbook_format.rs:2542:        assert_eq!(loaded.formats().lookup(id_b), Some("0.0000"));
crates/ql-io/src/qbook_format.rs:2545:            loaded.formats().lookup(ql_storage::FormatId(0)),
crates/ql-io/src/qbook_format.rs:2609:        assert_eq!(loaded.formats().lookup(loaded_id), Some("\"€\" #,##0.00"));
crates/ql-io/src/qbook_format.rs:2635:            loaded.formats().lookup(ql_storage::FormatId(0)),
crates/ql-io/src/qbook_format.rs:2641:                loaded.formats().lookup(ql_storage::FormatId(id)).is_none(),
crates/ql-oplog/src/replay.rs:402:                if workbook.formats().lookup(fid).is_none() {
crates/ql-oplog/src/replay.rs:730:            wb.formats().lookup(ql_storage::FormatId(200)),
crates/ql-oplog/src/replay.rs:750:            wb.formats().lookup(ql_storage::FormatId(0)),
crates/ql-storage/src/sheet.rs:33:    /// `workbook.formats().lookup(id)` to get the format-string, parse,
crates/ql-functions/src/lib.rs:36:pub use context_aware_fns::ContextAwareFn;
crates/ql-functions/src/lib.rs:37:pub use range_aware_fns::{FnArg, RangeAwareFn};
crates/ql-exec/src/scalar.rs:146:            if let Some(raf) = registry.lookup_range_aware(name) {
crates/ql-exec/src/scalar.rs:175:            if let Some(caf) = registry.lookup_context_aware(name) {
crates/ql-exec/src/scalar.rs:182:            match registry.lookup(name) {
crates/ql-storage/src/workbook.rs:193:    pub fn lookup(&self, name: &str) -> Option<NamedTarget> {
crates/ql-storage/src/workbook.rs:762:        assert!(wb.names().lookup("MyName").is_none());
crates/ql-storage/src/workbook.rs:779:        assert!(wb.names().lookup("MIXEDCASE").is_some());
crates/ql-storage/src/workbook.rs:781:        assert!(wb.names().lookup("MixedCase").is_none());
crates/ql-storage/src/workbook.rs:789:        assert!(wb.names().lookup("ANOTHERNAME").is_some());
crates/ql-storage/src/workbook.rs:1005:        assert!(wb.names().lookup("TAXRATE").is_some());
crates/ql-functions/src/registry.rs:18://! `register("SUM", sum_fn)` and `lookup("SUM")` exactly as before. The
crates/ql-functions/src/registry.rs:34:use crate::context_aware_fns::ContextAwareFn;
crates/ql-functions/src/registry.rs:35:use crate::range_aware_fns::RangeAwareFn;
crates/ql-functions/src/registry.rs:153:pub struct FunctionRegistry {
crates/ql-functions/src/registry.rs:157:impl Default for FunctionRegistry {
crates/ql-functions/src/registry.rs:163:impl FunctionRegistry {
crates/ql-functions/src/registry.rs:183:        let prior = self.fns.insert(name, f);
crates/ql-functions/src/registry.rs:244:    pub fn lookup(&self, name: &str) -> Option<ScalarFn> {
crates/ql-functions/src/registry.rs:246:        match self.fns.get(upper.as_str()) {
crates/ql-functions/src/registry.rs:256:    pub fn lookup_range_aware(&self, name: &str) -> Option<RangeAwareFn> {
crates/ql-functions/src/registry.rs:258:        match self.fns.get(upper.as_str()) {
crates/ql-functions/src/registry.rs:268:    pub fn lookup_context_aware(&self, name: &str) -> Option<ContextAwareFn> {
crates/ql-functions/src/registry.rs:270:        match self.fns.get(upper.as_str()) {
crates/ql-functions/src/registry.rs:282:        match self.fns.get(upper.as_str()) {
crates/ql-functions/src/registry.rs:295:        self.fns.get(upper.as_str())
crates/ql-functions/src/registry.rs:302:        self.fns
crates/ql-functions/src/registry.rs:311:        self.fns
crates/ql-functions/src/registry.rs:320:        self.fns
crates/ql-functions/src/registry.rs:330:        self.fns
crates/ql-functions/src/registry.rs:340:        self.fns.keys()
crates/ql-functions/src/registry.rs:344:        self.fns.len()
crates/ql-functions/src/registry.rs:348:        self.fns.is_empty()
crates/ql-functions/src/registry.rs:353:pub fn default_registry() -> FunctionRegistry {
crates/ql-functions/src/registry.rs:586:        assert!(r.lookup("SUM").is_none());
crates/ql-functions/src/registry.rs:626:        assert!(r.lookup_range_aware("SUMIF").is_some());
crates/ql-functions/src/registry.rs:627:        assert!(r.lookup_range_aware("sumif").is_some()); // case-insensitive
crates/ql-functions/src/registry.rs:628:        assert!(r.lookup_range_aware("COUNTIF").is_some());
crates/ql-functions/src/registry.rs:630:        assert!(r.lookup_range_aware("SUM").is_none());
crates/ql-functions/src/registry.rs:641:                r.lookup_range_aware(name).is_none(),
crates/ql-functions/src/registry.rs:684:        assert!(r.lookup_context_aware("ECHO.DS").is_some());
crates/ql-functions/src/registry.rs:685:        assert!(r.lookup_context_aware("echo.ds").is_some()); // case-insensitive
crates/ql-functions/src/registry.rs:686:        assert!(r.lookup("ECHO.DS").is_none()); // NOT in scalar table
crates/ql-functions/src/registry.rs:687:        assert!(r.lookup_range_aware("ECHO.DS").is_none()); // NOT in range-aware table
crates/ql-functions/src/registry.rs:694:        let f = r.lookup_context_aware("ECHO.DS").expect("registered");
crates/ql-functions/src/registry.rs:710:                r.lookup_context_aware(name).is_none(),
crates/ql-functions/src/registry.rs:721:                r.lookup_context_aware(name).is_none(),
crates/ql-functions/src/registry.rs:813:        let ai = r.lookup("AI").expect("AI is registered");
crates/ql-functions/src/registry.rs:821:        let ai_lower = r.lookup("ai").expect("ai (lowercase) resolves");
crates/ql-functions/src/registry.rs:828:        let sum_upper = r.lookup("SUM").unwrap();
crates/ql-functions/src/registry.rs:829:        let sum_lower = r.lookup("sum").unwrap();
crates/ql-functions/src/registry.rs:830:        let sum_mixed = r.lookup("Sum").unwrap();
crates/ql-functions/src/registry.rs:842:        assert!(r.lookup("FAKE_FN_NAME_XYZ").is_none());
crates/ql-functions/src/registry.rs:849:        let var_s = r.lookup("VAR.S").unwrap();
crates/ql-functions/src/registry.rs:890:        assert!(r.lookup("CUSTOM.FN").is_some());
crates/ql-functions/src/registry.rs:892:        assert!(r.lookup("custom.fn").is_some());
crates/ql-functions/src/registry.rs:899:        let var = r.lookup("VAR").unwrap();
crates/ql-functions/src/registry.rs:900:        let var_s = r.lookup("VAR.S").unwrap();
crates/ql-functions/src/registry.rs:908:        let sum = r.lookup("SUM").unwrap();
crates/ql-functions/src/registry.rs:969:        assert!(r.lookup("UFN").is_none());
crates/ql-functions/src/registry.rs:970:        assert!(r.lookup_range_aware("UFN").is_none());
crates/ql-functions/src/registry.rs:971:        assert!(r.lookup_context_aware("UFN").is_none());
crates/ql-functions/src/scalar_fns.rs:31:use crate::range_aware_fns::{coerce_numeric, NumericArg};
crates/ql-functions/src/range_fns.rs:41:use crate::range_aware_fns::{coerce_numeric, FnArg, NumericArg};
crates/ql-functions/src/range_fns.rs:600:pub fn vlookup(args: &[FnArg]) -> Value {
crates/ql-functions/src/range_fns.rs:656:pub fn hlookup(args: &[FnArg]) -> Value {
crates/ql-functions/src/range_fns.rs:1906:            vlookup(&[s(t("banana")), table, s(n(2.0)), s(Value::Boolean(false))]),
crates/ql-functions/src/range_fns.rs:1915:            vlookup(&[s(t("z")), table, s(n(2.0)), s(Value::Boolean(false))]),
crates/ql-functions/src/range_fns.rs:1940:        assert_eq!(vlookup(&[s(n(75.0)), table, s(n(2.0))]), t("C"));
crates/ql-functions/src/range_fns.rs:1947:            vlookup(&[s(t("a")), table, s(n(5.0)), s(Value::Boolean(false))]),
crates/ql-functions/src/range_fns.rs:1956:            vlookup(&[
crates/ql-functions/src/range_fns.rs:1965:            vlookup(&[s(t("a")), table, s(n(-1.0)), s(Value::Boolean(false))]),
crates/ql-functions/src/range_fns.rs:1974:            vlookup(&[
crates/ql-functions/src/range_fns.rs:1991:            hlookup(&[s(t("b")), table, s(n(2.0)), s(Value::Boolean(false))]),
crates/ql-functions/src/range_fns.rs:2000:            hlookup(&[s(t("a")), table, s(n(5.0)), s(Value::Boolean(false))]),
crates/ql-storage/src/lib.rs:55:        assert_eq!(wb.formats().lookup(FormatId::GENERAL), Some("General"));
crates/ql-storage/src/lib.rs:56:        assert_eq!(wb.formats().lookup(FormatId(14)), Some("m/d/yyyy"));
crates/ql-storage/src/lib.rs:100:        assert_eq!(wb.formats().lookup(id), Some("yyyy-mm-dd"));
crates/ql-functions/tests/per_function_overrides.rs:52:    let count = reg.lookup("COUNT").expect("COUNT is registered");
crates/ql-functions/tests/per_function_overrides.rs:79:    let count = reg.lookup("COUNT").expect("COUNT is registered");
crates/ql-functions/tests/per_function_overrides.rs:99:    let count = reg.lookup("COUNT").expect("COUNT is registered");
crates/ql-functions/tests/per_function_overrides.rs:115:    let counta = reg.lookup("COUNTA").expect("COUNTA is registered");
crates/ql-functions/tests/per_function_overrides.rs:130:    let counta = reg.lookup("COUNTA").expect("COUNTA is registered");
crates/ql-functions/tests/per_function_overrides.rs:149:    let isnum = reg.lookup("ISNUMBER").expect("ISNUMBER is registered");
crates/ql-functions/tests/per_function_overrides.rs:164:    let istext = reg.lookup("ISTEXT").expect("ISTEXT is registered");
crates/ql-functions/tests/per_function_overrides.rs:177:    let isblank = reg.lookup("ISBLANK").expect("ISBLANK is registered");
crates/ql-functions/tests/per_function_overrides.rs:190:    let islogical = reg.lookup("ISLOGICAL").expect("ISLOGICAL is registered");
crates/ql-functions/tests/per_function_overrides.rs:208:    let iserror = reg.lookup("ISERROR").expect("ISERROR is registered");
crates/ql-functions/tests/per_function_overrides.rs:223:    let isna = reg.lookup("ISNA").expect("ISNA is registered");
crates/ql-functions/tests/per_function_overrides.rs:239:    let iserr = reg.lookup("ISERR").expect("ISERR is registered");
crates/ql-functions/tests/per_function_overrides.rs:258:    let avg = reg.lookup("AVERAGE").expect("AVERAGE is registered");
crates/ql-functions/tests/per_function_overrides.rs:265:    let avg = reg.lookup("AVERAGE").expect("AVERAGE is registered");
crates/ql-functions/tests/per_function_overrides.rs:279:    let median = reg.lookup_range_aware("MEDIAN").expect("MEDIAN registered");
crates/ql-functions/tests/per_function_overrides.rs:286:    let mode = reg.lookup_range_aware("MODE").expect("MODE registered");
crates/ql-functions/tests/per_function_overrides.rs:301:    let match_fn = reg.lookup_range_aware("MATCH").expect("MATCH registered");
crates/ql-functions/tests/per_function_overrides.rs:317:    let vlookup = reg.lookup_range_aware("VLOOKUP").expect("VLOOKUP");
crates/ql-functions/tests/per_function_overrides.rs:321:        vlookup(&[s(n(1.0)), table, s(n(0.0))]),
crates/ql-functions/tests/per_function_overrides.rs:329:    let vlookup = reg.lookup_range_aware("VLOOKUP").expect("VLOOKUP");
crates/ql-functions/tests/per_function_overrides.rs:333:        vlookup(&[s(n(1.0)), table, s(n(5.0))]),
crates/ql-functions/tests/per_function_overrides.rs:341:    let vlookup = reg.lookup_range_aware("VLOOKUP").expect("VLOOKUP");
crates/ql-functions/tests/per_function_overrides.rs:345:        vlookup(&[s(n(99.0)), table, s(n(2.0)), s(Value::Boolean(false))]),
crates/ql-functions/tests/per_function_overrides.rs:358:    let hlookup = reg.lookup_range_aware("HLOOKUP").expect("HLOOKUP");
crates/ql-functions/tests/per_function_overrides.rs:364:        hlookup(&[s(n(1.0)), table, s(n(0.0))]),
crates/ql-functions/tests/per_function_overrides.rs:372:    let hlookup = reg.lookup_range_aware("HLOOKUP").expect("HLOOKUP");
crates/ql-functions/tests/per_function_overrides.rs:376:        hlookup(&[s(n(1.0)), table, s(n(5.0))]),
crates/ql-functions/tests/per_function_overrides.rs:384:    let hlookup = reg.lookup_range_aware("HLOOKUP").expect("HLOOKUP");
crates/ql-functions/tests/per_function_overrides.rs:388:        hlookup(&[s(n(99.0)), table, s(n(2.0)), s(Value::Boolean(false))]),
crates/ql-functions/tests/per_function_overrides.rs:396:    let hlookup = reg.lookup_range_aware("HLOOKUP").expect("HLOOKUP");
crates/ql-functions/tests/per_function_overrides.rs:400:        hlookup(&[s(n(2.0)), table, s(n(2.0)), s(Value::Boolean(false))]),
crates/ql-functions/tests/per_function_overrides.rs:412:    let choose = reg.lookup_range_aware("CHOOSE").expect("CHOOSE");
crates/ql-functions/tests/per_function_overrides.rs:428:    let choose = reg.lookup_range_aware("CHOOSE").expect("CHOOSE");
crates/ql-functions/tests/per_function_overrides.rs:438:    let choose = reg.lookup_range_aware("CHOOSE").expect("CHOOSE");
crates/ql-functions/tests/per_function_overrides.rs:459:    let roundup = reg.lookup("ROUNDUP").expect("ROUNDUP registered");
crates/ql-functions/tests/per_function_overrides.rs:475:    let rounddown = reg.lookup("ROUNDDOWN").expect("ROUNDDOWN registered");
crates/ql-functions/tests/per_function_overrides.rs:489:    let choose = reg.lookup_range_aware("CHOOSE").expect("CHOOSE");
crates/ql-functions/tests/per_function_overrides.rs:502:    let rank = reg.lookup_range_aware("RANK").expect("RANK");
crates/ql-functions/tests/per_function_overrides.rs:510:    let rank_avg = reg.lookup_range_aware("RANK.AVG").expect("RANK.AVG");
crates/ql-functions/tests/per_function_overrides.rs:522:    let large = reg.lookup_range_aware("LARGE").expect("LARGE");
crates/ql-functions/tests/per_function_overrides.rs:530:    let small = reg.lookup_range_aware("SMALL").expect("SMALL");
crates/ql-functions/tests/per_function_overrides.rs:544:    let large = reg.lookup_range_aware("LARGE").expect("LARGE");
crates/ql-functions/tests/per_function_overrides.rs:552:    let median = reg.lookup_range_aware("MEDIAN").expect("MEDIAN");
crates/ql-exec/src/workbook_runtime.rs:1060:            if self.workbook.formats().lookup(fid).is_none() {
crates/ql-exec/src/workbook_runtime.rs:1134:                .lookup(format_id)
crates/ql-exec/src/workbook_runtime.rs:2154:    fn set_name_uppercases_for_canonical_lookup() {
crates/ql-exec/src/workbook_runtime.rs:2947:                reg.lookup(name).is_some(),
crates/ql-exec/src/workbook_runtime.rs:2983:                reg.lookup_range_aware(name).is_some(),
crates/ql-exec/src/workbook_runtime.rs:2988:                reg.lookup(name).is_none(),
crates/ql-exec/src/workbook_runtime.rs:5099:            replay_wb.formats().lookup(custom_id),
crates/ql-exec/tests/oplog_e2e.rs:437:        loaded_wb.formats().lookup(custom_id),
crates/ql-storage/src/format.rs:100:    pub fn lookup(&self, id: FormatId) -> Option<&str> {
crates/ql-storage/src/format.rs:207:        assert_eq!(t.lookup(FormatId::GENERAL), Some("General"));
crates/ql-storage/src/format.rs:208:        assert_eq!(t.lookup(FormatId(14)), Some("m/d/yyyy"));
crates/ql-storage/src/format.rs:209:        assert_eq!(t.lookup(FormatId(49)), Some("@"));
crates/ql-storage/src/format.rs:216:        assert_eq!(t.lookup(FormatId(12)), None);
crates/ql-storage/src/format.rs:234:        assert_eq!(t.lookup(id), Some("\"€\" #,##0.00"));
crates/ql-storage/src/format.rs:258:        assert_eq!(t.lookup(FormatId(200)), Some("REPLAY"));

exec
/bin/zsh -lc 'git show afbaebce223''^:quantbook-engine/crates/ql-functions/src/registry.rs | rg "pub fn names|range_aware_names|context_aware_names|names_all|len|is_empty|register_context_aware|lookup_context_aware" -n -C 3' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc 'git show afbaebce223''^:quantbook-engine/crates/ql-functions/src/registry.rs | nl -ba | sed -n '"'1,180p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-qg5mZkDa' (errno=Operation not permitted)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-2GGms5FL' (errno=Operation not permitted)
51-    ///
52-    /// Phase 2A.7 audit M5: `name` MUST already be canonical upper-case. This is
53-    /// asserted at registration time so a stray `register("sum", ...)` doesn't
54:    /// silently create an unreachable entry (lookups uppercase the query, so a
55-    /// lower-case key would never be found). Duplicate registrations also
56:    /// panic — silent override would let a typo replace a built-in with a buggy
57-    /// shim. Phase 2 has a closed default function set; if dynamic registration
58-    /// ever becomes a real use case, swap this for `Result<(), RegisterError>`.
59-    pub fn register(&mut self, name: &'static str, f: ScalarFn) {
60-        assert!(
61:            !name.is_empty(),
62-            "FunctionRegistry::register: name must not be empty"
63-        );
64-        assert!(
--
83-        assert!(
84-            prior.is_none(),
85-            "FunctionRegistry::register: duplicate registration for {name:?} — \
86:             silent override would let a typo replace a built-in"
87-        );
88-    }
89-
--
93-    /// cannot live in both `fns` and `range_aware_fns`.
94-    pub fn register_range_aware(&mut self, name: &'static str, f: RangeAwareFn) {
95-        assert!(
96:            !name.is_empty(),
97-            "FunctionRegistry::register_range_aware: name must not be empty"
98-        );
99-        assert!(
--
124-    /// addition to the standard `&[Value]` args. Same canonical-uppercase
125-    /// requirement + duplicate panic + cross-table disjointness as the
126-    /// other two `register_*` methods.
127:    pub fn register_context_aware(&mut self, name: &'static str, f: ContextAwareFn) {
128-        assert!(
129:            !name.is_empty(),
130:            "FunctionRegistry::register_context_aware: name must not be empty"
131-        );
132-        assert!(
133-            name.bytes().all(|b| !b.is_ascii_lowercase()),
134:            "FunctionRegistry::register_context_aware: name {name:?} must be canonical \
135-             upper-case"
136-        );
137-        assert!(
138-            !self.fns.contains_key(name),
139:            "FunctionRegistry::register_context_aware: {name:?} is already registered \
140-             as a scalar function; cannot register in both tables"
141-        );
142-        assert!(
143-            !self.range_aware_fns.contains_key(name),
144:            "FunctionRegistry::register_context_aware: {name:?} is already registered \
145-             as a range-aware function; cannot register in both tables"
146-        );
147-        let prior = self.context_aware_fns.insert(name, f);
148-        assert!(
149-            prior.is_none(),
150:            "FunctionRegistry::register_context_aware: duplicate registration for {name:?}"
151-        );
152-    }
153-
--
172-    /// context-aware table. Callers should check this AFTER
173-    /// `lookup_range_aware` but BEFORE `lookup`. Dispatch order:
174-    /// `range_aware` → `context_aware` → `scalar`.
175:    pub fn lookup_context_aware(&self, name: &str) -> Option<ContextAwareFn> {
176-        let upper = name.to_ascii_uppercase();
177-        self.context_aware_fns.get(upper.as_str()).copied()
178-    }
179-
180:    pub fn names(&self) -> impl Iterator<Item = &&'static str> {
181-        self.fns.keys()
182-    }
183-
--
185-    /// range-aware table. The original `names()` only exposes scalar names;
186-    /// any coverage report or registry-walk that wants to enumerate ALL
187-    /// registered functions (e.g. for matrix-test completeness) must call
188:    /// both `names()` and `range_aware_names()`.
189:    pub fn range_aware_names(&self) -> impl Iterator<Item = &&'static str> {
190-        self.range_aware_fns.keys()
191-    }
192-
193-    /// **W5-69 (Phase 4.5.A.0):** names registered in the context-aware
194:    /// table. Same pattern as `range_aware_names()`; coverage walks must
195:    /// include this iterator OR use `names_all()` (which chains all 3).
196:    pub fn context_aware_names(&self) -> impl Iterator<Item = &&'static str> {
197-        self.context_aware_fns.keys()
198-    }
199-
--
202-    /// three tables are disjoint by registration invariant, so no
203-    /// deduplication is needed. **W5-69 (Phase 4.5.A.0):** extended to chain
204-    /// the context-aware table.
205:    pub fn names_all(&self) -> impl Iterator<Item = &&'static str> {
206-        self.fns
207-            .keys()
208-            .chain(self.range_aware_fns.keys())
209-            .chain(self.context_aware_fns.keys())
210-    }
211-
212:    pub fn len(&self) -> usize {
213:        self.fns.len() + self.range_aware_fns.len() + self.context_aware_fns.len()
214-    }
215-
216:    pub fn is_empty(&self) -> bool {
217:        self.fns.is_empty() && self.range_aware_fns.is_empty() && self.context_aware_fns.is_empty()
218-    }
219-}
220-
--
276-    r.register("PI", scalar_fns::pi);
277-    r.register("DEGREES", scalar_fns::degrees);
278-    r.register("RADIANS", scalar_fns::radians);
279:    r.register("LEN", scalar_fns::len);
280-    r.register("UPPER", scalar_fns::upper);
281-    r.register("LOWER", scalar_fns::lower);
282-    r.register("PROPER", scalar_fns::proper);
--
346-    // + locale-aware UTC offset from `EvalContext`. The legacy
347-    // `volatile::now` / `volatile::today` scalar functions are kept in
348-    // the source (deprecated callable API) but no longer registered.
349:    r.register_context_aware("NOW", volatile::now_ctx);
350:    r.register_context_aware("TODAY", volatile::today_ctx);
351-    r.register("RAND", volatile::rand);
352-    r.register("RANDBETWEEN", volatile::randbetween);
353-
--
355-    // foundational 8 (per W5-68 design § 5.1). DATE/YEAR/MONTH/DAY are
356-    // ContextAwareFn (need workbook.date_system for serial interp).
357-    // HOUR/MINUTE/SECOND/TIME are pure scalar (no date_system dep).
358:    r.register_context_aware("DATE", date_fns::date_ctx);
359:    r.register_context_aware("YEAR", date_fns::year_ctx);
360:    r.register_context_aware("MONTH", date_fns::month_ctx);
361:    r.register_context_aware("DAY", date_fns::day_ctx);
362-    r.register("HOUR", date_fns::hour);
363-    r.register("MINUTE", date_fns::minute);
364-    r.register("SECOND", date_fns::second);
--
367-    // **W5-73 (Phase 4.5.B wave 2):** date text parsing + month arithmetic +
368-    // weekday. All ContextAwareFn (date_system aware; TIMEVALUE is locale-
369-    // technically but consistent tier).
370:    r.register_context_aware("DATEVALUE", date_fns::datevalue_ctx);
371:    r.register_context_aware("TIMEVALUE", date_fns::timevalue_ctx);
372:    r.register_context_aware("WEEKDAY", date_fns::weekday_ctx);
373:    r.register_context_aware("EOMONTH", date_fns::eomonth_ctx);
374:    r.register_context_aware("EDATE", date_fns::edate_ctx);
375-
376-    // **W5-74 (Phase 4.5.B wave 3, CLOSES V1 wave 18/18):** business-date
377-    // + finance basics. DAYS is ScalarFn (pure subtraction); the rest
378-    // are ContextAwareFn. Holidays arg unsupported in V1 (see GAP-F-09,
379-    // GAP-F-10) — 3-arg NETWORKDAYS/WORKDAY returns #VALUE!.
380-    r.register("DAYS", date_fns::days);
381:    r.register_context_aware("NETWORKDAYS", date_fns::networkdays_ctx);
382:    r.register_context_aware("WORKDAY", date_fns::workday_ctx);
383:    r.register_context_aware("YEARFRAC", date_fns::yearfrac_ctx);
384-
385-    // **W5-75 (Phase 4.5.C V2 wave, 4 of 6):** date-arithmetic V2 fns.
386-    // NETWORKDAYS.INTL + WORKDAY.INTL deferred (carry GAP-F-09/10
387-    // tier-4 dependency).
388:    r.register_context_aware("DATEDIF", date_fns::datedif_ctx);
389:    r.register_context_aware("DAYS360", date_fns::days360_ctx);
390:    r.register_context_aware("WEEKNUM", date_fns::weeknum_ctx);
391:    r.register_context_aware("ISOWEEKNUM", date_fns::isoweeknum_ctx);
392-
393-    // **W5-83 (Phase 4.5.E):** `TEXT(value, format_string)` — format a
394-    // value into a text string per the workbook's number-format grammar.
395-    // ContextAwareFn because the renderer needs `DateSystem` for the
396-    // serial→date conversion path. Parser failures + V2-deferred tokens
397-    // surface as `#VALUE!`. Companion `format::render` shipped W5-78.
398:    r.register_context_aware("TEXT", format::text_ctx);
399-
400-    // Engine Phase 4.3 V2 batch — range-aware (W5-53, GAP-F-05
401-    // closure). These use the new `RangeAwareFn` table because the
--
451-    #[test]
452-    fn empty_registry_lookup_none() {
453-        let r = FunctionRegistry::new();
454:        assert!(r.is_empty());
455-        assert!(r.lookup("SUM").is_none());
456-    }
457-
--
486-        // V2 wave (W5-75: DATEDIF, DAYS360, WEEKNUM, ISOWEEKNUM = 4
487-        // of 6; NETWORKDAYS.INTL + WORKDAY.INTL deferred to tier-4) +
488-        // Phase 4.5.E (W5-83: TEXT = 1).
489:        assert_eq!(r.len(), 130);
490-    }
491-
492-    #[test]
--
534-    // Lets us verify dispatch + arg-shape without depending on any
535-    // not-yet-implemented date function.
536-    fn echo_date_system_year(args: &[Value], ctx: &ql_types::EvalContext) -> Value {
537:        if !args.is_empty() {
538-            return Value::Error(ql_types::ErrorValue::Value);
539-        }
540-        let n = match ctx.date_system {
--
547-    #[test]
548-    fn context_aware_register_and_lookup_works() {
549-        let mut r = FunctionRegistry::new();
550:        r.register_context_aware("ECHO.DS", echo_date_system_year);
551:        assert!(r.lookup_context_aware("ECHO.DS").is_some());
552:        assert!(r.lookup_context_aware("echo.ds").is_some()); // case-insensitive
553-        assert!(r.lookup("ECHO.DS").is_none()); // NOT in scalar table
554-        assert!(r.lookup_range_aware("ECHO.DS").is_none()); // NOT in range-aware table
555-    }
--
557-    #[test]
558-    fn context_aware_fn_receives_eval_context() {
559-        let mut r = FunctionRegistry::new();
560:        r.register_context_aware("ECHO.DS", echo_date_system_year);
561:        let f = r.lookup_context_aware("ECHO.DS").expect("registered");
562-        let ctx_1900 = ql_types::EvalContext::default();
563-        assert_eq!(f(&[], &ctx_1900), Value::Number(1900.0));
564-        let ctx_1904 = ql_types::EvalContext {
--
592-
593-    #[test]
594-    #[should_panic(expected = "is already registered as a scalar function")]
595:    fn register_context_aware_with_existing_scalar_name_panics() {
596-        let mut r = FunctionRegistry::new();
597-        r.register("FOO", scalar_fns::sum);
598:        r.register_context_aware("FOO", echo_date_system_year);
599-    }
600-
601-    #[test]
602-    #[should_panic(expected = "is already registered as a range-aware function")]
603:    fn register_context_aware_with_existing_range_aware_name_panics() {
604-        let mut r = FunctionRegistry::new();
605-        r.register_range_aware("FOO", range_fns::sumif);
606:        r.register_context_aware("FOO", echo_date_system_year);
607-    }
608-
609-    #[test]
610-    #[should_panic(expected = "is already registered as a context-aware function")]
611-    fn register_scalar_with_existing_context_aware_name_panics() {
612-        let mut r = FunctionRegistry::new();
613:        r.register_context_aware("FOO", echo_date_system_year);
614-        r.register("FOO", scalar_fns::sum);
615-    }
616-
--
618-    #[should_panic(expected = "is already registered as a context-aware function")]
619-    fn register_range_aware_with_existing_context_aware_name_panics() {
620-        let mut r = FunctionRegistry::new();
621:        r.register_context_aware("FOO", echo_date_system_year);
622-        r.register_range_aware("FOO", range_fns::sumif);
623-    }
624-
--
626-    #[should_panic(expected = "duplicate registration")]
627-    fn duplicate_context_aware_registration_panics() {
628-        let mut r = FunctionRegistry::new();
629:        r.register_context_aware("ECHO.DS", echo_date_system_year);
630:        r.register_context_aware("ECHO.DS", echo_date_system_year);
631-    }
632-
633-    #[test]
634:    fn names_all_chains_all_three_tables() {
635-        let mut r = FunctionRegistry::new();
636-        r.register("SCALAR_FN", scalar_fns::sum);
637-        r.register_range_aware("RANGE_FN", range_fns::sumif);
638:        r.register_context_aware("CONTEXT_FN", echo_date_system_year);
639:        let all: Vec<&str> = r.names_all().copied().collect();
640-        assert!(all.contains(&"SCALAR_FN"));
641-        assert!(all.contains(&"RANGE_FN"));
642-        assert!(all.contains(&"CONTEXT_FN"));
643:        assert_eq!(all.len(), 3);
644-    }
645-
646-    #[test]
647:    fn len_includes_context_aware_table() {
648-        let mut r = FunctionRegistry::new();
649-        r.register("S", scalar_fns::sum);
650:        assert_eq!(r.len(), 1);
651-        r.register_range_aware("RA", range_fns::sumif);
652:        assert_eq!(r.len(), 2);
653:        r.register_context_aware("CA", echo_date_system_year);
654:        assert_eq!(r.len(), 3);
655-    }
656-
657-    #[test]
658:    fn is_empty_checks_all_three_tables() {
659-        let mut r = FunctionRegistry::new();
660:        assert!(r.is_empty());
661:        r.register_context_aware("CA", echo_date_system_year);
662:        assert!(!r.is_empty());
663-    }
664-
665-    #[test]
666:    fn context_aware_names_returns_only_context_aware() {
667-        let mut r = FunctionRegistry::new();
668-        r.register("S", scalar_fns::sum);
669-        r.register_range_aware("RA", range_fns::sumif);
670:        r.register_context_aware("CA", echo_date_system_year);
671:        let names: Vec<&str> = r.context_aware_names().copied().collect();
672-        assert_eq!(names, vec!["CA"]);
673-    }
674-

 succeeded in 0ms:
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-zRR3PipS' (errno=Operation not permitted)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-rEpBup8c' (errno=Operation not permitted)
     1	//! Function registry — name → implementation dispatch.
     2	//!
     3	//! Names are stored uppercase per Excel canon. Lookup is case-insensitive: the caller
     4	//! can pass `"sum"`, `"SUM"`, or `"Sum"` and get the same function.
     5	
     6	use std::collections::HashMap;
     7	
     8	use ql_types::Value;
     9	
    10	use crate::context_aware_fns::ContextAwareFn;
    11	use crate::range_aware_fns::RangeAwareFn;
    12	use crate::{date_fns, format, range_fns, scalar_fns, volatile};
    13	
    14	/// Function signature: pre-evaluated args → result Value.
    15	pub type ScalarFn = fn(&[Value]) -> Value;
    16	
    17	/// Phase 0 function registry. Built by `default_registry()` with the W4-4 function set.
    18	///
    19	/// W5-53 adds a parallel `range_aware_fns` table for functions that
    20	/// need per-argument range-vs-scalar metadata (SUMIF, COUNTIF, etc.).
    21	/// W5-69 (Phase 4.5.A.0) adds `context_aware_fns` for date/locale/clock-
    22	/// aware functions that take an extra `&EvalContext` arg. Dispatch order:
    23	/// range_aware FIRST → context_aware SECOND → scalar LAST. A single name
    24	/// MUST NOT be registered in more than one table (registration panics on
    25	/// any cross-table collision).
    26	#[derive(Clone, Debug)]
    27	pub struct FunctionRegistry {
    28	    fns: HashMap<&'static str, ScalarFn>,
    29	    range_aware_fns: HashMap<&'static str, RangeAwareFn>,
    30	    context_aware_fns: HashMap<&'static str, ContextAwareFn>,
    31	}
    32	
    33	impl Default for FunctionRegistry {
    34	    fn default() -> Self {
    35	        Self::new()
    36	    }
    37	}
    38	
    39	impl FunctionRegistry {
    40	    /// Empty registry — caller adds functions via `register`. Use `default_registry()`
    41	    /// for the Phase 0 built-in set.
    42	    pub fn new() -> Self {
    43	        Self {
    44	            fns: HashMap::new(),
    45	            range_aware_fns: HashMap::new(),
    46	            context_aware_fns: HashMap::new(),
    47	        }
    48	    }
    49	
    50	    /// Register a function under `name`.
    51	    ///
    52	    /// Phase 2A.7 audit M5: `name` MUST already be canonical upper-case. This is
    53	    /// asserted at registration time so a stray `register("sum", ...)` doesn't
    54	    /// silently create an unreachable entry (lookups uppercase the query, so a
    55	    /// lower-case key would never be found). Duplicate registrations also
    56	    /// panic — silent override would let a typo replace a built-in with a buggy
    57	    /// shim. Phase 2 has a closed default function set; if dynamic registration
    58	    /// ever becomes a real use case, swap this for `Result<(), RegisterError>`.
    59	    pub fn register(&mut self, name: &'static str, f: ScalarFn) {
    60	        assert!(
    61	            !name.is_empty(),
    62	            "FunctionRegistry::register: name must not be empty"
    63	        );
    64	        assert!(
    65	            name.bytes().all(|b| !b.is_ascii_lowercase()),
    66	            "FunctionRegistry::register: name {name:?} must be canonical upper-case; \
    67	             lookups uppercase the query, so a lower-case key is unreachable"
    68	        );
    69	        // W5-69 (Phase 4.5.A.0): cross-table disjointness — a name
    70	        // already in range_aware_fns or context_aware_fns cannot also
    71	        // be registered as scalar.
    72	        assert!(
    73	            !self.range_aware_fns.contains_key(name),
    74	            "FunctionRegistry::register: {name:?} is already registered as a \
    75	             range-aware function; cannot register in both tables"
    76	        );
    77	        assert!(
    78	            !self.context_aware_fns.contains_key(name),
    79	            "FunctionRegistry::register: {name:?} is already registered as a \
    80	             context-aware function; cannot register in both tables"
    81	        );
    82	        let prior = self.fns.insert(name, f);
    83	        assert!(
    84	            prior.is_none(),
    85	            "FunctionRegistry::register: duplicate registration for {name:?} — \
    86	             silent override would let a typo replace a built-in"
    87	        );
    88	    }
    89	
    90	    /// W5-53: register a range-aware function under `name`. Same
    91	    /// canonical-uppercase requirement + duplicate panic as
    92	    /// `register`. Cross-table name collision also panics — a name
    93	    /// cannot live in both `fns` and `range_aware_fns`.
    94	    pub fn register_range_aware(&mut self, name: &'static str, f: RangeAwareFn) {
    95	        assert!(
    96	            !name.is_empty(),
    97	            "FunctionRegistry::register_range_aware: name must not be empty"
    98	        );
    99	        assert!(
   100	            name.bytes().all(|b| !b.is_ascii_lowercase()),
   101	            "FunctionRegistry::register_range_aware: name {name:?} must be canonical \
   102	             upper-case"
   103	        );
   104	        assert!(
   105	            !self.fns.contains_key(name),
   106	            "FunctionRegistry::register_range_aware: {name:?} is already registered \
   107	             as a scalar function; cannot register in both tables"
   108	        );
   109	        // W5-69 (Phase 4.5.A.0): also disjoint from context_aware_fns.
   110	        assert!(
   111	            !self.context_aware_fns.contains_key(name),
   112	            "FunctionRegistry::register_range_aware: {name:?} is already registered \
   113	             as a context-aware function; cannot register in both tables"
   114	        );
   115	        let prior = self.range_aware_fns.insert(name, f);
   116	        assert!(
   117	            prior.is_none(),
   118	            "FunctionRegistry::register_range_aware: duplicate registration for {name:?}"
   119	        );
   120	    }
   121	
   122	    /// **W5-69 (Phase 4.5.A.0):** register a context-aware function. These
   123	    /// receive `&EvalContext` (date_system + locale + now_provider) in
   124	    /// addition to the standard `&[Value]` args. Same canonical-uppercase
   125	    /// requirement + duplicate panic + cross-table disjointness as the
   126	    /// other two `register_*` methods.
   127	    pub fn register_context_aware(&mut self, name: &'static str, f: ContextAwareFn) {
   128	        assert!(
   129	            !name.is_empty(),
   130	            "FunctionRegistry::register_context_aware: name must not be empty"
   131	        );
   132	        assert!(
   133	            name.bytes().all(|b| !b.is_ascii_lowercase()),
   134	            "FunctionRegistry::register_context_aware: name {name:?} must be canonical \
   135	             upper-case"
   136	        );
   137	        assert!(
   138	            !self.fns.contains_key(name),
   139	            "FunctionRegistry::register_context_aware: {name:?} is already registered \
   140	             as a scalar function; cannot register in both tables"
   141	        );
   142	        assert!(
   143	            !self.range_aware_fns.contains_key(name),
   144	            "FunctionRegistry::register_context_aware: {name:?} is already registered \
   145	             as a range-aware function; cannot register in both tables"
   146	        );
   147	        let prior = self.context_aware_fns.insert(name, f);
   148	        assert!(
   149	            prior.is_none(),
   150	            "FunctionRegistry::register_context_aware: duplicate registration for {name:?}"
   151	        );
   152	    }
   153	
   154	    /// Case-insensitive lookup. Returns `None` if not registered.
   155	    pub fn lookup(&self, name: &str) -> Option<ScalarFn> {
   156	        // Allocate a single uppercase key for the lookup; the registry holds &'static
   157	        // uppercase names, so we compare on uppercase form.
   158	        let upper = name.to_ascii_uppercase();
   159	        self.fns.get(upper.as_str()).copied()
   160	    }
   161	
   162	    /// W5-53: case-insensitive lookup in the range-aware table.
   163	    /// Callers should check this BEFORE `lookup` — if a function is
   164	    /// range-aware, the dispatch must construct `Vec<FnArg>` rather
   165	    /// than flattening to `Vec<Value>`.
   166	    pub fn lookup_range_aware(&self, name: &str) -> Option<RangeAwareFn> {
   167	        let upper = name.to_ascii_uppercase();
   168	        self.range_aware_fns.get(upper.as_str()).copied()
   169	    }
   170	
   171	    /// **W5-69 (Phase 4.5.A.0):** case-insensitive lookup in the
   172	    /// context-aware table. Callers should check this AFTER
   173	    /// `lookup_range_aware` but BEFORE `lookup`. Dispatch order:
   174	    /// `range_aware` → `context_aware` → `scalar`.
   175	    pub fn lookup_context_aware(&self, name: &str) -> Option<ContextAwareFn> {
   176	        let upper = name.to_ascii_uppercase();
   177	        self.context_aware_fns.get(upper.as_str()).copied()
   178	    }
   179	
   180	    pub fn names(&self) -> impl Iterator<Item = &&'static str> {

exec
/bin/zsh -lc 'git show afbaebce223''^:quantbook-engine/crates/ql-functions/src/registry.rs | rg "pub fn register|fn insert|lookup'"\\(\" -n -C 2" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-HqwCY9Zb' (errno=Operation not permitted)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-iBRqxdYg' (errno=Operation not permitted)
57-    /// shim. Phase 2 has a closed default function set; if dynamic registration
58-    /// ever becomes a real use case, swap this for `Result<(), RegisterError>`.
59:    pub fn register(&mut self, name: &'static str, f: ScalarFn) {
60-        assert!(
61-            !name.is_empty(),
--
92-    /// `register`. Cross-table name collision also panics — a name
93-    /// cannot live in both `fns` and `range_aware_fns`.
94:    pub fn register_range_aware(&mut self, name: &'static str, f: RangeAwareFn) {
95-        assert!(
96-            !name.is_empty(),
--
125-    /// requirement + duplicate panic + cross-table disjointness as the
126-    /// other two `register_*` methods.
127:    pub fn register_context_aware(&mut self, name: &'static str, f: ContextAwareFn) {
128-        assert!(
129-            !name.is_empty(),
--
153-
154-    /// Case-insensitive lookup. Returns `None` if not registered.
155:    pub fn lookup(&self, name: &str) -> Option<ScalarFn> {
156-        // Allocate a single uppercase key for the lookup; the registry holds &'static
157-        // uppercase names, so we compare on uppercase form.
--
453-        let r = FunctionRegistry::new();
454-        assert!(r.is_empty());
455:        assert!(r.lookup("SUM").is_none());
456-    }
457-
--
551-        assert!(r.lookup_context_aware("ECHO.DS").is_some());
552-        assert!(r.lookup_context_aware("echo.ds").is_some()); // case-insensitive
553:        assert!(r.lookup("ECHO.DS").is_none()); // NOT in scalar table
554-        assert!(r.lookup_range_aware("ECHO.DS").is_none()); // NOT in range-aware table
555-    }
--
677-        use ql_types::ErrorValue;
678-        let r = default_registry();
679:        let ai = r.lookup("AI").expect("AI is registered");
680-        // AI ignores its args and returns the AINotAvailable error.
681-        assert_eq!(ai(&[]), Value::Error(ErrorValue::AINotAvailable));
--
685-        );
686-        // Case-insensitive lookup works.
687:        let ai_lower = r.lookup("ai").expect("ai (lowercase) resolves");
688-        assert_eq!(ai_lower(&[]), Value::Error(ErrorValue::AINotAvailable));
689-    }
--
692-    fn lookup_is_case_insensitive() {
693-        let r = default_registry();
694:        let sum_upper = r.lookup("SUM").unwrap();
695:        let sum_lower = r.lookup("sum").unwrap();
696:        let sum_mixed = r.lookup("Sum").unwrap();
697-        let result_upper = sum_upper(&[Value::Number(1.0), Value::Number(2.0)]);
698-        let result_lower = sum_lower(&[Value::Number(1.0), Value::Number(2.0)]);
--
706-    fn lookup_missing_returns_none() {
707-        let r = default_registry();
708:        assert!(r.lookup("FAKE_FN_NAME_XYZ").is_none());
709-    }
710-
--
713-        // VAR.S — Excel uses dot in modern compatibility-friendly names.
714-        let r = default_registry();
715:        let var_s = r.lookup("VAR.S").unwrap();
716-        let result = var_s(&[Value::Number(1.0), Value::Number(3.0)]);
717-        assert_eq!(result, Value::Number(2.0));
--
754-        let mut r = FunctionRegistry::new();
755-        r.register("CUSTOM.FN", scalar_fns::sum);
756:        assert!(r.lookup("CUSTOM.FN").is_some());
757-        // Case-insensitive lookup still works.
758:        assert!(r.lookup("custom.fn").is_some());
759-    }
760-
--
763-        let r = default_registry();
764-        // VAR and VAR.S — both map to sample variance.
765:        let var = r.lookup("VAR").unwrap();
766:        let var_s = r.lookup("VAR.S").unwrap();
767-        let data = &[Value::Number(1.0), Value::Number(3.0)];
768-        assert_eq!(var(data), var_s(data));
--
772-    fn end_to_end_sum_via_registry() {
773-        let r = default_registry();
774:        let sum = r.lookup("SUM").unwrap();
775-        let result = sum(&[
776-            Value::Number(10.0),

exec
/bin/zsh -lc "nl -ba docs/architecture/2026-05-14-array-formulas-and-spills.md | sed -n '145,195p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc "nl -ba docs/architecture/2026-05-14-array-formulas-and-spills.md | sed -n '535,565p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   145	    pub fn at(&self, row: u32, col: u32) -> &Value { ... }
   146	    pub fn first(&self) -> &Value { &self.cells[0] }
   147	    pub fn is_degenerate(&self) -> bool { self.rows == 0 || self.cols == 0 }
   148	}
   149	```
   150	
   151	`Value` itself stays scalar (no `Value::Array(...)` variant). Storage cells remain scalar by Excel canon — spilling MATERIALIZES arrays into per-cell scalars in the computed overlay. The array vs scalar distinction lives at the EVAL boundary via `EvalResult` (still in `ql-exec`).
   152	
   153	### 6.2 Function ABI — UNIFY, don't extend
   154	
   155	Codex HIGH: adding `ArrayFn` as a 4th parallel tier compounds the dispatch sprawl. Better: collapse the existing 3 tiers into ONE dispatch ABI that can carry scalar, range, array, and context.
   156	
   157	**Revised dispatch shape** in `ql-functions`:
   158	
   159	```rust
   160	// crates/ql-functions/src/registry.rs (revised)
   161	pub enum FunctionArg {
   162	    Scalar(Value),
   163	    Range { sheet: SheetId, range: Range, /* ... existing RangeAwareFn arg shape ... */ },
   164	    Array(ArrayValue),  // new — array values from `Expr::Array` or another function call
   165	}
   166	
   167	pub struct FunctionContext<'a> {
   168	    pub eval_ctx: &'a EvalContext,           // existing (date_system, locale, now)
   169	    pub workbook: &'a dyn FunctionWorkbook,  // existing trait abstraction
   170	    // Phase 4.7 doesn't change the context shape.
   171	}
   172	
   173	pub enum FunctionReturn {
   174	    Scalar(Value),
   175	    Array(ArrayValue),  // new — for SEQUENCE / FILTER / TRANSPOSE
   176	}
   177	
   178	pub type FunctionFn = fn(args: &[FunctionArg], ctx: &FunctionContext) -> FunctionReturn;
   179	```
   180	
   181	Migration:
   182	- Existing `ScalarFn` / `RangeAwareFn` / `ContextAwareFn` registrations re-route to the unified `FunctionFn` via shim adapters. Adapters live in `ql-functions` itself; downstream callers stay unchanged in v1 by exposing the legacy `register_scalar` / `register_range_aware` / `register_context_aware` helpers that internally build the unified `FunctionFn`.
   183	- New array-returning functions register directly with the unified ABI.
   184	- Cleanup of the legacy tier names is a future polish item.
   185	
   186	**Caller-side dispatch** in `ql-exec::eval`:
   187	
   188	```rust
   189	let arg_values: Vec<FunctionArg> = ...;  // materialize from ExprPlan args
   190	let ret: FunctionReturn = (function.f)(&arg_values, &ctx);
   191	let eval_result: EvalResult = match ret {
   192	    FunctionReturn::Scalar(v) => EvalResult::Scalar(v),
   193	    FunctionReturn::Array(a)  => EvalResult::Array(a),
   194	};
   195	```

 succeeded in 0ms:
   535	UNIQUE, SORT, SORTBY, RANDARRAY — straightforward extensions once the array dispatch ABI proves itself in Phase 4.7.M + N.
   536	
   537	## 14. Risks + known unknowns (post-Codex)
   538	
   539	### 14.1 Storage layer Result-vs-Panic
   540	
   541	`Workbook::register_spill` returns `Result<(), SpillBlockError>`. Runtime pre-validates + maps to `RuntimeError::Spill`. No silent failures. Pattern matches W5-93 sheet-name validation.
   542	
   543	### 14.2 Calcgraph nodes for spill targets — DECIDED LAZY
   544	
   545	Per § 10.1 producer-alias model: spill targets do NOT get CellNodes. The graph stays formula-node-heavy. Codex HIGH closed the original "eager" decision.
   546	
   547	### 14.3 Iteration order at recompute
   548	
   549	The producer-alias model makes the dependency chain explicit: `B3 (reads A3) → A1_FormulaNode (spill anchor)`. Tarjan walks this normally. The chain pretends A3's value is "owned" by the anchor's formula node — symmetric to a range read (`B3 reads A1:A10`) which also depends on each individual cell-or-formula in the range.
   550	
   551	**Verified analogy:** existing range deps work this way. `StripeIndex` tracks "which formulas depend on which (sheet, row/col)" — when A3 is touched, B3 (a reader of A3) gets dirtied via the stripe index. The new behavior: when A3 becomes a spill target, B3's NEXT dep-extraction reroutes A3 → A1. Until B3's deps are re-extracted, the stripe-based dirty propagation still works (touching A3 dirties B3, which then re-extracts and finds the rerouted edge).
   552	
   553	### 14.4 Function ABI — DECIDED UNIFY
   554	
   555	Per § 6.2: collapse the four tiers into one `FunctionFn` ABI. Legacy `register_scalar_fn` / `register_range_aware_fn` / `register_context_aware_fn` helpers preserved as shim adapters. No new tier.
   556	
   557	### 14.5 Implicit intersection (Phase 4.9)
   558	
   559	Excel 365+ dynamic arrays canonicalize "array in non-array context → spill or `@`". Legacy Excel canonicalizes "array in scalar context → implicit intersection with caller's row/col". Quantbook v1 Phase 4.7 chooses neither: array-in-scalar-context → `#CALC!` (§ 6.3). This is a defensible safe default; Phase 4.9 lights up `@` syntax + an `xls_calc` mode toggle.
   560	
   561	### 14.6 Re-extraction on spill shape change
   562	
   563	Per § 10.5: invalidate ALL readers transitively when a spill anchor's footprint changes. Big hammer at edit rate; acceptable per the existing W5-91 rename invalidation pattern. Per-spill reverse mapping deferred.
   564	
   565	### 14.7 Pre-W5-107 saved files

exec
/bin/zsh -lc 'rg "ScalarFn|RangeAwareFn|ContextAwareFn|FunctionArg|FunctionReturn|RegisteredFn|ArrayValue" docs/architecture/2026-05-14-array-formulas-and-spills.md -n' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
35:- **Functions:** registry has `ScalarFn` / `RangeAwareFn` / `ContextAwareFn` tiers in `crates/ql-functions/src/registry.rs`. **Will unify, not extend** (see § 6.2).
59:- Unified function ABI in `ql-functions` (collapse `ScalarFn` / `RangeAwareFn` / `ContextAwareFn` into one dispatch shape; gain array support).
129:### 6.1 `ArrayValue` type — lives in `ql-types`
133:**Revised placement:** `ArrayValue` lives in `ql-types` alongside `Value` and `ErrorValue`:
138:pub struct ArrayValue {
144:impl ArrayValue {
161:pub enum FunctionArg {
163:    Range { sheet: SheetId, range: Range, /* ... existing RangeAwareFn arg shape ... */ },
164:    Array(ArrayValue),  // new — array values from `Expr::Array` or another function call
173:pub enum FunctionReturn {
175:    Array(ArrayValue),  // new — for SEQUENCE / FILTER / TRANSPOSE
178:pub type FunctionFn = fn(args: &[FunctionArg], ctx: &FunctionContext) -> FunctionReturn;
182:- Existing `ScalarFn` / `RangeAwareFn` / `ContextAwareFn` registrations re-route to the unified `FunctionFn` via shim adapters. Adapters live in `ql-functions` itself; downstream callers stay unchanged in v1 by exposing the legacy `register_scalar` / `register_range_aware` / `register_context_aware` helpers that internally build the unified `FunctionFn`.
189:let arg_values: Vec<FunctionArg> = ...;  // materialize from ExprPlan args
190:let ret: FunctionReturn = (function.f)(&arg_values, &ctx);
192:    FunctionReturn::Scalar(v) => EvalResult::Scalar(v),
193:    FunctionReturn::Array(a)  => EvalResult::Array(a),
420:| 1 | **4.7.A** (W5-95) | `ArrayValue` in `ql-types` + module wiring | Self + Sonnet |
426:| 7 | **4.7.G** (W5-101) | Evaluator: `EvalResult::Array(ArrayValue)`; array-in-scalar-context → `#CALC!`; array-in-aggregate-context feeds cells | Self + Sonnet |
501:- `array` (required, FunctionArg::Array or ::Range — both 2D shapes).
523:- `array` (required, FunctionArg::Array or ::Range).
600:This revision incorporates all 5 HIGH + 4 MEDIUM + 3 LOW Codex findings. Doc-only ship as W5-94. Sub-phase 4.7.A (W5-95) — `ArrayValue` in `ql-types` — opens the implementation arc.

codex
Independent Codex pull-up review for W5-96 / Phase 4.7.B.

Commit scope confirmed:

`afbaebce223962470b581bf171b469159762ae15 W5-96 / Engine Phase 4.7.B — Unified FunctionFn ABI in ql-functions`

`git show --stat` confirms only:

- `crates/ql-functions/src/lib.rs`
- `crates/ql-functions/src/registry.rs`

The `ArrayValue` dependency is from W5-95, not this commit.

**Findings**
No HIGH findings.

MEDIUM: The implementation unifies registry storage, but does not fully implement the design-doc “one FunctionFn ABI” migration. `RegisteredFn` still stores four callable shapes directly: `Scalar(ScalarFn)`, `RangeAware(RangeAwareFn)`, `ContextAware(ContextAwareFn)`, and `Unified(FunctionFn)` at `crates/ql-functions/src/registry.rs:125`. The legacy `register_*` methods store legacy function pointers, not shim adapters to `FunctionFn`, at `crates/ql-functions/src/registry.rs:201`, `:208`, and `:220`. Suggested fix: either implement real adapter-based storage behind a single `FunctionFn`, or back-port the doc language to say W5-96 unified storage/tagged dispatch, while full ABI normalization remains future cleanup.

LOW: Iterator order is not exactly preserved for `names()`, `range_aware_names()`, `context_aware_names()`, and `names_all()`. Pre-W5-96 each tier iterated its own `HashMap`; now each tier filters one combined `HashMap`, so adding entries in other tiers can perturb per-tier iteration order. Citations: `crates/ql-functions/src/registry.rs:301`, `:310`, `:319`, `:339`. Suggested fix: document these iterators as unordered, or return sorted names if tests/callers need stable output.

LOW: Cross-tier duplicate panic text changed from variant-specific “already registered as scalar/range/context-aware” messages to a generic “duplicate registration” via `insert_or_panic`. Citation: `crates/ql-functions/src/registry.rs:183`. Suggested fix: if panic message text is considered test-visible API, restore tier-specific duplicate diagnostics; otherwise this is acceptable.

**Claim Review**
PASS: New public types live in `ql-functions`. `FunctionArg`, `FunctionReturn`, `FunctionContext`, `FunctionFn`, `RegisteredFn`, and `ScalarFn` are defined/re-exported through `registry.rs` and `lib.rs`.

PASS: `ArrayValue` comes from `ql-types` and avoids a dependency cycle. `registry.rs` imports `ql_types::{ArrayValue, EvalContext, Value}` at `crates/ql-functions/src/registry.rs:32`; `ql-types` re-exports `ArrayValue` at `crates/ql-types/src/lib.rs:24`.

PASS with MEDIUM caveat above: Internal storage is one `HashMap<&'static str, RegisteredFn>`, and legacy lookup methods are filter views. Storage is at `crates/ql-functions/src/registry.rs:153`; filtered lookups are at `:244`, `:256`, `:268`, and `:280`.

PASS: Legacy default registration call sites are unchanged in shape. `default_registry()` still uses `r.register`, `r.register_range_aware`, and `r.register_context_aware`; no array/unified functions are registered there yet.

PASS: New `register_unified`, `lookup_unified`, `lookup_any`, and `unified_names` exist. Citations: `crates/ql-functions/src/registry.rs:232`, `:280`, `:293`, `:329`.

PASS: Eval site is not migrated. `ql-exec/src/scalar.rs` still dispatches `lookup_range_aware` → `lookup_context_aware` → `lookup`, at `crates/ql-exec/src/scalar.rs:146`, `:175`, and `:182`.

NOT VERIFIED: I did not rerun the full workspace test suite because this environment is read-only and `cargo test` would need to write build artifacts. The code does contain the claimed unified-path tests around `crates/ql-functions/src/registry.rs:917`.

**Specific Scrutiny**
1. PASS: `FunctionArg::Range` carries the same shape as legacy `FnArg::Range`: `{ values: Vec<Value>, rows: usize, cols: usize }`. Compare `crates/ql-functions/src/registry.rs:68` with `crates/ql-functions/src/range_aware_fns.rs:88`.

2. PASS: `FunctionContext<'a>` is flexible enough for additive fields without changing the `FunctionFn` alias. It currently carries `pub eval_ctx: &'a EvalContext` at `crates/ql-functions/src/registry.rs:100`; adding fields to the same struct preserves the fn-pointer type.

3. PASS with LOW caveats: Lookup/register behavior is structurally preserved, but iterator order and duplicate panic text changed as noted above.

4. PASS: `lookup_any() -> Option<&RegisteredFn>` is usable from eval dispatch. Pattern matching on `&RegisteredFn` can borrow/copy the inner fn pointer; the variant payloads are plain fn pointers at `crates/ql-functions/src/registry.rs:127`, `:130`, `:133`, and `:136`.

5. PASS: I found no code outside `registry.rs` pattern-matching or constructing the old three-HashMap internals. The fields were private, and external code uses the public lookup/name APIs.

6. PASS: No obvious dead code introduced. Legacy `ScalarFn`, `RangeAwareFn`, `ContextAwareFn`, and `FnArg` remain live because `ql-exec/src/scalar.rs` still uses legacy dispatch.

7. FLAG LOW: Design doc §6.2 and §14.4 overstate the implementation. The doc says legacy registrations re-route via shim adapters to one `FunctionFn` ABI and mentions a workbook field in `FunctionContext`; implementation uses tagged legacy fn pointers and only `eval_ctx`. Suggested fix: back-port W5-96’s actual design: “one storage map + `RegisteredFn` tagged dispatch now; full adapter normalization optional later.”
tokens used
89,160
Independent Codex pull-up review for W5-96 / Phase 4.7.B.

Commit scope confirmed:

`afbaebce223962470b581bf171b469159762ae15 W5-96 / Engine Phase 4.7.B — Unified FunctionFn ABI in ql-functions`

`git show --stat` confirms only:

- `crates/ql-functions/src/lib.rs`
- `crates/ql-functions/src/registry.rs`

The `ArrayValue` dependency is from W5-95, not this commit.

**Findings**
No HIGH findings.

MEDIUM: The implementation unifies registry storage, but does not fully implement the design-doc “one FunctionFn ABI” migration. `RegisteredFn` still stores four callable shapes directly: `Scalar(ScalarFn)`, `RangeAware(RangeAwareFn)`, `ContextAware(ContextAwareFn)`, and `Unified(FunctionFn)` at `crates/ql-functions/src/registry.rs:125`. The legacy `register_*` methods store legacy function pointers, not shim adapters to `FunctionFn`, at `crates/ql-functions/src/registry.rs:201`, `:208`, and `:220`. Suggested fix: either implement real adapter-based storage behind a single `FunctionFn`, or back-port the doc language to say W5-96 unified storage/tagged dispatch, while full ABI normalization remains future cleanup.

LOW: Iterator order is not exactly preserved for `names()`, `range_aware_names()`, `context_aware_names()`, and `names_all()`. Pre-W5-96 each tier iterated its own `HashMap`; now each tier filters one combined `HashMap`, so adding entries in other tiers can perturb per-tier iteration order. Citations: `crates/ql-functions/src/registry.rs:301`, `:310`, `:319`, `:339`. Suggested fix: document these iterators as unordered, or return sorted names if tests/callers need stable output.

LOW: Cross-tier duplicate panic text changed from variant-specific “already registered as scalar/range/context-aware” messages to a generic “duplicate registration” via `insert_or_panic`. Citation: `crates/ql-functions/src/registry.rs:183`. Suggested fix: if panic message text is considered test-visible API, restore tier-specific duplicate diagnostics; otherwise this is acceptable.

**Claim Review**
PASS: New public types live in `ql-functions`. `FunctionArg`, `FunctionReturn`, `FunctionContext`, `FunctionFn`, `RegisteredFn`, and `ScalarFn` are defined/re-exported through `registry.rs` and `lib.rs`.

PASS: `ArrayValue` comes from `ql-types` and avoids a dependency cycle. `registry.rs` imports `ql_types::{ArrayValue, EvalContext, Value}` at `crates/ql-functions/src/registry.rs:32`; `ql-types` re-exports `ArrayValue` at `crates/ql-types/src/lib.rs:24`.

PASS with MEDIUM caveat above: Internal storage is one `HashMap<&'static str, RegisteredFn>`, and legacy lookup methods are filter views. Storage is at `crates/ql-functions/src/registry.rs:153`; filtered lookups are at `:244`, `:256`, `:268`, and `:280`.

PASS: Legacy default registration call sites are unchanged in shape. `default_registry()` still uses `r.register`, `r.register_range_aware`, and `r.register_context_aware`; no array/unified functions are registered there yet.

PASS: New `register_unified`, `lookup_unified`, `lookup_any`, and `unified_names` exist. Citations: `crates/ql-functions/src/registry.rs:232`, `:280`, `:293`, `:329`.

PASS: Eval site is not migrated. `ql-exec/src/scalar.rs` still dispatches `lookup_range_aware` → `lookup_context_aware` → `lookup`, at `crates/ql-exec/src/scalar.rs:146`, `:175`, and `:182`.

NOT VERIFIED: I did not rerun the full workspace test suite because this environment is read-only and `cargo test` would need to write build artifacts. The code does contain the claimed unified-path tests around `crates/ql-functions/src/registry.rs:917`.

**Specific Scrutiny**
1. PASS: `FunctionArg::Range` carries the same shape as legacy `FnArg::Range`: `{ values: Vec<Value>, rows: usize, cols: usize }`. Compare `crates/ql-functions/src/registry.rs:68` with `crates/ql-functions/src/range_aware_fns.rs:88`.

2. PASS: `FunctionContext<'a>` is flexible enough for additive fields without changing the `FunctionFn` alias. It currently carries `pub eval_ctx: &'a EvalContext` at `crates/ql-functions/src/registry.rs:100`; adding fields to the same struct preserves the fn-pointer type.

3. PASS with LOW caveats: Lookup/register behavior is structurally preserved, but iterator order and duplicate panic text changed as noted above.

4. PASS: `lookup_any() -> Option<&RegisteredFn>` is usable from eval dispatch. Pattern matching on `&RegisteredFn` can borrow/copy the inner fn pointer; the variant payloads are plain fn pointers at `crates/ql-functions/src/registry.rs:127`, `:130`, `:133`, and `:136`.

5. PASS: I found no code outside `registry.rs` pattern-matching or constructing the old three-HashMap internals. The fields were private, and external code uses the public lookup/name APIs.

6. PASS: No obvious dead code introduced. Legacy `ScalarFn`, `RangeAwareFn`, `ContextAwareFn`, and `FnArg` remain live because `ql-exec/src/scalar.rs` still uses legacy dispatch.

7. FLAG LOW: Design doc §6.2 and §14.4 overstate the implementation. The doc says legacy registrations re-route via shim adapters to one `FunctionFn` ABI and mentions a workbook field in `FunctionContext`; implementation uses tagged legacy fn pointers and only `eval_ctx`. Suggested fix: back-port W5-96’s actual design: “one storage map + `RegisteredFn` tagged dispatch now; full adapter normalization optional later.”
