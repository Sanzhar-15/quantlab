Mega-audit of the 2026-05-26 session — LANE CODEX: empirical verification of the ONLY shipped code change (B#1) + adversarial gap-hunt. Audit-only; report findings; do NOT change source.

CONTEXT
A session just: (1) executed the 5.8 Phase 5 megaudit → verdict PASS-WITH-FINDINGS → marked MASTER-PLAN Phase 5 COMPLETE + amended Exit-Criterion #2 (collaborative table-merge deferred as UNREACHABLE) + Op::Unknown spec; (2) shipped ONE engine code fix (B#1: export_snapshot tombstone leak); (3) locked the Phase 6 plan (docs/phase6/decision-lock.md). You audit whether the session's work is SOUND, empirically. The full session diff is `git diff dfa3d113c90..302c7243ddc`. The ONLY engine SOURCE change is commit `ff09a5e17a7` (crates/ql-collab/src/session.rs + crates/ql-bindings-node/src/lib.rs); everything else is docs.

REPO: this engine repo. Confirm `git log --oneline -1` (expect 302c7243ddc).
ENV GOTCHAS (verified this session): `rg` is NOT installed (use grep); `cargo` is at $HOME/.cargo/bin/cargo (NOT on PATH); the `ql-bindings-node` lib-TEST cannot link standalone (undefined napi runtime symbols _napi_*) — so verify the napi behavior via the ql-collab CORE layer or by reading, NOT `cargo test -p ql-bindings-node`.

CHECKLIST (build + run probes where useful, under crates/ql-collab/tests or a scratch target; clean up after):

C1. B#1 CORRECTNESS. `git show ff09a5e17a7`. The fix added `CollabSession::is_sheet_removed_in_cache(sheet)` (returns `removed_sheets.contains`) and changed `export_snapshot` to return EMPTY entries when that's true (mirroring workbook_snapshot's is_sheet_removed skip), keeping `snapshot_cells` tombstone-AGNOSTIC. Empirically verify (ql-collab probe): addSheet + PutValue(sheet 7) + RemoveSheet(7) ⇒ is_sheet_removed_in_cache(7)==true AND snapshot_cells(7) STILL returns the cell (R-V3.6-19 preserve); RestoreSheet(7) ⇒ ==false AND cell resurfaces. Run `$HOME/.cargo/bin/cargo test -p ql-collab` — confirm 255 pass / 0 fail. Assess: is the new test meaningful, or does it tautologically pass? Is the fix's logic correct + minimal?

C2. ★★ LEAK-CONSUMER GAP HUNT (the most important check). The 5.8 megaudit Lane B flagged the R-V3.6-19 closure (RemoveSheet stopped pruning the cache) missed "export_snapshot AND THE RAW snapshot_cells ACCESSOR." The B#1 fix touched ONLY export_snapshot. Find EVERY consumer of `snapshot_cells` (grep both crates) and any OTHER CollabSession accessor that exposes cached cells WITHOUT an is_sheet_removed filter. For each NON-TEST consumer (napi methods + internal callers), determine: does it filter tombstoned sheets? If ANY consumer still returns pre-tombstone cells for a removed sheet, that is a HIGH gap the B#1 fix did NOT close (Lane B's "raw snapshot_cells accessor" may still leak). Enumerate: export_snapshot (fixed?), workbook_snapshot (filters?), workbook_snapshot_delta (filters?), list_sheets_from_cache (filters?), and ANY napi method that surfaces snapshot_cells output raw. Report each path's status.

C3. TABLE-UNREACHABILITY RE-VERIFY (the verdict hinges on this). The PASS verdict treats Lane A's table-convergence HIGHs (concurrent CreateTable/RenameTable/RenameColumn abort rebuild_workbook) as UNREACHABLE because "no collaborative table producer exists." Independently re-verify by grep/empirics: is there ANY path by which a real producer emits Op::CreateTable/RenameTable/RenameColumn into a COLLABORATIVE (mergeable CollabSession) op log? Check: napi methods (crates/ql-bindings-node/src/lib.rs); public CollabSession pub fns; BatchCommit composition; any wiring of ql-exec WorkbookRuntime table producers INTO CollabSession. If you find a reachable path, the PASS verdict is WRONG (reachable HIGH). If none, confirm.

C4. PHASE-5-COMPLETE / NO-REGRESSION. Build clean + run the full Phase 5 suites at HEAD: ql-collab, ql-oplog, ql-storage, ql-collab-ws (use $HOME/.cargo/bin/cargo). Any regression from the B#1 change? Report counts.

OUTPUT: write to docs/audits/2026-05-26-session-megaudit/lane-codex.md. Per finding: #, Finding (one line), Severity (HIGH/MED/LOW/INFO), Files (path:line), Evidence (probe result OR cited code), Recommendation. End with VERDICT (is the session's shipped work SOUND? any HIGH gap — especially C2?) + coverage note (which of C1–C4 completed; any you could not and why).