# Codex audit — Reference-tier Step 4 (FORMULATEXT — CLOSES mini-phase)

You are auditing the **Step 4 implementation commit** of the reference-tier mini-phase (RT-V1-01). Step 4 registers FORMULATEXT — the **text batch** — and CLOSES the mini-phase. FORMULATEXT uses `ArgContract::Eager` + queries the workbook via `ReferenceQuery::formula_text_at`, which prepends `=` to the canonical stored formula text per Excel canon.

Per the 2026-05-17 audit-discipline rule, parallel Codex + Opus audit fires here. This is the FINAL implementation audit cycle before the mini-phase ships.

## Repository state

- Branch: `feat/quantbook-engine`
- HEAD post-Step-3.1: `43615dd7cd1`
- Working tree contains Step 4 changes (uncommitted) — read files from disk.
- Pre-Step-4: 3235 tests. Post-Step-4: 3260 (+25).
- Gates: cargo test green, clippy clean, fmt clean.

## Reading list

1. **Plan:** `.plans/_active.md` Phase 4.
2. **Design:** `docs/architecture/2026-05-17-reference-tier-design.md` § 2.5 + § 5.6 (`formulatext` pseudo-code).
3. **Cumulative summary:** `docs/audits/2026-05-17-reference-tier-audit-summary.md`.
4. **Prior audit transcripts** (context only): `2026-05-17-rt-step-{1,2,3}-{codex,opus}.md`.
5. **Audit-discipline rule:** `~/.claude/projects/-Users-sanzhar-Documents-Sanzhar-Sanzhar-quantlab/memory/quantbook_engine_audit_discipline.md`.

## Files in Step 4 scope

- `crates/ql-functions/src/reference_fns.rs` — added `formulatext` impl + 9 unit tests.
- `crates/ql-functions/src/registry.rs` — registered FORMULATEXT (`Eager`); count bump 199 → 200.
- `crates/ql-exec/src/plan.rs` — invariant test extended to all 7 names; FORMULATEXT-pending sentinel removed (mini-phase complete).
- **NEW:** `crates/ql-exec/tests/reference_fns_step4_e2e.rs` — 14 e2e tests including formula-cell happy path with leading-`=`, cross-sheet, named-cell, 1×1 range, cell-with-error-value not-propagated.
- `crates/ql-functions/tests/coverage.rs` — 1 new EXPLICITLY_DEFERRED entry.
- `docs/compat/excel-matrix.md` — FORMULATEXT row added.

## IronCalc + Microsoft refs

- `.references/ironcalc/base/src/functions/lookup_and_reference/mod.rs:850` — `fn_formulatext`.

## What to look for

### Correctness

1. **Leading `=` prepending:** the workbook stores canonical text WITHOUT `=`. `ReferenceQuery::formula_text_at` on `WorkbookEnv` (env.rs) prepends `=`. Per-fn impl receives the string ready to wrap in `Value::Text`. Verify: tests confirm leading `=`? Is there a path where `=` gets prepended twice?
2. **Microsoft canon for non-reference args:** `#N/A` (vs IronCalc's `#ERROR!`). Verify impl matches design § 2.5.
3. **1×1 range:** same single-cell treatment as ISFORMULA. Verify test `formulatext_of_1x1_range_pointing_at_formula_returns_text`.
4. **Multi-cell range:** `#N/A`. Verify.
5. **Cell-with-error-value not-propagated:** S3-HIGH-1 lesson. Test `formulatext_of_cell_with_error_value_returns_na_not_propagated_error` exists. Verify the materializer's CellRef arm (post-S3-HIGH-1 closure) emits `RefArg::Reference { address }` WITHOUT reading the cell value, so the cell's error doesn't leak into FORMULATEXT's return.
6. **Cross-sheet:** test `formulatext_cross_sheet_returns_text`. Verify.
7. **Named-cell:** test `formulatext_of_named_cell_pointing_at_formula_returns_text`. Verify the NameRef → AggregateNameRef → Range single-cell path.
8. **Error propagation:** `FORMULATEXT(#REF!)` → `#REF!`. Verify.
9. **`FORMULATEXT(SUM(A1:A3))` bind-fail:** S2-HIGH-3/S3-HIGH-4 propagation. Test `formulatext_of_sum_literal_range_bind_fails_v1_scope` pins this.

### Walker / deps

10. **FORMULATEXT dep-tracking:** NOT in `is_address_only_reference_fn`, NOT special-cased for LazyShape. Normal walker recurses into args; `CellRef` arg → value-dep on cell; `RangeRef` 1×1 arg → cell-dep (S3-HIGH-3 closure). v1 cost: value-deps over-recompute. Verify by tracing.
11. **`=FORMULATEXT(A1)` dirtying on formula-status change:** the value-dep on A1 dirties this formula when A1's value changes. Formula-status changes (set/clear) DO change the value (`formula_at` flips), so the value-dep correctly captures it. ✓
12. **`=FORMULATEXT(A1)` dirtying on formula-TEXT change:** rewriting A1's formula text changes the value too (eval result might differ). value-dep captures. ✓

### Registry + invariant tests

13. **`step_2_reference_aware_names_registered_in_reference_tier`** now covers all 7 names. The FORMULATEXT-pending sentinel was removed. Verify by reading.
14. **Count bump:** `default_registry_has_expected_count` 199 → 200. Verify comment line for W5-RT-4.

### Tests

15. **Per-fn count:** ~9 unit + ~14 e2e for FORMULATEXT. Per design § 7 MEDIUM-δ: 8+ per fn. ✓
16. **Leading-`=` verification test:** `formulatext_of_formula_cell_returns_text_with_leading_eq` exists. Verify.
17. **Storage-level vs runtime-level path:** all e2e tests use `Workbook::put_formula` (storage-level). The producer/replay divergence pinned for ISFORMULA (S3-HIGH-5) ALSO applies to FORMULATEXT — verify that's still a documented v1 issue and add a pin if missing.
18. **LibreOffice cross-checks:** the design § 7 enumeration lists `FORMULATEXT(A1)` where A1=`=1+2` → `"=1+2"`. Test covers. ✓

### Cross-cutting

19. **Plan-cache:** `=FORMULATEXT(A1)` plans are cell-INDEPENDENT (no `@`). Verify.
20. **Op-log replay:** does `=FORMULATEXT(A1)` evaluate correctly under replay? Uses `WorkbookEnv::with_formula_cell` which has `ReferenceQuery` impl. Verify.
21. **Matrix `excel-matrix.md`:** FORMULATEXT row added. Test count `9+14 e2e`. Verify accurate.
22. **Coverage matrix `EXPLICITLY_DEFERRED`:** FORMULATEXT entry accurate?

### Documentation

23. **Doc-comments vs impl:** drift?
24. **Design § 2.5 + § 5.6 vs impl:** matches?
25. **Module header in `reference_fns.rs`:** updated post-Step-3 to cover all 3 batches; verify Step 4 inclusion.

## Output format

Findings as HIGH (likely incorrect output) / MEDIUM (works but deviates / maintainability) / LOW (style / doc / nit). Each finding:
- ID
- Subject
- Where (file:line)
- Detail
- Recommendation
- Severity rationale

Be **critical**. List every concern. Don't stop at 2-3.

Write findings to `docs/audits/2026-05-17-rt-step-4-codex.md`.
