# Opus audit — Phase 5.4 V2 V1 (undo grouping)

Auditor: Opus subagent.
HEAD audited: `6138a7203f6`.
Total: 86,361 tokens / 58 tool uses / 583s.

## HIGH

**H1 — BLOCKING DEFECT: session.rs truncated mid-string at line 1436.**
The committed blob does NOT compile. Same git-index-padding-race
that struck `3590aa6d89e` (Phase 5.5 V1) earlier this session.
Commit message claims "4213 workspace tests" but the test binary
can't link. ✅ **CLOSED in this audit cycle** — restored the 4
missing trailing lines via Edit.

## MEDIUM

**M1 — `start_undo_group` docstring misrepresents nesting.** Says
"Loro-defined; treat as undefined." Reality: Loro 1.12.0
deterministically returns `Err(UndoGroupAlreadyStarted)`. ✅
Docstring rewritten + test pinned.

**M2 — `&mut self` on the 3 new methods.** Opus argued for `&self`
since Loro uses interior mutability. **KEPT as `&mut self`** — Loro's
PUBLIC wrapper (`pub fn group_start(&mut self) -> LoroResult<()>`)
takes `&mut self`; matching it is correct. `clear_undo_stack(&self)`
works because Loro's `UndoManager::clear(&self)` is `&self`. The
mixed-borrow surface mirrors Loro's wrapper exactly.

**M3 — Panic-mid-group leaks open group state; no RAII guard.**
Real footgun for IDE paste/fill-down callers. Documented in
docstring as a "caller pitfall" with explicit warning. V2 V1.1
will add the closure helper.

**M4 — Doc drift in 5 surfaces.** ✅ Closed:
- `session.rs:7-11` scaffold-overview: V2 V1 + commit hash added.
- `undo.rs:18-27` module docstring: V1 + V2 V1 surface listed.
- `undo.rs:40-44` pre-existing drift ("Undo creates INVERSE OPS"
  → "retracts originals from visible list") fixed.
- `lib.rs:25-30` module-layout entry for `undo`: V2 V1 mention.
- `crdt-data-model.md` § Undo/redo: V1 wired surface → "V1 + V2 V1
  wired surface" with 10 methods listed; V1 limitations → V1 / V2 V1
  limitations / V2 V2 follow-ups.

**M5 — `set_undo_merge_interval` negative-value behavior undocumented.**
✅ Docstring now documents: "Negative values behave as 0 (Loro
stores raw i64; compare `now - last_undo_time < interval_ms` is
false for non-negative elapsed time vs negative threshold)."

## LOW

- L1 missing tests (nested, empty, merge-interval functional): ✅
  added `nested_start_undo_group_returns_already_started_error` +
  `empty_undo_group_pushes_no_unit`. Merge-interval functional
  test deferred (needs time-mocking; flagged in docstring).
- L2 missing `redo_count() == 0` assertion: ✅ added.
- L3 group-leak on `?` propagation: KEPT — documented as caller
  pitfall in `start_undo_group` docstring; explicit RAII helper
  V2 V1.1.
- L4 entry-plan row missing commit hash: V2 V1 ship hash
  `6138a7203f6` + closure hash referenced in commit.
- L5 pre-existing undo.rs:40-44 drift: ✅ closed.

## Forward implications (Phase 5.7)

Opus correctly flagged: IDE paste/fill-down/table-import will hit
the panic-mid-group footgun. V2 V1.1 RAII helper is the right next
follow-up. V2 V1 is mechanically sufficient for happy-path testing.

## Verdict

**SHIPPED post-closure: 1 HIGH (truncation) + 5 MEDIUM (M1-M5,
M2 deferred) + 5 LOW (L1, L2, L5 closed; L3, L4 deferred or
addressed).** The HIGH was the critical catch; without Opus's
clean-checkout verification this would have shipped a broken
crate.
