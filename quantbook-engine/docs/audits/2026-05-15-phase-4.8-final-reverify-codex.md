OpenAI Codex v0.130.0
--------
workdir: /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR, /Users/sanzhar/.codex/memories]
reasoning effort: high
reasoning summaries: none
session id: 019e2b1e-fe12-7c01-b87b-5c96cfd16745
--------
user
# Phase 4.8 final re-verify

Your earlier closure-verify audit (transcript at
`docs/audits/2026-05-15-phase-4.8-closure-verify-codex.md`)
returned NOT SHIP-READY with one finding: MEDIUM-1 STILL OPEN —
the loader was inserting under raw `entry.name` instead of
`canonical_upper`, so a mixed-case TOML entry would silently
break `TableTable::lookup`.

This prompt asks for a focused re-verify on the two follow-up
fix commits AND nothing else. Be bounded — confirm or deny the
specific claims below, don't re-audit the whole Phase 4.8 arc.

## Fix commits since your last closure-verify

```
1a69c96cc38  W5-128 / Phase 4.8.O.4  canonical-key bug + Sonnet MEDIUM-NEW-1 comment fix
6717ae7fe9f  W5-129 / Phase 4.8.O.5  rewrite_column_ref unit tests (Sonnet LOW-NEW-1)
```

HEAD is now `6717ae7fe9f`. 2414 workspace tests, all 7 gates
green.

## Specific verifications

### W5-128 (your MEDIUM-1 second-pass)

Read `crates/ql-io/src/qbook_format.rs:1814` (the line that
previously did `Arc::from(entry.name.as_str())`). Confirm:

1. The `insert` key now uses `canonical_upper` (the uppercase
   form computed earlier in the loop), NOT raw `entry.name`.
2. The accompanying `meta.name` field is the same `canonical`
   Arc, so `TableMetadata::name` carries the uppercase canonical
   per the design contract.
3. `meta.display_name` still comes from `entry.display_name`
   (case-preserving — unchanged).
4. A new test `load_normalizes_mixed_case_table_name` pins that
   `entry.name = "Sales"` loads and is findable via
   `lookup_table("Sales")` AND `meta.name == "SALES"`.
5. A new test
   `malformed_table_rejected_when_mixed_case_duplicate_canonical_name`
   exercises two entries `"Sales"` + `"SALES"` and verifies the
   duplicate-name rejection fires (it would have silently
   coexisted pre-fix).

### W5-128 (Sonnet MEDIUM-NEW-1 comment fix)

Read the loader's NameTable-collision block (around qbook_format.rs:
1762). Confirm:

1. The comment no longer claims "sheet-scoped collisions are
   caught by the canonical-name comparison via `lookup_ci`."
2. The new comment accurately states sheet-scoped collisions are
   intentionally NOT checked — consistent with
   `WorkbookRuntime::create_table`.

### W5-129 (Sonnet LOW-NEW-1 — unit tests)

Read the new test block in `crates/ql-formula-syntax/src/ast.rs`
at the tail of `mod tests`. Confirm:

1. Five tests exist, named `rewrite_column_ref_*`.
2. Each test targets a distinct code path:
   - String literal pass-through (`Expr::String("The Qty column")`).
   - `TableSpecItem::Special` pass-through inside Combination.
   - `TableSpecSubtree::ThisRowColumnRange` rewriting both endpoints.
   - `TableSpecItem::ColumnRange` rewriting only the matching
     endpoint.
   - Cross-table isolation (`Inventory[Qty]` untouched when
     renaming `Sales.Qty`).
3. Each test asserts the SPECIFIC expected shape post-rewrite,
   not just "doesn't crash."

## Independent quality probes (small, NOT a full audit)

After the verifications above:

A. Does the new W5-128 canonical-key fix interact correctly with
   the duplicate-name check at line 1756? Specifically: the
   first entry's `canonical_upper` is computed and used for
   lookup, then for insert. The second entry's `canonical_upper`
   is computed independently. Walk through: same canonical →
   second `lookup` finds the first → duplicate-name error
   fires. Correct?

B. Does `meta.name` being clone-of-`canonical` (an Arc<str> from
   the local `canonical_upper` String) hold any lifetime / ownership
   concerns? Read carefully — `canonical_upper` is a local
   `String`, but `Arc::from(canonical_upper.as_str())` creates a
   fresh allocation. `meta.name = Arc::clone(&canonical)`. Then
   `tables.insert(canonical, meta)`. The HashMap key and `meta.name`
   point to the SAME allocation. Correct?

## Output

For each verification: "CLOSED" / "STILL OPEN" with one-sentence
justification + file:line cite.

For probes A/B: "OK" or "ISSUE FOUND" with description.

End with one-line verdict: **SHIP-READY** or **NOT SHIP-READY**.

## Hard rules

- No fallbacks.
- No schema-version range changes.
- No test count reduction.

codex
I’ll keep this scoped to the two commits and the named files/tests. I’m reading the relevant code and the prior audit context only enough to verify the specific claims.
exec
/bin/zsh -lc "nl -ba crates/ql-io/src/qbook_format.rs | sed -n '1740,1845p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1740	            }
  1741	            // **W5-126 (Phase 4.8.O.2 — Codex MEDIUM-1):** re-validate
  1742	            // the runtime registry invariants at load time. The W5-123
  1743	            // docstring said "loader trusts the file" — Codex flagged
  1744	            // that a hand-edited / corrupted v6 TOML could enter the
  1745	            // engine with state that runtime mutation wouldn't allow.
  1746	            // Concrete failure modes: silent duplicate-name overwrite
  1747	            // (leaks column ids into the allocator); namespace
  1748	            // collision with NameTable; overlapping table footprints
  1749	            // (HashMap-order indeterminism in `Workbook::table_at`);
  1750	            // duplicate or empty column names breaking `lookup_column`.
  1751	            let canonical_upper = entry.name.to_ascii_uppercase();
  1752	            // Name uniqueness against already-loaded tables (catches
  1753	            // duplicate canonical names within the same TablesSection
  1754	            // entries vec, since each insert lands in `wb.tables_mut()`
  1755	            // before the next iteration).
  1756	            if wb.tables().lookup(&canonical_upper).is_some() {
  1757	                return Err(QbookError::MalformedTable {
  1758	                    name: entry.name.clone(),
  1759	                    reason: "duplicate canonical table name in [[tables.entries]]".into(),
  1760	                });
  1761	            }
  1762	            // Shared namespace with NameTable. NameTable hydration ran
  1763	            // above; workbook-scoped conflicts are checked here via
  1764	            // `lookup_ci`. **Sheet-scoped name collisions are
  1765	            // intentionally NOT checked** — `WorkbookRuntime::create_table`
  1766	            // also only checks workbook-scoped names, so loader and
  1767	            // runtime are consistent (design § 13 #3: sheet-scoped
  1768	            // names shadow workbook-scoped within their sheet, and
  1769	            // tables are workbook-scoped, so the binder resolves them
  1770	            // from separate paths). Closing this is a wider design
  1771	            // change that belongs to a future polish wave, not the
  1772	            // loader.
  1773	            if wb.names().lookup_ci(&canonical_upper).is_some() {
  1774	                return Err(QbookError::MalformedTable {
  1775	                    name: entry.name.clone(),
  1776	                    reason: "table name collides with a workbook-scoped defined name".into(),
  1777	                });
  1778	            }
  1779	            // Column-level shape: non-empty `name` AND `display`,
  1780	            // unique canonical (lowercase) names, no `u32::MAX`
  1781	            // column ids (closes Codex MEDIUM-2 — `TableTable::insert`
  1782	            // does unchecked `max_id + 1` on the column-id allocator).
  1783	            {
  1784	                use std::collections::HashSet;
  1785	                let mut seen_canon: HashSet<String> = HashSet::new();
  1786	                for col in &entry.columns {
  1787	                    if col.name.is_empty() || col.display.is_empty() {
  1788	                        return Err(QbookError::MalformedTable {
  1789	                            name: entry.name.clone(),
  1790	                            reason: "table column names cannot be empty".into(),
  1791	                        });
  1792	                    }
  1793	                    if col.id == u32::MAX {
  1794	                        return Err(QbookError::MalformedTable {
  1795	                            name: entry.name.clone(),
  1796	                            reason: "column id cannot be u32::MAX (would overflow allocator)"
  1797	                                .into(),
  1798	                        });
  1799	                    }
  1800	                    if !seen_canon.insert(col.name.to_ascii_lowercase()) {
  1801	                        return Err(QbookError::MalformedTable {
  1802	                            name: entry.name.clone(),
  1803	                            reason: "duplicate canonical column name within table".into(),
  1804	                        });
  1805	                    }
  1806	                }
  1807	            }
  1808	            // Footprint non-overlap against tables already inserted in
  1809	            // earlier loop iterations.
  1810	            for r in entry.top_row..entry.top_row + entry.rows {
  1811	                for c in entry.top_col..entry.top_col + entry.cols {
  1812	                    if wb.table_at(entry.sheet, r, c).is_some() {
  1813	                        return Err(QbookError::MalformedTable {
  1814	                            name: entry.name.clone(),
  1815	                            reason: "table footprint overlaps a previously loaded table".into(),
  1816	                        });
  1817	                    }
  1818	                }
  1819	            }
  1820	            // **W5-128 (Phase 4.8.O.4 — Codex closure-verify MEDIUM-1 second-pass):**
  1821	            // insert under the UPPERCASE canonical, not the raw
  1822	            // `entry.name`. Engine-saved files always have uppercase
  1823	            // `entry.name`, but a hand-edited TOML with mixed-case
  1824	            // (`name = "Sales"`) would otherwise enter the HashMap
  1825	            // under key "Sales" while `TableTable::lookup` uppercases
  1826	            // every query — silently making the table unfindable.
  1827	            // The new pin in `load_normalizes_mixed_case_table_name`
  1828	            // catches the regression at boundary cases the W5-126
  1829	            // tests missed (those used identical uppercase names).
  1830	            let canonical: std::sync::Arc<str> = std::sync::Arc::from(canonical_upper.as_str());
  1831	            let columns: Vec<ql_storage::TableColumn> = entry
  1832	                .columns
  1833	                .into_iter()
  1834	                .map(|c| ql_storage::TableColumn {
  1835	                    id: c.id,
  1836	                    name: std::sync::Arc::from(c.name.as_str()),
  1837	                    display: std::sync::Arc::from(c.display.as_str()),
  1838	                    totals_function: c.totals_function.map(TotalsFunctionWire::to_runtime),
  1839	                })
  1840	                .collect();
  1841	            let meta = ql_storage::TableMetadata {
  1842	                name: std::sync::Arc::clone(&canonical),
  1843	                display_name: std::sync::Arc::from(entry.display_name.as_str()),
  1844	                sheet: entry.sheet,
  1845	                top_row: entry.top_row,

exec
/bin/zsh -lc "rg -n \"load_normalizes_mixed_case_table_name|malformed_table_rejected_when_mixed_case_duplicate_canonical_name|lookup_table\\(\\\"Sales\\\"\\)|meta\\.name ==|display_name\" crates/ql-io/src/qbook_format.rs" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
421:    pub display_name: String,
1231:                display_name: meta.display_name.as_ref().to_owned(),
1827:            // The new pin in `load_normalizes_mixed_case_table_name`
1843:                display_name: std::sync::Arc::from(entry.display_name.as_str()),
3716:        let display_name: Arc<str> = Arc::from(table_name);
3719:            display_name,
3738:        let pre_meta = wb.lookup_table("Sales").unwrap().clone();
3742:        let post = loaded.lookup_table("Sales").expect("table reloaded");
3744:        assert_eq!(post.display_name, pre_meta.display_name);
3776:                    display_name: Arc::from(*name),
3795:        assert!(loaded.lookup_table("Sales").is_some());
3797:        assert_eq!(loaded.lookup_table("Sales").unwrap().top_row, 0);
3835:                display_name: Arc::from("AllTotals"),
3918:display_name = "Phantom"
3956:display_name = "Sales"
3994:display_name = "Empty"
4031:display_name = "Far"
4070:display_name = "Wide"
4121:display_name = "Sales"
4137:display_name = "Sales"
4181:display_name = "Rate"
4220:display_name = "A"
4241:display_name = "B"
4284:display_name = "T"
4323:display_name = "T"
4369:display_name = "T"
4410:display_name = "A"
4426:display_name = "B"
4457:    fn load_normalizes_mixed_case_table_name() {
4473:display_name = "Sales"
4493:            .lookup_table("Sales")
4497:        // `meta.display_name` preserves the case-preserving form
4498:        // straight from the on-disk `display_name`.
4499:        assert_eq!(meta.display_name.as_ref(), "Sales");
4510:    fn malformed_table_rejected_when_mixed_case_duplicate_canonical_name() {
4524:display_name = "Sales"
4540:display_name = "Sales"

exec
/bin/zsh -lc 'rg -n "rewrite_column_ref_" crates/ql-formula-syntax/src/ast.rs && nl -ba crates/ql-formula-syntax/src/ast.rs | tail -n 260' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
957:    fn rewrite_column_ref_string_literal_is_immune() {
971:    fn rewrite_column_ref_special_item_passes_through() {
1000:    fn rewrite_column_ref_this_row_column_range_rewrites_both_endpoints() {
1020:    fn rewrite_column_ref_column_range_rewrites_only_matching_endpoint() {
1045:    fn rewrite_column_ref_cross_table_isolation_holds() {
   808	        let b = Expr::Number(1.5);
   809	        assert_eq!(a, b);
   810	        let c = Expr::Number(2.5);
   811	        assert_ne!(a, c);
   812	    }
   813	
   814	    // W5-91 (Phase 4.6.C) — rewrite_sheet_name_in_expr coverage.
   815	
   816	    fn cell(sheet: SheetRef, col: u32, row: u32) -> Expr {
   817	        Expr::CellRef(CellAddr {
   818	            sheet,
   819	            col,
   820	            row,
   821	            abs_col: false,
   822	            abs_row: false,
   823	        })
   824	    }
   825	
   826	    #[test]
   827	    fn rewrite_renames_matching_cellref_sheet_name() {
   828	        let expr = cell(SheetRef::Name(Arc::from("OldSheet")), 0, 0);
   829	        let new_name: Arc<str> = Arc::from("NewSheet");
   830	        let out = rewrite_sheet_name_in_expr(&expr, "OLDSHEET", &new_name);
   831	        match out {
   832	            Expr::CellRef(addr) => match addr.sheet {
   833	                SheetRef::Name(n) => assert_eq!(&*n, "NewSheet"),
   834	                other => panic!("expected SheetRef::Name, got {other:?}"),
   835	            },
   836	            other => panic!("expected CellRef, got {other:?}"),
   837	        }
   838	    }
   839	
   840	    #[test]
   841	    fn rewrite_skips_current_and_id_sheet_refs() {
   842	        // SheetRef::Current passes through untouched.
   843	        let expr = cell(SheetRef::Current, 0, 0);
   844	        let new_name: Arc<str> = Arc::from("New");
   845	        let out = rewrite_sheet_name_in_expr(&expr, "OLD", &new_name);
   846	        assert_eq!(out, expr);
   847	
   848	        // SheetRef::Id passes through untouched (rename hits AST by name,
   849	        // not by id — Id-bound refs survive across renames by design).
   850	        let expr_id = cell(SheetRef::Id(7), 0, 0);
   851	        let out_id = rewrite_sheet_name_in_expr(&expr_id, "OLD", &new_name);
   852	        assert_eq!(out_id, expr_id);
   853	    }
   854	
   855	    #[test]
   856	    fn rewrite_non_matching_name_passes_through() {
   857	        let expr = cell(SheetRef::Name(Arc::from("Other")), 0, 0);
   858	        let new_name: Arc<str> = Arc::from("New");
   859	        let out = rewrite_sheet_name_in_expr(&expr, "OLD", &new_name);
   860	        assert_eq!(out, expr);
   861	    }
   862	
   863	    #[test]
   864	    fn rewrite_recurses_into_binary_and_function() {
   865	        let lhs = cell(SheetRef::Name(Arc::from("Old")), 0, 0);
   866	        let rhs_inner = cell(SheetRef::Name(Arc::from("Old")), 1, 0);
   867	        let expr = Expr::Binary {
   868	            op: crate::token::Operator::Plus,
   869	            lhs: Box::new(lhs),
   870	            rhs: Box::new(Expr::Function {
   871	                name: Arc::from("SUM"),
   872	                args: vec![rhs_inner],
   873	            }),
   874	        };
   875	        let new_name: Arc<str> = Arc::from("Renamed");
   876	        let out = rewrite_sheet_name_in_expr(&expr, "OLD", &new_name);
   877	        match out {
   878	            Expr::Binary { lhs, rhs, .. } => {
   879	                match *lhs {
   880	                    Expr::CellRef(addr) => match addr.sheet {
   881	                        SheetRef::Name(n) => assert_eq!(&*n, "Renamed"),
   882	                        other => panic!("expected SheetRef::Name, got {other:?}"),
   883	                    },
   884	                    other => panic!("expected CellRef in lhs, got {other:?}"),
   885	                }
   886	                match *rhs {
   887	                    Expr::Function { args, .. } => match &args[0] {
   888	                        Expr::CellRef(addr) => match &addr.sheet {
   889	                            SheetRef::Name(n) => assert_eq!(&**n, "Renamed"),
   890	                            other => panic!("expected SheetRef::Name, got {other:?}"),
   891	                        },
   892	                        other => panic!("expected CellRef arg, got {other:?}"),
   893	                    },
   894	                    other => panic!("expected Function in rhs, got {other:?}"),
   895	                }
   896	            }
   897	            other => panic!("expected Binary, got {other:?}"),
   898	        }
   899	    }
   900	
   901	    #[test]
   902	    fn rewrite_recurses_into_rangeref_cells() {
   903	        let rng = Expr::RangeRef(RangeRef::Cells {
   904	            sheet: SheetRef::Name(Arc::from("OldSheet")),
   905	            start_col: 0,
   906	            start_row: 0,
   907	            end_col: 2,
   908	            end_row: 4,
   909	            abs_start_col: false,
   910	            abs_start_row: false,
   911	            abs_end_col: false,
   912	            abs_end_row: false,
   913	        });
   914	        let new_name: Arc<str> = Arc::from("NewSheet");
   915	        let out = rewrite_sheet_name_in_expr(&rng, "OLDSHEET", &new_name);
   916	        match out {
   917	            Expr::RangeRef(RangeRef::Cells { sheet, .. }) => match sheet {
   918	                SheetRef::Name(n) => assert_eq!(&*n, "NewSheet"),
   919	                other => panic!("expected SheetRef::Name, got {other:?}"),
   920	            },
   921	            other => panic!("expected RangeRef::Cells, got {other:?}"),
   922	        }
   923	    }
   924	
   925	    #[test]
   926	    fn rewrite_is_case_insensitive_on_match() {
   927	        // Stored ref is "oldsheet" lower; canonical comparison key is uppercase.
   928	        let expr = cell(SheetRef::Name(Arc::from("oldsheet")), 0, 0);
   929	        let new_name: Arc<str> = Arc::from("NewSheet");
   930	        let out = rewrite_sheet_name_in_expr(&expr, "OLDSHEET", &new_name);
   931	        match out {
   932	            Expr::CellRef(addr) => match addr.sheet {
   933	                SheetRef::Name(n) => assert_eq!(&*n, "NewSheet"),
   934	                other => panic!("expected SheetRef::Name, got {other:?}"),
   935	            },
   936	            other => panic!("expected CellRef, got {other:?}"),
   937	        }
   938	    }
   939	
   940	    // ===== W5-129 (Phase 4.8.O.5 — Sonnet cross-validation LOW-NEW-1) =====
   941	    //
   942	    // `rewrite_column_ref` was previously tested only transitively via
   943	    // the `oplog_e2e` `rename_column_op_log_replay_reconstructs_rename`
   944	    // end-to-end test. Sonnet's W5-128 cross-validation pass confirmed
   945	    // the code is correct but flagged the missing unit-level pins.
   946	    // These 5 tests target each non-Combination spec variant + the
   947	    // string-literal immunity property + the cross-table isolation guard.
   948	
   949	    fn sref(table_name: &str, spec: TableSpecSubtree) -> Expr {
   950	        Expr::StructuredRef {
   951	            table_name: Arc::from(table_name),
   952	            spec,
   953	        }
   954	    }
   955	
   956	    #[test]
   957	    fn rewrite_column_ref_string_literal_is_immune() {
   958	        // `="The Qty column"` — the string literal contains "Qty" but
   959	        // it's `Expr::String`, not a StructuredRef. The walker must
   960	        // pass it through unchanged.
   961	        let expr = Expr::String(Arc::from("The Qty column"));
   962	        let new_col: Arc<str> = Arc::from("Quantity");
   963	        let out = rewrite_column_ref(&expr, "SALES", "Qty", &new_col);
   964	        match out {
   965	            Expr::String(s) => assert_eq!(&*s, "The Qty column"),
   966	            other => panic!("expected String pass-through, got {other:?}"),
   967	        }
   968	    }
   969	
   970	    #[test]
   971	    fn rewrite_column_ref_special_item_passes_through() {
   972	        // `Sales[[#Headers], [Qty]]` — the `#Headers` SpecialItem must
   973	        // pass through; only the `Qty` Column item gets rewritten.
   974	        let spec = TableSpecSubtree::Combination(vec![
   975	            TableSpecItem::Special(SpecialItem::Headers),
   976	            TableSpecItem::Column(Arc::from("Qty")),
   977	        ]);
   978	        let new_col: Arc<str> = Arc::from("Quantity");
   979	        let out = rewrite_column_ref(&sref("Sales", spec), "SALES", "Qty", &new_col);
   980	        match out {
   981	            Expr::StructuredRef { spec, .. } => match spec {
   982	                TableSpecSubtree::Combination(items) => {
   983	                    assert_eq!(items.len(), 2);
   984	                    match &items[0] {
   985	                        TableSpecItem::Special(SpecialItem::Headers) => {}
   986	                        other => panic!("expected Headers, got {other:?}"),
   987	                    }
   988	                    match &items[1] {
   989	                        TableSpecItem::Column(c) => assert_eq!(&**c, "Quantity"),
   990	                        other => panic!("expected Column(Quantity), got {other:?}"),
   991	                    }
   992	                }
   993	                other => panic!("expected Combination, got {other:?}"),
   994	            },
   995	            other => panic!("expected StructuredRef, got {other:?}"),
   996	        }
   997	    }
   998	
   999	    #[test]
  1000	    fn rewrite_column_ref_this_row_column_range_rewrites_both_endpoints() {
  1001	        // `Sales[@[Qty]:[Qty]]` — both endpoints reference "Qty"; the
  1002	        // walker rewrites BOTH. (Cross-test exists in oplog_e2e but
  1003	        // not for this specific spec variant.)
  1004	        let spec = TableSpecSubtree::ThisRowColumnRange(Arc::from("Qty"), Arc::from("Qty"));
  1005	        let new_col: Arc<str> = Arc::from("Quantity");
  1006	        let out = rewrite_column_ref(&sref("Sales", spec), "SALES", "Qty", &new_col);
  1007	        match out {
  1008	            Expr::StructuredRef { spec, .. } => match spec {
  1009	                TableSpecSubtree::ThisRowColumnRange(c1, c2) => {
  1010	                    assert_eq!(&*c1, "Quantity");
  1011	                    assert_eq!(&*c2, "Quantity");
  1012	                }
  1013	                other => panic!("expected ThisRowColumnRange, got {other:?}"),
  1014	            },
  1015	            other => panic!("expected StructuredRef, got {other:?}"),
  1016	        }
  1017	    }
  1018	
  1019	    #[test]
  1020	    fn rewrite_column_ref_column_range_rewrites_only_matching_endpoint() {
  1021	        // `Sales[[Qty]:[Price]]` with rename Qty→Quantity — only c1
  1022	        // (the matching endpoint) gets rewritten. c2 ("Price") stays.
  1023	        let spec = TableSpecSubtree::Combination(vec![TableSpecItem::ColumnRange(
  1024	            Arc::from("Qty"),
  1025	            Arc::from("Price"),
  1026	        )]);
  1027	        let new_col: Arc<str> = Arc::from("Quantity");
  1028	        let out = rewrite_column_ref(&sref("Sales", spec), "SALES", "Qty", &new_col);
  1029	        match out {
  1030	            Expr::StructuredRef { spec, .. } => match spec {
  1031	                TableSpecSubtree::Combination(items) => match &items[0] {
  1032	                    TableSpecItem::ColumnRange(c1, c2) => {
  1033	                        assert_eq!(&**c1, "Quantity");
  1034	                        assert_eq!(&**c2, "Price");
  1035	                    }
  1036	                    other => panic!("expected ColumnRange, got {other:?}"),
  1037	                },
  1038	                other => panic!("expected Combination, got {other:?}"),
  1039	            },
  1040	            other => panic!("expected StructuredRef, got {other:?}"),
  1041	        }
  1042	    }
  1043	
  1044	    #[test]
  1045	    fn rewrite_column_ref_cross_table_isolation_holds() {
  1046	        // `Inventory[Qty]` when renaming `Sales.Qty` — the table
  1047	        // canonical doesn't match, so the spec must pass through
  1048	        // unchanged. This is the guard at `rewrite_column_ref`'s
  1049	        // `table_name.eq_ignore_ascii_case(table_canonical_upper)`
  1050	        // check, exercised at unit level.
  1051	        let spec = TableSpecSubtree::BareColumn(Arc::from("Qty"));
  1052	        let new_col: Arc<str> = Arc::from("Quantity");
  1053	        let out = rewrite_column_ref(&sref("Inventory", spec), "SALES", "Qty", &new_col);
  1054	        match out {
  1055	            Expr::StructuredRef { table_name, spec } => {
  1056	                assert_eq!(&*table_name, "Inventory");
  1057	                match spec {
  1058	                    TableSpecSubtree::BareColumn(c) => {
  1059	                        assert_eq!(&*c, "Qty", "cross-table column must NOT be rewritten")
  1060	                    }
  1061	                    other => panic!("expected BareColumn, got {other:?}"),
  1062	                }
  1063	            }
  1064	            other => panic!("expected StructuredRef, got {other:?}"),
  1065	        }
  1066	    }
  1067	}

exec
/bin/zsh -lc 'git status --short && git rev-parse --short=11 HEAD' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 764ms:
?? ../2026-05-11-222312-this-session-is-being-continued-from-a-previous-c.txt
?? ../2026-05-13-003338-this-session-is-being-continued-from-a-previous-c.txt
?? ../node_modules
?? .codex/
?? .plans/
?? 2026-05-14-222539-this-session-is-being-continued-from-a-previous-c.txt
?? docs/audits/2026-05-14-phase-4.7.H-codex-review.txt
?? docs/audits/2026-05-14-phase-4.7.H-sonnet-review.md
?? docs/audits/2026-05-15-phase-4.8-closing-megaudit-codex.md
?? docs/audits/2026-05-15-phase-4.8-closure-verify-codex.md
?? docs/audits/2026-05-15-phase-4.8-final-reverify-codex.md
6717ae7fe9f

codex
The loader block already shows the key path changed: `canonical_upper` is computed before validation, checked through `lookup`, then converted into one `Arc<str>` used for both metadata and the map insert. I’m checking the exact test bodies now so the final call can cite the pinned assertions rather than just test names.
exec
/bin/zsh -lc "nl -ba crates/ql-io/src/qbook_format.rs | sed -n '4448,4565p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  4448	
  4449	    /// **W5-128 / Codex closure-verify MEDIUM-1:** the loader inserts
  4450	    /// under `canonical_upper`, not the raw `entry.name`. A hand-
  4451	    /// edited TOML with `name = "Sales"` (mixed case) loaded under
  4452	    /// the raw key would have been silently unfindable since
  4453	    /// `TableTable::lookup` uppercases every query. After the fix,
  4454	    /// the table is reachable via the canonical (uppercase) key
  4455	    /// regardless of how `entry.name` was cased on disk.
  4456	    #[test]
  4457	    fn load_normalizes_mixed_case_table_name() {
  4458	        let dir = TempDir::new().unwrap();
  4459	        let path = dir.path().join("mixed.qbook");
  4460	        fs::create_dir_all(path.join("sheets")).unwrap();
  4461	        // `entry.name = "Sales"` — mixed-case. Loader must uppercase
  4462	        // before inserting so subsequent lookups find it.
  4463	        let toml = r#"
  4464	schema_version = 6
  4465	name = "mixed"
  4466	date_system = "1900"
  4467	sheets = [
  4468	  { id = 0, name = "Sheet1", chunk_rows = 16384, row_extent = 0, col_extent = 0 },
  4469	]
  4470	
  4471	[[tables.entries]]
  4472	name = "Sales"
  4473	display_name = "Sales"
  4474	sheet = 0
  4475	top_row = 0
  4476	top_col = 0
  4477	rows = 2
  4478	cols = 1
  4479	has_header = true
  4480	has_totals = false
  4481	
  4482	[[tables.entries.columns]]
  4483	id = 0
  4484	name = "qty"
  4485	display = "Qty"
  4486	"#;
  4487	        fs::write(path.join("workbook.toml"), toml).unwrap();
  4488	        fs::write(path.join("sheets").join("0.jsonl"), "").unwrap();
  4489	        let loaded = load_workbook(&path).expect("mixed-case name must normalize");
  4490	        // Loader must canonicalize the key — `lookup_table` uppercases
  4491	        // every query, so the table must be findable.
  4492	        let meta = loaded
  4493	            .lookup_table("Sales")
  4494	            .expect("table findable post-load");
  4495	        // `meta.name` is the canonical (uppercase) form per design.
  4496	        assert_eq!(meta.name.as_ref(), "SALES");
  4497	        // `meta.display_name` preserves the case-preserving form
  4498	        // straight from the on-disk `display_name`.
  4499	        assert_eq!(meta.display_name.as_ref(), "Sales");
  4500	    }
  4501	
  4502	    /// **W5-128 / Codex closure-verify MEDIUM-1:** two entries with
  4503	    /// the SAME canonical name but DIFFERENT cases (`"Sales"` and
  4504	    /// `"SALES"`). Pre-fix the W5-126 duplicate check missed this
  4505	    /// because the first insert keyed by raw `"Sales"` and the
  4506	    /// second's `lookup("SALES")` returned None against the
  4507	    /// mixed-case key. Post-fix both inserts canonicalize and the
  4508	    /// duplicate-name error fires.
  4509	    #[test]
  4510	    fn malformed_table_rejected_when_mixed_case_duplicate_canonical_name() {
  4511	        let dir = TempDir::new().unwrap();
  4512	        let path = dir.path().join("bad.qbook");
  4513	        fs::create_dir_all(path.join("sheets")).unwrap();
  4514	        let toml = r#"
  4515	schema_version = 6
  4516	name = "bad"
  4517	date_system = "1900"
  4518	sheets = [
  4519	  { id = 0, name = "Sheet1", chunk_rows = 16384, row_extent = 0, col_extent = 0 },
  4520	]
  4521	
  4522	[[tables.entries]]
  4523	name = "Sales"
  4524	display_name = "Sales"
  4525	sheet = 0
  4526	top_row = 0
  4527	top_col = 0
  4528	rows = 2
  4529	cols = 1
  4530	has_header = true
  4531	has_totals = false
  4532	
  4533	[[tables.entries.columns]]
  4534	id = 0
  4535	name = "qty"
  4536	display = "Qty"
  4537	
  4538	[[tables.entries]]
  4539	name = "SALES"
  4540	display_name = "Sales"
  4541	sheet = 0
  4542	top_row = 10
  4543	top_col = 0
  4544	rows = 2
  4545	cols = 1
  4546	has_header = true
  4547	has_totals = false
  4548	
  4549	[[tables.entries.columns]]
  4550	id = 1
  4551	name = "qty"
  4552	display = "Qty"
  4553	"#;
  4554	        fs::write(path.join("workbook.toml"), toml).unwrap();
  4555	        fs::write(path.join("sheets").join("0.jsonl"), "").unwrap();
  4556	        let err = load_workbook(&path).unwrap_err();
  4557	        assert!(
  4558	            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("duplicate canonical table name")),
  4559	            "expected MalformedTable about duplicate canonical, got {err:?}"
  4560	        );
  4561	    }
  4562	}

exec
/bin/zsh -lc "nl -ba crates/ql-io/src/qbook_format.rs | sed -n '1840,1868p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1840	                .collect();
  1841	            let meta = ql_storage::TableMetadata {
  1842	                name: std::sync::Arc::clone(&canonical),
  1843	                display_name: std::sync::Arc::from(entry.display_name.as_str()),
  1844	                sheet: entry.sheet,
  1845	                top_row: entry.top_row,
  1846	                top_col: entry.top_col,
  1847	                rows: entry.rows,
  1848	                cols: entry.cols,
  1849	                has_header: entry.has_header,
  1850	                has_totals: entry.has_totals,
  1851	                columns,
  1852	            };
  1853	            // `insert` auto-bumps `next_column_id` past any persisted
  1854	            // column id (added in 4.8.L to keep the loader-side ids
  1855	            // collision-free with future runtime allocations).
  1856	            wb.tables_mut().insert(canonical, meta);
  1857	        }
  1858	    }
  1859	
  1860	    // Phase 2A.13 audit cycle-3 H4: warn once per load if we rewrote any v1
  1861	    // `Error("#NULL!") + formula` cells to Pending. The rewrite is correct
  1862	    // for engine-produced files (where #NULL! was a stand-in for "not yet
  1863	    // evaluated") but COULD silently corrupt legitimate Excel #NULL! results
  1864	    // — the v1 wire format conflated both cases. The user should re-check
  1865	    // any formulas that legitimately produce #NULL! after this load.
  1866	    if legacy_pending_migrations > 0 {
  1867	        eprintln!(
  1868	            "warning: load_workbook {path:?} migrated {legacy_pending_migrations} v1 \

exec
/bin/zsh -lc 'git show --stat --oneline 1a69c96cc38 -- crates/ql-io/src/qbook_format.rs && git show --stat --oneline 6717ae7fe9f -- crates/ql-formula-syntax/src/ast.rs' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
1a69c96cc38 W5-128 / Engine Phase 4.8.O.4 — closure-verify second-pass: canonical key + comment
 quantbook-engine/crates/ql-io/src/qbook_format.rs | 144 +++++++++++++++++++++-
 1 file changed, 138 insertions(+), 6 deletions(-)
6717ae7fe9f W5-129 / Engine Phase 4.8.O.5 — rewrite_column_ref unit tests (Sonnet LOW-NEW-1)
 .../crates/ql-formula-syntax/src/ast.rs            | 128 +++++++++++++++++++++
 1 file changed, 128 insertions(+)

codex
HEAD is `6717ae7fe9f`.

**W5-128 MEDIUM-1 second-pass**

1. CLOSED: insert now uses `canonical`, built from `canonical_upper`, not raw `entry.name`; the map insert is `wb.tables_mut().insert(canonical, meta)`. `crates/ql-io/src/qbook_format.rs:1830`, `crates/ql-io/src/qbook_format.rs:1856`
2. CLOSED: `meta.name` is `Arc::clone(&canonical)`, so metadata carries the same uppercase canonical name. `crates/ql-io/src/qbook_format.rs:1841`
3. CLOSED: `meta.display_name` still comes directly from `entry.display_name`, preserving case. `crates/ql-io/src/qbook_format.rs:1843`
4. CLOSED: `load_normalizes_mixed_case_table_name` loads `name = "Sales"`, checks `lookup_table("Sales")`, and asserts `meta.name == "SALES"`. `crates/ql-io/src/qbook_format.rs:4457`
5. CLOSED: `malformed_table_rejected_when_mixed_case_duplicate_canonical_name` covers `"Sales"` plus `"SALES"` and asserts duplicate canonical rejection. `crates/ql-io/src/qbook_format.rs:4510`

**W5-128 Sonnet MEDIUM-NEW-1 comment fix**

1. CLOSED: the old “sheet-scoped collisions are caught” claim is gone. `crates/ql-io/src/qbook_format.rs:1762`
2. CLOSED: the new comment explicitly says sheet-scoped name collisions are intentionally not checked, matching `WorkbookRuntime::create_table`. `crates/ql-io/src/qbook_format.rs:1764`

**W5-129 Sonnet LOW-NEW-1**

1. CLOSED: five `rewrite_column_ref_*` tests exist. `crates/ql-formula-syntax/src/ast.rs:957`
2. CLOSED: they cover the five requested distinct paths: string literal, special item in combination, this-row column range, column range endpoint rewrite, and cross-table isolation. `crates/ql-formula-syntax/src/ast.rs:957`, `crates/ql-formula-syntax/src/ast.rs:971`, `crates/ql-formula-syntax/src/ast.rs:1000`, `crates/ql-formula-syntax/src/ast.rs:1020`, `crates/ql-formula-syntax/src/ast.rs:1045`
3. CLOSED: each test asserts the concrete post-rewrite AST shape and expected names, not merely success/no panic. `crates/ql-formula-syntax/src/ast.rs:965`

**Probes**

A. OK: first entry computes `canonical_upper`, duplicate-checks with `lookup`, inserts under the canonical key, and the second independently computed same canonical then hits the duplicate check. `crates/ql-io/src/qbook_format.rs:1751`

B. OK: `Arc::from(canonical_upper.as_str())` allocates owned storage; `meta.name` clones that `Arc`, and the HashMap key receives the original `Arc`, so both point to the same allocation. `crates/ql-io/src/qbook_format.rs:1830`

**SHIP-READY**
tokens used
45,998
HEAD is `6717ae7fe9f`.

**W5-128 MEDIUM-1 second-pass**

1. CLOSED: insert now uses `canonical`, built from `canonical_upper`, not raw `entry.name`; the map insert is `wb.tables_mut().insert(canonical, meta)`. `crates/ql-io/src/qbook_format.rs:1830`, `crates/ql-io/src/qbook_format.rs:1856`
2. CLOSED: `meta.name` is `Arc::clone(&canonical)`, so metadata carries the same uppercase canonical name. `crates/ql-io/src/qbook_format.rs:1841`
3. CLOSED: `meta.display_name` still comes directly from `entry.display_name`, preserving case. `crates/ql-io/src/qbook_format.rs:1843`
4. CLOSED: `load_normalizes_mixed_case_table_name` loads `name = "Sales"`, checks `lookup_table("Sales")`, and asserts `meta.name == "SALES"`. `crates/ql-io/src/qbook_format.rs:4457`
5. CLOSED: `malformed_table_rejected_when_mixed_case_duplicate_canonical_name` covers `"Sales"` plus `"SALES"` and asserts duplicate canonical rejection. `crates/ql-io/src/qbook_format.rs:4510`

**W5-128 Sonnet MEDIUM-NEW-1 comment fix**

1. CLOSED: the old “sheet-scoped collisions are caught” claim is gone. `crates/ql-io/src/qbook_format.rs:1762`
2. CLOSED: the new comment explicitly says sheet-scoped name collisions are intentionally not checked, matching `WorkbookRuntime::create_table`. `crates/ql-io/src/qbook_format.rs:1764`

**W5-129 Sonnet LOW-NEW-1**

1. CLOSED: five `rewrite_column_ref_*` tests exist. `crates/ql-formula-syntax/src/ast.rs:957`
2. CLOSED: they cover the five requested distinct paths: string literal, special item in combination, this-row column range, column range endpoint rewrite, and cross-table isolation. `crates/ql-formula-syntax/src/ast.rs:957`, `crates/ql-formula-syntax/src/ast.rs:971`, `crates/ql-formula-syntax/src/ast.rs:1000`, `crates/ql-formula-syntax/src/ast.rs:1020`, `crates/ql-formula-syntax/src/ast.rs:1045`
3. CLOSED: each test asserts the concrete post-rewrite AST shape and expected names, not merely success/no panic. `crates/ql-formula-syntax/src/ast.rs:965`

**Probes**

A. OK: first entry computes `canonical_upper`, duplicate-checks with `lookup`, inserts under the canonical key, and the second independently computed same canonical then hits the duplicate check. `crates/ql-io/src/qbook_format.rs:1751`

B. OK: `Arc::from(canonical_upper.as_str())` allocates owned storage; `meta.name` clones that `Arc`, and the HashMap key receives the original `Arc`, so both point to the same allocation. `crates/ql-io/src/qbook_format.rs:1830`

**SHIP-READY**
