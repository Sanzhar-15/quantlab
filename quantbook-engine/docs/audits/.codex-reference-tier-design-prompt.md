# Codex pre-review — Reference-tier mini-phase design doc

You are reviewing a design document for an architectural change to the Quantbook spreadsheet engine. The design has NOT been implemented yet — your job is to **find what's wrong, missing, or risky in the design BEFORE any code lands**. This pattern (design-first + Codex pre-review) previously closed 3 HIGH + 4 MEDIUM findings on the Phase 4.10.AA design (W5-162.1).

## Repository state

- Branch: `feat/quantbook-engine`
- HEAD: `8f9b37b5d6d` (docs/handoff-audit Opus output closure)
- Workspace: `/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine`
- 3082 tests passing, 0 failed, 1 ignored, 193 registered functions

## Documents to read

1. **The design doc itself:** `docs/architecture/2026-05-17-reference-tier-design.md` — this is the artefact under review.
2. **Predecessor design doc (template for tone + structure):** `docs/architecture/2026-05-16-phase-4.10-function-library-wave-2.md` — note its "Status" line cites the Codex pre-review that closed 3 HIGH + 4 MEDIUM. Same pattern applies here.
3. **Current registry source:** `crates/ql-functions/src/registry.rs` — verify `FunctionArg`, `RegisteredFn`, `FunctionFn`, `register_*` claims in the design doc match reality.
4. **Range-aware tier source:** `crates/ql-functions/src/range_aware_fns.rs` — verify `FnArg` claims.
5. **Context-aware tier source:** `crates/ql-functions/src/context_aware_fns.rs` — verify the dispatch-order claim in the design doc.
6. **Dispatcher source:** `crates/ql-exec/src/scalar.rs` — verify lines 137–317 dispatch claims; spot anything the design doc missed about `eval_at_cell_boundary` (line 557+) and the eval-context plumbing.
7. **Binder source:** `crates/ql-exec/src/plan.rs` — especially the `is_aggregate_function` list (line 377+) and the `bind_with_site_no_tables` / `bind_with_names_and_sheets` flow (line 461+). Verify the design's "B1 reuses `is_aggregate_function`" claim is workable.
8. **IronCalc references (port source):**
   - `.references/ironcalc/base/src/functions/lookup_and_reference/mod.rs` — `fn_row` (line 608), `fn_rows` (line 623), `fn_column` (line 636), `fn_columns` (line 675), `fn_formulatext` (line 850).
   - `.references/ironcalc/base/src/functions/information.rs` — `fn_isref` (line 119), `fn_isformula` (line 155).
9. **Audit-discipline rule:** see `~/.claude/projects/-Users-sanzhar-Documents-Sanzhar-Sanzhar-quantlab/memory/quantbook_engine_audit_discipline.md` for the severity-conflict reconciliation rules + the W5-162.1 pattern.

## Find these classes of problems

For the design doc:

1. **Architectural mistakes** — does the proposed `RefArg` enum cover all the cases? Is `Literal` truly needed or is it dead weight? Does D2 (recommended) lock us into worse maintenance than D1 (ParamSchema)? Is there an Option D4 I missed?
2. **Plumbing gaps** — does the dispatcher arm in § 5.3 actually work end-to-end? Is `CellEnv::call_site()` reachable from `eval_scalar_with_cache`, or does the call site only flow through `eval_at_cell_boundary`? What about the plan-cache path? Aggregate-cache path?
3. **Semantic divergence from Excel canon** — for each of the 7 target fns, are the error-class choices in § 5.6 (`#VALUE!` vs `#N/A` vs `#REF!`) correct against Microsoft canon and LibreOffice/OpenFormula? Specifically question 8 in § 10 — is the single-cell-only stance for ISFORMULA/FORMULATEXT a divergence we should accept or close?
4. **Binder pitfalls** — adding the 7 fns to `is_aggregate_function` works for `Expr::RangeRef` arguments. But what about `Expr::CellRef` arguments to `ROW(A1)`? Today the binder lowers cell refs to `ExprPlan::CellRef` — does adding to the aggregate list change that? Verify by reading the binder logic.
5. **`WorkbookRuntime` source-text retention** — Risk R3 explicitly defers this to investigation. Investigate now: does `WorkbookRuntime` retain the formula source text, or only the parsed AST? If only AST, is the printer round-trip path (`ql_formula_syntax::print`) viable? Test it against a non-trivial formula and check that the round-tripped output matches Excel's canonical FORMULATEXT output.
6. **Parser zero-arg ROW() / COLUMN()** — Risk R6 says "verify our parser accepts these". Verify NOW. Read `ql-formula-syntax/src/lib.rs` or its parser submodule and report.
7. **`ReferenceQuery` impls** — § 5.5 proposes a separate trait. Whose responsibility is it to implement? `WorkbookRuntime` likely; what about test mocks? What about the `MapCellEnv` / placeholder envs in `crates/ql-exec/src/env.rs`?
8. **Test plan adequacy** — § 7 proposes 6 tests per fn. Given that reference-tier is architecturally novel, is 6 enough? Should we also pin: (a) cross-sheet refs, (b) named-range args, (c) interaction with structured-refs (`Sales[Qty]`), (d) interaction with `@implicit-intersection`, (e) very-large ranges (workbook row limits), (f) #REF! propagation through ROW/COLUMN?
9. **Spill semantics defer** — § 8 R5 defers array spill of `ROW(A1:A5)`. Is that defer safe, given Phase 4.7's partial array plumbing? Or does this commit need to either fully support spill OR explicitly produce `#CALC!`?
10. **Migration path from D2 → D1 (ParamSchema)** — § 4 recommends D2 with a written migration plan. Is that migration plan realistic? Estimate effort. Identify the trigger condition that should cause the migration.
11. **Cross-cutting hits we missed** — does the design touch any other file we haven't named (plan cache key calculation? calcgraph dep extraction? export/import? IDE diagnostic surfaces?)
12. **Audit-discipline self-application** — does this design doc itself follow the audit-discipline rule? Specifically, does it commit to parallel-Codex+Opus audit on EACH ship commit, not just at phase end?

## Output format

Write findings as HIGH (likely incorrect output if implemented as designed) / MEDIUM (works but deviates from canon or has a maintainability concern) / LOW (style / doc / nit).

Each finding gets:
- **ID** (HIGH-1, MEDIUM-3, LOW-2 etc.)
- **Subject** (one-line title)
- **Where** (file:line or section §)
- **Detail** (what's wrong)
- **Recommendation** (what to do)
- **Severity rationale** if the call is non-obvious

Be critical. Find what's wrong. Don't summarize what's right unless directly asked.

Write to `docs/audits/2026-05-17-reference-tier-design-codex.md`.
