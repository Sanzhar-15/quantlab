OpenAI Codex v0.130.0
--------
workdir: /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR, /Users/sanzhar/.codex/memories]
reasoning effort: high
reasoning summaries: none
session id: 019e2949-b579-7141-b245-b7f8c7c745e2
--------
user
# Phase 4.8.O closure-verify

You ran the Phase 4.8 closing megaudit (audit transcript at
`docs/audits/2026-05-15-phase-4.8-closing-megaudit-codex.md`) and
returned NOT SHIP-READY with 1 HIGH + 2 MEDIUM + 2 LOW findings.

This prompt asks you to verify those fixes shipped clean. Mirrors
the Phase 4.7.O closure-verify pattern (commit `f00cb321e33`).

## Fix commits (chronological)

```
ed5387e7dda  W5-125 / Phase 4.8.O.1  HIGH-1 closure: footprint bounds
06b158ee0cf  W5-126 / Phase 4.8.O.2  MEDIUM-1 + MEDIUM-2 closure: loader re-validates runtime invariants
9c850f37733  W5-127 / Phase 4.8.O.3  LOW-1 + LOW-2 closure: resize no-op + composed e2e tests
```

HEAD is now `9c850f37733`. 2407 workspace tests, all 7 gates green
(`cargo test --workspace` / `clippy -D warnings` / `fmt --check` /
`doc --workspace --no-deps`).

## Per-finding verification

For each of your 5 original findings, read the cited file/line
ranges at HEAD and confirm:

1. The fix exists at the producer / replay / loader entry point
   you identified.
2. The fix's error message is reachable by normal runtime usage
   (not just synthetic tests).
3. Tests pin the fix at the right granularity.

### HIGH-1 — footprint bounds overflow

Original finding files:
- `crates/ql-exec/src/workbook_runtime.rs:1516-1587` (create_table)
- `crates/ql-exec/src/workbook_runtime.rs:1963-2054` (resize_table)
- `crates/ql-oplog/src/replay.rs:890-917` (apply_create_table)
- `crates/ql-oplog/src/replay.rs:694-795` (apply_resize_table)
- `crates/ql-io/src/qbook_format.rs:1700-1750` (v6 loader)

Fix: `TableMetadata::validate_footprint_bounds` in
`crates/ql-storage/src/tables.rs` + 5 call sites.

Verify:
- The helper uses `checked_add` (not `+` or `wrapping_add`).
- All 5 call sites invoke the helper BEFORE entering any footprint
  loop or `insert`.
- The new tests cover both the `MAX_ROW`/`MAX_COLUMN` upper bound
  AND the `u32::MAX` overflow case.
- The pre-existing `rows == 0 || cols == 0` check fires FIRST, so
  the helper's `debug_assert!(rows > 0 && cols > 0)` precondition
  is upheld.

### MEDIUM-1 — v6 loader skips runtime registry invariants

Original finding files:
- `crates/ql-io/src/qbook_format.rs:1700-1750`

Fix: load-side re-validation block at the same location.

Verify:
- Duplicate canonical name rejection (checks `wb.tables().lookup`).
- Defined-name namespace collision (checks `wb.names().lookup_ci`).
- Footprint non-overlap walk against tables inserted earlier in
  the same loop.
- Empty column name rejection (both `name` AND `display`).
- Per-table column-name canonical uniqueness check.
- Spill-anchor check INTENTIONALLY OMITTED — spills aren't persisted;
  `wb.spill_anchors` is empty at load. Is that correct?

### MEDIUM-2 — `TableTable::insert` `max_id + 1` unchecked

Original finding files:
- `crates/ql-storage/src/tables.rs:303-306`

Fix decision was: reject at load (the same `MEDIUM-1` block in
qbook_format.rs) rather than `TableTable::insert` returning an
error.

Verify:
- Load-side rejects `col.id == u32::MAX`.
- The `tables.rs:insert` impl is unchanged (still does
  `max_id + 1` via `checked_add` chain... or via `max()`?). Walk
  through the storage-side test cases to confirm the allocator
  invariant is still upheld for non-malicious inputs.

### LOW-1 — `resize_table` no-op still emits op

Original finding files:
- `crates/ql-exec/src/workbook_runtime.rs:1963-2093`

Fix: short-circuit `return Ok(())` when `new_rows == old_rows &&
new_cols == old_cols && added.is_empty() && removed.is_empty()`.

Verify:
- The short-circuit is placed AFTER the snapshot block (so the
  "table doesn't exist" error path still fires) but BEFORE op-log
  append.
- Test `resize_table_exact_noop_emits_nothing` asserts the op log
  contains only `CreateTable` after a no-op resize call.

### LOW-2 — composed e2e tests

Original finding files:
- `crates/ql-exec/tests/oplog_e2e.rs:649-759`
- `crates/ql-exec/tests/oplog_e2e.rs:768-811`

Fix: two new tests at the end of `oplog_e2e.rs`.

Verify:
- `composed_create_rename_column_resize_replay_roundtrip` — exercises
  the create → rename → resize pipeline through replay.
- `composed_save_load_then_rename_column_roundtrip` — exercises
  persistence × runtime-mutation through save → load → rename.

## Independent quality probes (NOT pre-existing findings)

After verifying the 5 closures, do ONE pass looking for new issues
the fixes might have introduced:

A. **Off-by-one in the footprint helper.** `top_row + rows - 1` is
   the LAST claimed row. Walk through the cases:
   - `top_row = 0, rows = 1` → last = 0. Pass.
   - `top_row = 0, rows = MAX_ROW + 1` → last = MAX_ROW. Pass.
   - `top_row = 0, rows = MAX_ROW + 2` → last = MAX_ROW + 1. Fail
     (correct).

B. **Loader rejection ordering.** If MEDIUM-1's loader checks fire
   AFTER `Workbook::tables_mut().insert` for the first valid entry
   but BEFORE the second invalid entry, does an error abort `load_
   workbook` cleanly or leave the first table half-inserted? Recovery
   is out of scope for `MalformedTable` — but check if `load_workbook`
   has rollback semantics or if a partial load is the documented
   contract.

C. **`resize_table` no-op + plan cache interaction.** A no-op resize
   returns `Ok(())` WITHOUT clearing the plan cache. Is that correct?
   Or could a cached plan be stale at the moment of a no-op
   (i.e. the plan cache is conceptually "dirty" but nobody is
   bumping the generation)?

## Output

Per finding: state "CLOSED" or "STILL OPEN" with a one-sentence
justification citing the new file/line at HEAD.

For new probes A/B/C: state "OK" or surface a new finding with
HIGH/MEDIUM/LOW severity.

End with a one-line verdict: **SHIP-READY** or **NOT SHIP-READY**.

## Hard rules

- No fallbacks (CLAUDE.md: errors must be visible).
- No schema-version range check changes.
- No test count reduction.

codex
I’ll verify this directly against `HEAD`, including the cited call sites and tests, then run targeted checks only where needed. I’m first reading the audit context and the current files around the modified areas.
exec
/bin/zsh -lc "sed -n '1,220p' docs/audits/2026-05-15-phase-4.8-closing-megaudit-codex.md" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
OpenAI Codex v0.130.0
--------
workdir: /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR, /Users/sanzhar/.codex/memories]
reasoning effort: high
reasoning summaries: none
session id: 019e2928-014b-7160-a333-daf015aa84e0
--------
user
# Phase 4.8 closing megaudit (4.8.A → L + W5-124)

You are reviewing the cumulative Phase 4.8 (structured references +
tables) arc. The arc is feature-complete. Your job is to find
integration-level issues that single-phase audits missed — the kind
of issue that only surfaces when multiple sub-phases interact.

This pattern caught a HIGH BLOCKER at the prior 4.7.O megaudit
(commit `34fb2d46311`). Treat it as load-bearing — go deeper than
"each sub-phase reviewed individually."

## Context

- Branch: `feat/quantbook-engine`. HEAD `7ec316402e8`.
- Design doc: `docs/architecture/2026-05-14-structured-references-and-tables.md`
  (765 lines, including § 14 status table + § 17 Codex pass-1 closure
  summary).
- Pre-arc HEAD: `9e5085a5405` (the prior session's Phase 4.8.I close).
- 2384 workspace tests, 0 failed, 1 ignored, all 7 gates green.

## Commits in scope (chronological)

```
ca4faa62dad  W5-121 / 4.8.I.2  rename_column + formula-text rewrite          (+11 tests)
666af927b3a  W5-122 / 4.8.J    resize_table + Op::ResizeTable                (+13 tests)
1e713cffbc7  W5-123 / 4.8.L    schema v5 → v6 + TableTable persistence       (+12 tests)
7ec316402e8  W5-124 / 4.8.J.2  uniform spill-anchor check (create+resize)    (+4 tests)
```

Two fmt-only cleanup commits (`749562bf41f`, `0bb7ef8aa9a`) precede
these but carry no semantic change.

## Areas to probe (in priority order)

### A. Cross-sub-phase invariants

These are the "only surfaces when N≥2 sub-phases meet" issues.

1. **Op log replay ordering invariants.** A producer session that
   does `create_table → rename_column → resize_table` emits a
   specific sequence. When replayed against a fresh workbook, does
   the order survive? Specifically: does `apply_rename_column`
   accept a column id that `apply_create_table` allocated, AND does
   the post-rename `apply_resize_table` see the renamed column in
   its trailing-column check?

2. **`TableTable::generation` accounting.** Every mutation must
   bump it (or the plan cache stays warm with stale `is_aggregate`
   resolutions). Check: create / drop / rename_table / rename_column
   / resize all bump it. Look for paths that mutate via `get_mut`
   without an explicit `bump_generation()` call. Are there any?

3. **`next_column_id` allocator preservation.** W5-123 added
   auto-bump in `TableTable::insert`. Verify: producer creates
   `Sales[Qty,Price]` (ids 0, 1) → resize_table appends `Discount`
   (id 2) → save → load → resize_table appends `Tax`. Does Tax get
   id 3 (correct) or 0 (collision)?

4. **`.qbook` persistence × runtime mutation interaction.** Save a
   workbook with a table. Reload. Now `rename_column` on the
   reloaded table. Does the rewrite walk over the reloaded formula
   text and produce identical output to the pre-save version?

5. **Spill-anchor uniform check (W5-124) — completeness.** The
   producer-side check fires at `create_table` and `resize_table`.
   But: what about `Workbook::tables_mut().insert(...)` (the
   loader's direct insertion path)? Does the loader at 4.8.L bypass
   the spill-anchor check? Is that intentional (load-time data is
   trusted) or a gap?

### B. Formula-text rewrite correctness (4.8.I + 4.8.I.2)

6. **String literals containing column names.** A formula like
   `="The Qty column"` — does `rename_column("Sales", "Qty",
   "Quantity")` accidentally rewrite the string literal? Walk
   through `rewrite_column_ref` mentally. (Same question for
   `rename_table`.)

7. **Specials-with-column-named-#Foo collision.** A table with a
   column literally named `#Headers` (escaped in source as
   `'#Headers`). Does `rename_column("Sales", "#Headers", "Hdrs")`
   correctly find the right column? Does the AST walker
   distinguish `TableSpecItem::Column("#Headers")` from
   `TableSpecItem::Special(SpecialItem::Headers)`?

8. **Plan-cache invalidation drift.** Rename and resize both do
   `plan_cache.clear()`. Is there a faster path (e.g.
   `plan_cache.clear_for_table(name)`) that should exist? Are there
   formulas NOT referencing the renamed/resized table that get
   needlessly re-bound on next eval?

### C. resize_table edge cases (4.8.J)

9. **Same-canonical no-op semantics — does it match rename?**
   `rename_column` returns `Ok(0)` for same-canonical, emits no op.
   Does `resize_table` do anything analogous when called with
   `new_rows == old_rows && new_cols == old_cols && added.is_empty()
   && removed.is_empty()`? Or does it always emit an op even for
   a no-op resize? Should it?

10. **Resize to zero data rows.** A table with `has_header=true,
    rows=3` (header + 2 data rows). Resize to `rows=1` (header only,
    no data). Does `SUM(Sales[Qty])` correctly surface
    `BindError::StructuredRefDegenerateRange` on next bind?

11. **Resize with column id pollution.** Producer adds column,
    then immediately drops it via two `resize_table` calls. The
    added column burned a `next_column_id`. Is that intentional
    or wasteful? (Probably intentional — match Postgres SERIAL
    semantics — but worth confirming the design intent.)

### D. Persistence (4.8.L)

12. **Round-trip determinism.** Save → load → save → diff the
    two `workbook.toml` files. Are they byte-identical? Test the
    sort orders (`tables_section` sorts by canonical name; do
    column orders within a table preserve insertion order or get
    sorted?).

13. **`MalformedTable` coverage.** The loader validates 3 things:
    sheet exists, non-zero dims, columns.len matches cols. What
    about: a column with empty `name` or empty `display`? A
    `totals_function: Some(Custom)` (does anything else need to be
    valid)? `top_row/top_col + rows/cols` overflowing `RowId/ColId`?

14. **Forward-compat sketch.** A v6 file with a future v7-only
    field in `TableColumnEntry` (e.g. `style_id: u32`) — does
    `deny_unknown_fields` correctly reject it? Important because
    silent acceptance of unknown fields here would cause the same
    class of bug that the v2 schema bump explicitly fixed (M9).

### E. Backwards compat

15. **Pre-existing fixtures.** Are there any test fixtures of v1-v5
    `.qbook` files in the repo (`tests/fixtures/`, `.references/`)?
    Do they all still load on v6 without the workbook becoming
    semantically different (e.g. an empty `TableTable` is fine, but
    a phantom format entry from v4 shouldn't appear)?

### F. Test coverage gaps

16. List any obvious test gaps. Specifically: every public method
    added in W5-121/W5-122/W5-123/W5-124 — is there at least one
    direct test? At least one round-trip test through op log?

## Output

Triage your findings as:

- **HIGH** = ship blocker; the engine can produce an inconsistent
  state or a crash from normal use.
- **MEDIUM** = correctness or robustness issue; user-visible but
  recoverable.
- **LOW** = polish; doc drift, dead code, naming, etc.

Each finding should cite: file path, line range, one-sentence
description of what's wrong, one-sentence proposed fix.

If a finding is uncertain (you'd need to read more), flag as
"NEEDS-INVESTIGATION" and describe what to check.

Be specific — generic advice like "consider edge cases" is not
useful. Concrete findings ("at `workbook_runtime.rs:1820`, when
`removed_columns.len() == old_cols`, the trailing_start cast to
usize underflows") are what we need.

## Hard rules

- Do NOT propose fixes that introduce fallbacks (CLAUDE.md hard
  rule: errors must be visible).
- Do NOT propose breaking the schema-version range check.
- Do NOT propose lowering test count.

End with a one-line verdict: **SHIP-READY** or **NOT SHIP-READY**.

codex
I’ll audit this in review mode: first mapping the touched code and tests across the four commits, then tracing the integration paths rather than re-running the full suite unless a concrete suspicion needs it.
exec
/bin/zsh -lc "rg --files | rg '(workbook|table|structured|qbook|fixture|reference|tests)'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
docs/phase0/references-reading-log.md
docs/architecture/2026-05-13-cross-sheet-references.md
docs/architecture/2026-05-14-structured-references-and-tables.md
crates/ql-exec/src/workbook_runtime.rs
crates/ql-io/src/qbook_format.rs
crates/ql-exec/tests/oplog_e2e.rs
crates/ql-exec/tests/error_precedence.rs
crates/ql-exec/tests/region_mul2_e2e.rs
crates/ql-exec/tests/ide_simulation.rs
crates/ql-oplog/tests/producer_replay_equivalence.rs
crates/ql-functions/tests/per_function_overrides.rs
crates/ql-functions/tests/coverage.rs
crates/ql-types/tests/coercion_matrix.rs
crates/ql-storage/src/tables.rs
crates/ql-storage/src/workbook.rs

exec
/bin/zsh -lc "rg -n \"struct TableTable|impl TableTable|generation|bump_generation|next_column_id|rename_column|resize_table|apply_resize_table|apply_rename_column|apply_create_table|StructuredRefDegenerateRange|deny_unknown_fields|MalformedTable|TableColumnEntry|tables_mut\\(\\)\" ." in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
./docs/MASTER-PLAN.md:158:   Cache parsed and bound `ExprPlan` next to workbook formula text, keyed by formula fingerprint plus sheet/name-table generation. Invalidate on formula text change, name-table mutation, and sheet add. Keep cache local to runtime/storage; do not invent graph scheduling here.  
./crates/ql-oplog/src/replay.rs:492:        } => apply_create_table(
./crates/ql-oplog/src/replay.rs:507:            if workbook.tables_mut().remove(canonical.as_str()).is_none() {
./crates/ql-oplog/src/replay.rs:522:        } => apply_rename_column(workbook, index, table, old_name, new_name),
./crates/ql-oplog/src/replay.rs:529:        } => apply_resize_table(
./crates/ql-oplog/src/replay.rs:582:        .tables_mut()
./crates/ql-oplog/src/replay.rs:587:    workbook.tables_mut().insert(new_canonical, meta);
./crates/ql-oplog/src/replay.rs:595:/// (case-preserving). Bumps the `TableTable` generation so plan caches
./crates/ql-oplog/src/replay.rs:599:fn apply_rename_column(

exec
/bin/zsh -lc "rg -n \"validate_footprint_bounds|resize_table_exact_noop|composed_create_rename|composed_save_load|MalformedTable|col\\.id == u32::MAX|u32::MAX|MAX_ROW|MAX_COLUMN|lookup_ci|lookup\\(\" crates/ql-storage/src/tables.rs crates/ql-exec/src/workbook_runtime.rs crates/ql-oplog/src/replay.rs crates/ql-io/src/qbook_format.rs crates/ql-exec/tests/oplog_e2e.rs" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
crates/ql-storage/src/tables.rs:20:use ql_types::{Address, ColId, Range, RowId, SheetId, MAX_COLUMN, MAX_ROW};
crates/ql-storage/src/tables.rs:231:    /// 1. `top_row + rows - 1 > MAX_ROW` (or same for col) — a table
crates/ql-storage/src/tables.rs:241:    /// `TableResizeRejected`, `ReplayError::*`, `QbookError::MalformedTable`).
crates/ql-storage/src/tables.rs:246:    pub fn validate_footprint_bounds(
crates/ql-storage/src/tables.rs:256:        if last_row > MAX_ROW {
crates/ql-storage/src/tables.rs:257:            return Err("table footprint extends past MAX_ROW");
crates/ql-storage/src/tables.rs:262:        if last_col > MAX_COLUMN {
crates/ql-storage/src/tables.rs:263:            return Err("table footprint extends past MAX_COLUMN");
crates/ql-storage/src/tables.rs:306:    pub fn lookup(&self, name: &str) -> Option<&TableMetadata> {
crates/ql-storage/src/tables.rs:370:    /// design § 4.1). Wraps at u32::MAX via `wrapping_add`; in practice
crates/ql-storage/src/tables.rs:532:        assert_eq!(tt.lookup("Sales"), Some(&t));
crates/ql-storage/src/tables.rs:533:        assert_eq!(tt.lookup("SALES"), Some(&t));
crates/ql-storage/src/tables.rs:534:        assert_eq!(tt.lookup("sales"), Some(&t));
crates/ql-storage/src/tables.rs:535:        assert_eq!(tt.lookup("Other"), None);
crates/ql-storage/src/tables.rs:684:    fn validate_footprint_bounds_accepts_in_range_footprint() {
crates/ql-storage/src/tables.rs:686:        assert!(TableMetadata::validate_footprint_bounds(0, 0, 3, 2).is_ok());
crates/ql-storage/src/tables.rs:687:        // Exactly at MAX_ROW / MAX_COLUMN.
crates/ql-storage/src/tables.rs:688:        assert!(TableMetadata::validate_footprint_bounds(MAX_ROW, MAX_COLUMN, 1, 1).is_ok());
crates/ql-storage/src/tables.rs:689:        assert!(TableMetadata::validate_footprint_bounds(MAX_ROW - 1, 0, 2, 1).is_ok());
crates/ql-storage/src/tables.rs:693:    fn validate_footprint_bounds_rejects_past_max_row() {
crates/ql-storage/src/tables.rs:694:        // top_row + rows - 1 = MAX_ROW + 1 → past the grid.
crates/ql-storage/src/tables.rs:695:        let err = TableMetadata::validate_footprint_bounds(MAX_ROW - 1, 0, 3, 1).unwrap_err();
crates/ql-storage/src/tables.rs:696:        assert!(err.contains("MAX_ROW"), "reason: {err}");
crates/ql-storage/src/tables.rs:698:        let err2 = TableMetadata::validate_footprint_bounds(MAX_ROW, 0, 100, 1).unwrap_err();
crates/ql-storage/src/tables.rs:699:        assert!(err2.contains("MAX_ROW"), "reason: {err2}");
crates/ql-storage/src/tables.rs:703:    fn validate_footprint_bounds_rejects_past_max_column() {
crates/ql-storage/src/tables.rs:704:        let err = TableMetadata::validate_footprint_bounds(0, MAX_COLUMN - 1, 1, 3).unwrap_err();
crates/ql-storage/src/tables.rs:705:        assert!(err.contains("MAX_COLUMN"), "reason: {err}");
crates/ql-storage/src/tables.rs:709:    fn validate_footprint_bounds_rejects_u32_overflow_on_rows() {
crates/ql-storage/src/tables.rs:712:        let err = TableMetadata::validate_footprint_bounds(u32::MAX, 0, 2, 1).unwrap_err();
crates/ql-storage/src/tables.rs:720:    fn validate_footprint_bounds_rejects_u32_overflow_on_cols() {
crates/ql-storage/src/tables.rs:721:        let err = TableMetadata::validate_footprint_bounds(0, u32::MAX, 1, 2).unwrap_err();
crates/ql-exec/tests/oplog_e2e.rs:67:        let target_a = names_a.lookup_ci(name).expect("present in a");
crates/ql-exec/tests/oplog_e2e.rs:69:            .lookup_ci(name)
crates/ql-exec/tests/oplog_e2e.rs:437:        loaded_wb.formats().lookup(custom_id),
crates/ql-exec/tests/oplog_e2e.rs:816:/// dimensions extend past MAX_ROW. This pins the producer-replay
crates/ql-exec/tests/oplog_e2e.rs:823:    use ql_types::MAX_ROW;
crates/ql-exec/tests/oplog_e2e.rs:829:            top_row: MAX_ROW - 1,
crates/ql-exec/tests/oplog_e2e.rs:843:            assert!(reason.contains("MAX_ROW"), "reason: {reason}");
crates/ql-exec/tests/oplog_e2e.rs:851:/// hand-crafted ResizeTable that grows past MAX_ROW.
crates/ql-exec/tests/oplog_e2e.rs:855:    use ql_types::MAX_ROW;
crates/ql-exec/tests/oplog_e2e.rs:861:            top_row: MAX_ROW - 4,
crates/ql-exec/tests/oplog_e2e.rs:870:    // Grow to 10 rows → last_row = MAX_ROW + 5 > MAX_ROW.
crates/ql-exec/tests/oplog_e2e.rs:885:            assert!(reason.contains("MAX_ROW"), "reason: {reason}");
crates/ql-exec/tests/oplog_e2e.rs:899:fn composed_create_rename_column_resize_replay_roundtrip() {
crates/ql-exec/tests/oplog_e2e.rs:951:fn composed_save_load_then_rename_column_roundtrip() {
crates/ql-oplog/src/replay.rs:45:use ql_types::{ColId, RowId, SheetId, Value, MAX_COLUMN, MAX_ROW};
crates/ql-oplog/src/replay.rs:455:                if workbook.formats().lookup(fid).is_none() {
crates/ql-oplog/src/replay.rs:557:    if workbook.tables().lookup(&old_canonical).is_none() {
crates/ql-oplog/src/replay.rs:565:        if workbook.tables().lookup(&new_canonical).is_some() {
crates/ql-oplog/src/replay.rs:572:        if workbook.names().lookup_ci(&new_canonical).is_some() {
crates/ql-oplog/src/replay.rs:677:                .lookup(&canonical)
crates/ql-oplog/src/replay.rs:704:        ql_storage::TableMetadata::validate_footprint_bounds(top_row, top_col, new_rows, new_cols)
crates/ql-oplog/src/replay.rs:852:    if workbook.tables().lookup(&canonical).is_some() {
crates/ql-oplog/src/replay.rs:859:    if workbook.names().lookup_ci(&canonical).is_some() {
crates/ql-oplog/src/replay.rs:911:        ql_storage::TableMetadata::validate_footprint_bounds(top_row, top_col, rows, cols)
crates/ql-oplog/src/replay.rs:981:    if row > MAX_ROW {
crates/ql-oplog/src/replay.rs:986:            why: "row exceeds MAX_ROW (1,048,575)",
crates/ql-oplog/src/replay.rs:989:    if col > MAX_COLUMN {
crates/ql-oplog/src/replay.rs:994:            why: "col exceeds MAX_COLUMN (16,383)",
crates/ql-oplog/src/replay.rs:1107:            wb.names().lookup_ci("TaxRate"),
crates/ql-oplog/src/replay.rs:1264:            wb.formats().lookup(ql_storage::FormatId(200)),
crates/ql-oplog/src/replay.rs:1284:            wb.formats().lookup(ql_storage::FormatId(0)),
crates/ql-oplog/src/replay.rs:1612:            wb.sheet(0).unwrap().scoped_names().lookup_ci("Rate"),
crates/ql-oplog/src/replay.rs:1637:            wb.names().lookup_ci("Rate"),
crates/ql-io/src/qbook_format.rs:77:use ql_types::{Address, ColId, ErrorValue, Range, RowId, SheetId, Value, MAX_COLUMN, MAX_ROW};
crates/ql-io/src/qbook_format.rs:278:    MalformedTable { name: String, reason: String },
crates/ql-io/src/qbook_format.rs:671:                if *row > MAX_ROW {
crates/ql-io/src/qbook_format.rs:674:                        reason: format!("Cell.row {row} exceeds MAX_ROW {MAX_ROW}"),
crates/ql-io/src/qbook_format.rs:677:                if *col > MAX_COLUMN {
crates/ql-io/src/qbook_format.rs:680:                        reason: format!("Cell.col {col} exceeds MAX_COLUMN {MAX_COLUMN}"),
crates/ql-io/src/qbook_format.rs:693:                    ("start_row", *start_row, MAX_ROW),
crates/ql-io/src/qbook_format.rs:694:                    ("start_col", *start_col, MAX_COLUMN),
crates/ql-io/src/qbook_format.rs:695:                    ("end_row", *end_row, MAX_ROW),
crates/ql-io/src/qbook_format.rs:696:                    ("end_col", *end_col, MAX_COLUMN),
crates/ql-io/src/qbook_format.rs:1524:                if entry.row > MAX_ROW || entry.col > MAX_COLUMN {
crates/ql-io/src/qbook_format.rs:1573:            if rec.row > MAX_ROW {
crates/ql-io/src/qbook_format.rs:1577:                    detail: format!("row {} exceeds MAX_ROW {MAX_ROW}", rec.row),
crates/ql-io/src/qbook_format.rs:1580:            if rec.col > MAX_COLUMN {
crates/ql-io/src/qbook_format.rs:1584:                    detail: format!("col {} exceeds MAX_COLUMN {MAX_COLUMN}", rec.col),
crates/ql-io/src/qbook_format.rs:1698:    // so a corrupted / hand-edited TOML surfaces `MalformedTable`
crates/ql-io/src/qbook_format.rs:1703:                return Err(QbookError::MalformedTable {
crates/ql-io/src/qbook_format.rs:1709:                return Err(QbookError::MalformedTable {
crates/ql-io/src/qbook_format.rs:1716:            // edited / corrupted TOML with `top_row + rows - 1 > MAX_ROW`
crates/ql-io/src/qbook_format.rs:1720:            if let Err(reason) = ql_storage::TableMetadata::validate_footprint_bounds(
crates/ql-io/src/qbook_format.rs:1726:                return Err(QbookError::MalformedTable {
crates/ql-io/src/qbook_format.rs:1732:                return Err(QbookError::MalformedTable {
crates/ql-io/src/qbook_format.rs:1756:            if wb.tables().lookup(&canonical_upper).is_some() {
crates/ql-io/src/qbook_format.rs:1757:                return Err(QbookError::MalformedTable {
crates/ql-io/src/qbook_format.rs:1766:            // by the canonical-name comparison via `lookup_ci`.
crates/ql-io/src/qbook_format.rs:1767:            if wb.names().lookup_ci(&canonical_upper).is_some() {
crates/ql-io/src/qbook_format.rs:1768:                return Err(QbookError::MalformedTable {
crates/ql-io/src/qbook_format.rs:1774:            // unique canonical (lowercase) names, no `u32::MAX`
crates/ql-io/src/qbook_format.rs:1782:                        return Err(QbookError::MalformedTable {
crates/ql-io/src/qbook_format.rs:1787:                    if col.id == u32::MAX {
crates/ql-io/src/qbook_format.rs:1788:                        return Err(QbookError::MalformedTable {
crates/ql-io/src/qbook_format.rs:1790:                            reason: "column id cannot be u32::MAX (would overflow allocator)"
crates/ql-io/src/qbook_format.rs:1795:                        return Err(QbookError::MalformedTable {
crates/ql-io/src/qbook_format.rs:1807:                        return Err(QbookError::MalformedTable {
crates/ql-io/src/qbook_format.rs:2082:        // u32::MAX is well over Excel's MAX_ROW = 1_048_575.
crates/ql-io/src/qbook_format.rs:2092:                    detail.contains("MAX_ROW"),
crates/ql-io/src/qbook_format.rs:2093:                    "detail should mention MAX_ROW: {detail:?}"
crates/ql-io/src/qbook_format.rs:2096:            other => panic!("expected MalformedCell mentioning MAX_ROW, got {other:?}"),
crates/ql-io/src/qbook_format.rs:2106:        // u32::MAX > MAX_COLUMN = 16_383.
crates/ql-io/src/qbook_format.rs:2116:                    detail.contains("MAX_COLUMN"),
crates/ql-io/src/qbook_format.rs:2117:                    "detail should mention MAX_COLUMN: {detail:?}"
crates/ql-io/src/qbook_format.rs:2120:            other => panic!("expected MalformedCell mentioning MAX_COLUMN, got {other:?}"),
crates/ql-io/src/qbook_format.rs:2777:            loaded.names().lookup("TAXRATE"),
crates/ql-io/src/qbook_format.rs:2781:            loaded.names().lookup("ANCHORA1"),
crates/ql-io/src/qbook_format.rs:2785:            loaded.names().lookup("SALESRANGE"),
crates/ql-io/src/qbook_format.rs:2789:            loaded.names().lookup("PROFIT"),
crates/ql-io/src/qbook_format.rs:2874:            loaded.formats().lookup(ql_storage::FormatId(14)),
crates/ql-io/src/qbook_format.rs:2900:        assert_eq!(loaded.formats().lookup(id_a), Some("\"⚓\" #,##0.00"));
crates/ql-io/src/qbook_format.rs:2901:        assert_eq!(loaded.formats().lookup(id_b), Some("0.0000"));
crates/ql-io/src/qbook_format.rs:2904:            loaded.formats().lookup(ql_storage::FormatId(0)),
crates/ql-io/src/qbook_format.rs:2968:        assert_eq!(loaded.formats().lookup(loaded_id), Some("\"€\" #,##0.00"));
crates/ql-io/src/qbook_format.rs:2994:            loaded.formats().lookup(ql_storage::FormatId(0)),
crates/ql-io/src/qbook_format.rs:3000:                loaded.formats().lookup(ql_storage::FormatId(id)).is_none(),
crates/ql-io/src/qbook_format.rs:3046:        // MAX_ROW is u32::MAX in `ql_types`; we use a far-too-large row.
crates/ql-io/src/qbook_format.rs:3047:        let row_too_big = u32::MAX;
crates/ql-io/src/qbook_format.rs:3500:            loaded.names().lookup_ci("Rate"),
crates/ql-io/src/qbook_format.rs:3504:            loaded.sheet(s0).unwrap().scoped_names().lookup_ci("Rate"),
crates/ql-io/src/qbook_format.rs:3508:            loaded.sheet(s1).unwrap().scoped_names().lookup_ci("Bonus"),
crates/ql-io/src/qbook_format.rs:3516:            .lookup_ci("Rate")
crates/ql-io/src/qbook_format.rs:3522:            .lookup_ci("Bonus")
crates/ql-io/src/qbook_format.rs:3553:            loaded.names().lookup_ci("Legacy"),
crates/ql-io/src/qbook_format.rs:3617:            loaded.names().lookup_ci("R"),
crates/ql-io/src/qbook_format.rs:3920:            matches!(err, QbookError::MalformedTable { ref name, .. } if name == "PHANTOM"),
crates/ql-io/src/qbook_format.rs:3921:            "expected MalformedTable for PHANTOM, got {err:?}"
crates/ql-io/src/qbook_format.rs:3958:            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("columns length")),
crates/ql-io/src/qbook_format.rs:3959:            "expected MalformedTable about columns length, got {err:?}"
crates/ql-io/src/qbook_format.rs:3992:            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("> 0")),
crates/ql-io/src/qbook_format.rs:3993:            "expected MalformedTable about zero dims, got {err:?}"
crates/ql-io/src/qbook_format.rs:4004:        // top_row = 1_048_574, rows = 3 → last_row = 1_048_576 > MAX_ROW (1_048_575).
crates/ql-io/src/qbook_format.rs:4033:            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("MAX_ROW")),
crates/ql-io/src/qbook_format.rs:4034:            "expected MalformedTable about MAX_ROW, got {err:?}"
crates/ql-io/src/qbook_format.rs:4043:        // top_col = 16383, cols = 2 → last_col = 16384 > MAX_COLUMN (16383).
crates/ql-io/src/qbook_format.rs:4077:            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("MAX_COLUMN")),
crates/ql-io/src/qbook_format.rs:4078:            "expected MalformedTable about MAX_COLUMN, got {err:?}"
crates/ql-io/src/qbook_format.rs:4086:    // unchecked at u32::MAX) by surfacing the same shape as
crates/ql-io/src/qbook_format.rs:4139:            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("duplicate canonical table name")),
crates/ql-io/src/qbook_format.rs:4140:            "expected MalformedTable about duplicate canonical, got {err:?}"
crates/ql-io/src/qbook_format.rs:4183:            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("collides with a workbook-scoped defined name")),
crates/ql-io/src/qbook_format.rs:4184:            "expected MalformedTable about defined-name collision, got {err:?}"
crates/ql-io/src/qbook_format.rs:4248:            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("overlaps")),
crates/ql-io/src/qbook_format.rs:4249:            "expected MalformedTable about overlap, got {err:?}"
crates/ql-io/src/qbook_format.rs:4286:            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("cannot be empty")),
crates/ql-io/src/qbook_format.rs:4287:            "expected MalformedTable about empty column name, got {err:?}"
crates/ql-io/src/qbook_format.rs:4330:            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("duplicate canonical column name")),
crates/ql-io/src/qbook_format.rs:4331:            "expected MalformedTable about duplicate column, got {err:?}"
crates/ql-io/src/qbook_format.rs:4335:    /// **W5-126 / Codex MEDIUM-2:** persisted column id at u32::MAX
crates/ql-io/src/qbook_format.rs:4371:            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("u32::MAX")),
crates/ql-io/src/qbook_format.rs:4372:            "expected MalformedTable about u32::MAX column id, got {err:?}"
crates/ql-exec/src/workbook_runtime.rs:43:    ArrayValue, ColId, ErrorValue, EvalContext, RowId, SheetId, Value, MAX_COLUMN, MAX_ROW,
crates/ql-exec/src/workbook_runtime.rs:70:/// `Sheet::put` (which asserts `row <= MAX_ROW && col <= MAX_COLUMN`). The
crates/ql-exec/src/workbook_runtime.rs:119:    if row > MAX_ROW {
crates/ql-exec/src/workbook_runtime.rs:124:            why: "row exceeds MAX_ROW (1,048,575)",
crates/ql-exec/src/workbook_runtime.rs:127:    if col > MAX_COLUMN {
crates/ql-exec/src/workbook_runtime.rs:132:            why: "col exceeds MAX_COLUMN (16,383)",
crates/ql-exec/src/workbook_runtime.rs:161:    /// Phase 2A.7 audit H1: row/col outside the Excel grid (MAX_ROW = 1,048,575;
crates/ql-exec/src/workbook_runtime.rs:162:    /// MAX_COLUMN = 16,383). Surfaces at buffer time so `Sheet::put` doesn't
crates/ql-exec/src/workbook_runtime.rs:530:        // asserts `row <= MAX_ROW && col <= MAX_COLUMN`).
crates/ql-exec/src/workbook_runtime.rs:1062:        // `<= MAX_ROW / MAX_COLUMN`.
crates/ql-exec/src/workbook_runtime.rs:1068:            !matches!((end_row, end_col), (Some(r), Some(c)) if r <= MAX_ROW && c <= MAX_COLUMN);
crates/ql-exec/src/workbook_runtime.rs:1527:        // upper bound fits within MAX_ROW / MAX_COLUMN. Closes the gap
crates/ql-exec/src/workbook_runtime.rs:1531:            ql_storage::TableMetadata::validate_footprint_bounds(top_row, top_col, rows, cols)
crates/ql-exec/src/workbook_runtime.rs:1564:        if self.workbook.tables().lookup(&canonical).is_some() {
crates/ql-exec/src/workbook_runtime.rs:1570:        if self.workbook.names().lookup_ci(&canonical).is_some() {
crates/ql-exec/src/workbook_runtime.rs:1649:        if self.workbook.tables().lookup(&canonical).is_none() {
crates/ql-exec/src/workbook_runtime.rs:1679:        if self.workbook.tables().lookup(&old_canonical).is_none() {
crates/ql-exec/src/workbook_runtime.rs:1688:        if self.workbook.tables().lookup(&new_canonical).is_some() {
crates/ql-exec/src/workbook_runtime.rs:1694:        if self.workbook.names().lookup_ci(&new_canonical).is_some() {
crates/ql-exec/src/workbook_runtime.rs:1800:            .lookup(&table_canonical)
crates/ql-exec/src/workbook_runtime.rs:1961:                .lookup(&canonical)
crates/ql-exec/src/workbook_runtime.rs:2001:        if let Err(reason) = ql_storage::TableMetadata::validate_footprint_bounds(
crates/ql-exec/src/workbook_runtime.rs:2512:            if self.workbook.formats().lookup(fid).is_none() {
crates/ql-exec/src/workbook_runtime.rs:2586:                .lookup(format_id)
crates/ql-exec/src/workbook_runtime.rs:3501:        // MAX_ROW = 1,048,575 — anything above is invalid.
crates/ql-exec/src/workbook_runtime.rs:3513:            other => panic!("expected InvalidCell with row > MAX_ROW, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:3523:        // MAX_COLUMN = 16,383 — anything above is invalid.
crates/ql-exec/src/workbook_runtime.rs:3535:            other => panic!("expected InvalidCell with col > MAX_COLUMN, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:3557:        // Boundary case: exactly MAX_ROW and MAX_COLUMN are accepted.
crates/ql-exec/src/workbook_runtime.rs:3912:    fn set_name_uppercases_for_canonical_lookup() {
crates/ql-exec/src/workbook_runtime.rs:4801:                reg.lookup(name).is_some(),
crates/ql-exec/src/workbook_runtime.rs:4842:                reg.lookup(name).is_none(),
crates/ql-exec/src/workbook_runtime.rs:4859:                reg.lookup(name).is_none(),
crates/ql-exec/src/workbook_runtime.rs:4965:            wb.names().lookup_ci("TaxRate"),
crates/ql-exec/src/workbook_runtime.rs:5184:            replay_wb.names().lookup_ci("TaxRate"),
crates/ql-exec/src/workbook_runtime.rs:6974:            replay_wb.formats().lookup(custom_id),
crates/ql-exec/src/workbook_runtime.rs:7230:            wb.sheet(s0).unwrap().scoped_names().lookup_ci("R"),
crates/ql-exec/src/workbook_runtime.rs:7403:    //   5. Bounds: anchor + shape exceeds MAX_ROW/MAX_COLUMN → anchor
crates/ql-exec/src/workbook_runtime.rs:7575:    /// Phase 4.7.J.2 — out-of-bounds spill: anchor at MAX_ROW with a
crates/ql-exec/src/workbook_runtime.rs:7584:        // 3 rows starting from MAX_ROW → out of bounds.
crates/ql-exec/src/workbook_runtime.rs:7585:        let v = rt.set_formula(0, MAX_ROW, 0, "{1; 2; 3}");
crates/ql-exec/src/workbook_runtime.rs:7586:        // Anchor cell validation passes (MAX_ROW is in range). Eval
crates/ql-exec/src/workbook_runtime.rs:7590:        assert_eq!(wb.spill_anchor_at(0, MAX_ROW, 0), None);
crates/ql-exec/src/workbook_runtime.rs:8456:        // Anchor at MAX_ROW with 3-row vertical array → out of bounds.
crates/ql-exec/src/workbook_runtime.rs:8457:        wb.put_formula(0, MAX_ROW, 0, "{1; 2; 3}");
crates/ql-exec/src/workbook_runtime.rs:8464:            wb.read(ql_types::Address::new(0, MAX_ROW, 0)),
crates/ql-exec/src/workbook_runtime.rs:8467:        assert_eq!(wb.spill_anchor_at(0, MAX_ROW, 0), None);
crates/ql-exec/src/workbook_runtime.rs:9673:        use ql_types::MAX_ROW;
crates/ql-exec/src/workbook_runtime.rs:9677:        // top_row = MAX_ROW - 1, rows = 3 → last row = MAX_ROW + 1 > MAX_ROW.
crates/ql-exec/src/workbook_runtime.rs:9682:                MAX_ROW - 1,
crates/ql-exec/src/workbook_runtime.rs:9693:                assert!(reason.contains("MAX_ROW"), "reason: {reason}");
crates/ql-exec/src/workbook_runtime.rs:9701:        use ql_types::MAX_COLUMN;
crates/ql-exec/src/workbook_runtime.rs:9705:        // top_col = MAX_COLUMN, cols = 2 → last col = MAX_COLUMN + 1 > MAX_COLUMN.
crates/ql-exec/src/workbook_runtime.rs:9711:                MAX_COLUMN,
crates/ql-exec/src/workbook_runtime.rs:9721:                assert!(reason.contains("MAX_COLUMN"), "reason: {reason}");
crates/ql-exec/src/workbook_runtime.rs:9732:        // top_row = u32::MAX, rows = 2 → top_row + rows wraps. Pre-W5-125 the
crates/ql-exec/src/workbook_runtime.rs:9733:        // footprint loop `for r in u32::MAX..u32::MAX+2 (wrapped to 1)` was
crates/ql-exec/src/workbook_runtime.rs:9736:            .create_table("Wrap", 0, u32::MAX, 0, 2, 1, true, false, vec!["X".into()])
crates/ql-exec/src/workbook_runtime.rs:9751:        use ql_types::MAX_ROW;
crates/ql-exec/src/workbook_runtime.rs:9756:            // Create a table near (but inside) MAX_ROW.
crates/ql-exec/src/workbook_runtime.rs:9760:                MAX_ROW - 4,
crates/ql-exec/src/workbook_runtime.rs:9770:        // Grow to 10 rows → last_row = MAX_ROW - 4 + 9 = MAX_ROW + 5 > MAX_ROW.
crates/ql-exec/src/workbook_runtime.rs:9775:                assert!(reason.contains("MAX_ROW"), "reason: {reason}");
crates/ql-exec/src/workbook_runtime.rs:10614:    fn resize_table_exact_noop_emits_nothing() {

exec
/bin/zsh -lc 'git rev-parse HEAD && git status --short' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 788ms:
?? ../2026-05-11-222312-this-session-is-being-continued-from-a-previous-c.txt
?? ../2026-05-13-003338-this-session-is-being-continued-from-a-previous-c.txt
?? ../node_modules
?? .codex/
?? .plans/
?? 2026-05-14-222539-this-session-is-being-continued-from-a-previous-c.txt
?? docs/audits/2026-05-14-phase-4.7.H-codex-review.txt
?? docs/audits/2026-05-14-phase-4.7.H-sonnet-review.md
?? docs/audits/2026-05-15-phase-4.8-closing-megaudit-codex.md
?? docs/audits/2026-05-15-phase-4.8-closure-verify-codex.md

codex
`HEAD` matches the requested commit. There are unrelated untracked files in the workspace, including a prior-looking closure verify draft, so I’ll leave the worktree untouched and base this review on the tracked source at `HEAD`.
exec
/bin/zsh -lc "nl -ba crates/ql-storage/src/tables.rs | sed -n '220,390p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc "nl -ba crates/ql-oplog/src/replay.rs | sed -n '680,735p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   680	                    name: name.to_owned(),
   681	                })?;
   682	        (
   683	            meta.sheet,
   684	            meta.top_row,
   685	            meta.top_col,
   686	            meta.rows,
   687	            meta.cols,
   688	            meta.columns
   689	                .iter()
   690	                .map(|c| c.display.as_ref().to_owned())
   691	                .collect::<Vec<_>>(),
   692	        )
   693	    };
   694	    if new_rows == 0 || new_cols == 0 {
   695	        return Err(ReplayError::TableResizeRejected {
   696	            index,
   697	            name: name.to_owned(),
   698	            reason: "table rows and cols must both be > 0",
   699	        });
   700	    }
   701	    // **W5-125 (Phase 4.8.O.1 — Codex HIGH-1):** validate footprint
   702	    // upper bound mirrors producer side.
   703	    if let Err(reason) =
   704	        ql_storage::TableMetadata::validate_footprint_bounds(top_row, top_col, new_rows, new_cols)
   705	    {
   706	        return Err(ReplayError::TableResizeRejected {
   707	            index,
   708	            name: name.to_owned(),
   709	            reason,
   710	        });
   711	    }
   712	    let added_len = added_columns.len() as u32;
   713	    let removed_len = removed_columns.len() as u32;
   714	    if removed_len > old_cols {
   715	        return Err(ReplayError::TableResizeRejected {
   716	            index,
   717	            name: name.to_owned(),
   718	            reason: "removed_columns count exceeds existing column count",
   719	        });
   720	    }
   721	    // Arithmetic invariant.
   722	    if new_cols != old_cols + added_len - removed_len {
   723	        return Err(ReplayError::TableResizeRejected {
   724	            index,
   725	            name: name.to_owned(),
   726	            reason: "new_cols does not match old_cols + added - removed",
   727	        });
   728	    }
   729	    // removed_columns must exactly match trailing displays
   730	    // (case-insensitive).
   731	    let trailing_start = (old_cols - removed_len) as usize;
   732	    for (i, expected) in removed_columns.iter().enumerate() {
   733	        let idx = trailing_start + i;
   734	        if !old_displays[idx].eq_ignore_ascii_case(expected) {
   735	            return Err(ReplayError::TableResizeRejected {

 succeeded in 0ms:
   220	    }
   221	
   222	    /// **W5-125 (Phase 4.8.O.1 — closing-megaudit HIGH-1):** validate
   223	    /// that a footprint anchored at `(top_row, top_col)` with
   224	    /// `(rows, cols)` extent fits within the Excel address grid.
   225	    ///
   226	    /// Producer (`WorkbookRuntime::create_table` / `resize_table`),
   227	    /// replay (`apply_create_table` / `apply_resize_table`), and the
   228	    /// v6 `.qbook` loader all call this BEFORE entering any footprint
   229	    /// loop, which closes two failure modes Codex flagged in the
   230	    /// 4.8.O megaudit:
   231	    /// 1. `top_row + rows - 1 > MAX_ROW` (or same for col) — a table
   232	    ///    can register cells beyond the addressable grid, causing
   233	    ///    `Sheet::put` panics later on writes into those cells.
   234	    /// 2. `top_row + rows` overflows `u32` — the footprint loop's
   235	    ///    `for r in top_row..top_row + rows` becomes either an empty
   236	    ///    range (skipping all overlap / spill-anchor checks) or runs
   237	    ///    with wrapped bounds.
   238	    ///
   239	    /// Returns a `&'static str` reason on failure for caller-side
   240	    /// error wrapping (`RuntimeError::TableCreateRejected` /
   241	    /// `TableResizeRejected`, `ReplayError::*`, `QbookError::MalformedTable`).
   242	    ///
   243	    /// **Pre-condition:** `rows > 0 && cols > 0`. Callers enforce
   244	    /// this separately so they can emit the more specific
   245	    /// "rows and cols must both be > 0" error for the zero case.
   246	    pub fn validate_footprint_bounds(
   247	        top_row: RowId,
   248	        top_col: ColId,
   249	        rows: u32,
   250	        cols: u32,
   251	    ) -> Result<(), &'static str> {
   252	        debug_assert!(rows > 0 && cols > 0, "caller must enforce non-zero dims");
   253	        let last_row = top_row
   254	            .checked_add(rows - 1)
   255	            .ok_or("table footprint extends past addressable row range (u32 overflow)")?;
   256	        if last_row > MAX_ROW {
   257	            return Err("table footprint extends past MAX_ROW");
   258	        }
   259	        let last_col = top_col
   260	            .checked_add(cols - 1)
   261	            .ok_or("table footprint extends past addressable column range (u32 overflow)")?;
   262	        if last_col > MAX_COLUMN {
   263	            return Err("table footprint extends past MAX_COLUMN");
   264	        }
   265	        Ok(())
   266	    }
   267	}
   268	
   269	/// **W5-110 (Phase 4.8.A):** workbook-level table registry.
   270	///
   271	/// Mirrors [`crate::NameTable`]: HashMap-backed storage keyed by
   272	/// uppercase-canonical name + generation counter for plan-cache
   273	/// invalidation. The `next_column_id` allocator hands out monotonic
   274	/// per-column ids across the whole workbook (one counter, all tables
   275	/// share); future phases (4.10 calculated columns, Phase 5 column move)
   276	/// consume these ids.
   277	///
   278	/// **No mutation API in 4.8.A.** Direct `tables.insert` from the qbook
   279	/// loader is fine; product code mutates via `WorkbookRuntime::create_table`
   280	/// etc. (4.8.H+) which emit op-log entries.
   281	#[derive(Clone, Debug, Default)]
   282	pub struct TableTable {
   283	    tables: HashMap<Arc<str>, TableMetadata>,
   284	    /// Monotonic counter bumped on every successful mutation. Plan-cache
   285	    /// invalidation keys can include this so column renames / resizes
   286	    /// invalidate cached `ExprPlan::StructuredRef` entries. See design
   287	    /// § 7.5.
   288	    generation: u64,
   289	    /// Monotonic per-column-id allocator. Shared across all tables.
   290	    /// Allocated at column-create time; never reused.
   291	    next_column_id: u32,
   292	}
   293	
   294	impl TableTable {
   295	    pub fn new() -> Self {
   296	        Self::default()
   297	    }
   298	
   299	    /// Bind-plan cache invalidation counter (see [`NameTable::generation`]
   300	    /// for the pattern). Bumped on every successful mutation.
   301	    pub fn generation(&self) -> u64 {
   302	        self.generation
   303	    }
   304	
   305	    /// Case-insensitive lookup. Uppercases the query.
   306	    pub fn lookup(&self, name: &str) -> Option<&TableMetadata> {
   307	        let upper = name.to_ascii_uppercase();
   308	        self.tables.get(upper.as_str())
   309	    }
   310	
   311	    /// Reverse lookup: which table contains the given cell, if any?
   312	    /// O(num_tables) scan; tables-per-workbook is small (typically < 100).
   313	    ///
   314	    /// Used by:
   315	    /// - the binder for `[@Col]` resolution (which table is the formula in?).
   316	    /// - `create_table` / `resize_table` validation (does the proposed
   317	    ///   footprint overlap another table?).
   318	    pub fn table_at(&self, addr: Address) -> Option<&TableMetadata> {
   319	        self.tables.values().find(|t| t.contains(addr))
   320	    }
   321	
   322	    /// All table entries; HashMap-arbitrary order.
   323	    pub fn iter(&self) -> impl Iterator<Item = (&Arc<str>, &TableMetadata)> + '_ {
   324	        self.tables.iter()
   325	    }
   326	
   327	    pub fn len(&self) -> usize {
   328	        self.tables.len()
   329	    }
   330	
   331	    pub fn is_empty(&self) -> bool {
   332	        self.tables.is_empty()
   333	    }
   334	
   335	    /// **Loader / mutation-internal API.** Insert a table by canonical
   336	    /// uppercase name. The caller is responsible for:
   337	    /// - canonicalizing the key (uppercase) BEFORE calling.
   338	    /// - validating uniqueness, non-overlap, valid Excel-shaped name
   339	    ///   (design § 5.3) — `WorkbookRuntime::create_table` does this in
   340	    ///   4.8.H.
   341	    ///
   342	    /// Bumps the generation counter on success.
   343	    ///
   344	    /// **W5-123 (Phase 4.8.L):** also auto-bumps `next_column_id` past
   345	    /// the highest column id in `meta.columns`. The `.qbook` loader
   346	    /// (Phase 4.8.L persistence) preserves stable column ids verbatim;
   347	    /// without this self-maintenance the runtime allocator could
   348	    /// later hand out an id that collides with one already in storage.
   349	    pub fn insert(&mut self, canonical: Arc<str>, meta: TableMetadata) {
   350	        if let Some(max_id) = meta.columns.iter().map(|c| c.id).max() {
   351	            self.next_column_id = self.next_column_id.max(max_id + 1);
   352	        }
   353	        self.tables.insert(canonical, meta);
   354	        self.generation = self.generation.wrapping_add(1);
   355	    }
   356	
   357	    /// Remove a table by canonical name. Bumps generation iff a removal
   358	    /// occurred (idempotent no-op clears don't invalidate caches).
   359	    pub fn remove(&mut self, canonical: &str) -> Option<TableMetadata> {
   360	        let upper = canonical.to_ascii_uppercase();
   361	        let removed = self.tables.remove(upper.as_str());
   362	        if removed.is_some() {
   363	            self.generation = self.generation.wrapping_add(1);
   364	        }
   365	        removed
   366	    }
   367	
   368	    /// Allocate and return the next column id. Each call returns a fresh
   369	    /// id; the counter is monotonic across the workbook lifetime (per
   370	    /// design § 4.1). Wraps at u32::MAX via `wrapping_add`; in practice
   371	    /// 4 billion column allocations is unreachable.
   372	    pub fn allocate_column_id(&mut self) -> u32 {
   373	        let id = self.next_column_id;
   374	        self.next_column_id = self.next_column_id.wrapping_add(1);
   375	        id
   376	    }
   377	
   378	    /// Mutable access to a table by canonical name. Used by the loader
   379	    /// and by mutation methods in 4.8.H+. Bumps generation on mutation
   380	    /// is the CALLER's responsibility (since `&mut TableMetadata` lets
   381	    /// the caller see no-op mutations too).
   382	    pub fn get_mut(&mut self, canonical: &str) -> Option<&mut TableMetadata> {
   383	        let upper = canonical.to_ascii_uppercase();
   384	        self.tables.get_mut(upper.as_str())
   385	    }
   386	
   387	    /// Explicitly bump generation. For callers that mutated through
   388	    /// `get_mut` and need to invalidate caches.
   389	    pub fn bump_generation(&mut self) {
   390	        self.generation = self.generation.wrapping_add(1);

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '1508,1595p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1508	        rows: u32,
  1509	        cols: u32,
  1510	        has_header: bool,
  1511	        has_totals: bool,
  1512	        column_names: Vec<String>,
  1513	    ) -> Result<(), RuntimeError> {
  1514	        let canonical = name.to_ascii_uppercase();
  1515	        // ----- Validation (all checks BEFORE op-log append) -----
  1516	        let sheet_count = self.workbook.sheet_count();
  1517	        if self.workbook.sheet(sheet).is_none() {
  1518	            return Err(RuntimeError::InvalidSheet { sheet, sheet_count });
  1519	        }
  1520	        if rows == 0 || cols == 0 {
  1521	            return Err(RuntimeError::TableCreateRejected {
  1522	                name: name.to_owned(),
  1523	                reason: "table rows and cols must both be > 0",
  1524	            });
  1525	        }
  1526	        // **W5-125 (Phase 4.8.O.1 — Codex HIGH-1):** validate footprint
  1527	        // upper bound fits within MAX_ROW / MAX_COLUMN. Closes the gap
  1528	        // where `for r in top_row..top_row + rows` either ran beyond
  1529	        // the addressable grid or u32-overflowed silently.
  1530	        if let Err(reason) =
  1531	            ql_storage::TableMetadata::validate_footprint_bounds(top_row, top_col, rows, cols)
  1532	        {
  1533	            return Err(RuntimeError::TableCreateRejected {
  1534	                name: name.to_owned(),
  1535	                reason,
  1536	            });
  1537	        }
  1538	        if column_names.len() != cols as usize {
  1539	            return Err(RuntimeError::TableCreateRejected {
  1540	                name: name.to_owned(),
  1541	                reason: "column_names length does not match cols",
  1542	            });
  1543	        }
  1544	        if column_names.iter().any(|s| s.is_empty()) {
  1545	            return Err(RuntimeError::TableCreateRejected {
  1546	                name: name.to_owned(),
  1547	                reason: "table column names cannot be empty",
  1548	            });
  1549	        }
  1550	        // Column-name uniqueness (case-insensitive).
  1551	        {
  1552	            use std::collections::HashSet;
  1553	            let mut seen: HashSet<String> = HashSet::new();
  1554	            for cn in &column_names {
  1555	                if !seen.insert(cn.to_ascii_lowercase()) {
  1556	                    return Err(RuntimeError::TableCreateRejected {
  1557	                        name: name.to_owned(),
  1558	                        reason: "table column names must be unique (case-insensitive)",
  1559	                    });
  1560	                }
  1561	            }
  1562	        }
  1563	        // Name uniqueness (shared namespace: TableTable + NameTable).
  1564	        if self.workbook.tables().lookup(&canonical).is_some() {
  1565	            return Err(RuntimeError::TableCreateRejected {
  1566	                name: name.to_owned(),
  1567	                reason: "table with this canonical name already exists",
  1568	            });
  1569	        }
  1570	        if self.workbook.names().lookup_ci(&canonical).is_some() {
  1571	            return Err(RuntimeError::TableCreateRejected {
  1572	                name: name.to_owned(),
  1573	                reason: "defined-name with this canonical name already exists (shared namespace)",
  1574	            });
  1575	        }
  1576	        // Non-overlap + no spill anchor inside the footprint.
  1577	        // **W5-124 (Phase 4.8.J.2):** spill-anchor invariant § 4.3 #5 —
  1578	        // Excel canon: array formulas can't anchor inside a table.
  1579	        // Previously enforced only at `write_spill` time (the write-side
  1580	        // check consults `Workbook::table_at(anchor)`); creating a
  1581	        // table over an existing anchor was silently allowed, leaving
  1582	        // the table claiming a cell whose value is computed-from-spill.
  1583	        // Single fused loop avoids walking the proposed footprint twice.
  1584	        for r in top_row..top_row + rows {
  1585	            for c in top_col..top_col + cols {
  1586	                if self.workbook.table_at(sheet, r, c).is_some() {
  1587	                    return Err(RuntimeError::TableCreateRejected {
  1588	                        name: name.to_owned(),
  1589	                        reason: "table footprint overlaps an existing table",
  1590	                    });
  1591	                }
  1592	                if self.workbook.spill_anchor_at(sheet, r, c).is_some() {
  1593	                    return Err(RuntimeError::TableCreateRejected {
  1594	                        name: name.to_owned(),
  1595	                        reason: "table footprint contains a spill anchor",

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '1948,2078p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1948	        name: &str,
  1949	        new_rows: u32,
  1950	        new_cols: u32,
  1951	        added_columns: Vec<String>,
  1952	        removed_columns: Vec<String>,
  1953	    ) -> Result<(), RuntimeError> {
  1954	        use ql_storage::TableColumn;
  1955	        let canonical = name.to_ascii_uppercase();
  1956	        // Snapshot the immutable bits we need to validate.
  1957	        let (sheet, top_row, top_col, old_rows, old_cols, old_displays) = {
  1958	            let meta = self
  1959	                .workbook
  1960	                .tables()
  1961	                .lookup(&canonical)
  1962	                .ok_or_else(|| RuntimeError::TableNotFound(name.to_owned()))?;
  1963	            (
  1964	                meta.sheet,
  1965	                meta.top_row,
  1966	                meta.top_col,
  1967	                meta.rows,
  1968	                meta.cols,
  1969	                meta.columns
  1970	                    .iter()
  1971	                    .map(|c| c.display.as_ref().to_owned())
  1972	                    .collect::<Vec<_>>(),
  1973	            )
  1974	        };
  1975	        // **W5-127 (Phase 4.8.O.3 — Codex LOW-1):** exact no-op
  1976	        // short-circuit. Mirrors `rename_column`'s same-canonical
  1977	        // semantics. Avoids emitting an `Op::ResizeTable`, bumping
  1978	        // `TableTable::generation`, and clearing the plan cache when
  1979	        // nothing actually changes. The table is known to exist
  1980	        // (snapshot above would have errored). All dims match and no
  1981	        // columns are being added/removed, so there's nothing to
  1982	        // mutate.
  1983	        if new_rows == old_rows
  1984	            && new_cols == old_cols
  1985	            && added_columns.is_empty()
  1986	            && removed_columns.is_empty()
  1987	        {
  1988	            return Ok(());
  1989	        }
  1990	        if new_rows == 0 || new_cols == 0 {
  1991	            return Err(RuntimeError::TableResizeRejected {
  1992	                name: name.to_owned(),
  1993	                reason: "table rows and cols must both be > 0",
  1994	            });
  1995	        }
  1996	        // **W5-125 (Phase 4.8.O.1 — Codex HIGH-1):** validate footprint
  1997	        // upper bound. `top_row` + `top_col` are inherited from the
  1998	        // existing TableMetadata (so they were validated at create
  1999	        // time), but new_rows / new_cols can extend past the addressable
  2000	        // grid; check before the overlap / spill-anchor walk.
  2001	        if let Err(reason) = ql_storage::TableMetadata::validate_footprint_bounds(
  2002	            top_row, top_col, new_rows, new_cols,
  2003	        ) {
  2004	            return Err(RuntimeError::TableResizeRejected {
  2005	                name: name.to_owned(),
  2006	                reason,
  2007	            });
  2008	        }
  2009	        let added_len = added_columns.len() as u32;
  2010	        let removed_len = removed_columns.len() as u32;
  2011	        if removed_len > old_cols {
  2012	            return Err(RuntimeError::TableResizeRejected {
  2013	                name: name.to_owned(),
  2014	                reason: "removed_columns count exceeds existing column count",
  2015	            });
  2016	        }
  2017	        if new_cols != old_cols + added_len - removed_len {
  2018	            return Err(RuntimeError::TableResizeRejected {
  2019	                name: name.to_owned(),
  2020	                reason: "new_cols does not match old_cols + added - removed",
  2021	            });
  2022	        }
  2023	        // removed_columns must exactly match trailing displays
  2024	        // (case-insensitive).
  2025	        let trailing_start = (old_cols - removed_len) as usize;
  2026	        for (i, expected) in removed_columns.iter().enumerate() {
  2027	            let idx = trailing_start + i;
  2028	            if !old_displays[idx].eq_ignore_ascii_case(expected) {
  2029	                return Err(RuntimeError::TableResizeRejected {
  2030	                    name: name.to_owned(),
  2031	                    reason: "removed_columns do not match trailing columns",
  2032	                });
  2033	            }
  2034	        }
  2035	        if added_columns.iter().any(|s| s.is_empty()) {
  2036	            return Err(RuntimeError::TableResizeRejected {
  2037	                name: name.to_owned(),
  2038	                reason: "added column names cannot be empty",
  2039	            });
  2040	        }
  2041	        // Build the final canonical (lowercase) roster + check
  2042	        // uniqueness.
  2043	        let mut final_canon: Vec<String> = old_displays
  2044	            .iter()
  2045	            .take(trailing_start)
  2046	            .map(|d| d.to_ascii_lowercase())
  2047	            .collect();
  2048	        for a in &added_columns {
  2049	            final_canon.push(a.to_ascii_lowercase());
  2050	        }
  2051	        {
  2052	            use std::collections::HashSet;
  2053	            let mut seen: HashSet<&str> = HashSet::new();
  2054	            for cn in &final_canon {
  2055	                if !seen.insert(cn.as_str()) {
  2056	                    return Err(RuntimeError::TableResizeRejected {
  2057	                        name: name.to_owned(),
  2058	                        reason: "final column roster has duplicate canonical names",
  2059	                    });
  2060	                }
  2061	            }
  2062	        }
  2063	        // Footprint-overlap + no-spill-anchor checks: only cells NEWLY
  2064	        // claimed need checking (cells in the OLD footprint already
  2065	        // belong to this table; create_table verified them at creation
  2066	        // time).
  2067	        // **W5-124 (Phase 4.8.J.2):** spill-anchor check mirrors
  2068	        // create_table's, restricted to newly-claimed cells.
  2069	        let old_end_row = top_row + old_rows;
  2070	        let old_end_col = top_col + old_cols;
  2071	        let new_end_row = top_row + new_rows;
  2072	        let new_end_col = top_col + new_cols;
  2073	        for r in top_row..new_end_row {
  2074	            for c in top_col..new_end_col {
  2075	                let inside_old = r < old_end_row && c < old_end_col;
  2076	                if inside_old {
  2077	                    continue;
  2078	                }

exec
/bin/zsh -lc "nl -ba crates/ql-storage/src/tables.rs | sed -n '500,745p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc "nl -ba crates/ql-io/src/qbook_format.rs | sed -n '1688,1825p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc "nl -ba crates/ql-oplog/src/replay.rs | sed -n '735,930p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   500	        let (idx3, _) = t.lookup_column("Qty").unwrap();
   501	        assert_eq!(idx3, 2);
   502	    }
   503	
   504	    #[test]
   505	    fn lookup_column_returns_none_for_missing() {
   506	        let t = sales_table();
   507	        assert!(t.lookup_column("Foobar").is_none());
   508	    }
   509	
   510	    #[test]
   511	    fn contains_checks_full_footprint() {
   512	        let t = sales_table();
   513	        // Inside (row 5, col 2 — data area).
   514	        assert!(t.contains(Address::new(0, 5, 2)));
   515	        // Header row.
   516	        assert!(t.contains(Address::new(0, 0, 0)));
   517	        // Totals row.
   518	        assert!(t.contains(Address::new(0, 9, 3)));
   519	        // Outside col.
   520	        assert!(!t.contains(Address::new(0, 5, 4)));
   521	        // Outside row.
   522	        assert!(!t.contains(Address::new(0, 10, 0)));
   523	        // Different sheet.
   524	        assert!(!t.contains(Address::new(1, 5, 2)));
   525	    }
   526	
   527	    #[test]
   528	    fn table_table_insert_lookup_round_trip() {
   529	        let mut tt = TableTable::new();
   530	        let t = sales_table();
   531	        tt.insert(Arc::clone(&t.name), t.clone());
   532	        assert_eq!(tt.lookup("Sales"), Some(&t));
   533	        assert_eq!(tt.lookup("SALES"), Some(&t));
   534	        assert_eq!(tt.lookup("sales"), Some(&t));
   535	        assert_eq!(tt.lookup("Other"), None);
   536	        assert_eq!(tt.len(), 1);
   537	        assert!(!tt.is_empty());
   538	    }
   539	
   540	    #[test]
   541	    fn table_table_generation_bumps_on_mutation() {
   542	        let mut tt = TableTable::new();
   543	        let g0 = tt.generation();
   544	        tt.insert(Arc::from("FOO"), sales_table());
   545	        let g1 = tt.generation();
   546	        assert_ne!(g0, g1);
   547	        tt.remove("FOO");
   548	        let g2 = tt.generation();
   549	        assert_ne!(g1, g2);
   550	        // Idempotent remove doesn't bump.
   551	        tt.remove("FOO");
   552	        assert_eq!(g2, tt.generation());
   553	    }
   554	
   555	    #[test]
   556	    fn allocate_column_id_is_monotonic() {
   557	        let mut tt = TableTable::new();
   558	        assert_eq!(tt.allocate_column_id(), 0);
   559	        assert_eq!(tt.allocate_column_id(), 1);
   560	        assert_eq!(tt.allocate_column_id(), 2);
   561	    }
   562	
   563	    /// **W5-123 (Phase 4.8.L):** `insert` auto-bumps `next_column_id`
   564	    /// past any column id in the inserted metadata. Without this, the
   565	    /// `.qbook` loader (which preserves persisted column ids verbatim)
   566	    /// would let `allocate_column_id` later hand out an id that
   567	    /// collides with one already in storage.
   568	    #[test]
   569	    fn insert_bumps_next_column_id_past_loaded_ids() {
   570	        let mut tt = TableTable::new();
   571	        // Simulate loader inserting a table whose columns have ids
   572	        // [10, 20, 30] (allocated in a previous session).
   573	        let canonical: Arc<str> = Arc::from("LOADED");
   574	        tt.insert(
   575	            canonical,
   576	            TableMetadata {
   577	                name: Arc::from("LOADED"),
   578	                display_name: Arc::from("Loaded"),
   579	                sheet: 0,
   580	                top_row: 0,
   581	                top_col: 0,
   582	                rows: 2,
   583	                cols: 3,
   584	                has_header: true,
   585	                has_totals: false,
   586	                columns: vec![col(10, "A"), col(20, "B"), col(30, "C")],
   587	            },
   588	        );
   589	        // Next allocator call must return 31 (or higher), NEVER ≤ 30.
   590	        let next = tt.allocate_column_id();
   591	        assert_eq!(next, 31, "expected allocator to skip past id 30");
   592	    }
   593	
   594	    /// Two `insert` calls: the second table's column ids are LOWER than
   595	    /// the first's. The allocator must STAY at the higher water mark.
   596	    #[test]
   597	    fn insert_preserves_high_water_across_multiple_inserts() {
   598	        let mut tt = TableTable::new();
   599	        tt.insert(
   600	            Arc::from("A"),
   601	            TableMetadata {
   602	                name: Arc::from("A"),
   603	                display_name: Arc::from("A"),
   604	                sheet: 0,
   605	                top_row: 0,
   606	                top_col: 0,
   607	                rows: 1,
   608	                cols: 1,
   609	                has_header: false,
   610	                has_totals: false,
   611	                columns: vec![col(100, "X")],
   612	            },
   613	        );
   614	        tt.insert(
   615	            Arc::from("B"),
   616	            TableMetadata {
   617	                name: Arc::from("B"),
   618	                display_name: Arc::from("B"),
   619	                sheet: 0,
   620	                top_row: 0,
   621	                top_col: 5,
   622	                rows: 1,
   623	                cols: 1,
   624	                has_header: false,
   625	                has_totals: false,
   626	                columns: vec![col(5, "Y")],
   627	            },
   628	        );
   629	        assert_eq!(
   630	            tt.allocate_column_id(),
   631	            101,
   632	            "allocator must stay past highest seen (100)"
   633	        );
   634	    }
   635	
   636	    #[test]
   637	    fn insert_empty_columns_does_not_change_next_column_id() {
   638	        let mut tt = TableTable::new();
   639	        // Pre-bump to 5.
   640	        for _ in 0..5 {
   641	            let _ = tt.allocate_column_id();
   642	        }
   643	        tt.insert(
   644	            Arc::from("EMPTY"),
   645	            TableMetadata {
   646	                name: Arc::from("EMPTY"),
   647	                display_name: Arc::from("Empty"),
   648	                sheet: 0,
   649	                top_row: 0,
   650	                top_col: 0,
   651	                rows: 1,
   652	                cols: 0,
   653	                has_header: false,
   654	                has_totals: false,
   655	                columns: vec![],
   656	            },
   657	        );
   658	        // Should still hand out 5 next.
   659	        assert_eq!(tt.allocate_column_id(), 5);
   660	    }
   661	
   662	    #[test]
   663	    fn table_at_finds_containing_table() {
   664	        let mut tt = TableTable::new();
   665	        tt.insert(Arc::from("SALES"), sales_table());
   666	        // Header cell.
   667	        assert_eq!(
   668	            tt.table_at(Address::new(0, 0, 0)).map(|t| t.name.as_ref()),
   669	            Some("SALES")
   670	        );
   671	        // Data cell.
   672	        assert_eq!(
   673	            tt.table_at(Address::new(0, 5, 2)).map(|t| t.name.as_ref()),
   674	            Some("SALES")
   675	        );
   676	        // Outside.
   677	        assert!(tt.table_at(Address::new(0, 100, 0)).is_none());
   678	        assert!(tt.table_at(Address::new(1, 0, 0)).is_none());
   679	    }
   680	
   681	    // ===== W5-125 (Phase 4.8.O.1) — footprint-bounds helper =====
   682	
   683	    #[test]
   684	    fn validate_footprint_bounds_accepts_in_range_footprint() {
   685	        // Pin the happy path: a small table at (0, 0).
   686	        assert!(TableMetadata::validate_footprint_bounds(0, 0, 3, 2).is_ok());
   687	        // Exactly at MAX_ROW / MAX_COLUMN.
   688	        assert!(TableMetadata::validate_footprint_bounds(MAX_ROW, MAX_COLUMN, 1, 1).is_ok());
   689	        assert!(TableMetadata::validate_footprint_bounds(MAX_ROW - 1, 0, 2, 1).is_ok());
   690	    }
   691	
   692	    #[test]
   693	    fn validate_footprint_bounds_rejects_past_max_row() {
   694	        // top_row + rows - 1 = MAX_ROW + 1 → past the grid.
   695	        let err = TableMetadata::validate_footprint_bounds(MAX_ROW - 1, 0, 3, 1).unwrap_err();
   696	        assert!(err.contains("MAX_ROW"), "reason: {err}");
   697	        // Far over.
   698	        let err2 = TableMetadata::validate_footprint_bounds(MAX_ROW, 0, 100, 1).unwrap_err();
   699	        assert!(err2.contains("MAX_ROW"), "reason: {err2}");
   700	    }
   701	
   702	    #[test]
   703	    fn validate_footprint_bounds_rejects_past_max_column() {
   704	        let err = TableMetadata::validate_footprint_bounds(0, MAX_COLUMN - 1, 1, 3).unwrap_err();
   705	        assert!(err.contains("MAX_COLUMN"), "reason: {err}");
   706	    }
   707	
   708	    #[test]
   709	    fn validate_footprint_bounds_rejects_u32_overflow_on_rows() {
   710	        // top_row + rows - 1 overflows u32 — the loop's `top_row + rows`
   711	        // expression would wrap silently before this fix.
   712	        let err = TableMetadata::validate_footprint_bounds(u32::MAX, 0, 2, 1).unwrap_err();
   713	        assert!(
   714	            err.contains("u32 overflow") || err.contains("addressable row"),
   715	            "reason: {err}"
   716	        );
   717	    }
   718	
   719	    #[test]
   720	    fn validate_footprint_bounds_rejects_u32_overflow_on_cols() {
   721	        let err = TableMetadata::validate_footprint_bounds(0, u32::MAX, 1, 2).unwrap_err();
   722	        assert!(
   723	            err.contains("u32 overflow") || err.contains("addressable column"),
   724	            "reason: {err}"
   725	        );
   726	    }
   727	}

 succeeded in 0ms:
  1688	    // so a `None` envelope tables field is the regression-neutral path
  1689	    // (loader leaves `TableTable::default()`).
  1690	    //
  1691	    // Direct `tables_mut().insert(...)` bypasses `WorkbookRuntime::
  1692	    // create_table` validation; that's the intentional loader pattern
  1693	    // (matches how `Workbook::add_sheet` etc. are called bypassing
  1694	    // op-log emission). Per design § 4.3 invariants 1-4 the persisted
  1695	    // shape is already legal (it was validated at create / resize
  1696	    // time on a prior session). We do enforce structural shape
  1697	    // (non-zero dims, columns length matches cols, sheet id exists)
  1698	    // so a corrupted / hand-edited TOML surfaces `MalformedTable`
  1699	    // rather than panicking downstream.
  1700	    if let Some(tables_section) = envelope.tables {
  1701	        for entry in tables_section.entries {
  1702	            if wb.sheet(entry.sheet).is_none() {
  1703	                return Err(QbookError::MalformedTable {
  1704	                    name: entry.name.clone(),
  1705	                    reason: format!("references unknown sheet id {}", entry.sheet),
  1706	                });
  1707	            }
  1708	            if entry.rows == 0 || entry.cols == 0 {
  1709	                return Err(QbookError::MalformedTable {
  1710	                    name: entry.name.clone(),
  1711	                    reason: "table rows and cols must both be > 0".into(),
  1712	                });
  1713	            }
  1714	            // **W5-125 (Phase 4.8.O.1 — Codex HIGH-1):** validate
  1715	            // footprint upper bound mirrors producer + replay. A hand-
  1716	            // edited / corrupted TOML with `top_row + rows - 1 > MAX_ROW`
  1717	            // would otherwise let a table register cells beyond the
  1718	            // addressable grid, and a u32-overflowing footprint would
  1719	            // skip downstream checks.
  1720	            if let Err(reason) = ql_storage::TableMetadata::validate_footprint_bounds(
  1721	                entry.top_row,
  1722	                entry.top_col,
  1723	                entry.rows,
  1724	                entry.cols,
  1725	            ) {
  1726	                return Err(QbookError::MalformedTable {
  1727	                    name: entry.name.clone(),
  1728	                    reason: reason.to_owned(),
  1729	                });
  1730	            }
  1731	            if entry.columns.len() != entry.cols as usize {
  1732	                return Err(QbookError::MalformedTable {
  1733	                    name: entry.name.clone(),
  1734	                    reason: format!(
  1735	                        "columns length {} does not match cols {}",
  1736	                        entry.columns.len(),
  1737	                        entry.cols
  1738	                    ),
  1739	                });
  1740	            }
  1741	            // **W5-126 (Phase 4.8.O.2 — Codex MEDIUM-1):** re-validate
  1742	            // the runtime registry invariants at load time. The W5-123
  1743	            // docstring said "loader trusts the file" — Codex flagged
  1744	            // that a hand-edited / corrupted v6 TOML could enter the
  1745	            // engine with state that runtime mutation wouldn't allow.
  1746	            // Concrete failure modes: silent duplicate-name overwrite
  1747	            // (leaks column ids into the allocator); namespace
  1748	            // collision with NameTable; overlapping table footprints
  1749	            // (HashMap-order indeterminism in `Workbook::table_at`);
  1750	            // duplicate or empty column names breaking `lookup_column`.
  1751	            let canonical_upper = entry.name.to_ascii_uppercase();
  1752	            // Name uniqueness against already-loaded tables (catches
  1753	            // duplicate canonical names within the same TablesSection
  1754	            // entries vec, since each insert lands in `wb.tables_mut()`
  1755	            // before the next iteration).
  1756	            if wb.tables().lookup(&canonical_upper).is_some() {
  1757	                return Err(QbookError::MalformedTable {
  1758	                    name: entry.name.clone(),
  1759	                    reason: "duplicate canonical table name in [[tables.entries]]".into(),
  1760	                });
  1761	            }
  1762	            // Shared namespace with NameTable (workbook + sheet-scoped).
  1763	            // NameTable hydration ran above, so any prior conflicts are
  1764	            // already in `wb.names()` / sheet scoped_names. We check
  1765	            // workbook-scoped only; sheet-scoped collisions are caught
  1766	            // by the canonical-name comparison via `lookup_ci`.
  1767	            if wb.names().lookup_ci(&canonical_upper).is_some() {
  1768	                return Err(QbookError::MalformedTable {
  1769	                    name: entry.name.clone(),
  1770	                    reason: "table name collides with a workbook-scoped defined name".into(),
  1771	                });
  1772	            }
  1773	            // Column-level shape: non-empty `name` AND `display`,
  1774	            // unique canonical (lowercase) names, no `u32::MAX`
  1775	            // column ids (closes Codex MEDIUM-2 — `TableTable::insert`
  1776	            // does unchecked `max_id + 1` on the column-id allocator).
  1777	            {
  1778	                use std::collections::HashSet;
  1779	                let mut seen_canon: HashSet<String> = HashSet::new();
  1780	                for col in &entry.columns {
  1781	                    if col.name.is_empty() || col.display.is_empty() {
  1782	                        return Err(QbookError::MalformedTable {
  1783	                            name: entry.name.clone(),
  1784	                            reason: "table column names cannot be empty".into(),
  1785	                        });
  1786	                    }
  1787	                    if col.id == u32::MAX {
  1788	                        return Err(QbookError::MalformedTable {
  1789	                            name: entry.name.clone(),
  1790	                            reason: "column id cannot be u32::MAX (would overflow allocator)"
  1791	                                .into(),
  1792	                        });
  1793	                    }
  1794	                    if !seen_canon.insert(col.name.to_ascii_lowercase()) {
  1795	                        return Err(QbookError::MalformedTable {
  1796	                            name: entry.name.clone(),
  1797	                            reason: "duplicate canonical column name within table".into(),
  1798	                        });
  1799	                    }
  1800	                }
  1801	            }
  1802	            // Footprint non-overlap against tables already inserted in
  1803	            // earlier loop iterations.
  1804	            for r in entry.top_row..entry.top_row + entry.rows {
  1805	                for c in entry.top_col..entry.top_col + entry.cols {
  1806	                    if wb.table_at(entry.sheet, r, c).is_some() {
  1807	                        return Err(QbookError::MalformedTable {
  1808	                            name: entry.name.clone(),
  1809	                            reason: "table footprint overlaps a previously loaded table".into(),
  1810	                        });
  1811	                    }
  1812	                }
  1813	            }
  1814	            let canonical: std::sync::Arc<str> = std::sync::Arc::from(entry.name.as_str());
  1815	            let columns: Vec<ql_storage::TableColumn> = entry
  1816	                .columns
  1817	                .into_iter()
  1818	                .map(|c| ql_storage::TableColumn {
  1819	                    id: c.id,
  1820	                    name: std::sync::Arc::from(c.name.as_str()),
  1821	                    display: std::sync::Arc::from(c.display.as_str()),
  1822	                    totals_function: c.totals_function.map(TotalsFunctionWire::to_runtime),
  1823	                })
  1824	                .collect();
  1825	            let meta = ql_storage::TableMetadata {

 succeeded in 0ms:
   735	            return Err(ReplayError::TableResizeRejected {
   736	                index,
   737	                name: name.to_owned(),
   738	                reason: "removed_columns do not match trailing columns",
   739	            });
   740	        }
   741	    }
   742	    // added_columns: non-empty + unique vs final roster.
   743	    if added_columns.iter().any(|s| s.is_empty()) {
   744	        return Err(ReplayError::TableResizeRejected {
   745	            index,
   746	            name: name.to_owned(),
   747	            reason: "added column names cannot be empty",
   748	        });
   749	    }
   750	    // Build the final canonical (lowercase) roster.
   751	    let mut final_canon: Vec<String> = old_displays
   752	        .iter()
   753	        .take(trailing_start)
   754	        .map(|d| d.to_ascii_lowercase())
   755	        .collect();
   756	    for a in added_columns {
   757	        final_canon.push(a.to_ascii_lowercase());
   758	    }
   759	    {
   760	        use std::collections::HashSet;
   761	        let mut seen: HashSet<&str> = HashSet::new();
   762	        for cn in &final_canon {
   763	            if !seen.insert(cn.as_str()) {
   764	                return Err(ReplayError::TableResizeRejected {
   765	                    index,
   766	                    name: name.to_owned(),
   767	                    reason: "final column roster has duplicate canonical names",
   768	                });
   769	            }
   770	        }
   771	    }
   772	    // Footprint-overlap check: only cells NEWLY claimed by this resize
   773	    // need checking. Old-footprint cells already belong to this table.
   774	    // We test all cells in [top_row..top_row+new_rows] x [top_col..
   775	    // top_col+new_cols] and skip those inside the OLD footprint.
   776	    let old_end_row = top_row + old_rows;
   777	    let old_end_col = top_col + old_cols;
   778	    let new_end_row = top_row + new_rows;
   779	    let new_end_col = top_col + new_cols;
   780	    for r in top_row..new_end_row {
   781	        for c in top_col..new_end_col {
   782	            let inside_old = r < old_end_row && c < old_end_col;
   783	            if inside_old {
   784	                continue;
   785	            }
   786	            if let Some(other) = workbook.table_at(sheet, r, c) {
   787	                if !other.name.eq_ignore_ascii_case(&canonical) {
   788	                    return Err(ReplayError::TableResizeRejected {
   789	                        index,
   790	                        name: name.to_owned(),
   791	                        reason: "new footprint overlaps an existing table",
   792	                    });
   793	                }
   794	            }
   795	            // **W5-124 (Phase 4.8.J.2):** spill-anchor check mirrors
   796	            // producer side. Per design § 4.3 invariant #5, tables
   797	            // block ALL spill anchors inside their footprint.
   798	            if workbook.spill_anchor_at(sheet, r, c).is_some() {
   799	                return Err(ReplayError::TableResizeRejected {
   800	                    index,
   801	                    name: name.to_owned(),
   802	                    reason: "new footprint contains a spill anchor",
   803	                });
   804	            }
   805	        }
   806	    }
   807	    // ----- Mutation -----
   808	    // Allocate new column ids BEFORE taking &mut on the table entry
   809	    // (allocate_column_id lives on TableTable not TableMetadata).
   810	    let new_ids: Vec<u32> = (0..added_columns.len())
   811	        .map(|_| workbook.tables_mut().allocate_column_id())
   812	        .collect();
   813	    let meta = workbook
   814	        .tables_mut()
   815	        .get_mut(&canonical)
   816	        .expect("verified at top");
   817	    meta.rows = new_rows;
   818	    meta.cols = new_cols;
   819	    meta.columns
   820	        .truncate(meta.columns.len() - removed_len as usize);
   821	    for (cn, id) in added_columns.iter().zip(new_ids) {
   822	        meta.columns.push(TableColumn {
   823	            id,
   824	            name: std::sync::Arc::from(cn.to_ascii_lowercase().as_str()),
   825	            display: std::sync::Arc::from(cn.as_str()),
   826	            totals_function: None,
   827	        });
   828	    }
   829	    workbook.tables_mut().bump_generation();
   830	    Ok(())
   831	}
   832	
   833	/// **W5-118 (Phase 4.8.H):** apply a `CreateTable` op against the
   834	/// workbook. Validates the same invariants the producer side checks
   835	/// (mirror via `WorkbookRuntime::create_table`).
   836	#[allow(clippy::too_many_arguments)]
   837	fn apply_create_table(
   838	    workbook: &mut Workbook,
   839	    index: usize,
   840	    name: &str,
   841	    sheet: SheetId,
   842	    top_row: RowId,
   843	    top_col: ColId,
   844	    rows: u32,
   845	    cols: u32,
   846	    has_header: bool,
   847	    has_totals: bool,
   848	    column_names: &[String],
   849	) -> Result<(), ReplayError> {
   850	    use ql_storage::{TableColumn, TableMetadata};
   851	    let canonical: std::sync::Arc<str> = std::sync::Arc::from(name.to_ascii_uppercase().as_str());
   852	    if workbook.tables().lookup(&canonical).is_some() {
   853	        return Err(ReplayError::TableCreateRejected {
   854	            index,
   855	            name: name.to_owned(),
   856	            reason: "table with this canonical name already exists",
   857	        });
   858	    }
   859	    if workbook.names().lookup_ci(&canonical).is_some() {
   860	        return Err(ReplayError::TableCreateRejected {
   861	            index,
   862	            name: name.to_owned(),
   863	            reason: "defined-name with this canonical name already exists (shared namespace)",
   864	        });
   865	    }
   866	    if column_names.len() != cols as usize {
   867	        return Err(ReplayError::TableCreateRejected {
   868	            index,
   869	            name: name.to_owned(),
   870	            reason: "column_names length does not match cols",
   871	        });
   872	    }
   873	    if column_names.iter().any(|s| s.is_empty()) {
   874	        return Err(ReplayError::TableCreateRejected {
   875	            index,
   876	            name: name.to_owned(),
   877	            reason: "table column names cannot be empty",
   878	        });
   879	    }
   880	    // Column-name uniqueness (case-insensitive).
   881	    {
   882	        use std::collections::HashSet;
   883	        let mut seen: HashSet<String> = HashSet::new();
   884	        for cn in column_names {
   885	            if !seen.insert(cn.to_ascii_lowercase()) {
   886	                return Err(ReplayError::TableCreateRejected {
   887	                    index,
   888	                    name: name.to_owned(),
   889	                    reason: "table column names must be unique (case-insensitive)",
   890	                });
   891	            }
   892	        }
   893	    }
   894	    if workbook.sheet(sheet).is_none() {
   895	        return Err(ReplayError::TableCreateRejected {
   896	            index,
   897	            name: name.to_owned(),
   898	            reason: "table references unknown sheet",
   899	        });
   900	    }
   901	    if rows == 0 || cols == 0 {
   902	        return Err(ReplayError::TableCreateRejected {
   903	            index,
   904	            name: name.to_owned(),
   905	            reason: "table rows and cols must both be > 0",
   906	        });
   907	    }
   908	    // **W5-125 (Phase 4.8.O.1 — Codex HIGH-1):** validate footprint
   909	    // upper bound mirrors producer side.
   910	    if let Err(reason) =
   911	        ql_storage::TableMetadata::validate_footprint_bounds(top_row, top_col, rows, cols)
   912	    {
   913	        return Err(ReplayError::TableCreateRejected {
   914	            index,
   915	            name: name.to_owned(),
   916	            reason,
   917	        });
   918	    }
   919	    // Non-overlap + no-spill-anchor inside the footprint.
   920	    // **W5-124 (Phase 4.8.J.2):** spill-anchor check mirrors producer
   921	    // side per design § 4.3 invariant #5.
   922	    for r in top_row..top_row + rows {
   923	        for c in top_col..top_col + cols {
   924	            if workbook.table_at(sheet, r, c).is_some() {
   925	                return Err(ReplayError::TableCreateRejected {
   926	                    index,
   927	                    name: name.to_owned(),
   928	                    reason: "table footprint overlaps an existing table",
   929	                });
   930	            }

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '10600,10645p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
 10600	            } if name == "SALES"
 10601	                && *new_rows == 5
 10602	                && *new_cols == 2
 10603	                && added_columns == &vec!["Price".to_owned()]
 10604	                && removed_columns.is_empty()
 10605	        ));
 10606	    }
 10607	
 10608	    /// **W5-127 (Phase 4.8.O.3 — Codex LOW-1):** exact no-op resize
 10609	    /// (same dims, no column changes) returns `Ok(())` without
 10610	    /// emitting an `Op::ResizeTable`. Mirrors `rename_column`'s
 10611	    /// same-canonical no-op contract — caller-visible behavior is
 10612	    /// unchanged but the op log stays compact.
 10613	    #[test]
 10614	    fn resize_table_exact_noop_emits_nothing() {
 10615	        let mut wb = make_runtime_workbook();
 10616	        let reg = default_registry();
 10617	        let mut oplog = OpLog::new();
 10618	        {
 10619	            let mut rt = WorkbookRuntime::with_oplog(&mut wb, &reg, &mut oplog);
 10620	            rt.create_table(
 10621	                "Sales",
 10622	                0,
 10623	                0,
 10624	                0,
 10625	                3,
 10626	                2,
 10627	                true,
 10628	                false,
 10629	                vec!["Qty".into(), "Price".into()],
 10630	            )
 10631	            .unwrap();
 10632	            // Exact no-op: same dims, empty added + removed.
 10633	            rt.resize_table("Sales", 3, 2, vec![], vec![])
 10634	                .expect("exact no-op must succeed silently");
 10635	        }
 10636	        let ops: Vec<Op> = oplog.iter().collect::<Result<_, _>>().unwrap();
 10637	        // Only the CreateTable op landed; no ResizeTable emitted.
 10638	        assert_eq!(ops.len(), 1, "got {ops:?}");
 10639	        assert!(matches!(&ops[0], Op::CreateTable { .. }));
 10640	        // Sanity: table metadata unchanged.
 10641	        let meta = wb.lookup_table("Sales").unwrap();
 10642	        assert_eq!(meta.rows, 3);
 10643	        assert_eq!(meta.cols, 2);
 10644	        assert_eq!(meta.columns.len(), 2);
 10645	    }

codex
The main HIGH fix is present structurally: the helper uses `checked_add`, and producer/replay/load call it before the affected footprint walks. I’m now checking the load-side validations and the tests that make those paths reachable through normal APIs.
exec
/bin/zsh -lc "nl -ba crates/ql-io/src/qbook_format.rs | sed -n '1825,1855p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1825	            let meta = ql_storage::TableMetadata {
  1826	                name: std::sync::Arc::clone(&canonical),
  1827	                display_name: std::sync::Arc::from(entry.display_name.as_str()),
  1828	                sheet: entry.sheet,
  1829	                top_row: entry.top_row,
  1830	                top_col: entry.top_col,
  1831	                rows: entry.rows,
  1832	                cols: entry.cols,
  1833	                has_header: entry.has_header,
  1834	                has_totals: entry.has_totals,
  1835	                columns,
  1836	            };
  1837	            // `insert` auto-bumps `next_column_id` past any persisted
  1838	            // column id (added in 4.8.L to keep the loader-side ids
  1839	            // collision-free with future runtime allocations).
  1840	            wb.tables_mut().insert(canonical, meta);
  1841	        }
  1842	    }
  1843	
  1844	    // Phase 2A.13 audit cycle-3 H4: warn once per load if we rewrote any v1
  1845	    // `Error("#NULL!") + formula` cells to Pending. The rewrite is correct
  1846	    // for engine-produced files (where #NULL! was a stand-in for "not yet
  1847	    // evaluated") but COULD silently corrupt legitimate Excel #NULL! results
  1848	    // — the v1 wire format conflated both cases. The user should re-check
  1849	    // any formulas that legitimately produce #NULL! after this load.
  1850	    if legacy_pending_migrations > 0 {
  1851	        eprintln!(
  1852	            "warning: load_workbook {path:?} migrated {legacy_pending_migrations} v1 \
  1853	             `Error(#NULL!) + formula` cells to the v2 Pending semantic. \
  1854	             If any of those formulas were authored to produce #NULL! intentionally \
  1855	             (e.g. `=SUM(A1 B1)` with a space), their evaluated values will be lost \

exec
/bin/zsh -lc "nl -ba crates/ql-io/src/qbook_format.rs | sed -n '3988,4380p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  3988	        fs::write(path.join("workbook.toml"), toml).unwrap();
  3989	        fs::write(path.join("sheets").join("0.jsonl"), "").unwrap();
  3990	        let err = load_workbook(&path).unwrap_err();
  3991	        assert!(
  3992	            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("> 0")),
  3993	            "expected MalformedTable about zero dims, got {err:?}"
  3994	        );
  3995	    }
  3996	
  3997	    // ===== W5-125 (Phase 4.8.O.1) — loader footprint-bounds upper-limit check =====
  3998	
  3999	    #[test]
  4000	    fn malformed_table_rejected_when_footprint_past_max_row() {
  4001	        let dir = TempDir::new().unwrap();
  4002	        let path = dir.path().join("bad.qbook");
  4003	        fs::create_dir_all(path.join("sheets")).unwrap();
  4004	        // top_row = 1_048_574, rows = 3 → last_row = 1_048_576 > MAX_ROW (1_048_575).
  4005	        let toml = r#"
  4006	schema_version = 6
  4007	name = "bad"
  4008	date_system = "1900"
  4009	sheets = [
  4010	  { id = 0, name = "Sheet1", chunk_rows = 16384, row_extent = 0, col_extent = 0 },
  4011	]
  4012	
  4013	[[tables.entries]]
  4014	name = "FAR"
  4015	display_name = "Far"
  4016	sheet = 0
  4017	top_row = 1048574
  4018	top_col = 0
  4019	rows = 3
  4020	cols = 1
  4021	has_header = true
  4022	has_totals = false
  4023	
  4024	[[tables.entries.columns]]
  4025	id = 0
  4026	name = "qty"
  4027	display = "Qty"
  4028	"#;
  4029	        fs::write(path.join("workbook.toml"), toml).unwrap();
  4030	        fs::write(path.join("sheets").join("0.jsonl"), "").unwrap();
  4031	        let err = load_workbook(&path).unwrap_err();
  4032	        assert!(
  4033	            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("MAX_ROW")),
  4034	            "expected MalformedTable about MAX_ROW, got {err:?}"
  4035	        );
  4036	    }
  4037	
  4038	    #[test]
  4039	    fn malformed_table_rejected_when_footprint_past_max_column() {
  4040	        let dir = TempDir::new().unwrap();
  4041	        let path = dir.path().join("bad.qbook");
  4042	        fs::create_dir_all(path.join("sheets")).unwrap();
  4043	        // top_col = 16383, cols = 2 → last_col = 16384 > MAX_COLUMN (16383).
  4044	        let toml = r#"
  4045	schema_version = 6
  4046	name = "bad"
  4047	date_system = "1900"
  4048	sheets = [
  4049	  { id = 0, name = "Sheet1", chunk_rows = 16384, row_extent = 0, col_extent = 0 },
  4050	]
  4051	
  4052	[[tables.entries]]
  4053	name = "WIDE"
  4054	display_name = "Wide"
  4055	sheet = 0
  4056	top_row = 0
  4057	top_col = 16383
  4058	rows = 1
  4059	cols = 2
  4060	has_header = true
  4061	has_totals = false
  4062	
  4063	[[tables.entries.columns]]
  4064	id = 0
  4065	name = "a"
  4066	display = "A"
  4067	
  4068	[[tables.entries.columns]]
  4069	id = 1
  4070	name = "b"
  4071	display = "B"
  4072	"#;
  4073	        fs::write(path.join("workbook.toml"), toml).unwrap();
  4074	        fs::write(path.join("sheets").join("0.jsonl"), "").unwrap();
  4075	        let err = load_workbook(&path).unwrap_err();
  4076	        assert!(
  4077	            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("MAX_COLUMN")),
  4078	            "expected MalformedTable about MAX_COLUMN, got {err:?}"
  4079	        );
  4080	    }
  4081	
  4082	    // ===== W5-126 (Phase 4.8.O.2) — loader re-validates runtime invariants =====
  4083	    //
  4084	    // Closes Codex 4.8.O megaudit MEDIUM-1 (loader skipped overlap /
  4085	    // namespace / column dedup checks) and MEDIUM-2 (`max_id + 1`
  4086	    // unchecked at u32::MAX) by surfacing the same shape as
  4087	    // `WorkbookRuntime::create_table` would reject.
  4088	
  4089	    #[test]
  4090	    fn malformed_table_rejected_when_duplicate_canonical_name() {
  4091	        let dir = TempDir::new().unwrap();
  4092	        let path = dir.path().join("bad.qbook");
  4093	        fs::create_dir_all(path.join("sheets")).unwrap();
  4094	        // Two entries with the same canonical name (case-insensitive).
  4095	        let toml = r#"
  4096	schema_version = 6
  4097	name = "bad"
  4098	date_system = "1900"
  4099	sheets = [
  4100	  { id = 0, name = "Sheet1", chunk_rows = 16384, row_extent = 0, col_extent = 0 },
  4101	]
  4102	
  4103	[[tables.entries]]
  4104	name = "SALES"
  4105	display_name = "Sales"
  4106	sheet = 0
  4107	top_row = 0
  4108	top_col = 0
  4109	rows = 2
  4110	cols = 1
  4111	has_header = true
  4112	has_totals = false
  4113	
  4114	[[tables.entries.columns]]
  4115	id = 0
  4116	name = "qty"
  4117	display = "Qty"
  4118	
  4119	[[tables.entries]]
  4120	name = "SALES"
  4121	display_name = "Sales"
  4122	sheet = 0
  4123	top_row = 10
  4124	top_col = 0
  4125	rows = 2
  4126	cols = 1
  4127	has_header = true
  4128	has_totals = false
  4129	
  4130	[[tables.entries.columns]]
  4131	id = 1
  4132	name = "qty"
  4133	display = "Qty"
  4134	"#;
  4135	        fs::write(path.join("workbook.toml"), toml).unwrap();
  4136	        fs::write(path.join("sheets").join("0.jsonl"), "").unwrap();
  4137	        let err = load_workbook(&path).unwrap_err();
  4138	        assert!(
  4139	            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("duplicate canonical table name")),
  4140	            "expected MalformedTable about duplicate canonical, got {err:?}"
  4141	        );
  4142	    }
  4143	
  4144	    #[test]
  4145	    fn malformed_table_rejected_when_name_collides_with_defined_name() {
  4146	        let dir = TempDir::new().unwrap();
  4147	        let path = dir.path().join("bad.qbook");
  4148	        fs::create_dir_all(path.join("sheets")).unwrap();
  4149	        // `Rate` is registered as a defined-name constant; table named
  4150	        // `Rate` collides with it (Excel canon — shared namespace).
  4151	        let toml = r#"
  4152	schema_version = 6
  4153	name = "bad"
  4154	date_system = "1900"
  4155	sheets = [
  4156	  { id = 0, name = "Sheet1", chunk_rows = 16384, row_extent = 0, col_extent = 0 },
  4157	]
  4158	
  4159	[[names.entries]]
  4160	name = "Rate"
  4161	target = { kind = "constant", value = { Number = 0.21 } }
  4162	
  4163	[[tables.entries]]
  4164	name = "RATE"
  4165	display_name = "Rate"
  4166	sheet = 0
  4167	top_row = 0
  4168	top_col = 0
  4169	rows = 2
  4170	cols = 1
  4171	has_header = true
  4172	has_totals = false
  4173	
  4174	[[tables.entries.columns]]
  4175	id = 0
  4176	name = "qty"
  4177	display = "Qty"
  4178	"#;
  4179	        fs::write(path.join("workbook.toml"), toml).unwrap();
  4180	        fs::write(path.join("sheets").join("0.jsonl"), "").unwrap();
  4181	        let err = load_workbook(&path).unwrap_err();
  4182	        assert!(
  4183	            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("collides with a workbook-scoped defined name")),
  4184	            "expected MalformedTable about defined-name collision, got {err:?}"
  4185	        );
  4186	    }
  4187	
  4188	    #[test]
  4189	    fn malformed_table_rejected_when_footprints_overlap() {
  4190	        let dir = TempDir::new().unwrap();
  4191	        let path = dir.path().join("bad.qbook");
  4192	        fs::create_dir_all(path.join("sheets")).unwrap();
  4193	        // Two non-duplicate names but overlapping footprints on sheet 0.
  4194	        let toml = r#"
  4195	schema_version = 6
  4196	name = "bad"
  4197	date_system = "1900"
  4198	sheets = [
  4199	  { id = 0, name = "Sheet1", chunk_rows = 16384, row_extent = 0, col_extent = 0 },
  4200	]
  4201	
  4202	[[tables.entries]]
  4203	name = "A"
  4204	display_name = "A"
  4205	sheet = 0
  4206	top_row = 0
  4207	top_col = 0
  4208	rows = 5
  4209	cols = 2
  4210	has_header = true
  4211	has_totals = false
  4212	
  4213	[[tables.entries.columns]]
  4214	id = 0
  4215	name = "x"
  4216	display = "X"
  4217	
  4218	[[tables.entries.columns]]
  4219	id = 1
  4220	name = "y"
  4221	display = "Y"
  4222	
  4223	[[tables.entries]]
  4224	name = "B"
  4225	display_name = "B"
  4226	sheet = 0
  4227	top_row = 2
  4228	top_col = 1
  4229	rows = 3
  4230	cols = 2
  4231	has_header = true
  4232	has_totals = false
  4233	
  4234	[[tables.entries.columns]]
  4235	id = 2
  4236	name = "p"
  4237	display = "P"
  4238	
  4239	[[tables.entries.columns]]
  4240	id = 3
  4241	name = "q"
  4242	display = "Q"
  4243	"#;
  4244	        fs::write(path.join("workbook.toml"), toml).unwrap();
  4245	        fs::write(path.join("sheets").join("0.jsonl"), "").unwrap();
  4246	        let err = load_workbook(&path).unwrap_err();
  4247	        assert!(
  4248	            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("overlaps")),
  4249	            "expected MalformedTable about overlap, got {err:?}"
  4250	        );
  4251	    }
  4252	
  4253	    #[test]
  4254	    fn malformed_table_rejected_when_column_name_empty() {
  4255	        let dir = TempDir::new().unwrap();
  4256	        let path = dir.path().join("bad.qbook");
  4257	        fs::create_dir_all(path.join("sheets")).unwrap();
  4258	        let toml = r#"
  4259	schema_version = 6
  4260	name = "bad"
  4261	date_system = "1900"
  4262	sheets = [
  4263	  { id = 0, name = "Sheet1", chunk_rows = 16384, row_extent = 0, col_extent = 0 },
  4264	]
  4265	
  4266	[[tables.entries]]
  4267	name = "T"
  4268	display_name = "T"
  4269	sheet = 0
  4270	top_row = 0
  4271	top_col = 0
  4272	rows = 2
  4273	cols = 1
  4274	has_header = true
  4275	has_totals = false
  4276	
  4277	[[tables.entries.columns]]
  4278	id = 0
  4279	name = ""
  4280	display = "X"
  4281	"#;
  4282	        fs::write(path.join("workbook.toml"), toml).unwrap();
  4283	        fs::write(path.join("sheets").join("0.jsonl"), "").unwrap();
  4284	        let err = load_workbook(&path).unwrap_err();
  4285	        assert!(
  4286	            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("cannot be empty")),
  4287	            "expected MalformedTable about empty column name, got {err:?}"
  4288	        );
  4289	    }
  4290	
  4291	    #[test]
  4292	    fn malformed_table_rejected_when_duplicate_column_name() {
  4293	        let dir = TempDir::new().unwrap();
  4294	        let path = dir.path().join("bad.qbook");
  4295	        fs::create_dir_all(path.join("sheets")).unwrap();
  4296	        // Two columns with the same canonical (case-insensitive) name.
  4297	        let toml = r#"
  4298	schema_version = 6
  4299	name = "bad"
  4300	date_system = "1900"
  4301	sheets = [
  4302	  { id = 0, name = "Sheet1", chunk_rows = 16384, row_extent = 0, col_extent = 0 },
  4303	]
  4304	
  4305	[[tables.entries]]
  4306	name = "T"
  4307	display_name = "T"
  4308	sheet = 0
  4309	top_row = 0
  4310	top_col = 0
  4311	rows = 2
  4312	cols = 2
  4313	has_header = true
  4314	has_totals = false
  4315	
  4316	[[tables.entries.columns]]
  4317	id = 0
  4318	name = "qty"
  4319	display = "Qty"
  4320	
  4321	[[tables.entries.columns]]
  4322	id = 1
  4323	name = "qty"
  4324	display = "QTY"
  4325	"#;
  4326	        fs::write(path.join("workbook.toml"), toml).unwrap();
  4327	        fs::write(path.join("sheets").join("0.jsonl"), "").unwrap();
  4328	        let err = load_workbook(&path).unwrap_err();
  4329	        assert!(
  4330	            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("duplicate canonical column name")),
  4331	            "expected MalformedTable about duplicate column, got {err:?}"
  4332	        );
  4333	    }
  4334	
  4335	    /// **W5-126 / Codex MEDIUM-2:** persisted column id at u32::MAX
  4336	    /// would panic in `TableTable::insert` (`max_id + 1` overflow) or
  4337	    /// wrap in release. Catch at load.
  4338	    #[test]
  4339	    fn malformed_table_rejected_when_column_id_is_u32_max() {
  4340	        let dir = TempDir::new().unwrap();
  4341	        let path = dir.path().join("bad.qbook");
  4342	        fs::create_dir_all(path.join("sheets")).unwrap();
  4343	        let toml = r#"
  4344	schema_version = 6
  4345	name = "bad"
  4346	date_system = "1900"
  4347	sheets = [
  4348	  { id = 0, name = "Sheet1", chunk_rows = 16384, row_extent = 0, col_extent = 0 },
  4349	]
  4350	
  4351	[[tables.entries]]
  4352	name = "T"
  4353	display_name = "T"
  4354	sheet = 0
  4355	top_row = 0
  4356	top_col = 0
  4357	rows = 2
  4358	cols = 1
  4359	has_header = true
  4360	has_totals = false
  4361	
  4362	[[tables.entries.columns]]
  4363	id = 4294967295
  4364	name = "qty"
  4365	display = "Qty"
  4366	"#;
  4367	        fs::write(path.join("workbook.toml"), toml).unwrap();
  4368	        fs::write(path.join("sheets").join("0.jsonl"), "").unwrap();
  4369	        let err = load_workbook(&path).unwrap_err();
  4370	        assert!(
  4371	            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("u32::MAX")),
  4372	            "expected MalformedTable about u32::MAX column id, got {err:?}"
  4373	        );
  4374	    }
  4375	
  4376	    /// Happy path: two distinct non-overlapping tables on the same
  4377	    /// sheet — should load cleanly. Pins that the new MEDIUM-1
  4378	    /// validation doesn't reject legitimate multi-table workbooks.
  4379	    #[test]
  4380	    fn load_accepts_two_non_overlapping_tables() {

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '2078,2145p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  2078	                }
  2079	                if let Some(other) = self.workbook.table_at(sheet, r, c) {
  2080	                    if !other.name.eq_ignore_ascii_case(&canonical) {
  2081	                        return Err(RuntimeError::TableResizeRejected {
  2082	                            name: name.to_owned(),
  2083	                            reason: "new footprint overlaps an existing table",
  2084	                        });
  2085	                    }
  2086	                }
  2087	                if self.workbook.spill_anchor_at(sheet, r, c).is_some() {
  2088	                    return Err(RuntimeError::TableResizeRejected {
  2089	                        name: name.to_owned(),
  2090	                        reason: "new footprint contains a spill anchor",
  2091	                    });
  2092	                }
  2093	            }
  2094	        }
  2095	        // ----- Op-log append (BEFORE mutation per W5-103) -----
  2096	        if let Some(oplog) = self.oplog.as_deref_mut() {
  2097	            oplog.append(Op::ResizeTable {
  2098	                name: canonical.clone(),
  2099	                new_rows,
  2100	                new_cols,
  2101	                added_columns: added_columns.clone(),
  2102	                removed_columns: removed_columns.clone(),
  2103	            })?;
  2104	        }
  2105	        // ----- Mutation -----
  2106	        // Allocate new column ids first; mutable borrows on TableTable
  2107	        // versus TableMetadata conflict otherwise.
  2108	        let new_ids: Vec<u32> = (0..added_columns.len())
  2109	            .map(|_| self.workbook.tables_mut().allocate_column_id())
  2110	            .collect();
  2111	        let meta = self
  2112	            .workbook
  2113	            .tables_mut()
  2114	            .get_mut(&canonical)
  2115	            .expect("verified at top");
  2116	        meta.rows = new_rows;
  2117	        meta.cols = new_cols;
  2118	        meta.columns
  2119	            .truncate(meta.columns.len() - removed_len as usize);
  2120	        for (cn, id) in added_columns.iter().zip(new_ids) {
  2121	            meta.columns.push(TableColumn {
  2122	                id,
  2123	                name: Arc::from(cn.to_ascii_lowercase().as_str()),
  2124	                display: Arc::from(cn.as_str()),
  2125	                totals_function: None,
  2126	            });
  2127	        }
  2128	        self.workbook.tables_mut().bump_generation();
  2129	        // Plan cache clear so formulas re-bind against the new range
  2130	        // / column roster on next eval. Targeted invalidation via
  2131	        // `table_to_formulas` is design § 4.5 / 4.8.G.3 (deferred).
  2132	        self.plan_cache.clear();
  2133	        Ok(())
  2134	    }
  2135	
  2136	    /// **W5-92 (Phase 4.6.D):** create a new sheet,
  2137	    /// emitting `Op::AddSheet` into the attached op log (if any). Returns
  2138	    /// the new sheet's `SheetId`. Wraps `Workbook::add_sheet_with_chunk_rows`.
  2139	    ///
  2140	    /// `chunk_rows` is the per-sheet chunk size. Pass `16_384` (the engine
  2141	    /// default) unless you have a specific reason — the qbook loader uses
  2142	    /// the saved chunk_rows so layouts round-trip.
  2143	    ///
  2144	    /// Direct callers of `Workbook::add_sheet` / `add_sheet_with_chunk_rows`
  2145	    /// bypass the op log silently — that path is documented as low-level.

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '1595,1640p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1595	                        reason: "table footprint contains a spill anchor",
  1596	                    });
  1597	                }
  1598	            }
  1599	        }
  1600	        // ----- Op-log append (BEFORE mutation per W5-103 atomicity) -----
  1601	        if let Some(oplog) = self.oplog.as_deref_mut() {
  1602	            oplog.append(Op::CreateTable {
  1603	                name: canonical.clone(),
  1604	                sheet,
  1605	                top_row,
  1606	                top_col,
  1607	                rows,
  1608	                cols,
  1609	                has_header,
  1610	                has_totals,
  1611	                column_names: column_names.clone(),
  1612	            })?;
  1613	        }
  1614	        // ----- Mutation -----
  1615	        use ql_storage::{TableColumn, TableMetadata};
  1616	        let columns: Vec<TableColumn> = column_names
  1617	            .iter()
  1618	            .map(|cn| TableColumn {
  1619	                id: self.workbook.tables_mut().allocate_column_id(),
  1620	                name: Arc::from(cn.to_ascii_lowercase().as_str()),
  1621	                display: Arc::from(cn.as_str()),
  1622	                totals_function: None,
  1623	            })
  1624	            .collect();
  1625	        let meta = TableMetadata {
  1626	            name: Arc::from(canonical.as_str()),
  1627	            display_name: Arc::from(name),
  1628	            sheet,
  1629	            top_row,
  1630	            top_col,
  1631	            rows,
  1632	            cols,
  1633	            has_header,
  1634	            has_totals,
  1635	            columns,
  1636	        };
  1637	        self.workbook
  1638	            .tables_mut()
  1639	            .insert(Arc::from(canonical.as_str()), meta);
  1640	        Ok(())

exec
/bin/zsh -lc "rg -n \"display = \\\"\\\"|display.*empty|column display|cannot be empty|empty display\" crates/ql-io/src/qbook_format.rs crates/ql-exec/src/workbook_runtime.rs crates/ql-oplog/src/replay.rs crates/ql-storage/src/tables.rs" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
crates/ql-oplog/src/replay.rs:626:            reason: "column name cannot be empty",
crates/ql-oplog/src/replay.rs:747:            reason: "added column names cannot be empty",
crates/ql-oplog/src/replay.rs:877:            reason: "table column names cannot be empty",
crates/ql-exec/src/workbook_runtime.rs:1547:                reason: "table column names cannot be empty",
crates/ql-exec/src/workbook_runtime.rs:1814:                reason: "column name cannot be empty",
crates/ql-exec/src/workbook_runtime.rs:2038:                reason: "added column names cannot be empty",
crates/ql-exec/src/workbook_runtime.rs:10237:                assert!(reason.contains("cannot be empty"), "reason: {reason}");
crates/ql-io/src/qbook_format.rs:1781:                    if col.name.is_empty() || col.display.is_empty() {
crates/ql-io/src/qbook_format.rs:1784:                            reason: "table column names cannot be empty".into(),
crates/ql-io/src/qbook_format.rs:4286:            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("cannot be empty")),

exec
/bin/zsh -lc "nl -ba crates/ql-exec/tests/oplog_e2e.rs | sed -n '812,980p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   812	}
   813	
   814	/// **W5-125 (Phase 4.8.O.1 — Codex HIGH-1) e2e:** verify that the
   815	/// replay-side footprint-bounds check rejects a `CreateTable` op whose
   816	/// dimensions extend past MAX_ROW. This pins the producer-replay
   817	/// symmetry: an op-log entry constructed by hand (bypassing the
   818	/// producer guard) still fails loudly at replay rather than registering
   819	/// an out-of-grid table.
   820	#[test]
   821	fn replay_rejects_create_table_with_footprint_past_max_row() {
   822	    use ql_oplog::{Op, ReplayError};
   823	    use ql_types::MAX_ROW;
   824	    let mut oplog = ql_oplog::OpLog::new();
   825	    oplog
   826	        .append(Op::CreateTable {
   827	            name: "BAD".to_owned(),
   828	            sheet: 0,
   829	            top_row: MAX_ROW - 1,
   830	            top_col: 0,
   831	            rows: 3,
   832	            cols: 1,
   833	            has_header: true,
   834	            has_totals: false,
   835	            column_names: vec!["x".to_owned()],
   836	        })
   837	        .unwrap();
   838	    let mut wb = fresh_wb();
   839	    let reg = default_registry();
   840	    let err = replay_into(&oplog, &mut wb, &reg).unwrap_err();
   841	    match err {
   842	        ReplayError::TableCreateRejected { reason, .. } => {
   843	            assert!(reason.contains("MAX_ROW"), "reason: {reason}");
   844	        }
   845	        other => panic!("expected TableCreateRejected, got {other:?}"),
   846	    }
   847	}
   848	
   849	/// **W5-125 (Phase 4.8.O.1 — Codex HIGH-1) e2e:** mirror of the above
   850	/// for `ResizeTable`. Constructs a valid CreateTable then a
   851	/// hand-crafted ResizeTable that grows past MAX_ROW.
   852	#[test]
   853	fn replay_rejects_resize_table_with_footprint_past_max_row() {
   854	    use ql_oplog::{Op, ReplayError};
   855	    use ql_types::MAX_ROW;
   856	    let mut oplog = ql_oplog::OpLog::new();
   857	    oplog
   858	        .append(Op::CreateTable {
   859	            name: "SALES".to_owned(),
   860	            sheet: 0,
   861	            top_row: MAX_ROW - 4,
   862	            top_col: 0,
   863	            rows: 3,
   864	            cols: 1,
   865	            has_header: true,
   866	            has_totals: false,
   867	            column_names: vec!["qty".to_owned()],
   868	        })
   869	        .unwrap();
   870	    // Grow to 10 rows → last_row = MAX_ROW + 5 > MAX_ROW.
   871	    oplog
   872	        .append(Op::ResizeTable {
   873	            name: "SALES".to_owned(),
   874	            new_rows: 10,
   875	            new_cols: 1,
   876	            added_columns: vec![],
   877	            removed_columns: vec![],
   878	        })
   879	        .unwrap();
   880	    let mut wb = fresh_wb();
   881	    let reg = default_registry();
   882	    let err = replay_into(&oplog, &mut wb, &reg).unwrap_err();
   883	    match err {
   884	        ReplayError::TableResizeRejected { reason, .. } => {
   885	            assert!(reason.contains("MAX_ROW"), "reason: {reason}");
   886	        }
   887	        other => panic!("expected TableResizeRejected, got {other:?}"),
   888	    }
   889	}
   890	
   891	/// **W5-127 (Phase 4.8.O.3 — Codex LOW-2 composed coverage):**
   892	/// producer emits `create_table → rename_column → resize_table` (the
   893	/// rename targets a column that later gets removed by the resize).
   894	/// Replay against a fresh workbook must reconstruct the FINAL state
   895	/// (renamed column metadata, then dropped by resize). Pins
   896	/// cross-sub-phase replay-order correctness — each op sees the state
   897	/// produced by the previous one.
   898	#[test]
   899	fn composed_create_rename_column_resize_replay_roundtrip() {
   900	    let mut producer_wb = fresh_wb();
   901	    let reg = default_registry();
   902	    let mut producer_oplog = OpLog::new();
   903	    {
   904	        let mut rt = WorkbookRuntime::with_oplog(&mut producer_wb, &reg, &mut producer_oplog);
   905	        rt.create_table(
   906	            "Sales",
   907	            0,
   908	            0,
   909	            0,
   910	            3,
   911	            2,
   912	            true,
   913	            false,
   914	            vec!["Qty".into(), "Price".into()],
   915	        )
   916	        .unwrap();
   917	        // Rename the trailing column.
   918	        let n = rt.rename_column("Sales", "Price", "Cost").unwrap();
   919	        assert_eq!(n, 0, "no formulas reference Price yet");
   920	        // Resize away the just-renamed trailing column.
   921	        rt.resize_table("Sales", 3, 1, vec![], vec!["Cost".into()])
   922	            .unwrap();
   923	    }
   924	    // Producer side: only Qty remains.
   925	    let meta = producer_wb.lookup_table("Sales").unwrap();
   926	    assert_eq!(meta.cols, 1);
   927	    assert!(meta.lookup_column("Qty").is_some());
   928	    assert!(meta.lookup_column("Price").is_none());
   929	    assert!(meta.lookup_column("Cost").is_none());
   930	
   931	    // Replay side: same final state.
   932	    let mut replay_wb = fresh_wb();
   933	    replay_into(&producer_oplog, &mut replay_wb, &reg).unwrap();
   934	    let r_meta = replay_wb.lookup_table("Sales").expect("table reloaded");
   935	    assert_eq!(r_meta.cols, 1);
   936	    assert!(r_meta.lookup_column("Qty").is_some());
   937	    assert!(r_meta.lookup_column("Price").is_none());
   938	    assert!(r_meta.lookup_column("Cost").is_none());
   939	    // Same column id allocator state — Qty's id matches producer.
   940	    let producer_qty_id = meta.lookup_column("Qty").unwrap().1.id;
   941	    let replay_qty_id = r_meta.lookup_column("Qty").unwrap().1.id;
   942	    assert_eq!(producer_qty_id, replay_qty_id);
   943	}
   944	
   945	/// **W5-127 (Phase 4.8.O.3 — Codex LOW-2 composed coverage):**
   946	/// save → load → `rename_column` → verify formula text + value
   947	/// parity. Pins that the persistence × runtime-mutation interaction
   948	/// holds — a workbook reloaded from disk supports the same formula-
   949	/// rewriting mutations as a workbook freshly created in memory.
   950	#[test]
   951	fn composed_save_load_then_rename_column_roundtrip() {
   952	    use tempfile::TempDir;
   953	    let reg = default_registry();
   954	    let mut producer_wb = fresh_wb();
   955	    {
   956	        let mut rt = WorkbookRuntime::new(&mut producer_wb, &reg);
   957	        rt.create_table("Sales", 0, 0, 0, 3, 1, true, false, vec!["Qty".into()])
   958	            .unwrap();
   959	        rt.set_value(0, 1, 0, Value::Number(10.0)).unwrap();
   960	        rt.set_value(0, 2, 0, Value::Number(20.0)).unwrap();
   961	        let v = rt.set_formula(0, 5, 0, "SUM(Sales[Qty])").unwrap();
   962	        assert_eq!(v, Value::Number(30.0));
   963	    }
   964	
   965	    let dir = TempDir::new().unwrap();
   966	    let path = dir.path().join("composed.qbook");
   967	    ql_io::save_workbook(&producer_wb, "composed", &path).unwrap();
   968	
   969	    let (mut reloaded, recompute) = ql_exec::load_workbook_and_recompute(&path, &reg).unwrap();
   970	    assert!(recompute.is_complete());
   971	    // Sanity: pre-rename load+recompute matches pre-save.
   972	    assert_eq!(reloaded.read(Address::new(0, 5, 0)), Value::Number(30.0));
   973	
   974	    // Now rename a column on the reloaded workbook. The formula text
   975	    // rewrite must walk over the reloaded formula and produce the
   976	    // same shape as a never-saved workbook would.
   977	    {
   978	        let mut rt = WorkbookRuntime::new(&mut reloaded, &reg);
   979	        let n = rt.rename_column("Sales", "Qty", "Quantity").unwrap();
   980	        assert_eq!(n, 1, "one formula rewritten");

exec
/bin/zsh -lc 'rg -n "schema_version|SUPPORTED|MIN|MAX|deny_unknown_fields|TableEntry|TableColumnEntry|TablesSection" crates/ql-io/src/qbook_format.rs' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
7://! ├── workbook.toml          # envelope: schema_version, name, sheet list, names
24://! - **v1** (Phase 1 W5-6 + W5-9): envelope { schema_version, name, sheets },
34://!     - `#[serde(deny_unknown_fields)]` on every envelope / record / wire-value
44://! - **Within a version**: `deny_unknown_fields` rejects extra fields loudly.
77:use ql_types::{Address, ColId, ErrorValue, Range, RowId, SheetId, Value, MAX_COLUMN, MAX_ROW};
85:///   `Pending` CellWireValue, `deny_unknown_fields` strictness.
114:/// `UnsupportedSchema` (the schema_version comparison upgrade is the
121:///   - top-level `tables: Option<TablesSection>` — workbook-scoped
130:///     `schema_version > WORKBOOK_SCHEMA_VERSION` check → loud
144:pub const MIN_SUPPORTED_SCHEMA_VERSION: u32 = 1;
176:         {MIN_SUPPORTED_SCHEMA_VERSION}..={WORKBOOK_SCHEMA_VERSION})"
283:/// Phase 2A.8: `#[serde(deny_unknown_fields)]` rejects any unknown TOML key —
288:#[serde(deny_unknown_fields)]
290:    pub schema_version: u32,
318:    pub tables: Option<TablesSection>,
348:#[serde(deny_unknown_fields)]
374:#[serde(deny_unknown_fields)]
381:#[serde(deny_unknown_fields)]
391:#[serde(deny_unknown_fields)]
400:/// `TableEntry` preserves stable column ids across save/load (the
407:#[serde(deny_unknown_fields)]
408:pub struct TablesSection {
409:    pub entries: Vec<TableEntry>,
412:/// **W5-123 (Phase 4.8.L):** one entry in `TablesSection`. Mirrors
415:#[serde(deny_unknown_fields)]
416:pub struct TableEntry {
429:    pub columns: Vec<TableColumnEntry>,
432:/// **W5-123 (Phase 4.8.L):** one entry in `TableEntry::columns`.
438:#[serde(deny_unknown_fields)]
439:pub struct TableColumnEntry {
510:#[serde(deny_unknown_fields)]
516:#[serde(deny_unknown_fields)]
561:/// - **Phase 2A.8** added `deny_unknown_fields` per audit M9. Any unrecognized
564:#[serde(deny_unknown_fields)]
671:                if *row > MAX_ROW {
674:                        reason: format!("Cell.row {row} exceeds MAX_ROW {MAX_ROW}"),
677:                if *col > MAX_COLUMN {
680:                        reason: format!("Cell.col {col} exceeds MAX_COLUMN {MAX_COLUMN}"),
693:                    ("start_row", *start_row, MAX_ROW),
694:                    ("start_col", *start_col, MAX_COLUMN),
695:                    ("end_row", *end_row, MAX_ROW),
696:                    ("end_col", *end_col, MAX_COLUMN),
1226:        let mut entries: Vec<TableEntry> = wb
1229:            .map(|(_canon, meta)| TableEntry {
1242:                    .map(|c| TableColumnEntry {
1255:            Some(TablesSection { entries })
1260:        schema_version: WORKBOOK_SCHEMA_VERSION,
1389:            "Quantbook atomic-save marker v1 (schema_version={WORKBOOK_SCHEMA_VERSION}).\n\
1434:    // Phase 2A.8: accept schema versions in [MIN_SUPPORTED..=current].
1437:    if envelope.schema_version < MIN_SUPPORTED_SCHEMA_VERSION
1438:        || envelope.schema_version > WORKBOOK_SCHEMA_VERSION
1441:            found: envelope.schema_version,
1444:    let is_v1 = envelope.schema_version == 1;
1524:                if entry.row > MAX_ROW || entry.col > MAX_COLUMN {
1573:            if rec.row > MAX_ROW {
1577:                    detail: format!("row {} exceeds MAX_ROW {MAX_ROW}", rec.row),
1580:            if rec.col > MAX_COLUMN {
1584:                    detail: format!("col {} exceeds MAX_COLUMN {MAX_COLUMN}", rec.col),
1716:            // edited / corrupted TOML with `top_row + rows - 1 > MAX_ROW`
1753:            // duplicate canonical names within the same TablesSection
1774:            // unique canonical (lowercase) names, no `u32::MAX`
1787:                    if col.id == u32::MAX {
1790:                            reason: "column id cannot be u32::MAX (would overflow allocator)"
2014:    fn unsupported_schema_version_errors() {
2022:            "schema_version = 999\nname = \"x\"\nsheets = []\n",
2082:        // u32::MAX is well over Excel's MAX_ROW = 1_048_575.
2092:                    detail.contains("MAX_ROW"),
2093:                    "detail should mention MAX_ROW: {detail:?}"
2096:            other => panic!("expected MalformedCell mentioning MAX_ROW, got {other:?}"),
2106:        // u32::MAX > MAX_COLUMN = 16_383.
2116:                    detail.contains("MAX_COLUMN"),
2117:                    "detail should mention MAX_COLUMN: {detail:?}"
2120:            other => panic!("expected MalformedCell mentioning MAX_COLUMN, got {other:?}"),
2155:            r#"schema_version = 1
2224:        assert_eq!(env.schema_version, WORKBOOK_SCHEMA_VERSION); // Phase 2A.8: now 2
2438:            r#"schema_version = 1
2727:    /// Phase 2A.8 audit M9 (deny_unknown_fields): a workbook.toml with an
2830:schema_version = 2
2980:schema_version = 3
3017:schema_version = 4
3046:        // MAX_ROW is u32::MAX in `ql_types`; we use a far-too-large row.
3047:        let row_too_big = u32::MAX;
3050:schema_version = 4
3094:schema_version = 4
3259:        //   workbook.toml: schema_version = 1, no `names` section
3264:schema_version = 1
3308:    fn future_schema_version_rejected() {
3313:schema_version = 7
3470:    fn schema_version_constant_is_six() {
3536:schema_version = 4
3568:schema_version = 5
3633:schema_version = 5
3661:schema_version = 5
3867:schema_version = 5
3882:    // pre-existing `future_schema_version_rejected` test above — once
3893:schema_version = 6
3931:schema_version = 6
3969:schema_version = 6
4004:        // top_row = 1_048_574, rows = 3 → last_row = 1_048_576 > MAX_ROW (1_048_575).
4006:schema_version = 6
4033:            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("MAX_ROW")),
4034:            "expected MalformedTable about MAX_ROW, got {err:?}"
4043:        // top_col = 16383, cols = 2 → last_col = 16384 > MAX_COLUMN (16383).
4045:schema_version = 6
4077:            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("MAX_COLUMN")),
4078:            "expected MalformedTable about MAX_COLUMN, got {err:?}"
4086:    // unchecked at u32::MAX) by surfacing the same shape as
4096:schema_version = 6
4152:schema_version = 6
4195:schema_version = 6
4259:schema_version = 6
4298:schema_version = 6
4335:    /// **W5-126 / Codex MEDIUM-2:** persisted column id at u32::MAX
4344:schema_version = 6
4371:            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("u32::MAX")),
4372:            "expected MalformedTable about u32::MAX column id, got {err:?}"
4385:schema_version = 6

exec
/bin/zsh -lc "rg -n \"save_workbook\\(|load_workbook\\(|fn load_workbook|pub fn load_workbook|struct WorkbookRuntime|plan_cache|clear\\(\" crates/ql-io/src/qbook_format.rs crates/ql-exec/src/workbook_runtime.rs crates/ql-storage/src/workbook.rs" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
crates/ql-storage/src/workbook.rs:154:    pub fn clear(&mut self, name: &str) {
crates/ql-storage/src/workbook.rs:932:        wb.names_mut().clear("MIXEDCASE");
crates/ql-storage/src/workbook.rs:934:        wb.names_mut().clear("anothername");
crates/ql-io/src/qbook_format.rs:786:pub fn save_workbook(wb: &Workbook, name: &str, path: &Path) -> Result<(), QbookError> {
crates/ql-io/src/qbook_format.rs:1414:pub fn load_workbook(path: &Path) -> Result<Workbook, QbookError> {
crates/ql-io/src/qbook_format.rs:1896:        save_workbook(&wb, "test", &path).unwrap();
crates/ql-io/src/qbook_format.rs:1898:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:1915:        save_workbook(&wb, "test", &path).unwrap();
crates/ql-io/src/qbook_format.rs:1917:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:1942:        save_workbook(&wb, "v", &path).unwrap();
crates/ql-io/src/qbook_format.rs:1944:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:1966:        save_workbook(&wb, "ms", &path).unwrap();
crates/ql-io/src/qbook_format.rs:1967:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:1982:        save_workbook(&wb, "t", &path).unwrap();
crates/ql-io/src/qbook_format.rs:1984:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2003:        save_workbook(&wb, "s", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2026:        match load_workbook(&path) {
crates/ql-io/src/qbook_format.rs:2038:        match load_workbook(&path) {
crates/ql-io/src/qbook_format.rs:2047:        match load_workbook(path) {
crates/ql-io/src/qbook_format.rs:2060:        save_workbook(&wb, "x", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2065:        match load_workbook(&path) {
crates/ql-io/src/qbook_format.rs:2081:        save_workbook(&wb, "x", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2089:        match load_workbook(&path) {
crates/ql-io/src/qbook_format.rs:2105:        save_workbook(&wb, "x", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2113:        match load_workbook(&path) {
crates/ql-io/src/qbook_format.rs:2131:        save_workbook(&wb, "x", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2136:        match load_workbook(&path) {
crates/ql-io/src/qbook_format.rs:2177:        match load_workbook(&path) {
crates/ql-io/src/qbook_format.rs:2199:        save_workbook(&wb, "x", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2209:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2220:        save_workbook(&wb, "my workbook", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2292:        match save_workbook(&wb, "nan-test", &path) {
crates/ql-io/src/qbook_format.rs:2322:        match save_workbook(&wb, "inf-test", &path) {
crates/ql-io/src/qbook_format.rs:2341:        let result = save_workbook(&wb, "neg-inf-test", &path);
crates/ql-io/src/qbook_format.rs:2365:        save_workbook(&wb, "formulas", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2366:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2392:        save_workbook(&wb, "fonly", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2393:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2418:        save_workbook(&wb, "multi", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2419:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2457:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2470:        save_workbook(&wb, "atomic-test", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2501:        save_workbook(&original, "first", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2508:        let result = save_workbook(&bad_wb, "bad", &path);
crates/ql-io/src/qbook_format.rs:2513:        let reloaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2543:        save_workbook(&wb, "rollforward", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2558:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2572:        save_workbook(&wb, "user-sibling", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2586:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2607:        save_workbook(&wb, "rollback", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2619:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2635:        save_workbook(&wb, "multi-stale", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2651:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2676:        let result = load_workbook(&path);
crates/ql-io/src/qbook_format.rs:2711:        let result = save_workbook(&wb, "conflict", &path);
crates/ql-io/src/qbook_format.rs:2734:        save_workbook(&wb, "extra", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2742:        let result = load_workbook(&path);
crates/ql-io/src/qbook_format.rs:2771:        save_workbook(&wb, "names", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2772:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2804:        save_workbook(&wb, "ds_1900", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2805:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2816:        save_workbook(&wb, "ds_1904", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2817:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2842:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2870:        save_workbook(&wb, "empty_formats", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2872:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2897:        save_workbook(&wb, "custom_formats", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2898:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2929:        save_workbook(&wb, "overlay", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2930:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2964:        save_workbook(&wb, "full", &path).unwrap();
crates/ql-io/src/qbook_format.rs:2965:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:2991:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:3032:        let result = load_workbook(&path);
crates/ql-io/src/qbook_format.rs:3071:        let result = load_workbook(&path);
crates/ql-io/src/qbook_format.rs:3114:        let result = load_workbook(&path);
crates/ql-io/src/qbook_format.rs:3142:        save_workbook(&wb, "pending", &path).unwrap();
crates/ql-io/src/qbook_format.rs:3144:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:3176:        save_workbook(&wb, "spill-skip", &path).unwrap();
crates/ql-io/src/qbook_format.rs:3226:        save_workbook(&wb, "spill-load", &path).unwrap();
crates/ql-io/src/qbook_format.rs:3227:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:3279:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:3326:        let result = load_workbook(&path);
crates/ql-io/src/qbook_format.rs:3348:        save_workbook(&original, "v1", &path).unwrap();
crates/ql-io/src/qbook_format.rs:3363:            let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:3377:            let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:3394:            let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:3414:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:3428:        save_workbook(&original, "v1", &path).unwrap();
crates/ql-io/src/qbook_format.rs:3452:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:3496:        save_workbook(&wb, "scoped", &path).unwrap();
crates/ql-io/src/qbook_format.rs:3497:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:3551:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:3583:        let result = load_workbook(&path);
crates/ql-io/src/qbook_format.rs:3606:        save_workbook(&wb, "wb_only", &path).unwrap();
crates/ql-io/src/qbook_format.rs:3615:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:3645:        let result = load_workbook(&path);
crates/ql-io/src/qbook_format.rs:3671:        let result = load_workbook(&path);
crates/ql-io/src/qbook_format.rs:3723:        save_workbook(&wb, "test", &path).unwrap();
crates/ql-io/src/qbook_format.rs:3725:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:3777:        save_workbook(&wb, "test", &path).unwrap();
crates/ql-io/src/qbook_format.rs:3778:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:3830:        save_workbook(&wb, "test", &path).unwrap();
crates/ql-io/src/qbook_format.rs:3831:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:3853:        save_workbook(&wb, "test", &path).unwrap();
crates/ql-io/src/qbook_format.rs:3876:        let loaded = load_workbook(&path).unwrap();
crates/ql-io/src/qbook_format.rs:3918:        let err = load_workbook(&path).unwrap_err();
crates/ql-io/src/qbook_format.rs:3956:        let err = load_workbook(&path).unwrap_err();
crates/ql-io/src/qbook_format.rs:3990:        let err = load_workbook(&path).unwrap_err();
crates/ql-io/src/qbook_format.rs:4031:        let err = load_workbook(&path).unwrap_err();
crates/ql-io/src/qbook_format.rs:4075:        let err = load_workbook(&path).unwrap_err();
crates/ql-io/src/qbook_format.rs:4137:        let err = load_workbook(&path).unwrap_err();
crates/ql-io/src/qbook_format.rs:4181:        let err = load_workbook(&path).unwrap_err();
crates/ql-io/src/qbook_format.rs:4246:        let err = load_workbook(&path).unwrap_err();
crates/ql-io/src/qbook_format.rs:4284:        let err = load_workbook(&path).unwrap_err();
crates/ql-io/src/qbook_format.rs:4328:        let err = load_workbook(&path).unwrap_err();
crates/ql-io/src/qbook_format.rs:4369:        let err = load_workbook(&path).unwrap_err();
crates/ql-io/src/qbook_format.rs:4426:        let loaded = load_workbook(&path).expect("non-overlapping tables must load");
crates/ql-exec/src/workbook_runtime.rs:49:use crate::plan_cache::{PlanCache, PlanCacheKey, PlanCacheStats};
crates/ql-exec/src/workbook_runtime.rs:397:pub struct WorkbookRuntime<'a> {
crates/ql-exec/src/workbook_runtime.rs:407:    /// See `crate::plan_cache` module docs for the invalidation contract.
crates/ql-exec/src/workbook_runtime.rs:408:    plan_cache: PlanCache,
crates/ql-exec/src/workbook_runtime.rs:431:            plan_cache: PlanCache::new(),
crates/ql-exec/src/workbook_runtime.rs:455:            plan_cache: PlanCache::new(),
crates/ql-exec/src/workbook_runtime.rs:477:            plan_cache: PlanCache::new(),
crates/ql-exec/src/workbook_runtime.rs:499:            plan_cache: PlanCache::new(),
crates/ql-exec/src/workbook_runtime.rs:509:        self.plan_cache.stats()
crates/ql-exec/src/workbook_runtime.rs:546:            self.plan_cache
crates/ql-exec/src/workbook_runtime.rs:946:                .plan_cache
crates/ql-exec/src/workbook_runtime.rs:1473:        self.plan_cache.clear();
crates/ql-exec/src/workbook_runtime.rs:1761:        self.plan_cache.clear();
crates/ql-exec/src/workbook_runtime.rs:1899:        self.plan_cache.clear();
crates/ql-exec/src/workbook_runtime.rs:2132:        self.plan_cache.clear();
crates/ql-exec/src/workbook_runtime.rs:2295:        self.plan_cache.clear();
crates/ql-exec/src/workbook_runtime.rs:2535:                overlay.clear(row, col);
crates/ql-exec/src/workbook_runtime.rs:3063:                                    match self.plan_cache.get_or_insert::<_, RuntimeError>(
crates/ql-exec/src/workbook_runtime.rs:3211:        // borrow on `self.plan_cache`. Re-borrow the workbook reference
crates/ql-exec/src/workbook_runtime.rs:3216:            self.plan_cache
crates/ql-exec/src/workbook_runtime.rs:3699:        ql_io::save_workbook(&wb, "rt-e2e", &path).unwrap();
crates/ql-exec/src/workbook_runtime.rs:3700:        let mut loaded = ql_io::load_workbook(&path).unwrap();
crates/ql-exec/src/workbook_runtime.rs:4367:    fn name_table_mutation_invalidates_bind_plan_cache() {
crates/ql-exec/src/workbook_runtime.rs:4446:    /// counterpart `Timings::bind_plan_cache_hits/misses` fields exist
crates/ql-exec/src/workbook_runtime.rs:4462:        timings.bind_plan_cache_hits = s.hits;
crates/ql-exec/src/workbook_runtime.rs:4463:        timings.bind_plan_cache_misses = s.misses;
crates/ql-exec/src/workbook_runtime.rs:4464:        assert_eq!(timings.bind_plan_cache_hits, 1);
crates/ql-exec/src/workbook_runtime.rs:4465:        assert_eq!(timings.bind_plan_cache_misses, 1);
crates/ql-exec/src/workbook_runtime.rs:4466:        assert_eq!(timings.bind_plan_cache_hit_rate(), Some(0.5));
crates/ql-exec/src/workbook_runtime.rs:4767:    fn validate_formula_does_not_pollute_plan_cache() {
crates/ql-exec/src/workbook_runtime.rs:5602:            ql_io::save_workbook(&wb, "OVR-3-03", &path).unwrap();
crates/ql-exec/src/workbook_runtime.rs:5605:        let wb_loaded = ql_io::load_workbook(&path).unwrap();
crates/ql-exec/src/workbook_runtime.rs:7364:    fn set_sheet_scoped_name_invalidates_plan_cache() {

exec
/bin/zsh -lc "nl -ba crates/ql-exec/tests/oplog_e2e.rs | sed -n '978,1015p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   978	        let mut rt = WorkbookRuntime::new(&mut reloaded, &reg);
   979	        let n = rt.rename_column("Sales", "Qty", "Quantity").unwrap();
   980	        assert_eq!(n, 1, "one formula rewritten");
   981	        assert!(rt.recompute_all().is_complete());
   982	    }
   983	    let text = reloaded.formula_at(0, 5, 0).expect("formula").clone();
   984	    assert_eq!(text.as_ref(), "SUM(Sales[Quantity])");
   985	    assert_eq!(
   986	        reloaded.read(Address::new(0, 5, 0)),
   987	        Value::Number(30.0),
   988	        "post-rename SUM(Sales[Quantity]) value preserved"
   989	    );
   990	    // Metadata reflects the rename.
   991	    let meta = reloaded.lookup_table("Sales").unwrap();
   992	    assert!(meta.lookup_column("Quantity").is_some());
   993	    assert!(meta.lookup_column("Qty").is_none());
   994	}

exec
/bin/zsh -lc "nl -ba crates/ql-io/src/qbook_format.rs | sed -n '1410,1455p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1410	/// Phase 2A.8 audit H2: before reading, calls `recover_from_crashed_save` to
  1411	/// detect and resolve orphan `.bak-<random>` siblings left by a prior crashed
  1412	/// save. Recovery either rolls forward (target valid, cleanup orphan) or rolls
  1413	/// back (target missing/invalid, restore orphan).
  1414	pub fn load_workbook(path: &Path) -> Result<Workbook, QbookError> {
  1415	    // Recovery happens BEFORE the directory check — if the prior save crashed
  1416	    // between step 2 (rename → bak) and step 3 (rename temp → target), the
  1417	    // target may not yet exist. Recovery restores it from .bak.
  1418	    recover_from_crashed_save(path)?;
  1419	
  1420	    if !path.is_dir() {
  1421	        return Err(QbookError::NotADirectory {
  1422	            path: path.to_path_buf(),
  1423	        });
  1424	    }
  1425	
  1426	    // Read + parse envelope.
  1427	    let toml_path = path.join("workbook.toml");
  1428	    if !toml_path.is_file() {
  1429	        return Err(QbookError::MissingFile { file: toml_path });
  1430	    }
  1431	    let toml_str = fs::read_to_string(&toml_path)?;
  1432	    let envelope: WorkbookEnvelope = toml::from_str(&toml_str)?;
  1433	
  1434	    // Phase 2A.8: accept schema versions in [MIN_SUPPORTED..=current].
  1435	    // v1 envelopes load on v2 binaries (with `names: None`); future v3 will
  1436	    // reject with this same path until callers explicitly opt in.
  1437	    if envelope.schema_version < MIN_SUPPORTED_SCHEMA_VERSION
  1438	        || envelope.schema_version > WORKBOOK_SCHEMA_VERSION
  1439	    {
  1440	        return Err(QbookError::UnsupportedSchema {
  1441	            found: envelope.schema_version,
  1442	        });
  1443	    }
  1444	    let is_v1 = envelope.schema_version == 1;
  1445	
  1446	    // Phase 2A.13 audit cycle-3 H4: counter for legacy-pending migrations so we
  1447	    // can warn the user once per load (rather than silently corrupting any
  1448	    // legitimate `#NULL!` formula results from v1 workbooks).
  1449	    let mut legacy_pending_migrations: usize = 0;
  1450	
  1451	    // Construct the Workbook + sheets in id order. Sheet ids in the envelope must be
  1452	    // sequential 0..N — Workbook::add_sheet allocates ids in that order.
  1453	    // Audit M3 fix (2026-05-12): previously this panicked; now returns Result error.
  1454	    let mut wb = Workbook::new();
  1455	    // **W5-71 (Phase 4.5.A.2):** apply the envelope's date_system, defaulting

exec
/bin/zsh -lc "nl -ba crates/ql-io/src/qbook_format.rs | sed -n '400,445p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '380,560p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   400	/// `TableEntry` preserves stable column ids across save/load (the
   401	/// loader bypasses `TableTable::allocate_column_id` and `insert`
   402	/// auto-bumps the allocator past the highest persisted id).
   403	///
   404	/// Entries are sorted ascending by canonical (uppercase) name on save
   405	/// for deterministic diffs.
   406	#[derive(Clone, Debug, Serialize, Deserialize)]
   407	#[serde(deny_unknown_fields)]
   408	pub struct TablesSection {
   409	    pub entries: Vec<TableEntry>,
   410	}
   411	
   412	/// **W5-123 (Phase 4.8.L):** one entry in `TablesSection`. Mirrors
   413	/// `ql_storage::TableMetadata`.
   414	#[derive(Clone, Debug, Serialize, Deserialize)]
   415	#[serde(deny_unknown_fields)]
   416	pub struct TableEntry {
   417	    /// Canonical (uppercase) name. The loader keys `TableTable` by this.
   418	    pub name: String,
   419	    /// Case-preserving display name (the form the user typed at create
   420	    /// time; printed by the formula printer).
   421	    pub display_name: String,
   422	    pub sheet: u16,
   423	    pub top_row: u32,
   424	    pub top_col: u32,
   425	    pub rows: u32,
   426	    pub cols: u32,
   427	    pub has_header: bool,
   428	    pub has_totals: bool,
   429	    pub columns: Vec<TableColumnEntry>,
   430	}
   431	
   432	/// **W5-123 (Phase 4.8.L):** one entry in `TableEntry::columns`.
   433	/// Mirrors `ql_storage::TableColumn`. `id` is the stable monotonic
   434	/// id allocated at create / resize time; preserving it across
   435	/// save/load is necessary so future Phase-5 column-move ops can
   436	/// reference columns by id rather than position.
   437	#[derive(Clone, Debug, Serialize, Deserialize)]
   438	#[serde(deny_unknown_fields)]
   439	pub struct TableColumnEntry {
   440	    pub id: u32,
   441	    /// Canonical (lowercase) name; used for case-insensitive `Table[Col]`
   442	    /// lookups via `TableMetadata::lookup_column`.
   443	    pub name: String,
   444	    /// Case-preserving display name (the form the user typed; printed
   445	    /// by `Sales[<display>]`).

 succeeded in 0ms:
   380	    /// Number of structural failures (lex/parse/bind). Excludes evaluation
   381	    /// errors like `#DIV/0!` which are normal cell values.
   382	    pub fn failed_count(&self) -> usize {
   383	        self.failures.len()
   384	    }
   385	}
   386	
   387	/// Live-formula facade. Wraps a `&mut Workbook` + `&FunctionRegistry`.
   388	///
   389	/// Construct one per session of cell edits. Re-creating per call is cheap (the
   390	/// struct holds borrows, no heap state of its own).
   391	///
   392	/// Phase 2A.3.b (2026-05-12): optionally attach an `OpLog` via
   393	/// `WorkbookRuntime::with_oplog`. When attached, `set_value` and `set_formula`
   394	/// emit ops into the log in append-before-mutate order. `transaction()`
   395	/// re-borrows the op-log handle into the returned transaction so a single op
   396	/// log records both individual edits and batched commits cohesively.
   397	pub struct WorkbookRuntime<'a> {
   398	    workbook: &'a mut Workbook,
   399	    registry: &'a FunctionRegistry,
   400	    /// Optional op-log sink. When `Some`, all producer methods append before
   401	    /// mutating the workbook so a failing append leaves the workbook
   402	    /// unchanged. When `None`, the runtime behaves exactly as Phase 1 W5-10.
   403	    oplog: Option<&'a mut OpLog>,
   404	    /// Phase 2B.3 bind-plan cache. Lives for the runtime's lifetime;
   405	    /// successive `set_formula` and `recompute_all` calls hit the cache
   406	    /// for unchanged formula text under the same NameTable generation.
   407	    /// See `crate::plan_cache` module docs for the invalidation contract.
   408	    plan_cache: PlanCache,
   409	    /// Phase 3.1 (2026-05-12) optional calcgraph session. When `Some`,
   410	    /// every mutation method calls the corresponding hook on the
   411	    /// session AFTER the workbook mutation succeeds. Phase 3.1 hooks
   412	    /// are stubs (counter bumps + cell-index updates); Phase 3.3 wires
   413	    /// dirty propagation through the same surface. See
   414	    /// `crate::calcgraph_session` module docs for the ownership model.
   415	    graph: Option<&'a mut crate::CalcgraphSession>,
   416	    /// **W5-82 (Phase 4.5.D part 6):** parsed `FormatString` cache keyed
   417	    /// by `FormatId`. `read_display` populates on-demand from
   418	    /// `Workbook::formats()` lookup; `FormatTable::register_at` rejects
   419	    /// id→string mutations, so cache entries stay stable for the
   420	    /// runtime's lifetime. A new `RegisterFormat` op merely inserts a
   421	    /// new entry; no invalidation needed.
   422	    format_cache: std::collections::HashMap<FormatId, FormatString>,
   423	}
   424	
   425	impl<'a> WorkbookRuntime<'a> {
   426	    pub fn new(workbook: &'a mut Workbook, registry: &'a FunctionRegistry) -> Self {
   427	        Self {
   428	            workbook,
   429	            registry,
   430	            oplog: None,
   431	            plan_cache: PlanCache::new(),
   432	            format_cache: std::collections::HashMap::new(),
   433	            graph: None,
   434	        }
   435	    }
   436	
   437	    /// Phase 2A.3.b: construct a runtime that records every mutation to the
   438	    /// supplied `OpLog`. Producer-replay equivalence: replaying the resulting
   439	    /// log against a fresh workbook (then calling `recompute_all` to
   440	    /// materialize formula values) reproduces the same observable state.
   441	    ///
   442	    /// See module docs for the documented limitations: `set_value(Value::Blank)`
   443	    /// emits no `PutValue` (CellWireValue lacks a Blank variant in 2A.3.b);
   444	    /// NaN/Inf in number values fail serde_json serialization and surface as
   445	    /// `RuntimeError::OpLog`.
   446	    pub fn with_oplog(
   447	        workbook: &'a mut Workbook,
   448	        registry: &'a FunctionRegistry,
   449	        oplog: &'a mut OpLog,
   450	    ) -> Self {
   451	        Self {
   452	            workbook,
   453	            registry,
   454	            oplog: Some(oplog),
   455	            plan_cache: PlanCache::new(),
   456	            format_cache: std::collections::HashMap::new(),
   457	            graph: None,
   458	        }
   459	    }
   460	
   461	    /// Phase 3.1 (2026-05-12): construct a runtime that fires calcgraph
   462	    /// mutation hooks on every producer method. Phase 3.1 hooks are
   463	    /// stubs (counter bumps + cell-index updates) — they accumulate
   464	    /// state that Phase 3.3 will turn into real dirty propagation and
   465	    /// edge updates. Today's recompute_all still walks formulas in
   466	    /// HashMap order; Phase 3.4 replaces it with a Tarjan-SCC-scheduled
   467	    /// graph walk.
   468	    pub fn with_graph(
   469	        workbook: &'a mut Workbook,
   470	        registry: &'a FunctionRegistry,
   471	        graph: &'a mut crate::CalcgraphSession,
   472	    ) -> Self {
   473	        Self {
   474	            workbook,
   475	            registry,
   476	            oplog: None,
   477	            plan_cache: PlanCache::new(),
   478	            format_cache: std::collections::HashMap::new(),
   479	            graph: Some(graph),
   480	        }
   481	    }
   482	
   483	    /// Phase 3.1 (2026-05-12): construct a runtime with BOTH an attached
   484	    /// op log and an attached calcgraph session. The combined-attachment
   485	    /// constructor mirrors the IDE pattern (open file → load oplog +
   486	    /// rebuild graph → hand both to the runtime per edit). Order of
   487	    /// operations per mutation: lex+parse+bind → op-log append → workbook
   488	    /// mutation → graph hook.
   489	    pub fn with_oplog_and_graph(
   490	        workbook: &'a mut Workbook,
   491	        registry: &'a FunctionRegistry,
   492	        oplog: &'a mut OpLog,
   493	        graph: &'a mut crate::CalcgraphSession,
   494	    ) -> Self {
   495	        Self {
   496	            workbook,
   497	            registry,
   498	            oplog: Some(oplog),
   499	            plan_cache: PlanCache::new(),
   500	            format_cache: std::collections::HashMap::new(),
   501	            graph: Some(graph),
   502	        }
   503	    }
   504	
   505	    /// Phase 2B.3: snapshot of cumulative bind-plan-cache observability
   506	    /// since this runtime was constructed. Includes hit count, miss
   507	    /// count, entry count, and convenience hit-rate.
   508	    pub fn cache_stats(&self) -> PlanCacheStats {
   509	        self.plan_cache.stats()
   510	    }
   511	
   512	    /// Set a cell to a formula. Pipeline: lex → parse → bind (against `sheet`) →
   513	    /// eval against the current workbook → put_at the result + put_formula the text.
   514	    ///
   515	    /// Returns the evaluated value, or a `RuntimeError` if any pipeline stage fails.
   516	    /// On error, the workbook is unchanged (no partial writes).
   517	    ///
   518	    /// `formula_text` is the formula body without the leading `=`. The Sheet must
   519	    /// already exist.
   520	    pub fn set_formula(
   521	        &mut self,
   522	        sheet: SheetId,
   523	        row: RowId,
   524	        col: ColId,
   525	        formula_text: impl Into<Arc<str>>,
   526	    ) -> Result<Value, RuntimeError> {
   527	        // Phase 2A.7 audit H1 (was 2A.6 L4 sheet-only): validate sheet AND
   528	        // row/col bounds up front. Without these checks, out-of-grid cells
   529	        // sneak past lex/parse/bind and panic inside `Sheet::put` (which
   530	        // asserts `row <= MAX_ROW && col <= MAX_COLUMN`).
   531	        validate_cell(self.workbook, sheet, row, col)?;
   532	
   533	        let formula_text = formula_text.into();
   534	        // Phase 2B.3: consult the bind-plan cache first. The key includes
   535	        // the current NameTable generation so a name registration since
   536	        // the last bind produces a miss → re-bind against the new table.
   537	        // Cached `Arc<ExprPlan>` is shared with `recompute_all`, so a
   538	        // subsequent recompute of this cell skips lex/parse/bind entirely.
   539	        let name_gen = self.workbook.names().generation();
   540	        let cache_key = PlanCacheKey {
   541	            text: Arc::clone(&formula_text),
   542	            sheet,
   543	            name_gen,
   544	        };
   545	        let plan: Arc<crate::plan::ExprPlan> =
   546	            self.plan_cache
   547	                .get_or_insert::<_, RuntimeError>(cache_key, || {
   548	                    let tokens = lex(formula_text.as_ref())?;
   549	                    let expr = parse(tokens)?;
   550	                    // Phase 2A.1 (2026-05-12): bind against the workbook's
   551	                    // NameTable so `Expr::NameRef` resolves against defined
   552	                    // names. W5-92 (Phase 4.6.D): pass `&Workbook` for names
   553	                    // so the two-tier sheet-then-workbook chain fires.
   554	                    // **W5-114 (Phase 4.8.E):** carry the formula's own cell
   555	                    // address through BindSite; 4.8.F's structured-ref
   556	                    // `[@Col]` resolution reads it.
   557	                    Ok(bind_with_site(
   558	                        &expr,
   559	                        BindSite::at_cell(ql_types::Address::new(sheet, row, col)),
   560	                        self.workbook,

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '534,570p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '3038,3080p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   534	        // Phase 2B.3: consult the bind-plan cache first. The key includes
   535	        // the current NameTable generation so a name registration since
   536	        // the last bind produces a miss → re-bind against the new table.
   537	        // Cached `Arc<ExprPlan>` is shared with `recompute_all`, so a
   538	        // subsequent recompute of this cell skips lex/parse/bind entirely.
   539	        let name_gen = self.workbook.names().generation();
   540	        let cache_key = PlanCacheKey {
   541	            text: Arc::clone(&formula_text),
   542	            sheet,
   543	            name_gen,
   544	        };
   545	        let plan: Arc<crate::plan::ExprPlan> =
   546	            self.plan_cache
   547	                .get_or_insert::<_, RuntimeError>(cache_key, || {
   548	                    let tokens = lex(formula_text.as_ref())?;
   549	                    let expr = parse(tokens)?;
   550	                    // Phase 2A.1 (2026-05-12): bind against the workbook's
   551	                    // NameTable so `Expr::NameRef` resolves against defined
   552	                    // names. W5-92 (Phase 4.6.D): pass `&Workbook` for names
   553	                    // so the two-tier sheet-then-workbook chain fires.
   554	                    // **W5-114 (Phase 4.8.E):** carry the formula's own cell
   555	                    // address through BindSite; 4.8.F's structured-ref
   556	                    // `[@Col]` resolution reads it.
   557	                    Ok(bind_with_site(
   558	                        &expr,
   559	                        BindSite::at_cell(ql_types::Address::new(sheet, row, col)),
   560	                        self.workbook,
   561	                        self.workbook,
   562	                        self.workbook,
   563	                    )?)
   564	                })?;
   565	
   566	        // **Phase 2A.3.b op-log emission, BEFORE any storage mutation.**
   567	        // The contract on this method ("On error, the workbook is unchanged")
   568	        // requires that op-log append fail BEFORE we touch workbook state.
   569	        // We emit `PutFormula` only — the evaluated value isn't recorded
   570	        // because replay re-derives it via `WorkbookRuntime::recompute_all`

 succeeded in 0ms:
  3038	                                    if !seen.insert(n) {
  3039	                                        continue;
  3040	                                    }
  3041	                                    let Some((s, r, c)) = session.cell_address_for(n) else {
  3042	                                        continue;
  3043	                                    };
  3044	                                    if (s, r, c) == (sheet, row, col) {
  3045	                                        continue;
  3046	                                    }
  3047	                                    let Some(text) = self.workbook.formula_at(s, r, c).cloned()
  3048	                                    else {
  3049	                                        continue;
  3050	                                    };
  3051	                                    readers.push((n, s, r, c, text));
  3052	                                }
  3053	                            }
  3054	                            for (rn, reader_sheet, reader_row, reader_col, reader_text) in readers {
  3055	                                let name_gen = self.workbook.names().generation();
  3056	                                let cache_key = PlanCacheKey {
  3057	                                    text: Arc::clone(&reader_text),
  3058	                                    sheet: reader_sheet,
  3059	                                    name_gen,
  3060	                                };
  3061	                                let workbook: &Workbook = self.workbook;
  3062	                                let plan: Arc<crate::plan::ExprPlan> =
  3063	                                    match self.plan_cache.get_or_insert::<_, RuntimeError>(
  3064	                                        cache_key,
  3065	                                        || {
  3066	                                            let tokens = lex(reader_text.as_ref())?;
  3067	                                            let expr = parse(tokens)?;
  3068	                                            // **W5-114 (Phase 4.8.E):** reader cell addr.
  3069	                                            Ok(bind_with_site(
  3070	                                                &expr,
  3071	                                                BindSite::at_cell(ql_types::Address::new(
  3072	                                                    reader_sheet,
  3073	                                                    reader_row,
  3074	                                                    reader_col,
  3075	                                                )),
  3076	                                                workbook,
  3077	                                                workbook,
  3078	                                                workbook,
  3079	                                            )?)
  3080	                                        },

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/plan_cache.rs | sed -n '1,140p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
     1	//! Phase 2B.3 (2026-05-12) — bind-plan cache V0.
     2	//!
     3	//! Caches parsed + bound [`ExprPlan`] values keyed by the formula text, the
     4	//! owning sheet, and the workbook's NameTable generation. Lives on
     5	//! [`crate::WorkbookRuntime`] and is shared into spawned
     6	//! [`crate::WorkbookTransaction`] instances through forwarding.
     7	//!
     8	//! ## Invalidation contract
     9	//!
    10	//! Cache entries are NOT explicitly removed on workbook mutation. Instead,
    11	//! the cache key is `(formula_text, sheet_id, name_table_generation)`. When
    12	//! the workbook mutates state that would change a bind result:
    13	//!
    14	//! - User edits formula text → new text, new key, automatic miss.
    15	//! - Caller registers/removes a name → `NameTable::generation()` advances
    16	//!   (per Phase 2B.3 workbook.rs), so plans bound against the prior
    17	//!   generation no longer match.
    18	//!
    19	//! Stale entries accumulate over time; Phase 3 calcgraph integration adds
    20	//! explicit eviction tied to dirty propagation. For Phase 2B.3 the cache
    21	//! is small enough that drift is acceptable — typical workbook formula
    22	//! cardinality is in the thousands, not millions.
    23	//!
    24	//! ## What the cache stores
    25	//!
    26	//! `Arc<ExprPlan>` so cache hits hand back a refcount-bump without cloning
    27	//! the plan tree. `ExprPlan` is `Clone + Debug + PartialEq` per
    28	//! `ql-exec::plan` but the plan tree is non-trivial; sharing via `Arc`
    29	//! keeps the cache cheap.
    30	//!
    31	//! ## References
    32	//!
    33	//! - `.references/hyperformula/src/parser/ParserWithCaching.ts` — HF caches
    34	//!   parsed ASTs keyed by formula text + sheet metadata for the same
    35	//!   reason. They evict on text change; we additionally include
    36	//!   `NameTable::generation` so the bind result (not just the parse) stays
    37	//!   consistent.
    38	//! - `.references/hyperformula/src/parser/Cache.ts` — the size-bounded LRU
    39	//!   wrapper. V0 here is unbounded; a bounded eviction policy lands when
    40	//!   workbook size makes it necessary.
    41	
    42	use std::collections::HashMap;
    43	use std::sync::Arc;
    44	
    45	use ql_types::SheetId;
    46	
    47	use crate::plan::ExprPlan;
    48	
    49	/// Composite cache key. Reflects everything that, if changed, would change
    50	/// the bind result for a given formula text:
    51	///
    52	/// - `text` — the raw formula source (without leading `=`). Different text
    53	///   yields a different parse tree, so plans don't share.
    54	/// - `sheet` — owning sheet for the binding. Same text in different sheets
    55	///   binds to different cell references (relative refs resolve against the
    56	///   owning sheet).
    57	/// - `name_gen` — `NameTable::generation()` at the moment of bind. A
    58	///   subsequent name registration / removal bumps this, so old plans
    59	///   become unreachable.
    60	#[derive(Clone, Debug, Hash, Eq, PartialEq)]
    61	pub struct PlanCacheKey {
    62	    pub text: Arc<str>,
    63	    pub sheet: SheetId,
    64	    pub name_gen: u64,
    65	}
    66	
    67	/// Snapshot of cache observability data — exposed via
    68	/// `WorkbookRuntime::cache_stats()` and surfaced through `ql-profile`.
    69	#[derive(Clone, Copy, Debug, Default, PartialEq, Eq)]
    70	pub struct PlanCacheStats {
    71	    pub hits: u64,
    72	    pub misses: u64,
    73	    pub entries: usize,
    74	}
    75	
    76	impl PlanCacheStats {
    77	    /// Hit rate as a fraction in `[0.0, 1.0]`. Returns `None` when no
    78	    /// lookups have happened yet so callers don't divide by zero or
    79	    /// display "0.0%" for an unused cache.
    80	    pub fn hit_rate(&self) -> Option<f64> {
    81	        let total = self.hits + self.misses;
    82	        if total == 0 {
    83	            None
    84	        } else {
    85	            Some(self.hits as f64 / total as f64)
    86	        }
    87	    }
    88	}
    89	
    90	/// Phase 2B.3 bind-plan cache.
    91	///
    92	/// Stores `Arc<ExprPlan>` per `(text, sheet, name_gen)` key. Hits return
    93	/// the cached plan (refcount-bump only); misses run the caller-supplied
    94	/// build closure. Accumulates hit/miss counters for observability.
    95	///
    96	/// `Default` constructs an empty cache. The cache is opt-in via
    97	/// `WorkbookRuntime::with_plan_cache_disabled` — not yet implemented; the
    98	/// default runtime constructor builds an empty cache that gets populated
    99	/// on first lookup.
   100	#[derive(Debug, Default)]
   101	pub struct PlanCache {
   102	    entries: HashMap<PlanCacheKey, Arc<ExprPlan>>,
   103	    hits: u64,
   104	    misses: u64,
   105	}
   106	
   107	impl PlanCache {
   108	    /// Empty cache. Same as `Default::default`.
   109	    pub fn new() -> Self {
   110	        Self::default()
   111	    }
   112	
   113	    /// Look up the plan for `key`. On hit, returns the cached `Arc<ExprPlan>`
   114	    /// and increments the hit counter. On miss, runs `build()` (which does
   115	    /// the lex/parse/bind), stores the result, and returns it. Errors from
   116	    /// `build()` are passed through without caching — failed binds aren't
   117	    /// memoized, so a fix to the formula text gets a clean retry.
   118	    pub fn get_or_insert<F, E>(&mut self, key: PlanCacheKey, build: F) -> Result<Arc<ExprPlan>, E>
   119	    where
   120	        F: FnOnce() -> Result<ExprPlan, E>,
   121	    {
   122	        if let Some(plan) = self.entries.get(&key) {
   123	            self.hits += 1;
   124	            return Ok(Arc::clone(plan));
   125	        }
   126	        self.misses += 1;
   127	        let plan = Arc::new(build()?);
   128	        self.entries.insert(key, Arc::clone(&plan));
   129	        Ok(plan)
   130	    }
   131	
   132	    /// Insert a plan unconditionally (overwriting any existing entry for
   133	    /// the same key). Available for callers that already bound the plan
   134	    /// out-of-band and want to pre-warm the cache.
   135	    ///
   136	    /// Phase 2B.7 audit (correctness L1 / D4): the prior doc-comment
   137	    /// claimed `WorkbookRuntime::set_formula` uses this method — it
   138	    /// doesn't (set_formula uses `get_or_insert`). No production caller
   139	    /// uses `insert` today; the method stays for tests and future pre-
   140	    /// warming callers (Phase 3 calcgraph integration may need it).

exec
/bin/zsh -lc "rg -n \"table_gen|tables\\(\\)\\.generation|PlanCacheKey\" crates/ql-exec/src crates/ql-storage/src" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
crates/ql-storage/src/tables.rs:541:    fn table_table_generation_bumps_on_mutation() {
crates/ql-exec/src/lib.rs:101:pub use plan_cache::{PlanCache, PlanCacheKey, PlanCacheStats};
crates/ql-exec/src/plan_cache.rs:11://! the cache key is `(formula_text, sheet_id, name_table_generation)`. When
crates/ql-exec/src/plan_cache.rs:61:pub struct PlanCacheKey {
crates/ql-exec/src/plan_cache.rs:102:    entries: HashMap<PlanCacheKey, Arc<ExprPlan>>,
crates/ql-exec/src/plan_cache.rs:118:    pub fn get_or_insert<F, E>(&mut self, key: PlanCacheKey, build: F) -> Result<Arc<ExprPlan>, E>
crates/ql-exec/src/plan_cache.rs:141:    pub fn insert(&mut self, key: PlanCacheKey, plan: Arc<ExprPlan>) {
crates/ql-exec/src/plan_cache.rs:179:    fn key(text: &str, sheet: SheetId, name_gen: u64) -> PlanCacheKey {
crates/ql-exec/src/plan_cache.rs:180:        PlanCacheKey {
crates/ql-exec/src/workbook_runtime.rs:49:use crate::plan_cache::{PlanCache, PlanCacheKey, PlanCacheStats};
crates/ql-exec/src/workbook_runtime.rs:540:        let cache_key = PlanCacheKey {
crates/ql-exec/src/workbook_runtime.rs:940:            let cache_key = PlanCacheKey {
crates/ql-exec/src/workbook_runtime.rs:3056:                                let cache_key = PlanCacheKey {
crates/ql-exec/src/workbook_runtime.rs:3204:        let cache_key = PlanCacheKey {
crates/ql-exec/src/workbook_runtime.rs:4424:    /// PlanCacheKey) but we exercise it end-to-end through the runtime.

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '1637,1665p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1637	        self.workbook
  1638	            .tables_mut()
  1639	            .insert(Arc::from(canonical.as_str()), meta);
  1640	        Ok(())
  1641	    }
  1642	
  1643	    /// **W5-118 (Phase 4.8.H):** drop a table's metadata. Cells inside
  1644	    /// the table footprint are untouched. Formulas referencing the
  1645	    /// dropped table re-bind to `BindError::UnknownTable` on next
  1646	    /// recompute. Emits `Op::DropTable` to the attached op log (if any).
  1647	    pub fn drop_table(&mut self, name: &str) -> Result<(), RuntimeError> {
  1648	        let canonical = name.to_ascii_uppercase();
  1649	        if self.workbook.tables().lookup(&canonical).is_none() {
  1650	            return Err(RuntimeError::TableNotFound(name.to_owned()));
  1651	        }
  1652	        if let Some(oplog) = self.oplog.as_deref_mut() {
  1653	            oplog.append(Op::DropTable {
  1654	                name: canonical.clone(),
  1655	            })?;
  1656	        }
  1657	        let _ = self.workbook.tables_mut().remove(&canonical);
  1658	        Ok(())
  1659	    }
  1660	
  1661	    /// **W5-119 (Phase 4.8.I):** rename a table. Per design § 12.2 + HIGH-1
  1662	    /// closure (Codex pass-1), this REWRITES STORED FORMULA TEXT — Excel
  1663	    /// canon: the next bind sees the new name and binds successfully.
  1664	    ///
  1665	    /// Process:

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '1455,1480p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1455	
  1456	        // 5. Calcgraph notification — fan out the same as workbook-
  1457	        //    scoped sets. Phase 3.3's name→formula tracking is name-
  1458	        //    keyed and doesn't currently distinguish scopes; over-
  1459	        //    invalidation is the conservative direction here.
  1460	        if let Some(g) = self.graph.as_deref_mut() {
  1461	            g.on_set_name(name);
  1462	        }
  1463	
  1464	        // 6. **W5-93 (Phase 4.6.E closure):** invalidate the plan cache.
  1465	        //    Codex HIGH-2: the cache key currently includes only the
  1466	        //    workbook-scoped `NameTable::generation()` — a sheet-scoped
  1467	        //    name change wouldn't bump that counter, so cached plans
  1468	        //    bound against `=Rate` (resolved to workbook-scoped) would
  1469	        //    keep evaluating against the workbook value even after a
  1470	        //    sheet-scoped `Rate` was registered. Full flush is acceptable
  1471	        //    at edit rate (matches the rename pattern); per-sheet
  1472	        //    generation counters are design § 10.5 future polish.
  1473	        self.plan_cache.clear();
  1474	
  1475	        Ok(())
  1476	    }
  1477	
  1478	    // ===== W5-118 (Phase 4.8.H) — table mutation API =====
  1479	
  1480	    /// **W5-118 (Phase 4.8.H):** register a new workbook-scoped table,

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '1888,1905p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1888	        let (col_idx, _) = meta
  1889	            .lookup_column(old_col)
  1890	            .expect("verified at top before any mutation");
  1891	        meta.columns[col_idx as usize].name = Arc::from(new_lower.as_str());
  1892	        meta.columns[col_idx as usize].display = Arc::clone(&new_display_arc);
  1893	        // `get_mut` doesn't bump generation; do it explicitly so plan
  1894	        // caches keyed on `TableTable::generation` invalidate.
  1895	        self.workbook.tables_mut().bump_generation();
  1896	        // Invalidate plan cache by clearing — every formula that
  1897	        // referenced the OLD column now has new text, so cache lookups
  1898	        // miss anyway; bare invalidation prevents stale entries.
  1899	        self.plan_cache.clear();
  1900	        Ok(rewritten)
  1901	    }
  1902	
  1903	    /// **W5-122 (Phase 4.8.J):** resize a table's footprint per
  1904	    /// design § 12.3. Three scenarios are supported by a single op:
  1905	    ///

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '1748,1770p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1748	        // Re-key the TableTable entry.
  1749	        let mut meta = self
  1750	            .workbook
  1751	            .tables_mut()
  1752	            .remove(&old_canonical)
  1753	            .expect("verified at top");
  1754	        let new_canonical_arc: Arc<str> = Arc::from(new_canonical.as_str());
  1755	        meta.name = Arc::clone(&new_canonical_arc);
  1756	        meta.display_name = Arc::clone(&new_display_arc);
  1757	        self.workbook.tables_mut().insert(new_canonical_arc, meta);
  1758	        // Invalidate plan cache by clearing — every formula that
  1759	        // referenced the OLD name now has new text, so cache lookups
  1760	        // miss anyway; the bare invalidation prevents stale entries.
  1761	        self.plan_cache.clear();
  1762	        Ok(rewritten)
  1763	    }
  1764	
  1765	    /// **W5-121 (Phase 4.8.I.2):** rename a column within an existing
  1766	    /// table. Mirrors [`Self::rename_table`] semantics for column refs:
  1767	    /// rewrites stored formula text in every cell whose StructuredRef
  1768	    /// references the renamed column (Excel canon — the next bind sees
  1769	    /// the new name).
  1770	    ///

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '9660,9790p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  9660	        wb.register_spill((0, 20, 0), SpillShape::new(2, 1))
  9661	            .expect("register_spill must succeed");
  9662	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  9663	        rt.resize_table("Sales", 5, 1, vec![], vec![])
  9664	            .expect("anchor outside new footprint must not block");
  9665	        let meta = wb.lookup_table("Sales").unwrap();
  9666	        assert_eq!(meta.rows, 5);
  9667	    }
  9668	
  9669	    // ===== W5-125 (Phase 4.8.O.1) — footprint-bounds upper-limit check =====
  9670	
  9671	    #[test]
  9672	    fn create_table_rejects_footprint_past_max_row() {
  9673	        use ql_types::MAX_ROW;
  9674	        let mut wb = make_runtime_workbook();
  9675	        let reg = default_registry();
  9676	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  9677	        // top_row = MAX_ROW - 1, rows = 3 → last row = MAX_ROW + 1 > MAX_ROW.
  9678	        let err = rt
  9679	            .create_table(
  9680	                "Far",
  9681	                0,
  9682	                MAX_ROW - 1,
  9683	                0,
  9684	                3,
  9685	                1,
  9686	                true,
  9687	                false,
  9688	                vec!["Qty".into()],
  9689	            )
  9690	            .unwrap_err();
  9691	        match err {
  9692	            RuntimeError::TableCreateRejected { reason, .. } => {
  9693	                assert!(reason.contains("MAX_ROW"), "reason: {reason}");
  9694	            }
  9695	            other => panic!("expected TableCreateRejected, got {other:?}"),
  9696	        }
  9697	    }
  9698	
  9699	    #[test]
  9700	    fn create_table_rejects_footprint_past_max_column() {
  9701	        use ql_types::MAX_COLUMN;
  9702	        let mut wb = make_runtime_workbook();
  9703	        let reg = default_registry();
  9704	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  9705	        // top_col = MAX_COLUMN, cols = 2 → last col = MAX_COLUMN + 1 > MAX_COLUMN.
  9706	        let err = rt
  9707	            .create_table(
  9708	                "Wide",
  9709	                0,
  9710	                0,
  9711	                MAX_COLUMN,
  9712	                1,
  9713	                2,
  9714	                true,
  9715	                false,
  9716	                vec!["A".into(), "B".into()],
  9717	            )
  9718	            .unwrap_err();
  9719	        match err {
  9720	            RuntimeError::TableCreateRejected { reason, .. } => {
  9721	                assert!(reason.contains("MAX_COLUMN"), "reason: {reason}");
  9722	            }
  9723	            other => panic!("expected TableCreateRejected, got {other:?}"),
  9724	        }
  9725	    }
  9726	
  9727	    #[test]
  9728	    fn create_table_rejects_footprint_u32_overflow_on_rows() {
  9729	        let mut wb = make_runtime_workbook();
  9730	        let reg = default_registry();
  9731	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  9732	        // top_row = u32::MAX, rows = 2 → top_row + rows wraps. Pre-W5-125 the
  9733	        // footprint loop `for r in u32::MAX..u32::MAX+2 (wrapped to 1)` was
  9734	        // empty and downstream overlap / spill-anchor checks silently skipped.
  9735	        let err = rt
  9736	            .create_table("Wrap", 0, u32::MAX, 0, 2, 1, true, false, vec!["X".into()])
  9737	            .unwrap_err();
  9738	        match err {
  9739	            RuntimeError::TableCreateRejected { reason, .. } => {
  9740	                assert!(
  9741	                    reason.contains("u32 overflow") || reason.contains("addressable"),
  9742	                    "reason: {reason}"
  9743	                );
  9744	            }
  9745	            other => panic!("expected TableCreateRejected, got {other:?}"),
  9746	        }
  9747	    }
  9748	
  9749	    #[test]
  9750	    fn resize_table_rejects_growing_past_max_row() {
  9751	        use ql_types::MAX_ROW;
  9752	        let mut wb = make_runtime_workbook();
  9753	        let reg = default_registry();
  9754	        {
  9755	            let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  9756	            // Create a table near (but inside) MAX_ROW.
  9757	            rt.create_table(
  9758	                "Sales",
  9759	                0,
  9760	                MAX_ROW - 4,
  9761	                0,
  9762	                3,
  9763	                1,
  9764	                true,
  9765	                false,
  9766	                vec!["Qty".into()],
  9767	            )
  9768	            .unwrap();
  9769	        }
  9770	        // Grow to 10 rows → last_row = MAX_ROW - 4 + 9 = MAX_ROW + 5 > MAX_ROW.
  9771	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  9772	        let err = rt.resize_table("Sales", 10, 1, vec![], vec![]).unwrap_err();
  9773	        match err {
  9774	            RuntimeError::TableResizeRejected { reason, .. } => {
  9775	                assert!(reason.contains("MAX_ROW"), "reason: {reason}");
  9776	            }
  9777	            other => panic!("expected TableResizeRejected, got {other:?}"),
  9778	        }
  9779	    }
  9780	
  9781	    #[test]
  9782	    fn create_table_rejects_overlap() {
  9783	        let mut wb = make_runtime_workbook();
  9784	        let reg = default_registry();
  9785	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  9786	        rt.create_table(
  9787	            "A",
  9788	            0,
  9789	            0,
  9790	            0,

exec
/bin/zsh -lc 'rg -n "rows == 0|new_rows == 0|entry.rows == 0|rows and cols must both be > 0|validate_footprint_bounds" crates/ql-exec/src/workbook_runtime.rs crates/ql-oplog/src/replay.rs crates/ql-io/src/qbook_format.rs' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
crates/ql-oplog/src/replay.rs:694:    if new_rows == 0 || new_cols == 0 {
crates/ql-oplog/src/replay.rs:698:            reason: "table rows and cols must both be > 0",
crates/ql-oplog/src/replay.rs:704:        ql_storage::TableMetadata::validate_footprint_bounds(top_row, top_col, new_rows, new_cols)
crates/ql-oplog/src/replay.rs:901:    if rows == 0 || cols == 0 {
crates/ql-oplog/src/replay.rs:905:            reason: "table rows and cols must both be > 0",
crates/ql-oplog/src/replay.rs:911:        ql_storage::TableMetadata::validate_footprint_bounds(top_row, top_col, rows, cols)
crates/ql-io/src/qbook_format.rs:1708:            if entry.rows == 0 || entry.cols == 0 {
crates/ql-io/src/qbook_format.rs:1711:                    reason: "table rows and cols must both be > 0".into(),
crates/ql-io/src/qbook_format.rs:1720:            if let Err(reason) = ql_storage::TableMetadata::validate_footprint_bounds(
crates/ql-exec/src/workbook_runtime.rs:207:    /// called with `chunk_rows == 0`. `Sheet::with_chunk_rows` accepts it
crates/ql-exec/src/workbook_runtime.rs:1014:    /// 1. Degenerate check (`rows == 0 || cols == 0` → `#CALC!`).
crates/ql-exec/src/workbook_runtime.rs:1520:        if rows == 0 || cols == 0 {
crates/ql-exec/src/workbook_runtime.rs:1523:                reason: "table rows and cols must both be > 0",
crates/ql-exec/src/workbook_runtime.rs:1531:            ql_storage::TableMetadata::validate_footprint_bounds(top_row, top_col, rows, cols)
crates/ql-exec/src/workbook_runtime.rs:1990:        if new_rows == 0 || new_cols == 0 {
crates/ql-exec/src/workbook_runtime.rs:1993:                reason: "table rows and cols must both be > 0",
crates/ql-exec/src/workbook_runtime.rs:2001:        if let Err(reason) = ql_storage::TableMetadata::validate_footprint_bounds(
crates/ql-exec/src/workbook_runtime.rs:2156:        if chunk_rows == 0 {
crates/ql-exec/src/workbook_runtime.rs:4635:    /// Phase 2B.7 audit H1: `add_sheet` rejects `chunk_rows == 0` BEFORE

exec
/bin/zsh -lc "rg -n \"get_mut\\(|bump_generation|insert\\(|remove\\(\" crates/ql-exec/src/workbook_runtime.rs crates/ql-oplog/src/replay.rs crates/ql-storage/src/tables.rs crates/ql-io/src/qbook_format.rs" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
crates/ql-storage/src/tables.rs:349:    pub fn insert(&mut self, canonical: Arc<str>, meta: TableMetadata) {
crates/ql-storage/src/tables.rs:353:        self.tables.insert(canonical, meta);
crates/ql-storage/src/tables.rs:359:    pub fn remove(&mut self, canonical: &str) -> Option<TableMetadata> {
crates/ql-storage/src/tables.rs:361:        let removed = self.tables.remove(upper.as_str());
crates/ql-storage/src/tables.rs:382:    pub fn get_mut(&mut self, canonical: &str) -> Option<&mut TableMetadata> {
crates/ql-storage/src/tables.rs:384:        self.tables.get_mut(upper.as_str())
crates/ql-storage/src/tables.rs:389:    pub fn bump_generation(&mut self) {
crates/ql-storage/src/tables.rs:531:        tt.insert(Arc::clone(&t.name), t.clone());
crates/ql-storage/src/tables.rs:544:        tt.insert(Arc::from("FOO"), sales_table());
crates/ql-storage/src/tables.rs:547:        tt.remove("FOO");
crates/ql-storage/src/tables.rs:551:        tt.remove("FOO");
crates/ql-storage/src/tables.rs:574:        tt.insert(
crates/ql-storage/src/tables.rs:599:        tt.insert(
crates/ql-storage/src/tables.rs:614:        tt.insert(
crates/ql-storage/src/tables.rs:643:        tt.insert(
crates/ql-storage/src/tables.rs:665:        tt.insert(Arc::from("SALES"), sales_table());
crates/ql-io/src/qbook_format.rs:1352:                    written.insert((row, col));
crates/ql-io/src/qbook_format.rs:1691:    // Direct `tables_mut().insert(...)` bypasses `WorkbookRuntime::
crates/ql-io/src/qbook_format.rs:1794:                    if !seen_canon.insert(col.name.to_ascii_lowercase()) {
crates/ql-io/src/qbook_format.rs:1840:            wb.tables_mut().insert(canonical, meta);
crates/ql-io/src/qbook_format.rs:3713:        wb.tables_mut().insert(canonical_upper, meta);
crates/ql-io/src/qbook_format.rs:3756:            wb.tables_mut().insert(
crates/ql-io/src/qbook_format.rs:3815:        wb.tables_mut().insert(
crates/ql-oplog/src/replay.rs:507:            if workbook.tables_mut().remove(canonical.as_str()).is_none() {
crates/ql-oplog/src/replay.rs:583:        .remove(&old_canonical)
crates/ql-oplog/src/replay.rs:587:    workbook.tables_mut().insert(new_canonical, meta);
crates/ql-oplog/src/replay.rs:609:        .get_mut(&table_canonical)
crates/ql-oplog/src/replay.rs:649:    workbook.tables_mut().bump_generation();
crates/ql-oplog/src/replay.rs:763:            if !seen.insert(cn.as_str()) {
crates/ql-oplog/src/replay.rs:815:        .get_mut(&canonical)
crates/ql-oplog/src/replay.rs:829:    workbook.tables_mut().bump_generation();
crates/ql-oplog/src/replay.rs:885:            if !seen.insert(cn.to_ascii_lowercase()) {
crates/ql-oplog/src/replay.rs:962:    workbook.tables_mut().insert(canonical, meta);
crates/ql-exec/src/workbook_runtime.rs:769:                        affected.insert((sheet, row + dr, col + dc));
crates/ql-exec/src/workbook_runtime.rs:784:                        affected.insert((host_anchor.0, host_anchor.1 + dr, host_anchor.2 + dc));
crates/ql-exec/src/workbook_runtime.rs:911:                    if !seen.insert(node) {
crates/ql-exec/src/workbook_runtime.rs:1301:                            affected.insert((sheet, row + dr, col + dc));
crates/ql-exec/src/workbook_runtime.rs:1320:                            affected.insert(cell);
crates/ql-exec/src/workbook_runtime.rs:1555:                if !seen.insert(cn.to_ascii_lowercase()) {
crates/ql-exec/src/workbook_runtime.rs:1639:            .insert(Arc::from(canonical.as_str()), meta);
crates/ql-exec/src/workbook_runtime.rs:1657:        let _ = self.workbook.tables_mut().remove(&canonical);
crates/ql-exec/src/workbook_runtime.rs:1752:            .remove(&old_canonical)
crates/ql-exec/src/workbook_runtime.rs:1757:        self.workbook.tables_mut().insert(new_canonical_arc, meta);
crates/ql-exec/src/workbook_runtime.rs:1886:            .get_mut(&table_canonical)
crates/ql-exec/src/workbook_runtime.rs:1895:        self.workbook.tables_mut().bump_generation();
crates/ql-exec/src/workbook_runtime.rs:2055:                if !seen.insert(cn.as_str()) {
crates/ql-exec/src/workbook_runtime.rs:2114:            .get_mut(&canonical)
crates/ql-exec/src/workbook_runtime.rs:2128:        self.workbook.tables_mut().bump_generation();
crates/ql-exec/src/workbook_runtime.rs:2592:            self.format_cache.insert(format_id, parsed);
crates/ql-exec/src/workbook_runtime.rs:2852:                originally_dirty.insert(*n);
crates/ql-exec/src/workbook_runtime.rs:2874:                    changed.insert((sheet, row, col));
crates/ql-exec/src/workbook_runtime.rs:2975:                            changed.insert((sheet, row, col));
crates/ql-exec/src/workbook_runtime.rs:3004:                                        affected.insert((sheet, row + dr, col + dc));
crates/ql-exec/src/workbook_runtime.rs:3038:                                    if !seen.insert(n) {
crates/ql-exec/src/workbook_runtime.rs:9426:        wb.tables_mut().insert(Arc::clone(&table.name), table);
crates/ql-exec/src/workbook_runtime.rs:9473:        wb.tables_mut().insert(Arc::clone(&table.name), table);
crates/ql-exec/src/workbook_runtime.rs:9518:        wb.tables_mut().insert(Arc::clone(&table.name), table);
crates/ql-exec/src/workbook_runtime.rs:10688:        wb.tables_mut().insert(Arc::clone(&table.name), table);

exec
/bin/zsh -lc 'rg -n "resize_table.*u32::MAX|create_table.*u32::MAX|footprint_past|max_row|max_column|u32 overflow|TableCreateRejected|TableResizeRejected" crates/ql-exec/src/workbook_runtime.rs crates/ql-oplog/src/replay.rs crates/ql-exec/tests/oplog_e2e.rs crates/ql-io/src/qbook_format.rs' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
crates/ql-exec/tests/oplog_e2e.rs:821:fn replay_rejects_create_table_with_footprint_past_max_row() {
crates/ql-exec/tests/oplog_e2e.rs:842:        ReplayError::TableCreateRejected { reason, .. } => {
crates/ql-exec/tests/oplog_e2e.rs:845:        other => panic!("expected TableCreateRejected, got {other:?}"),
crates/ql-exec/tests/oplog_e2e.rs:853:fn replay_rejects_resize_table_with_footprint_past_max_row() {
crates/ql-exec/tests/oplog_e2e.rs:884:        ReplayError::TableResizeRejected { reason, .. } => {
crates/ql-exec/tests/oplog_e2e.rs:887:        other => panic!("expected TableResizeRejected, got {other:?}"),
crates/ql-oplog/src/replay.rs:169:    TableCreateRejected {
crates/ql-oplog/src/replay.rs:211:    TableResizeRejected {
crates/ql-oplog/src/replay.rs:566:            return Err(ReplayError::TableCreateRejected {
crates/ql-oplog/src/replay.rs:573:            return Err(ReplayError::TableCreateRejected {
crates/ql-oplog/src/replay.rs:695:        return Err(ReplayError::TableResizeRejected {
crates/ql-oplog/src/replay.rs:706:        return Err(ReplayError::TableResizeRejected {
crates/ql-oplog/src/replay.rs:715:        return Err(ReplayError::TableResizeRejected {
crates/ql-oplog/src/replay.rs:723:        return Err(ReplayError::TableResizeRejected {
crates/ql-oplog/src/replay.rs:735:            return Err(ReplayError::TableResizeRejected {
crates/ql-oplog/src/replay.rs:744:        return Err(ReplayError::TableResizeRejected {
crates/ql-oplog/src/replay.rs:764:                return Err(ReplayError::TableResizeRejected {
crates/ql-oplog/src/replay.rs:788:                    return Err(ReplayError::TableResizeRejected {
crates/ql-oplog/src/replay.rs:799:                return Err(ReplayError::TableResizeRejected {
crates/ql-oplog/src/replay.rs:853:        return Err(ReplayError::TableCreateRejected {
crates/ql-oplog/src/replay.rs:860:        return Err(ReplayError::TableCreateRejected {
crates/ql-oplog/src/replay.rs:867:        return Err(ReplayError::TableCreateRejected {
crates/ql-oplog/src/replay.rs:874:        return Err(ReplayError::TableCreateRejected {
crates/ql-oplog/src/replay.rs:886:                return Err(ReplayError::TableCreateRejected {
crates/ql-oplog/src/replay.rs:895:        return Err(ReplayError::TableCreateRejected {
crates/ql-oplog/src/replay.rs:902:        return Err(ReplayError::TableCreateRejected {
crates/ql-oplog/src/replay.rs:913:        return Err(ReplayError::TableCreateRejected {
crates/ql-oplog/src/replay.rs:925:                return Err(ReplayError::TableCreateRejected {
crates/ql-oplog/src/replay.rs:932:                return Err(ReplayError::TableCreateRejected {
crates/ql-io/src/qbook_format.rs:2075:    fn row_over_max_row_errors_not_panics() {
crates/ql-io/src/qbook_format.rs:4000:    fn malformed_table_rejected_when_footprint_past_max_row() {
crates/ql-io/src/qbook_format.rs:4039:    fn malformed_table_rejected_when_footprint_past_max_column() {
crates/ql-exec/src/workbook_runtime.rs:241:    TableCreateRejected { name: String, reason: &'static str },
crates/ql-exec/src/workbook_runtime.rs:270:    TableResizeRejected { name: String, reason: &'static str },
crates/ql-exec/src/workbook_runtime.rs:1521:            return Err(RuntimeError::TableCreateRejected {
crates/ql-exec/src/workbook_runtime.rs:1533:            return Err(RuntimeError::TableCreateRejected {
crates/ql-exec/src/workbook_runtime.rs:1539:            return Err(RuntimeError::TableCreateRejected {
crates/ql-exec/src/workbook_runtime.rs:1545:            return Err(RuntimeError::TableCreateRejected {
crates/ql-exec/src/workbook_runtime.rs:1556:                    return Err(RuntimeError::TableCreateRejected {
crates/ql-exec/src/workbook_runtime.rs:1565:            return Err(RuntimeError::TableCreateRejected {
crates/ql-exec/src/workbook_runtime.rs:1571:            return Err(RuntimeError::TableCreateRejected {
crates/ql-exec/src/workbook_runtime.rs:1587:                    return Err(RuntimeError::TableCreateRejected {
crates/ql-exec/src/workbook_runtime.rs:1593:                    return Err(RuntimeError::TableCreateRejected {
crates/ql-exec/src/workbook_runtime.rs:1689:            return Err(RuntimeError::TableCreateRejected {
crates/ql-exec/src/workbook_runtime.rs:1695:            return Err(RuntimeError::TableCreateRejected {
crates/ql-exec/src/workbook_runtime.rs:1991:            return Err(RuntimeError::TableResizeRejected {
crates/ql-exec/src/workbook_runtime.rs:2004:            return Err(RuntimeError::TableResizeRejected {
crates/ql-exec/src/workbook_runtime.rs:2012:            return Err(RuntimeError::TableResizeRejected {
crates/ql-exec/src/workbook_runtime.rs:2018:            return Err(RuntimeError::TableResizeRejected {
crates/ql-exec/src/workbook_runtime.rs:2029:                return Err(RuntimeError::TableResizeRejected {
crates/ql-exec/src/workbook_runtime.rs:2036:            return Err(RuntimeError::TableResizeRejected {
crates/ql-exec/src/workbook_runtime.rs:2056:                    return Err(RuntimeError::TableResizeRejected {
crates/ql-exec/src/workbook_runtime.rs:2081:                        return Err(RuntimeError::TableResizeRejected {
crates/ql-exec/src/workbook_runtime.rs:2088:                    return Err(RuntimeError::TableResizeRejected {
crates/ql-exec/src/workbook_runtime.rs:3556:    fn set_formula_at_max_row_max_col_ok() {
crates/ql-exec/src/workbook_runtime.rs:9593:            RuntimeError::TableCreateRejected { reason, .. } => {
crates/ql-exec/src/workbook_runtime.rs:9596:            other => panic!("expected TableCreateRejected, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:9642:            RuntimeError::TableResizeRejected { reason, .. } => {
crates/ql-exec/src/workbook_runtime.rs:9645:            other => panic!("expected TableResizeRejected, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:9672:    fn create_table_rejects_footprint_past_max_row() {
crates/ql-exec/src/workbook_runtime.rs:9692:            RuntimeError::TableCreateRejected { reason, .. } => {
crates/ql-exec/src/workbook_runtime.rs:9695:            other => panic!("expected TableCreateRejected, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:9700:    fn create_table_rejects_footprint_past_max_column() {
crates/ql-exec/src/workbook_runtime.rs:9720:            RuntimeError::TableCreateRejected { reason, .. } => {
crates/ql-exec/src/workbook_runtime.rs:9723:            other => panic!("expected TableCreateRejected, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:9736:            .create_table("Wrap", 0, u32::MAX, 0, 2, 1, true, false, vec!["X".into()])
crates/ql-exec/src/workbook_runtime.rs:9739:            RuntimeError::TableCreateRejected { reason, .. } => {
crates/ql-exec/src/workbook_runtime.rs:9741:                    reason.contains("u32 overflow") || reason.contains("addressable"),
crates/ql-exec/src/workbook_runtime.rs:9745:            other => panic!("expected TableCreateRejected, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:9750:    fn resize_table_rejects_growing_past_max_row() {
crates/ql-exec/src/workbook_runtime.rs:9774:            RuntimeError::TableResizeRejected { reason, .. } => {
crates/ql-exec/src/workbook_runtime.rs:9777:            other => panic!("expected TableResizeRejected, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:9813:            RuntimeError::TableCreateRejected { reason, .. } => {
crates/ql-exec/src/workbook_runtime.rs:9816:            other => panic!("expected TableCreateRejected, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:9839:            RuntimeError::TableCreateRejected { reason, .. } => {
crates/ql-exec/src/workbook_runtime.rs:9842:            other => panic!("expected TableCreateRejected, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:9857:            RuntimeError::TableCreateRejected { reason, .. } => {
crates/ql-exec/src/workbook_runtime.rs:9860:            other => panic!("expected TableCreateRejected, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:9951:            RuntimeError::TableCreateRejected { reason, .. } => {
crates/ql-exec/src/workbook_runtime.rs:9954:            other => panic!("expected TableCreateRejected, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:10452:            RuntimeError::TableResizeRejected { reason, .. } => {
crates/ql-exec/src/workbook_runtime.rs:10455:            other => panic!("expected TableResizeRejected, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:10471:            RuntimeError::TableResizeRejected { reason, .. } => {
crates/ql-exec/src/workbook_runtime.rs:10474:            other => panic!("expected TableResizeRejected, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:10500:            RuntimeError::TableResizeRejected { reason, .. } => {
crates/ql-exec/src/workbook_runtime.rs:10503:            other => panic!("expected TableResizeRejected, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:10524:            RuntimeError::TableResizeRejected { reason, .. } => {
crates/ql-exec/src/workbook_runtime.rs:10527:            other => panic!("expected TableResizeRejected, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:10543:            RuntimeError::TableResizeRejected { reason, .. } => {
crates/ql-exec/src/workbook_runtime.rs:10549:            other => panic!("expected TableResizeRejected, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:10567:            RuntimeError::TableResizeRejected { reason, .. } => {
crates/ql-exec/src/workbook_runtime.rs:10573:            other => panic!("expected TableResizeRejected, got {other:?}"),

codex
One load-side detail needs closer scrutiny: the duplicate-table check is case-insensitive only if prior entries were inserted under uppercase keys. I’m checking whether the loader enforces that canonical uppercase wire invariant before treating MEDIUM-1 as closed.
exec
/bin/zsh -lc "nl -ba crates/ql-io/src/qbook_format.rs | sed -n '1218,1250p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1218	    // **W5-123 (Phase 4.8.L):** serialize `TableTable` if non-empty.
  1219	    // Sorted ascending by canonical (uppercase) name for deterministic
  1220	    // diffs. Empty TableTable → `None` so v1-v5 readers don't see an
  1221	    // unexpected `[[tables]]` block that they'd misinterpret (the
  1222	    // schema-version gate already forces UnsupportedSchema for them,
  1223	    // but omitting on empty also keeps v6 TOML minimal for the common
  1224	    // "no tables yet" case).
  1225	    let tables_section = {
  1226	        let mut entries: Vec<TableEntry> = wb
  1227	            .tables()
  1228	            .iter()
  1229	            .map(|(_canon, meta)| TableEntry {
  1230	                name: meta.name.as_ref().to_owned(),
  1231	                display_name: meta.display_name.as_ref().to_owned(),
  1232	                sheet: meta.sheet,
  1233	                top_row: meta.top_row,
  1234	                top_col: meta.top_col,
  1235	                rows: meta.rows,
  1236	                cols: meta.cols,
  1237	                has_header: meta.has_header,
  1238	                has_totals: meta.has_totals,
  1239	                columns: meta
  1240	                    .columns
  1241	                    .iter()
  1242	                    .map(|c| TableColumnEntry {
  1243	                        id: c.id,
  1244	                        name: c.name.as_ref().to_owned(),
  1245	                        display: c.display.as_ref().to_owned(),
  1246	                        totals_function: c.totals_function.map(TotalsFunctionWire::from_runtime),
  1247	                    })
  1248	                    .collect(),
  1249	            })
  1250	            .collect();

exec
/bin/zsh -lc "rg -n \"lookup_table|table_at\\(|tables\\(\\)\\.lookup\" crates/ql-storage/src/workbook.rs crates/ql-exec/src/workbook_runtime.rs crates/ql-oplog/src/replay.rs" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc "rg -n \"entry\\.name|display_name|to_ascii_uppercase\\(\\).*entry\\.name|canonical_upper|MalformedTable.*canonical|TableEntry\" crates/ql-io/src/qbook_format.rs crates/ql-storage/src/workbook.rs" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
crates/ql-oplog/src/replay.rs:557:    if workbook.tables().lookup(&old_canonical).is_none() {
crates/ql-oplog/src/replay.rs:565:        if workbook.tables().lookup(&new_canonical).is_some() {
crates/ql-oplog/src/replay.rs:786:            if let Some(other) = workbook.table_at(sheet, r, c) {
crates/ql-oplog/src/replay.rs:852:    if workbook.tables().lookup(&canonical).is_some() {
crates/ql-oplog/src/replay.rs:924:            if workbook.table_at(sheet, r, c).is_some() {
crates/ql-storage/src/workbook.rs:472:    pub fn lookup_table(&self, name: &str) -> Option<&crate::TableMetadata> {
crates/ql-storage/src/workbook.rs:482:    pub fn table_at(
crates/ql-storage/src/workbook.rs:489:            .table_at(ql_types::Address::new(sheet, row, col))
crates/ql-exec/src/workbook_runtime.rs:1564:        if self.workbook.tables().lookup(&canonical).is_some() {
crates/ql-exec/src/workbook_runtime.rs:1580:        // check consults `Workbook::table_at(anchor)`); creating a
crates/ql-exec/src/workbook_runtime.rs:1586:                if self.workbook.table_at(sheet, r, c).is_some() {
crates/ql-exec/src/workbook_runtime.rs:1649:        if self.workbook.tables().lookup(&canonical).is_none() {
crates/ql-exec/src/workbook_runtime.rs:1679:        if self.workbook.tables().lookup(&old_canonical).is_none() {
crates/ql-exec/src/workbook_runtime.rs:1688:        if self.workbook.tables().lookup(&new_canonical).is_some() {
crates/ql-exec/src/workbook_runtime.rs:2079:                if let Some(other) = self.workbook.table_at(sheet, r, c) {
crates/ql-exec/src/workbook_runtime.rs:9554:        let t = wb.lookup_table("Sales").expect("registered");
crates/ql-exec/src/workbook_runtime.rs:9621:        assert!(wb.lookup_table("T").is_some());
crates/ql-exec/src/workbook_runtime.rs:9665:        let meta = wb.lookup_table("Sales").unwrap();
crates/ql-exec/src/workbook_runtime.rs:9875:        assert!(wb.lookup_table("Sales").is_none());
crates/ql-exec/src/workbook_runtime.rs:9919:        assert!(wb.lookup_table("Sales").is_none());
crates/ql-exec/src/workbook_runtime.rs:9920:        assert!(wb.lookup_table("Orders").is_some());
crates/ql-exec/src/workbook_runtime.rs:10019:        let meta = wb.lookup_table("Sales").expect("table present");
crates/ql-exec/src/workbook_runtime.rs:10085:        let meta = wb.lookup_table("Sales").unwrap();
crates/ql-exec/src/workbook_runtime.rs:10167:        let orders = wb.lookup_table("Orders").unwrap();
crates/ql-exec/src/workbook_runtime.rs:10326:        let meta = wb.lookup_table("Sales").unwrap();
crates/ql-exec/src/workbook_runtime.rs:10367:        assert!(wb.table_at(0, 3, 0).is_none(), "row 3 no longer in table");
crates/ql-exec/src/workbook_runtime.rs:10385:        let meta = wb.lookup_table("Sales").unwrap();
crates/ql-exec/src/workbook_runtime.rs:10424:        let meta = wb.lookup_table("Sales").unwrap();
crates/ql-exec/src/workbook_runtime.rs:10641:        let meta = wb.lookup_table("Sales").unwrap();

 succeeded in 0ms:
crates/ql-storage/src/workbook.rs:1117:    fn set_name_rejects_reserved_ai_canonical_upper() {
crates/ql-io/src/qbook_format.rs:400:/// `TableEntry` preserves stable column ids across save/load (the
crates/ql-io/src/qbook_format.rs:409:    pub entries: Vec<TableEntry>,
crates/ql-io/src/qbook_format.rs:416:pub struct TableEntry {
crates/ql-io/src/qbook_format.rs:421:    pub display_name: String,
crates/ql-io/src/qbook_format.rs:432:/// **W5-123 (Phase 4.8.L):** one entry in `TableEntry::columns`.
crates/ql-io/src/qbook_format.rs:1226:        let mut entries: Vec<TableEntry> = wb
crates/ql-io/src/qbook_format.rs:1229:            .map(|(_canon, meta)| TableEntry {
crates/ql-io/src/qbook_format.rs:1231:                display_name: meta.display_name.as_ref().to_owned(),
crates/ql-io/src/qbook_format.rs:1659:            let target = entry.target.to_target(&entry.name)?;
crates/ql-io/src/qbook_format.rs:1662:                    wb.set_name(&entry.name, target)
crates/ql-io/src/qbook_format.rs:1664:                            name: entry.name.clone(),
crates/ql-io/src/qbook_format.rs:1672:                                name: entry.name.clone(),
crates/ql-io/src/qbook_format.rs:1675:                    sheet.set_scoped_name(&entry.name, target).map_err(|e| {
crates/ql-io/src/qbook_format.rs:1677:                            name: entry.name.clone(),
crates/ql-io/src/qbook_format.rs:1704:                    name: entry.name.clone(),
crates/ql-io/src/qbook_format.rs:1710:                    name: entry.name.clone(),
crates/ql-io/src/qbook_format.rs:1727:                    name: entry.name.clone(),
crates/ql-io/src/qbook_format.rs:1733:                    name: entry.name.clone(),
crates/ql-io/src/qbook_format.rs:1751:            let canonical_upper = entry.name.to_ascii_uppercase();
crates/ql-io/src/qbook_format.rs:1756:            if wb.tables().lookup(&canonical_upper).is_some() {
crates/ql-io/src/qbook_format.rs:1758:                    name: entry.name.clone(),
crates/ql-io/src/qbook_format.rs:1767:            if wb.names().lookup_ci(&canonical_upper).is_some() {
crates/ql-io/src/qbook_format.rs:1769:                    name: entry.name.clone(),
crates/ql-io/src/qbook_format.rs:1783:                            name: entry.name.clone(),
crates/ql-io/src/qbook_format.rs:1789:                            name: entry.name.clone(),
crates/ql-io/src/qbook_format.rs:1796:                            name: entry.name.clone(),
crates/ql-io/src/qbook_format.rs:1808:                            name: entry.name.clone(),
crates/ql-io/src/qbook_format.rs:1814:            let canonical: std::sync::Arc<str> = std::sync::Arc::from(entry.name.as_str());
crates/ql-io/src/qbook_format.rs:1827:                display_name: std::sync::Arc::from(entry.display_name.as_str()),
crates/ql-io/src/qbook_format.rs:3699:        let canonical_upper: Arc<str> = Arc::from(table_name.to_ascii_uppercase().as_str());
crates/ql-io/src/qbook_format.rs:3700:        let display_name: Arc<str> = Arc::from(table_name);
crates/ql-io/src/qbook_format.rs:3702:            name: Arc::clone(&canonical_upper),
crates/ql-io/src/qbook_format.rs:3703:            display_name,
crates/ql-io/src/qbook_format.rs:3713:        wb.tables_mut().insert(canonical_upper, meta);
crates/ql-io/src/qbook_format.rs:3728:        assert_eq!(post.display_name, pre_meta.display_name);
crates/ql-io/src/qbook_format.rs:3760:                    display_name: Arc::from(*name),
crates/ql-io/src/qbook_format.rs:3819:                display_name: Arc::from("AllTotals"),
crates/ql-io/src/qbook_format.rs:3902:display_name = "Phantom"
crates/ql-io/src/qbook_format.rs:3940:display_name = "Sales"
crates/ql-io/src/qbook_format.rs:3978:display_name = "Empty"
crates/ql-io/src/qbook_format.rs:4015:display_name = "Far"
crates/ql-io/src/qbook_format.rs:4054:display_name = "Wide"
crates/ql-io/src/qbook_format.rs:4105:display_name = "Sales"
crates/ql-io/src/qbook_format.rs:4121:display_name = "Sales"
crates/ql-io/src/qbook_format.rs:4139:            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("duplicate canonical table name")),
crates/ql-io/src/qbook_format.rs:4140:            "expected MalformedTable about duplicate canonical, got {err:?}"
crates/ql-io/src/qbook_format.rs:4165:display_name = "Rate"
crates/ql-io/src/qbook_format.rs:4204:display_name = "A"
crates/ql-io/src/qbook_format.rs:4225:display_name = "B"
crates/ql-io/src/qbook_format.rs:4268:display_name = "T"
crates/ql-io/src/qbook_format.rs:4307:display_name = "T"
crates/ql-io/src/qbook_format.rs:4330:            matches!(err, QbookError::MalformedTable { ref reason, .. } if reason.contains("duplicate canonical column name")),
crates/ql-io/src/qbook_format.rs:4353:display_name = "T"
crates/ql-io/src/qbook_format.rs:4394:display_name = "A"
crates/ql-io/src/qbook_format.rs:4410:display_name = "B"

exec
/bin/zsh -lc "nl -ba crates/ql-storage/src/workbook.rs | sed -n '720,760p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   720	    /// `put_at` is for the qbook loader, the op-log replay path, runtime-
   721	    /// internal pass 2 of formula recompute, and tests. GAP-O-03.
   722	    pub fn put_at(&mut self, sheet: SheetId, row: RowId, col: ColId, value: Value) {
   723	        self.put(Address::new(sheet, row, col), value);
   724	    }
   725	
   726	    /// **Phase 3.5 (2026-05-12) — CORR-25.** Write a FORMULA-OUTPUT value at
   727	    /// `(sheet, row, col)`. Routes to the computed-overlay lane (NOT the user lane).
   728	    /// Used by `WorkbookRuntime::set_formula` (with the freshly-evaluated result of
   729	    /// the formula text) and by `recompute_all` / `recompute_dirty` for every
   730	    /// formula cell in their pass.
   731	    ///
   732	    /// Panics if `sheet` is out of bounds — same contract as `put_at`. Does NOT touch
   733	    /// the user overlay; if a cell is transitioning from "user-typed value" to
   734	    /// "formula", the caller (the runtime) MUST first call `clear_user_at` so the
   735	    /// stale user value doesn't mask the new computed value via the read cascade.
   736	    pub fn put_computed_at(&mut self, sheet: SheetId, row: RowId, col: ColId, value: Value) {
   737	        let total = self.sheet_count();
   738	        match self.sheet_mut(sheet) {
   739	            Some(s) => s.put_computed(row, col, value),
   740	            None => {
   741	                panic!("Workbook::put_computed_at: sheet {sheet} does not exist (have {total})")
   742	            }
   743	        }
   744	    }
   745	
   746	    /// **Phase 3.5.** Drop the computed-overlay entry at `(sheet, row, col)`. No-op if
   747	    /// the cell has no computed entry or the sheet is out of bounds. Called by
   748	    /// `clear_formula` (the formula text is gone → its output is stale).
   749	    pub fn clear_computed_at(&mut self, sheet: SheetId, row: RowId, col: ColId) {
   750	        if let Some(s) = self.sheet_mut(sheet) {
   751	            s.clear_computed(row, col);
   752	        }
   753	    }
   754	
   755	    /// **Phase 3.5.** Drop the user-overlay entry at `(sheet, row, col)`. No-op if
   756	    /// the cell has no user entry or the sheet is out of bounds. Called by the runtime's
   757	    /// `set_formula` to clear a stale user value before writing the new computed
   758	    /// formula output.
   759	    pub fn clear_user_at(&mut self, sheet: SheetId, row: RowId, col: ColId) {
   760	        if let Some(s) = self.sheet_mut(sheet) {

exec
/bin/zsh -lc "nl -ba crates/ql-storage/src/workbook.rs | sed -n '460,495p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   460	
   461	    /// **W5-110 (Phase 4.8.A) — LOW-LEVEL.** Mutable access to the
   462	    /// table registry. Bypasses the op log silently; product mutations
   463	    /// go through `WorkbookRuntime::create_table` etc. (4.8.H+) which
   464	    /// emit op-log entries. Direct callers: qbook loader, tests,
   465	    /// engine-internal reconstruction.
   466	    pub fn tables_mut(&mut self) -> &mut crate::TableTable {
   467	        &mut self.tables
   468	    }
   469	
   470	    /// **W5-110 (Phase 4.8.A):** convenience case-insensitive table
   471	    /// lookup. Returns `None` if no table with that name exists.
   472	    pub fn lookup_table(&self, name: &str) -> Option<&crate::TableMetadata> {
   473	        self.tables.lookup(name)
   474	    }
   475	
   476	    /// **W5-110 (Phase 4.8.A):** reverse lookup — which table contains
   477	    /// the given cell, if any? Used by:
   478	    /// - the binder for `[@Col]` resolution.
   479	    /// - `create_table` / `resize_table` overlap validation.
   480	    /// - `write_spill` to block spill anchors inside table footprints
   481	    ///   (design § 4.3 invariant #5).
   482	    pub fn table_at(
   483	        &self,
   484	        sheet: SheetId,
   485	        row: RowId,
   486	        col: ColId,
   487	    ) -> Option<&crate::TableMetadata> {
   488	        self.tables
   489	            .table_at(ql_types::Address::new(sheet, row, col))
   490	    }
   491	
   492	    /// Phase 2A.1 convenience: register a name → target binding on the workbook's
   493	    /// name table. Phase 2A.6 audit M6: canonicalization lives on
   494	    /// `NameTable::set` (this is a thin pass-through). Phase 2A.9 audit M6:
   495	    /// reserved-name registration (currently `AI` per CORR-06) is refused —

codex
The closure tests are present, but the duplicate canonical-name test only covers identical uppercase wire names. I’m running a small targeted test set now for the shipped tests, then I’ll finish the review with the case-variant loader path called out explicitly.
exec
/bin/zsh -lc 'cargo test -p ql-storage validate_footprint_bounds -p ql-exec resize_table_exact_noop_emits_nothing -p ql-exec composed_create_rename_column_resize_replay_roundtrip -p ql-exec composed_save_load_then_rename_column_roundtrip -p ql-io malformed_table_rejected_when_footprint_past_max_row -p ql-io malformed_table_rejected_when_footprint_past_max_column -p ql-io malformed_table_rejected_when_duplicate_canonical_name -p ql-io malformed_table_rejected_when_name_collides_with_defined_name -p ql-io malformed_table_rejected_when_footprints_overlap -p ql-io malformed_table_rejected_when_column_name_empty -p ql-io malformed_table_rejected_when_duplicate_column_name -p ql-io malformed_table_rejected_when_column_id_is_u32_max' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 exited 127 in 0ms:
zsh:1: command not found: cargo

codex
The targeted test command could not run because `cargo` is not on this shell’s `PATH` (`zsh: command not found: cargo`). I’ll treat the reported green gates as external evidence and keep this verification code-review based.
exec
/bin/zsh -lc 'which cargo || true' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
cargo not found

exec
/bin/zsh -lc 'git show --name-only --oneline --no-renames ed5387e7dda 06b158ee0cf 9c850f37733' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
ed5387e7dda W5-125 / Engine Phase 4.8.O.1 — footprint bounds (Codex megaudit HIGH-1)
quantbook-engine/crates/ql-exec/src/workbook_runtime.rs
quantbook-engine/crates/ql-exec/tests/oplog_e2e.rs
quantbook-engine/crates/ql-io/src/qbook_format.rs
quantbook-engine/crates/ql-oplog/src/replay.rs
quantbook-engine/crates/ql-storage/src/tables.rs
quantbook-engine/docs/architecture/2026-05-14-structured-references-and-tables.md
06b158ee0cf W5-126 / Engine Phase 4.8.O.2 — loader re-validates runtime invariants
quantbook-engine/crates/ql-io/src/qbook_format.rs
9c850f37733 W5-127 / Engine Phase 4.8.O.3 — resize no-op + composed e2e tests
quantbook-engine/crates/ql-exec/src/workbook_runtime.rs
quantbook-engine/crates/ql-exec/tests/oplog_e2e.rs

exec
/bin/zsh -lc 'ls -la ~/.cargo/bin/cargo 2>/dev/null || true' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
lrwxrwxrwx@ 1 sanzhar  staff  6 May 11 16:00 /Users/sanzhar/.cargo/bin/cargo -> rustup

exec
/bin/zsh -lc 'git show --stat --oneline --decorate --no-renames HEAD~3..HEAD' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 210ms:
9c850f37733 (HEAD -> feat/quantbook-engine) W5-127 / Engine Phase 4.8.O.3 — resize no-op + composed e2e tests
 .../crates/ql-exec/src/workbook_runtime.rs         |  54 +++++++++++
 quantbook-engine/crates/ql-exec/tests/oplog_e2e.rs | 105 +++++++++++++++++++++
 2 files changed, 159 insertions(+)
06b158ee0cf W5-126 / Engine Phase 4.8.O.2 — loader re-validates runtime invariants
 quantbook-engine/crates/ql-io/src/qbook_format.rs | 422 ++++++++++++++++++++++
 1 file changed, 422 insertions(+)
ed5387e7dda W5-125 / Engine Phase 4.8.O.1 — footprint bounds (Codex megaudit HIGH-1)
 .../crates/ql-exec/src/workbook_runtime.rs         | 137 +++++++++++++++++++++
 quantbook-engine/crates/ql-exec/tests/oplog_e2e.rs |  77 ++++++++++++
 quantbook-engine/crates/ql-io/src/qbook_format.rs  | 102 +++++++++++++++
 quantbook-engine/crates/ql-oplog/src/replay.rs     |  22 ++++
 quantbook-engine/crates/ql-storage/src/tables.rs   |  95 +++++++++++++-
 .../2026-05-14-structured-references-and-tables.md |   2 +
 6 files changed, 434 insertions(+), 1 deletion(-)

exec
/bin/zsh -lc '/Users/sanzhar/.cargo/bin/cargo test -p ql-storage validate_footprint_bounds -p ql-exec resize_table_exact_noop_emits_nothing -p ql-exec composed_create_rename_column_resize_replay_roundtrip -p ql-exec composed_save_load_then_rename_column_roundtrip -p ql-io malformed_table_rejected_when_footprint_past_max_row -p ql-io malformed_table_rejected_when_footprint_past_max_column -p ql-io malformed_table_rejected_when_duplicate_canonical_name -p ql-io malformed_table_rejected_when_name_collides_with_defined_name -p ql-io malformed_table_rejected_when_footprints_overlap -p ql-io malformed_table_rejected_when_column_name_empty -p ql-io malformed_table_rejected_when_duplicate_column_name -p ql-io malformed_table_rejected_when_column_id_is_u32_max' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 exited 1 in 0ms:
error: unexpected argument 'resize_table_exact_noop_emits_nothing' found

Usage: cargo test [OPTIONS] [TESTNAME] [-- [ARGS]...]

For more information, try '--help'.

exec
/bin/zsh -lc '/Users/sanzhar/.cargo/bin/cargo test -p ql-storage validate_footprint_bounds' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc '/Users/sanzhar/.cargo/bin/cargo test -p ql-exec resize_table_exact_noop_emits_nothing' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc '/Users/sanzhar/.cargo/bin/cargo test -p ql-exec --test oplog_e2e composed' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc '/Users/sanzhar/.cargo/bin/cargo test -p ql-io malformed_table_rejected_when' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 1708ms:
    Finished `test` profile [unoptimized + debuginfo] target(s) in 1.68s
     Running unittests src/lib.rs (target/debug/deps/ql_exec-b3f1062c75853a85)

running 1 test
test workbook_runtime::tests::resize_table_exact_noop_emits_nothing ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 537 filtered out; finished in 0.00s

     Running tests/error_precedence.rs (target/debug/deps/error_precedence-e2befdcc2eba4b3f)

running 0 tests

test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 15 filtered out; finished in 0.00s

     Running tests/ide_simulation.rs (target/debug/deps/ide_simulation-72a7d3a1ebd3350a)

running 0 tests

test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 7 filtered out; finished in 0.00s

     Running tests/oplog_e2e.rs (target/debug/deps/oplog_e2e-97d2ec293211cbd2)

running 0 tests

test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 20 filtered out; finished in 0.00s

     Running tests/region_mul2_e2e.rs (target/debug/deps/region_mul2_e2e-295add40907d53df)

running 0 tests

test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 21 filtered out; finished in 0.00s


 succeeded in 1710ms:
    Finished `test` profile [unoptimized + debuginfo] target(s) in 1.70s
     Running tests/oplog_e2e.rs (target/debug/deps/oplog_e2e-97d2ec293211cbd2)

running 2 tests
test composed_create_rename_column_resize_replay_roundtrip ... ok
test composed_save_load_then_rename_column_roundtrip ... ok

test result: ok. 2 passed; 0 failed; 0 ignored; 0 measured; 18 filtered out; finished in 0.00s


 succeeded in 2032ms:
    Finished `test` profile [unoptimized + debuginfo] target(s) in 0.81s
     Running unittests src/lib.rs (target/debug/deps/ql_storage-af55a8e7d49d6ab4)

running 5 tests
test tables::tests::validate_footprint_bounds_accepts_in_range_footprint ... ok
test tables::tests::validate_footprint_bounds_rejects_past_max_column ... ok
test tables::tests::validate_footprint_bounds_rejects_past_max_row ... ok
test tables::tests::validate_footprint_bounds_rejects_u32_overflow_on_cols ... ok
test tables::tests::validate_footprint_bounds_rejects_u32_overflow_on_rows ... ok

test result: ok. 5 passed; 0 failed; 0 ignored; 0 measured; 169 filtered out; finished in 0.00s


 succeeded in 3450ms:
   Compiling ql-io v0.1.0 (/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-io)
    Finished `test` profile [unoptimized + debuginfo] target(s) in 1.64s
     Running unittests src/lib.rs (target/debug/deps/ql_io-c12c590e4ec5d97d)

running 8 tests
test qbook_format::tests::malformed_table_rejected_when_footprint_past_max_row ... ok
test qbook_format::tests::malformed_table_rejected_when_duplicate_canonical_name ... ok
test qbook_format::tests::malformed_table_rejected_when_duplicate_column_name ... ok
test qbook_format::tests::malformed_table_rejected_when_column_name_empty ... ok
test qbook_format::tests::malformed_table_rejected_when_column_id_is_u32_max ... ok
test qbook_format::tests::malformed_table_rejected_when_footprints_overlap ... ok
test qbook_format::tests::malformed_table_rejected_when_name_collides_with_defined_name ... ok
test qbook_format::tests::malformed_table_rejected_when_footprint_past_max_column ... ok

test result: ok. 8 passed; 0 failed; 0 ignored; 0 measured; 75 filtered out; finished in 0.01s


exec
/bin/zsh -lc "nl -ba crates/ql-io/src/qbook_format.rs | sed -n '3160,3235p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  3160	        let dir = TempDir::new().unwrap();
  3161	        let path = dir.path().join("spill-skip.qbook");
  3162	
  3163	        let mut wb = Workbook::new();
  3164	        let s = wb.add_sheet("S");
  3165	        // Simulate the post-set_formula state for `A1 = {1, 2, 3}`:
  3166	        //   - Formula text at A1.
  3167	        //   - Computed values at A1=1, B1=2, C1=3.
  3168	        //   - Spill anchor registered with shape (1, 3).
  3169	        wb.put_formula(s, 0, 0, "{1, 2, 3}");
  3170	        wb.put_computed_at(s, 0, 0, Value::Number(1.0));
  3171	        wb.put_computed_at(s, 0, 1, Value::Number(2.0));
  3172	        wb.put_computed_at(s, 0, 2, Value::Number(3.0));
  3173	        wb.register_spill((s, 0, 0), ql_storage::SpillShape::new(1, 3))
  3174	            .unwrap();
  3175	
  3176	        save_workbook(&wb, "spill-skip", &path).unwrap();
  3177	
  3178	        // Read the saved sheet JSONL directly to confirm B1/C1 are NOT
  3179	        // recorded.
  3180	        let sheet_path = path.join("sheets").join(format!("{s}.jsonl"));
  3181	        let content = fs::read_to_string(&sheet_path).unwrap();
  3182	        // Anchor A1 record must be present.
  3183	        assert!(
  3184	            content.lines().any(|l| {
  3185	                let r: CellRecord = serde_json::from_str(l).unwrap();
  3186	                r.row == 0 && r.col == 0 && r.formula.is_some()
  3187	            }),
  3188	            "anchor A1 record must be present with formula text. got:\n{content}"
  3189	        );
  3190	        // B1 and C1 records must be ABSENT.
  3191	        for (row, col, name) in [(0, 1, "B1"), (0, 2, "C1")] {
  3192	            assert!(
  3193	                content.lines().all(|l| {
  3194	                    let r: CellRecord = serde_json::from_str(l).unwrap();
  3195	                    !(r.row == row && r.col == col)
  3196	                }),
  3197	                "{name} record must NOT appear in saved JSONL (spill target). got:\n{content}"
  3198	            );
  3199	        }
  3200	    }
  3201	
  3202	    /// **W5-105 (Phase 4.7.L)** load round-trip: save a workbook with a
  3203	    /// spill, load it back, verify:
  3204	    ///   - the anchor's formula text survives.
  3205	    ///   - the anchor's computed value survives in the COMPUTED lane
  3206	    ///     (the loader at qbook_format.rs:1449-1453 routes formula
  3207	    ///     cells' non-Blank saved values through `put_computed_at`,
  3208	    ///     preserving the formula-owned-cell invariant "no user-overlay
  3209	    ///     entry on a formula cell").
  3210	    ///   - the SpillAnchorTable + target cells (B1, C1) are EMPTY.
  3211	    ///     `recompute_all` re-derives them per design § 12.3.
  3212	    #[test]
  3213	    fn spill_targets_load_as_blank_pending_recompute() {
  3214	        let dir = TempDir::new().unwrap();
  3215	        let path = dir.path().join("spill-load.qbook");
  3216	
  3217	        let mut wb = Workbook::new();
  3218	        let s = wb.add_sheet("S");
  3219	        wb.put_formula(s, 0, 0, "{1, 2, 3}");
  3220	        wb.put_computed_at(s, 0, 0, Value::Number(1.0));
  3221	        wb.put_computed_at(s, 0, 1, Value::Number(2.0));
  3222	        wb.put_computed_at(s, 0, 2, Value::Number(3.0));
  3223	        wb.register_spill((s, 0, 0), ql_storage::SpillShape::new(1, 3))
  3224	            .unwrap();
  3225	
  3226	        save_workbook(&wb, "spill-load", &path).unwrap();
  3227	        let loaded = load_workbook(&path).unwrap();
  3228	
  3229	        // Anchor formula text survives.
  3230	        assert_eq!(
  3231	            loaded.formula_at(s, 0, 0).map(|s| s.as_ref()),
  3232	            Some("{1, 2, 3}")
  3233	        );
  3234	        // Anchor's value at load: Number(1.0) in user lane. (Per design
  3235	        // § 12.2: anchors save normally — formula + value. The

exec
/bin/zsh -lc 'rg -n "spill|spill_anchors|register_spill|write_spill|Spill" crates/ql-io/src/qbook_format.rs crates/ql-storage/src/workbook.rs crates/ql-exec/src/workbook_runtime.rs | head -n 120' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
crates/ql-storage/src/workbook.rs:267:    /// **W5-101 (Phase 4.7.H):** spill-anchor table. Workbook-level
crates/ql-storage/src/workbook.rs:268:    /// because spills can theoretically span sheets (rare in Excel; v1
crates/ql-storage/src/workbook.rs:270:    /// design; mutation goes through `register_spill` / `clear_spill_at`
crates/ql-storage/src/workbook.rs:273:    /// `crates/ql-storage/src/spill.rs` module docs.
crates/ql-storage/src/workbook.rs:274:    spill_anchors: crate::SpillAnchorTable,
crates/ql-storage/src/workbook.rs:480:    /// - `write_spill` to block spill anchors inside table footprints
crates/ql-storage/src/workbook.rs:589:    // ===== W5-101 (Phase 4.7.H) spill-anchor API =====
crates/ql-storage/src/workbook.rs:591:    /// **W5-101 (Phase 4.7.H):** read access to the spill-anchor table.
crates/ql-storage/src/workbook.rs:594:    pub fn spill_anchors(&self) -> &crate::SpillAnchorTable {
crates/ql-storage/src/workbook.rs:595:        &self.spill_anchors
crates/ql-storage/src/workbook.rs:599:    /// active spill anchor? Returns `Some(&shape)` iff yes. Delegates to
crates/ql-storage/src/workbook.rs:600:    /// `SpillAnchorTable::anchor_at`.
crates/ql-storage/src/workbook.rs:601:    pub fn spill_anchor_at(
crates/ql-storage/src/workbook.rs:606:    ) -> Option<&crate::SpillShape> {
crates/ql-storage/src/workbook.rs:607:        self.spill_anchors.anchor_at(sheet, row, col)
crates/ql-storage/src/workbook.rs:611:    /// is a spill target (anchor or any non-anchor target cell), return
crates/ql-storage/src/workbook.rs:612:    /// the anchor. `O(1)`. Used by the runtime spill-invalidation path
crates/ql-storage/src/workbook.rs:613:    /// (4.7.K) on `set_value` to detect user writes into spill ranges.
crates/ql-storage/src/workbook.rs:614:    pub fn spill_target_anchor(
crates/ql-storage/src/workbook.rs:620:        self.spill_anchors.target_anchor(sheet, row, col)
crates/ql-storage/src/workbook.rs:623:    /// **W5-101 (Phase 4.7.H):** register a new spill. Map-only — does
crates/ql-storage/src/workbook.rs:624:    /// NOT touch computed overlays. The runtime spill-writeback path
crates/ql-storage/src/workbook.rs:626:    /// target cell value AFTER `register_spill` succeeds.
crates/ql-storage/src/workbook.rs:627:    pub fn register_spill(
crates/ql-storage/src/workbook.rs:630:        shape: crate::SpillShape,
crates/ql-storage/src/workbook.rs:631:    ) -> Result<(), crate::SpillBlockError> {
crates/ql-storage/src/workbook.rs:632:        self.spill_anchors.register(anchor, shape)
crates/ql-storage/src/workbook.rs:638:    /// spill before re-eval" step (design § 8.3) and when clearing a
crates/ql-storage/src/workbook.rs:642:    /// - `SpillNotFoundError` — the cell isn't an anchor. Callers that
crates/ql-storage/src/workbook.rs:644:    ///   `spill_anchor_at(...).is_some()`.
crates/ql-storage/src/workbook.rs:645:    pub fn clear_spill_at(
crates/ql-storage/src/workbook.rs:648:    ) -> Result<(), crate::SpillNotFoundError> {
crates/ql-storage/src/workbook.rs:649:        let shape = self.spill_anchors.unregister(anchor)?;
crates/ql-storage/src/workbook.rs:651:        // This is the coupling reason `clear_spill_at` lives on
crates/ql-storage/src/workbook.rs:653:        // map-only `SpillAnchorTable`.
crates/ql-storage/src/workbook.rs:666:    /// `clear_spill_at`. Returns `true` if a spill was cleared, `false`
crates/ql-storage/src/workbook.rs:668:    /// runtime spill-writeback path (Phase 4.7.J) which calls "clear
crates/ql-storage/src/workbook.rs:669:    /// before re-eval" regardless of whether a prior spill existed.
crates/ql-storage/src/workbook.rs:670:    /// Equivalent to `clear_spill_at(anchor).is_ok()` semantically.
crates/ql-storage/src/workbook.rs:671:    pub fn clear_spill_if_present(&mut self, anchor: (SheetId, RowId, ColId)) -> bool {
crates/ql-storage/src/workbook.rs:672:        self.clear_spill_at(anchor).is_ok()
crates/ql-storage/src/workbook.rs:1377:    // ===== W5-101 (Phase 4.7.H) Workbook spill-anchor integration =====
crates/ql-storage/src/workbook.rs:1380:    fn workbook_register_spill_round_trip() {
crates/ql-storage/src/workbook.rs:1383:        // Register a 2x3 spill at (0, 1, 1) — covers B2:D3.
crates/ql-storage/src/workbook.rs:1384:        wb.register_spill((0, 1, 1), crate::SpillShape::new(2, 3))
crates/ql-storage/src/workbook.rs:1387:            wb.spill_anchor_at(0, 1, 1),
crates/ql-storage/src/workbook.rs:1388:            Some(&crate::SpillShape::new(2, 3))
crates/ql-storage/src/workbook.rs:1394:                    wb.spill_target_anchor(0, 1 + dr, 1 + dc),
crates/ql-storage/src/workbook.rs:1405:    fn workbook_clear_spill_at_clears_computed_overlays() {
crates/ql-storage/src/workbook.rs:1408:        // Register a 1x3 spill at (0, 0, 0) covering A1:C1.
crates/ql-storage/src/workbook.rs:1409:        wb.register_spill((0, 0, 0), crate::SpillShape::new(1, 3))
crates/ql-storage/src/workbook.rs:1419:        // clear_spill_at: unregisters AND clears overlays at every target.
crates/ql-storage/src/workbook.rs:1420:        wb.clear_spill_at((0, 0, 0)).unwrap();
crates/ql-storage/src/workbook.rs:1422:        assert!(wb.spill_anchor_at(0, 0, 0).is_none());
crates/ql-storage/src/workbook.rs:1423:        assert!(wb.spill_target_anchor(0, 0, 1).is_none());
crates/ql-storage/src/workbook.rs:1431:    fn workbook_register_spill_rejects_collision_with_existing() {
crates/ql-storage/src/workbook.rs:1434:        wb.register_spill((0, 0, 0), crate::SpillShape::new(3, 1))
crates/ql-storage/src/workbook.rs:1436:        // Second spill at (0, 1, 0) would overlap A2/A3 of the first.
crates/ql-storage/src/workbook.rs:1438:            .register_spill((0, 1, 0), crate::SpillShape::new(3, 1))
crates/ql-storage/src/workbook.rs:1442:            crate::SpillBlockError::TargetCellOccupied { .. }
crates/ql-storage/src/workbook.rs:1447:    fn workbook_clear_spill_at_unknown_anchor_errors() {
crates/ql-storage/src/workbook.rs:1450:        let err = wb.clear_spill_at((0, 5, 5)).unwrap_err();
crates/ql-storage/src/workbook.rs:1456:    fn workbook_clear_spill_at_preserves_user_overlays_outside_rectangle() {
crates/ql-storage/src/workbook.rs:1457:        // A user-typed value outside the spill rectangle must survive
crates/ql-storage/src/workbook.rs:1458:        // clear_spill_at. Verifies the clear loop is bounded to the
crates/ql-storage/src/workbook.rs:1462:        wb.register_spill((0, 0, 0), crate::SpillShape::new(1, 3))
crates/ql-storage/src/workbook.rs:1467:        // User value at A2 (row 1, col 0) — OUTSIDE the 1x3 spill.
crates/ql-storage/src/workbook.rs:1469:        wb.clear_spill_at((0, 0, 0)).unwrap();
crates/ql-storage/src/workbook.rs:1470:        // Spill range cleared.
crates/ql-storage/src/workbook.rs:1477:    fn workbook_spill_anchors_accessor_returns_table() {
crates/ql-storage/src/workbook.rs:1480:        assert_eq!(wb.spill_anchors().len(), 0);
crates/ql-storage/src/workbook.rs:1481:        wb.register_spill((0, 0, 0), crate::SpillShape::new(2, 2))
crates/ql-storage/src/workbook.rs:1483:        wb.register_spill((0, 5, 5), crate::SpillShape::new(1, 1))
crates/ql-storage/src/workbook.rs:1485:        assert_eq!(wb.spill_anchors().len(), 2);
crates/ql-storage/src/workbook.rs:1488:    // ===== W5-101-AUDIT (Codex LOW-3) — clear_spill_if_present =====
crates/ql-storage/src/workbook.rs:1491:    fn workbook_clear_spill_if_present_returns_true_when_cleared() {
crates/ql-storage/src/workbook.rs:1494:        wb.register_spill((0, 0, 0), crate::SpillShape::new(2, 1))
crates/ql-storage/src/workbook.rs:1496:        let cleared = wb.clear_spill_if_present((0, 0, 0));
crates/ql-storage/src/workbook.rs:1498:        assert!(wb.spill_anchor_at(0, 0, 0).is_none());
crates/ql-storage/src/workbook.rs:1502:    fn workbook_clear_spill_if_present_returns_false_when_missing() {
crates/ql-storage/src/workbook.rs:1506:        let cleared = wb.clear_spill_if_present((0, 5, 5));
crates/ql-storage/src/workbook.rs:1511:    fn workbook_clear_spill_at_clears_anchor_own_computed_overlay() {
crates/ql-storage/src/workbook.rs:1513:        // (the (0,0) of its own spill range) gets its computed
crates/ql-storage/src/workbook.rs:1517:        wb.register_spill((0, 0, 0), crate::SpillShape::new(1, 1))
crates/ql-storage/src/workbook.rs:1521:        wb.clear_spill_at((0, 0, 0)).unwrap();
crates/ql-io/src/qbook_format.rs:737:        ErrorValue::Spill => "#SPILL!".to_string(),
crates/ql-io/src/qbook_format.rs:757:        "#SPILL!" => ErrorValue::Spill,
crates/ql-io/src/qbook_format.rs:1303:                // non-anchor spill TARGET cells. Their computed values are
crates/ql-io/src/qbook_format.rs:1307:                // `write_spill`). Saving target cells would emit
crates/ql-io/src/qbook_format.rs:1308:                // `CellRecord { value: <spilled>, formula: None }` records
crates/ql-io/src/qbook_format.rs:1310:                // re-spill — the very issue Codex W5-94 design HIGH-5
crates/ql-io/src/qbook_format.rs:1315:                if let Some(anchor) = wb.spill_target_anchor(sheet_id, row, col) {
crates/ql-io/src/qbook_format.rs:3153:    /// **W5-105 (Phase 4.7.L)**: spill TARGET cells must NOT be saved
crates/ql-io/src/qbook_format.rs:3159:    fn spill_target_cells_skipped_on_save() {
crates/ql-io/src/qbook_format.rs:3161:        let path = dir.path().join("spill-skip.qbook");
crates/ql-io/src/qbook_format.rs:3168:        //   - Spill anchor registered with shape (1, 3).
crates/ql-io/src/qbook_format.rs:3173:        wb.register_spill((s, 0, 0), ql_storage::SpillShape::new(1, 3))
crates/ql-io/src/qbook_format.rs:3176:        save_workbook(&wb, "spill-skip", &path).unwrap();
crates/ql-io/src/qbook_format.rs:3197:                "{name} record must NOT appear in saved JSONL (spill target). got:\n{content}"
crates/ql-io/src/qbook_format.rs:3203:    /// spill, load it back, verify:
crates/ql-io/src/qbook_format.rs:3210:    ///   - the SpillAnchorTable + target cells (B1, C1) are EMPTY.
crates/ql-io/src/qbook_format.rs:3213:    fn spill_targets_load_as_blank_pending_recompute() {
crates/ql-io/src/qbook_format.rs:3215:        let path = dir.path().join("spill-load.qbook");
crates/ql-io/src/qbook_format.rs:3223:        wb.register_spill((s, 0, 0), ql_storage::SpillShape::new(1, 3))
crates/ql-io/src/qbook_format.rs:3226:        save_workbook(&wb, "spill-load", &path).unwrap();
crates/ql-io/src/qbook_format.rs:3237:        // then routes through write_spill → clear_user_at → put_computed_at
crates/ql-io/src/qbook_format.rs:3243:        // SpillAnchorTable does NOT round-trip — it's runtime-derived
crates/ql-io/src/qbook_format.rs:3245:        // no spill state until recompute fires.
crates/ql-io/src/qbook_format.rs:3246:        assert_eq!(loaded.spill_anchor_at(s, 0, 0), None);
crates/ql-exec/src/workbook_runtime.rs:41:use ql_storage::{FormatId, SpillShape, Workbook};
crates/ql-exec/src/workbook_runtime.rs:232:    /// dirty. Signals a runaway spill shape transition or workbook
crates/ql-exec/src/workbook_runtime.rs:575:        // 4.7.J.2 clear_spill calls were inserted between bind and append,
crates/ql-exec/src/workbook_runtime.rs:577:        // spill / old self-spill already dissolved. Restored ordering:
crates/ql-exec/src/workbook_runtime.rs:578:        // bind → append → (only then) clear_spill mutations.
crates/ql-exec/src/workbook_runtime.rs:590:        // spill, dissolve THAT spill first. Per design § 10.2 (user
crates/ql-exec/src/workbook_runtime.rs:591:        // write into spill target): typing into a spilled cell breaks
crates/ql-exec/src/workbook_runtime.rs:592:        // the host spill — the host's anchor formula stays put, but
crates/ql-exec/src/workbook_runtime.rs:606:        //     storage layer (no spill) and dirty set (anchor not
crates/ql-exec/src/workbook_runtime.rs:611:        // self-spill clear below) because:
crates/ql-exec/src/workbook_runtime.rs:612:        //   - The host spill's anchor is at a DIFFERENT cell, so its

exec
/bin/zsh -lc "nl -ba crates/ql-storage/src/workbook.rs | sed -n '30,75p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc 'rg -n "spill_anchor_at|spill_anchors" crates/ql-storage/src/workbook.rs' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
    30	pub enum NamedTarget {
    31	    /// Named single cell (`SalesRow1 = $A$1`).
    32	    Cell(Address),
    33	    /// Named range (`Sales = $A$2:$A$1000`).
    34	    Range(Range),
    35	    /// Named constant (`TaxRate = 0.21`).
    36	    Constant(Value),
    37	    /// Named formula (`Profit = Revenue - Costs`). Stored as the raw formula source; the
    38	    /// binder re-parses + evaluates in the use-site context. Phase 3+ feature.
    39	    Formula(std::sync::Arc<str>),
    40	}
    41	
    42	/// Defined-names table.
    43	///
    44	/// Phase 2A.1 (2026-05-12): real workbook-scope storage. Phase 2A.6 audit fix
    45	/// (H2/M6/D5): canonicalization is enforced at the **insert** boundary — `set`
    46	/// uppercases the key on write, so every entry lives at its canonical
    47	/// upper-case form. `lookup` is case-sensitive on canonical form (fast path for
    48	/// the parser, which already canonicalizes); `lookup_ci` exists as a
    49	/// belt-and-suspenders entry point for callers that bypass the parser
    50	/// canonicalization. Sheet-scope names (Excel's `Sheet1!Local`) are deferred to
    51	/// Phase 3+ ql-formula-semantics.
    52	///
    53	/// ## Generation counter (Phase 2B.3)
    54	///
    55	/// `generation` is bumped on every successful `set` / `clear`. Bind-plan
    56	/// caches in `ql-exec` key cached plans by `(formula_text, sheet,
    57	/// generation)` so a name registration / rename invalidates affected plans
    58	/// without an explicit cache-flush call — the next lookup just misses.
    59	#[derive(Clone, Debug, Default)]
    60	pub struct NameTable {
    61	    entries: HashMap<Arc<str>, NamedTarget>,
    62	    /// Monotonic counter bumped on every successful mutation. See struct
    63	    /// docs for the cache-invalidation contract. Wraps at u64::MAX (4×10^18
    64	    /// mutations — not a realistic concern).
    65	    generation: u64,
    66	}
    67	
    68	/// Errors emitted by `NameTable::set` when a registration is refused.
    69	///
    70	/// Phase 2A.9 audit M6 (2026-05-12): closes the AI() reservation hole. The
    71	/// previous `NameTable::set` returned `()` and silently accepted any name,
    72	/// including ones reserved by the engine. A user calling
    73	/// `wb.set_name("AI", NamedTarget::Constant(...))` then writing `=AI` bypassed
    74	/// the CORR-06 AI sentinel because `=AI` (no parens) goes through the binder's
    75	/// NameRef path instead of the function-call path. Now the registration

 succeeded in 0ms:
274:    spill_anchors: crate::SpillAnchorTable,
594:    pub fn spill_anchors(&self) -> &crate::SpillAnchorTable {
595:        &self.spill_anchors
601:    pub fn spill_anchor_at(
607:        self.spill_anchors.anchor_at(sheet, row, col)
620:        self.spill_anchors.target_anchor(sheet, row, col)
632:        self.spill_anchors.register(anchor, shape)
644:    ///   `spill_anchor_at(...).is_some()`.
649:        let shape = self.spill_anchors.unregister(anchor)?;
1387:            wb.spill_anchor_at(0, 1, 1),
1422:        assert!(wb.spill_anchor_at(0, 0, 0).is_none());
1477:    fn workbook_spill_anchors_accessor_returns_table() {
1480:        assert_eq!(wb.spill_anchors().len(), 0);
1485:        assert_eq!(wb.spill_anchors().len(), 2);
1498:        assert!(wb.spill_anchor_at(0, 0, 0).is_none());

codex
Verified `HEAD = 9c850f37733`.

**Per Finding**
- HIGH-1 — CLOSED: `validate_footprint_bounds` uses `checked_add` at [tables.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-storage/src/tables.rs:253), and all five producer/replay/loader paths call it after zero-dim checks and before footprint loops/inserts.
- MEDIUM-1 — STILL OPEN: most loader invariants are present at [qbook_format.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-io/src/qbook_format.rs:1751), but duplicate canonical names can still bypass if the first persisted key is mixed-case because insertion uses raw `entry.name` at [qbook_format.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-io/src/qbook_format.rs:1814) while the test only covers identical uppercase names.
- MEDIUM-2 — CLOSED: load rejects `col.id == u32::MAX` at [qbook_format.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-io/src/qbook_format.rs:1787); `TableTable::insert` still has unchecked `max_id + 1` at [tables.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-storage/src/tables.rs:349), but loader and allocator tests pin non-malicious inputs.
- LOW-1 — CLOSED: no-op resize returns after the existence snapshot at [workbook_runtime.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:1956) and before op-log append at [workbook_runtime.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:2095), with the op-log test at [workbook_runtime.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:10614).
- LOW-2 — CLOSED: composed replay and persistence/runtime tests exist at [oplog_e2e.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/tests/oplog_e2e.rs:899) and [oplog_e2e.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/tests/oplog_e2e.rs:951).

**Probes**
- A — OK: `top + rows - 1` semantics are correct via `checked_add(rows - 1)`.
- B — OK: `load_workbook` builds a local `Workbook`; on `MalformedTable`, partial inserts are dropped with the failed load result.
- C — OK: exact no-op resize performs no structural mutation, so skipping plan-cache clear is correct for that path.

Targeted tests run with explicit cargo path passed: footprint helper, resize no-op, composed e2e, and malformed-table tests.

**NOT SHIP-READY**
tokens used
109,017
Verified `HEAD = 9c850f37733`.

**Per Finding**
- HIGH-1 — CLOSED: `validate_footprint_bounds` uses `checked_add` at [tables.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-storage/src/tables.rs:253), and all five producer/replay/loader paths call it after zero-dim checks and before footprint loops/inserts.
- MEDIUM-1 — STILL OPEN: most loader invariants are present at [qbook_format.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-io/src/qbook_format.rs:1751), but duplicate canonical names can still bypass if the first persisted key is mixed-case because insertion uses raw `entry.name` at [qbook_format.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-io/src/qbook_format.rs:1814) while the test only covers identical uppercase names.
- MEDIUM-2 — CLOSED: load rejects `col.id == u32::MAX` at [qbook_format.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-io/src/qbook_format.rs:1787); `TableTable::insert` still has unchecked `max_id + 1` at [tables.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-storage/src/tables.rs:349), but loader and allocator tests pin non-malicious inputs.
- LOW-1 — CLOSED: no-op resize returns after the existence snapshot at [workbook_runtime.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:1956) and before op-log append at [workbook_runtime.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:2095), with the op-log test at [workbook_runtime.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:10614).
- LOW-2 — CLOSED: composed replay and persistence/runtime tests exist at [oplog_e2e.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/tests/oplog_e2e.rs:899) and [oplog_e2e.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/tests/oplog_e2e.rs:951).

**Probes**
- A — OK: `top + rows - 1` semantics are correct via `checked_add(rows - 1)`.
- B — OK: `load_workbook` builds a local `Workbook`; on `MalformedTable`, partial inserts are dropped with the failed load result.
- C — OK: exact no-op resize performs no structural mutation, so skipping plan-cache clear is correct for that path.

Targeted tests run with explicit cargo path passed: footprint helper, resize no-op, composed e2e, and malformed-table tests.

**NOT SHIP-READY**
