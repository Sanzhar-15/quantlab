Reading additional input from stdin...
OpenAI Codex v0.130.0
--------
workdir: /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
model: gpt-5.5
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 019e24ef-c4a3-74c1-abe3-f7b8ebd6869a
--------
user
Independent Codex pull-up review of Phase 4.7.H (W5-101) — SpillAnchorTable storage + Workbook::clear_spill_at.

Read these first:
1. The design doc at docs/architecture/2026-05-14-array-formulas-and-spills.md — particularly section 7 (Spill anchor data model) and section 8.3 (Re-eval invariant).
2. Your prior reviews: docs/audits/2026-05-14-phase-4.7-design-codex-review.txt — Codex flagged in MEDIUM-7 that SpillAnchorTable must be map-only with overlay clearing in workbook layer.
3. The W5-101 commit: run `git show 57db4639473 --stat` then inspect:
   - crates/ql-storage/src/spill.rs (new module — SpillShape, SpillAnchorTable, SpillBlockError, SpillNotFoundError)
   - crates/ql-storage/src/workbook.rs (new spill_anchors field + accessors + register_spill + clear_spill_at)
   - crates/ql-storage/src/lib.rs (re-exports)

Working directory: /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
Branch: feat/quantbook-engine, HEAD 57db4639473.

The implementation claims:
- SpillAnchorTable is map-only — no overlay clearing in the table layer.
- Workbook::clear_spill_at is the workbook-layer convenience that unregisters + clears computed overlays at every target cell.
- register is atomic — the WHOLE rectangle is pre-checked for collision before any insert.
- The anchor cell IS its own target — targets[anchor] = anchor.
- Both-or-neither invariant: every anchors entry has exactly rows*cols matching targets entries.
- Disjoint footprints invariant: no cell belongs to two anchors.
- Degenerate shapes are rejected at register time.
- 21 module tests + 6 workbook tests, 2122 workspace tests total.

Verify each claim. For each:
- PASS / FLAG verdict + one-sentence justification.
- For FLAG: HIGH/MEDIUM/LOW + concrete file.rs:line citation + suggested fix.

Specifically scrutinize:
1. Atomicity of `register`: the pre-scan walks the rectangle for collisions, then a separate insert loop. Is there any path where a panic mid-insert (e.g. HashMap allocation OOM, integer overflow on `arow + dr`) could leave the table in a partially-inserted state?
2. Integer overflow on rectangle iteration: `for dr in 0..shape.rows { for dc in 0..shape.cols { anchor.1 + dr } }` — if anchor.1 is near u32::MAX and dr extends beyond, the addition wraps. Does this need an overflow guard for very large spill ranges at the workbook edge? Excel's max row is 1,048,575 (MAX_ROW), so a 1-million-tall spill at row 0 is in-bounds; spills at the edge of the worksheet COULD overflow. Should `register` enforce that `anchor + shape` fits within MAX_ROW / MAX_COLUMN?
3. The Workbook::clear_spill_at signature: returns SpillNotFoundError on miss. Callers (the future 4.7.J runtime path) will likely WANT idempotent clearing — call it whether or not the anchor exists, succeed silently. Should this be split into a `try_clear_spill_at` (errors) and `clear_spill_if_present` (idempotent)?
4. `clear_spill_at` iterates the rectangle using `arow + dr`, `acol + dc`. Same overflow concern as #2. If the runtime registered a spill that PRE-overflowed (caller mistake), unregister would silently miss cells.
5. The reverse map is large for big spills — a 1000-row SEQUENCE spill creates 1000 HashMap entries in `targets`. Storage-side fine; just verify the memory accounting is reasonable. Per design § 14.2 the calcgraph stays formula-node-heavy (no per-target nodes), so the cost is bounded to this HashMap.
6. SpillShape derives PartialEq + Eq + Hash. The Hash derive uses field order (rows, cols) — verify this doesn't have any collision issues with other (u32, u32) tuples that might share a HashMap key space. Hashes have separate spaces per type so this is fine; just a sanity check.
7. `SpillAnchorTable::iter_anchors` returns HashMap iteration order, which is non-deterministic. The doc says "Order is HashMap-iteration order — unspecified and may change across mutations." Is this OK for the future persistence save path (4.7.L) which needs deterministic output? Or will save sort separately?
8. Cross-sheet spill: register_spill takes `(SheetId, RowId, ColId)` so a 2x3 spill at (sheet=0, ...) only iterates within sheet 0. Per design v1 scope is single-sheet only. Verify no test attempts a "spill across sheets" registration that would be ill-defined.

Optional but useful:
9. Defensive testing of u32 overflow at the workbook boundary: would help close the integer-overflow gap if you flag it.

Write your full review to stdout — the caller tees to a file.
codex
I’ll review the docs and commit state first, then inspect the spill/workbook code and tests against each claim. Since this is a review request, I’ll keep the output findings-led with concrete citations.
exec
/bin/zsh -lc "sed -n '1,260p' docs/architecture/2026-05-14-array-formulas-and-spills.md" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc "sed -n '1,260p' docs/audits/2026-05-14-phase-4.7-design-codex-review.txt" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
# Phase 4.7 — Array Formulas And Dynamic Spills (design)

**Date:** 2026-05-14
**Phase:** 4.7 (per `docs/MASTER-PLAN.md` §7)
**Status:** APPROVED post-Codex review (W5-94). Codex review at `docs/audits/2026-05-14-phase-4.7-design-codex-review.txt`: 5 HIGH + 4 MEDIUM + 3 LOW design findings, all folded into this doc.
**Pattern:** mirrors Phase 4.6 design doc (`2026-05-13-cross-sheet-references.md`).

## Codex review changelog

The original draft was substantively wrong on five HIGH points. This revision encodes the corrected design:

- **HIGH (§ 6.3):** "scalar context returns first cell" was not Excel canon. **Revised:** v1 returns `#CALC!` for array-in-scalar-context; proper implicit intersection / `@` deferred to Phase 4.9.
- **HIGH (§ 6 + § 6.2):** `ArrayFn` over `EvalResult` (defined in `ql-exec`) created a `ql-functions` → `ql-exec` reverse-dependency cycle. **Revised:** array value type moves to `ql-types`; function ABI unifies the four tiers into one dispatch shape.
- **HIGH (§ 10):** parallel `outgoing_spill_targets` adjacency + eager CellNode creation conflicts with the existing calcgraph scheduler. **Revised:** spill targets are PRODUCER ALIASES — at dependency-extraction time, a `CellRef` to a cell with `spill_target_anchor(c) == Some(anchor)` becomes an edge to the anchor's formula node, lazy and graph-shape-preserving.
- **HIGH (§ 8.3):** missing "always unregister old spill before re-eval" step. **Revised:** explicit unregister-then-eval invariant; even blocked re-eval clears the old footprint first.
- **HIGH (§ 12):** "no schema bump because computed overlay round-trips" was false — current save emits non-formula non-blank cells as user-overlay records, so spill targets reload as user blockers. **Revised:** save side SKIPS cells inside an active spill range (lookup via `spill_target_anchor`); load → recompute_all re-derives. No schema bump needed.
- **MEDIUM:** `SpillAnchorTable` map-only; overlay clearing in `Workbook` API. Binder returns `BindError`, not panic. Sub-phase order moved graph before runtime.
- **LOW:** `ErrorValue::Spill` already exists at `crates/ql-types/src/error.rs:38` — moved to "already in place." Terminology: anchor vs target vs Excel's `A1#` spill-range-ref syntax (Phase 4.9). Blocking example fixed.

## 1. Acceptance criteria (from MASTER-PLAN)

- **ARR-4-01** array literals parse and evaluate.
- **ARR-4-02** spill writes computed overlay only.
- **ARR-4-03** blocked spills return the right error (`#SPILL!`).
- **ARR-4-04** dynamic spill resizing invalidates dependents.

## 2. Current state — what already works, what's missing

### 2.1 Already in place

- **AST:** `Expr::Array(Vec<Vec<Expr>>)` shape-locked in `crates/ql-formula-syntax/src/ast.rs:63`. `Expr::Spill(Box<Expr>)` shape-locked at line 67 but is RESERVED for Excel's `A1#` spill-range-ref syntax (Phase 4.9), NOT for the runtime spill anchor. Parser doesn't construct either; binder rejects with `BindError::UnsupportedVariant`.
- **`ErrorValue::Spill`** at `crates/ql-types/src/error.rs:38` with canonical `"#SPILL!"` sigil. Round-trips through `CellWireValue::Error` wire format (verified by W5-93 sigil round-trip tests).
- **Storage:** `ColumnStore::computed_overlays: Vec<SparseOverlay>` already exists (Phase 3.5, W5-38). `Sheet::put_computed` / `clear_computed` API in place. Read cascade is `user → computed → base`.
- **Calcgraph:** `Node::Spill(SpillNode { anchor, shape_rows, shape_cols })` shape-locked in `ql-calcgraph/src/node.rs:107`. `add_spill_node` panics with "Phase 3+ feature" message. **NB: post-revision this node may not be needed at all** — spill targets aren't graph nodes (see § 10).
- **Functions:** registry has `ScalarFn` / `RangeAwareFn` / `ContextAwareFn` tiers in `crates/ql-functions/src/registry.rs`. **Will unify, not extend** (see § 6.2).
- **Op log + persistence:** stable. No new variants planned. NO schema bump.

### 2.2 Crate dependency map (relevant for § 6)

```
ql-types          (leaf)
  ↑
ql-functions      (depends on ql-types)
  ↑
ql-storage / ql-formula-syntax / ql-io / ql-oplog / ql-calcgraph
  ↑
ql-exec           (depends on all of the above)
```

`ql-functions` CANNOT import from `ql-exec` — that would close a cycle. Any type referenced by the function ABI must live in `ql-types` or `ql-functions` itself. **This is why the original draft's `ArrayFn(args: &[EvalResult])` design was wrong.**

### 2.3 Missing

- Lexer: `{` and `}` tokens; `,` and `;` as array-context separators.
- Parser: array-literal parsing rules.
- Printer: round-trip for `Expr::Array`.
- Binder: `Expr::Array` → `ExprPlan::Array` lowering with element-arity validation.
- Array value type in `ql-types` (so `ql-functions` can reference it).
- Unified function ABI in `ql-functions` (collapse `ScalarFn` / `RangeAwareFn` / `ContextAwareFn` into one dispatch shape; gain array support).
- Storage: spill-anchor table (anchor → shape lookup; target → anchor reverse).
- Workbook-layer `clear_spill_at(anchor)` (clears both the table entries AND the computed overlays at every target).
- Runtime: spill writeback (clear-old-first → eval → block-check → register-and-write).
- Calcgraph: producer-alias edges for spill targets (lazy, at dep-extraction time).
- Persistence: save-side skip for cells that are spill targets (lookup via `spill_target_anchor`).
- First array-returning functions: SEQUENCE, FILTER, TRANSPOSE.

## 3. Grammar

### 3.1 Array literal syntax

ASCII-only separators. Match Excel's en-US canonical syntax (deferred locale support is Phase 4.9):

```
array_literal := '{' array_row (';' array_row)* '}'
array_row    := array_cell (',' array_cell)*
array_cell   := literal | unary literal     // see § 3.3 for constraints
literal      := NUMBER | STRING | BOOL      // no nested refs in Phase 4.7
```

Horizontal: `{1, 2, 3}` → 1×3.
Vertical: `{1; 2; 3}` → 3×1.
2D: `{1, 2; 3, 4}` → 2×2.

### 3.2 Element-arity rule

All rows must have the same number of cells. Mismatched-row literals (e.g. `{1, 2; 3}`) error at parse time with `ParseError::ArrayRowArityMismatch { expected, found, row }`. **This is a Quantbook v1 subset** — Excel pads with `#N/A` for missing cells, and `{1,2;3}` evaluates as `{1,2;3,#N/A}`. Quantbook v1 chooses loud rejection over silent padding; the pad-with-`#N/A` variant is a Phase 4.10 polish item gated on test-corpus completeness.

### 3.3 Element kinds restricted in v1

Array cells in v1 — the allowed `array_cell` subset:

- `NUMBER` (positive or negative literal)
- `STRING` literal
- `BOOL` literal (`TRUE` / `FALSE`)
- `error literal` — `#NULL!`, `#DIV/0!`, `#VALUE!`, `#REF!`, `#NAME?`, `#NUM!`, `#N/A`, `#SPILL!`, `#CALC!` — the nine Excel-canon error sigils available as `ErrorValue::ALL` entries in `ql-types`. Needed for `FILTER(_, _, #N/A)` etc. (W5-97 closure / Sonnet M2: `#GETTING_DATA` was listed in the original draft but is not in `ErrorValue::ALL` — it's an Excel-Online-specific volatile data-wait state that we don't model in v1; lifting it would require adding the variant alongside `Disconnected`/`Timeout`. Deferred to Phase 4.10 or whenever a real use case appears.)

NO nested CellRef / RangeRef / Function calls / Unary operators (other than the implicit sign on numeric literals). Codex MEDIUM finding: explicitly document this as a Quantbook v1 subset.

Phase 4.10 lifts to allow refs (`{A1, B1; A2, B2}`) once the array evaluator and dependency extraction prove stable.

**Rationale:** array literals with refs would require shape determination at bind/eval time, not parse time, AND would entangle the array-literal grammar with the dependency graph. Restricting v1 to constants keeps the parser simple and the test corpus closed.

## 4. AST

`Expr::Array(Vec<Vec<Expr>>)` is already shape-locked. The parser populates it directly. Each inner `Expr` is constrained to the kinds in § 3.3.

**`Expr::Spill(Box<Expr>)` terminology note** (Codex LOW): the existing shape-lock at `ast.rs:67` is RESERVED for Excel's `A1#` spill-range-ref SYNTAX (Phase 4.9), NOT the runtime spill anchor. Excel's `A1#` means "the spill range anchored at A1" — it's a reference operator, not an anchor marker. The runtime spill anchor (the formula cell at the top-left of a spilled range) lives in `Workbook::spill_anchors` (§ 7), not in the AST. Phase 4.7 does not construct `Expr::Spill`; that's Phase 4.9's job alongside `@`.

## 5. Binder — `ExprPlan::Array`

New variant:

```rust
ExprPlan::Array(Vec<Vec<ExprPlan>>)
```

Binder rule: `Expr::Array` → `ExprPlan::Array` element-wise. Each element binds to a `Number` / `Bool` / `String` / `Error` plan (no `Unary` — the lexer folds signs into literals).

**Codex MEDIUM:** `Expr::Array` is a PUBLIC AST variant. Tests + tools can construct invalid ASTs (e.g. ragged shapes via `Vec::push`). The binder MUST reject ragged ASTs with a clean error, not panic:

```rust
BindError::ArrayRowArityMismatch { expected: u32, found: u32, row: u32 }
```

This shadows the equivalent `ParseError` for the case where the AST was constructed directly. No `BindError::UnsupportedVariant` for `Expr::Array` itself — it's now supported.

## 6. Evaluator — REDESIGN per Codex HIGH

### 6.1 `ArrayValue` type — lives in `ql-types`

Codex HIGH: `EvalResult` in `ql-exec` cannot be referenced by `ql-functions` without closing a dependency cycle (`ql-functions` → `ql-types` only; `ql-exec` → `ql-functions`).

**Revised placement:** `ArrayValue` lives in `ql-types` alongside `Value` and `ErrorValue`:

```rust
// crates/ql-types/src/array.rs (new module)
#[derive(Clone, Debug, PartialEq)]
pub struct ArrayValue {
    pub rows: u32,
    pub cols: u32,
    pub cells: Vec<Value>,  // row-major, length = rows * cols
}

impl ArrayValue {
    pub fn at(&self, row: u32, col: u32) -> &Value { ... }
    pub fn first(&self) -> &Value { &self.cells[0] }
    pub fn is_degenerate(&self) -> bool { self.rows == 0 || self.cols == 0 }
}
```

`Value` itself stays scalar (no `Value::Array(...)` variant). Storage cells remain scalar by Excel canon — spilling MATERIALIZES arrays into per-cell scalars in the computed overlay. The array vs scalar distinction lives at the EVAL boundary via `EvalResult` (still in `ql-exec`).

### 6.2 Function ABI — UNIFY STORAGE, tagged dispatch

Codex HIGH: adding `ArrayFn` as a 4th parallel tier compounds the dispatch sprawl. The right move is to collapse the three parallel HashMaps in `FunctionRegistry` into **one storage map** keyed by name, valued by a tagged enum that distinguishes the tier of the underlying fn pointer.

**As-shipped dispatch shape** in `ql-functions` (W5-96 / Phase 4.7.B):

```rust
// crates/ql-functions/src/registry.rs (shipped W5-96)
pub enum FunctionArg {
    Scalar(Value),
    Range { values: Vec<Value>, rows: usize, cols: usize },  // matches existing FnArg::Range
    Array(ArrayValue),                                       // new — from Expr::Array or another fn return
}

pub enum FunctionReturn {
    Scalar(Value),
    Array(ArrayValue),  // new — for SEQUENCE / FILTER / TRANSPOSE
}

pub struct FunctionContext<'a> {
    pub eval_ctx: &'a EvalContext,
    // Additive shape — fields can land later without changing the FunctionFn alias.
    // Workbook access threads through eval-site env in W5-100 (Phase 4.7.G), not here.
}

pub type FunctionFn = fn(&[FunctionArg], &FunctionContext) -> FunctionReturn;

/// Tagged union — one variant per supported callable shape.
pub enum RegisteredFn {
    Scalar(ScalarFn),               // legacy `fn(&[Value]) -> Value`
    RangeAware(RangeAwareFn),       // legacy `fn(&[FnArg]) -> Value`
    ContextAware(ContextAwareFn),   // legacy `fn(&[Value], &EvalContext) -> Value`
    Unified(FunctionFn),            // new array-returning ABI
}

pub struct FunctionRegistry {
    fns: HashMap<&'static str, RegisteredFn>,
}
```

**Migration:**
- Existing `register` / `register_range_aware` / `register_context_aware` keep their public signatures; they store the appropriate `RegisteredFn::*` variant. The ~130 `r.register("SUM", scalar_fns::sum)` lines in `default_registry` work unchanged.
- New array-returning functions register via `register_unified(name, FunctionFn)`.
- The `lookup_*` filter views (`lookup`, `lookup_range_aware`, `lookup_context_aware`, `lookup_unified`) match on the enum variant and return the tier-specific fn pointer or `None`.
- `lookup_any(name) -> Option<&RegisteredFn>` is the eval-site entry point that returns the tagged enum directly — one HashMap lookup, one match arm per tier.

**Why tagged dispatch, not adapter-wrapping:** the alternative ("every register_* shim-wraps the legacy fn into a `FunctionFn`") would require boxing fn pointers (`Box<dyn Fn>`) because a bare `fn` pointer can't capture the wrapped legacy fn pointer. Tagged dispatch keeps fn pointers as plain `fn` values (Copy, no allocation) and pushes the per-tier match into the single eval-site dispatch instead of into per-call adapter calls. Net effect: same single-storage-map win Codex wanted, lower runtime overhead, simpler implementation. Full adapter normalization remains available as a future polish if the eval-site arms grow unwieldy.

**Caller-side dispatch** in `ql-exec::eval` (W5-100-AUDIT / Phase 4.7.G migration, shipped post-Sonnet closure):

```rust
match registry.lookup_any(name) {
    Some(RegisteredFn::Scalar(f)) => /* materialize Vec<Value>, call f */ ,
    Some(RegisteredFn::RangeAware(f)) => /* materialize Vec<FnArg>, call f */ ,
    Some(RegisteredFn::ContextAware(f)) => /* materialize Vec<Value> + EvalContext, call f */ ,
    Some(RegisteredFn::Unified(f)) => {
        // Materialize Vec<FunctionArg> + FunctionContext; route the return:
        let ret: FunctionReturn = f(&args, &ctx);
        let eval_result: EvalResult = match ret {
            FunctionReturn::Scalar(v) => EvalResult::Scalar(v),
            FunctionReturn::Array(a)  => EvalResult::Array(a),
        };
    }
    None => /* unresolved function name */ ,
}
```

`EvalResult` (still in `ql-exec`) wraps the ABI-side return at the eval boundary. No cycle.

### 6.3 Eval contexts — REDESIGN per Codex HIGH

The original "scalar context returns first cell" rule was **NOT Excel canon**. Excel's behavior:
- **Dynamic-array Excel (365+):** array-in-scalar-context spills. `@` explicitly opts back into legacy implicit-intersection.
- **Legacy Excel:** implicit intersection takes the cell at the row/col INTERSECTION of the calling formula's row/col with the array's row/col span. "First cell" is wrong in both modes.

**Revised v1 rule:** array-in-scalar-context produces `Value::Error(ErrorValue::Calc)`. Specifically:

- **Cell-boundary context** (the formula expression at a cell's root): array result → spill (§ 8).
- **Sub-expression scalar context** (binary operand, scalar function arg): array result → `#CALC!`.
- **Aggregate-arg context** (existing `BindContext::AggregateArg` from Phase 2B.4): array result is consumed as a 2D range of values, NOT as a single scalar. This already works for `RangeRef` and named ranges; extending to literal arrays just feeds the cells.

Test:
- `=SEQUENCE(3) + 1` (no spill — wrapped in `+1` binary op) → `#CALC!`. The `+1` operand context is scalar.
- `=SUM(SEQUENCE(3))` → 6. The SUM arg context is aggregate; SEQUENCE's 3×1 result feeds as 3 cells.
- `=SEQUENCE(3)` at cell A1 → spills to A1, A2, A3.

Implicit intersection / `@` / xlsx-canon scalarization is **Phase 4.9 work**. Documented at § 14.5.

## 7. Spill anchor data model (storage)

### 7.1 New `SpillAnchorTable` in `ql-storage`

```rust
pub struct SpillAnchorTable {
    /// Anchor (sheet, row, col) → shape (rows, cols).
    /// One entry per spilling formula.
    anchors: HashMap<(SheetId, RowId, ColId), SpillShape>,
    /// Reverse map: each spilled-into cell → its anchor.
    /// Used for fast "is this cell part of a spill?" lookup
    /// at user-write time.
    targets: HashMap<(SheetId, RowId, ColId), (SheetId, RowId, ColId)>,
}

pub struct SpillShape {
    pub rows: u32,
    pub cols: u32,
}
```

 succeeded in 0ms:
Reading additional input from stdin...
OpenAI Codex v0.130.0
--------
workdir: /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
model: gpt-5.5
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 019e244a-d859-7361-81f7-fa07772f00b0
--------
user
Read this design doc fully before responding:
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/docs/architecture/2026-05-14-array-formulas-and-spills.md

Also read the prior phases design doc for the pattern:
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/docs/architecture/2026-05-13-cross-sheet-references.md

This is the Phase 4.7 design doc (Array Formulas + Dynamic Spills) for the Quantbook engine. It is the spec for ~13 sub-phases of implementation work. You are being asked for an independent design review BEFORE any code ships.

Working directory: /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
Branch: feat/quantbook-engine, HEAD b5bfc751cbc (post-W5-93).

For context, examine these existing files to verify the designs claims about current state:
- crates/ql-formula-syntax/src/ast.rs (Expr::Array, Expr::Spill shape-locks)
- crates/ql-storage/src/column.rs (computed_overlays existing)
- crates/ql-storage/src/sheet.rs (put_computed)
- crates/ql-calcgraph/src/node.rs (SpillNode shape-lock)
- crates/ql-calcgraph/src/graph.rs (existing edge model — for section 10.1 evaluation)
- crates/ql-exec/src/plan.rs (ExprPlan variants — for section 5)
- crates/ql-exec/src/eval/* (eval entry points — for section 6)
- crates/ql-functions/src/registry.rs (function-tier model — for section 6.2 / section 14.4)

Walk through each section 1-16 and return:
- A PASS / FLAG verdict per section with one-sentence justification.
- For each FLAG: HIGH/MEDIUM/LOW severity + concrete concern + suggested fix.
- Explicit answers to the 5 open questions in section 16.
- A "What design decisions are still wrong" punch list ordered HIGH → MEDIUM → LOW.

HIGH = architectural mistake that will require painful rework after implementation.
MEDIUM = inconsistency or unclear contract; closure-prefers-fixed before sub-phase 4.7.A.
LOW = nit / phrasing / future-polish hint.

Be independent. Do not anchor on my framing if the implementation reality (per the code you inspect) disagrees. Specifically scrutinize:
- The "scalar context returns first cell" rule (section 6.3). Is that actually Excel canon? Or is it #VALUE!? Or is it implicit-intersection?
- The "eager vs lazy" decision in section 14.2.
- The Option A parallel adjacency in section 10.1.
- The function-tier proliferation (section 6.2 / section 14.4).
- The "no schema bump" claim in section 12.

Write your full review to stdout (it gets tee-d to a file by the caller).
codex
I’ll read the two docs and the named source files first, then I’ll compare the spec against the implementation reality section by section. This is a review request, so I’ll lead with concrete findings and avoid making code changes.
exec
/bin/zsh -lc "sed -n '1,260p' docs/architecture/2026-05-14-array-formulas-and-spills.md" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
# Phase 4.7 — Array Formulas And Dynamic Spills (design)

**Date:** 2026-05-14
**Phase:** 4.7 (per `docs/MASTER-PLAN.md` §7)
**Status:** DRAFT for Codex review.
**Pattern:** mirrors Phase 4.6 design doc (`2026-05-13-cross-sheet-references.md`).

## 1. Acceptance criteria (from MASTER-PLAN)

- **ARR-4-01** array literals parse and evaluate.
- **ARR-4-02** spill writes computed overlay only.
- **ARR-4-03** blocked spills return the right error (`#SPILL!`).
- **ARR-4-04** dynamic spill resizing invalidates dependents.

## 2. Current state — what already works, what's missing

### 2.1 Already in place

- **AST:** `Expr::Array(Vec<Vec<Expr>>)` and `Expr::Spill(Box<Expr>)` are SHAPE-LOCKED in `crates/ql-formula-syntax/src/ast.rs:63-67`. Parser doesn't construct them; binder rejects with `BindError::UnsupportedVariant`.
- **Storage:** `ColumnStore::computed_overlays: Vec<SparseOverlay>` already exists (Phase 3.5, W5-38). `Sheet::put_computed` / `clear_computed` API in place. Read cascade is `user → computed → base`.
- **Calcgraph:** `Node::Spill(SpillNode { anchor, shape_rows, shape_cols })` shape-locked in `ql-calcgraph/src/node.rs:107`. `add_spill_node` panics with "Phase 3+ feature" message.
- **Functions:** registry has `ScalarFn` / `RangeAwareFn` / `ContextAwareFn` tiers. No array-returning function tier yet.
- **Op log + persistence:** stable. No new variants planned.

### 2.2 Missing

- Lexer: `{` and `}` tokens; `,` and `;` as array-context separators.
- Parser: array-literal parsing rules.
- Printer: round-trip for `Expr::Array`.
- Binder: `Expr::Array` → `ExprPlan::Array` lowering with element-arity validation.
- Evaluator: array result values; spill-array result distinction.
- Storage: spill-anchor table (anchor → shape lookup; target → anchor reverse).
- Runtime: spill writeback (computed overlay only); spill blocking.
- Calcgraph: spill invalidation edges (spill target → anchor formula).
- Function registry: array-returning function tier (or extend existing tier).
- First array-returning functions: SEQUENCE, FILTER (+ TRANSPOSE for completeness).

## 3. Grammar

### 3.1 Array literal syntax

ASCII-only separators. Match Excel's en-US canonical syntax (deferred locale support is Phase 4.9):

```
array_literal := '{' array_row (';' array_row)* '}'
array_row    := array_cell (',' array_cell)*
array_cell   := literal | unary literal     // see § 3.3 for constraints
literal      := NUMBER | STRING | BOOL      // no nested refs in Phase 4.7
```

Horizontal: `{1, 2, 3}` → 1×3.
Vertical: `{1; 2; 3}` → 3×1.
2D: `{1, 2; 3, 4}` → 2×2.

### 3.2 Element-arity rule

All rows must have the same number of cells. Mismatched-row literals (e.g. `{1, 2; 3}`) error at parse time with `ParseError::ArrayRowArityMismatch { expected, found, row }`. Matches Excel canon: Excel pads with `#N/A` but for our v1 we reject loudly — pad-with-N/A is a Phase 4.10 polish item.

### 3.3 Element kinds restricted in v1

Array cells in v1: NUMBER / STRING / BOOL / unary-minus-NUMBER only. NO nested CellRef / RangeRef / Function calls inside an array literal. This matches IronCalc's `ArrayNode` model (numbers/strings/booleans/errors). Phase 4.10 can lift to allow `{A1, B1; A2, B2}` if needed.

**Rationale:** array literals with refs would require shape determination at bind/eval time, not parse time. Restricting v1 to constants keeps the implementation tractable and the test corpus complete.

## 4. AST

`Expr::Array(Vec<Vec<Expr>>)` is already shape-locked. The parser populates it directly. Each inner `Expr` is constrained to the kinds in § 3.3.

`Expr::Spill(Box<Expr>)` stays shape-locked but unconstructed in Phase 4.7 — spilling is a RUNTIME behavior, not a syntactic node. (Excel's `@` implicit-intersection operator is Phase 4.9 work, NOT here.)

## 5. Binder — `ExprPlan::Array`

New variant:

```rust
ExprPlan::Array(Vec<Vec<ExprPlan>>)
```

Binder rule: `Expr::Array` → `ExprPlan::Array` element-wise. Each element binds to a `Number` / `Bool` / `String` / `Unary { op: Neg, .. }` plan. No `BindError::UnsupportedVariant` for the literal itself (it's now supported).

Element-arity already validated at parse time. The binder asserts it (defense in depth) and panics if violated — that'd be a parser bug, not user input.

## 6. Evaluator

### 6.1 `ArrayValue` type

New value type at the EVAL layer (NOT at `ql_types::Value`). Lives in `ql_exec::eval`:

```rust
pub enum EvalResult {
    Scalar(Value),
    Array(ArrayValue),
}

pub struct ArrayValue {
    pub rows: u32,
    pub cols: u32,
    pub cells: Vec<Value>,  // row-major, length = rows * cols
}
```

**Why not put `ArrayValue` in `ql_types::Value`?** Two reasons:
1. Cells in storage are scalar by Excel canon (`Value::Number`, `Value::Text`, etc.). Spilling MATERIALIZES arrays into per-cell scalars in the computed overlay.
2. A `Value` is what `Sheet::read` returns; promoting that to an enum-of-enum would touch every caller. Eval is the right boundary.

### 6.2 Function tier extension

`FunctionRegistry` gains a new tier:

```rust
pub type ArrayFn = fn(args: &[EvalResult], ctx: &EvalContext) -> EvalResult;
```

Registration: `register_array_fn(name, sig, f)`. Dispatched by name like the other tiers. Functions that ALWAYS return arrays (SEQUENCE, FILTER, UNIQUE, SORT, TRANSPOSE) register as `ArrayFn`. Functions that CAN return arrays based on input (e.g. an arithmetic op on arrays — Phase 4.7 doesn't do this; that's array-of-scalars broadcasting, Phase 4.10) stay scalar.

### 6.3 Eval contexts

- **Scalar context** (current default): `EvalResult::Array(_)` → first cell (`cells[0]`); rest discarded. Matches Excel's pre-365 implicit-intersection behavior. Used inside `Function::ScalarFn` argument lists.
- **Array context** (new): the formula at the cell BOUNDARY. Used by `WorkbookRuntime::set_formula` + recompute. If the eval result is `EvalResult::Array`, the runtime spills.

The eval entry point for the runtime call site distinguishes between scalar contexts (inside aggregate args, inside binary operators) and the cell-boundary array context. This is the "cell-boundary" notion that HyperFormula calls "vertex value" — only the top-level formula at a cell can spill.

## 7. Spill anchor data model (storage)

### 7.1 New `SpillAnchorTable` in `ql-storage`

```rust
pub struct SpillAnchorTable {
    /// Anchor (sheet, row, col) → shape (rows, cols).
    /// One entry per spilling formula.
    anchors: HashMap<(SheetId, RowId, ColId), SpillShape>,
    /// Reverse map: each spilled-into cell → its anchor.
    /// Used for fast "is this cell part of a spill?" lookup
    /// at user-write time.
    targets: HashMap<(SheetId, RowId, ColId), (SheetId, RowId, ColId)>,
}

pub struct SpillShape {
    pub rows: u32,
    pub cols: u32,
}
```

**Invariant:** `targets[(s, r, c)] = (anchor_s, anchor_r, anchor_c)` iff `(anchor_s, anchor_r, anchor_c)` exists in `anchors` AND `(r, c)` is inside the rectangle `(anchor_r..anchor_r+shape.rows, anchor_c..anchor_c+shape.cols)`.

The anchor cell itself is BOTH an anchor AND its own target (the (0,0) of the spill range). Both maps include it.

### 7.2 Workbook integration

`Workbook::spill_anchors: SpillAnchorTable` field. Accessors:

- `pub fn spill_anchors(&self) -> &SpillAnchorTable`
- `pub fn spill_anchor_at(&self, sheet, row, col) -> Option<&SpillShape>` (anchor lookup)
- `pub fn spill_target_anchor(&self, sheet, row, col) -> Option<(SheetId, RowId, ColId)>` (reverse lookup; the anchor that this cell belongs to, or `None`)

Mutators (low-level — production routes through runtime):
- `pub fn register_spill(&mut self, anchor, shape) -> Result<(), SpillBlockError>` — adds to both maps; errors if the range collides with an existing spill that doesn't share the same anchor.
- `pub fn unregister_spill(&mut self, anchor)` — removes the anchor + all its targets.

### 7.3 Persistence

`Workbook::spill_anchors` is RUNTIME-DERIVED state. NOT persisted. On `.qbook` load, `recompute_all` re-derives the table by re-evaluating each formula.

**No schema bump for Phase 4.7.** This is a property of the design — the computed overlay already round-trips correctly via the v4 envelope's per-sheet overlay; spill anchors are re-derived from formulas.

## 8. Spill writeback (runtime)

### 8.1 `WorkbookRuntime::set_formula` extension

Pre-W5-93 flow: parse → bind → eval (scalar context) → `put_computed` at the cell.

New flow (Phase 4.7):

1. Parse + bind formula.
2. Evaluate at cell-boundary context. Result is `EvalResult::Scalar(v)` or `EvalResult::Array(a)`.
3. If scalar: existing path. `put_computed` at the cell.
4. If array:
   a. Compute spill target rectangle `(row..row+a.rows, col..col+a.cols)`.
   b. Bounds-check: range must fit within `MAX_ROW` / `MAX_COLUMN`. Out-of-bounds → `#SPILL!` at anchor.
   c. **Blocking check (§ 9):** every cell in target range MUST be empty (no user value AND no other spill anchor's target). Else → `#SPILL!` at anchor.
   d. Register anchor: `workbook.spill_anchors.register_spill(anchor, shape)`.
   e. Write each array cell to its computed overlay via `put_computed`.
5. Op log: emit `Op::PutFormula { text }` only. The spill range is re-derived at replay time via the same eval path.

### 8.2 Anchor formula uniqueness

Per Excel canon: only the anchor cell has a formula. The other cells in the spill range have NO formula entry. `iter_formulas()` returns only the anchor.

When the user types into a cell within a spill range, the spill is invalidated (§ 10).

### 8.3 Shrinking / expanding spills

When a formula's eval result changes shape (e.g. `=SEQUENCE(5)` → `=SEQUENCE(3)`):

1. `unregister_spill(anchor)` first — clears the old anchor + targets + computed overlays at old positions.
2. Re-run the writeback path with the new shape.

Order matters: clear-then-write avoids spurious blocking errors when the new shape overlaps the old shape's footprint.

## 9. Spill blocking + `#SPILL!`

### 9.1 Blocking conditions


exec
/bin/zsh -lc 'git show 57db4639473 --stat' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-hiS6fjnH' (errno=Operation not permitted)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-XgTEZjjL' (errno=Operation not permitted)
commit 57db463947320f312fb19f4819da5b06a4df019a
Author: steppen0mad <nurtaymergaliyev@gmail.com>
Date:   Thu May 14 06:18:34 2026 +0100

    W5-101 / Engine Phase 4.7.H — SpillAnchorTable storage + Workbook::clear_spill_at
    
    Eighth implementation sub-phase of Phase 4.7. First storage-side step
    toward the runtime spill path (Phase 4.7.J). Per design § 7.
    
    New module `crates/ql-storage/src/spill.rs`:
    - `SpillShape { rows: u32, cols: u32 }` — spill-rectangle dimensions.
    - `SpillAnchorTable` — workbook-level table with two HashMaps:
      - `anchors: HashMap<(SheetId, RowId, ColId), SpillShape>` — one
        entry per spilling formula.
      - `targets: HashMap<(SheetId, RowId, ColId), (SheetId, RowId, ColId)>`
        — reverse map for O(1) "is this cell part of a spill?" lookup at
        user-write / dep-extraction time.
    - `SpillBlockError { TargetCellOccupied, AnchorAlreadyExists,
      DegenerateShape }` — registration failures. Runtime spill-writeback
      (Phase 4.7.J) maps these into `Value::Error(ErrorValue::Spill)` at
      the anchor.
    - `SpillNotFoundError` — `unregister` called on a non-anchor.
    
    `SpillAnchorTable` API (map-only per Codex W5-94 design § 7 review):
    - `new()` / `Default`.
    - `anchor_at(sheet, row, col) -> Option<&SpillShape>` — forward lookup.
    - `target_anchor(sheet, row, col) -> Option<(SheetId, RowId, ColId)>`
      — reverse lookup.
    - `register(anchor, shape) -> Result<(), SpillBlockError>` —
      pre-checks collision atomically: walks the WHOLE rectangle BEFORE
      inserting anything, so a partial registration is impossible.
    - `unregister(anchor) -> Result<SpillShape, SpillNotFoundError>` —
      returns the cleared shape so callers can iterate the rectangle for
      overlay-clearing without re-querying.
    - `iter_anchors()`, `len()`, `is_empty()`.
    
    Invariants enforced:
    1. Both-or-neither: for every `anchors` entry, exactly
       `shape.rows * shape.cols` matching `targets` entries point back.
    2. Anchor-is-also-target: `targets[anchor] = anchor`. One map lookup
       answers "is this cell part of a spill?" for anchor + non-anchor
       cells alike.
    3. Disjoint footprints: `register` rejects overlapping rectangles.
    
    Workbook integration:
    - New `Workbook::spill_anchors: SpillAnchorTable` field.
    - Read accessors: `spill_anchors()`, `spill_anchor_at(s, r, c)`,
      `spill_target_anchor(s, r, c)`.
    - `register_spill(anchor, shape) -> Result<(), SpillBlockError>` —
      map-only delegate.
    - `clear_spill_at(anchor) -> Result<(), SpillNotFoundError>` —
      workbook-layer convenience: unregisters anchor + clears the
      computed overlay at EVERY target cell. This is the function the
      runtime calls during "clear old spill before re-eval" (design
      § 8.3) and on `clear_formula` at an anchor cell (design § 10.3).
    
    The Workbook-level `clear_spill_at` couples the table mutation with
    `Sheet::clear_computed` because the workbook owns sheet access. The
    `SpillAnchorTable` stays pure-map; coupling lives one layer up.
    
    Tests (+27 from 2101 → 2122 - some baseline drift):
    - 21 spill-module tests covering register/unregister/lookup, 1x1 /
      5x1 / 2x3 rectangles, anchor-is-target invariant, collision
      rejection atomicity, degenerate-shape rejection, re-register
      after unregister, two disjoint spills, cross-sheet independence,
      iter_anchors, SpillShape::cell_count.
    - 6 workbook integration tests:
      - register_spill round-trip.
      - clear_spill_at clears computed overlays at every target.
      - register_spill rejects collision with existing.
      - clear_spill_at unknown anchor errors.
      - clear_spill_at preserves user overlays OUTSIDE the rectangle.
      - spill_anchors accessor returns the table.
    
    Gates (all 7 green):
    - fmt clean, clippy -D warnings clean
    - cargo test --workspace: 2122 passed / 0 failed
    - check-build-flags / check-cargo-lock-pins / check-multiversion-
      clones: OK
    - cargo audit: 1 allowed warning (atomic-polyfill, pre-existing)
    
    Next: Sonnet pull-up per design plan. Then W5-102 / Phase 4.7.I —
    calcgraph producer-alias dep extraction (the next storage-related
    piece — `walk_plan_for_deps` reroutes spill-target CellRef → anchor
    formula node).
    
    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>

 quantbook-engine/crates/ql-storage/src/lib.rs      |   2 +
 quantbook-engine/crates/ql-storage/src/spill.rs    | 492 +++++++++++++++++++++
 quantbook-engine/crates/ql-storage/src/workbook.rs | 195 +++++++-
 3 files changed, 688 insertions(+), 1 deletion(-)

codex
I have the commit summary and the design context. Next I’m reading the actual storage/workbook implementations with line numbers so any flags are tied to precise code, not just the commit message.
exec
/bin/zsh -lc "nl -ba crates/ql-storage/src/spill.rs | sed -n '1,620p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
     1	//! Spill-anchor storage — workbook-level table mapping anchor cells to
     2	//! their spill shapes, with a reverse map from each spilled-into cell
     3	//! back to its anchor.
     4	//!
     5	//! **Phase 4.7.H (W5-101):** introduces the storage data structures for
     6	//! dynamic-array spills. Per design doc § 7:
     7	//! `docs/architecture/2026-05-14-array-formulas-and-spills.md` § 7.
     8	//!
     9	//! ## Two-map shape
    10	//!
    11	//! ```text
    12	//! SpillAnchorTable {
    13	//!     anchors:  HashMap<(SheetId, RowId, ColId), SpillShape>,
    14	//!     targets:  HashMap<(SheetId, RowId, ColId), (SheetId, RowId, ColId)>,
    15	//! }
    16	//! ```
    17	//!
    18	//! - `anchors` — one entry per spilling formula. Key is the anchor cell
    19	//!   (top-left of the spill rectangle); value is the rectangle's
    20	//!   `(rows, cols)`.
    21	//! - `targets` — reverse map for fast `O(1)` "is this cell part of a
    22	//!   spill?" lookup at user-write / dep-extraction time. Key is each
    23	//!   cell in the spill rectangle (including the anchor itself); value
    24	//!   is the anchor.
    25	//!
    26	//! ## Invariants
    27	//!
    28	//! 1. **Both-or-neither:** for every key in `anchors`, exactly
    29	//!    `shape.rows * shape.cols` matching keys exist in `targets`, all
    30	//!    pointing back at the anchor. `register` and `unregister` are the
    31	//!    only mutators and preserve this jointly.
    32	//! 2. **Anchor-is-also-target:** the anchor cell itself is in `targets`
    33	//!    with `targets[(anchor)] = (anchor)`. This simplifies the
    34	//!    spill-detection logic at user-write time — a single map lookup
    35	//!    answers "is this cell part of a spill (anchor or otherwise)?".
    36	//! 3. **Disjoint footprints:** no cell belongs to two different anchors'
    37	//!    targets. The `register` collision check enforces this.
    38	//!
    39	//! ## Why map-only
    40	//!
    41	//! Per Codex W5-94 design review MEDIUM (§ 7): `SpillAnchorTable` is
    42	//! intentionally map-only. Overlay clearing on spill removal lives in
    43	//! `Workbook::clear_spill_at`, which couples the table mutation with
    44	//! `Sheet::clear_computed` calls because the workbook owns sheet
    45	//! access. Keeping the table layer pure-map keeps testability +
    46	//! responsibility clean.
    47	
    48	use std::collections::HashMap;
    49	
    50	use ql_types::{ColId, RowId, SheetId};
    51	
    52	/// Spill-rectangle dimensions. `rows * cols` is the total cell count;
    53	/// degenerate shapes (`rows == 0` or `cols == 0`) are runtime-detected
    54	/// at the WORKBOOK / RUNTIME boundary before reaching `register` —
    55	/// `SpillAnchorTable::register` requires both `rows >= 1` and `cols >= 1`
    56	/// per the invariant.
    57	#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
    58	pub struct SpillShape {
    59	    pub rows: u32,
    60	    pub cols: u32,
    61	}
    62	
    63	impl SpillShape {
    64	    /// Construct a shape; both dimensions must be >= 1.
    65	    pub fn new(rows: u32, cols: u32) -> Self {
    66	        Self { rows, cols }
    67	    }
    68	
    69	    /// Total cell count (rows * cols).
    70	    pub fn cell_count(&self) -> u64 {
    71	        (self.rows as u64) * (self.cols as u64)
    72	    }
    73	}
    74	
    75	/// Errors emitted by `SpillAnchorTable::register` when the rectangle
    76	/// would collide with an existing spill OR overlap an existing anchor.
    77	///
    78	/// The runtime spill-writeback path (Phase 4.7.J) maps these into
    79	/// `Value::Error(ErrorValue::Spill)` at the anchor cell; the storage
    80	/// layer surfaces them structurally so callers can distinguish causes.
    81	#[derive(Clone, Debug, PartialEq, Eq, thiserror::Error)]
    82	pub enum SpillBlockError {
    83	    /// A non-anchor cell inside the proposed spill rectangle is
    84	    /// already a target of a DIFFERENT existing spill. The runtime
    85	    /// surfaces this as `#SPILL!` at the proposed anchor.
    86	    #[error(
    87	        "spill blocked: cell ({sheet},{row},{col}) is already a target of \
    88	         anchor ({anchor_sheet},{anchor_row},{anchor_col})"
    89	    )]
    90	    TargetCellOccupied {
    91	        sheet: SheetId,
    92	        row: RowId,
    93	        col: ColId,
    94	        anchor_sheet: SheetId,
    95	        anchor_row: RowId,
    96	        anchor_col: ColId,
    97	    },
    98	
    99	    /// The proposed anchor cell IS an existing spill anchor (different
   100	    /// shape, or attempting to register twice without clearing first).
   101	    /// The runtime should call `clear_spill_at(anchor)` before re-
   102	    /// registering — see design § 8.3 (re-eval invariant).
   103	    #[error(
   104	        "spill blocked: cell ({sheet},{row},{col}) is already an anchor; \
   105	         clear it first via clear_spill_at"
   106	    )]
   107	    AnchorAlreadyExists {
   108	        sheet: SheetId,
   109	        row: RowId,
   110	        col: ColId,
   111	    },
   112	
   113	    /// Spill rectangle has zero rows or zero cols. Degenerate-shape
   114	    /// arrays from functions like `FILTER` with an all-false mask are
   115	    /// runtime errors per design § 8.1; the storage layer rejects
   116	    /// degenerate registration as a defensive guard.
   117	    #[error(
   118	        "spill blocked: degenerate shape ({rows} rows, {cols} cols) — both \
   119	         dimensions must be >= 1"
   120	    )]
   121	    DegenerateShape { rows: u32, cols: u32 },
   122	}
   123	
   124	/// Returned by `unregister` when called on a cell that isn't an anchor.
   125	#[derive(Clone, Debug, PartialEq, Eq, thiserror::Error)]
   126	#[error("no spill anchor at ({sheet},{row},{col})")]
   127	pub struct SpillNotFoundError {
   128	    pub sheet: SheetId,
   129	    pub row: RowId,
   130	    pub col: ColId,
   131	}
   132	
   133	/// Workbook-level spill-anchor table.
   134	///
   135	/// **Map-only:** mutations affect ONLY the two HashMaps. Computed-
   136	/// overlay clearing on `unregister` is the workbook layer's job
   137	/// (`Workbook::clear_spill_at`). See module-level docs for the
   138	/// rationale.
   139	#[derive(Clone, Debug, Default)]
   140	pub struct SpillAnchorTable {
   141	    anchors: HashMap<(SheetId, RowId, ColId), SpillShape>,
   142	    targets: HashMap<(SheetId, RowId, ColId), (SheetId, RowId, ColId)>,
   143	}
   144	
   145	impl SpillAnchorTable {
   146	    /// Empty table.
   147	    pub fn new() -> Self {
   148	        Self::default()
   149	    }
   150	
   151	    /// Lookup an anchor by cell. Returns `Some(shape)` iff the cell is
   152	    /// an active spill anchor. `O(1)`.
   153	    pub fn anchor_at(&self, sheet: SheetId, row: RowId, col: ColId) -> Option<&SpillShape> {
   154	        self.anchors.get(&(sheet, row, col))
   155	    }
   156	
   157	    /// Reverse lookup: the anchor that this cell is a target of.
   158	    /// Returns `Some((anchor_s, anchor_r, anchor_c))` iff this cell is
   159	    /// either an anchor or any non-anchor target cell. `O(1)`. Used by
   160	    /// the runtime spill-invalidation path (Phase 4.7.K) — when a user
   161	    /// writes a value at this cell, the anchor needs to re-evaluate.
   162	    pub fn target_anchor(
   163	        &self,
   164	        sheet: SheetId,
   165	        row: RowId,
   166	        col: ColId,
   167	    ) -> Option<(SheetId, RowId, ColId)> {
   168	        self.targets.get(&(sheet, row, col)).copied()
   169	    }
   170	
   171	    /// Register a new spill: `anchor` is the top-left cell, `shape` is
   172	    /// the rectangle's `(rows, cols)`. Adds the anchor to `anchors` AND
   173	    /// every cell in the rectangle to `targets`.
   174	    ///
   175	    /// Errors per `SpillBlockError`:
   176	    /// - `AnchorAlreadyExists` — `anchor` is already in `anchors`.
   177	    ///   Caller must `unregister(anchor)` first.
   178	    /// - `TargetCellOccupied` — some cell inside the rectangle is in
   179	    ///   `targets` already (a DIFFERENT spill claims it).
   180	    /// - `DegenerateShape` — `shape.rows == 0` or `shape.cols == 0`.
   181	    ///
   182	    /// **Atomicity:** registration is all-or-nothing. If the collision
   183	    /// scan finds an occupied target mid-iteration, NO entries are
   184	    /// inserted — the table state is unchanged.
   185	    pub fn register(
   186	        &mut self,
   187	        anchor: (SheetId, RowId, ColId),
   188	        shape: SpillShape,
   189	    ) -> Result<(), SpillBlockError> {
   190	        if shape.rows == 0 || shape.cols == 0 {
   191	            return Err(SpillBlockError::DegenerateShape {
   192	                rows: shape.rows,
   193	                cols: shape.cols,
   194	            });
   195	        }
   196	        // Pre-check: anchor not already registered.
   197	        if self.anchors.contains_key(&anchor) {
   198	            return Err(SpillBlockError::AnchorAlreadyExists {
   199	                sheet: anchor.0,
   200	                row: anchor.1,
   201	                col: anchor.2,
   202	            });
   203	        }
   204	        // Pre-check: every cell in the rectangle is unoccupied. We
   205	        // walk the WHOLE rectangle BEFORE inserting anything so a
   206	        // collision in the middle doesn't leave a partial registration.
   207	        let (asheet, arow, acol) = anchor;
   208	        for dr in 0..shape.rows {
   209	            for dc in 0..shape.cols {
   210	                let cell = (asheet, arow + dr, acol + dc);
   211	                if let Some(existing_anchor) = self.targets.get(&cell) {
   212	                    return Err(SpillBlockError::TargetCellOccupied {
   213	                        sheet: cell.0,
   214	                        row: cell.1,
   215	                        col: cell.2,
   216	                        anchor_sheet: existing_anchor.0,
   217	                        anchor_row: existing_anchor.1,
   218	                        anchor_col: existing_anchor.2,
   219	                    });
   220	                }
   221	            }
   222	        }
   223	        // Insertion. No partial state — all pre-checks passed.
   224	        self.anchors.insert(anchor, shape);
   225	        for dr in 0..shape.rows {
   226	            for dc in 0..shape.cols {
   227	                let cell = (asheet, arow + dr, acol + dc);
   228	                self.targets.insert(cell, anchor);
   229	            }
   230	        }
   231	        Ok(())
   232	    }
   233	
   234	    /// Remove a spill anchor + all its targets. Returns the cleared
   235	    /// shape so callers (e.g. `Workbook::clear_spill_at`) can iterate
   236	    /// the rectangle for overlay-clearing without re-querying.
   237	    pub fn unregister(
   238	        &mut self,
   239	        anchor: (SheetId, RowId, ColId),
   240	    ) -> Result<SpillShape, SpillNotFoundError> {
   241	        let shape = self.anchors.remove(&anchor).ok_or(SpillNotFoundError {
   242	            sheet: anchor.0,
   243	            row: anchor.1,
   244	            col: anchor.2,
   245	        })?;
   246	        let (asheet, arow, acol) = anchor;
   247	        for dr in 0..shape.rows {
   248	            for dc in 0..shape.cols {
   249	                self.targets.remove(&(asheet, arow + dr, acol + dc));
   250	            }
   251	        }
   252	        Ok(shape)
   253	    }
   254	
   255	    /// Iterator over `(anchor_cell, shape)` for every registered spill.
   256	    /// Order is HashMap-iteration order — unspecified and may change
   257	    /// across mutations. Callers that need a stable order must collect
   258	    /// + sort.
   259	    pub fn iter_anchors(
   260	        &self,
   261	    ) -> impl Iterator<Item = ((SheetId, RowId, ColId), &SpillShape)> + '_ {
   262	        self.anchors.iter().map(|(k, v)| (*k, v))
   263	    }
   264	
   265	    /// Total anchor count (NOT total target-cell count).
   266	    pub fn len(&self) -> usize {
   267	        self.anchors.len()
   268	    }
   269	
   270	    pub fn is_empty(&self) -> bool {
   271	        self.anchors.is_empty()
   272	    }
   273	}
   274	
   275	#[cfg(test)]
   276	mod tests {
   277	    use super::*;
   278	
   279	    fn at(s: SheetId, r: RowId, c: ColId) -> (SheetId, RowId, ColId) {
   280	        (s, r, c)
   281	    }
   282	
   283	    #[test]
   284	    fn empty_table_lookups_return_none() {
   285	        let t = SpillAnchorTable::new();
   286	        assert!(t.anchor_at(0, 0, 0).is_none());
   287	        assert!(t.target_anchor(0, 0, 0).is_none());
   288	        assert!(t.is_empty());
   289	        assert_eq!(t.len(), 0);
   290	    }
   291	
   292	    #[test]
   293	    fn register_1x1_spill() {
   294	        let mut t = SpillAnchorTable::new();
   295	        t.register(at(0, 5, 3), SpillShape::new(1, 1)).unwrap();
   296	        assert_eq!(t.anchor_at(0, 5, 3), Some(&SpillShape::new(1, 1)));
   297	        // Anchor IS its own target.
   298	        assert_eq!(t.target_anchor(0, 5, 3), Some(at(0, 5, 3)));
   299	        assert_eq!(t.len(), 1);
   300	    }
   301	
   302	    #[test]
   303	    fn register_5x1_spill_populates_targets() {
   304	        // `=SEQUENCE(5)` at A1 spills A1..A5.
   305	        let mut t = SpillAnchorTable::new();
   306	        let anchor = at(0, 0, 0);
   307	        t.register(anchor, SpillShape::new(5, 1)).unwrap();
   308	        for r in 0..5 {
   309	            assert_eq!(t.target_anchor(0, r, 0), Some(anchor), "row {r}");
   310	        }
   311	        // Outside the rectangle: no entries.
   312	        assert!(t.target_anchor(0, 5, 0).is_none());
   313	        assert!(t.target_anchor(0, 0, 1).is_none());
   314	    }
   315	
   316	    #[test]
   317	    fn register_2x3_spill_populates_all_cells() {
   318	        // 2×3 rectangle at B2 covers B2:D3.
   319	        let mut t = SpillAnchorTable::new();
   320	        let anchor = at(0, 1, 1);
   321	        t.register(anchor, SpillShape::new(2, 3)).unwrap();
   322	        for dr in 0..2 {
   323	            for dc in 0..3 {
   324	                assert_eq!(
   325	                    t.target_anchor(0, 1 + dr, 1 + dc),
   326	                    Some(anchor),
   327	                    "cell ({},{})",
   328	                    1 + dr,
   329	                    1 + dc
   330	                );
   331	            }
   332	        }
   333	        assert_eq!(t.len(), 1);
   334	    }
   335	
   336	    #[test]
   337	    fn register_rejects_anchor_already_exists() {
   338	        let mut t = SpillAnchorTable::new();
   339	        let anchor = at(0, 0, 0);
   340	        t.register(anchor, SpillShape::new(3, 1)).unwrap();
   341	        let err = t.register(anchor, SpillShape::new(3, 1)).unwrap_err();
   342	        assert!(matches!(err, SpillBlockError::AnchorAlreadyExists { .. }));
   343	    }
   344	
   345	    #[test]
   346	    fn register_rejects_target_collision_atomically() {
   347	        // First spill at A1: 3x1. Second spill at A2: 3x1 — would
   348	        // overlap A2, A3 with the first spill. Reject; no partial
   349	        // insert.
   350	        let mut t = SpillAnchorTable::new();
   351	        t.register(at(0, 0, 0), SpillShape::new(3, 1)).unwrap();
   352	        let len_before = t.len();
   353	        let target_count_before = t.targets.len();
   354	        let err = t.register(at(0, 1, 0), SpillShape::new(3, 1)).unwrap_err();
   355	        match err {
   356	            SpillBlockError::TargetCellOccupied {
   357	                sheet,
   358	                row,
   359	                col,
   360	                anchor_sheet,
   361	                anchor_row,
   362	                anchor_col,
   363	            } => {
   364	                // First collision encountered in iteration order: A2.
   365	                assert_eq!((sheet, row, col), (0, 1, 0));
   366	                assert_eq!((anchor_sheet, anchor_row, anchor_col), (0, 0, 0));
   367	            }
   368	            other => panic!("expected TargetCellOccupied, got {other:?}"),
   369	        }
   370	        // Atomicity: failed registration left the table unchanged.
   371	        assert_eq!(t.len(), len_before);
   372	        assert_eq!(t.targets.len(), target_count_before);
   373	        assert!(t.anchor_at(0, 1, 0).is_none());
   374	    }
   375	
   376	    #[test]
   377	    fn register_rejects_degenerate_zero_rows() {
   378	        let mut t = SpillAnchorTable::new();
   379	        let err = t.register(at(0, 0, 0), SpillShape::new(0, 3)).unwrap_err();
   380	        assert!(matches!(
   381	            err,
   382	            SpillBlockError::DegenerateShape { rows: 0, cols: 3 }
   383	        ));
   384	    }
   385	
   386	    #[test]
   387	    fn register_rejects_degenerate_zero_cols() {
   388	        let mut t = SpillAnchorTable::new();
   389	        let err = t.register(at(0, 0, 0), SpillShape::new(3, 0)).unwrap_err();
   390	        assert!(matches!(
   391	            err,
   392	            SpillBlockError::DegenerateShape { rows: 3, cols: 0 }
   393	        ));
   394	    }
   395	
   396	    #[test]
   397	    fn unregister_clears_anchor_and_all_targets() {
   398	        let mut t = SpillAnchorTable::new();
   399	        let anchor = at(0, 0, 0);
   400	        t.register(anchor, SpillShape::new(2, 2)).unwrap();
   401	        let shape = t.unregister(anchor).unwrap();
   402	        assert_eq!(shape, SpillShape::new(2, 2));
   403	        // Anchor gone.
   404	        assert!(t.anchor_at(0, 0, 0).is_none());
   405	        // All four target cells gone.
   406	        for dr in 0..2 {
   407	            for dc in 0..2 {
   408	                assert!(
   409	                    t.target_anchor(0, dr, dc).is_none(),
   410	                    "cell ({dr},{dc}) still in targets"
   411	                );
   412	            }
   413	        }
   414	        assert!(t.is_empty());
   415	    }
   416	
   417	    #[test]
   418	    fn unregister_unknown_anchor_errors() {
   419	        let mut t = SpillAnchorTable::new();
   420	        let err = t.unregister(at(0, 5, 5)).unwrap_err();
   421	        assert_eq!(
   422	            err,
   423	            SpillNotFoundError {
   424	                sheet: 0,
   425	                row: 5,
   426	                col: 5
   427	            }
   428	        );
   429	    }
   430	
   431	    #[test]
   432	    fn register_unregister_register_works() {
   433	        // Re-registering after unregister is the expected re-eval
   434	        // pattern per design § 8.3.
   435	        let mut t = SpillAnchorTable::new();
   436	        let anchor = at(0, 0, 0);
   437	        t.register(anchor, SpillShape::new(3, 1)).unwrap();
   438	        t.unregister(anchor).unwrap();
   439	        t.register(anchor, SpillShape::new(5, 1)).unwrap();
   440	        assert_eq!(t.anchor_at(0, 0, 0), Some(&SpillShape::new(5, 1)));
   441	        // New shape's targets are populated.
   442	        assert_eq!(t.target_anchor(0, 4, 0), Some(anchor));
   443	    }
   444	
   445	    #[test]
   446	    fn two_disjoint_spills_coexist() {
   447	        // Spill A: 2x2 at (0,0). Spill B: 2x2 at (5,5).
   448	        let mut t = SpillAnchorTable::new();
   449	        let a = at(0, 0, 0);
   450	        let b = at(0, 5, 5);
   451	        t.register(a, SpillShape::new(2, 2)).unwrap();
   452	        t.register(b, SpillShape::new(2, 2)).unwrap();
   453	        assert_eq!(t.len(), 2);
   454	        // Each cell maps to the right anchor.
   455	        assert_eq!(t.target_anchor(0, 1, 1), Some(a));
   456	        assert_eq!(t.target_anchor(0, 6, 6), Some(b));
   457	    }
   458	
   459	    #[test]
   460	    fn spills_on_different_sheets_are_independent() {
   461	        let mut t = SpillAnchorTable::new();
   462	        let a = at(0, 2, 2);
   463	        let b = at(1, 2, 2);
   464	        t.register(a, SpillShape::new(3, 3)).unwrap();
   465	        t.register(b, SpillShape::new(3, 3)).unwrap();
   466	        assert_eq!(t.target_anchor(0, 2, 2), Some(a));
   467	        assert_eq!(t.target_anchor(1, 2, 2), Some(b));
   468	        // Cross-sheet doesn't bleed.
   469	        assert!(t.target_anchor(0, 2, 5).is_none());
   470	        assert!(t.target_anchor(1, 2, 5).is_none());
   471	    }
   472	
   473	    #[test]
   474	    fn iter_anchors_yields_all_registered() {
   475	        let mut t = SpillAnchorTable::new();
   476	        t.register(at(0, 0, 0), SpillShape::new(1, 1)).unwrap();
   477	        t.register(at(0, 5, 5), SpillShape::new(2, 2)).unwrap();
   478	        let anchors: Vec<_> = t.iter_anchors().collect();
   479	        assert_eq!(anchors.len(), 2);
   480	        // Iteration order is unspecified; collect + assert membership.
   481	        let cells: std::collections::HashSet<_> = anchors.iter().map(|(c, _)| *c).collect();
   482	        assert!(cells.contains(&at(0, 0, 0)));
   483	        assert!(cells.contains(&at(0, 5, 5)));
   484	    }
   485	
   486	    #[test]
   487	    fn spill_shape_cell_count() {
   488	        assert_eq!(SpillShape::new(5, 1).cell_count(), 5);
   489	        assert_eq!(SpillShape::new(3, 4).cell_count(), 12);
   490	        assert_eq!(SpillShape::new(1, 1).cell_count(), 1);
   491	    }
   492	}

exec
/bin/zsh -lc "nl -ba crates/ql-storage/src/workbook.rs | sed -n '1,360p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
     1	//! `Workbook` — top-level container; holds the sheet list + the name table.
     2	//!
     3	//! Per spec Part V §4 Week 2 Days 3-4:
     4	//! - `sheets: Vec<Sheet>` indexed by `SheetId` (u16).
     5	//! - `names: NameTable` — workbook-level defined names. Phase 2A.1 (2026-05-12)
     6	//!   wired the real HashMap-backed storage and `Workbook::set_name` API; the
     7	//!   binder in `ql-exec` resolves names against this table at bind time.
     8	//!   Sheet-scope names land Phase 3+ with `ql-formula-semantics`.
     9	
    10	use std::collections::HashMap;
    11	use std::sync::Arc;
    12	
    13	use ql_types::{Address, ColId, ErrorValue, Range, RowId, SheetId, Value};
    14	
    15	use crate::sheet::Sheet;
    16	
    17	/// What a defined name resolves to.
    18	///
    19	/// Per codex r13 N9 + opus arch F5: the prior `lookup -> Option<Address>` couldn't represent
    20	/// the common Excel defined-name targets (range, constant, formula). The shape was a wrong
    21	/// stub that would force a breaking change in Week 3 ql-calcgraph binding. Widening to the
    22	/// enum NOW is non-breaking later — calcgraph callers pattern-match on the variant they need.
    23	///
    24	/// Phase 0 shipped all variants for forward compatibility. Phase 2A.1 (2026-05-12)
    25	/// added real storage for `Cell` and `Constant` variants; the `Range` and `Formula`
    26	/// variants are still resolution-deferred (the binder returns
    27	/// `BindError::UnsupportedVariant` for them — Phase 2B+ aggregate-context wiring
    28	/// closes that gap).
    29	#[derive(Clone, Debug, PartialEq)]
    30	pub enum NamedTarget {
    31	    /// Named single cell (`SalesRow1 = $A$1`).
    32	    Cell(Address),
    33	    /// Named range (`Sales = $A$2:$A$1000`).
    34	    Range(Range),
    35	    /// Named constant (`TaxRate = 0.21`).
    36	    Constant(Value),
    37	    /// Named formula (`Profit = Revenue - Costs`). Stored as the raw formula source; the
    38	    /// binder re-parses + evaluates in the use-site context. Phase 3+ feature.
    39	    Formula(std::sync::Arc<str>),
    40	}
    41	
    42	/// Defined-names table.
    43	///
    44	/// Phase 2A.1 (2026-05-12): real workbook-scope storage. Phase 2A.6 audit fix
    45	/// (H2/M6/D5): canonicalization is enforced at the **insert** boundary — `set`
    46	/// uppercases the key on write, so every entry lives at its canonical
    47	/// upper-case form. `lookup` is case-sensitive on canonical form (fast path for
    48	/// the parser, which already canonicalizes); `lookup_ci` exists as a
    49	/// belt-and-suspenders entry point for callers that bypass the parser
    50	/// canonicalization. Sheet-scope names (Excel's `Sheet1!Local`) are deferred to
    51	/// Phase 3+ ql-formula-semantics.
    52	///
    53	/// ## Generation counter (Phase 2B.3)
    54	///
    55	/// `generation` is bumped on every successful `set` / `clear`. Bind-plan
    56	/// caches in `ql-exec` key cached plans by `(formula_text, sheet,
    57	/// generation)` so a name registration / rename invalidates affected plans
    58	/// without an explicit cache-flush call — the next lookup just misses.
    59	#[derive(Clone, Debug, Default)]
    60	pub struct NameTable {
    61	    entries: HashMap<Arc<str>, NamedTarget>,
    62	    /// Monotonic counter bumped on every successful mutation. See struct
    63	    /// docs for the cache-invalidation contract. Wraps at u64::MAX (4×10^18
    64	    /// mutations — not a realistic concern).
    65	    generation: u64,
    66	}
    67	
    68	/// Errors emitted by `NameTable::set` when a registration is refused.
    69	///
    70	/// Phase 2A.9 audit M6 (2026-05-12): closes the AI() reservation hole. The
    71	/// previous `NameTable::set` returned `()` and silently accepted any name,
    72	/// including ones reserved by the engine. A user calling
    73	/// `wb.set_name("AI", NamedTarget::Constant(...))` then writing `=AI` bypassed
    74	/// the CORR-06 AI sentinel because `=AI` (no parens) goes through the binder's
    75	/// NameRef path instead of the function-call path. Now the registration
    76	/// itself is refused.
    77	#[derive(Clone, Debug, PartialEq, Eq, thiserror::Error)]
    78	pub enum NameTableError {
    79	    /// The name is reserved by the engine. Per CORR-06: `AI`. Future
    80	    /// reservations belong in `is_reserved_name` below.
    81	    #[error("name {0:?} is reserved by the engine and cannot be registered")]
    82	    Reserved(Arc<str>),
    83	}
    84	
    85	/// **Phase 4.6.AA (W5-86):** errors from `Workbook::validate_sheet_name`,
    86	/// used by `add_sheet` / `rename_sheet` (Phase 4.6.C). Distinct from
    87	/// `NameTableError` because the validation rules differ (sheet names
    88	/// allow reserved-name-table entries like `AI`; sheet names reject
    89	/// Excel-reserved characters; etc.).
    90	#[derive(Clone, Debug, PartialEq, Eq, thiserror::Error)]
    91	pub enum SheetNameError {
    92	    /// Empty string — Excel canon rejects.
    93	    #[error("sheet name cannot be empty")]
    94	    Empty,
    95	
    96	    /// Excel-reserved character in the proposed name. Set: `:`, `\`,
    97	    /// `/`, `?`, `*`, `[`, `]`. xlsx round-trip prerequisite (Phase 4.11).
    98	    #[error("sheet name contains Excel-reserved character {0:?}")]
    99	    ReservedCharacter(char),
   100	
   101	    /// Canonical duplicate of an existing sheet name (case-insensitive
   102	    /// comparison per `Workbook::canonical_sheet_name`).
   103	    #[error("sheet name {name:?} is already in use (case-insensitive)")]
   104	    Duplicate { name: String },
   105	}
   106	
   107	/// Phase 2A.9 audit M6: the canonical-uppercase reserved-name list. Names
   108	/// here cannot be registered via `NameTable::set` or `Workbook::set_name`.
   109	/// Keep small; document each entry.
   110	fn is_reserved_name(canonical: &str) -> bool {
   111	    matches!(
   112	        canonical,
   113	        // CORR-06 / T4-D05: AI is the reserved sentinel that dispatches to
   114	        // `Error(AINotAvailable)` until the Phase 4+ AI() function ships.
   115	        "AI"
   116	    )
   117	}
   118	
   119	impl NameTable {
   120	    pub fn new() -> Self {
   121	        Self::default()
   122	    }
   123	
   124	    /// Register a name → target binding. The key is uppercased before insertion
   125	    /// so the table always stores entries in canonical Excel case.
   126	    ///
   127	    /// Phase 2A.6 audit M6/D5 added the on-write canonicalization (was: stored
   128	    /// as-is, creating a disagreement with `lookup` that uppercased queries).
   129	    ///
   130	    /// Phase 2A.9 audit M6 added the reserved-name guard. Names returned `true`
   131	    /// from `is_reserved_name` (currently just `AI` per CORR-06) are refused
   132	    /// with `NameTableError::Reserved`. The return type is now `Result` —
   133	    /// callers that previously expected `()` need to handle the error or
   134	    /// `.expect(...)` it.
   135	    pub fn set(
   136	        &mut self,
   137	        name: impl AsRef<str>,
   138	        target: NamedTarget,
   139	    ) -> Result<(), NameTableError> {
   140	        let upper: Arc<str> = Arc::from(name.as_ref().to_ascii_uppercase().as_str());
   141	        if is_reserved_name(&upper) {
   142	            return Err(NameTableError::Reserved(upper));
   143	        }
   144	        self.entries.insert(upper, target);
   145	        // Phase 2B.3: bump generation so bind-plan caches keyed by the prior
   146	        // generation miss on next lookup. wrapping_add so we never panic on
   147	        // u64::MAX (practically unreachable but defensive).
   148	        self.generation = self.generation.wrapping_add(1);
   149	        Ok(())
   150	    }
   151	
   152	    /// Remove a name binding. Uppercases the query so callers don't have to
   153	    /// match the on-write canonicalization. Idempotent.
   154	    pub fn clear(&mut self, name: &str) {
   155	        let upper = name.to_ascii_uppercase();
   156	        // Only bump generation if a binding was actually removed — idempotent
   157	        // no-op clears don't invalidate any cache.
   158	        if self.entries.remove(upper.as_str()).is_some() {
   159	            self.generation = self.generation.wrapping_add(1);
   160	        }
   161	    }
   162	
   163	    /// Phase 2B.3 (2026-05-12): monotonic generation counter, bumped on every
   164	    /// successful `set` / `clear`. Bind-plan caches in `ql-exec` key cached
   165	    /// plans by `(formula_text, sheet, generation)` so a name mutation
   166	    /// invalidates affected plans on next lookup without an explicit flush.
   167	    pub fn generation(&self) -> u64 {
   168	        self.generation
   169	    }
   170	
   171	    /// Phase 2B.7 audit H3 (2026-05-12): pre-check whether `set(name, _)`
   172	    /// would succeed without doing the mutation. Lets the `ql-exec`
   173	    /// `WorkbookRuntime::set_name` wrapper run validate-then-append-then-
   174	    /// mutate so a reserved-name rejection doesn't leave the workbook
   175	    /// either (a) modified with no op log entry, or (b) un-modified but
   176	    /// with an orphan op log entry.
   177	    ///
   178	    /// Returns `Ok(())` if a future `set(name, _)` will succeed,
   179	    /// `Err(NameTableError::Reserved(...))` otherwise. Pure: no
   180	    /// mutation, no generation bump.
   181	    pub fn would_accept(&self, name: &str) -> Result<(), NameTableError> {
   182	        let upper: Arc<str> = Arc::from(name.to_ascii_uppercase().as_str());
   183	        if is_reserved_name(&upper) {
   184	            return Err(NameTableError::Reserved(upper));
   185	        }
   186	        Ok(())
   187	    }
   188	
   189	    /// Look up a name. Case-sensitive on the canonical-uppercase form — used
   190	    /// by the parser-internal path where the query is already canonicalized.
   191	    /// External callers should prefer `lookup_ci` unless they've already
   192	    /// uppercased the query.
   193	    pub fn lookup(&self, name: &str) -> Option<NamedTarget> {
   194	        self.entries.get(name).cloned()
   195	    }
   196	
   197	    /// Case-insensitive lookup: uppercases the query before searching.
   198	    /// Phase 2A.6 audit H2: the `NameLookup for NameTable` impl uses this so
   199	    /// the binder is robust against future callers who forget to canonicalize.
   200	    pub fn lookup_ci(&self, name: &str) -> Option<NamedTarget> {
   201	        let upper = name.to_ascii_uppercase();
   202	        self.entries.get(upper.as_str()).cloned()
   203	    }
   204	
   205	    pub fn len(&self) -> usize {
   206	        self.entries.len()
   207	    }
   208	
   209	    pub fn is_empty(&self) -> bool {
   210	        self.entries.is_empty()
   211	    }
   212	
   213	    /// Iterate all `(name, target)` bindings. Order is HashMap-arbitrary.
   214	    pub fn iter(&self) -> impl Iterator<Item = (&Arc<str>, &NamedTarget)> + '_ {
   215	        self.entries.iter()
   216	    }
   217	}
   218	
   219	/// Top-level Quantbook container.
   220	///
   221	/// Phase 1 W5-9 adds `formula_cells`: a HashMap keyed by `(sheet, row, col)` mapping
   222	/// formula-bearing cells to their formula source text (e.g. `"A1 + B1"`). The cell's
   223	/// EVALUATED result still lives in the Sheet's columnar storage as a Value; this map
   224	/// records that the value was *produced by a formula* (so it can be recomputed and so
   225	/// the save path persists the formula). Cells without an entry here are literals.
   226	///
   227	/// This is the minimum Phase 1 formula-persistence model. Phase 4+ adds:
   228	/// - Calcgraph dependency tracking (computed-overlay separation per CORR-25).
   229	/// - Automatic recompute on dependency change.
   230	/// - Cross-sheet formula references.
   231	///
   232	/// ## Clone semantics
   233	///
   234	/// `Clone` is **shallow with respect to Arrow chunk buffers**: each
   235	/// `ColumnStore` holds `Vec<Arc<dyn arrow_array::Array>>`, and cloning the
   236	/// Workbook clones the Arc handles (cheap refcount bump), NOT the underlying
   237	/// Arrow buffers. This is safe today because the only mutating APIs
   238	/// (`Workbook::put`, `Sheet::put`, `ColumnStore::put`, `replace_chunk`,
   239	/// `append_chunk`) operate at the chunk granularity — they install whole new
   240	/// `ArrayRef` values, never mutate an existing Arrow buffer in place. So a
   241	/// clone and the original never see each other's writes through their shared
   242	/// chunk Arcs.
   243	///
   244	/// Phase 2A.12 audit H4 (persistence-agent finding): if a future API ever
   245	/// mutates a chunk's bytes in place (e.g. `Float64Array::values_mut` via
   246	/// `Arc::make_mut`), it MUST also break the Arc-sharing contract — either
   247	/// clone the buffer first or document the in-place mutation. The
   248	/// chunk-replace-only invariant is what makes shallow Clone correct.
   249	#[derive(Clone, Debug, Default)]
   250	pub struct Workbook {
   251	    sheets: Vec<Sheet>,
   252	    names: NameTable,
   253	    formula_cells: HashMap<(SheetId, RowId, ColId), Arc<str>>,
   254	    /// **W5-71 (Phase 4.5.A.2):** Excel date system for this workbook.
   255	    /// Default `Excel1900` (Windows Excel canon). The `.qbook` envelope
   256	    /// persists this; xlsx I/O (Phase 4.11) maps it from the workbook's
   257	    /// `date1904` SST flag. Functions read this via
   258	    /// `WorkbookEnv::eval_context()` to drive serial ↔ date conversion.
   259	    date_system: ql_types::DateSystem,
   260	    /// **W5-79 (Phase 4.5.D part 3):** workbook-level format-string
   261	    /// interning table. Cells with non-General formats reference an id
   262	    /// into this table via per-sheet [`crate::CellFormatOverlay`].
   263	    /// Default loads the Excel built-ins from
   264	    /// `docs/architecture/2026-05-13-format-string-grammar.md` § 10
   265	    /// (parser-relevant subset).
   266	    formats: crate::FormatTable,
   267	    /// **W5-101 (Phase 4.7.H):** spill-anchor table. Workbook-level
   268	    /// because spills can theoretically span sheets (rare in Excel; v1
   269	    /// keeps single-sheet but the table doesn't assume). Map-only by
   270	    /// design; mutation goes through `register_spill` / `clear_spill_at`
   271	    /// — the latter is the workbook-layer convenience that also clears
   272	    /// computed overlays at every target cell. See
   273	    /// `crates/ql-storage/src/spill.rs` module docs.
   274	    spill_anchors: crate::SpillAnchorTable,
   275	}
   276	
   277	impl Workbook {
   278	    pub fn new() -> Self {
   279	        Self::default()
   280	    }
   281	
   282	    /// **W5-71 (Phase 4.5.A.2):** the workbook's date system.
   283	    pub fn date_system(&self) -> ql_types::DateSystem {
   284	        self.date_system
   285	    }
   286	
   287	    /// **W5-71 (Phase 4.5.A.2):** set the workbook's date system.
   288	    /// Used by the `.qbook` loader on file load (defaults to `Excel1900`
   289	    /// when missing from the envelope) and by xlsx import in Phase 4.11.
   290	    /// Switching post-load shifts every serial by 1462 days — not
   291	    /// recommended as a runtime operation.
   292	    ///
   293	    /// **W5-76 mega-audit caveat:** `WorkbookEnv` (`ql-exec`) caches the
   294	    /// `EvalContext` (and therefore the `DateSystem`) at construction.
   295	    /// Mutating the workbook's date system AFTER a `WorkbookEnv` has been
   296	    /// constructed will produce a stale eval context until a fresh
   297	    /// `WorkbookEnv` is created. Today every recompute path constructs
   298	    /// a fresh `WorkbookEnv`, so normal evaluation is safe; callers that
   299	    /// hold a long-lived `WorkbookEnv` across `set_date_system` must
   300	    /// rebuild it. Treat this method as loader-only.
   301	    pub fn set_date_system(&mut self, system: ql_types::DateSystem) {
   302	        self.date_system = system;
   303	    }
   304	
   305	    /// **W5-79 (Phase 4.5.D part 3):** read access to the workbook's
   306	    /// format-string interning table.
   307	    pub fn formats(&self) -> &crate::FormatTable {
   308	        &self.formats
   309	    }
   310	
   311	    /// **W5-79 (Phase 4.5.D part 3):** mutable access for `intern` /
   312	    /// `register_at`. Phase 4.5.D part 4 (W5-80) will route mutations
   313	    /// through `WorkbookRuntime::register_format` so they land in the
   314	    /// op log; direct access stays available for the loader + tests.
   315	    pub fn formats_mut(&mut self) -> &mut crate::FormatTable {
   316	        &mut self.formats
   317	    }
   318	
   319	    pub fn sheet_count(&self) -> usize {
   320	        self.sheets.len()
   321	    }
   322	
   323	    // ===== Phase 4.6.AA (W5-86): sheet registry + canonicalizer =====
   324	
   325	    /// **Phase 4.6.AA (W5-86):** canonicalize a sheet name for
   326	    /// case-insensitive comparison. ASCII uppercase per the parent
   327	    /// design doc § 7.3 (consistency with `NameTable::set`'s rule;
   328	    /// Unicode casefolding deferred to a follow-up gap).
   329	    ///
   330	    /// Excel canonical: sheet names compare case-insensitively (so
   331	    /// `"Sheet1"` and `"SHEET1"` are the same sheet) but the DISPLAY
   332	    /// form preserves user-supplied case. This function returns the
   333	    /// canonical form used for lookup / duplicate-detection ONLY;
   334	    /// display goes through `Sheet::name()`.
   335	    ///
   336	    /// V1 simplification: ASCII-only uppercase. `"Café"` and `"café"`
   337	    /// canonicalize to different forms (the non-ASCII `é` is not
   338	    /// folded). Excel itself uses Unicode casefolding here; if a real
   339	    /// workbook with non-ASCII sheet names surfaces a collision, this
   340	    /// gets revisited.
   341	    pub fn canonical_sheet_name(name: &str) -> String {
   342	        name.to_ascii_uppercase()
   343	    }
   344	
   345	    /// **Phase 4.6.AA (W5-86):** find a sheet by name (case-insensitive
   346	    /// per `canonical_sheet_name`). Returns the first match in id order.
   347	    ///
   348	    /// Lookup cost: O(sheet_count). Acceptable for typical workbooks
   349	    /// (low double-digits of sheets). If profiling shows pain, a
   350	    /// canonical-name → id index can be added without changing this
   351	    /// API surface.
   352	    pub fn sheet_id_by_name(&self, name: &str) -> Option<SheetId> {
   353	        let canonical = Self::canonical_sheet_name(name);
   354	        for (idx, sheet) in self.sheets.iter().enumerate() {
   355	            if Self::canonical_sheet_name(sheet.name()) == canonical {
   356	                return Some(idx as SheetId);
   357	            }
   358	        }
   359	        None
   360	    }

exec
/bin/zsh -lc "nl -ba crates/ql-storage/src/lib.rs | sed -n '1,180p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
     1	//! `ql-storage` — Workbook, Sheet, ColumnStore, SparseOverlay.
     2	//!
     3	//! Per spec Part V §4 Week 2 Days 3-4. Phase 0 minimum, with Phase 2A
     4	//! extensions to `Workbook`:
     5	//! - [`workbook`] — top-level `Workbook` + `NameTable` (real HashMap-backed
     6	//!   storage since Phase 2A.1; canonicalizing-on-write since Phase 2A.6).
     7	//! - [`sheet`] — per-sheet `Sheet` + conservative `Bounds` tracking.
     8	//! - [`column`] — `ColumnStore`: chunked Arrow base + per-chunk sparse overlay; default
     9	//!   chunk size 16,384 rows (`QBOOK_CHUNK_ROWS` env override).
    10	//! - [`overlay`] — `SparseOverlay`: per-chunk `BTreeMap<RowId, Value>`; overlay-first reads.
    11	//!
    12	//! Design locks (from spec): chunk-replace, not cell-mutate. Result columns from recompute
    13	//! swap whole chunks via `ColumnStore::replace_chunk`; cell-level writes go through the
    14	//! overlay (`ColumnStore::put`) and are never applied to the base array directly.
    15	
    16	pub mod column;
    17	pub mod format;
    18	pub mod format_overlay;
    19	pub mod overlay;
    20	pub mod sheet;
    21	pub mod spill;
    22	pub mod workbook;
    23	
    24	pub use column::{chunk_rows_from_env, ColumnStore, DEFAULT_CHUNK_ROWS};
    25	pub use format::{FormatId, FormatTable, FormatTableError, FIRST_CUSTOM_FORMAT_ID};
    26	pub use format_overlay::CellFormatOverlay;
    27	pub use overlay::SparseOverlay;
    28	pub use sheet::{Bounds, Sheet};
    29	pub use spill::{SpillAnchorTable, SpillBlockError, SpillNotFoundError, SpillShape};
    30	pub use workbook::{NameTable, NameTableError, NamedTarget, SheetNameError, Workbook};
    31	
    32	#[cfg(test)]
    33	mod tests {
    34	    use super::*;
    35	    use ql_types::{Address, Value};
    36	
    37	    #[test]
    38	    fn end_to_end_minimum_workbook_read_write() {
    39	        // The smallest E2E that touches every module: workbook → sheet → column → overlay.
    40	        let mut wb = Workbook::new();
    41	        let s = wb.add_sheet_with_chunk_rows("Sheet1", 16);
    42	        wb.put(Address::new(s, 0, 0), Value::Number(42.0));
    43	        assert_eq!(wb.read(Address::new(s, 0, 0)), Value::Number(42.0));
    44	        // Confirms re-exports compile + integration is wired.
    45	        let _: SparseOverlay = SparseOverlay::new();
    46	        let _: ColumnStore = ColumnStore::new();
    47	        let _: Sheet = Sheet::new("t");
    48	        let _: NameTable = NameTable::new();
    49	        let _: Bounds = Bounds::default();
    50	    }
    51	
    52	    // ===== W5-79 / Phase 4.5.D part 3 — FormatTable + overlay integration =====
    53	
    54	    #[test]
    55	    fn workbook_default_has_format_table_with_builtins() {
    56	        let wb = Workbook::new();
    57	        assert_eq!(wb.formats().lookup(FormatId::GENERAL), Some("General"));
    58	        assert_eq!(wb.formats().lookup(FormatId(14)), Some("m/d/yyyy"));
    59	    }
    60	
    61	    #[test]
    62	    fn workbook_intern_returns_existing_builtin_id() {
    63	        let mut wb = Workbook::new();
    64	        let id = wb.formats_mut().intern("0.00");
    65	        assert_eq!(id, FormatId(2));
    66	    }
    67	
    68	    #[test]
    69	    fn workbook_intern_new_string_allocates_custom_id() {
    70	        let mut wb = Workbook::new();
    71	        let id = wb.formats_mut().intern("\"⚓\" #,##0");
    72	        assert_eq!(id.0, FIRST_CUSTOM_FORMAT_ID);
    73	    }
    74	
    75	    #[test]
    76	    fn sheet_format_overlay_starts_empty() {
    77	        let s = Sheet::new("Sheet1");
    78	        assert!(s.format_overlay().is_empty());
    79	    }
    80	
    81	    #[test]
    82	    fn sheet_format_overlay_set_and_clear_round_trip() {
    83	        let mut s = Sheet::new("Sheet1");
    84	        s.format_overlay_mut().set(3, 5, FormatId(14));
    85	        assert_eq!(s.format_overlay().get(3, 5), Some(FormatId(14)));
    86	        s.format_overlay_mut().clear(3, 5);
    87	        assert!(s.format_overlay().get(3, 5).is_none());
    88	    }
    89	
    90	    #[test]
    91	    fn workbook_independent_sheets_have_independent_overlays() {
    92	        let mut wb = Workbook::new();
    93	        let s0 = wb.add_sheet_with_chunk_rows("A", 16);
    94	        let s1 = wb.add_sheet_with_chunk_rows("B", 16);
    95	        // Compose: set a format on A:(0,0) via the table + overlay APIs.
    96	        let id = wb.formats_mut().intern("yyyy-mm-dd");
    97	        wb.sheet_mut(s0).unwrap().format_overlay_mut().set(0, 0, id);
    98	        // Sheet B should not see the entry.
    99	        assert_eq!(wb.sheet(s0).unwrap().format_overlay().get(0, 0), Some(id));
   100	        assert_eq!(wb.sheet(s1).unwrap().format_overlay().get(0, 0), None);
   101	        // FormatTable lookup resolves back to the source string.
   102	        assert_eq!(wb.formats().lookup(id), Some("yyyy-mm-dd"));
   103	    }
   104	}

exec
/bin/zsh -lc 'rg -n "MAX_ROW|MAX_COLUMN|RowId|ColId|type Row|type Col|struct Row" crates/ql-*' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
crates/ql-exec/src/lower.rs:47:use ql_types::ColId;
crates/ql-exec/src/lower.rs:53:    MulScalar { input_col: ColId, scalar: f64 },
crates/ql-exec/src/lower.rs:55:    AddScalar { input_col: ColId, scalar: f64 },
crates/ql-exec/src/lower.rs:57:    SubScalar { input_col: ColId, scalar: f64 },
crates/ql-exec/src/lower.rs:59:    ScalarSub { scalar: f64, input_col: ColId },
crates/ql-exec/src/lower.rs:65:    MulArray { lhs_col: ColId, rhs_col: ColId },
crates/ql-exec/src/lower.rs:67:    AddArray { lhs_col: ColId, rhs_col: ColId },
crates/ql-exec/src/lower.rs:69:    SubArray { lhs_col: ColId, rhs_col: ColId },
crates/ql-exec/src/lower.rs:204:    fn cellref(col: ColId) -> ExprPlan {
crates/ql-formula-syntax/src/lexer.rs:36:    /// constructor enforces this even though our `ColId = u32` could technically hold more.
crates/ql-formula-syntax/src/lexer.rs:67:pub const MAX_COLUMN: u32 = 16_383;
crates/ql-formula-syntax/src/lexer.rs:69:pub const MAX_ROW: u32 = 1_048_575;
crates/ql-formula-syntax/src/lexer.rs:442:        if row > MAX_ROW {
crates/ql-formula-syntax/src/lexer.rs:550:    if zero_based > MAX_COLUMN {
crates/ql-formula-syntax/src/lexer.rs:861:        assert_eq!(column_letters_to_index("XFD").unwrap(), MAX_COLUMN);
crates/ql-formula-syntax/src/lexer.rs:868:        assert_eq!(column_letters_to_index("xfd").unwrap(), MAX_COLUMN);
crates/ql-formula-syntax/src/lexer.rs:926:            MAX_COLUMN,
crates/ql-formula-syntax/src/lexer.rs:927:            MAX_ROW,
crates/ql-calcgraph/src/stripes.rs:37:use ql_types::{ColId, RowId, SheetId};
crates/ql-calcgraph/src/stripes.rs:50:/// a `RowId`; for `StripeType::Column`, a `ColId`. Stored as `u32` since both type-alias
crates/ql-calcgraph/src/stripes.rs:267:    pub fn candidates_for_cell(&self, sheet: SheetId, row: RowId, col: ColId) -> HashSet<NodeId> {
crates/ql-calcgraph/src/stripes.rs:295:pub fn range_contains_rowcol(range: &RangeRef, row: RowId, col: ColId) -> bool {
crates/ql-formula-syntax/src/ast.rs:19:use ql_types::{ColId, RowId, SheetId};
crates/ql-formula-syntax/src/ast.rs:292:    pub col: ColId,
crates/ql-formula-syntax/src/ast.rs:293:    pub row: RowId,
crates/ql-formula-syntax/src/ast.rs:319:        start_col: ColId,
crates/ql-formula-syntax/src/ast.rs:320:        start_row: RowId,
crates/ql-formula-syntax/src/ast.rs:321:        end_col: ColId,
crates/ql-formula-syntax/src/ast.rs:322:        end_row: RowId,
crates/ql-formula-syntax/src/ast.rs:331:        start_col: ColId,
crates/ql-formula-syntax/src/ast.rs:332:        end_col: ColId,
crates/ql-formula-syntax/src/ast.rs:339:        start_row: RowId,
crates/ql-formula-syntax/src/ast.rs:340:        end_row: RowId,
crates/ql-exec/src/calcgraph_session.rs:13://! - O(1) `cell_index: HashMap<(SheetId, RowId, ColId), NodeId>` for
crates/ql-exec/src/calcgraph_session.rs:82:use ql_types::{ColId, Range, RowId, SheetId};
crates/ql-exec/src/calcgraph_session.rs:94:/// - `start_row == 0 && end_row == RowId::MAX` → `WholeColumn` (one
crates/ql-exec/src/calcgraph_session.rs:96:/// - `start_col == 0 && end_col == ColId::MAX` → `WholeRow` (one
crates/ql-exec/src/calcgraph_session.rs:104:    let whole_rows = r.start_row == 0 && r.end_row == RowId::MAX;
crates/ql-exec/src/calcgraph_session.rs:105:    let whole_cols = r.start_col == 0 && r.end_col == ColId::MAX;
crates/ql-exec/src/calcgraph_session.rs:177:    pub cells: Vec<(SheetId, RowId, ColId)>,
crates/ql-exec/src/calcgraph_session.rs:261:    cell_index: HashMap<(SheetId, RowId, ColId), NodeId>,
crates/ql-exec/src/calcgraph_session.rs:286:    cell_to_formulas: HashMap<(SheetId, RowId, ColId), HashSet<NodeId>>,
crates/ql-exec/src/calcgraph_session.rs:344:    pub row: RowId,
crates/ql-exec/src/calcgraph_session.rs:345:    pub col: ColId,
crates/ql-exec/src/calcgraph_session.rs:374:    pub fn cell_node_for(&self, sheet: SheetId, row: RowId, col: ColId) -> Option<NodeId> {
crates/ql-exec/src/calcgraph_session.rs:381:    fn or_insert_cell_node(&mut self, sheet: SheetId, row: RowId, col: ColId) -> NodeId {
crates/ql-exec/src/calcgraph_session.rs:475:        let mut seen_cells: HashSet<(SheetId, RowId, ColId)> = HashSet::new();
crates/ql-exec/src/calcgraph_session.rs:592:        let mut formulas: Vec<(SheetId, RowId, ColId, Arc<str>)> = wb
crates/ql-exec/src/calcgraph_session.rs:687:    pub fn on_set_value(&mut self, sheet: SheetId, row: RowId, col: ColId) {
crates/ql-exec/src/calcgraph_session.rs:703:    pub fn on_set_formula(&mut self, sheet: SheetId, row: RowId, col: ColId, plan: &ExprPlan) {
crates/ql-exec/src/calcgraph_session.rs:748:    pub fn on_clear_formula(&mut self, sheet: SheetId, row: RowId, col: ColId) {
crates/ql-exec/src/calcgraph_session.rs:828:    fn mark_dirty_from_cell_write(&mut self, sheet: SheetId, row: RowId, col: ColId) {
crates/ql-exec/src/calcgraph_session.rs:962:    pub fn cell_address_for(&self, node: NodeId) -> Option<(SheetId, RowId, ColId)> {
crates/ql-exec/src/calcgraph_session.rs:1026:        let dirty_addrs: Vec<(NodeId, SheetId, RowId, ColId)> = dirty_vec
crates/ql-exec/src/calcgraph_session.rs:1068:    fn workbook_with_formulas(formulas: &[(SheetId, RowId, ColId, &str)]) -> Workbook {
crates/ql-exec/src/calcgraph_session.rs:1135:            let mut ids: Vec<((SheetId, RowId, ColId), NodeId)> =
crates/ql-exec/src/calcgraph_session.rs:1485:        let by_addr: HashMap<(SheetId, RowId, ColId), &RebuildFailure> = r
crates/ql-exec/src/calcgraph_session.rs:1621:        range_start_row: RowId,
crates/ql-exec/src/calcgraph_session.rs:1622:        range_end_row: RowId,
crates/ql-exec/src/calcgraph_session.rs:1623:        formula_cell: (RowId, ColId),
crates/ql-exec/src/calcgraph_session.rs:1716:                end_row: RowId::MAX,
crates/ql-exec/src/calcgraph_session.rs:1730:                end_col: ColId::MAX,
crates/ql-exec/src/calcgraph_session.rs:1822:                end_row: RowId::MAX,
crates/ql-exec/src/calcgraph_session.rs:2153:            end_row: ql_types::RowId::MAX,
crates/ql-exec/src/calcgraph_session.rs:2177:            end_row: ql_types::RowId::MAX,
crates/ql-exec/src/calcgraph_session.rs:2520:            end_row: ql_types::RowId::MAX,
crates/ql-calcgraph/src/node.rs:17:use ql_types::{ColId, RowId, SheetId};
crates/ql-calcgraph/src/node.rs:20:/// than any real workbook (`Excel max = MAX_ROW * MAX_COLUMN * sheet_count ≈ 1.7×10^10`,
crates/ql-calcgraph/src/node.rs:57:    pub row: RowId,
crates/ql-calcgraph/src/node.rs:58:    pub col: ColId,
crates/ql-calcgraph/src/node.rs:92:    pub target_start_row: RowId,
crates/ql-calcgraph/src/node.rs:93:    pub target_start_col: ColId,
crates/ql-calcgraph/src/node.rs:94:    pub target_end_row: RowId,
crates/ql-calcgraph/src/node.rs:95:    pub target_end_col: ColId,
crates/ql-calcgraph/src/node.rs:109:    pub shape_rows: RowId,
crates/ql-calcgraph/src/node.rs:110:    pub shape_cols: ColId,
crates/ql-exec/src/aggregate_cache.rs:43:use ql_types::{ColId, Range, RowId, SheetId, Value};
crates/ql-exec/src/aggregate_cache.rs:117:    pub fn invalidate_at(&mut self, sheet: SheetId, row: RowId, col: ColId) {
crates/ql-exec/src/aggregate_cache.rs:170:    fn mk_range(sheet: SheetId, sr: RowId, sc: ColId, er: RowId, ec: ColId) -> Range {
crates/ql-io/src/qbook_format.rs:77:use ql_types::{Address, ColId, ErrorValue, Range, RowId, SheetId, Value, MAX_COLUMN, MAX_ROW};
crates/ql-io/src/qbook_format.rs:210:        row: RowId,
crates/ql-io/src/qbook_format.rs:211:        col: ColId,
crates/ql-io/src/qbook_format.rs:532:                if *row > MAX_ROW {
crates/ql-io/src/qbook_format.rs:535:                        reason: format!("Cell.row {row} exceeds MAX_ROW {MAX_ROW}"),
crates/ql-io/src/qbook_format.rs:538:                if *col > MAX_COLUMN {
crates/ql-io/src/qbook_format.rs:541:                        reason: format!("Cell.col {col} exceeds MAX_COLUMN {MAX_COLUMN}"),
crates/ql-io/src/qbook_format.rs:554:                    ("start_row", *start_row, MAX_ROW),
crates/ql-io/src/qbook_format.rs:555:                    ("start_col", *start_col, MAX_COLUMN),
crates/ql-io/src/qbook_format.rs:556:                    ("end_row", *end_row, MAX_ROW),
crates/ql-io/src/qbook_format.rs:557:                    ("end_col", *end_col, MAX_COLUMN),
crates/ql-io/src/qbook_format.rs:1104:        let mut formula_positions: Vec<(RowId, ColId)> = wb
crates/ql-io/src/qbook_format.rs:1113:        let mut written: std::collections::HashSet<(RowId, ColId)> =
crates/ql-io/src/qbook_format.rs:1323:                if entry.row > MAX_ROW || entry.col > MAX_COLUMN {
crates/ql-io/src/qbook_format.rs:1372:            if rec.row > MAX_ROW {
crates/ql-io/src/qbook_format.rs:1376:                    detail: format!("row {} exceeds MAX_ROW {MAX_ROW}", rec.row),
crates/ql-io/src/qbook_format.rs:1379:            if rec.col > MAX_COLUMN {
crates/ql-io/src/qbook_format.rs:1383:                    detail: format!("col {} exceeds MAX_COLUMN {MAX_COLUMN}", rec.col),
crates/ql-io/src/qbook_format.rs:1428:            let row = rec.row as RowId;
crates/ql-io/src/qbook_format.rs:1429:            let col = rec.col as ColId;
crates/ql-io/src/qbook_format.rs:1517:    fn cell(row: u32, col: u32, v: Value) -> ((SheetId, RowId, ColId), Value) {
crates/ql-io/src/qbook_format.rs:1521:    fn wb_with(name: &str, cells: &[((SheetId, RowId, ColId), Value)]) -> Workbook {
crates/ql-io/src/qbook_format.rs:1723:        // u32::MAX is well over Excel's MAX_ROW = 1_048_575.
crates/ql-io/src/qbook_format.rs:1733:                    detail.contains("MAX_ROW"),
crates/ql-io/src/qbook_format.rs:1734:                    "detail should mention MAX_ROW: {detail:?}"
crates/ql-io/src/qbook_format.rs:1737:            other => panic!("expected MalformedCell mentioning MAX_ROW, got {other:?}"),
crates/ql-io/src/qbook_format.rs:1747:        // u32::MAX > MAX_COLUMN = 16_383.
crates/ql-io/src/qbook_format.rs:1757:                    detail.contains("MAX_COLUMN"),
crates/ql-io/src/qbook_format.rs:1758:                    "detail should mention MAX_COLUMN: {detail:?}"
crates/ql-io/src/qbook_format.rs:1761:            other => panic!("expected MalformedCell mentioning MAX_COLUMN, got {other:?}"),
crates/ql-io/src/qbook_format.rs:2687:        // MAX_ROW is u32::MAX in `ql_types`; we use a far-too-large row.
crates/ql-formula-syntax/src/lib.rs:36:pub use lexer::{column_letters_to_index, lex, LexError, MAX_COLUMN, MAX_ROW};
crates/ql-storage/src/sheet.rs:8:use ql_types::{ColId, RowId, Value};
crates/ql-storage/src/sheet.rs:18:    pub row_extent: RowId,
crates/ql-storage/src/sheet.rs:20:    pub col_extent: ColId,
crates/ql-storage/src/sheet.rs:139:    pub fn column(&self, col: ColId) -> Option<&ColumnStore> {
crates/ql-storage/src/sheet.rs:144:    pub fn read(&self, row: RowId, col: ColId) -> Value {
crates/ql-storage/src/sheet.rs:155:    pub fn put(&mut self, row: RowId, col: ColId, value: Value) {
crates/ql-storage/src/sheet.rs:156:        use ql_types::address::{MAX_COLUMN, MAX_ROW};
crates/ql-storage/src/sheet.rs:158:            row <= MAX_ROW,
crates/ql-storage/src/sheet.rs:159:            "Sheet::put: row {row} exceeds Excel max row {MAX_ROW}"
crates/ql-storage/src/sheet.rs:162:            col <= MAX_COLUMN,
crates/ql-storage/src/sheet.rs:163:            "Sheet::put: col {col} exceeds Excel max column {MAX_COLUMN}"
crates/ql-storage/src/sheet.rs:189:    pub fn put_computed(&mut self, row: RowId, col: ColId, value: Value) {
crates/ql-storage/src/sheet.rs:190:        use ql_types::address::{MAX_COLUMN, MAX_ROW};
crates/ql-storage/src/sheet.rs:192:            row <= MAX_ROW,
crates/ql-storage/src/sheet.rs:193:            "Sheet::put_computed: row {row} exceeds Excel max row {MAX_ROW}"
crates/ql-storage/src/sheet.rs:196:            col <= MAX_COLUMN,
crates/ql-storage/src/sheet.rs:197:            "Sheet::put_computed: col {col} exceeds Excel max column {MAX_COLUMN}"
crates/ql-storage/src/sheet.rs:224:    pub fn clear_computed(&mut self, row: RowId, col: ColId) {
crates/ql-storage/src/sheet.rs:234:    pub fn clear_user(&mut self, row: RowId, col: ColId) {
crates/ql-storage/src/sheet.rs:243:    /// Per opus consistency N-3: the prior `column.row_count() as RowId` silently truncated
crates/ql-storage/src/sheet.rs:247:        let col_idx: ColId = self
crates/ql-storage/src/sheet.rs:251:            .expect("Sheet::append_column: column count exceeds ColId range");
crates/ql-storage/src/sheet.rs:252:        let row_extent: RowId = column
crates/ql-storage/src/sheet.rs:255:            .expect("Sheet::append_column: column row_count exceeds RowId range");
crates/ql-storage/src/sheet.rs:262:            panic!("Sheet::append_column: col_idx + 1 overflows ColId");
crates/ql-calcgraph/src/dirty.rs:41:use ql_types::{ColId, RowId, SheetId, MAX_ROW};
crates/ql-calcgraph/src/dirty.rs:52:    pub col: ColId,
crates/ql-calcgraph/src/dirty.rs:92:    pub fn chunks_in_column(&self, sheet: SheetId, col: ColId) -> usize {
crates/ql-calcgraph/src/dirty.rs:108:    pub fn chunk_indices_in_column(&self, sheet: SheetId, col: ColId) -> Vec<u32> {
crates/ql-calcgraph/src/dirty.rs:127:    /// Audit M3 (2026-05-12): asserts `row <= MAX_ROW` so downstream chunk-arithmetic
crates/ql-calcgraph/src/dirty.rs:131:    pub fn mark_cell(&mut self, sheet: SheetId, row: RowId, col: ColId) {
crates/ql-calcgraph/src/dirty.rs:133:            row <= MAX_ROW,
crates/ql-calcgraph/src/dirty.rs:134:            "ChunkDirtySet::mark_cell: row {row} exceeds Excel MAX_ROW {MAX_ROW}"
crates/ql-calcgraph/src/dirty.rs:158:        start_row: RowId,
crates/ql-calcgraph/src/dirty.rs:159:        end_row: RowId,
crates/ql-calcgraph/src/dirty.rs:160:        col: ColId,
crates/ql-calcgraph/src/dirty.rs:166:        // Audit M3 (2026-05-12): bound rows under MAX_ROW so chunk-arithmetic stays in u32.
crates/ql-calcgraph/src/dirty.rs:168:            end_row <= MAX_ROW,
crates/ql-calcgraph/src/dirty.rs:169:            "ChunkDirtySet::mark_range_in_column: end_row {end_row} exceeds Excel MAX_ROW {MAX_ROW}"
crates/ql-calcgraph/src/dirty.rs:198:    pub fn is_cell_dirty(&self, sheet: SheetId, row: RowId, col: ColId) -> bool {
crates/ql-calcgraph/src/dirty.rs:390:    // Audit M3 (2026-05-12): row bounds enforced under MAX_ROW so chunk arithmetic
crates/ql-calcgraph/src/dirty.rs:394:    #[should_panic(expected = "exceeds Excel MAX_ROW")]
crates/ql-calcgraph/src/dirty.rs:401:    #[should_panic(expected = "exceeds Excel MAX_ROW")]
crates/ql-calcgraph/src/dirty.rs:410:        d.mark_cell(0, MAX_ROW, 0);
crates/ql-calcgraph/src/dirty.rs:411:        assert!(d.is_cell_dirty(0, MAX_ROW, 0));
crates/ql-exec/src/plan.rs:36:use ql_types::{ColId, ErrorValue, Range, RowId, SheetId};
crates/ql-exec/src/plan.rs:46:        row: RowId,
crates/ql-exec/src/plan.rs:47:        col: ColId,
crates/ql-exec/src/plan.rs:553:    Cell(SheetId, RowId, ColId, bool, bool),
crates/ql-oplog/src/replay.rs:45:use ql_types::{ColId, RowId, SheetId, Value, MAX_COLUMN, MAX_ROW};
crates/ql-oplog/src/replay.rs:72:        row: RowId,
crates/ql-oplog/src/replay.rs:73:        col: ColId,
crates/ql-oplog/src/replay.rs:435:    row: RowId,
crates/ql-oplog/src/replay.rs:436:    col: ColId,
crates/ql-oplog/src/replay.rs:447:    if row > MAX_ROW {
crates/ql-oplog/src/replay.rs:452:            why: "row exceeds MAX_ROW (1,048,575)",
crates/ql-oplog/src/replay.rs:455:    if col > MAX_COLUMN {
crates/ql-oplog/src/replay.rs:460:            why: "col exceeds MAX_COLUMN (16,383)",
crates/ql-storage/src/format_overlay.rs:8://! The sparse map keyed by `(RowId, ColId)` costs proportional to the
crates/ql-storage/src/format_overlay.rs:12://! **V1 design choice (W5-79):** single `HashMap<(RowId, ColId),
crates/ql-storage/src/format_overlay.rs:21:use ql_types::{ColId, RowId};
crates/ql-storage/src/format_overlay.rs:30:    entries: HashMap<(RowId, ColId), FormatId>,
crates/ql-storage/src/format_overlay.rs:41:    pub fn get(&self, row: RowId, col: ColId) -> Option<FormatId> {
crates/ql-storage/src/format_overlay.rs:47:    pub fn set(&mut self, row: RowId, col: ColId, id: FormatId) -> Option<FormatId> {
crates/ql-storage/src/format_overlay.rs:53:    pub fn clear(&mut self, row: RowId, col: ColId) -> Option<FormatId> {
crates/ql-storage/src/format_overlay.rs:74:    pub fn iter(&self) -> impl Iterator<Item = ((RowId, ColId), FormatId)> + '_ {
crates/ql-calcgraph/src/lib.rs:26://! (deterministic Vec ordering on `dependents_for_cell`), and M3 (MAX_ROW bound on
crates/ql-types/src/array.rs:183:    /// Number of rows. `u32` for symmetry with `RowId`.
crates/ql-oplog/src/op.rs:37:use ql_types::{ColId, RowId, SheetId};
crates/ql-oplog/src/op.rs:46:        row: RowId,
crates/ql-oplog/src/op.rs:47:        col: ColId,
crates/ql-oplog/src/op.rs:56:        row: RowId,
crates/ql-oplog/src/op.rs:57:        col: ColId,
crates/ql-oplog/src/op.rs:65:        row: RowId,
crates/ql-oplog/src/op.rs:66:        col: ColId,
crates/ql-oplog/src/op.rs:129:        row: RowId,
crates/ql-oplog/src/op.rs:130:        col: ColId,
crates/ql-calcgraph/src/graph.rs:32:use ql_types::{ColId, RowId, SheetId};
crates/ql-calcgraph/src/graph.rs:140:    pub fn add_cell_node(&mut self, sheet: SheetId, row: RowId, col: ColId) -> NodeId {
crates/ql-calcgraph/src/graph.rs:154:    pub fn add_spill_node(&mut self, _anchor: NodeId, _shape: (RowId, ColId)) -> NodeId {
crates/ql-calcgraph/src/graph.rs:296:    pub fn dependents_for_cell(&self, sheet: SheetId, row: RowId, col: ColId) -> Vec<NodeId> {
crates/ql-storage/src/column.rs:35:use ql_types::{RowId, Value, MAX_ROW};
crates/ql-storage/src/column.rs:170:    pub fn read(&self, row: RowId) -> Value {
crates/ql-storage/src/column.rs:202:    /// **Bound** (per opus arch F3): `row <= MAX_ROW` (Excel's 1,048,575). Beyond that, the
crates/ql-storage/src/column.rs:205:    pub fn put(&mut self, row: RowId, value: Value) {
crates/ql-storage/src/column.rs:207:            row <= MAX_ROW,
crates/ql-storage/src/column.rs:208:            "ColumnStore::put: row {row} exceeds Excel max row {MAX_ROW}"
crates/ql-storage/src/column.rs:231:    /// `recompute_all`, and `recompute_dirty`. Same autogrowth + MAX_ROW bound as `put`.
crates/ql-storage/src/column.rs:232:    pub fn put_computed(&mut self, row: RowId, value: Value) {
crates/ql-storage/src/column.rs:234:            row <= MAX_ROW,
crates/ql-storage/src/column.rs:235:            "ColumnStore::put_computed: row {row} exceeds Excel max row {MAX_ROW}"
crates/ql-storage/src/column.rs:252:    pub fn clear_computed(&mut self, row: RowId) {
crates/ql-storage/src/column.rs:263:    pub fn clear_user(&mut self, row: RowId) {
crates/ql-storage/src/column.rs:372:    ///     for rel_row in 0..base.len() as RowId {
crates/ql-storage/src/column.rs:393:    fn locate(&self, row: RowId) -> (usize, RowId) {
crates/ql-storage/src/column.rs:412:fn read_base(base: &dyn Array, rel_row: RowId) -> Value {
crates/ql-storage/src/column.rs:515:    // -- FIX-2: F3 — ColumnStore::put rejects row > MAX_ROW ---
crates/ql-storage/src/column.rs:520:        use ql_types::MAX_ROW;
crates/ql-storage/src/column.rs:522:        c.put(MAX_ROW + 1, Value::Number(1.0));
crates/ql-storage/src/column.rs:527:        use ql_types::MAX_ROW;
crates/ql-storage/src/column.rs:529:        c.put(MAX_ROW, Value::Number(42.0));
crates/ql-storage/src/column.rs:530:        assert_eq!(c.read(MAX_ROW), Value::Number(42.0));
crates/ql-calcgraph/benches/region_split_merge.rs:14://! "25M cells" doesn't fit in a single Excel column (MAX_ROW = 1,048,575). The bench
crates/ql-calcgraph/benches/region_split_merge.rs:16://! are sampled uniformly from column 0 rows [0, MAX_ROW].
crates/ql-calcgraph/benches/region_split_merge.rs:22:use ql_types::MAX_ROW;
crates/ql-calcgraph/benches/region_split_merge.rs:26:/// respecting Excel's MAX_ROW.
crates/ql-calcgraph/benches/region_split_merge.rs:37:        target_end_row: MAX_ROW,
crates/ql-calcgraph/benches/region_split_merge.rs:40:        chunk_count: (MAX_ROW + 1) / CHUNK_ROWS + 1,
crates/ql-calcgraph/benches/region_split_merge.rs:51:        let row = ((state >> 32) as u32) % (MAX_ROW + 1);
crates/ql-storage/src/workbook.rs:13:use ql_types::{Address, ColId, ErrorValue, Range, RowId, SheetId, Value};
crates/ql-storage/src/workbook.rs:253:    formula_cells: HashMap<(SheetId, RowId, ColId), Arc<str>>,
crates/ql-storage/src/workbook.rs:558:        row: RowId,
crates/ql-storage/src/workbook.rs:559:        col: ColId,
crates/ql-storage/src/workbook.rs:571:        row: RowId,
crates/ql-storage/src/workbook.rs:572:        col: ColId,
crates/ql-storage/src/workbook.rs:573:    ) -> Option<(SheetId, RowId, ColId)> {
crates/ql-storage/src/workbook.rs:583:        anchor: (SheetId, RowId, ColId),
crates/ql-storage/src/workbook.rs:601:        anchor: (SheetId, RowId, ColId),
crates/ql-storage/src/workbook.rs:666:    pub fn put_at(&mut self, sheet: SheetId, row: RowId, col: ColId, value: Value) {
crates/ql-storage/src/workbook.rs:680:    pub fn put_computed_at(&mut self, sheet: SheetId, row: RowId, col: ColId, value: Value) {
crates/ql-storage/src/workbook.rs:693:    pub fn clear_computed_at(&mut self, sheet: SheetId, row: RowId, col: ColId) {
crates/ql-storage/src/workbook.rs:703:    pub fn clear_user_at(&mut self, sheet: SheetId, row: RowId, col: ColId) {
crates/ql-storage/src/workbook.rs:718:    /// MAX_ROW`, or `col > MAX_COLUMN`. The prior implementation accepted any
crates/ql-storage/src/workbook.rs:729:        row: RowId,
crates/ql-storage/src/workbook.rs:730:        col: ColId,
crates/ql-storage/src/workbook.rs:739:            row <= ql_types::MAX_ROW,
crates/ql-storage/src/workbook.rs:740:            "Workbook::put_formula: row {row} exceeds MAX_ROW {}",
crates/ql-storage/src/workbook.rs:741:            ql_types::MAX_ROW
crates/ql-storage/src/workbook.rs:744:            col <= ql_types::MAX_COLUMN,
crates/ql-storage/src/workbook.rs:745:            "Workbook::put_formula: col {col} exceeds MAX_COLUMN {}",
crates/ql-storage/src/workbook.rs:746:            ql_types::MAX_COLUMN
crates/ql-storage/src/workbook.rs:764:    pub fn clear_formula(&mut self, sheet: SheetId, row: RowId, col: ColId) {
crates/ql-storage/src/workbook.rs:770:    pub fn formula_at(&self, sheet: SheetId, row: RowId, col: ColId) -> Option<&Arc<str>> {
crates/ql-storage/src/workbook.rs:781:    pub fn iter_formulas(&self) -> impl Iterator<Item = (SheetId, RowId, ColId, &Arc<str>)> + '_ {
crates/ql-storage/src/workbook.rs:916:    #[should_panic(expected = "Workbook::put_formula: row 1048576 exceeds MAX_ROW")]
crates/ql-storage/src/workbook.rs:924:    #[should_panic(expected = "Workbook::put_formula: col 16384 exceeds MAX_COLUMN")]
crates/ql-exec/src/env.rs:17:    ColId, ErrorValue, EvalContext, Range, RowId, SheetId, Value, DEFAULT_EVAL_CONTEXT,
crates/ql-exec/src/env.rs:24:    fn read_cell(&self, sheet: SheetId, row: RowId, col: ColId) -> Value;
crates/ql-exec/src/env.rs:42:            // Safety: end_row could be `RowId::MAX` (whole-column); the
crates/ql-exec/src/env.rs:103:    fn read_cell(&self, sheet: SheetId, row: RowId, col: ColId) -> Value {
crates/ql-exec/src/env.rs:119:    /// `SUM(A:A)` named range with `end_row = RowId::MAX` reads only the
crates/ql-exec/src/env.rs:121:    /// real sheet), not all 4 billion `RowId` slots. Without this clamp
crates/ql-exec/src/env.rs:156:    ///   `RowId::MAX` / `ColId::MAX`): clamp to sheet bounds. Shape and
crates/ql-exec/src/env.rs:171:            range.end_row == ql_types::RowId::MAX || range.end_col == ql_types::ColId::MAX;
crates/ql-exec/src/env.rs:226:    cells: std::collections::HashMap<(SheetId, RowId, ColId), Value>,
crates/ql-exec/src/env.rs:234:    pub fn put(&mut self, sheet: SheetId, row: RowId, col: ColId, value: Value) {
crates/ql-exec/src/env.rs:240:    fn read_cell(&self, sheet: SheetId, row: RowId, col: ColId) -> Value {
crates/ql-storage/src/spill.rs:13://!     anchors:  HashMap<(SheetId, RowId, ColId), SpillShape>,
crates/ql-storage/src/spill.rs:14://!     targets:  HashMap<(SheetId, RowId, ColId), (SheetId, RowId, ColId)>,
crates/ql-storage/src/spill.rs:50:use ql_types::{ColId, RowId, SheetId};
crates/ql-storage/src/spill.rs:92:        row: RowId,
crates/ql-storage/src/spill.rs:93:        col: ColId,
crates/ql-storage/src/spill.rs:95:        anchor_row: RowId,
crates/ql-storage/src/spill.rs:96:        anchor_col: ColId,
crates/ql-storage/src/spill.rs:109:        row: RowId,
crates/ql-storage/src/spill.rs:110:        col: ColId,
crates/ql-storage/src/spill.rs:129:    pub row: RowId,
crates/ql-storage/src/spill.rs:130:    pub col: ColId,
crates/ql-storage/src/spill.rs:141:    anchors: HashMap<(SheetId, RowId, ColId), SpillShape>,
crates/ql-storage/src/spill.rs:142:    targets: HashMap<(SheetId, RowId, ColId), (SheetId, RowId, ColId)>,
crates/ql-storage/src/spill.rs:153:    pub fn anchor_at(&self, sheet: SheetId, row: RowId, col: ColId) -> Option<&SpillShape> {
crates/ql-storage/src/spill.rs:165:        row: RowId,
crates/ql-storage/src/spill.rs:166:        col: ColId,
crates/ql-storage/src/spill.rs:167:    ) -> Option<(SheetId, RowId, ColId)> {
crates/ql-storage/src/spill.rs:187:        anchor: (SheetId, RowId, ColId),
crates/ql-storage/src/spill.rs:239:        anchor: (SheetId, RowId, ColId),
crates/ql-storage/src/spill.rs:261:    ) -> impl Iterator<Item = ((SheetId, RowId, ColId), &SpillShape)> + '_ {
crates/ql-storage/src/spill.rs:279:    fn at(s: SheetId, r: RowId, c: ColId) -> (SheetId, RowId, ColId) {
crates/ql-exec/src/scalar.rs:1257:                row: ql_types::RowId,
crates/ql-exec/src/scalar.rs:1258:                col: ql_types::ColId,
crates/ql-storage/src/overlay.rs:15:use ql_types::{RowId, Value};
crates/ql-storage/src/overlay.rs:20:    entries: BTreeMap<RowId, Value>,
crates/ql-storage/src/overlay.rs:31:    pub fn put(&mut self, rel_row: RowId, value: Value) -> Option<Value> {
crates/ql-storage/src/overlay.rs:37:    pub fn get(&self, rel_row: RowId) -> Option<&Value> {
crates/ql-storage/src/overlay.rs:43:    pub fn remove(&mut self, rel_row: RowId) -> Option<Value> {
crates/ql-storage/src/overlay.rs:63:    pub fn iter(&self) -> impl Iterator<Item = (RowId, &Value)> {
crates/ql-storage/src/overlay.rs:137:        let rows: Vec<RowId> = o.iter().map(|(r, _)| r).collect();
crates/ql-storage/src/lib.rs:10://! - [`overlay`] — `SparseOverlay`: per-chunk `BTreeMap<RowId, Value>`; overlay-first reads.
crates/ql-types/src/address.rs:1://! Cell coordinate types — `SheetId`, `RowId`, `ColId`, `Address`, `Range`.
crates/ql-types/src/address.rs:10://! - `RowId   = u32` — Excel has 1,048,576 rows; u32 (4.3B) covers it 4000× over.
crates/ql-types/src/address.rs:11://! - `ColId   = u32` — Excel has 16,384 cols; u32 covers it 260K× over.
crates/ql-types/src/address.rs:20:/// 32-bit row index within a sheet. 0-indexed (row "1" in Excel A1 notation = `RowId(0)`).
crates/ql-types/src/address.rs:21:pub type RowId = u32;
crates/ql-types/src/address.rs:23:/// 32-bit column index within a sheet. 0-indexed (column "A" = `ColId(0)`).
crates/ql-types/src/address.rs:24:pub type ColId = u32;
crates/ql-types/src/address.rs:28:pub const MAX_ROW: RowId = 1_048_575;
crates/ql-types/src/address.rs:31:pub const MAX_COLUMN: ColId = 16_383;
crates/ql-types/src/address.rs:37:    pub row: RowId,
crates/ql-types/src/address.rs:38:    pub col: ColId,
crates/ql-types/src/address.rs:42:    pub const fn new(sheet: SheetId, row: RowId, col: ColId) -> Self {
crates/ql-types/src/address.rs:62:    pub start_row: RowId,
crates/ql-types/src/address.rs:64:    pub start_col: ColId,
crates/ql-types/src/address.rs:66:    pub end_row: RowId,
crates/ql-types/src/address.rs:68:    pub end_col: ColId,
crates/ql-types/src/address.rs:73:    pub fn new(sheet: SheetId, row1: RowId, col1: ColId, row2: RowId, col2: ColId) -> Self {
crates/ql-types/src/address.rs:244:        assert_eq!(std::mem::size_of::<RowId>(), 4);
crates/ql-types/src/address.rs:245:        assert_eq!(std::mem::size_of::<ColId>(), 4);
crates/ql-types/src/lib.rs:11://! - [`address`] — cell coordinate types: `SheetId`, `RowId`, `ColId`, `Address`, `Range`.
crates/ql-types/src/lib.rs:23:pub use address::{Address, ColId, Range, RowId, SheetId, MAX_COLUMN, MAX_ROW};
crates/ql-exec/src/transaction.rs:40:use ql_types::{ColId, RowId, SheetId, Value};
crates/ql-exec/src/transaction.rs:63:        row: RowId,
crates/ql-exec/src/transaction.rs:64:        col: ColId,
crates/ql-exec/src/transaction.rs:69:        row: RowId,
crates/ql-exec/src/transaction.rs:70:        col: ColId,
crates/ql-exec/src/transaction.rs:95:    cell_kinds: HashMap<(SheetId, RowId, ColId), OpKind>,
crates/ql-exec/src/transaction.rs:161:        row: RowId,
crates/ql-exec/src/transaction.rs:162:        col: ColId,
crates/ql-exec/src/transaction.rs:199:        row: RowId,
crates/ql-exec/src/transaction.rs:200:        col: ColId,
crates/ql-exec/src/transaction.rs:235:        row: RowId,
crates/ql-exec/src/transaction.rs:236:        col: ColId,
crates/ql-exec/src/workbook_runtime.rs:42:use ql_types::{ColId, ErrorValue, EvalContext, RowId, SheetId, Value, MAX_COLUMN, MAX_ROW};
crates/ql-exec/src/workbook_runtime.rs:68:/// `Sheet::put` (which asserts `row <= MAX_ROW && col <= MAX_COLUMN`). The
crates/ql-exec/src/workbook_runtime.rs:113:    row: RowId,
crates/ql-exec/src/workbook_runtime.rs:114:    col: ColId,
crates/ql-exec/src/workbook_runtime.rs:117:    if row > MAX_ROW {
crates/ql-exec/src/workbook_runtime.rs:122:            why: "row exceeds MAX_ROW (1,048,575)",
crates/ql-exec/src/workbook_runtime.rs:125:    if col > MAX_COLUMN {
crates/ql-exec/src/workbook_runtime.rs:130:            why: "col exceeds MAX_COLUMN (16,383)",
crates/ql-exec/src/workbook_runtime.rs:159:    /// Phase 2A.7 audit H1: row/col outside the Excel grid (MAX_ROW = 1,048,575;
crates/ql-exec/src/workbook_runtime.rs:160:    /// MAX_COLUMN = 16,383). Surfaces at buffer time so `Sheet::put` doesn't
crates/ql-exec/src/workbook_runtime.rs:165:        row: RowId,
crates/ql-exec/src/workbook_runtime.rs:166:        col: ColId,
crates/ql-exec/src/workbook_runtime.rs:176:        row: RowId,
crates/ql-exec/src/workbook_runtime.rs:177:        col: ColId,
crates/ql-exec/src/workbook_runtime.rs:261:    pub row: RowId,
crates/ql-exec/src/workbook_runtime.rs:262:    pub col: ColId,
crates/ql-exec/src/workbook_runtime.rs:479:        row: RowId,
crates/ql-exec/src/workbook_runtime.rs:480:        col: ColId,
crates/ql-exec/src/workbook_runtime.rs:486:        // asserts `row <= MAX_ROW && col <= MAX_COLUMN`).
crates/ql-exec/src/workbook_runtime.rs:581:        row: RowId,
crates/ql-exec/src/workbook_runtime.rs:582:        col: ColId,
crates/ql-exec/src/workbook_runtime.rs:869:        let mut rewritten_cells: Vec<(SheetId, RowId, ColId, String)> = Vec::new();
crates/ql-exec/src/workbook_runtime.rs:939:        row: RowId,
crates/ql-exec/src/workbook_runtime.rs:940:        col: ColId,
crates/ql-exec/src/workbook_runtime.rs:1051:        row: RowId,
crates/ql-exec/src/workbook_runtime.rs:1052:        col: ColId,
crates/ql-exec/src/workbook_runtime.rs:1121:    pub fn read_display(&mut self, sheet: SheetId, row: RowId, col: ColId) -> String {
crates/ql-exec/src/workbook_runtime.rs:1215:        row: RowId,
crates/ql-exec/src/workbook_runtime.rs:1216:        col: ColId,
crates/ql-exec/src/workbook_runtime.rs:1231:        let entries: Vec<(SheetId, RowId, ColId, Arc<str>)> = self
crates/ql-exec/src/workbook_runtime.rs:1336:        let mut prior: HashMap<(SheetId, RowId, ColId), Value> = HashMap::new();
crates/ql-exec/src/workbook_runtime.rs:1348:        let mut changed: HashSet<(SheetId, RowId, ColId)> = HashSet::new();
crates/ql-exec/src/workbook_runtime.rs:1743:        // MAX_ROW = 1,048,575 — anything above is invalid.
crates/ql-exec/src/workbook_runtime.rs:1755:            other => panic!("expected InvalidCell with row > MAX_ROW, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:1765:        // MAX_COLUMN = 16,383 — anything above is invalid.
crates/ql-exec/src/workbook_runtime.rs:1777:            other => panic!("expected InvalidCell with col > MAX_COLUMN, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:1799:        // Boundary case: exactly MAX_ROW and MAX_COLUMN are accepted.
crates/ql-exec/src/workbook_runtime.rs:3943:    /// `SUM(WholeCol)` where WholeCol is start_row=0, end_row=RowId::MAX
crates/ql-exec/src/workbook_runtime.rs:3962:                end_row: ql_types::RowId::MAX, // whole column
crates/ql-exec/src/workbook_runtime.rs:3974:        // RowId::MAX cells.

exec
/bin/zsh -lc "nl -ba crates/ql-storage/src/workbook.rs | sed -n '520,630p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   520	        name: impl Into<String>,
   521	        chunk_rows: u32,
   522	    ) -> Result<SheetId, SheetNameError> {
   523	        let name: String = name.into();
   524	        self.validate_sheet_name(&name)?;
   525	        let id = self.sheets.len();
   526	        assert!(
   527	            id < SheetId::MAX as usize,
   528	            "too many sheets (max {})",
   529	            SheetId::MAX as usize
   530	        );
   531	        self.sheets.push(Sheet::with_chunk_rows(name, chunk_rows));
   532	        Ok(id as SheetId)
   533	    }
   534	
   535	    pub fn sheet(&self, id: SheetId) -> Option<&Sheet> {
   536	        self.sheets.get(id as usize)
   537	    }
   538	
   539	    pub fn sheet_mut(&mut self, id: SheetId) -> Option<&mut Sheet> {
   540	        self.sheets.get_mut(id as usize)
   541	    }
   542	
   543	    // ===== W5-101 (Phase 4.7.H) spill-anchor API =====
   544	
   545	    /// **W5-101 (Phase 4.7.H):** read access to the spill-anchor table.
   546	    /// Production callers (runtime, calcgraph dep extraction in 4.7.I,
   547	    /// persistence save-skip in 4.7.L) use this for lookups.
   548	    pub fn spill_anchors(&self) -> &crate::SpillAnchorTable {
   549	        &self.spill_anchors
   550	    }
   551	
   552	    /// **W5-101 (Phase 4.7.H):** convenience: is `(sheet, row, col)` an
   553	    /// active spill anchor? Returns `Some(&shape)` iff yes. Delegates to
   554	    /// `SpillAnchorTable::anchor_at`.
   555	    pub fn spill_anchor_at(
   556	        &self,
   557	        sheet: SheetId,
   558	        row: RowId,
   559	        col: ColId,
   560	    ) -> Option<&crate::SpillShape> {
   561	        self.spill_anchors.anchor_at(sheet, row, col)
   562	    }
   563	
   564	    /// **W5-101 (Phase 4.7.H):** reverse lookup — if `(sheet, row, col)`
   565	    /// is a spill target (anchor or any non-anchor target cell), return
   566	    /// the anchor. `O(1)`. Used by the runtime spill-invalidation path
   567	    /// (4.7.K) on `set_value` to detect user writes into spill ranges.
   568	    pub fn spill_target_anchor(
   569	        &self,
   570	        sheet: SheetId,
   571	        row: RowId,
   572	        col: ColId,
   573	    ) -> Option<(SheetId, RowId, ColId)> {
   574	        self.spill_anchors.target_anchor(sheet, row, col)
   575	    }
   576	
   577	    /// **W5-101 (Phase 4.7.H):** register a new spill. Map-only — does
   578	    /// NOT touch computed overlays. The runtime spill-writeback path
   579	    /// (4.7.J) couples this with `put_computed_at` calls for each
   580	    /// target cell value AFTER `register_spill` succeeds.
   581	    pub fn register_spill(
   582	        &mut self,
   583	        anchor: (SheetId, RowId, ColId),
   584	        shape: crate::SpillShape,
   585	    ) -> Result<(), crate::SpillBlockError> {
   586	        self.spill_anchors.register(anchor, shape)
   587	    }
   588	
   589	    /// **W5-101 (Phase 4.7.H):** workbook-layer convenience: unregister
   590	    /// the anchor + clear the computed overlay at every target cell.
   591	    /// This is the function the runtime calls during the "clear old
   592	    /// spill before re-eval" step (design § 8.3) and when clearing a
   593	    /// formula at an anchor cell (design § 10.3).
   594	    ///
   595	    /// Errors:
   596	    /// - `SpillNotFoundError` — the cell isn't an anchor. Callers that
   597	    ///   want a no-op-on-miss behavior should pre-check
   598	    ///   `spill_anchor_at(...).is_some()`.
   599	    pub fn clear_spill_at(
   600	        &mut self,
   601	        anchor: (SheetId, RowId, ColId),
   602	    ) -> Result<(), crate::SpillNotFoundError> {
   603	        let shape = self.spill_anchors.unregister(anchor)?;
   604	        // Iterate the cleared rectangle and clear computed overlays.
   605	        // This is the coupling reason `clear_spill_at` lives on
   606	        // `Workbook` (which owns sheet access) rather than on the
   607	        // map-only `SpillAnchorTable`.
   608	        let (asheet, arow, acol) = anchor;
   609	        if let Some(sheet) = self.sheets.get_mut(asheet as usize) {
   610	            for dr in 0..shape.rows {
   611	                for dc in 0..shape.cols {
   612	                    sheet.clear_computed(arow + dr, acol + dc);
   613	                }
   614	            }
   615	        }
   616	        Ok(())
   617	    }
   618	
   619	    /// Read by `Address`.
   620	    ///
   621	    /// - **Missing sheet** (sheet id ≥ `sheet_count()`) → `Value::Error(Ref)`
   622	    ///   (Excel canon: `#REF!`).
   623	    /// - **Within-sheet missing row/col** → `Value::Blank` (Excel canon: empty
   624	    ///   cell coerces to 0 / "" / FALSE in arithmetic / text / logical
   625	    ///   contexts).
   626	    ///
   627	    /// Phase 2A.13 audit cycle-3 M2: the prior implementation returned `Blank`
   628	    /// for missing-sheet too, conflating "sheet deleted" with "empty cell."
   629	    /// The Phase 2A.7 H6 fix corrected this at the evaluator layer
   630	    /// (`ql-exec::WorkbookEnv::read_cell`); this commit aligns the storage

exec
/bin/zsh -lc "nl -ba crates/ql-storage/src/workbook.rs | sed -n '630,820p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   630	    /// (`ql-exec::WorkbookEnv::read_cell`); this commit aligns the storage
   631	    /// layer's public read API. Any host code that bypasses `WorkbookEnv`
   632	    /// (CLI tools, IDE inspectors, tests) now sees the same `#REF!` semantic.
   633	    pub fn read(&self, addr: Address) -> Value {
   634	        match self.sheet(addr.sheet) {
   635	            Some(s) => s.read(addr.row, addr.col),
   636	            None => Value::Error(ErrorValue::Ref),
   637	        }
   638	    }
   639	
   640	    /// Write by `Address`. Panics if `addr.sheet` is out of bounds (callers must `add_sheet`
   641	    /// first; this avoids implicit-sheet-creation silent bugs).
   642	    pub fn put(&mut self, addr: Address, value: Value) {
   643	        // Capture sheet_count before the mut-borrow so the panic path can use it.
   644	        let total = self.sheet_count();
   645	        match self.sheet_mut(addr.sheet) {
   646	            Some(s) => s.put(addr.row, addr.col, value),
   647	            None => panic!(
   648	                "Workbook::put: sheet {} does not exist (have {})",
   649	                addr.sheet, total
   650	            ),
   651	        }
   652	    }
   653	
   654	    /// Convenience: write a literal row,col,value tuple without constructing an Address.
   655	    /// Phase 3.5: this is the USER-edit path — routes to `Sheet::put` →
   656	    /// `ColumnStore::put` which writes the user overlay AND clears any stale computed
   657	    /// entry at the same cell.
   658	    ///
   659	    /// **Phase 2B.5 (2026-05-12) — LOW-LEVEL.** Bypasses the op log AND the
   660	    /// runtime's bind-plan cache; does NOT clear any pre-existing formula
   661	    /// at the cell (use `clear_formula` separately if needed). Product code
   662	    /// should route through `ql_exec::WorkbookRuntime::set_value`, which
   663	    /// records `Op::PutValue` + an optional `Op::ClearFormula`. Direct
   664	    /// `put_at` is for the qbook loader, the op-log replay path, runtime-
   665	    /// internal pass 2 of formula recompute, and tests. GAP-O-03.
   666	    pub fn put_at(&mut self, sheet: SheetId, row: RowId, col: ColId, value: Value) {
   667	        self.put(Address::new(sheet, row, col), value);
   668	    }
   669	
   670	    /// **Phase 3.5 (2026-05-12) — CORR-25.** Write a FORMULA-OUTPUT value at
   671	    /// `(sheet, row, col)`. Routes to the computed-overlay lane (NOT the user lane).
   672	    /// Used by `WorkbookRuntime::set_formula` (with the freshly-evaluated result of
   673	    /// the formula text) and by `recompute_all` / `recompute_dirty` for every
   674	    /// formula cell in their pass.
   675	    ///
   676	    /// Panics if `sheet` is out of bounds — same contract as `put_at`. Does NOT touch
   677	    /// the user overlay; if a cell is transitioning from "user-typed value" to
   678	    /// "formula", the caller (the runtime) MUST first call `clear_user_at` so the
   679	    /// stale user value doesn't mask the new computed value via the read cascade.
   680	    pub fn put_computed_at(&mut self, sheet: SheetId, row: RowId, col: ColId, value: Value) {
   681	        let total = self.sheet_count();
   682	        match self.sheet_mut(sheet) {
   683	            Some(s) => s.put_computed(row, col, value),
   684	            None => {
   685	                panic!("Workbook::put_computed_at: sheet {sheet} does not exist (have {total})")
   686	            }
   687	        }
   688	    }
   689	
   690	    /// **Phase 3.5.** Drop the computed-overlay entry at `(sheet, row, col)`. No-op if
   691	    /// the cell has no computed entry or the sheet is out of bounds. Called by
   692	    /// `clear_formula` (the formula text is gone → its output is stale).
   693	    pub fn clear_computed_at(&mut self, sheet: SheetId, row: RowId, col: ColId) {
   694	        if let Some(s) = self.sheet_mut(sheet) {
   695	            s.clear_computed(row, col);
   696	        }
   697	    }
   698	
   699	    /// **Phase 3.5.** Drop the user-overlay entry at `(sheet, row, col)`. No-op if
   700	    /// the cell has no user entry or the sheet is out of bounds. Called by the runtime's
   701	    /// `set_formula` to clear a stale user value before writing the new computed
   702	    /// formula output.
   703	    pub fn clear_user_at(&mut self, sheet: SheetId, row: RowId, col: ColId) {
   704	        if let Some(s) = self.sheet_mut(sheet) {
   705	            s.clear_user(row, col);
   706	        }
   707	    }
   708	
   709	    /// Phase 1 W5-9: record formula source text for a cell. The caller is responsible
   710	    /// for evaluating the formula and storing the result via `put`/`put_at` — this
   711	    /// method just persists the formula association. Pass an empty string or omit the
   712	    /// call to mark a cell as literal-only.
   713	    ///
   714	    /// `formula` is the formula body WITHOUT the leading `=` (matching Excel's
   715	    /// canonical AST representation).
   716	    ///
   717	    /// Phase 2A.13 audit cycle-3 H2: panics if `sheet >= sheet_count()`, `row >
   718	    /// MAX_ROW`, or `col > MAX_COLUMN`. The prior implementation accepted any
   719	    /// (sheet, row, col), making `validate_cell` at the runtime/transaction layer
   720	    /// bypassable: a `formula_cells` entry with an out-of-bounds coord would
   721	    /// later panic deep inside `Sheet::put` during `recompute_all`'s
   722	    /// `put_at`. Matching the storage-layer convention from `Sheet::put` and
   723	    /// `Workbook::put_at` (which also panic on invariant violation), the
   724	    /// in-process programmer-error path stays loud. Trust-boundary callers
   725	    /// (ql-io loader, IDE host) MUST validate before reaching here.
   726	    pub fn put_formula(
   727	        &mut self,
   728	        sheet: SheetId,
   729	        row: RowId,
   730	        col: ColId,
   731	        formula: impl Into<Arc<str>>,
   732	    ) {
   733	        assert!(
   734	            (sheet as usize) < self.sheets.len(),
   735	            "Workbook::put_formula: sheet {sheet} does not exist (have {})",
   736	            self.sheets.len()
   737	        );
   738	        assert!(
   739	            row <= ql_types::MAX_ROW,
   740	            "Workbook::put_formula: row {row} exceeds MAX_ROW {}",
   741	            ql_types::MAX_ROW
   742	        );
   743	        assert!(
   744	            col <= ql_types::MAX_COLUMN,
   745	            "Workbook::put_formula: col {col} exceeds MAX_COLUMN {}",
   746	            ql_types::MAX_COLUMN
   747	        );
   748	        self.formula_cells.insert((sheet, row, col), formula.into());
   749	    }
   750	
   751	    /// Remove any formula association for a cell. Used when a formula cell becomes a
   752	    /// literal (e.g. user types over the formula with a value). Idempotent: removing
   753	    /// a non-existent entry is a no-op.
   754	    ///
   755	    /// Phase 3.5 (CORR-25): ALSO drops any computed-overlay entry at this cell. A
   756	    /// cell without a formula text has no business carrying a formula output; the
   757	    /// invariant the rest of the engine relies on is "computed overlay populated ⇒
   758	    /// formula text present", and `clear_formula` is the only path that breaks the
   759	    /// formula→computed pair, so the storage layer enforces both sides here.
   760	    ///
   761	    /// **Phase 2B.5 (2026-05-12) — LOW-LEVEL.** Bypasses the op log. Use
   762	    /// `ql_exec::WorkbookRuntime::clear_formula` to record `Op::ClearFormula`
   763	    /// in an attached log. GAP-O-03.
   764	    pub fn clear_formula(&mut self, sheet: SheetId, row: RowId, col: ColId) {
   765	        self.formula_cells.remove(&(sheet, row, col));
   766	        self.clear_computed_at(sheet, row, col);
   767	    }
   768	
   769	    /// Borrow the formula source for a cell, if it has one.
   770	    pub fn formula_at(&self, sheet: SheetId, row: RowId, col: ColId) -> Option<&Arc<str>> {
   771	        self.formula_cells.get(&(sheet, row, col))
   772	    }
   773	
   774	    /// Count of formula cells across the workbook.
   775	    pub fn formula_count(&self) -> usize {
   776	        self.formula_cells.len()
   777	    }
   778	
   779	    /// Iterate every formula cell as `(sheet, row, col, formula)`. Iteration order is
   780	    /// HashMap-arbitrary; callers that need determinism (qbook save) should sort first.
   781	    pub fn iter_formulas(&self) -> impl Iterator<Item = (SheetId, RowId, ColId, &Arc<str>)> + '_ {
   782	        self.formula_cells
   783	            .iter()
   784	            .map(|((s, r, c), f)| (*s, *r, *c, f))
   785	    }
   786	}
   787	
   788	#[cfg(test)]
   789	mod tests {
   790	    use super::*;
   791	    use ql_types::ErrorValue;
   792	
   793	    #[test]
   794	    fn empty_workbook_has_no_sheets() {
   795	        let wb = Workbook::new();
   796	        assert_eq!(wb.sheet_count(), 0);
   797	        assert!(wb.sheet(0).is_none());
   798	        // Phase 2A.13 audit cycle-3 M2: read out-of-bounds sheet → #REF!
   799	        // (was: Blank). Aligns the storage-layer public read API with
   800	        // `WorkbookEnv::read_cell` per Excel canon.
   801	        assert_eq!(
   802	            wb.read(Address::new(0, 0, 0)),
   803	            Value::Error(ErrorValue::Ref)
   804	        );
   805	    }
   806	
   807	    #[test]
   808	    fn add_sheet_returns_zero_based_id() {
   809	        let mut wb = Workbook::new();
   810	        let id0 = wb.add_sheet("Sheet1");
   811	        assert_eq!(id0, 0);
   812	        let id1 = wb.add_sheet("Sheet2");
   813	        assert_eq!(id1, 1);
   814	        assert_eq!(wb.sheet_count(), 2);
   815	        assert_eq!(wb.sheet(0).unwrap().name(), "Sheet1");
   816	        assert_eq!(wb.sheet(1).unwrap().name(), "Sheet2");
   817	    }
   818	
   819	    #[test]
   820	    fn put_then_read_across_sheets() {

exec
/bin/zsh -lc "nl -ba crates/ql-storage/src/workbook.rs | sed -n '980,1180p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   980	    }
   981	
   982	    #[test]
   983	    fn put_and_get_formula() {
   984	        let mut wb = Workbook::new();
   985	        let s = wb.add_sheet("S");
   986	        wb.put_formula(s, 0, 0, "A1 + B1");
   987	        assert_eq!(wb.formula_count(), 1);
   988	        assert_eq!(wb.formula_at(s, 0, 0).map(|s| s.as_ref()), Some("A1 + B1"));
   989	        assert!(wb.formula_at(s, 0, 1).is_none());
   990	    }
   991	
   992	    #[test]
   993	    fn clear_formula_removes_entry() {
   994	        let mut wb = Workbook::new();
   995	        let s = wb.add_sheet("S");
   996	        wb.put_formula(s, 0, 0, "A1 + 1");
   997	        wb.clear_formula(s, 0, 0);
   998	        assert_eq!(wb.formula_count(), 0);
   999	        assert!(wb.formula_at(s, 0, 0).is_none());
  1000	    }
  1001	
  1002	    #[test]
  1003	    fn clear_nonexistent_formula_is_noop() {
  1004	        let mut wb = Workbook::new();
  1005	        wb.add_sheet("S");
  1006	        // No panic; idempotent.
  1007	        wb.clear_formula(0, 5, 5);
  1008	        assert_eq!(wb.formula_count(), 0);
  1009	    }
  1010	
  1011	    #[test]
  1012	    fn put_formula_replaces_existing() {
  1013	        let mut wb = Workbook::new();
  1014	        let s = wb.add_sheet("S");
  1015	        wb.put_formula(s, 0, 0, "A1 + 1");
  1016	        wb.put_formula(s, 0, 0, "B1 * 2");
  1017	        assert_eq!(wb.formula_count(), 1);
  1018	        assert_eq!(wb.formula_at(s, 0, 0).map(|s| s.as_ref()), Some("B1 * 2"));
  1019	    }
  1020	
  1021	    #[test]
  1022	    fn iter_formulas_yields_all() {
  1023	        let mut wb = Workbook::new();
  1024	        let s = wb.add_sheet("S");
  1025	        wb.put_formula(s, 0, 0, "f1");
  1026	        wb.put_formula(s, 1, 0, "f2");
  1027	        wb.put_formula(s, 5, 3, "f3");
  1028	        let mut items: Vec<_> = wb
  1029	            .iter_formulas()
  1030	            .map(|(sh, r, c, f)| (sh, r, c, f.as_ref().to_owned()))
  1031	            .collect();
  1032	        items.sort();
  1033	        assert_eq!(
  1034	            items,
  1035	            vec![
  1036	                (s, 0, 0, "f1".to_owned()),
  1037	                (s, 1, 0, "f2".to_owned()),
  1038	                (s, 5, 3, "f3".to_owned()),
  1039	            ]
  1040	        );
  1041	    }
  1042	
  1043	    #[test]
  1044	    fn formula_and_value_coexist_on_same_cell() {
  1045	        // A formula cell ALSO holds an evaluated value in the Sheet's columnar storage.
  1046	        // The caller (or future runtime) is responsible for keeping them consistent.
  1047	        let mut wb = Workbook::new();
  1048	        let s = wb.add_sheet("S");
  1049	        wb.put_at(s, 0, 0, Value::Number(42.0));
  1050	        wb.put_formula(s, 0, 0, "A1 + 1");
  1051	        // Both visible.
  1052	        assert_eq!(wb.read(Address::new(s, 0, 0)), Value::Number(42.0));
  1053	        assert_eq!(wb.formula_at(s, 0, 0).map(|s| s.as_ref()), Some("A1 + 1"));
  1054	    }
  1055	
  1056	    // ===== Phase 2A.9 audit M6: reserved-name rejection =====
  1057	
  1058	    /// `Workbook::set_name("AI", ...)` is refused — the AI sentinel per
  1059	    /// CORR-06 can't be overridden by a user-defined name.
  1060	    #[test]
  1061	    fn set_name_rejects_reserved_ai_canonical_upper() {
  1062	        let mut wb = Workbook::new();
  1063	        let result = wb.set_name("AI", NamedTarget::Constant(Value::Number(42.0)));
  1064	        match result {
  1065	            Err(NameTableError::Reserved(name)) => assert_eq!(name.as_ref(), "AI"),
  1066	            other => panic!("expected Reserved(AI), got {other:?}"),
  1067	        }
  1068	        // NameTable remains empty.
  1069	        assert!(wb.names().is_empty());
  1070	    }
  1071	
  1072	    /// Case-insensitive: `Workbook::set_name("ai", ...)` also refused (the
  1073	    /// uppercase-on-insert step canonicalizes before checking reservation).
  1074	    #[test]
  1075	    fn set_name_rejects_reserved_ai_case_insensitive() {
  1076	        let mut wb = Workbook::new();
  1077	        let result = wb.set_name("ai", NamedTarget::Constant(Value::Number(42.0)));
  1078	        assert!(matches!(result, Err(NameTableError::Reserved(_))));
  1079	        let result_mixed = wb.set_name("Ai", NamedTarget::Constant(Value::Number(42.0)));
  1080	        assert!(matches!(result_mixed, Err(NameTableError::Reserved(_))));
  1081	    }
  1082	
  1083	    /// Non-reserved names succeed as before.
  1084	    #[test]
  1085	    fn set_name_accepts_non_reserved() {
  1086	        let mut wb = Workbook::new();
  1087	        wb.set_name("TaxRate", NamedTarget::Constant(Value::Number(0.21)))
  1088	            .unwrap();
  1089	        assert!(wb.names().lookup("TAXRATE").is_some());
  1090	    }
  1091	
  1092	    // ===== W5-86 / Phase 4.6.AA — sheet registry + canonicalizer =====
  1093	
  1094	    #[test]
  1095	    fn canonical_sheet_name_uppercases_ascii() {
  1096	        assert_eq!(Workbook::canonical_sheet_name("Sheet1"), "SHEET1");
  1097	        assert_eq!(Workbook::canonical_sheet_name("sheet1"), "SHEET1");
  1098	        assert_eq!(Workbook::canonical_sheet_name("SHEET1"), "SHEET1");
  1099	    }
  1100	
  1101	    #[test]
  1102	    fn canonical_sheet_name_passes_non_ascii_through() {
  1103	        // V1 simplification: ASCII-only uppercasing; non-ASCII letters
  1104	        // stay as-is. `Café` and `café` canonicalize to different forms.
  1105	        // Documented as a known V1 simplification.
  1106	        assert_eq!(Workbook::canonical_sheet_name("Café"), "CAFé");
  1107	    }
  1108	
  1109	    #[test]
  1110	    fn sheet_id_by_name_finds_case_insensitive() {
  1111	        let mut wb = Workbook::new();
  1112	        let s0 = wb.add_sheet("Inventory");
  1113	        let s1 = wb.add_sheet("Q3 2025");
  1114	        assert_eq!(wb.sheet_id_by_name("Inventory"), Some(s0));
  1115	        assert_eq!(wb.sheet_id_by_name("inventory"), Some(s0));
  1116	        assert_eq!(wb.sheet_id_by_name("INVENTORY"), Some(s0));
  1117	        assert_eq!(wb.sheet_id_by_name("Q3 2025"), Some(s1));
  1118	        assert_eq!(wb.sheet_id_by_name("q3 2025"), Some(s1));
  1119	    }
  1120	
  1121	    #[test]
  1122	    fn sheet_id_by_name_returns_none_on_miss() {
  1123	        let mut wb = Workbook::new();
  1124	        wb.add_sheet("S1");
  1125	        assert_eq!(wb.sheet_id_by_name("S2"), None);
  1126	        assert_eq!(wb.sheet_id_by_name(""), None);
  1127	    }
  1128	
  1129	    #[test]
  1130	    fn sheet_id_by_name_first_match_in_id_order() {
  1131	        // Duplicates are caught at add time via `validate_sheet_name`, but
  1132	        // if a workbook somehow has two sheets with the same canonical
  1133	        // name (e.g. loaded from a corrupted .qbook), `sheet_id_by_name`
  1134	        // returns the FIRST in id order.
  1135	        let mut wb = Workbook::new();
  1136	        // Bypass validation to construct the test fixture.
  1137	        let s0 = wb.add_sheet("Test");
  1138	        wb.sheets.push(Sheet::new("test")); // Same canonical form.
  1139	        let s1: SheetId = (wb.sheet_count() - 1) as SheetId;
  1140	        assert_eq!(wb.sheet_id_by_name("test"), Some(s0));
  1141	        // Confirm both exist.
  1142	        assert!(wb.sheet(s0).is_some());
  1143	        assert!(wb.sheet(s1).is_some());
  1144	    }
  1145	
  1146	    #[test]
  1147	    fn validate_sheet_name_rejects_empty() {
  1148	        let wb = Workbook::new();
  1149	        assert_eq!(wb.validate_sheet_name(""), Err(SheetNameError::Empty));
  1150	    }
  1151	
  1152	    #[test]
  1153	    fn validate_sheet_name_rejects_excel_reserved_characters() {
  1154	        let wb = Workbook::new();
  1155	        for c in [':', '\\', '/', '?', '*', '[', ']'] {
  1156	            let name = format!("S{c}1");
  1157	            let result = wb.validate_sheet_name(&name);
  1158	            assert_eq!(
  1159	                result,
  1160	                Err(SheetNameError::ReservedCharacter(c)),
  1161	                "expected ReservedCharacter({c:?}) for {name:?}, got {result:?}"
  1162	            );
  1163	        }
  1164	    }
  1165	
  1166	    #[test]
  1167	    fn validate_sheet_name_rejects_canonical_duplicate() {
  1168	        let mut wb = Workbook::new();
  1169	        wb.add_sheet("Inventory");
  1170	        assert_eq!(
  1171	            wb.validate_sheet_name("Inventory"),
  1172	            Err(SheetNameError::Duplicate {
  1173	                name: "Inventory".to_owned()
  1174	            })
  1175	        );
  1176	        // Case-insensitive duplicate also rejected.
  1177	        assert_eq!(
  1178	            wb.validate_sheet_name("inventory"),
  1179	            Err(SheetNameError::Duplicate {
  1180	                name: "inventory".to_owned()

exec
/bin/zsh -lc "nl -ba crates/ql-types/src/address.rs | sed -n '1,90p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
     1	//! Cell coordinate types — `SheetId`, `RowId`, `ColId`, `Address`, `Range`.
     2	//!
     3	//! Per spec Part V §2.8, these foundation types live in `ql-types` so every downstream crate
     4	//! (ql-storage, ql-calcgraph, ql-formula-syntax, ql-functions) can consume them without a
     5	//! mutual-dependency cycle. They're added in Week 2 Days 3-4 (when ql-storage first needs them)
     6	//! rather than Days 1-2 (which was just `Value` + `ErrorValue` + coercion per master plan §6.3).
     7	//!
     8	//! Phase 0 representation choices (kept simple; pack/compact later if profiling demands):
     9	//! - `SheetId = u16` — 65,536 sheets per workbook is far beyond any real workbook (Excel: 255).
    10	//! - `RowId   = u32` — Excel has 1,048,576 rows; u32 (4.3B) covers it 4000× over.
    11	//! - `ColId   = u32` — Excel has 16,384 cols; u32 covers it 260K× over.
    12	//! - `Address` is a plain struct (12 bytes incl. padding). The Formualizer-style packed
    13	//!   `Coord(u64)` is a future optimization — not premature for Phase 0.
    14	
    15	use std::fmt;
    16	
    17	/// 16-bit sheet index within a workbook. 0-indexed.
    18	pub type SheetId = u16;
    19	
    20	/// 32-bit row index within a sheet. 0-indexed (row "1" in Excel A1 notation = `RowId(0)`).
    21	pub type RowId = u32;
    22	
    23	/// 32-bit column index within a sheet. 0-indexed (column "A" = `ColId(0)`).
    24	pub type ColId = u32;
    25	
    26	/// Excel's row max — 1,048,576 rows means max 0-indexed row is 1,048,575. Used as the
    27	/// upper bound for ColumnStore writes, Sheet::put, and any other grid-write op.
    28	pub const MAX_ROW: RowId = 1_048_575;
    29	
    30	/// Excel's column max — XFD = 16,383 (0-indexed). Used as the upper bound for grid writes.
    31	pub const MAX_COLUMN: ColId = 16_383;
    32	
    33	/// A sheet-qualified cell address.
    34	#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, Ord, PartialOrd)]
    35	pub struct Address {
    36	    pub sheet: SheetId,
    37	    pub row: RowId,
    38	    pub col: ColId,
    39	}
    40	
    41	impl Address {
    42	    pub const fn new(sheet: SheetId, row: RowId, col: ColId) -> Self {
    43	        Self { sheet, row, col }
    44	    }
    45	}
    46	
    47	impl fmt::Display for Address {
    48	    /// Render in `sheet:row,col` form (0-indexed). The Excel-style A1 form lives in
    49	    /// `ql-formula-syntax` because it's part of the formula-language surface; this is the
    50	    /// engine-internal canonical form.
    51	    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
    52	        write!(f, "{}:{},{}", self.sheet, self.row, self.col)
    53	    }
    54	}
    55	
    56	/// A rectangular range within a single sheet. `start` ≤ `end` per `Ord` on `Address`,
    57	/// enforced by the constructor. Empty ranges (start == end) are valid.
    58	#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
    59	pub struct Range {
    60	    pub sheet: SheetId,
    61	    /// Inclusive top-left row.
    62	    pub start_row: RowId,
    63	    /// Inclusive top-left column.
    64	    pub start_col: ColId,
    65	    /// Inclusive bottom-right row.
    66	    pub end_row: RowId,
    67	    /// Inclusive bottom-right column.
    68	    pub end_col: ColId,
    69	}
    70	
    71	impl Range {
    72	    /// Construct a range, normalizing so `start ≤ end` per axis.
    73	    pub fn new(sheet: SheetId, row1: RowId, col1: ColId, row2: RowId, col2: ColId) -> Self {
    74	        Self {
    75	            sheet,
    76	            start_row: row1.min(row2),
    77	            start_col: col1.min(col2),
    78	            end_row: row1.max(row2),
    79	            end_col: col1.max(col2),
    80	        }
    81	    }
    82	
    83	    /// Total number of cells in the range (inclusive on both ends).
    84	    ///
    85	    /// Uses widening then checked arithmetic. With u32 row/col bounds, the theoretical max
    86	    /// is 2^32 × 2^32 = 2^64 = u64::MAX EXACTLY (1 too many for u64::MAX). For Excel-canonical
    87	    /// bounds (1M rows × 16K cols = ~17B cells) this is comfortably under u64::MAX, but the
    88	    /// arithmetic is bound-checked anyway (per opus arch F2 + founder "No Fallbacks" rule).
    89	    pub fn cell_count(&self) -> u64 {
    90	        let rows: u64 = u64::from(self.end_row - self.start_row) + 1;

exec
/bin/zsh -lc 'rg -n "spill|clear_spill|register_spill" crates/ql-storage/src/workbook.rs' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
267:    /// **W5-101 (Phase 4.7.H):** spill-anchor table. Workbook-level
268:    /// because spills can theoretically span sheets (rare in Excel; v1
270:    /// design; mutation goes through `register_spill` / `clear_spill_at`
273:    /// `crates/ql-storage/src/spill.rs` module docs.
274:    spill_anchors: crate::SpillAnchorTable,
543:    // ===== W5-101 (Phase 4.7.H) spill-anchor API =====
545:    /// **W5-101 (Phase 4.7.H):** read access to the spill-anchor table.
548:    pub fn spill_anchors(&self) -> &crate::SpillAnchorTable {
549:        &self.spill_anchors
553:    /// active spill anchor? Returns `Some(&shape)` iff yes. Delegates to
555:    pub fn spill_anchor_at(
561:        self.spill_anchors.anchor_at(sheet, row, col)
565:    /// is a spill target (anchor or any non-anchor target cell), return
566:    /// the anchor. `O(1)`. Used by the runtime spill-invalidation path
567:    /// (4.7.K) on `set_value` to detect user writes into spill ranges.
568:    pub fn spill_target_anchor(
574:        self.spill_anchors.target_anchor(sheet, row, col)
577:    /// **W5-101 (Phase 4.7.H):** register a new spill. Map-only — does
578:    /// NOT touch computed overlays. The runtime spill-writeback path
580:    /// target cell value AFTER `register_spill` succeeds.
581:    pub fn register_spill(
586:        self.spill_anchors.register(anchor, shape)
592:    /// spill before re-eval" step (design § 8.3) and when clearing a
598:    ///   `spill_anchor_at(...).is_some()`.
599:    pub fn clear_spill_at(
603:        let shape = self.spill_anchors.unregister(anchor)?;
605:        // This is the coupling reason `clear_spill_at` lives on
1321:    // ===== W5-101 (Phase 4.7.H) Workbook spill-anchor integration =====
1324:    fn workbook_register_spill_round_trip() {
1327:        // Register a 2x3 spill at (0, 1, 1) — covers B2:D3.
1328:        wb.register_spill((0, 1, 1), crate::SpillShape::new(2, 3))
1331:            wb.spill_anchor_at(0, 1, 1),
1338:                    wb.spill_target_anchor(0, 1 + dr, 1 + dc),
1349:    fn workbook_clear_spill_at_clears_computed_overlays() {
1352:        // Register a 1x3 spill at (0, 0, 0) covering A1:C1.
1353:        wb.register_spill((0, 0, 0), crate::SpillShape::new(1, 3))
1363:        // clear_spill_at: unregisters AND clears overlays at every target.
1364:        wb.clear_spill_at((0, 0, 0)).unwrap();
1366:        assert!(wb.spill_anchor_at(0, 0, 0).is_none());
1367:        assert!(wb.spill_target_anchor(0, 0, 1).is_none());
1375:    fn workbook_register_spill_rejects_collision_with_existing() {
1378:        wb.register_spill((0, 0, 0), crate::SpillShape::new(3, 1))
1380:        // Second spill at (0, 1, 0) would overlap A2/A3 of the first.
1382:            .register_spill((0, 1, 0), crate::SpillShape::new(3, 1))
1391:    fn workbook_clear_spill_at_unknown_anchor_errors() {
1394:        let err = wb.clear_spill_at((0, 5, 5)).unwrap_err();
1400:    fn workbook_clear_spill_at_preserves_user_overlays_outside_rectangle() {
1401:        // A user-typed value outside the spill rectangle must survive
1402:        // clear_spill_at. Verifies the clear loop is bounded to the
1406:        wb.register_spill((0, 0, 0), crate::SpillShape::new(1, 3))
1411:        // User value at A2 (row 1, col 0) — OUTSIDE the 1x3 spill.
1413:        wb.clear_spill_at((0, 0, 0)).unwrap();
1421:    fn workbook_spill_anchors_accessor_returns_table() {
1424:        assert_eq!(wb.spill_anchors().len(), 0);
1425:        wb.register_spill((0, 0, 0), crate::SpillShape::new(2, 2))
1427:        wb.register_spill((0, 5, 5), crate::SpillShape::new(1, 1))
1429:        assert_eq!(wb.spill_anchors().len(), 2);

exec
/bin/zsh -lc "nl -ba crates/ql-storage/src/workbook.rs | sed -n '1500,1685p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
exec
/bin/zsh -lc "rg -n \"HashMap|RandomState|derive\\(.*Hash|iter_anchors|spill_anchor\" docs/architecture/2026-05-14-array-formulas-and-spills.md crates/ql-storage/src/spill.rs crates/ql-storage/src/workbook.rs" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
crates/ql-storage/src/spill.rs:13://!     anchors:  HashMap<(SheetId, RowId, ColId), SpillShape>,
crates/ql-storage/src/spill.rs:14://!     targets:  HashMap<(SheetId, RowId, ColId), (SheetId, RowId, ColId)>,
crates/ql-storage/src/spill.rs:48:use std::collections::HashMap;
crates/ql-storage/src/spill.rs:57:#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
crates/ql-storage/src/spill.rs:135:/// **Map-only:** mutations affect ONLY the two HashMaps. Computed-
crates/ql-storage/src/spill.rs:141:    anchors: HashMap<(SheetId, RowId, ColId), SpillShape>,
crates/ql-storage/src/spill.rs:142:    targets: HashMap<(SheetId, RowId, ColId), (SheetId, RowId, ColId)>,
crates/ql-storage/src/spill.rs:256:    /// Order is HashMap-iteration order — unspecified and may change
crates/ql-storage/src/spill.rs:259:    pub fn iter_anchors(
crates/ql-storage/src/spill.rs:474:    fn iter_anchors_yields_all_registered() {
crates/ql-storage/src/spill.rs:478:        let anchors: Vec<_> = t.iter_anchors().collect();
docs/architecture/2026-05-14-array-formulas-and-spills.md:107:**`Expr::Spill(Box<Expr>)` terminology note** (Codex LOW): the existing shape-lock at `ast.rs:67` is RESERVED for Excel's `A1#` spill-range-ref SYNTAX (Phase 4.9), NOT the runtime spill anchor. Excel's `A1#` means "the spill range anchored at A1" — it's a reference operator, not an anchor marker. The runtime spill anchor (the formula cell at the top-left of a spilled range) lives in `Workbook::spill_anchors` (§ 7), not in the AST. Phase 4.7 does not construct `Expr::Spill`; that's Phase 4.9's job alongside `@`.
docs/architecture/2026-05-14-array-formulas-and-spills.md:155:Codex HIGH: adding `ArrayFn` as a 4th parallel tier compounds the dispatch sprawl. The right move is to collapse the three parallel HashMaps in `FunctionRegistry` into **one storage map** keyed by name, valued by a tagged enum that distinguishes the tier of the underlying fn pointer.
docs/architecture/2026-05-14-array-formulas-and-spills.md:189:    fns: HashMap<&'static str, RegisteredFn>,
docs/architecture/2026-05-14-array-formulas-and-spills.md:197:- `lookup_any(name) -> Option<&RegisteredFn>` is the eval-site entry point that returns the tagged enum directly — one HashMap lookup, one match arm per tier.
docs/architecture/2026-05-14-array-formulas-and-spills.md:249:    anchors: HashMap<(SheetId, RowId, ColId), SpillShape>,
docs/architecture/2026-05-14-array-formulas-and-spills.md:253:    targets: HashMap<(SheetId, RowId, ColId), (SheetId, RowId, ColId)>,
docs/architecture/2026-05-14-array-formulas-and-spills.md:273:    pub fn spill_anchors(&self) -> &SpillAnchorTable { &self.spill_anchors }
docs/architecture/2026-05-14-array-formulas-and-spills.md:274:    pub fn spill_anchor_at(&self, sheet, row, col) -> Option<&SpillShape> { ... }
docs/architecture/2026-05-14-array-formulas-and-spills.md:293:`Workbook::spill_anchors` is RUNTIME-DERIVED state. NOT persisted. On `.qbook` load, `recompute_all` re-derives the table by re-evaluating each formula.
docs/architecture/2026-05-14-array-formulas-and-spills.md:306:2. **Clear any prior spill anchored at this cell.** If `workbook.spill_anchor_at(s, r, c).is_some()`, call `workbook.clear_spill_at(...)`. This is mandatory EVERY re-evaluation — even if the new result will be blocked or change shape. Codex HIGH: the original draft missed this, which would cause the old footprint to block the new spill or itself.
docs/architecture/2026-05-14-array-formulas-and-spills.md:343:   - Anchor for another spill (`workbook.spill_anchor_at(s, r, c).is_some()`).
docs/architecture/2026-05-14-array-formulas-and-spills.md:349:When blocked: anchor cell gets `Value::Error(ErrorValue::Spill)` in computed overlay. NO entries added to `spill_anchors`. NO target overlays written. The formula text stays at the anchor (so re-eval after the blocker clears can spill correctly).
docs/architecture/2026-05-14-array-formulas-and-spills.md:419:1. If `workbook.spill_anchor_at(s, r, c).is_some()`:
docs/architecture/2026-05-14-array-formulas-and-spills.md:479:- If the cell is the anchor (`workbook.spill_anchor_at(s, r, c).is_some()`): emit the formula record normally (anchor has the formula).
docs/architecture/2026-05-14-array-formulas-and-spills.md:483:This requires the saver to consult `spill_target_anchor` per cell — cheap (one HashMap lookup per cell).
docs/architecture/2026-05-14-array-formulas-and-spills.md:578:Per § 6.2 (and W5-96 implementation): collapse the three parallel HashMaps in `FunctionRegistry` into one storage map keyed by name and valued by the `RegisteredFn` tagged enum. Legacy `register` / `register_range_aware` / `register_context_aware` helpers preserved and store the appropriate `RegisteredFn::*` variant; new array-returning functions register via `register_unified`. Eval site dispatches via `lookup_any` and a single match-on-tier (target: W5-101 / Phase 4.7.G).
crates/ql-storage/src/workbook.rs:6://!   wired the real HashMap-backed storage and `Workbook::set_name` API; the
crates/ql-storage/src/workbook.rs:10:use std::collections::HashMap;
crates/ql-storage/src/workbook.rs:61:    entries: HashMap<Arc<str>, NamedTarget>,
crates/ql-storage/src/workbook.rs:213:    /// Iterate all `(name, target)` bindings. Order is HashMap-arbitrary.
crates/ql-storage/src/workbook.rs:221:/// Phase 1 W5-9 adds `formula_cells`: a HashMap keyed by `(sheet, row, col)` mapping
crates/ql-storage/src/workbook.rs:253:    formula_cells: HashMap<(SheetId, RowId, ColId), Arc<str>>,
crates/ql-storage/src/workbook.rs:274:    spill_anchors: crate::SpillAnchorTable,
crates/ql-storage/src/workbook.rs:548:    pub fn spill_anchors(&self) -> &crate::SpillAnchorTable {
crates/ql-storage/src/workbook.rs:549:        &self.spill_anchors
crates/ql-storage/src/workbook.rs:555:    pub fn spill_anchor_at(
crates/ql-storage/src/workbook.rs:561:        self.spill_anchors.anchor_at(sheet, row, col)
crates/ql-storage/src/workbook.rs:574:        self.spill_anchors.target_anchor(sheet, row, col)
crates/ql-storage/src/workbook.rs:586:        self.spill_anchors.register(anchor, shape)
crates/ql-storage/src/workbook.rs:598:    ///   `spill_anchor_at(...).is_some()`.
crates/ql-storage/src/workbook.rs:603:        let shape = self.spill_anchors.unregister(anchor)?;
crates/ql-storage/src/workbook.rs:780:    /// HashMap-arbitrary; callers that need determinism (qbook save) should sort first.
crates/ql-storage/src/workbook.rs:1331:            wb.spill_anchor_at(0, 1, 1),
crates/ql-storage/src/workbook.rs:1366:        assert!(wb.spill_anchor_at(0, 0, 0).is_none());
crates/ql-storage/src/workbook.rs:1421:    fn workbook_spill_anchors_accessor_returns_table() {
crates/ql-storage/src/workbook.rs:1424:        assert_eq!(wb.spill_anchors().len(), 0);
crates/ql-storage/src/workbook.rs:1429:        assert_eq!(wb.spill_anchors().len(), 2);

exec
/bin/zsh -lc "nl -ba crates/ql-storage/src/workbook.rs | sed -n '1310,1435p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1310	    #[test]
  1311	    #[should_panic(expected = "name validation failed")]
  1312	    fn add_sheet_panics_on_duplicate() {
  1313	        // The infallible wrapper panics with a clear message rather
  1314	        // than silently producing a corrupt workbook. Closes Codex
  1315	        // HIGH-1 at the test-friendly path.
  1316	        let mut wb = Workbook::new();
  1317	        wb.add_sheet("Sheet1");
  1318	        wb.add_sheet("Sheet1"); // panics here
  1319	    }
  1320	
  1321	    // ===== W5-101 (Phase 4.7.H) Workbook spill-anchor integration =====
  1322	
  1323	    #[test]
  1324	    fn workbook_register_spill_round_trip() {
  1325	        let mut wb = Workbook::new();
  1326	        wb.add_sheet("S");
  1327	        // Register a 2x3 spill at (0, 1, 1) — covers B2:D3.
  1328	        wb.register_spill((0, 1, 1), crate::SpillShape::new(2, 3))
  1329	            .unwrap();
  1330	        assert_eq!(
  1331	            wb.spill_anchor_at(0, 1, 1),
  1332	            Some(&crate::SpillShape::new(2, 3))
  1333	        );
  1334	        // Reverse lookup at every target cell.
  1335	        for dr in 0..2 {
  1336	            for dc in 0..3 {
  1337	                assert_eq!(
  1338	                    wb.spill_target_anchor(0, 1 + dr, 1 + dc),
  1339	                    Some((0, 1, 1)),
  1340	                    "cell ({},{}) → anchor (0,1,1)",
  1341	                    1 + dr,
  1342	                    1 + dc
  1343	                );
  1344	            }
  1345	        }
  1346	    }
  1347	
  1348	    #[test]
  1349	    fn workbook_clear_spill_at_clears_computed_overlays() {
  1350	        let mut wb = Workbook::new();
  1351	        wb.add_sheet("S");
  1352	        // Register a 1x3 spill at (0, 0, 0) covering A1:C1.
  1353	        wb.register_spill((0, 0, 0), crate::SpillShape::new(1, 3))
  1354	            .unwrap();
  1355	        // Simulate the runtime writing computed values at each target.
  1356	        wb.put_computed_at(0, 0, 0, Value::Number(10.0));
  1357	        wb.put_computed_at(0, 0, 1, Value::Number(20.0));
  1358	        wb.put_computed_at(0, 0, 2, Value::Number(30.0));
  1359	        // Sanity: reads return the computed values.
  1360	        assert_eq!(wb.read(Address::new(0, 0, 0)), Value::Number(10.0));
  1361	        assert_eq!(wb.read(Address::new(0, 0, 1)), Value::Number(20.0));
  1362	        assert_eq!(wb.read(Address::new(0, 0, 2)), Value::Number(30.0));
  1363	        // clear_spill_at: unregisters AND clears overlays at every target.
  1364	        wb.clear_spill_at((0, 0, 0)).unwrap();
  1365	        // Anchor + targets gone from the table.
  1366	        assert!(wb.spill_anchor_at(0, 0, 0).is_none());
  1367	        assert!(wb.spill_target_anchor(0, 0, 1).is_none());
  1368	        // Reads now fall through to Blank (computed overlay cleared).
  1369	        assert_eq!(wb.read(Address::new(0, 0, 0)), Value::Blank);
  1370	        assert_eq!(wb.read(Address::new(0, 0, 1)), Value::Blank);
  1371	        assert_eq!(wb.read(Address::new(0, 0, 2)), Value::Blank);
  1372	    }
  1373	
  1374	    #[test]
  1375	    fn workbook_register_spill_rejects_collision_with_existing() {
  1376	        let mut wb = Workbook::new();
  1377	        wb.add_sheet("S");
  1378	        wb.register_spill((0, 0, 0), crate::SpillShape::new(3, 1))
  1379	            .unwrap();
  1380	        // Second spill at (0, 1, 0) would overlap A2/A3 of the first.
  1381	        let err = wb
  1382	            .register_spill((0, 1, 0), crate::SpillShape::new(3, 1))
  1383	            .unwrap_err();
  1384	        assert!(matches!(
  1385	            err,
  1386	            crate::SpillBlockError::TargetCellOccupied { .. }
  1387	        ));
  1388	    }
  1389	
  1390	    #[test]
  1391	    fn workbook_clear_spill_at_unknown_anchor_errors() {
  1392	        let mut wb = Workbook::new();
  1393	        wb.add_sheet("S");
  1394	        let err = wb.clear_spill_at((0, 5, 5)).unwrap_err();
  1395	        assert_eq!(err.row, 5);
  1396	        assert_eq!(err.col, 5);
  1397	    }
  1398	
  1399	    #[test]
  1400	    fn workbook_clear_spill_at_preserves_user_overlays_outside_rectangle() {
  1401	        // A user-typed value outside the spill rectangle must survive
  1402	        // clear_spill_at. Verifies the clear loop is bounded to the
  1403	        // shape rectangle.
  1404	        let mut wb = Workbook::new();
  1405	        wb.add_sheet("S");
  1406	        wb.register_spill((0, 0, 0), crate::SpillShape::new(1, 3))
  1407	            .unwrap();
  1408	        wb.put_computed_at(0, 0, 0, Value::Number(1.0));
  1409	        wb.put_computed_at(0, 0, 1, Value::Number(2.0));
  1410	        wb.put_computed_at(0, 0, 2, Value::Number(3.0));
  1411	        // User value at A2 (row 1, col 0) — OUTSIDE the 1x3 spill.
  1412	        wb.put_at(0, 1, 0, Value::Number(99.0));
  1413	        wb.clear_spill_at((0, 0, 0)).unwrap();
  1414	        // Spill range cleared.
  1415	        assert_eq!(wb.read(Address::new(0, 0, 0)), Value::Blank);
  1416	        // User value at A2 untouched.
  1417	        assert_eq!(wb.read(Address::new(0, 1, 0)), Value::Number(99.0));
  1418	    }
  1419	
  1420	    #[test]
  1421	    fn workbook_spill_anchors_accessor_returns_table() {
  1422	        let mut wb = Workbook::new();
  1423	        wb.add_sheet("S");
  1424	        assert_eq!(wb.spill_anchors().len(), 0);
  1425	        wb.register_spill((0, 0, 0), crate::SpillShape::new(2, 2))
  1426	            .unwrap();
  1427	        wb.register_spill((0, 5, 5), crate::SpillShape::new(1, 1))
  1428	            .unwrap();
  1429	        assert_eq!(wb.spill_anchors().len(), 2);
  1430	    }
  1431	}

codex
The core implementation matches the advertised two-map design, but the boundary arithmetic is not guarded. I’m checking whether that’s already handled elsewhere in shared coordinate APIs before deciding severity.
exec
/bin/zsh -lc "sed -n '300,370p' docs/architecture/2026-05-14-array-formulas-and-spills.md" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc "sed -n '240,380p' docs/architecture/2026-05-14-array-formulas-and-spills.md" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:

Pre-Phase 4.7 flow: parse → bind → eval (scalar context) → `put_computed` at the cell.

**New flow (Phase 4.7):**

1. Parse + bind formula.
2. **Clear any prior spill anchored at this cell.** If `workbook.spill_anchor_at(s, r, c).is_some()`, call `workbook.clear_spill_at(...)`. This is mandatory EVERY re-evaluation — even if the new result will be blocked or change shape. Codex HIGH: the original draft missed this, which would cause the old footprint to block the new spill or itself.
3. Evaluate at cell-boundary context. Result is `EvalResult::Scalar(v)` or `EvalResult::Array(a)`.
4. **Scalar path:** existing path. `put_computed` at the cell.
5. **Array path:**
   a. Compute spill target rectangle `(row..row+a.rows, col..col+a.cols)`.
   b. **Bounds check:** range must fit within `MAX_ROW` / `MAX_COLUMN`. Out-of-bounds → `Value::Error(ErrorValue::Spill)` at anchor; no targets registered, no overlays written.
   c. **Degenerate check:** `a.is_degenerate()` (rows=0 or cols=0) → `Value::Error(ErrorValue::Calc)` at anchor. Degenerate-array origin is a function-eval contract (e.g. `FILTER` with all-false mask without `if_empty`); the runtime DOES NOT convert this to `#SPILL!`.
   d. **Blocking check (§ 9):** scan target range; any non-anchor cell occupied → `Value::Error(ErrorValue::Spill)` at anchor; no targets registered, no overlays written.
   e. **Register + write:** `workbook.register_spill(anchor, shape)?` then `put_computed_at` for each cell in the rectangle.
6. Op log: emit `Op::PutFormula { text }` only. The spill range is re-derived at replay time via recompute.

### 8.2 Anchor formula uniqueness

Per Excel canon: only the anchor cell has a formula. The other cells in the spill range have NO formula entry. `iter_formulas()` returns only the anchor. Step (5e) above writes ONLY to computed overlays at the target cells — no `put_formula` for non-anchor cells.

When the user types into a cell within a spill range, the spill is invalidated (§ 10).

### 8.3 Re-eval invariant

Step (2) is the heart of correct re-evaluation. The invariant is: **before any cell-boundary eval, the OLD spill state at that anchor MUST be cleared.** This guarantees:

- Shape changes (`=SEQUENCE(5)` → `=SEQUENCE(3)`) work: old A4/A5 entries cleared before new shape evaluates.
- Self-blocking is impossible: the new eval can't see its own old footprint as occupied.
- Blocked re-evaluation also clears the old footprint, so `=SEQUENCE(5)` followed by typing into A3 (which blocks the spill, anchor → `#SPILL!`) doesn't leave stale A1, A2 entries.

If the formula is removed entirely (`clear_formula`), step (2) still runs — the spill is cleared. The formula text removal then leaves the anchor cell blank.

## 9. Spill blocking + `#SPILL!`

### 9.1 Blocking conditions (precedence order)

The anchor evaluates to `Value::Error(ErrorValue::Spill)` when ANY of, in checked order:

1. **Bounds:** spill range extends beyond `MAX_ROW` / `MAX_COLUMN`. (§ 8.1 step b.)
2. **Occupied target cells:** for each non-anchor cell `(s, r, c)` in the target rectangle, ANY of:
   - User overlay non-blank (user-typed value).
   - Computed overlay non-blank AND the cell is NOT a current target of this same anchor (since step 8.1(2) cleared the old footprint, this means another formula's computed output OR another spill anchor's target lives there).
   - Anchor for another spill (`workbook.spill_anchor_at(s, r, c).is_some()`).

Degenerate arrays (rows=0 or cols=0) are NOT a blocking case — they're `#CALC!` per § 8.1 step c. This distinction matters because degenerate-shape origins are function-eval contracts (`FILTER` with all-false mask) and should surface that contract's error, not `#SPILL!`.

### 9.2 What happens at the anchor

When blocked: anchor cell gets `Value::Error(ErrorValue::Spill)` in computed overlay. NO entries added to `spill_anchors`. NO target overlays written. The formula text stays at the anchor (so re-eval after the blocker clears can spill correctly).

### 9.3 `ErrorValue::Spill` — ALREADY IN PLACE

`ErrorValue::Spill` at `crates/ql-types/src/error.rs:38` exists with canonical `"#SPILL!"` sigil + round-trip through `CellWireValue::Error`. Phase 4.7 does NOT add this variant — it consumes it. Codex LOW: original draft incorrectly listed this as new work.

### 9.4 Test surface

- `{1,2;3,4}` at A1 with `B2 = 5` typed → blocked → `A1 = #SPILL!`. **NB:** the 2×2 spill from A1 covers A1:B2 (columns 0-1, rows 0-1); `B2` is in-range. Codex LOW fixed: original draft used `D1` as the blocker, but D1 (column 3) is outside the 2×2 footprint.
- `=SEQUENCE(2,2)` at A1 (clear range) → spills 2×2 → A1=1, B1=2, A2=3, B2=4.
- After spilling, the user types `5` at B2 → spill invalidated → A1 re-evaluates → step 8.1(2) clears A1..B2 → step 8.1(5d) detects B2 has user `5` → A1 = `#SPILL!`.
- After the blocker clears (B2 cleared) → re-recompute → spill restored.
- `FILTER({1;2;3}, {FALSE;FALSE;FALSE})` (degenerate result) without `if_empty` → `#CALC!`, NOT `#SPILL!`.
- `=SEQUENCE(3)` at row `MAX_ROW - 1` → out-of-bounds spill → `#SPILL!`.

## 10. Spill invalidation (calcgraph) — REDESIGN per Codex HIGH

### 10.1 Producer-alias model (LAZY, no parallel adjacency)

Codex HIGH: the original draft's "parallel `outgoing_spill_targets` adjacency + eager `CellNode` creation" plan was incompatible with the existing calcgraph scheduler. `outgoing` edges mean "formula DEPENDS ON producer." Adding spill-target CellNodes as eager nodes that mimic formula nodes would either lose ordering or schedule non-formula targets through Tarjan.

**Revised model:** spill targets are PRODUCER ALIASES at dep-extraction time.

 succeeded in 0ms:

## 7. Spill anchor data model (storage)

### 7.1 New `SpillAnchorTable` in `ql-storage`

```rust
pub struct SpillAnchorTable {
    /// Anchor (sheet, row, col) → shape (rows, cols).
    /// One entry per spilling formula.
    anchors: HashMap<(SheetId, RowId, ColId), SpillShape>,
    /// Reverse map: each spilled-into cell → its anchor.
    /// Used for fast "is this cell part of a spill?" lookup
    /// at user-write time.
    targets: HashMap<(SheetId, RowId, ColId), (SheetId, RowId, ColId)>,
}

pub struct SpillShape {
    pub rows: u32,
    pub cols: u32,
}
```

**Invariant:** `targets[(s, r, c)] = (anchor_s, anchor_r, anchor_c)` iff `(anchor_s, anchor_r, anchor_c)` exists in `anchors` AND `(r, c)` is inside the rectangle `(anchor_r..anchor_r+shape.rows, anchor_c..anchor_c+shape.cols)`.

The anchor cell itself is BOTH an anchor AND its own target (the (0,0) of the spill range). Both maps include it.

### 7.2 Workbook integration — MAP-ONLY `SpillAnchorTable`

Codex MEDIUM: keep `SpillAnchorTable` map-only; overlay clearing lives in `Workbook` (which owns sheet/overlay access).

```rust
// crates/ql-storage/src/workbook.rs
impl Workbook {
    pub fn spill_anchors(&self) -> &SpillAnchorTable { &self.spill_anchors }
    pub fn spill_anchor_at(&self, sheet, row, col) -> Option<&SpillShape> { ... }
    pub fn spill_target_anchor(&self, sheet, row, col) -> Option<(SheetId, RowId, ColId)> { ... }

    /// Map-only mutator. Errors on rectangle collision; does NOT touch
    /// computed overlays. Production callers (runtime) couple this with
    /// `put_computed_at` for each target cell.
    pub fn register_spill(&mut self, anchor, shape) -> Result<(), SpillBlockError> { ... }

    /// Workbook-layer convenience: unregister anchor + clear computed
    /// overlays at every target cell. Used by runtime when re-evaluating
    /// a spill (§ 8.3) and when clearing a formula at a spill anchor.
    pub fn clear_spill_at(&mut self, anchor) -> Result<(), SpillNotFoundError> { ... }
}
```

`clear_spill_at` is the named workbook-layer operation that owns the overlay sweep. `SpillAnchorTable` itself just tracks the table state.

### 7.3 Persistence

`Workbook::spill_anchors` is RUNTIME-DERIVED state. NOT persisted. On `.qbook` load, `recompute_all` re-derives the table by re-evaluating each formula.

**No schema bump for Phase 4.7.** This is a property of the design — the computed overlay already round-trips correctly via the v4 envelope's per-sheet overlay; spill anchors are re-derived from formulas.

## 8. Spill writeback (runtime) — REDESIGN per Codex HIGH

### 8.1 `WorkbookRuntime::set_formula` extension

Pre-Phase 4.7 flow: parse → bind → eval (scalar context) → `put_computed` at the cell.

**New flow (Phase 4.7):**

1. Parse + bind formula.
2. **Clear any prior spill anchored at this cell.** If `workbook.spill_anchor_at(s, r, c).is_some()`, call `workbook.clear_spill_at(...)`. This is mandatory EVERY re-evaluation — even if the new result will be blocked or change shape. Codex HIGH: the original draft missed this, which would cause the old footprint to block the new spill or itself.
3. Evaluate at cell-boundary context. Result is `EvalResult::Scalar(v)` or `EvalResult::Array(a)`.
4. **Scalar path:** existing path. `put_computed` at the cell.
5. **Array path:**
   a. Compute spill target rectangle `(row..row+a.rows, col..col+a.cols)`.
   b. **Bounds check:** range must fit within `MAX_ROW` / `MAX_COLUMN`. Out-of-bounds → `Value::Error(ErrorValue::Spill)` at anchor; no targets registered, no overlays written.
   c. **Degenerate check:** `a.is_degenerate()` (rows=0 or cols=0) → `Value::Error(ErrorValue::Calc)` at anchor. Degenerate-array origin is a function-eval contract (e.g. `FILTER` with all-false mask without `if_empty`); the runtime DOES NOT convert this to `#SPILL!`.
   d. **Blocking check (§ 9):** scan target range; any non-anchor cell occupied → `Value::Error(ErrorValue::Spill)` at anchor; no targets registered, no overlays written.
   e. **Register + write:** `workbook.register_spill(anchor, shape)?` then `put_computed_at` for each cell in the rectangle.
6. Op log: emit `Op::PutFormula { text }` only. The spill range is re-derived at replay time via recompute.

### 8.2 Anchor formula uniqueness

Per Excel canon: only the anchor cell has a formula. The other cells in the spill range have NO formula entry. `iter_formulas()` returns only the anchor. Step (5e) above writes ONLY to computed overlays at the target cells — no `put_formula` for non-anchor cells.

When the user types into a cell within a spill range, the spill is invalidated (§ 10).

### 8.3 Re-eval invariant

Step (2) is the heart of correct re-evaluation. The invariant is: **before any cell-boundary eval, the OLD spill state at that anchor MUST be cleared.** This guarantees:

- Shape changes (`=SEQUENCE(5)` → `=SEQUENCE(3)`) work: old A4/A5 entries cleared before new shape evaluates.
- Self-blocking is impossible: the new eval can't see its own old footprint as occupied.
- Blocked re-evaluation also clears the old footprint, so `=SEQUENCE(5)` followed by typing into A3 (which blocks the spill, anchor → `#SPILL!`) doesn't leave stale A1, A2 entries.

If the formula is removed entirely (`clear_formula`), step (2) still runs — the spill is cleared. The formula text removal then leaves the anchor cell blank.

## 9. Spill blocking + `#SPILL!`

### 9.1 Blocking conditions (precedence order)

The anchor evaluates to `Value::Error(ErrorValue::Spill)` when ANY of, in checked order:

1. **Bounds:** spill range extends beyond `MAX_ROW` / `MAX_COLUMN`. (§ 8.1 step b.)
2. **Occupied target cells:** for each non-anchor cell `(s, r, c)` in the target rectangle, ANY of:
   - User overlay non-blank (user-typed value).
   - Computed overlay non-blank AND the cell is NOT a current target of this same anchor (since step 8.1(2) cleared the old footprint, this means another formula's computed output OR another spill anchor's target lives there).
   - Anchor for another spill (`workbook.spill_anchor_at(s, r, c).is_some()`).

Degenerate arrays (rows=0 or cols=0) are NOT a blocking case — they're `#CALC!` per § 8.1 step c. This distinction matters because degenerate-shape origins are function-eval contracts (`FILTER` with all-false mask) and should surface that contract's error, not `#SPILL!`.

### 9.2 What happens at the anchor

When blocked: anchor cell gets `Value::Error(ErrorValue::Spill)` in computed overlay. NO entries added to `spill_anchors`. NO target overlays written. The formula text stays at the anchor (so re-eval after the blocker clears can spill correctly).

### 9.3 `ErrorValue::Spill` — ALREADY IN PLACE

`ErrorValue::Spill` at `crates/ql-types/src/error.rs:38` exists with canonical `"#SPILL!"` sigil + round-trip through `CellWireValue::Error`. Phase 4.7 does NOT add this variant — it consumes it. Codex LOW: original draft incorrectly listed this as new work.

### 9.4 Test surface

- `{1,2;3,4}` at A1 with `B2 = 5` typed → blocked → `A1 = #SPILL!`. **NB:** the 2×2 spill from A1 covers A1:B2 (columns 0-1, rows 0-1); `B2` is in-range. Codex LOW fixed: original draft used `D1` as the blocker, but D1 (column 3) is outside the 2×2 footprint.
- `=SEQUENCE(2,2)` at A1 (clear range) → spills 2×2 → A1=1, B1=2, A2=3, B2=4.
- After spilling, the user types `5` at B2 → spill invalidated → A1 re-evaluates → step 8.1(2) clears A1..B2 → step 8.1(5d) detects B2 has user `5` → A1 = `#SPILL!`.
- After the blocker clears (B2 cleared) → re-recompute → spill restored.
- `FILTER({1;2;3}, {FALSE;FALSE;FALSE})` (degenerate result) without `if_empty` → `#CALC!`, NOT `#SPILL!`.
- `=SEQUENCE(3)` at row `MAX_ROW - 1` → out-of-bounds spill → `#SPILL!`.

## 10. Spill invalidation (calcgraph) — REDESIGN per Codex HIGH

### 10.1 Producer-alias model (LAZY, no parallel adjacency)

Codex HIGH: the original draft's "parallel `outgoing_spill_targets` adjacency + eager `CellNode` creation" plan was incompatible with the existing calcgraph scheduler. `outgoing` edges mean "formula DEPENDS ON producer." Adding spill-target CellNodes as eager nodes that mimic formula nodes would either lose ordering or schedule non-formula targets through Tarjan.

**Revised model:** spill targets are PRODUCER ALIASES at dep-extraction time.

```
Spill anchor at A1: SEQUENCE(3) → produces A1, A2, A3.
Reader formula at B1: =A2 + 1.

Old (wrong) model:
  CellNode(A2) added to graph; outgoing edge from CellNode(A2) → FormulaNode(A1).
  B1 depends on CellNode(A2), which dirties on CellNode(A2)-touch.

New (right) model:

exec
/bin/zsh -lc 'rg -n "2122|21 spill|6 workbook|W5-101" docs crates Cargo.toml' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
docs/architecture/2026-05-14-array-formulas-and-spills.md:450:| 8 | **4.7.H** (W5-101) | `SpillAnchorTable` storage (map-only) + `Workbook::clear_spill_at` (overlay-aware) | Self + Sonnet |
docs/architecture/2026-05-14-array-formulas-and-spills.md:578:Per § 6.2 (and W5-96 implementation): collapse the three parallel HashMaps in `FunctionRegistry` into one storage map keyed by name and valued by the `RegisteredFn` tagged enum. Legacy `register` / `register_range_aware` / `register_context_aware` helpers preserved and store the appropriate `RegisteredFn::*` variant; new array-returning functions register via `register_unified`. Eval site dispatches via `lookup_any` and a single match-on-tier (target: W5-101 / Phase 4.7.G).
crates/ql-storage/src/spill.rs:5://! **Phase 4.7.H (W5-101):** introduces the storage data structures for
crates/ql-storage/src/workbook.rs:267:    /// **W5-101 (Phase 4.7.H):** spill-anchor table. Workbook-level
crates/ql-storage/src/workbook.rs:543:    // ===== W5-101 (Phase 4.7.H) spill-anchor API =====
crates/ql-storage/src/workbook.rs:545:    /// **W5-101 (Phase 4.7.H):** read access to the spill-anchor table.
crates/ql-storage/src/workbook.rs:552:    /// **W5-101 (Phase 4.7.H):** convenience: is `(sheet, row, col)` an
crates/ql-storage/src/workbook.rs:564:    /// **W5-101 (Phase 4.7.H):** reverse lookup — if `(sheet, row, col)`
crates/ql-storage/src/workbook.rs:577:    /// **W5-101 (Phase 4.7.H):** register a new spill. Map-only — does
crates/ql-storage/src/workbook.rs:589:    /// **W5-101 (Phase 4.7.H):** workbook-layer convenience: unregister
crates/ql-storage/src/workbook.rs:1321:    // ===== W5-101 (Phase 4.7.H) Workbook spill-anchor integration =====
docs/audits/2026-05-13-w5-49-50-51-megaudit-codex.txt:5779:  2122	        assert_eq!(
docs/audits/2026-05-14-phase-4.7.F-codex-review.txt:191:449:| 7 | **4.7.G** (W5-101) | Evaluator: `EvalResult::Array(ArrayValue)`; array-in-scalar-context → `#CALC!`; array-in-aggregate-context feeds cells | Self + Sonnet |
docs/audits/2026-05-14-phase-4.7.F-codex-review.txt:210:578:Per § 6.2 (and W5-96 implementation): collapse the three parallel HashMaps in `FunctionRegistry` into one storage map keyed by name and valued by the `RegisteredFn` tagged enum. Legacy `register` / `register_range_aware` / `register_context_aware` helpers preserved and store the appropriate `RegisteredFn::*` variant; new array-returning functions register via `register_unified`. Eval site dispatches via `lookup_any` and a single match-on-tier (target: W5-101 / Phase 4.7.G).
docs/audits/2026-05-14-phase-4.7.F-codex-review.txt:557:    97	    /// Eval-site dispatch: Phase 4.7.G (W5-101). The runtime materializes
docs/audits/2026-05-14-phase-4.7.F-codex-review.txt:6423:+    /// Eval-site dispatch: Phase 4.7.G (W5-101). The runtime materializes
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:598:      sequential lookups. Eval-site migration deferred to W5-101
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:611:    migration to one `match RegisteredFn` lands in W5-101 (Phase 4.7.G)
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:670:    22	//! match-on-enum lands in W5-101 (Phase 4.7.G) when the array-eval path
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:675:    27	//! dispatch in W5-101 will route them through a new `match` arm that
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:708:    60	/// The eval site (W5-101 / Phase 4.7.G) materializes `FunctionArg` from
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:746:    98	/// lets us add workbook-level state later (W5-101+) without changing the
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:770:   122	/// `lookup_context_aware`); W5-101 (Phase 4.7.G) migrates that site
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:927:   279	    /// path (W5-101 / Phase 4.7.G).
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:939:   291	    /// calls. Migrated callers (W5-101+) use this; pre-W5-96 callers
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:1907:docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:611:    migration to one `match RegisteredFn` lands in W5-101 (Phase 4.7.G)
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:1917:docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:770:   122	/// `lookup_context_aware`); W5-101 (Phase 4.7.G) migrates that site
docs/audits/2026-05-14-phase-4.7.B-codex-review.txt:4086:426:| 7 | **4.7.G** (W5-101) | Evaluator: `EvalResult::Array(ArrayValue)`; array-in-scalar-context → `#CALC!`; array-in-aggregate-context feeds cells | Self + Sonnet |
docs/audits/2026-05-14-phase-4.7-design-codex-review.txt:1112:| **4.7.G** (W5-101) | `SpillAnchorTable` storage; reverse map; tests | Self + Sonnet |
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:14:Independent Codex pull-up review of Phase 4.7.H (W5-101) — SpillAnchorTable storage + Workbook::clear_spill_at.
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:19:3. The W5-101 commit: run `git show 57db4639473 --stat` then inspect:
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:35:- 21 module tests + 6 workbook tests, 2122 workspace tests total.
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:596:    W5-101 / Engine Phase 4.7.H — SpillAnchorTable storage + Workbook::clear_spill_at
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:652:    Tests (+27 from 2101 → 2122 - some baseline drift):
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:653:    - 21 spill-module tests covering register/unregister/lookup, 1x1 /
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:658:    - 6 workbook integration tests:
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:668:    - cargo test --workspace: 2122 passed / 0 failed
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:694:     5	//! **Phase 4.7.H (W5-101):** introduces the storage data structures for
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:1452:   267	    /// **W5-101 (Phase 4.7.H):** spill-anchor table. Workbook-level
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:2047:   543	    // ===== W5-101 (Phase 4.7.H) spill-anchor API =====
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:2049:   545	    /// **W5-101 (Phase 4.7.H):** read access to the spill-anchor table.
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:2056:   552	    /// **W5-101 (Phase 4.7.H):** convenience: is `(sheet, row, col)` an
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:2068:   564	    /// **W5-101 (Phase 4.7.H):** reverse lookup — if `(sheet, row, col)`
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:2081:   577	    /// **W5-101 (Phase 4.7.H):** register a new spill. Map-only — does
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:2093:   589	    /// **W5-101 (Phase 4.7.H):** workbook-layer convenience: unregister
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:2633:267:    /// **W5-101 (Phase 4.7.H):** spill-anchor table. Workbook-level
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:2638:543:    // ===== W5-101 (Phase 4.7.H) spill-anchor API =====
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:2639:545:    /// **W5-101 (Phase 4.7.H):** read access to the spill-anchor table.
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:2650:577:    /// **W5-101 (Phase 4.7.H):** register a new spill. Map-only — does
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:2660:1321:    // ===== W5-101 (Phase 4.7.H) Workbook spill-anchor integration =====
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:2723:docs/architecture/2026-05-14-array-formulas-and-spills.md:578:Per § 6.2 (and W5-96 implementation): collapse the three parallel HashMaps in `FunctionRegistry` into one storage map keyed by name and valued by the `RegisteredFn` tagged enum. Legacy `register` / `register_range_aware` / `register_context_aware` helpers preserved and store the appropriate `RegisteredFn::*` variant; new array-returning functions register via `register_unified`. Eval site dispatches via `lookup_any` and a single match-on-tier (target: W5-101 / Phase 4.7.G).
docs/audits/2026-05-14-phase-4.7.H-codex-review.txt:2760:  1321	    // ===== W5-101 (Phase 4.7.H) Workbook spill-anchor integration =====
docs/audits/2026-05-14-phase-4.7.G-sonnet-review.md:225:**Finding:** Design doc § 11 table lists `4.7.G (W5-101)`, but the actual commit is tagged `W5-100`. The numbering drifted by one because W5-98 bundled sub-phases 4.7.D + 4.7.E, causing W5-99 to land 4.7.F (design had it at W5-100) and W5-100 to land 4.7.G (design had it at W5-101).
docs/audits/2026-05-14-phase-4.7.G-sonnet-review.md:227:The inline design doc references also say `W5-101 (Phase 4.7.G)` in two places:
docs/audits/2026-05-14-phase-4.7.G-sonnet-review.md:228:- `docs/architecture/2026-05-14-array-formulas-and-spills.md` line 175: `"W5-101 (Phase 4.7.G)"`.
docs/audits/2026-05-14-phase-4.7.G-sonnet-review.md:229:- Same doc line 201: `"(W5-101 / Phase 4.7.G migration target)"`.
docs/audits/2026-05-14-phase-4.7.G-sonnet-review.md:233:**Suggested fix:** Update the § 11 table row and the two inline mentions in the design doc to say `4.7.G (W5-100)`. Future sub-phases should be renumbered accordingly (4.7.H → W5-101, etc.).
docs/audits/2026-05-14-phase-4.7.G-sonnet-review.md:237:**Finding:** Design doc § 6.2 states: "Caller-side dispatch in `ql-exec::eval` (W5-101 / Phase 4.7.G migration target): ... `match registry.lookup_any(name) {...}`". The implication is that Phase 4.7.G should migrate `eval_scalar_with_cache`'s three-branch dispatch (`lookup_range_aware` → `lookup_context_aware` → `lookup`) to the unified `lookup_any` + match-on-tier approach.
docs/audits/2026-05-14-phase-4.7.G-sonnet-review.md:302:   Says `4.7.G (W5-101)` — should be `4.7.G (W5-100)`. Renumber downstream rows (4.7.H → W5-101, etc.) to prevent reader confusion.
crates/ql-exec/src/plan.rs:102:    /// Eval-site dispatch: Phase 4.7.G (W5-101). The runtime materializes
crates/ql-functions/src/registry.rs:22://! match-on-enum lands in W5-101 (Phase 4.7.G) when the array-eval path
crates/ql-functions/src/registry.rs:27://! dispatch in W5-101 will route them through a new `match` arm that
crates/ql-functions/src/registry.rs:60:/// The eval site (W5-101 / Phase 4.7.G) materializes `FunctionArg` from
crates/ql-functions/src/registry.rs:98:/// lets us add workbook-level state later (W5-101+) without changing the
crates/ql-functions/src/registry.rs:122:/// `lookup_context_aware`); W5-101 (Phase 4.7.G) migrates that site
crates/ql-functions/src/registry.rs:279:    /// path (W5-101 / Phase 4.7.G).
crates/ql-functions/src/registry.rs:291:    /// calls. Migrated callers (W5-101+) use this; pre-W5-96 callers

exec
/bin/zsh -lc 'git rev-parse --abbrev-ref HEAD && git rev-parse HEAD && git status --short' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 842ms:
 M crates/ql-storage/src/spill.rs
 M crates/ql-storage/src/workbook.rs
?? ../2026-05-11-222312-this-session-is-being-continued-from-a-previous-c.txt
?? ../2026-05-13-003338-this-session-is-being-continued-from-a-previous-c.txt
?? ../node_modules
?? .plans/
?? docs/audits/2026-05-14-phase-4.7.H-codex-review.txt

exec
/bin/zsh -lc "git diff -- crates/ql-storage/src/spill.rs crates/ql-storage/src/workbook.rs | sed -n '1,260p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-2oKtCAxa' (errno=Operation not permitted)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-D0nmwHhP' (errno=Operation not permitted)
diff --git a/quantbook-engine/crates/ql-storage/src/spill.rs b/quantbook-engine/crates/ql-storage/src/spill.rs
index e08ff4a382c..2f32d6ff4cc 100644
--- a/quantbook-engine/crates/ql-storage/src/spill.rs
+++ b/quantbook-engine/crates/ql-storage/src/spill.rs
@@ -489,4 +489,4 @@ mod tests {
         assert_eq!(SpillShape::new(3, 4).cell_count(), 12);
         assert_eq!(SpillShape::new(1, 1).cell_count(), 1);
     }
-}
\ No newline at end of file
+}
diff --git a/quantbook-engine/crates/ql-storage/src/workbook.rs b/quantbook-engine/crates/ql-storage/src/workbook.rs
index 0675d4fa969..1d371f96783 100644
--- a/quantbook-engine/crates/ql-storage/src/workbook.rs
+++ b/quantbook-engine/crates/ql-storage/src/workbook.rs
@@ -1426,4 +1426,6 @@ mod tests {
             .unwrap();
         wb.register_spill((0, 5, 5), crate::SpillShape::new(1, 1))
             .unwrap();
-        assert_eq!(wb.s
\ No newline at end of file
+        assert_eq!(wb.spill_anchors().len(), 2);
+    }
+}

codex
One important wrinkle: the working tree has edits in the reviewed files. They only fix file endings/truncation in the checked-out copy, while `HEAD` itself appears to contain a truncated `workbook.rs` tail. I’m checking the committed blob directly before finalizing, because the request is specifically for commit `57db4639473`.
exec
/bin/zsh -lc 'git show 57db463947320f312fb19f4819da5b06a4df019a:quantbook-engine/crates/ql-storage/src/workbook.rs | tail -n 30 | nl -ba' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc 'git show 57db463947320f312fb19f4819da5b06a4df019a:quantbook-engine/crates/ql-storage/src/spill.rs | tail -n 20 | nl -ba' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-cD7B7O0b' (errno=Operation not permitted)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-Ro4WFkqO' (errno=Operation not permitted)
     1	    #[test]
     2	    fn iter_anchors_yields_all_registered() {
     3	        let mut t = SpillAnchorTable::new();
     4	        t.register(at(0, 0, 0), SpillShape::new(1, 1)).unwrap();
     5	        t.register(at(0, 5, 5), SpillShape::new(2, 2)).unwrap();
     6	        let anchors: Vec<_> = t.iter_anchors().collect();
     7	        assert_eq!(anchors.len(), 2);
     8	        // Iteration order is unspecified; collect + assert membership.
     9	        let cells: std::collections::HashSet<_> = anchors.iter().map(|(c, _)| *c).collect();
    10	        assert!(cells.contains(&at(0, 0, 0)));
    11	        assert!(cells.contains(&at(0, 5, 5)));
    12	    }
    13	
    14	    #[test]
    15	    fn spill_shape_cell_count() {
    16	        assert_eq!(SpillShape::new(5, 1).cell_count(), 5);
    17	        assert_eq!(SpillShape::new(3, 4).cell_count(), 12);
    18	        assert_eq!(SpillShape::new(1, 1).cell_count(), 1);
    19	    }
    20	}
 succeeded in 0ms:
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-KkeqR85J' (errno=Operation not permitted)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-0kRVI8PO' (errno=Operation not permitted)
     1	    fn workbook_clear_spill_at_preserves_user_overlays_outside_rectangle() {
     2	        // A user-typed value outside the spill rectangle must survive
     3	        // clear_spill_at. Verifies the clear loop is bounded to the
     4	        // shape rectangle.
     5	        let mut wb = Workbook::new();
     6	        wb.add_sheet("S");
     7	        wb.register_spill((0, 0, 0), crate::SpillShape::new(1, 3))
     8	            .unwrap();
     9	        wb.put_computed_at(0, 0, 0, Value::Number(1.0));
    10	        wb.put_computed_at(0, 0, 1, Value::Number(2.0));
    11	        wb.put_computed_at(0, 0, 2, Value::Number(3.0));
    12	        // User value at A2 (row 1, col 0) — OUTSIDE the 1x3 spill.
    13	        wb.put_at(0, 1, 0, Value::Number(99.0));
    14	        wb.clear_spill_at((0, 0, 0)).unwrap();
    15	        // Spill range cleared.
    16	        assert_eq!(wb.read(Address::new(0, 0, 0)), Value::Blank);
    17	        // User value at A2 untouched.
    18	        assert_eq!(wb.read(Address::new(0, 1, 0)), Value::Number(99.0));
    19	    }
    20	
    21	    #[test]
    22	    fn workbook_spill_anchors_accessor_returns_table() {
    23	        let mut wb = Workbook::new();
    24	        wb.add_sheet("S");
    25	        assert_eq!(wb.spill_anchors().len(), 0);
    26	        wb.register_spill((0, 0, 0), crate::SpillShape::new(2, 2))
    27	            .unwrap();
    28	        wb.register_spill((0, 5, 5), crate::SpillShape::new(1, 1))
    29	            .unwrap();
    30	        assert_eq!(wb.s
exec
/bin/zsh -lc 'git show 57db463947320f312fb19f4819da5b06a4df019a:quantbook-engine/crates/ql-storage/src/workbook.rs | nl -ba | tail -n 12' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-6QUPAGD4' (errno=Operation not permitted)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-yzZ4VVwL' (errno=Operation not permitted)
  1418	    }
  1419	
  1420	    #[test]
  1421	    fn workbook_spill_anchors_accessor_returns_table() {
  1422	        let mut wb = Workbook::new();
  1423	        wb.add_sheet("S");
  1424	        assert_eq!(wb.spill_anchors().len(), 0);
  1425	        wb.register_spill((0, 0, 0), crate::SpillShape::new(2, 2))
  1426	            .unwrap();
  1427	        wb.register_spill((0, 5, 5), crate::SpillShape::new(1, 1))
  1428	            .unwrap();
  1429	        assert_eq!(wb.s
codex
**Top Finding**

FLAG HIGH: the committed blob for `57db4639473` appears truncated and does not compile: `crates/ql-storage/src/workbook.rs:1429` ends at `assert_eq!(wb.s` with no closing test/module braces. The working tree has an uncommitted fix for this tail, but the requested commit as-is cannot truthfully have passed `cargo test --workspace`; amend/recommit the fixed tail before accepting W5-101.

**Claim Verdicts**

1. SpillAnchorTable is map-only: PASS. `SpillAnchorTable` only mutates `anchors` and `targets`; overlay clearing is only in `Workbook::clear_spill_at` at `crates/ql-storage/src/workbook.rs:603-615`.

2. `Workbook::clear_spill_at` unregisters plus clears computed overlays: PASS. It calls `unregister` then `sheet.clear_computed(...)` over the target rectangle at `workbook.rs:603-615`.

3. Register is atomic: PASS for returned collision errors, FLAG LOW for panic-atomicity. Collision pre-scan happens before mutation at `spill.rs:204-222`, but `anchors.insert` happens before target inserts at `spill.rs:224-229`; a recoverable panic during target insertion would leave partial state. Suggested fix: precompute target cells into a `Vec`, `try_reserve` both maps, then insert only after allocation succeeds.

4. Anchor cell is its own target: PASS. The insert loop includes `dr=0, dc=0` at `spill.rs:225-229`, and tests assert this at `spill.rs:292-299`.

5. Both-or-neither invariant: PASS for normal in-bounds registrations. `register` inserts all rectangle targets and `unregister` removes the same rectangle at `spill.rs:224-251`; see overflow flag below for invalid boundary cases.

6. Disjoint footprints invariant: PASS. `register` rejects any proposed cell already present in `targets` at `spill.rs:208-220`.

7. Degenerate shapes rejected at register time: PASS. `rows == 0 || cols == 0` returns `DegenerateShape` at `spill.rs:190-195`.

8. 21 module tests + 6 workbook tests, 2122 workspace tests total: FLAG HIGH for the commit. The test count matches the source shape and commit message, but committed `workbook.rs:1429` is syntactically truncated, so the committed test result claim is not credible until the tail fix is committed.

**Specific Scrutiny**

1. Atomicity/panic path: FLAG LOW at `spill.rs:224-229`. Error atomicity is correct; panic atomicity is not formally transactional after `anchors.insert`. OOM normally aborts, but the safer pattern is `try_reserve` plus precomputed target cells before mutation.

2. Integer overflow / worksheet bounds: FLAG MEDIUM at `spill.rs:208-210`, `spill.rs:225-227`, and `workbook.rs:610-612`. `arow + dr` / `acol + dc` is unchecked and storage accepts coordinates beyond `MAX_ROW` / `MAX_COLUMN`; release builds can wrap near `u32::MAX`, and valid `RowId` values above Excel bounds can be registered. Suggested fix: add bounds validation using `checked_add(shape.rows - 1)` and `checked_add(shape.cols - 1)`, requiring end row/col `<= ql_types::MAX_ROW/MAX_COLUMN`, and return a structural error before scanning.

3. `clear_spill_at` miss semantics: FLAG LOW at `workbook.rs:599-603`. The current strict API is documented, but the future runtime path would be cleaner with an idempotent wrapper. Suggested fix: keep `try_clear_spill_at` or current `clear_spill_at` for strict callers, and add `clear_spill_if_present(...) -> bool` or `-> Option<SpillShape>` for re-eval.

4. `clear_spill_at` overflow: FLAG MEDIUM at `workbook.rs:610-612`. Same unchecked rectangle math; if an invalid/overflowed spill is registered, clearing can miss or clear wrapped cells. Suggested fix: centralize a checked spill-cell iterator and use it in both register/unregister/clear.

5. Reverse map memory: PASS. A 1000-row spill means 1000 `targets` entries, bounded to storage-side O(spill footprint); no calcgraph per-target node cost is introduced here.

6. `SpillShape` Hash derive: PASS. `#[derive(Hash)]` at `spill.rs:57` is fine; hash collisions are handled by `HashMap`, and type-level key spaces do not create correctness issues.

7. `iter_anchors` nondeterminism: PASS with future-save caveat. It explicitly documents unspecified order at `spill.rs:255-258`; persistence should collect and sort, same pattern as `Workbook::iter_formulas` at `workbook.rs:779-780`.

8. Cross-sheet spill: PASS. The anchor key includes `SheetId`, but rectangle iteration keeps `asheet` constant at `spill.rs:207-210`; tests only verify same-row/col spills on different sheets are independent at `spill.rs:459-471`, not cross-sheet ranges.

9. Optional overflow tests: recommended. Add tests for `register((0, MAX_ROW, 0), SpillShape::new(2, 1))`, `register((0, 0, MAX_COLUMN), SpillShape::new(1, 2))`, and near-`u32::MAX` anchors to lock the checked-arithmetic behavior.
tokens used
75,764
**Top Finding**

FLAG HIGH: the committed blob for `57db4639473` appears truncated and does not compile: `crates/ql-storage/src/workbook.rs:1429` ends at `assert_eq!(wb.s` with no closing test/module braces. The working tree has an uncommitted fix for this tail, but the requested commit as-is cannot truthfully have passed `cargo test --workspace`; amend/recommit the fixed tail before accepting W5-101.

**Claim Verdicts**

1. SpillAnchorTable is map-only: PASS. `SpillAnchorTable` only mutates `anchors` and `targets`; overlay clearing is only in `Workbook::clear_spill_at` at `crates/ql-storage/src/workbook.rs:603-615`.

2. `Workbook::clear_spill_at` unregisters plus clears computed overlays: PASS. It calls `unregister` then `sheet.clear_computed(...)` over the target rectangle at `workbook.rs:603-615`.

3. Register is atomic: PASS for returned collision errors, FLAG LOW for panic-atomicity. Collision pre-scan happens before mutation at `spill.rs:204-222`, but `anchors.insert` happens before target inserts at `spill.rs:224-229`; a recoverable panic during target insertion would leave partial state. Suggested fix: precompute target cells into a `Vec`, `try_reserve` both maps, then insert only after allocation succeeds.

4. Anchor cell is its own target: PASS. The insert loop includes `dr=0, dc=0` at `spill.rs:225-229`, and tests assert this at `spill.rs:292-299`.

5. Both-or-neither invariant: PASS for normal in-bounds registrations. `register` inserts all rectangle targets and `unregister` removes the same rectangle at `spill.rs:224-251`; see overflow flag below for invalid boundary cases.

6. Disjoint footprints invariant: PASS. `register` rejects any proposed cell already present in `targets` at `spill.rs:208-220`.

7. Degenerate shapes rejected at register time: PASS. `rows == 0 || cols == 0` returns `DegenerateShape` at `spill.rs:190-195`.

8. 21 module tests + 6 workbook tests, 2122 workspace tests total: FLAG HIGH for the commit. The test count matches the source shape and commit message, but committed `workbook.rs:1429` is syntactically truncated, so the committed test result claim is not credible until the tail fix is committed.

**Specific Scrutiny**

1. Atomicity/panic path: FLAG LOW at `spill.rs:224-229`. Error atomicity is correct; panic atomicity is not formally transactional after `anchors.insert`. OOM normally aborts, but the safer pattern is `try_reserve` plus precomputed target cells before mutation.

2. Integer overflow / worksheet bounds: FLAG MEDIUM at `spill.rs:208-210`, `spill.rs:225-227`, and `workbook.rs:610-612`. `arow + dr` / `acol + dc` is unchecked and storage accepts coordinates beyond `MAX_ROW` / `MAX_COLUMN`; release builds can wrap near `u32::MAX`, and valid `RowId` values above Excel bounds can be registered. Suggested fix: add bounds validation using `checked_add(shape.rows - 1)` and `checked_add(shape.cols - 1)`, requiring end row/col `<= ql_types::MAX_ROW/MAX_COLUMN`, and return a structural error before scanning.

3. `clear_spill_at` miss semantics: FLAG LOW at `workbook.rs:599-603`. The current strict API is documented, but the future runtime path would be cleaner with an idempotent wrapper. Suggested fix: keep `try_clear_spill_at` or current `clear_spill_at` for strict callers, and add `clear_spill_if_present(...) -> bool` or `-> Option<SpillShape>` for re-eval.

4. `clear_spill_at` overflow: FLAG MEDIUM at `workbook.rs:610-612`. Same unchecked rectangle math; if an invalid/overflowed spill is registered, clearing can miss or clear wrapped cells. Suggested fix: centralize a checked spill-cell iterator and use it in both register/unregister/clear.

5. Reverse map memory: PASS. A 1000-row spill means 1000 `targets` entries, bounded to storage-side O(spill footprint); no calcgraph per-target node cost is introduced here.

6. `SpillShape` Hash derive: PASS. `#[derive(Hash)]` at `spill.rs:57` is fine; hash collisions are handled by `HashMap`, and type-level key spaces do not create correctness issues.

7. `iter_anchors` nondeterminism: PASS with future-save caveat. It explicitly documents unspecified order at `spill.rs:255-258`; persistence should collect and sort, same pattern as `Workbook::iter_formulas` at `workbook.rs:779-780`.

8. Cross-sheet spill: PASS. The anchor key includes `SheetId`, but rectangle iteration keeps `asheet` constant at `spill.rs:207-210`; tests only verify same-row/col spills on different sheets are independent at `spill.rs:459-471`, not cross-sheet ranges.

9. Optional overflow tests: recommended. Add tests for `register((0, MAX_ROW, 0), SpillShape::new(2, 1))`, `register((0, 0, MAX_COLUMN), SpillShape::new(1, 2))`, and near-`u32::MAX` anchors to lock the checked-arithmetic behavior.
