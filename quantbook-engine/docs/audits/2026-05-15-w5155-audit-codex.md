Reading prompt from stdin...
OpenAI Codex v0.130.0
--------
workdir: /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR, /Users/sanzhar/.codex/memories]
reasoning effort: high
reasoning summaries: none
session id: 019e2cf3-e0f7-76f0-afb1-19e01b985647
--------
user
# W5-155 audit task

Audit commit `a01b40f5101` in this repo. The change wires the W5-154 `on_table_drop` hook into `WorkbookRuntime::drop_table` in `crates/ql-exec/src/workbook_runtime.rs`. The W5-154 hook + reverse index were added in the prior commit `b83c19c7a7d` to `crates/ql-exec/src/calcgraph_session.rs`.

Tasks:

1. **Validate the W5-155 wiring.** Does the call to `g.on_table_drop(&canonical)` deliver what the docstring promises? The W5-154 hook docstring on `calcgraph_session.rs:947` claims: "After this hook fires, the next recompute will: 1. Find each previously-table-referencing formula in `dirty`. 2. Re-bind it (table is gone → `BindError::UnknownTable` → `Value::Error(#NAME?)`)."

2. **Plan cache interaction.** `PlanCacheKey` is `(text, sheet, name_gen, cell_anchor)` (see `crates/ql-exec/src/plan_cache.rs`). Note `name_gen` is `NameTable` generation, NOT `TableTable` generation. `try_recompute_with_simd_profile` (around `workbook_runtime.rs:3350`) is the recompute path — does it correctly re-bind structured-ref formulas after a `drop_table`, or does it serve a stale cached `ExprPlan::StructuredRef { resolved: <pre-drop range> }`?

3. **Symmetry with peers.** `rename_table`, `rename_column`, `resize_table` all call `self.plan_cache.clear()`. `drop_table` does NOT. Is this an accidental asymmetry or intentional?

4. **Op-log replay.** `crates/ql-oplog/src/replay.rs:523` handles `Op::DropTable` directly via `workbook.tables_mut().remove(...)` — replay has no calcgraph. Is this correct given that post-replay state is rebuilt via `CalcgraphSession::rebuild_from_workbook`?

5. **Hook ordering.** Hook fires AFTER op-log append, BEFORE `tables_mut().remove`. Is the ordering correct or could it interleave badly with another thread/borrow?

6. **Test coverage.** Does the new test `drop_table_fires_on_table_drop_hook_and_dirties_readers` actually prove that B1 = SUM(Sales[Qty]) ends up #NAME? after drop_table + recompute_dirty? Or only that it ends up dirty?

7. **Stale docstrings.** Search for any docstring or comment that claims drop_table relies on `plan_cache.clear()`, or that the hook isn't wired yet (W5-154 said "wiring lands in a follow-up commit"). Are they updated?

Report findings as HIGH / MEDIUM / LOW with file:line, the contract being violated, and proposed fix. Be terse but specific. If your conclusions disagree with the auditor (me, Claude), say so plainly.

codex
I’ll audit this as a code review against the specified commits and current worktree. I’m going to read the relevant files and diffs first, then verify the recompute/cache path and tests before reporting findings.
exec
/bin/zsh -lc "rg -n \"on_table_drop|drop_table|PlanCacheKey|StructuredRef|plan_cache\\.clear|rebuild_from_workbook|drop_table_fires\" crates/ql-exec/src crates/ql-oplog/src" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
crates/ql-exec/src/aggregate_cache.rs:132:    /// Drop every cached entry. Used by `CalcgraphSession::rebuild_from_workbook`
crates/ql-exec/src/plan_cache.rs:69:pub struct PlanCacheKey {
crates/ql-exec/src/plan_cache.rs:111:    entries: HashMap<PlanCacheKey, Arc<ExprPlan>>,
crates/ql-exec/src/plan_cache.rs:127:    pub fn get_or_insert<F, E>(&mut self, key: PlanCacheKey, build: F) -> Result<Arc<ExprPlan>, E>
crates/ql-exec/src/plan_cache.rs:150:    pub fn insert(&mut self, key: PlanCacheKey, plan: Arc<ExprPlan>) {
crates/ql-exec/src/plan_cache.rs:188:    fn key(text: &str, sheet: SheetId, name_gen: u64) -> PlanCacheKey {
crates/ql-exec/src/plan_cache.rs:189:        PlanCacheKey {
crates/ql-exec/src/scalar.rs:102:        ExprPlan::StructuredRef { .. } => Value::Error(ErrorValue::Calc),
crates/ql-exec/src/scalar.rs:200:                            ExprPlan::StructuredRef {
crates/ql-exec/src/scalar.rs:257:                            ExprPlan::StructuredRef {
crates/ql-exec/src/scalar.rs:333:                        // **W5-116/117 (Phase 4.8.G + G.2):** single-StructuredRef
crates/ql-exec/src/scalar.rs:339:                        [ExprPlan::StructuredRef {
crates/ql-exec/src/scalar.rs:376:                                | ExprPlan::StructuredRef { .. }
crates/ql-exec/src/scalar.rs:394:                                ExprPlan::StructuredRef {
crates/ql-exec/src/scalar.rs:454:        ExprPlan::StructuredRef {
crates/ql-exec/src/scalar.rs:468:/// **W5-117 (Phase 4.8.G.2):** narrow a `StructuredRef::resolved` range
crates/ql-exec/src/calcgraph_session.rs:17://! - `rebuild_from_workbook` adds a `CellNode` per formula cell in
crates/ql-exec/src/calcgraph_session.rs:30://! - `rebuild_from_workbook` lex+parse+binds every formula and runs
crates/ql-exec/src/calcgraph_session.rs:187:    /// the formula (via `ExprPlan::StructuredRef.table_name`). Used by
crates/ql-exec/src/calcgraph_session.rs:259:        ExprPlan::StructuredRef {
crates/ql-exec/src/calcgraph_session.rs:272:/// or `rebuild_from_workbook` to build from an existing workbook's
crates/ql-exec/src/calcgraph_session.rs:306:    /// `ExprPlan::StructuredRef`. Used by `on_table_*` mutation
crates/ql-exec/src/calcgraph_session.rs:311:    /// Today (W5-154): only `on_table_drop` consumes the index. The
crates/ql-exec/src/calcgraph_session.rs:354:    /// **W5-154 (Phase 4.8.G.3 foundation):** counts of `on_table_drop`
crates/ql-exec/src/calcgraph_session.rs:360:/// Phase 3.2: aggregate outcome of `rebuild_from_workbook`. Parallel
crates/ql-exec/src/calcgraph_session.rs:617:        // `table_to_formulas` for every StructuredRef table dep.
crates/ql-exec/src/calcgraph_session.rs:663:            // Re-binding a formula whose StructuredRef target
crates/ql-exec/src/calcgraph_session.rs:666:            // `on_table_drop` would dirty stale formulas.
crates/ql-exec/src/calcgraph_session.rs:693:    pub fn rebuild_from_workbook(wb: &Workbook) -> RebuildResult {
crates/ql-exec/src/calcgraph_session.rs:766:    /// workbook's NameTable. Used by `rebuild_from_workbook` and by
crates/ql-exec/src/calcgraph_session.rs:929:    /// `WorkbookRuntime::drop_table`. Walks `table_to_formulas[name]`
crates/ql-exec/src/calcgraph_session.rs:943:    /// Today (W5-154): the WorkbookRuntime drop_table path does NOT
crates/ql-exec/src/calcgraph_session.rs:944:    /// yet call this hook (still relies on `plan_cache.clear()`).
crates/ql-exec/src/calcgraph_session.rs:947:    pub fn on_table_drop(&mut self, name: &str) {
crates/ql-exec/src/calcgraph_session.rs:954:        // original case, drop_table receives the user-typed name
crates/ql-exec/src/calcgraph_session.rs:1359:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:1370:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:1397:            let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:1482:    // machinery via the public `rebuild_from_workbook` /
crates/ql-exec/src/calcgraph_session.rs:1496:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:1536:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:1582:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:1615:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:1703:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:1736:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:1802:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:1919:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:1943:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:2022:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:2100:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:2134:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:2180:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:2208:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:3020:    fn rebuild_from_workbook_starts_clean_no_dirty() {
crates/ql-exec/src/calcgraph_session.rs:3022:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:3035:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:3078:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:3127:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:3166:            let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:3195:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:3234:        let r = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/calcgraph_session.rs:3536:    // index + on_table_drop hook tests.
crates/ql-exec/src/calcgraph_session.rs:3539:    // StructuredRef-bearing formula registers in `table_to_formulas`
crates/ql-exec/src/calcgraph_session.rs:3540:    // at extract time; `on_table_drop` BFS-dirties the indexed
crates/ql-exec/src/calcgraph_session.rs:3544:    /// **W5-154:** `table_to_formulas` populates for StructuredRef
crates/ql-exec/src/calcgraph_session.rs:3546:    /// `ExprPlan::StructuredRef` and pushes the table name into
crates/ql-exec/src/calcgraph_session.rs:3554:        // Synthesize an ExprPlan::StructuredRef directly. Range value
crates/ql-exec/src/calcgraph_session.rs:3556:        let plan = ExprPlan::StructuredRef {
crates/ql-exec/src/calcgraph_session.rs:3571:    /// **W5-154:** `on_table_drop` BFS-fans dirty to the directly-
crates/ql-exec/src/calcgraph_session.rs:3575:    fn on_table_drop_marks_table_referencing_formulas_dirty() {
crates/ql-exec/src/calcgraph_session.rs:3580:        let plan_f1 = ExprPlan::StructuredRef {
crates/ql-exec/src/calcgraph_session.rs:3593:        s.on_table_drop("Sales");
crates/ql-exec/src/calcgraph_session.rs:3599:            "on_table_drop must dirty the formula that references the dropped table"
crates/ql-exec/src/calcgraph_session.rs:3603:    /// **W5-154:** `on_table_drop` is case-insensitive on lookup.
crates/ql-exec/src/calcgraph_session.rs:3605:    /// `on_table_drop("SALES")` or `on_table_drop("sales")` still
crates/ql-exec/src/calcgraph_session.rs:3608:    fn on_table_drop_is_case_insensitive_via_fallback_scan() {
crates/ql-exec/src/calcgraph_session.rs:3612:        let plan = ExprPlan::StructuredRef {
crates/ql-exec/src/calcgraph_session.rs:3625:        s.on_table_drop("sales");
crates/ql-exec/src/calcgraph_session.rs:3629:            "on_table_drop must do case-insensitive fallback when exact-match misses"
crates/ql-exec/src/calcgraph_session.rs:3633:    /// **W5-154:** rebinding a formula whose StructuredRef target
crates/ql-exec/src/calcgraph_session.rs:3635:    /// Pre-fix, `on_table_drop("OldTable")` would dirty the formula
crates/ql-exec/src/calcgraph_session.rs:3643:        let plan_v1 = ExprPlan::StructuredRef {
crates/ql-exec/src/calcgraph_session.rs:3653:        let plan_v2 = ExprPlan::StructuredRef {
crates/ql-exec/src/calcgraph_session.rs:3666:        s.on_table_drop("Sales");
crates/ql-exec/src/calcgraph_session.rs:3674:        s.on_table_drop("Costs");
crates/ql-exec/src/calcgraph_session.rs:3686:    fn on_table_drop_bumps_hook_count() {
crates/ql-exec/src/calcgraph_session.rs:3689:        s.on_table_drop("Nonexistent");
crates/ql-exec/src/calcgraph_session.rs:3691:        s.on_table_drop("AlsoNonexistent");
crates/ql-exec/src/env.rs:107:    /// known. Used by the eval-side `ExprPlan::StructuredRef` arm to
crates/ql-exec/src/env.rs:274:    /// constructed via `with_formula_cell`. The scalar.rs StructuredRef
crates/ql-exec/src/lib.rs:101:pub use plan_cache::{PlanCache, PlanCacheKey, PlanCacheStats};
crates/ql-oplog/src/op.rs:172:    /// recompute. Mirrors `WorkbookRuntime::drop_table`.
crates/ql-exec/src/plan.rs:134:    StructuredRef {
crates/ql-exec/src/plan.rs:288:    StructuredRefDegenerateRange(std::sync::Arc<str>),
crates/ql-exec/src/plan.rs:532:/// `tables` parameter for `Expr::StructuredRef` resolution; legacy
crates/ql-exec/src/plan.rs:548:/// `bind_with_site(.., &EmptyTableLookup)` — any `Expr::StructuredRef`
crates/ql-exec/src/plan.rs:770:        Expr::StructuredRef { table_name, spec } => {
crates/ql-exec/src/plan.rs:826:        //   9. StructuredRef inner → patch `is_this_row: true` on
crates/ql-exec/src/plan.rs:928:        Expr::StructuredRef { table_name, spec } => {
crates/ql-exec/src/plan.rs:934:                ExprPlan::StructuredRef {
crates/ql-exec/src/plan.rs:939:                } => Ok(ExprPlan::StructuredRef {
crates/ql-exec/src/plan.rs:946:                // ExprPlan::StructuredRef; the wildcard arm is
crates/ql-exec/src/plan.rs:1229:/// `Expr::StructuredRef { table_name, spec }` → `ExprPlan::StructuredRef
crates/ql-exec/src/plan.rs:1254:/// against the table registry. Produces `ExprPlan::StructuredRef`
crates/ql-exec/src/plan.rs:1293:                BindError::StructuredRefDegenerateRange(Arc::clone(&canonical_name))
crates/ql-exec/src/plan.rs:1306:                BindError::StructuredRefDegenerateRange(Arc::clone(&canonical_name))
crates/ql-exec/src/plan.rs:1316:                BindError::StructuredRefDegenerateRange(Arc::clone(&canonical_name))
crates/ql-exec/src/plan.rs:1319:                BindError::StructuredRefDegenerateRange(Arc::clone(&canonical_name))
crates/ql-exec/src/plan.rs:1331:    Ok(ExprPlan::StructuredRef {
crates/ql-exec/src/plan.rs:1400:            .ok_or_else(|| BindError::StructuredRefDegenerateRange(Arc::clone(canonical)))?;
crates/ql-exec/src/plan.rs:1420:                .ok_or_else(|| BindError::StructuredRefDegenerateRange(Arc::clone(canonical)))?,
crates/ql-exec/src/plan.rs:1430:                    .ok_or_else(|| BindError::StructuredRefDegenerateRange(Arc::clone(canonical)))?
crates/ql-exec/src/plan.rs:1512:        Expr::StructuredRef { .. } => "StructuredRef",
crates/ql-exec/src/plan.rs:2096:        let expr = Expr::StructuredRef {
crates/ql-exec/src/plan.rs:2109:            ExprPlan::StructuredRef {
crates/ql-exec/src/plan.rs:2123:            other => panic!("expected StructuredRef, got {other:?}"),
crates/ql-exec/src/plan.rs:2130:        let expr = Expr::StructuredRef {
crates/ql-exec/src/plan.rs:2151:        let expr = Expr::StructuredRef {
crates/ql-exec/src/plan.rs:2175:        let expr = Expr::StructuredRef {
crates/ql-exec/src/plan.rs:2188:            ExprPlan::StructuredRef { resolved, .. } => {
crates/ql-exec/src/plan.rs:2195:            other => panic!("expected StructuredRef, got {other:?}"),
crates/ql-exec/src/plan.rs:2203:        let expr = Expr::StructuredRef {
crates/ql-exec/src/plan.rs:2219:            ExprPlan::StructuredRef { resolved, .. } => {
crates/ql-exec/src/plan.rs:2225:            other => panic!("expected StructuredRef, got {other:?}"),
crates/ql-exec/src/plan.rs:2233:        let expr = Expr::StructuredRef {
crates/ql-exec/src/plan.rs:2249:            ExprPlan::StructuredRef { resolved, .. } => {
crates/ql-exec/src/plan.rs:2255:            other => panic!("expected StructuredRef, got {other:?}"),
crates/ql-exec/src/plan.rs:2262:        let expr = Expr::StructuredRef {
crates/ql-exec/src/plan.rs:2275:            ExprPlan::StructuredRef { resolved, .. } => {
crates/ql-exec/src/plan.rs:2281:            other => panic!("expected StructuredRef, got {other:?}"),
crates/ql-exec/src/plan.rs:2288:        let expr = Expr::StructuredRef {
crates/ql-exec/src/plan.rs:2301:            ExprPlan::StructuredRef {
crates/ql-exec/src/plan.rs:2313:            other => panic!("expected StructuredRef, got {other:?}"),
crates/ql-exec/src/plan.rs:2322:        let expr = Expr::StructuredRef {
crates/ql-exec/src/plan.rs:2957:    /// `is_this_row: true` on the bound StructuredRef. This is the
crates/ql-exec/src/plan.rs:2977:            ExprPlan::StructuredRef { is_this_row, .. } => {
crates/ql-exec/src/plan.rs:2980:            other => panic!("expected ExprPlan::StructuredRef, got {other:?}"),
crates/ql-exec/src/plan.rs:3011:            ExprPlan::StructuredRef { is_this_row, .. } => {
crates/ql-exec/src/plan.rs:3017:            other => panic!("expected ExprPlan::StructuredRef, got {other:?}"),
crates/ql-exec/src/plan.rs:3044:            ExprPlan::StructuredRef { is_this_row, .. } => {
crates/ql-exec/src/plan.rs:3050:            other => panic!("expected ExprPlan::StructuredRef, got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:51:use crate::plan_cache::{PlanCache, PlanCacheKey, PlanCacheStats};
crates/ql-exec/src/workbook_runtime.rs:257:    /// **W5-118 (Phase 4.8.H):** `drop_table` was called on a name
crates/ql-exec/src/workbook_runtime.rs:605:        let cache_key = PlanCacheKey {
crates/ql-exec/src/workbook_runtime.rs:1012:            let cache_key = PlanCacheKey {
crates/ql-exec/src/workbook_runtime.rs:1546:        self.plan_cache.clear();
crates/ql-exec/src/workbook_runtime.rs:1722:    /// `on_table_drop` hook (W5-154) so formulas previously
crates/ql-exec/src/workbook_runtime.rs:1725:    /// with cached `ExprPlan::StructuredRef` plans would continue
crates/ql-exec/src/workbook_runtime.rs:1738:    pub fn drop_table(&mut self, name: &str) -> Result<(), RuntimeError> {
crates/ql-exec/src/workbook_runtime.rs:1753:            g.on_table_drop(&canonical);
crates/ql-exec/src/workbook_runtime.rs:1877:                continue; // no StructuredRef references the renamed table
crates/ql-exec/src/workbook_runtime.rs:1908:        self.plan_cache.clear();
crates/ql-exec/src/workbook_runtime.rs:1914:    /// rewrites stored formula text in every cell whose StructuredRef
crates/ql-exec/src/workbook_runtime.rs:2046:        self.plan_cache.clear();
crates/ql-exec/src/workbook_runtime.rs:2279:        self.plan_cache.clear();
crates/ql-exec/src/workbook_runtime.rs:2442:        self.plan_cache.clear();
crates/ql-exec/src/workbook_runtime.rs:3210:                                let cache_key = PlanCacheKey {
crates/ql-exec/src/workbook_runtime.rs:3369:        let cache_key = PlanCacheKey {
crates/ql-exec/src/workbook_runtime.rs:4605:    /// PlanCacheKey) but we exercise it end-to-end through the runtime.
crates/ql-exec/src/workbook_runtime.rs:5460:        let rebuild = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/workbook_runtime.rs:5601:        let rebuild = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/workbook_runtime.rs:5872:        let rebuild = crate::CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/workbook_runtime.rs:9575:    /// - 4.8.B: lexer Token::StructuredRef.
crates/ql-exec/src/workbook_runtime.rs:9576:    /// - 4.8.C: parser Expr::StructuredRef + Combination/BareColumn.
crates/ql-exec/src/workbook_runtime.rs:9578:    /// - 4.8.F: TableLookup blanket impl + ExprPlan::StructuredRef
crates/ql-exec/src/workbook_runtime.rs:9580:    /// - 4.8.G: eval-side StructuredRef arm in scalar.rs aggregate path.
crates/ql-exec/src/workbook_runtime.rs:9715:    // ===== W5-118 (Phase 4.8.H) — create_table / drop_table + op log =====
crates/ql-exec/src/workbook_runtime.rs:10048:    fn drop_table_removes_metadata_and_emits_op() {
crates/ql-exec/src/workbook_runtime.rs:10056:            rt.drop_table("Sales").unwrap();
crates/ql-exec/src/workbook_runtime.rs:10065:    fn drop_table_missing_errors() {
crates/ql-exec/src/workbook_runtime.rs:10069:        let err = rt.drop_table("Nope").unwrap_err();
crates/ql-exec/src/workbook_runtime.rs:10076:    /// **W5-155 (Phase 4.8.G.3):** `WorkbookRuntime::drop_table` must
crates/ql-exec/src/workbook_runtime.rs:10077:    /// invoke the calcgraph `on_table_drop` hook so any formula
crates/ql-exec/src/workbook_runtime.rs:10083:    fn drop_table_fires_on_table_drop_hook_and_dirties_readers() {
crates/ql-exec/src/workbook_runtime.rs:10113:            rt.drop_table("Sales").unwrap();
crates/ql-exec/src/workbook_runtime.rs:10118:            "drop_table must call on_table_drop exactly once"
crates/ql-exec/src/workbook_runtime.rs:10899:    /// works for StructuredRef single-arg (not just SUM).
crates/ql-exec/src/workbook_runtime.rs:11102:    /// stays in StructuredRef.bracket_content; outside-bracket `@`
crates/ql-exec/src/workbook_runtime.rs:11104:    /// canonicalize to ExprPlan::StructuredRef with
crates/ql-exec/src/workbook_runtime.rs:11615:    /// But `PlanCacheKey` is `(canonical_text, sheet, name_gen)` —

exec
/bin/zsh -lc 'git status --short && git rev-parse --show-toplevel && git show --stat --oneline --decorate --no-renames a01b40f5101' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 1013ms:
?? ../2026-05-11-222312-this-session-is-being-continued-from-a-previous-c.txt
?? ../2026-05-13-003338-this-session-is-being-continued-from-a-previous-c.txt
?? ../node_modules
?? .codex/prompts/2026-05-14-AA-design-review.md
?? .codex/prompts/2026-05-14-J-127-verify.md
?? .codex/prompts/2026-05-14-J-128-verify.md
?? .codex/prompts/2026-05-14-J-129-verify.md
?? .codex/prompts/2026-05-14-J-megaudit-132-133followup-verify.md
?? .codex/prompts/2026-05-14-J-megaudit-HIGH3-verify.md
?? .codex/prompts/2026-05-14-J-megaudit-LOW-verify.md
?? .codex/prompts/2026-05-14-J-megaudit-MEDIUM4-verify.md
?? .codex/prompts/2026-05-14-J-megaudit-MEDIUM5-verify.md
?? .codex/prompts/2026-05-14-K-verify.md
?? .codex/prompts/2026-05-14-L-verify.md
?? .codex/prompts/2026-05-14-M-verify.md
?? .codex/prompts/2026-05-14-N-verify.md
?? .codex/prompts/2026-05-14-O-closure-verify.md
?? .codex/prompts/2026-05-14-O-megaudit.md
?? .codex/prompts/2026-05-14-phase-4.7.I-review.md
?? .codex/prompts/2026-05-14-phase-4.7.J-megaudit-pass2.md
?? .codex/prompts/2026-05-14-phase-4.7.J-review.md
?? .codex/prompts/2026-05-15-phase-4.8-closing-megaudit.md
?? .codex/prompts/2026-05-15-phase-4.8-closure-verify.md
?? .codex/prompts/2026-05-15-phase-4.8-final-reverify.md
?? .codex/prompts/2026-05-15-phase-4.9-design-review.md
?? .codex/prompts/2026-05-15-phase-4.9-mid-arc-audit.md
?? .plans/
?? 2026-05-14-222539-this-session-is-being-continued-from-a-previous-c.txt
?? docs/audits/.codex-w5155-prompt.md
?? docs/audits/2026-05-14-phase-4.7.H-codex-review.txt
?? docs/audits/2026-05-14-phase-4.7.H-sonnet-review.md
?? docs/audits/2026-05-15-phase-4.8-closing-megaudit-codex.md
?? docs/audits/2026-05-15-phase-4.8-closure-verify-codex.md
?? docs/audits/2026-05-15-phase-4.8-final-reverify-codex.md
?? docs/audits/2026-05-15-phase-4.9-design-review-codex.md
?? docs/audits/2026-05-15-phase-4.9-mid-arc-audit-codex.md
?? docs/audits/2026-05-15-w5155-audit-codex.md
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
a01b40f5101 (HEAD -> feat/quantbook-engine) W5-155 / Engine Phase 4.8.G.3 — wire on_table_drop into WorkbookRuntime::drop_table
 .../crates/ql-exec/src/workbook_runtime.rs         | 75 ++++++++++++++++++++++
 1 file changed, 75 insertions(+)

codex
The first pass already shows one likely stale W5-154 comment and that `drop_table` still does not clear the plan cache. I’m reading the exact code paths now to separate a real stale-plan bug from documentation/test gaps.
exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '3330,3415p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '1688,1775p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1688	        use ql_storage::{TableColumn, TableMetadata};
  1689	        let columns: Vec<TableColumn> = column_names
  1690	            .iter()
  1691	            .map(|cn| TableColumn {
  1692	                id: self.workbook.tables_mut().allocate_column_id(),
  1693	                name: Arc::from(cn.to_ascii_lowercase().as_str()),
  1694	                display: Arc::from(cn.as_str()),
  1695	                totals_function: None,
  1696	            })
  1697	            .collect();
  1698	        let meta = TableMetadata {
  1699	            name: Arc::from(canonical.as_str()),
  1700	            display_name: Arc::from(name),
  1701	            sheet,
  1702	            top_row,
  1703	            top_col,
  1704	            rows,
  1705	            cols,
  1706	            has_header,
  1707	            has_totals,
  1708	            columns,
  1709	        };
  1710	        self.workbook
  1711	            .tables_mut()
  1712	            .insert(Arc::from(canonical.as_str()), meta);
  1713	        Ok(())
  1714	    }
  1715	
  1716	    /// **W5-118 (Phase 4.8.H):** drop a table's metadata. Cells inside
  1717	    /// the table footprint are untouched. Formulas referencing the
  1718	    /// dropped table re-bind to `BindError::UnknownTable` on next
  1719	    /// recompute. Emits `Op::DropTable` to the attached op log (if any).
  1720	    ///
  1721	    /// **W5-155 (Phase 4.8.G.3):** also fires the calcgraph's
  1722	    /// `on_table_drop` hook (W5-154) so formulas previously
  1723	    /// referencing this table get BFS-dirty-fanned. Pre-W5-155 the
  1724	    /// drop completed without notifying the calcgraph — formulas
  1725	    /// with cached `ExprPlan::StructuredRef` plans would continue
  1726	    /// reading the (now-untyped) cell range until something else
  1727	    /// invalidated them. Post-W5-155 the hook fires:
  1728	    /// 1. `table_to_formulas[name]` enumerates the readers.
  1729	    /// 2. Each reader marked dirty + BFS-fanout (W5-91 H2 pattern).
  1730	    /// 3. Next recompute re-binds → `BindError::UnknownTable` →
  1731	    ///    `Value::Error(#NAME?)` per the existing 4.8.F binder.
  1732	    ///
  1733	    /// The hook fires AFTER the op-log append (atomic with the
  1734	    /// mutation, per W5-103) and BEFORE `tables_mut().remove`
  1735	    /// (graph fanout is read-only on workbook state; the order
  1736	    /// is irrelevant for correctness but matches the pre-existing
  1737	    /// rename/resize convention).
  1738	    pub fn drop_table(&mut self, name: &str) -> Result<(), RuntimeError> {
  1739	        let canonical = name.to_ascii_uppercase();
  1740	        if self.workbook.tables().lookup(&canonical).is_none() {
  1741	            return Err(RuntimeError::TableNotFound(name.to_owned()));
  1742	        }
  1743	        if let Some(oplog) = self.oplog.as_deref_mut() {
  1744	            oplog.append(Op::DropTable {
  1745	                name: canonical.clone(),
  1746	            })?;
  1747	        }
  1748	        // **W5-155 (Phase 4.8.G.3):** dirty-fan readers BEFORE
  1749	        // removing the metadata. The hook uses case-insensitive
  1750	        // lookup so either case (canonical-upper or the user's
  1751	        // input casing) reaches the index.
  1752	        if let Some(g) = self.graph.as_deref_mut() {
  1753	            g.on_table_drop(&canonical);
  1754	        }
  1755	        let _ = self.workbook.tables_mut().remove(&canonical);
  1756	        Ok(())
  1757	    }
  1758	
  1759	    /// **W5-119 (Phase 4.8.I):** rename a table. Per design § 12.2 + HIGH-1
  1760	    /// closure (Codex pass-1), this REWRITES STORED FORMULA TEXT — Excel
  1761	    /// canon: the next bind sees the new name and binds successfully.
  1762	    ///
  1763	    /// Process:
  1764	    /// 1. Validate: source exists; target name available (TableTable +
  1765	    ///    NameTable shared namespace); not a no-op (same canonical name).
  1766	    /// 2. Op-log append `Op::RenameTable` (before any mutation).
  1767	    /// 3. Walk every formula cell; parse the text; rewrite via
  1768	    ///    `ast::rewrite_table_ref`; if the AST changed, print it back
  1769	    ///    and emit `Op::PutFormula` + update storage.
  1770	    /// 4. Re-key the `TableTable` entry from old canonical to new
  1771	    ///    canonical; update display_name.
  1772	    ///
  1773	    /// Returns the number of formula cells whose text was rewritten.
  1774	    /// **W5-146 (Phase 4.9.K):** set the workbook's reference mode
  1775	    /// (A1 or R1C1) and append `Op::SetReferenceMode` to the op-log.

 succeeded in 0ms:
  3330	    /// arrays at the correct anchor cell.
  3331	    fn try_recompute_with_aggregate_cache(
  3332	        &mut self,
  3333	        sheet: SheetId,
  3334	        row: RowId,
  3335	        col: ColId,
  3336	        formula_text: &Arc<str>,
  3337	        agg_cache: &dyn crate::aggregate_cache::AggregateCache,
  3338	    ) -> Result<Value, RuntimeError> {
  3339	        self.try_recompute_with_simd_profile(sheet, row, col, formula_text, agg_cache)
  3340	            .map(|(v, _, _, _)| v)
  3341	    }
  3342	
  3343	    /// Phase 3.9 (W5-42): like `try_recompute_with_aggregate_cache`
  3344	    /// but ALSO returns a `bool` for whether the formula's bound plan
  3345	    /// was SIMD-eligible per `crate::lower::classify`. The bool is
  3346	    /// pure observability — the actual SIMD dispatch via `simd::*`
  3347	    /// happens at the bench / FormulaRegion path. `recompute_dirty`
  3348	    /// uses this to populate `RecomputeResult.simd_classified` so the
  3349	    /// IDE profile can show where region optimization would help.
  3350	    fn try_recompute_with_simd_profile(
  3351	        &mut self,
  3352	        sheet: SheetId,
  3353	        row: RowId,
  3354	        col: ColId,
  3355	        formula_text: &Arc<str>,
  3356	        agg_cache: &dyn crate::aggregate_cache::AggregateCache,
  3357	    ) -> Result<(Value, bool, Option<SpillShape>, Option<SpillShape>), RuntimeError> {
  3358	        let name_gen = self.workbook.names().generation();
  3359	        // **W5-150 (Phase 4.9.O HIGH-1):** cell-aware key when `@`
  3360	        // is present in the stored formula text. This is the
  3361	        // recompute path — same canonical text at different cells
  3362	        // would otherwise share the cell-specific `@`-narrowed
  3363	        // plan from the first bind, leaking the wrong cell ref.
  3364	        let cell_anchor = if formula_text.contains('@') {
  3365	            Some((row, col))
  3366	        } else {
  3367	            None
  3368	        };
  3369	        let cache_key = PlanCacheKey {
  3370	            text: Arc::clone(formula_text),
  3371	            sheet,
  3372	            name_gen,
  3373	            cell_anchor,
  3374	        };
  3375	        // Borrow split: we need an immutable view of the workbook
  3376	        // (for `names()` inside the closure) while holding a mutable
  3377	        // borrow on `self.plan_cache`. Re-borrow the workbook reference
  3378	        // by name so Rust's borrow checker can split them — both fields
  3379	        // are disjoint subfields of `self`.
  3380	        let workbook: &Workbook = self.workbook;
  3381	        let plan: Arc<crate::plan::ExprPlan> =
  3382	            self.plan_cache
  3383	                .get_or_insert::<_, RuntimeError>(cache_key, || {
  3384	                    // **W5-147 (Phase 4.9.K):** formula_text here is the
  3385	                    // CANONICAL (A1+EnUs) text stored at set_formula
  3386	                    // time. Lex with the canonical mode/locale —
  3387	                    // regardless of the workbook's current
  3388	                    // reference_mode + locale settings, the stored text
  3389	                    // is always A1+EnUs per design § 4.4.
  3390	                    let tokens = lex_with(
  3391	                        formula_text.as_ref(),
  3392	                        ql_types::ReferenceMode::A1,
  3393	                        ql_types::Locale::EnUs,
  3394	                    )?;
  3395	                    let expr = parse(tokens)?;
  3396	                    // W5-92 (Phase 4.6.D): pass `workbook` for names so
  3397	                    // the two-tier sheet-then-workbook scope chain fires.
  3398	                    // **W5-114 (Phase 4.8.E):** carry recomputed cell address.
  3399	                    Ok(bind_with_site(
  3400	                        &expr,
  3401	                        BindSite::at_cell(ql_types::Address::new(sheet, row, col)),
  3402	                        workbook,
  3403	                        workbook,
  3404	                        workbook,
  3405	                    )?)
  3406	                })?;
  3407	
  3408	        // Phase 3.9: classify the plan against the SIMD kernel set.
  3409	        // Pure function over the plan tree; no allocation.
  3410	        let simd_eligible = crate::lower::classify(plan.as_ref()).is_applicable();
  3411	
  3412	        // **W5-103 megaudit HIGH-2 closure (#128, all 3 reviewers
  3413	        // cross-confirmed):** route through `eval_at_cell_boundary`
  3414	        // (same entry set_formula uses) so a top-level
  3415	        // `ExprPlan::Array` produces an `EvalResult::Array` here and

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/calcgraph_session.rs | sed -n '920,975p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   920	    /// `WorkbookRuntime::add_sheet`. Phase 3.1 stub still: bumps
   921	    /// counter. No dirty effect — a new sheet contains no formulas
   922	    /// yet. Phase 4.6 (cross-sheet references) will use this to update
   923	    /// the graph's sheet-id tracking.
   924	    pub fn on_add_sheet(&mut self, _new_sheet: SheetId) {
   925	        self.hook_counts.add_sheet = self.hook_counts.add_sheet.saturating_add(1);
   926	    }
   927	
   928	    /// **W5-154 (Phase 4.8.G.3 foundation):** mutation hook fired by
   929	    /// `WorkbookRuntime::drop_table`. Walks `table_to_formulas[name]`
   930	    /// (case-preserved as stored at extract time) and dirty-fans every
   931	    /// formula that referenced the dropped table. BFS-fanout matches
   932	    /// the W5-91 / `on_set_name` H2 fix — directly-affected formulas
   933	    /// seed `mark_dirty_from_cell_write` at their own cell address so
   934	    /// downstream chains also dirty.
   935	    ///
   936	    /// After this hook fires, the next recompute will:
   937	    /// 1. Find each previously-table-referencing formula in `dirty`.
   938	    /// 2. Re-bind it (table is gone → `BindError::UnknownTable` →
   939	    ///    `Value::Error(#NAME?)` per the existing 4.8.F binder).
   940	    /// 3. The cell value becomes the error; downstream sees the
   941	    ///    error propagate.
   942	    ///
   943	    /// Today (W5-154): the WorkbookRuntime drop_table path does NOT
   944	    /// yet call this hook (still relies on `plan_cache.clear()`).
   945	    /// Wiring lands in a follow-up commit; this hook ships as the
   946	    /// foundation that the wiring will use.
   947	    pub fn on_table_drop(&mut self, name: &str) {
   948	        self.hook_counts.table_drop = self.hook_counts.table_drop.saturating_add(1);
   949	        // Table names are stored in `deps.tables` as the parser's
   950	        // case-preserving Arc<str>. Look up exact-match first, then
   951	        // fall back to case-insensitive scan if the exact key isn't
   952	        // present (covers tables renamed under a different case
   953	        // during the formula's lifetime — extract captured the
   954	        // original case, drop_table receives the user-typed name
   955	        // which may differ).
   956	        let dependents: Vec<NodeId> = if let Some(set) = self.table_to_formulas.get(name) {
   957	            set.iter().copied().collect()
   958	        } else {
   959	            self.table_to_formulas
   960	                .iter()
   961	                .filter(|(k, _)| k.eq_ignore_ascii_case(name))
   962	                .flat_map(|(_, set)| set.iter().copied())
   963	                .collect()
   964	        };
   965	        for n in dependents {
   966	            self.dirty.insert(n);
   967	            if let Some((s, r, c)) = self.cell_address_for(n) {
   968	                self.mark_dirty_from_cell_write(s, r, c);
   969	            }
   970	        }
   971	    }
   972	
   973	    /// Phase 3.3 (single-hop) + Phase 3.4 (BFS): internal fanout —
   974	    /// for a write at `(sheet, row, col)`, mark every formula node
   975	    /// that TRANSITIVELY depends on this cell dirty. Two reverse-

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '3190,3245p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  3190	                                    };
  3191	                                    if (s, r, c) == (sheet, row, col) {
  3192	                                        continue;
  3193	                                    }
  3194	                                    let Some(text) = self.workbook.formula_at(s, r, c).cloned()
  3195	                                    else {
  3196	                                        continue;
  3197	                                    };
  3198	                                    readers.push((n, s, r, c, text));
  3199	                                }
  3200	                            }
  3201	                            for (rn, reader_sheet, reader_row, reader_col, reader_text) in readers {
  3202	                                let name_gen = self.workbook.names().generation();
  3203	                                // **W5-150 (Phase 4.9.O HIGH-1):** cell-aware key when `@`
  3204	                                // is present.
  3205	                                let cell_anchor = if reader_text.contains('@') {
  3206	                                    Some((reader_row, reader_col))
  3207	                                } else {
  3208	                                    None
  3209	                                };
  3210	                                let cache_key = PlanCacheKey {
  3211	                                    text: Arc::clone(&reader_text),
  3212	                                    sheet: reader_sheet,
  3213	                                    name_gen,
  3214	                                    cell_anchor,
  3215	                                };
  3216	                                let workbook: &Workbook = self.workbook;
  3217	                                let plan: Arc<crate::plan::ExprPlan> =
  3218	                                    match self.plan_cache.get_or_insert::<_, RuntimeError>(
  3219	                                        cache_key,
  3220	                                        || {
  3221	                                            let tokens = lex(reader_text.as_ref())?;
  3222	                                            let expr = parse(tokens)?;
  3223	                                            // **W5-114 (Phase 4.8.E):** reader cell addr.
  3224	                                            Ok(bind_with_site(
  3225	                                                &expr,
  3226	                                                BindSite::at_cell(ql_types::Address::new(
  3227	                                                    reader_sheet,
  3228	                                                    reader_row,
  3229	                                                    reader_col,
  3230	                                                )),
  3231	                                                workbook,
  3232	                                                workbook,
  3233	                                                workbook,
  3234	                                            )?)
  3235	                                        },
  3236	                                    ) {
  3237	                                        Ok(p) => p,
  3238	                                        Err(_) => {
  3239	                                            // No-fallbacks rule: bind failure
  3240	                                            // here mirrors set_formula path's
  3241	                                            // mark_dirty handling — surface at
  3242	                                            // reader's own recompute next time.
  3243	                                            session.mark_dirty(rn);
  3244	                                            continue;
  3245	                                        }

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/plan_cache.rs | sed -n '1,145p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
     1	//! Phase 2B.3 (2026-05-12) — bind-plan cache V0.
     2	//!
     3	//! Caches parsed + bound [`ExprPlan`] values keyed by the formula text, the
     4	//! owning sheet, and the workbook's NameTable generation. Lives on
     5	//! [`crate::WorkbookRuntime`] and is shared into spawned
     6	//! [`crate::WorkbookTransaction`] instances through forwarding.
     7	//!
     8	//! ## Invalidation contract
     9	//!
    10	//! Cache entries are NOT explicitly removed on workbook mutation. Instead,
    11	//! the cache key is `(formula_text, sheet_id, name_table_generation)`. When
    12	//! the workbook mutates state that would change a bind result:
    13	//!
    14	//! - User edits formula text → new text, new key, automatic miss.
    15	//! - Caller registers/removes a name → `NameTable::generation()` advances
    16	//!   (per Phase 2B.3 workbook.rs), so plans bound against the prior
    17	//!   generation no longer match.
    18	//!
    19	//! Stale entries accumulate over time; Phase 3 calcgraph integration adds
    20	//! explicit eviction tied to dirty propagation. For Phase 2B.3 the cache
    21	//! is small enough that drift is acceptable — typical workbook formula
    22	//! cardinality is in the thousands, not millions.
    23	//!
    24	//! ## What the cache stores
    25	//!
    26	//! `Arc<ExprPlan>` so cache hits hand back a refcount-bump without cloning
    27	//! the plan tree. `ExprPlan` is `Clone + Debug + PartialEq` per
    28	//! `ql-exec::plan` but the plan tree is non-trivial; sharing via `Arc`
    29	//! keeps the cache cheap.
    30	//!
    31	//! ## References
    32	//!
    33	//! - `.references/hyperformula/src/parser/ParserWithCaching.ts` — HF caches
    34	//!   parsed ASTs keyed by formula text + sheet metadata for the same
    35	//!   reason. They evict on text change; we additionally include
    36	//!   `NameTable::generation` so the bind result (not just the parse) stays
    37	//!   consistent.
    38	//! - `.references/hyperformula/src/parser/Cache.ts` — the size-bounded LRU
    39	//!   wrapper. V0 here is unbounded; a bounded eviction policy lands when
    40	//!   workbook size makes it necessary.
    41	
    42	use std::collections::HashMap;
    43	use std::sync::Arc;
    44	
    45	use ql_types::{ColId, RowId, SheetId};
    46	
    47	use crate::plan::ExprPlan;
    48	
    49	/// Composite cache key. Reflects everything that, if changed, would change
    50	/// the bind result for a given formula text:
    51	///
    52	/// - `text` — the raw formula source (without leading `=`). Different text
    53	///   yields a different parse tree, so plans don't share.
    54	/// - `sheet` — owning sheet for the binding. Same text in different sheets
    55	///   binds to different cell references (relative refs resolve against the
    56	///   owning sheet).
    57	/// - `name_gen` — `NameTable::generation()` at the moment of bind. A
    58	///   subsequent name registration / removal bumps this, so old plans
    59	///   become unreachable.
    60	/// - `cell_anchor` (W5-150) — `Some((row, col))` when the bind result is
    61	///   cell-anchor-dependent, `None` otherwise. Set by callers when the
    62	///   canonical text contains `@` (implicit intersection narrows
    63	///   `@<range>` at bind time using the formula's own cell — same
    64	///   canonical text at different cells produces different ExprPlans).
    65	///   For non-`@` formulas (`SUM(A1:A10)` shared across rows etc.) this
    66	///   stays `None` so the cache shares plans across cells, preserving
    67	///   the pre-4.9.O hit-rate. Closes 4.9.O HIGH-1.
    68	#[derive(Clone, Debug, Hash, Eq, PartialEq)]
    69	pub struct PlanCacheKey {
    70	    pub text: Arc<str>,
    71	    pub sheet: SheetId,
    72	    pub name_gen: u64,
    73	    pub cell_anchor: Option<(RowId, ColId)>,
    74	}
    75	
    76	/// Snapshot of cache observability data — exposed via
    77	/// `WorkbookRuntime::cache_stats()` and surfaced through `ql-profile`.
    78	#[derive(Clone, Copy, Debug, Default, PartialEq, Eq)]
    79	pub struct PlanCacheStats {
    80	    pub hits: u64,
    81	    pub misses: u64,
    82	    pub entries: usize,
    83	}
    84	
    85	impl PlanCacheStats {
    86	    /// Hit rate as a fraction in `[0.0, 1.0]`. Returns `None` when no
    87	    /// lookups have happened yet so callers don't divide by zero or
    88	    /// display "0.0%" for an unused cache.
    89	    pub fn hit_rate(&self) -> Option<f64> {
    90	        let total = self.hits + self.misses;
    91	        if total == 0 {
    92	            None
    93	        } else {
    94	            Some(self.hits as f64 / total as f64)
    95	        }
    96	    }
    97	}
    98	
    99	/// Phase 2B.3 bind-plan cache.
   100	///
   101	/// Stores `Arc<ExprPlan>` per `(text, sheet, name_gen)` key. Hits return
   102	/// the cached plan (refcount-bump only); misses run the caller-supplied
   103	/// build closure. Accumulates hit/miss counters for observability.
   104	///
   105	/// `Default` constructs an empty cache. The cache is opt-in via
   106	/// `WorkbookRuntime::with_plan_cache_disabled` — not yet implemented; the
   107	/// default runtime constructor builds an empty cache that gets populated
   108	/// on first lookup.
   109	#[derive(Debug, Default)]
   110	pub struct PlanCache {
   111	    entries: HashMap<PlanCacheKey, Arc<ExprPlan>>,
   112	    hits: u64,
   113	    misses: u64,
   114	}
   115	
   116	impl PlanCache {
   117	    /// Empty cache. Same as `Default::default`.
   118	    pub fn new() -> Self {
   119	        Self::default()
   120	    }
   121	
   122	    /// Look up the plan for `key`. On hit, returns the cached `Arc<ExprPlan>`
   123	    /// and increments the hit counter. On miss, runs `build()` (which does
   124	    /// the lex/parse/bind), stores the result, and returns it. Errors from
   125	    /// `build()` are passed through without caching — failed binds aren't
   126	    /// memoized, so a fix to the formula text gets a clean retry.
   127	    pub fn get_or_insert<F, E>(&mut self, key: PlanCacheKey, build: F) -> Result<Arc<ExprPlan>, E>
   128	    where
   129	        F: FnOnce() -> Result<ExprPlan, E>,
   130	    {
   131	        if let Some(plan) = self.entries.get(&key) {
   132	            self.hits += 1;
   133	            return Ok(Arc::clone(plan));
   134	        }
   135	        self.misses += 1;
   136	        let plan = Arc::new(build()?);
   137	        self.entries.insert(key, Arc::clone(&plan));
   138	        Ok(plan)
   139	    }
   140	
   141	    /// Insert a plan unconditionally (overwriting any existing entry for
   142	    /// the same key). Available for callers that already bound the plan
   143	    /// out-of-band and want to pre-warm the cache.
   144	    ///
   145	    /// Phase 2B.7 audit (correctness L1 / D4): the prior doc-comment

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '3020,3125p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  3020	                    self.workbook.put_computed_at(sheet, row, col, circ.clone());
  3021	                    changed.insert((sheet, row, col));
  3022	                }
  3023	            }
  3024	
  3025	            // Sorted nodes evaluate in dependency-first order.
  3026	            for node in &sched.sorted {
  3027	                let Some((sheet, row, col)) = session.cell_address_for(*node) else {
  3028	                    continue;
  3029	                };
  3030	                let Some(text) = self.workbook.formula_at(sheet, row, col).cloned() else {
  3031	                    continue;
  3032	                };
  3033	
  3034	                // Phase 3.8: VEQ decision — does any upstream of `node`
  3035	                // have a reason to recompute? Cases:
  3036	                //   a) Volatile (NOW/RAND/etc.) — always re-eval (its
  3037	                //      value can change without any cell edit).
  3038	                //   b) Has named-range deps — V1 conservatively re-evals
  3039	                //      (we don't track per-cell-in-range changes yet).
  3040	                //   c) Has at least one direct-cell dep that's a
  3041	                //      formula in the original dirty set AND that
  3042	                //      formula's value changed → re-eval.
  3043	                //   d) Has NO direct-cell dep that's in the original
  3044	                //      dirty set (top-level dirty; came from an external
  3045	                //      edit) → re-eval.
  3046	                //   e) All direct-cell deps are in the dirty set but
  3047	                //      none ended up in `changed` → SKIP.
  3048	                let is_volatile = session.is_volatile(*node);
  3049	                let needs_eval = if is_volatile {
  3050	                    true
  3051	                } else if let Some(deps) = session.formula_deps(*node) {
  3052	                    if !deps.named_ranges.is_empty() {
  3053	                        true
  3054	                    } else {
  3055	                        let mut had_dirty_dep = false;
  3056	                        let mut had_changed_dep = false;
  3057	                        // **Codex audit HIGH-1 closure**: VEQ-defeat for
  3058	                        // producer-aliased deps. A dep that resolves to a
  3059	                        // current spill ANCHOR may have target-value
  3060	                        // changes that the VEQ "changed[anchor]" set
  3061	                        // doesn't capture (anchor value can be invariant
  3062	                        // while target values change — e.g.
  3063	                        // SEQUENCE(3,1,1,A1)'s start=1 is fixed but
  3064	                        // step depends on A1, so C2/C3 vary). Force
  3065	                        // re-eval whenever a dep is a CURRENT spill
  3066	                        // anchor and was dirty in some iteration.
  3067	                        let mut had_aliased_dirty_dep = false;
  3068	                        for &(ds, dr, dc) in &deps.cells {
  3069	                            if let Some(dep_node) = session.cell_node_for(ds, dr, dc) {
  3070	                                if originally_dirty.contains(&dep_node) {
  3071	                                    had_dirty_dep = true;
  3072	                                    if changed.contains(&(ds, dr, dc)) {
  3073	                                        had_changed_dep = true;
  3074	                                        break;
  3075	                                    }
  3076	                                    if self.workbook.spill_anchor_at(ds, dr, dc).is_some() {
  3077	                                        had_aliased_dirty_dep = true;
  3078	                                    }
  3079	                                }
  3080	                            }
  3081	                        }
  3082	                        // Re-eval if: top-level dirty (no dirty dep),
  3083	                        // OR a dirty dep value-changed,
  3084	                        // OR a dirty dep is a spill anchor (target
  3085	                        //    values may have changed even if anchor
  3086	                        //    value didn't).
  3087	                        !had_dirty_dep || had_changed_dep || had_aliased_dirty_dep
  3088	                    }
  3089	                } else {
  3090	                    // No tracked deps (e.g., `=1+1`). Treat as top-level.
  3091	                    true
  3092	                };
  3093	
  3094	                if !needs_eval {
  3095	                    skipped_value_equality += 1;
  3096	                    continue;
  3097	                }
  3098	
  3099	                // Phase 3.6 (W5-39): route through the session's aggregate
  3100	                // cache so `SUM(Sales)`-style formulas hit the cache when
  3101	                // no cell inside `Sales` changed (AGG-3-01).
  3102	                // Phase 3.9 (W5-42): also classify the plan for SIMD
  3103	                // eligibility (SIMD-3-03 profile). We need the plan
  3104	                // post-bind — call a slightly-expanded helper that
  3105	                // returns the value AND the SimdShape classification.
  3106	                let agg_cache = session.aggregate_cache();
  3107	                match self.try_recompute_with_simd_profile(sheet, row, col, &text, agg_cache) {
  3108	                    Ok((value, simd_eligible, old_spill_shape, new_spill_shape)) => {
  3109	                        if simd_eligible {
  3110	                            simd_classified += 1;
  3111	                        }
  3112	                        // Phase 3.8: value-equality check. If the freshly-
  3113	                        // computed value matches the snapshot, suppress
  3114	                        // the write entirely and don't record this cell
  3115	                        // as `changed` — downstream formulas that depend
  3116	                        // only on this one will skip too.
  3117	                        let prior_val = prior.get(&(sheet, row, col));
  3118	                        if prior_val == Some(&value) {
  3119	                            skipped_value_equality += 1;
  3120	                        } else {
  3121	                            self.workbook.put_computed_at(sheet, row, col, value);
  3122	                            changed.insert((sheet, row, col));
  3123	                        }
  3124	                        // **Task #136 hook pass (recompute_dirty side)**: if
  3125	                        // the formula's spill shape changed (or was a new

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '3410,3485p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '3125,3190p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  3410	        let simd_eligible = crate::lower::classify(plan.as_ref()).is_applicable();
  3411	
  3412	        // **W5-103 megaudit HIGH-2 closure (#128, all 3 reviewers
  3413	        // cross-confirmed):** route through `eval_at_cell_boundary`
  3414	        // (same entry set_formula uses) so a top-level
  3415	        // `ExprPlan::Array` produces an `EvalResult::Array` here and
  3416	        // materializes via `write_spill` — instead of collapsing to
  3417	        // `#CALC!` (which `eval_scalar_with_cache` returns per the
  3418	        // scalar-context contract at scalar.rs:95).
  3419	        //
  3420	        // Without this, op-log replay of `PutFormula { text: "{1,2,3}" }`
  3421	        // followed by `recompute_all` would write #CALC! at the
  3422	        // anchor instead of spilling — design § 7.3/§ 12.3
  3423	        // "load → recompute re-derives spills" contract broken,
  3424	        // persistence layer (4.7.L) hard-blocked.
  3425	        //
  3426	        // The clear-old + write_spill sequence mirrors set_formula's
  3427	        // 4.7.J.2 pattern. Unlike set_formula, we DO NOT fire
  3428	        // on_set_formula / on_set_value / re-extract hooks: recompute
  3429	        // doesn't change a formula's identity, only its computed
  3430	        // value. Downstream readers were already dirtied by whatever
  3431	        // made THIS formula dirty in the first place.
  3432	        //
  3433	        // **W5-108 (Phase 4.7.O) — Codex MEDIUM-5 closure**: clear
  3434	        // the OLD spill footprint BEFORE eval, matching design § 10.3
  3435	        // step 4 ("clear-old-before-eval") and `set_formula`'s
  3436	        // identical ordering at line 621. Pre-fix ordering was
  3437	        // eval → clear → write, which let a formula's eval observe
  3438	        // its own OLD spill-target computed overlays through indirect
  3439	        // reads (named ranges, aggregates). Direct self-reads create
  3440	        // a producer-alias self-loop (Tarjan → #CIRC!) so the gap was
  3441	        // narrow, but the design contract is "clear-before-eval" and
  3442	        // bringing the code into alignment removes a latent
  3443	        // composition footgun.
  3444	        //
  3445	        // We capture old_spill_shape BEFORE clear so the caller
  3446	        // (recompute_dirty's fixed-point loop) can fire hooks for the
  3447	        // dissolved footprint after eval+write_spill complete.
  3448	        let old_spill_shape = self.workbook.spill_anchor_at(sheet, row, col).copied();
  3449	        self.workbook.clear_spill_if_present((sheet, row, col));
  3450	
  3451	        let result: crate::eval_result::EvalResult = {
  3452	            // **W5-117 (Phase 4.8.G.2):** carry recomputed cell for
  3453	            // structured-ref `[@Col]` row narrowing.
  3454	            let env = WorkbookEnv::with_formula_cell(
  3455	                self.workbook,
  3456	                ql_types::Address::new(sheet, row, col),
  3457	            );
  3458	            crate::scalar::eval_at_cell_boundary(plan.as_ref(), &env, self.registry, agg_cache)
  3459	        };
  3460	
  3461	        // **Note (Sonnet #128 LOW-1, deferred):** the Array arm here
  3462	        // writes the anchor via `write_spill`; the caller also
  3463	        // `put_computed_at(anchor, anchor_value)` — a no-op duplicate
  3464	        // write. Deferred; harmless.
  3465	        //
  3466	        // **W5-106 (Phase 4.7.M / task #136 closure)**: capture the
  3467	        // NEW shape via write_spill's return. We DON'T fire
  3468	        // on_set_value hooks here — the caller (recompute_dirty in
  3469	        // particular) owns the session reference at this scope
  3470	        // (recompute_dirty `take()`s `self.graph` into a local, so
  3471	        // `self.graph` is `None` during the sorted loop). Bubble the
  3472	        // shape info up via the return; the caller fires hooks
  3473	        // against its session.
  3474	        let (anchor_value, new_spill_shape) = match result {
  3475	            crate::eval_result::EvalResult::Scalar(v) => (v, None),
  3476	            crate::eval_result::EvalResult::Array(array) => {
  3477	                let formula_text_arc = self
  3478	                    .workbook
  3479	                    .formula_at(sheet, row, col)
  3480	                    .cloned()
  3481	                    .expect("recompute: formula_at MUST be Some — we're recomputing this cell");
  3482	                self.write_spill(sheet, row, col, array, formula_text_arc)
  3483	            }
  3484	        };
  3485	

 succeeded in 0ms:
  3125	                        // the formula's spill shape changed (or was a new
  3126	                        // spill, or just dissolved), fire on_set_value at
  3127	                        // each non-anchor cell in the UNION of (old, new)
  3128	                        // footprints. Readers indexed under those cells
  3129	                        // get marked dirty; the fixed-point loop picks them
  3130	                        // up in a follow-up iteration.
  3131	                        //
  3132	                        // **Codex audit HIGH-1 closure**: ALSO fire
  3133	                        // on_set_value(anchor) — `cell_to_formulas[anchor]`
  3134	                        // holds readers producer-aliased to this spill
  3135	                        // (4.7.I). They need to dirty even if the anchor's
  3136	                        // OWN value is VEQ-unchanged, because TARGET values
  3137	                        // may have changed (e.g. SEQUENCE(3,1,1,A1): anchor
  3138	                        // value = start = 1 invariant, but C2/C3 vary with
  3139	                        // A1). Over-conservative for direct readers of the
  3140	                        // anchor (they'd re-eval to the same value and
  3141	                        // VEQ-skip the write), but correct.
  3142	                        if old_spill_shape.is_some() || new_spill_shape.is_some() {
  3143	                            use std::collections::HashSet;
  3144	                            let mut affected: HashSet<(SheetId, RowId, ColId)> = HashSet::new();
  3145	                            for shape in [old_spill_shape, new_spill_shape].into_iter().flatten() {
  3146	                                for dr in 0..shape.rows {
  3147	                                    for dc in 0..shape.cols {
  3148	                                        if dr == 0 && dc == 0 {
  3149	                                            continue;
  3150	                                        }
  3151	                                        affected.insert((sheet, row + dr, col + dc));
  3152	                                    }
  3153	                                }
  3154	                            }
  3155	                            for (s, r, c) in affected {
  3156	                                session.on_set_value(s, r, c);
  3157	                            }
  3158	                            // Anchor cell — dirties aliased readers.
  3159	                            session.on_set_value(sheet, row, col);
  3160	
  3161	                            // **Codex audit HIGH-2 closure**: re-extract
  3162	                            // readers in the (old, new) footprint. Mirrors
  3163	                            // set_formula's 4.7.J.4 pattern. Required for
  3164	                            // dissolution-via-recompute: aliased readers'
  3165	                            // deps stay pointing at the dissolved anchor;
  3166	                            // future writes to actually-read cells (e.g.
  3167	                            // user typing into a now-free target) miss
  3168	                            // the reader without re-extraction.
  3169	                            //
  3170	                            // Inlined (can't call self.reextract_spill_footprint_readers
  3171	                            // because it accesses self.graph which is None
  3172	                            // during recompute_dirty's session_slot window).
  3173	                            let mut seen: HashSet<ql_calcgraph::NodeId> = HashSet::new();
  3174	                            let mut readers: Vec<(
  3175	                                ql_calcgraph::NodeId,
  3176	                                SheetId,
  3177	                                RowId,
  3178	                                ColId,
  3179	                                Arc<str>,
  3180	                            )> = Vec::new();
  3181	                            for shape in [old_spill_shape, new_spill_shape].into_iter().flatten() {
  3182	                                for n in
  3183	                                    session.readers_in_rect(sheet, row, col, shape.rows, shape.cols)
  3184	                                {
  3185	                                    if !seen.insert(n) {
  3186	                                        continue;
  3187	                                    }
  3188	                                    let Some((s, r, c)) = session.cell_address_for(n) else {
  3189	                                        continue;
  3190	                                    };

exec
/bin/zsh -lc "nl -ba crates/ql-oplog/src/replay.rs | sed -n '500,545p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   500	        Op::CreateTable {
   501	            name,
   502	            sheet,
   503	            top_row,
   504	            top_col,
   505	            rows,
   506	            cols,
   507	            has_header,
   508	            has_totals,
   509	            column_names,
   510	        } => apply_create_table(
   511	            workbook,
   512	            index,
   513	            name,
   514	            *sheet,
   515	            *top_row,
   516	            *top_col,
   517	            *rows,
   518	            *cols,
   519	            *has_header,
   520	            *has_totals,
   521	            column_names,
   522	        ),
   523	        Op::DropTable { name } => {
   524	            let canonical = name.to_ascii_uppercase();
   525	            if workbook.tables_mut().remove(canonical.as_str()).is_none() {
   526	                return Err(ReplayError::TableNotFound {
   527	                    index,
   528	                    name: name.clone(),
   529	                });
   530	            }
   531	            Ok(())
   532	        }
   533	        Op::RenameTable { old_name, new_name } => {
   534	            apply_rename_table(workbook, index, old_name, new_name)
   535	        }
   536	        Op::RenameColumn {
   537	            table,
   538	            old_name,
   539	            new_name,
   540	        } => apply_rename_column(workbook, index, table, old_name, new_name),
   541	        Op::ResizeTable {
   542	            name,
   543	            new_rows,
   544	            new_cols,
   545	            added_columns,

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '3535,3605p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  3535	    }
  3536	
  3537	    #[test]
  3538	    fn set_formula_with_function_call() {
  3539	        let mut wb = make_runtime_workbook();
  3540	        wb.put_at(0, 0, 0, Value::Number(10.0));
  3541	        wb.put_at(0, 1, 0, Value::Number(20.0));
  3542	        wb.put_at(0, 2, 0, Value::Number(30.0));
  3543	        let reg = default_registry();
  3544	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  3545	
  3546	        // =SUM(A1, A2, A3) → 60
  3547	        let v = rt.set_formula(0, 0, 1, "SUM(A1, A2, A3)").unwrap();
  3548	        assert_eq!(v, Value::Number(60.0));
  3549	    }
  3550	
  3551	    #[test]
  3552	    fn set_formula_propagates_div_by_zero() {
  3553	        let mut wb = make_runtime_workbook();
  3554	        let reg = default_registry();
  3555	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  3556	
  3557	        let v = rt.set_formula(0, 0, 0, "10 / 0").unwrap();
  3558	        assert_eq!(v, Value::Error(ErrorValue::DivZero));
  3559	        // Even error-evaluated formulas persist their text.
  3560	        assert_eq!(wb.formula_at(0, 0, 0).map(|s| s.as_ref()), Some("10 / 0"));
  3561	    }
  3562	
  3563	    #[test]
  3564	    fn set_formula_invalid_syntax_returns_parse_error() {
  3565	        let mut wb = make_runtime_workbook();
  3566	        let reg = default_registry();
  3567	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  3568	
  3569	        // Unclosed paren — guaranteed parse error.
  3570	        let result = rt.set_formula(0, 0, 0, "(1 + 2");
  3571	        assert!(
  3572	            matches!(result, Err(RuntimeError::Parse(_))),
  3573	            "expected Parse error, got {result:?}"
  3574	        );
  3575	        // No partial write on parse failure.
  3576	        assert_eq!(wb.read(ql_types::Address::new(0, 0, 0)), Value::Blank);
  3577	        assert!(wb.formula_at(0, 0, 0).is_none());
  3578	    }
  3579	
  3580	    #[test]
  3581	    fn set_formula_trailing_tokens_returns_parse_error() {
  3582	        let mut wb = make_runtime_workbook();
  3583	        let reg = default_registry();
  3584	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  3585	
  3586	        let result = rt.set_formula(0, 0, 0, "1 + 2 3");
  3587	        assert!(matches!(result, Err(RuntimeError::Parse(_))));
  3588	        // No partial write.
  3589	        assert!(wb.formula_at(0, 0, 0).is_none());
  3590	    }
  3591	
  3592	    /// Phase 2A.5 (2026-05-12): `=VAR.S(1, 2, 3)` end-to-end — lexer accepts the
  3593	    /// dotted identifier, parser builds Expr::Function { name: "VAR.S" }, binder
  3594	    /// produces ExprPlan::Function, scalar evaluator dispatches via the registry
  3595	    /// to the variance kernel. Sample variance of {1,2,3} is 1.0.
  3596	    #[test]
  3597	    fn set_formula_var_s_dotted_function_dispatches() {
  3598	        let mut wb = make_runtime_workbook();
  3599	        let reg = default_registry();
  3600	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  3601	
  3602	        let v = rt.set_formula(0, 0, 0, "VAR.S(1, 2, 3)").unwrap();
  3603	        assert_eq!(v, Value::Number(1.0));
  3604	    }
  3605	

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '3485,3535p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  3485	
  3486	        Ok((
  3487	            anchor_value,
  3488	            simd_eligible,
  3489	            old_spill_shape,
  3490	            new_spill_shape,
  3491	        ))
  3492	    }
  3493	}
  3494	
  3495	#[cfg(test)]
  3496	mod tests {
  3497	    use super::*;
  3498	    use ql_functions::default_registry;
  3499	    use ql_types::ErrorValue;
  3500	
  3501	    fn make_runtime_workbook() -> Workbook {
  3502	        let mut wb = Workbook::new();
  3503	        wb.add_sheet("S");
  3504	        wb
  3505	    }
  3506	
  3507	    // ===== set_formula =====
  3508	
  3509	    #[test]
  3510	    fn set_formula_literal_arithmetic() {
  3511	        let mut wb = make_runtime_workbook();
  3512	        let reg = default_registry();
  3513	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  3514	
  3515	        let v = rt.set_formula(0, 0, 0, "1 + 2 * 3").unwrap();
  3516	        assert_eq!(v, Value::Number(7.0));
  3517	        // Persisted: both the formula and the value.
  3518	        assert_eq!(wb.read(ql_types::Address::new(0, 0, 0)), Value::Number(7.0));
  3519	        assert_eq!(
  3520	            wb.formula_at(0, 0, 0).map(|s| s.as_ref()),
  3521	            Some("1 + 2 * 3")
  3522	        );
  3523	    }
  3524	
  3525	    #[test]
  3526	    fn set_formula_reads_existing_cell() {
  3527	        let mut wb = make_runtime_workbook();
  3528	        wb.put_at(0, 0, 0, Value::Number(10.0));
  3529	        let reg = default_registry();
  3530	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  3531	
  3532	        // =A1 * 2 → 20
  3533	        let v = rt.set_formula(0, 1, 0, "A1 * 2").unwrap();
  3534	        assert_eq!(v, Value::Number(20.0));
  3535	    }

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '10040,10135p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
 10040	            RuntimeError::TableCreateRejected { reason, .. } => {
 10041	                assert!(reason.contains("defined-name"), "reason: {reason}");
 10042	            }
 10043	            other => panic!("expected TableCreateRejected, got {other:?}"),
 10044	        }
 10045	    }
 10046	
 10047	    #[test]
 10048	    fn drop_table_removes_metadata_and_emits_op() {
 10049	        let mut wb = make_runtime_workbook();
 10050	        let reg = default_registry();
 10051	        let mut oplog = OpLog::new();
 10052	        {
 10053	            let mut rt = WorkbookRuntime::with_oplog(&mut wb, &reg, &mut oplog);
 10054	            rt.create_table("Sales", 0, 0, 0, 2, 1, true, false, vec!["Qty".into()])
 10055	                .unwrap();
 10056	            rt.drop_table("Sales").unwrap();
 10057	        }
 10058	        assert!(wb.lookup_table("Sales").is_none());
 10059	        let ops: Vec<Op> = oplog.iter().collect::<Result<_, _>>().unwrap();
 10060	        assert_eq!(ops.len(), 2);
 10061	        assert!(matches!(&ops[1], Op::DropTable { name } if name == "SALES"));
 10062	    }
 10063	
 10064	    #[test]
 10065	    fn drop_table_missing_errors() {
 10066	        let mut wb = make_runtime_workbook();
 10067	        let reg = default_registry();
 10068	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
 10069	        let err = rt.drop_table("Nope").unwrap_err();
 10070	        match err {
 10071	            RuntimeError::TableNotFound(n) => assert_eq!(n, "Nope"),
 10072	            other => panic!("expected TableNotFound, got {other:?}"),
 10073	        }
 10074	    }
 10075	
 10076	    /// **W5-155 (Phase 4.8.G.3):** `WorkbookRuntime::drop_table` must
 10077	    /// invoke the calcgraph `on_table_drop` hook so any formula
 10078	    /// referencing the dropped table is BFS-dirtied. Pre-W5-155 the
 10079	    /// runtime relied on plan-cache invalidation alone, which left the
 10080	    /// dirty set untouched and missed cells got stale values until an
 10081	    /// unrelated edit happened to touch them.
 10082	    #[test]
 10083	    fn drop_table_fires_on_table_drop_hook_and_dirties_readers() {
 10084	        use crate::CalcgraphSession;
 10085	        let mut wb = make_runtime_workbook();
 10086	        let reg = default_registry();
 10087	        let mut graph = CalcgraphSession::new();
 10088	        // Build: Sales table at A1:A3 (header + 2 data rows) with
 10089	        // column Qty, values 10/20, and B1 = SUM(Sales[Qty]).
 10090	        {
 10091	            let mut rt = WorkbookRuntime::with_graph(&mut wb, &reg, &mut graph);
 10092	            rt.create_table("Sales", 0, 0, 0, 3, 1, true, false, vec!["Qty".into()])
 10093	                .unwrap();
 10094	            rt.set_value(0, 1, 0, Value::Number(10.0)).unwrap();
 10095	            rt.set_value(0, 2, 0, Value::Number(20.0)).unwrap();
 10096	            let v = rt.set_formula(0, 0, 1, "SUM(Sales[Qty])").unwrap();
 10097	            assert_eq!(v, Value::Number(30.0));
 10098	            // Drain dirty so the post-drop assertion is unambiguous.
 10099	            let _ = rt.recompute_dirty().expect("graph attached");
 10100	        }
 10101	        let formula_node = graph
 10102	            .cell_node_for(0, 0, 1)
 10103	            .expect("B1 formula node registered");
 10104	        assert!(
 10105	            !graph.is_dirty(formula_node),
 10106	            "after recompute_dirty B1 should be clean"
 10107	        );
 10108	        let before = graph.hook_counts().table_drop;
 10109	
 10110	        // Drop the table — the hook must fire and dirty B1.
 10111	        {
 10112	            let mut rt = WorkbookRuntime::with_graph(&mut wb, &reg, &mut graph);
 10113	            rt.drop_table("Sales").unwrap();
 10114	        }
 10115	        assert_eq!(
 10116	            graph.hook_counts().table_drop,
 10117	            before + 1,
 10118	            "drop_table must call on_table_drop exactly once"
 10119	        );
 10120	        assert!(
 10121	            graph.is_dirty(formula_node),
 10122	            "B1 = SUM(Sales[Qty]) must be dirty after Sales is dropped"
 10123	        );
 10124	    }
 10125	
 10126	    // ===== W5-119 (Phase 4.8.I) — rename_table =====
 10127	
 10128	    #[test]
 10129	    fn rename_table_happy_path_rewrites_formula_text() {
 10130	        let mut wb = make_runtime_workbook();
 10131	        let reg = default_registry();
 10132	        {
 10133	            let mut rt = WorkbookRuntime::new(&mut wb, &reg);
 10134	            rt.create_table("Sales", 0, 0, 0, 3, 1, true, false, vec!["Qty".into()])
 10135	                .unwrap();

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '3245,3330p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  3245	                                        }
  3246	                                    };
  3247	                                session.reextract_deps(rn, plan.as_ref(), self.workbook);
  3248	                                session.mark_dirty(rn);
  3249	                            }
  3250	                        }
  3251	                        succeeded += 1;
  3252	                    }
  3253	                    Err(error) => failures.push(RecomputeFailure {
  3254	                        sheet,
  3255	                        row,
  3256	                        col,
  3257	                        formula_text: text,
  3258	                        error,
  3259	                    }),
  3260	                }
  3261	            }
  3262	        } // end MAX_ITERATIONS loop
  3263	
  3264	        // **Codex audit MEDIUM closure**: if we hit MAX_ITERATIONS
  3265	        // AND session.dirty() is still non-empty, the fixed point
  3266	        // wasn't reached. Surface as a synthetic RecomputeFailure
  3267	        // so the caller's is_complete() check signals the problem
  3268	        // instead of silently returning what looks like a clean
  3269	        // recompute. Per CLAUDE.md no-fallbacks rule.
  3270	        if iter_count == MAX_ITERATIONS && !session.dirty_formulas().is_empty() {
  3271	            failures.push(RecomputeFailure {
  3272	                sheet: 0,
  3273	                row: 0,
  3274	                col: 0,
  3275	                formula_text: Arc::from(format!(
  3276	                    "recompute_dirty hit MAX_ITERATIONS={MAX_ITERATIONS} \
  3277	                     with {} cells still dirty — possible runaway spill \
  3278	                     shape transition or workbook misconfiguration",
  3279	                    session.dirty_formulas().len()
  3280	                )),
  3281	                error: RuntimeError::RecomputeIterationCap,
  3282	            });
  3283	        }
  3284	
  3285	        self.graph = session_slot;
  3286	        Some(RecomputeResult {
  3287	            attempted,
  3288	            succeeded,
  3289	            failures,
  3290	            skipped_value_equality,
  3291	            simd_classified,
  3292	        })
  3293	    }
  3294	
  3295	    /// Phase 2B.3 helper for `recompute_all`: consult the bind-plan cache
  3296	    /// before doing lex/parse/bind work, then evaluate. Failures (lex /
  3297	    /// parse / bind) propagate as `RuntimeError`; the caller bundles them
  3298	    /// into a `RecomputeFailure`. Successful binds are cached so a
  3299	    /// subsequent recompute (or a `set_formula` editing a nearby cell
  3300	    /// with the same text) hits.
  3301	    ///
  3302	    /// Phase 3.6 (W5-39): this is the no-aggregate-cache wrapper. The
  3303	    /// `recompute_dirty` path uses
  3304	    /// [`Self::try_recompute_with_aggregate_cache`] which threads the
  3305	    /// session's aggregate cache through the evaluator.
  3306	    fn try_recompute_one_cached(
  3307	        &mut self,
  3308	        sheet: SheetId,
  3309	        row: RowId,
  3310	        col: ColId,
  3311	        formula_text: &Arc<str>,
  3312	    ) -> Result<Value, RuntimeError> {
  3313	        self.try_recompute_with_aggregate_cache(
  3314	            sheet,
  3315	            row,
  3316	            col,
  3317	            formula_text,
  3318	            &crate::aggregate_cache::NoAggregateCache,
  3319	        )
  3320	    }
  3321	
  3322	    /// Phase 3.6 (W5-39): variant of `try_recompute_one_cached` that
  3323	    /// threads an `AggregateCache` through the scalar evaluator so
  3324	    /// `SUM(Sales)` / `AVERAGE(Sales)` calls consult + populate the
  3325	    /// cache. Used by `recompute_dirty` which owns a session-side
  3326	    /// `InMemAggregateCache`.
  3327	    ///
  3328	    /// **W5-103 (#128):** also takes `row, col` so the spill-writeback
  3329	    /// branch inside `try_recompute_with_simd_profile` can materialize
  3330	    /// arrays at the correct anchor cell.

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '980,1045p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   980	                    // by on_set_formula. Resolve address + formula text
   981	                    // in a single pass.
   982	                    let Some((s, r, c)) = g.cell_address_for(node) else {
   983	                        continue;
   984	                    };
   985	                    if (s, r, c) == (anchor_sheet, anchor_row, anchor_col) {
   986	                        continue;
   987	                    }
   988	                    // A reader without a formula text means
   989	                    // cell_to_formulas has a stale entry — defensive skip.
   990	                    let Some(text) = self.workbook.formula_at(s, r, c).cloned() else {
   991	                        continue;
   992	                    };
   993	                    result.push((node, s, r, c, text));
   994	                }
   995	            }
   996	            result
   997	        };
   998	
   999	        // Step 2: re-bind + re-extract for each reader. The borrow of
  1000	        // `g` from step 1 is released; we now alternate immutable workbook
  1001	        // reads (inside the PlanCache miss closure) with mutable graph
  1002	        // mutations (reextract_deps).
  1003	        for (node, reader_sheet, reader_row, reader_col, text) in reader_info {
  1004	            let name_gen = self.workbook.names().generation();
  1005	            // **W5-150 (Phase 4.9.O HIGH-1):** cell-aware key when `@`
  1006	            // is present (see set_formula for rationale).
  1007	            let cell_anchor = if text.contains('@') {
  1008	                Some((reader_row, reader_col))
  1009	            } else {
  1010	                None
  1011	            };
  1012	            let cache_key = PlanCacheKey {
  1013	                text: Arc::clone(&text),
  1014	                sheet: reader_sheet,
  1015	                name_gen,
  1016	                cell_anchor,
  1017	            };
  1018	            let plan: Arc<crate::plan::ExprPlan> = match self
  1019	                .plan_cache
  1020	                .get_or_insert::<_, RuntimeError>(cache_key, || {
  1021	                    let tokens = lex(text.as_ref())?;
  1022	                    let expr = parse(tokens)?;
  1023	                    // **W5-114 (Phase 4.8.E):** carry reader cell address.
  1024	                    Ok(bind_with_site(
  1025	                        &expr,
  1026	                        BindSite::at_cell(ql_types::Address::new(
  1027	                            reader_sheet,
  1028	                            reader_row,
  1029	                            reader_col,
  1030	                        )),
  1031	                        self.workbook,
  1032	                        self.workbook,
  1033	                        self.workbook,
  1034	                    )?)
  1035	                }) {
  1036	                Ok(p) => p,
  1037	                Err(_e) => {
  1038	                    // **Codex audit pass-1 MEDIUM-2 closure (#129):**
  1039	                    // the reader's formula no longer binds (workbook
  1040	                    // state shifted in a way that broke its plan —
  1041	                    // e.g. a referenced name was removed). Per
  1042	                    // CLAUDE.md no-fallbacks, silently `continue` is
  1043	                    // not acceptable — that preserves stale
  1044	                    // `cell_to_formulas` + graph-edge state and the
  1045	                    // failure becomes invisible to the user.

exec
/bin/zsh -lc "rg -n \"UnknownTable|BindError|Value::Error\\(ErrorValue::Name|ErrorValue::Name|RuntimeError::Bind|Bind\\(\" crates/ql-exec/src crates/ql-types/src crates/ql-formula/src" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 exited 2 in 0ms:
rg: crates/ql-formula/src: No such file or directory (os error 2)
crates/ql-exec/src/calcgraph_session.rs:938:    /// 2. Re-bind it (table is gone → `BindError::UnknownTable` →
crates/ql-exec/src/calcgraph_session.rs:1331:    use crate::plan::BindError;
crates/ql-exec/src/calcgraph_session.rs:1767:                RuntimeError::Bind(BindError::UnresolvedName(_))
crates/ql-types/src/error.rs:72:            ErrorValue::Name => "#NAME?",
crates/ql-types/src/error.rs:93:        ErrorValue::Name,
crates/ql-types/src/error.rs:177:        assert_eq!(ErrorValue::Name.sigil(), "#NAME?");
crates/ql-exec/src/plan.rs:80:    /// [`BindError::NamedRangeInScalarContext`] instead of binding to this
crates/ql-exec/src/plan.rs:93:    /// this (`BindError::ArrayRowArityMismatch`) on every public input AND
crates/ql-exec/src/plan.rs:157:pub enum BindError {
crates/ql-exec/src/plan.rs:263:    UnknownTable(std::sync::Arc<str>),
crates/ql-exec/src/plan.rs:268:    UnknownTableColumn {
crates/ql-exec/src/plan.rs:340:/// (aggregate-arg) or surfaces as `BindError::NamedRangeInScalarContext`
crates/ql-exec/src/plan.rs:452:/// `BindError::UnresolvedName`; any cross-sheet ref (`SheetRef::Name`)
crates/ql-exec/src/plan.rs:453:/// produces `BindError::UnknownSheet`.
crates/ql-exec/src/plan.rs:456:pub fn bind(expr: &Expr, owning_sheet: SheetId) -> Result<ExprPlan, BindError> {
crates/ql-exec/src/plan.rs:462:/// `BindError::UnknownSheet`. Callers that need cross-sheet support
crates/ql-exec/src/plan.rs:469:) -> Result<ExprPlan, BindError> {
crates/ql-exec/src/plan.rs:490:) -> Result<ExprPlan, BindError> {
crates/ql-exec/src/plan.rs:508:/// surfaces `BindError::ThisRowRequiresOwningCell` in that case.
crates/ql-exec/src/plan.rs:535:/// `BindError::UnknownTable`).
crates/ql-exec/src/plan.rs:542:) -> Result<ExprPlan, BindError> {
crates/ql-exec/src/plan.rs:549:/// surfaces `BindError::UnknownTable`.
crates/ql-exec/src/plan.rs:555:) -> Result<ExprPlan, BindError> {
crates/ql-exec/src/plan.rs:566:) -> Result<ExprPlan, BindError> {
crates/ql-exec/src/plan.rs:585:) -> Result<ExprPlan, BindError> {
crates/ql-exec/src/plan.rs:631:        Expr::RangeRef(_) => Err(BindError::UnsupportedVariant(
crates/ql-exec/src/plan.rs:671:                return Err(BindError::EmptyArrayLiteral);
crates/ql-exec/src/plan.rs:678:                    return Err(BindError::ArrayRowArityMismatch {
crates/ql-exec/src/plan.rs:699:                            return Err(BindError::ArrayCellNotLiteral {
crates/ql-exec/src/plan.rs:713:        Expr::Spill(_) => Err(BindError::UnsupportedVariant(
crates/ql-exec/src/plan.rs:751:                    BindContext::Scalar => Err(BindError::NamedRangeInScalarContext(name.clone())),
crates/ql-exec/src/plan.rs:757:                    Err(BindError::NamedFormulaUnsupported(name.clone()))
crates/ql-exec/src/plan.rs:760:                Some(ResolvedName::Blank) => Err(BindError::NamedTargetIsBlank(name.clone())),
crates/ql-exec/src/plan.rs:762:                    Err(BindError::NamedTargetIsError(name.clone(), e))
crates/ql-exec/src/plan.rs:764:                None => Err(BindError::UnresolvedName(name.clone())),
crates/ql-exec/src/plan.rs:781:        // fail with `BindError::R1C1RequiresAnchor` rather than
crates/ql-exec/src/plan.rs:832:        // Out-of-range narrow → `BindError::UnsupportedVariant` to
crates/ql-exec/src/plan.rs:859:) -> Result<ExprPlan, BindError> {
crates/ql-exec/src/plan.rs:883:                return Err(BindError::EmptyArrayLiteral);
crates/ql-exec/src/plan.rs:962:        Expr::Spill(_) => Err(BindError::UnsupportedVariant(
crates/ql-exec/src/plan.rs:977:) -> Result<ExprPlan, BindError> {
crates/ql-exec/src/plan.rs:981:        .ok_or(BindError::ImplicitIntersectionRequiresAnchor)?;
crates/ql-exec/src/plan.rs:1088:        RangeRef::R1C1Cells { .. } => Err(BindError::UnsupportedVariant(
crates/ql-exec/src/plan.rs:1120:) -> Result<u32, BindError> {
crates/ql-exec/src/plan.rs:1128:                return Err(BindError::R1C1OutOfBounds {
crates/ql-exec/src/plan.rs:1137:                return Err(BindError::R1C1RequiresAnchor);
crates/ql-exec/src/plan.rs:1141:                return Err(BindError::R1C1OutOfBounds {
crates/ql-exec/src/plan.rs:1158:/// surface them as distinct `BindError` variants instead of either coercing
crates/ql-exec/src/plan.rs:1196:/// returns `None`, so any NameRef becomes `BindError::UnresolvedName`.
crates/ql-exec/src/plan.rs:1216:/// entry points: any cross-sheet ref surfaces as `BindError::UnknownSheet`.
crates/ql-exec/src/plan.rs:1241:/// `BindError::UnknownTable(name)`.
crates/ql-exec/src/plan.rs:1275:) -> Result<ExprPlan, BindError> {
crates/ql-exec/src/plan.rs:1279:        .ok_or_else(|| BindError::UnknownTable(Arc::clone(table_name)))?;
crates/ql-exec/src/plan.rs:1282:    let unknown_column = |col: &Arc<str>| BindError::UnknownTableColumn {
crates/ql-exec/src/plan.rs:1293:                BindError::StructuredRefDegenerateRange(Arc::clone(&canonical_name))
crates/ql-exec/src/plan.rs:1306:                BindError::StructuredRefDegenerateRange(Arc::clone(&canonical_name))
crates/ql-exec/src/plan.rs:1316:                BindError::StructuredRefDegenerateRange(Arc::clone(&canonical_name))
crates/ql-exec/src/plan.rs:1319:                BindError::StructuredRefDegenerateRange(Arc::clone(&canonical_name))
crates/ql-exec/src/plan.rs:1354:) -> Result<(Range, bool), BindError> {
crates/ql-exec/src/plan.rs:1395:) -> Result<(ql_types::RowId, ql_types::RowId), BindError> {
crates/ql-exec/src/plan.rs:1400:            .ok_or_else(|| BindError::StructuredRefDegenerateRange(Arc::clone(canonical)))?;
crates/ql-exec/src/plan.rs:1414:                .ok_or_else(|| BindError::TableHasNoHeader(Arc::clone(canonical)))?,
crates/ql-exec/src/plan.rs:1417:                .ok_or_else(|| BindError::TableHasNoTotals(Arc::clone(canonical)))?,
crates/ql-exec/src/plan.rs:1420:                .ok_or_else(|| BindError::StructuredRefDegenerateRange(Arc::clone(canonical)))?,
crates/ql-exec/src/plan.rs:1430:                    .ok_or_else(|| BindError::StructuredRefDegenerateRange(Arc::clone(canonical)))?
crates/ql-exec/src/plan.rs:1447:) -> Result<(ql_types::ColId, ql_types::ColId), BindError> {
crates/ql-exec/src/plan.rs:1461:                        .ok_or_else(|| BindError::UnknownTableColumn {
crates/ql-exec/src/plan.rs:1472:                        .ok_or_else(|| BindError::UnknownTableColumn {
crates/ql-exec/src/plan.rs:1479:                        .ok_or_else(|| BindError::UnknownTableColumn {
crates/ql-exec/src/plan.rs:1525:///   `BindError::UnknownSheet`.
crates/ql-exec/src/plan.rs:1530:) -> Result<SheetId, BindError> {
crates/ql-exec/src/plan.rs:1536:            .ok_or_else(|| BindError::UnknownSheet(n.clone())),
crates/ql-exec/src/plan.rs:1648:            Err(BindError::UnsupportedVariant(_))
crates/ql-exec/src/plan.rs:1787:            BindError::UnknownSheet(name) => assert_eq!(name.as_ref(), "Nonexistent"),
crates/ql-exec/src/plan.rs:1804:        assert!(matches!(err, BindError::UnknownSheet(_)));
crates/ql-exec/src/plan.rs:1898:            BindError::ArrayRowArityMismatch {
crates/ql-exec/src/plan.rs:1927:            BindError::ArrayCellNotLiteral { row, col, got } => {
crates/ql-exec/src/plan.rs:1947:            BindError::ArrayCellNotLiteral { got, .. } => assert_eq!(got, "Array"),
crates/ql-exec/src/plan.rs:1958:        assert!(matches!(err, BindError::EmptyArrayLiteral));
crates/ql-exec/src/plan.rs:1969:        assert!(matches!(err, BindError::EmptyArrayLiteral));
crates/ql-exec/src/plan.rs:1983:            BindError::ArrayCellNotLiteral { row, col, got } => {
crates/ql-exec/src/plan.rs:2004:            BindError::ArrayCellNotLiteral { row, col, got } => {
crates/ql-exec/src/plan.rs:2143:            BindError::UnknownTable(name) => assert_eq!(name.as_ref(), "Sales"),
crates/ql-exec/src/plan.rs:2144:            other => panic!("expected UnknownTable, got {other:?}"),
crates/ql-exec/src/plan.rs:2164:            BindError::UnknownTableColumn { table, column } => {
crates/ql-exec/src/plan.rs:2168:            other => panic!("expected UnknownTableColumn, got {other:?}"),
crates/ql-exec/src/plan.rs:2335:            BindError::TableHasNoHeader(name) => assert_eq!(name.as_ref(), "SALES"),
crates/ql-exec/src/plan.rs:2354:    fn bind_r1c1_at(expr: &Expr, anchor: Address) -> Result<ExprPlan, BindError> {
crates/ql-exec/src/plan.rs:2364:    fn bind_r1c1_no_anchor(expr: &Expr) -> Result<ExprPlan, BindError> {
crates/ql-exec/src/plan.rs:2499:        assert!(matches!(err, BindError::R1C1RequiresAnchor));
crates/ql-exec/src/plan.rs:2532:            BindError::R1C1OutOfBounds { resolved_row, .. } => {
crates/ql-exec/src/plan.rs:2554:            BindError::R1C1OutOfBounds { resolved_row, .. } => {
crates/ql-exec/src/plan.rs:2610:            BindError::UnknownSheet(n) => assert_eq!(n.as_ref(), "Missing"),
crates/ql-exec/src/plan.rs:2639:        assert!(matches!(err, BindError::R1C1OutOfBounds { .. }));
crates/ql-exec/src/plan.rs:2656:    fn bind_at(src: &str, anchor: Address) -> Result<ExprPlan, BindError> {
crates/ql-exec/src/plan.rs:2953:        assert!(matches!(err, BindError::ImplicitIntersectionRequiresAnchor));
crates/ql-exec/src/lib.rs:9://!   `BindError` variants including `UnresolvedName`,
crates/ql-exec/src/lib.rs:99:    BindError, BindSite, ExprPlan, NameLookup, ResolvedName, SheetResolver, TableLookup,
crates/ql-exec/src/transaction.rs:522:                Err(RuntimeError::Bind(crate::plan::BindError::UnresolvedName(
crates/ql-exec/src/transaction.rs:526:            "expected Bind(UnresolvedName), got {result:?}"
crates/ql-exec/src/transaction.rs:795:        use crate::plan::BindError;
crates/ql-exec/src/transaction.rs:804:            Err(RuntimeError::Bind(BindError::NamedTargetIsBlank(name))) => {
crates/ql-exec/src/transaction.rs:808:            other => panic!("expected Bind(NamedTargetIsBlank), got {other:?}"),
crates/ql-exec/src/transaction.rs:814:        use crate::plan::BindError;
crates/ql-exec/src/transaction.rs:826:            Err(RuntimeError::Bind(BindError::NamedTargetIsError(name, err))) => {
crates/ql-exec/src/transaction.rs:830:            other => panic!("expected Bind(NamedTargetIsError), got {other:?}"),
crates/ql-exec/src/env.rs:386:/// `ResolvedName` variants (the binder converts them to specific `BindError`
crates/ql-exec/src/workbook_runtime.rs:50:use crate::plan::{bind_with_site, BindError, BindSite};
crates/ql-exec/src/workbook_runtime.rs:148:    // Display now that LexError + BindError implement thiserror::Error with
crates/ql-exec/src/workbook_runtime.rs:170:    Bind(BindError),
crates/ql-exec/src/workbook_runtime.rs:293:impl From<BindError> for RuntimeError {
crates/ql-exec/src/workbook_runtime.rs:294:    fn from(e: BindError) -> Self {
crates/ql-exec/src/workbook_runtime.rs:295:        Self::Bind(e)
crates/ql-exec/src/workbook_runtime.rs:1718:    /// dropped table re-bind to `BindError::UnknownTable` on next
crates/ql-exec/src/workbook_runtime.rs:1730:    /// 3. Next recompute re-binds → `BindError::UnknownTable` →
crates/ql-exec/src/workbook_runtime.rs:3979:        // No name registered → bind-time UnresolvedName, surfaced as RuntimeError::Bind.
crates/ql-exec/src/workbook_runtime.rs:3982:            Err(RuntimeError::Bind(BindError::UnresolvedName(name))) => {
crates/ql-exec/src/workbook_runtime.rs:3985:            other => panic!("expected Bind(UnresolvedName), got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:3993:    /// Rust-debug syntax. Previously `RuntimeError::Bind(BindError::Unresolved
crates/ql-exec/src/workbook_runtime.rs:4060:            Err(RuntimeError::Bind(BindError::NamedRangeInScalarContext(name))) => {
crates/ql-exec/src/workbook_runtime.rs:4064:            other => panic!("expected Bind(NamedRangeInScalarContext), got {other:?}"),
crates/ql-exec/src/workbook_runtime.rs:4769:            Err(RuntimeError::Bind(BindError::NamedRangeInScalarContext(n))) => {
crates/ql-exec/src/workbook_runtime.rs:4777:            Err(RuntimeError::Bind(BindError::NamedRangeInScalarContext(n))) => {
crates/ql-exec/src/workbook_runtime.rs:4785:            Err(RuntimeError::Bind(BindError::NamedRangeInScalarContext(n))) => {
crates/ql-exec/src/workbook_runtime.rs:4809:            Err(RuntimeError::Bind(BindError::NamedFormulaUnsupported(n))) => {
crates/ql-exec/src/workbook_runtime.rs:7268:            RuntimeError::Bind(crate::plan::BindError::UnknownSheet(name)) => {
crates/ql-exec/src/workbook_runtime.rs:7271:            other => panic!("expected RuntimeError::Bind(UnknownSheet), got {other:?}"),
crates/ql-exec/src/scalar.rs:53:/// `Function` variants return `#NAME?` (`ErrorValue::Name`). To dispatch through a
crates/ql-exec/src/scalar.rs:72:        ExprPlan::Function { .. } => Value::Error(ErrorValue::Name),
crates/ql-exec/src/scalar.rs:433:                None => Value::Error(ErrorValue::Name),
crates/ql-exec/src/scalar.rs:437:        // supposed to surface `BindError::NamedRangeInScalarContext`
crates/ql-exec/src/scalar.rs:548:/// `ExprPlan::Array(vec![])` (`BindError::EmptyArrayLiteral`) and
crates/ql-exec/src/scalar.rs:550:/// (`BindError::ArrayRowArityMismatch`). So when we reach this code,
crates/ql-exec/src/scalar.rs:1192:        assert_eq!(eval_scalar(&plan, &env), Value::Error(ErrorValue::Name));
crates/ql-exec/src/scalar.rs:1203:        assert_eq!(eval_reg(&expr, &env, &reg), Value::Error(ErrorValue::Name));

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '560,650p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   560	        // The `parse → print_with` round-trip canonicalizes:
   561	        //   - R1C1Ref → `$E$3` / `A1` (abs flags from AxisSpec).
   562	        //   - `2,5` (DE) → `2.5` (EN).
   563	        //   - `SUM(A1; B2)` (DE) → `SUM(A1, B2)` (EN).
   564	        //   - `@A1` stays `@A1` (implicit intersection is locale + mode
   565	        //     invariant; preserved through canonicalization).
   566	        let mode = self.workbook.reference_mode();
   567	        let locale = self.workbook.locale();
   568	        let addr = ql_types::Address::new(sheet, row, col);
   569	        let site = FormulaSite::at_cell(addr);
   570	
   571	        let tokens = lex_with(raw_text.as_ref(), mode, locale)?;
   572	        let expr = parse(tokens)?;
   573	        let canonical_text: Arc<str> = Arc::from(print_with(
   574	            &expr,
   575	            ql_types::ReferenceMode::A1,
   576	            ql_types::Locale::EnUs,
   577	            Some(site),
   578	        )?);
   579	        // **W5-147:** downstream code (put_formula, write_spill, the
   580	        // failure-tracking path that reads `formula_text` by name) all
   581	        // expect a single `formula_text` Arc<str>. Rebind here so the
   582	        // canonical form propagates through unchanged code paths.
   583	        let formula_text: Arc<str> = Arc::clone(&canonical_text);
   584	
   585	        // Phase 2B.3: consult the bind-plan cache. **W5-147 update**:
   586	        // the cache key now uses `canonical_text` (not the raw input)
   587	        // so two writes with different RAW text (`A1` vs `R1C1` for
   588	        // the same anchor; `2.5` vs `2,5`) but the same canonical form
   589	        // share a single plan. The NameTable-generation slot still
   590	        // invalidates on name registration.
   591	        //
   592	        // **W5-150 (Phase 4.9.O HIGH-1 closure):** `@`-bearing
   593	        // canonical text narrows AT BIND TIME using the formula's
   594	        // own cell (W5-144). The bound plan is cell-specific, so
   595	        // include the cell anchor in the cache key when `@` is
   596	        // present. Non-`@` formulas keep `cell_anchor: None` so the
   597	        // cache continues to share plans across cells (preserves
   598	        // the SUM(A1:A10)-across-rows fast path).
   599	        let name_gen = self.workbook.names().generation();
   600	        let cell_anchor = if canonical_text.contains('@') {
   601	            Some((row, col))
   602	        } else {
   603	            None
   604	        };
   605	        let cache_key = PlanCacheKey {
   606	            text: Arc::clone(&canonical_text),
   607	            sheet,
   608	            name_gen,
   609	            cell_anchor,
   610	        };
   611	        let plan: Arc<crate::plan::ExprPlan> =
   612	            self.plan_cache
   613	                .get_or_insert::<_, RuntimeError>(cache_key, || {
   614	                    // We already have the parsed `expr` — bind directly.
   615	                    // **W5-114 (Phase 4.8.E):** carry the formula's own cell
   616	                    // address through BindSite; 4.8.F's structured-ref
   617	                    // `[@Col]` resolution reads it.
   618	                    Ok(bind_with_site(
   619	                        &expr,
   620	                        BindSite::at_cell(addr),
   621	                        self.workbook,
   622	                        self.workbook,
   623	                        self.workbook,
   624	                    )?)
   625	                })?;
   626	
   627	        // **Phase 2A.3.b op-log emission, BEFORE any storage mutation.**
   628	        // The contract on this method ("On error, the workbook is unchanged")
   629	        // requires that op-log append fail BEFORE we touch workbook state.
   630	        // We emit `PutFormula` only — the evaluated value isn't recorded
   631	        // because replay re-derives it via `WorkbookRuntime::recompute_all`
   632	        // (see replay.rs module docs).
   633	        //
   634	        // **W5-103 megaudit HIGH-3 closure** (Codex pass-2 finding): pre-4.7.J
   635	        // ordering had this append BEFORE mutation. The 4.7.J.5 +
   636	        // 4.7.J.2 clear_spill calls were inserted between bind and append,
   637	        // which broke atomicity — a failing append would leave the host
   638	        // spill / old self-spill already dissolved. Restored ordering:
   639	        // bind → append → (only then) clear_spill mutations.
   640	        if let Some(oplog) = self.oplog.as_deref_mut() {
   641	            oplog.append(Op::PutFormula {
   642	                sheet,
   643	                row,
   644	                col,
   645	                // **W5-147 (Phase 4.9.K):** persist CANONICAL (A1+EnUs)
   646	                // text per design § 4.4. Replay against a fresh
   647	                // workbook re-applies the canonical text regardless
   648	                // of the original user-typed mode / locale.
   649	                text: canonical_text.as_ref().to_owned(),
   650	            })?;

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '1045,1085p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1045	                    // failure becomes invisible to the user.
  1046	                    //
  1047	                    // We can't bubble up: the user's set_formula on
  1048	                    // the ANCHOR succeeded; failing it because some
  1049	                    // UNRELATED reader's bind broke would be punitive.
  1050	                    //
  1051	                    // Compromise: mark the reader's NodeId dirty so
  1052	                    // the next recompute re-attempts evaluation. The
  1053	                    // recompute path will produce a visible error at
  1054	                    // the reader's cell. The stale graph state for
  1055	                    // this reader survives until then — accepted as
  1056	                    // a known limitation; a full fix needs a
  1057	                    // structured diagnostics channel (deferred).
  1058	                    if let Some(g) = self.graph.as_deref_mut() {
  1059	                        g.mark_dirty(node);
  1060	                    }
  1061	                    continue;
  1062	                }
  1063	            };
  1064	            if let Some(g) = self.graph.as_deref_mut() {
  1065	                g.reextract_deps(node, plan.as_ref(), self.workbook);
  1066	                // **megaudit HIGH-1 closure (#127)**: dirty the
  1067	                // re-extracted reader. on_set_value at the OLD/NEW
  1068	                // footprint addresses (fired before this loop) used
  1069	                // the PRE-rewrite cell_to_formulas index. After
  1070	                // reextract_deps moves the reader's index entry, a
  1071	                // reader that USED to be at an old address but NOW
  1072	                // points to a different address would not have been
  1073	                // dirtied by the earlier on_set_value pass (its old
  1074	                // index entry was empty by the time the new index
  1075	                // landed). Marking the reader dirty here ensures the
  1076	                // next recompute_dirty re-evaluates it against the
  1077	                // post-dissolution / post-reshape workbook state.
  1078	                g.mark_dirty(node);
  1079	            }
  1080	        }
  1081	    }
  1082	
  1083	    /// **W5-103 (Phase 4.7.J.2 / 4.7.J.3)** — spill writeback path.
  1084	    /// Called from `set_formula` when `eval_at_cell_boundary` returns
  1085	    /// `EvalResult::Array`. Performs (per design § 8.1):

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/calcgraph_session.rs | sed -n '820,920p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/calcgraph_session.rs | sed -n '760,820p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '2840,3025p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   760	            succeeded,
   761	            failures,
   762	        }
   763	    }
   764	
   765	    /// Internal: lex + parse + bind a formula text against the
   766	    /// workbook's NameTable. Used by `rebuild_from_workbook` and by
   767	    /// `on_set_formula`'s text-only fallback. Phase 3.3 may add a
   768	    /// cached version that integrates with `PlanCache`.
   769	    ///
   770	    /// **W5-114 (Phase 4.8.E):** signature now takes `BindSite` so
   771	    /// the formula's cell address flows through for structured-ref
   772	    /// `[@Col]` resolution (4.8.F).
   773	    fn bind_text(text: &str, site: BindSite, wb: &Workbook) -> Result<ExprPlan, RuntimeError> {
   774	        let tokens = lex(text)?;
   775	        let expr = parse(tokens)?;
   776	        // W5-92 (Phase 4.6.D): pass `wb` for names so the two-tier
   777	        // sheet-then-workbook scope chain fires; was `wb.names()`
   778	        // (workbook-scoped only).
   779	        Ok(bind_with_site(&expr, site, wb, wb, wb)?)
   780	    }
   781	
   782	    /// **G3-02 acceptance (hook 1/5).** Mutation hook fired by
   783	    /// `WorkbookRuntime::set_value`. Phase 3.3 (2026-05-12): the hook
   784	    /// now marks every formula that depends on `(sheet, row, col)`
   785	    /// dirty via two parallel paths:
   786	    ///
   787	    /// 1. Range deps via `Graph::dependents_for_cell` — stripe lookup
   788	    ///    + precision check (DIR-3-02 acceptance).
   789	    /// 2. Direct cell deps via the session-side `cell_to_formulas`
   790	    ///    reverse index (formulas that hold an `ExprPlan::CellRef` to
   791	    ///    this address).
   792	    ///
   793	    /// The hook DOES NOT create a node for the written cell: nodes
   794	    /// exist for FORMULA cells only; dep lookups for plain literal
   795	    /// cells go through the (sheet, row, col) tuple in both indices.
   796	    pub fn on_set_value(&mut self, sheet: SheetId, row: RowId, col: ColId) {
   797	        self.hook_counts.set_value = self.hook_counts.set_value.saturating_add(1);
   798	        self.mark_dirty_from_cell_write(sheet, row, col);
   799	    }
   800	
   801	    /// **G3-02 acceptance (hook 2/5).** Mutation hook fired by
   802	    /// `WorkbookRuntime::set_formula`. Ensures the cell has a node,
   803	    /// walks the bound plan to register cell + range + name + volatile
   804	    /// dependencies (Phase 3.2), AND propagates dirty to downstream
   805	    /// formulas (Phase 3.3) — set_formula at a cell that other
   806	    /// formulas already reference means those downstream formulas now
   807	    /// see a different value and must recompute.
   808	    ///
   809	    /// The runtime passes the already-bound `ExprPlan` it just
   810	    /// evaluated; this hook does NOT re-bind. The plan shape is stable
   811	    /// since 2B.4 (Phase 3.2 doesn't add new variants).
   812	    pub fn on_set_formula(
   813	        &mut self,
   814	        sheet: SheetId,
   815	        row: RowId,
   816	        col: ColId,
   817	        plan: &ExprPlan,
   818	        workbook: &Workbook,
   819	    ) {
   820	        self.hook_counts.set_formula = self.hook_counts.set_formula.saturating_add(1);

 succeeded in 0ms:
   820	        self.hook_counts.set_formula = self.hook_counts.set_formula.saturating_add(1);
   821	        // Phase 3.4: distinguish first-time-formula at this cell vs
   822	        // re-bind. The first case needs RETROACTIVE forward edges
   823	        // from previously-existing formulas that already reference
   824	        // this cell (those formulas' extract didn't see a NodeId for
   825	        // this cell at the time, so no edge was added).
   826	        let is_new_formula = self.cell_node_for(sheet, row, col).is_none();
   827	        let node = self.or_insert_cell_node(sheet, row, col);
   828	        if is_new_formula {
   829	            let referencing: Vec<NodeId> = self
   830	                .cell_to_formulas
   831	                .get(&(sheet, row, col))
   832	                .map(|s| s.iter().copied().collect())
   833	                .unwrap_or_default();
   834	            for prev in referencing {
   835	                if prev != node {
   836	                    self.graph.add_edge(prev, node);
   837	                }
   838	            }
   839	        }
   840	        self.extract_and_register_deps(node, sheet, plan, workbook);
   841	        // Phase 3.3: dirty downstream — formulas referencing the cell
   842	        // whose formula text just changed see a (potentially) new
   843	        // value. The formula itself is NOT marked dirty (the runtime
   844	        // wrote a fresh value for it before invoking this hook).
   845	        self.mark_dirty_from_cell_write(sheet, row, col);
   846	    }
   847	
   848	    /// **G3-02 acceptance (hook 3/5).** Mutation hook fired by
   849	    /// `WorkbookRuntime::clear_formula`. Phase 3.2: drops the cell's
   850	    /// session-side dep state (formula_deps, volatile, name reverse
   851	    /// index, cell reverse index). Phase 3.3: also marks downstream
   852	    /// formulas dirty (the cleared formula no longer produces its
   853	    /// prior value; dependents must recompute) and removes the
   854	    /// cleared formula itself from the dirty set.
   855	    ///
   856	    /// **W5-52 (audit closure):** also revoke graph-side state —
   857	    /// `clear_outgoing` (direct cell edges) + `clear_range_deps_for_formula`
   858	    /// (range deps in stripes + `formula_to_range_deps`). W5-50 wired
   859	    /// this into `extract_and_register_deps` (the rebind path) but
   860	    /// missed the clear path; Codex + Sonnet mega-audit independently
   861	    /// caught it. Without these calls, clearing a formula leaves the
   862	    /// same stale graph state that W5-50 fixes — reproducing the
   863	    /// H3/H4 staleness class on the clear path.
   864	    pub fn on_clear_formula(&mut self, sheet: SheetId, row: RowId, col: ColId) {
   865	        self.hook_counts.clear_formula = self.hook_counts.clear_formula.saturating_add(1);
   866	        if let Some(node) = self.cell_index.get(&(sheet, row, col)).copied() {
   867	            self.remove_formula_deps(node);
   868	            // W5-52: revoke graph-side state for the cleared formula.
   869	            // Without these, stale outgoing edges + stripe entries
   870	            // persist and the next schedule_dirty can rediscover the
   871	            // cleared formula as a dependent of writes to its OLD
   872	            // deps (or as a Tarjan cycle participant if it pointed
   873	            // at a cell that later points back).
   874	            self.graph.clear_outgoing(node);
   875	            self.graph.clear_range_deps_for_formula(node);
   876	            // The cleared formula doesn't compute anymore. If it was
   877	            // dirty (queued for recompute), drop it from the set.
   878	            self.dirty.remove(&node);
   879	        }
   880	        // Downstream formulas see this cell change — propagate dirty.
   881	        self.mark_dirty_from_cell_write(sheet, row, col);
   882	    }
   883	
   884	    /// **G3-02 acceptance (hook 4/5).** Mutation hook fired by
   885	    /// `WorkbookRuntime::set_name`. Phase 3.3 (2026-05-12): marks all
   886	    /// formulas referencing `name` (per `name_to_formulas`) dirty.
   887	    /// Today this reverse index is populated only by
   888	    /// `ExprPlan::AggregateNameRef` — see GAP-R-07 for the scalar-
   889	    /// NameRef precision gap (PlanCache `name_gen` still covers
   890	    /// correctness; per-name precision is the open follow-up).
   891	    ///
   892	    /// Phase 3.10 megaudit H2 (2026-05-12): the prior implementation
   893	    /// only marked DIRECT name-referencing formulas dirty, leaving
   894	    /// downstream-of-downstream stale. Fixed: each directly-affected
   895	    /// formula now seeds a `mark_dirty_from_cell_write` BFS at its
   896	    /// own cell address, fanning out the same way cell writes do.
   897	    pub fn on_set_name(&mut self, name: &str) {
   898	        self.hook_counts.set_name = self.hook_counts.set_name.saturating_add(1);
   899	        let upper = name.to_ascii_uppercase();
   900	        let dependents: Vec<NodeId> = self
   901	            .name_to_formulas
   902	            .get(upper.as_str())
   903	            .map(|s| s.iter().copied().collect())
   904	            .unwrap_or_default();
   905	        for n in dependents {
   906	            self.dirty.insert(n);
   907	            // Phase 3.10 H2 fix: transitive fanout. Whatever depends on
   908	            // this name-referencing formula must also recompute when
   909	            // the name's target changes, just like for a direct cell
   910	            // edit. mark_dirty_from_cell_write does the BFS via the
   911	            // reverse-dep + stripe path so any chain or range dep is
   912	            // reached.
   913	            if let Some((s, r, c)) = self.cell_address_for(n) {
   914	                self.mark_dirty_from_cell_write(s, r, c);
   915	            }
   916	        }
   917	    }
   918	
   919	    /// **G3-02 acceptance (hook 5/5).** Mutation hook fired by
   920	    /// `WorkbookRuntime::add_sheet`. Phase 3.1 stub still: bumps

 succeeded in 0ms:
  2840	        //
  2841	        // Previously the validate path used scalar eval directly, which
  2842	        // made the IDE preview inconsistent with `set_formula`'s actual
  2843	        // spill behavior. With this change, validate and set_formula
  2844	        // produce equivalent anchor-value previews. The IDE doesn't get
  2845	        // the SHAPE of the spill from validate; that would need a
  2846	        // dedicated `ValidationResult` enum (deferred — see GAP-I-04).
  2847	        let nc = crate::aggregate_cache::NoAggregateCache;
  2848	        let result = crate::scalar::eval_at_cell_boundary(&plan, &env, self.registry, &nc);
  2849	        Ok(match result {
  2850	            crate::eval_result::EvalResult::Scalar(v) => v,
  2851	            // Anchor cell preview = array.at(0,0). Degenerate arrays
  2852	            // surface as `#CALC!` per the existing scalar-context
  2853	            // contract (preserved via `into_scalar_for_test`-style
  2854	            // logic inlined here to avoid the cfg(test) gate).
  2855	            crate::eval_result::EvalResult::Array(a) if a.is_degenerate() => {
  2856	                Value::Error(ErrorValue::Calc)
  2857	            }
  2858	            crate::eval_result::EvalResult::Array(a) => a.at(0, 0).clone(),
  2859	        })
  2860	    }
  2861	
  2862	    pub fn recompute_all(&mut self) -> RecomputeResult {
  2863	        // Snapshot the formula list so we don't hold a borrow during eval.
  2864	        let entries: Vec<(SheetId, RowId, ColId, Arc<str>)> = self
  2865	            .workbook
  2866	            .iter_formulas()
  2867	            .map(|(s, r, c, f)| (s, r, c, Arc::clone(f)))
  2868	            .collect();
  2869	        let attempted = entries.len();
  2870	        let mut succeeded = 0;
  2871	        let mut failures: Vec<RecomputeFailure> = Vec::new();
  2872	
  2873	        for (sheet, row, col, formula_text) in entries {
  2874	            match self.try_recompute_one_cached(sheet, row, col, &formula_text) {
  2875	                Ok(value) => {
  2876	                    // Phase 3.5 (CORR-25): formula outputs route to the
  2877	                    // COMPUTED overlay, never the user lane.
  2878	                    self.workbook.put_computed_at(sheet, row, col, value);
  2879	                    succeeded += 1;
  2880	                }
  2881	                Err(error) => {
  2882	                    failures.push(RecomputeFailure {
  2883	                        sheet,
  2884	                        row,
  2885	                        col,
  2886	                        formula_text,
  2887	                        error,
  2888	                    });
  2889	                }
  2890	            }
  2891	        }
  2892	
  2893	        RecomputeResult {
  2894	            attempted,
  2895	            succeeded,
  2896	            failures,
  2897	            // Phase 3.8: `recompute_all` doesn't run the VEQ check —
  2898	            // it's the HashMap-order legacy path that always
  2899	            // re-evaluates everything. `recompute_dirty` is the path
  2900	            // that benefits from value-equality short-circuit.
  2901	            skipped_value_equality: 0,
  2902	            // Phase 3.9: SIMD-eligibility profile is recompute_dirty-
  2903	            // only. `recompute_all` is the legacy full-pass path.
  2904	            simd_classified: 0,
  2905	        }
  2906	    }
  2907	
  2908	    /// **Engine Phase 3.4 (2026-05-12) — W5-37 SCH-3-01..04 entry
  2909	    /// point.** Recompute only the formulas the attached
  2910	    /// [`CalcgraphSession`] has marked dirty since the last call.
  2911	    /// The scheduler runs iterative Tarjan SCC over the dirty
  2912	    /// subset; nodes in non-trivial SCCs (or with self-loops) are
  2913	    /// written as `Value::Error(ErrorValue::Circ)`. Everything else
  2914	    /// is evaluated in topological order via the existing PlanCache
  2915	    /// pipeline.
  2916	    ///
  2917	    /// Returns `None` when no `CalcgraphSession` is attached (the
  2918	    /// runtime was constructed via `new` or `with_oplog`); the
  2919	    /// dirty-set lives on the session, so we can't do incremental
  2920	    /// recompute without it. Callers in that mode should use
  2921	    /// `recompute_all` for a full HashMap-order pass instead.
  2922	    ///
  2923	    /// Returns `Some(RecomputeResult)` otherwise, matching the
  2924	    /// aggregation shape of `recompute_all`: per-cell failures
  2925	    /// (parse / bind issues that have somehow surfaced post-bind,
  2926	    /// e.g. a name was deleted between extract and recompute) are
  2927	    /// collected, the rest succeed.
  2928	    pub fn recompute_dirty(&mut self) -> Option<RecomputeResult> {
  2929	        // The session lives behind an `Option<&'a mut CalcgraphSession>`
  2930	        // — take it out for the duration of this call so we can borrow
  2931	        // both the workbook and the session simultaneously. Reattach
  2932	        // before returning.
  2933	        let mut session_slot = self.graph.take();
  2934	        let session = session_slot.as_deref_mut()?;
  2935	
  2936	        // **W5-106 (Phase 4.7.M / task #136 closure)**: fixed-point
  2937	        // loop. Each iteration:
  2938	        //   1. claim + schedule the current dirty set.
  2939	        //   2. snapshot prior + originally_dirty for newly-seen nodes
  2940	        //      (carry across iterations so VEQ short-circuit still
  2941	        //      works on second-iter D1's that depend on iter-1
  2942	        //      formulas).
  2943	        //   3. evaluate cycled + sorted.
  2944	        //   4. if write_spill fired during the sorted loop, it called
  2945	        //      on_set_value at spill-footprint cells, adding to dirty.
  2946	        //   5. loop until session.dirty is empty.
  2947	        //
  2948	        // Bounded by `MAX_ITERATIONS` to prevent runaway in pathological
  2949	        // cases (a workbook configuration that would not terminate).
  2950	        // 100 is generous — any single set_value edit normally needs
  2951	        // ≤ 2 iterations (one for the initial dirty set, one for spill
  2952	        // shape-transition follow-ups).
  2953	        const MAX_ITERATIONS: usize = 100;
  2954	        let mut attempted: usize = 0;
  2955	        let mut succeeded = 0;
  2956	        let mut failures: Vec<RecomputeFailure> = Vec::new();
  2957	        let mut skipped_value_equality: usize = 0;
  2958	        // Phase 3.9 (W5-42): SIMD-eligibility profile. V1 observability
  2959	        // only.
  2960	        let mut simd_classified: usize = 0;
  2961	
  2962	        // Phase 3.8 (W5-41, VEQ-3-01..03) — value-equality short-
  2963	        // circuit. State accumulates across fixed-point iterations:
  2964	        //   - `prior`: workbook value at each first-seen node, captured
  2965	        //     BEFORE that iteration's evaluation. Carries across iters
  2966	        //     so VEQ comparisons stay anchored to the pre-recompute
  2967	        //     value, not intermediate spill-write values.
  2968	        //   - `originally_dirty`: NodeIds claimed by ANY iteration's
  2969	        //     schedule. Used to distinguish "top-level dirty" from
  2970	        //     "downstream dirty" for the VEQ decision.
  2971	        //   - `changed`: addresses whose value differs from `prior`
  2972	        //     after any iteration's write. Built up across iters.
  2973	        use std::collections::{HashMap, HashSet};
  2974	        let mut prior: HashMap<(SheetId, RowId, ColId), Value> = HashMap::new();
  2975	        let mut originally_dirty: HashSet<ql_calcgraph::NodeId> = HashSet::new();
  2976	        let mut changed: HashSet<(SheetId, RowId, ColId)> = HashSet::new();
  2977	        // **Codex audit MEDIUM closure**: surface iteration-cap hits
  2978	        // instead of silently breaking. After the loop, if `iter_count`
  2979	        // reached MAX_ITERATIONS AND session.dirty() is still non-empty,
  2980	        // surface a synthetic failure so the caller's `is_complete()`
  2981	        // check (which inspects `failures.is_empty()`) signals the
  2982	        // problem.
  2983	        let mut iter_count: usize = 0;
  2984	
  2985	        for _iter in 0..MAX_ITERATIONS {
  2986	            iter_count += 1;
  2987	            // Phase 3.4: claim dirty + topo-sort. Edges in the graph
  2988	            // model the dep direction (`outgoing(F) = what F depends on`).
  2989	            // Tarjan emits SCCs in reverse-topo of the condensation
  2990	            // which, for our edge orientation, is dependency-first order.
  2991	            let sched = session.schedule_dirty();
  2992	            if sched.total_count() == 0 {
  2993	                break; // Fixed point reached.
  2994	            }
  2995	            attempted += sched.total_count();
  2996	
  2997	            // Snapshot prior + originally_dirty for newly-seen nodes.
  2998	            for n in sched.sorted.iter().chain(sched.cycled.iter()) {
  2999	                originally_dirty.insert(*n);
  3000	                if let Some(addr) = session.cell_address_for(*n) {
  3001	                    prior.entry(addr).or_insert_with(|| {
  3002	                        self.workbook
  3003	                            .read(ql_types::Address::new(addr.0, addr.1, addr.2))
  3004	                    });
  3005	                }
  3006	            }
  3007	
  3008	            // Cycled nodes get `#CIRC!` regardless of whether the
  3009	            // formula still binds. Phase 3.5: routes to the computed
  3010	            // overlay. Phase 3.8: still apply value-equality.
  3011	            let circ = Value::Error(ErrorValue::Circ);
  3012	            for node in &sched.cycled {
  3013	                let Some((sheet, row, col)) = session.cell_address_for(*node) else {
  3014	                    continue;
  3015	                };
  3016	                let prior_val = prior.get(&(sheet, row, col));
  3017	                if prior_val == Some(&circ) {
  3018	                    skipped_value_equality += 1;
  3019	                } else {
  3020	                    self.workbook.put_computed_at(sheet, row, col, circ.clone());
  3021	                    changed.insert((sheet, row, col));
  3022	                }
  3023	            }
  3024	
  3025	            // Sorted nodes evaluate in dependency-first order.

exec
/bin/zsh -lc 'rg -n "fn schedule_dirty|Schedule" crates/ql-exec/src/calcgraph_session.rs crates/ql-calcgraph/src' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
crates/ql-exec/src/calcgraph_session.rs:78:    range_contains_rowcol, schedule_with_supplemental, CellNode, Graph, Node, NodeId, Schedule,
crates/ql-exec/src/calcgraph_session.rs:1237:    /// scheduler over it. The returned [`Schedule`] partitions nodes:
crates/ql-exec/src/calcgraph_session.rs:1252:    pub fn schedule_dirty(&mut self) -> Schedule {
crates/ql-exec/src/calcgraph_session.rs:1280:    /// Schedule).
crates/ql-exec/src/calcgraph_session.rs:3111:    /// SCH-3-02: cycles surface in `Schedule::cycled` carrying every
crates/ql-exec/src/calcgraph_session.rs:3161:    /// produce the same Schedule.
crates/ql-exec/src/calcgraph_session.rs:3219:    /// Phase 3.4 edge case: empty dirty set produces empty Schedule.
crates/ql-exec/src/calcgraph_session.rs:3221:    fn schedule_dirty_with_empty_dirty_is_empty_schedule() {
crates/ql-calcgraph/src/topo.rs:63:pub struct Schedule {
crates/ql-calcgraph/src/topo.rs:68:impl Schedule {
crates/ql-calcgraph/src/topo.rs:78:/// Schedule the given dirty subset for evaluation. Returns `(sorted, cycled)` where
crates/ql-calcgraph/src/topo.rs:87:pub fn schedule(graph: &Graph, dirty_subset: &[NodeId]) -> Schedule {
crates/ql-calcgraph/src/topo.rs:92:/// **W5-50 — GAP-G-03 closure (small Option C).** Schedule with caller-
crates/ql-calcgraph/src/topo.rs:116:) -> Schedule {
crates/ql-calcgraph/src/topo.rs:119:        return Schedule::default();
crates/ql-calcgraph/src/topo.rs:153:    Schedule { sorted, cycled }
crates/ql-calcgraph/src/dirty.rs:215:/// `topo::schedule` partitions cycled nodes into `Schedule::cycled` for the evaluator to
crates/ql-calcgraph/src/lib.rs:15://!   Schedule { sorted, cycled }. Applies CORR-23 (Tarjan, not Kahn).
crates/ql-calcgraph/src/lib.rs:57:pub use topo::{schedule, schedule_with_supplemental, Schedule};

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/calcgraph_session.rs | sed -n '1228,1265p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1228	        let Some((reader_sheet, _, _)) = self.cell_address_for(reader_node) else {
  1229	            return false;
  1230	        };
  1231	        self.extract_and_register_deps(reader_node, reader_sheet, plan, workbook);
  1232	        true
  1233	    }
  1234	
  1235	    /// **Phase 3.4 acceptance (SCH-3-01..04 entry point).** Atomically
  1236	    /// claim the dirty set and run the Phase 0 W3-3 iterative Tarjan
  1237	    /// scheduler over it. The returned [`Schedule`] partitions nodes:
  1238	    ///
  1239	    /// - `sorted`: dependency-first topological order. Evaluate
  1240	    ///   `sorted[0]` before `sorted[1]`, etc.
  1241	    /// - `cycled`: nodes in a non-trivial strongly connected component
  1242	    ///   (size > 1) or with a self-loop. Per Phase 0 spec these get
  1243	    ///   `Value::Error(ErrorValue::Circ)`.
  1244	    ///
  1245	    /// The dirty set is cleared by this call. Edges to non-dirty
  1246	    /// nodes are not traversed (those nodes aren't being recomputed;
  1247	    /// their current values are read as-is). Input ordering is
  1248	    /// sorted-by-NodeId before the scheduler runs so SCH-3-03
  1249	    /// determinism holds: same workbook + same edits → same schedule
  1250	    /// across runs (the underlying scheduler is already deterministic
  1251	    /// in its input slice order; we just normalize HashSet iteration).
  1252	    pub fn schedule_dirty(&mut self) -> Schedule {
  1253	        let mut dirty_vec: Vec<NodeId> = self.dirty.drain().collect();
  1254	        dirty_vec.sort();
  1255	        // W5-50 (GAP-G-03 closure): inject supplemental adjacency so
  1256	        // Tarjan can see range-induced ordering / cycles. The Phase 0
  1257	        // graph stores range deps in stripes + `formula_to_range_deps`
  1258	        // rather than as `add_edge` edges (the A4 acceptance: SUM(A:A)
  1259	        // must compress to ONE RangeRef, not 25M edges). For Tarjan we
  1260	        // expand range deps lazily into temp edges F → G where G is a
  1261	        // dirty formula inside one of F's ranges.
  1262	        let supplemental = self.build_range_supplemental(&dirty_vec);
  1263	        schedule_with_supplemental(&self.graph, &dirty_vec, &supplemental)
  1264	    }
  1265	

codex
The key recompute path confirms the risk: `schedule_dirty()` drains the dirty set before evaluation, and the cache lookup does not include table metadata generation. If a cached structured-ref plan is reused, the dirty mark is consumed and the old range can be evaluated as if the table still existed.
exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '1870,1920p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1870	            let expr = match parse(tokens) {
  1871	                Ok(e) => e,
  1872	                Err(_) => continue,
  1873	            };
  1874	            let new_expr =
  1875	                ql_formula_syntax::rewrite_table_ref(&expr, &old_canonical, &new_display_arc);
  1876	            if new_expr == expr {
  1877	                continue; // no StructuredRef references the renamed table
  1878	            }
  1879	            let new_text = ql_formula_syntax::print(&new_expr);
  1880	            // Emit PutFormula so replay reconstructs the rewrite.
  1881	            if let Some(oplog) = self.oplog.as_deref_mut() {
  1882	                oplog.append(Op::PutFormula {
  1883	                    sheet: s,
  1884	                    row: r,
  1885	                    col: c,
  1886	                    text: new_text.clone(),
  1887	                })?;
  1888	            }
  1889	            // Update workbook formula text directly (bypass set_formula
  1890	            // to avoid re-running the bind eagerly; the recompute path
  1891	            // will re-bind against the new name at next eval).
  1892	            self.workbook.put_formula(s, r, c, new_text);
  1893	            rewritten += 1;
  1894	        }
  1895	        // Re-key the TableTable entry.
  1896	        let mut meta = self
  1897	            .workbook
  1898	            .tables_mut()
  1899	            .remove(&old_canonical)
  1900	            .expect("verified at top");
  1901	        let new_canonical_arc: Arc<str> = Arc::from(new_canonical.as_str());
  1902	        meta.name = Arc::clone(&new_canonical_arc);
  1903	        meta.display_name = Arc::clone(&new_display_arc);
  1904	        self.workbook.tables_mut().insert(new_canonical_arc, meta);
  1905	        // Invalidate plan cache by clearing — every formula that
  1906	        // referenced the OLD name now has new text, so cache lookups
  1907	        // miss anyway; the bare invalidation prevents stale entries.
  1908	        self.plan_cache.clear();
  1909	        Ok(rewritten)
  1910	    }
  1911	
  1912	    /// **W5-121 (Phase 4.8.I.2):** rename a column within an existing
  1913	    /// table. Mirrors [`Self::rename_table`] semantics for column refs:
  1914	    /// rewrites stored formula text in every cell whose StructuredRef
  1915	    /// references the renamed column (Excel canon — the next bind sees
  1916	    /// the new name).
  1917	    ///
  1918	    /// Process:
  1919	    /// 1. Validate: table exists; source column exists (case-insensitive);
  1920	    ///    target name available within the table; non-empty; not a no-op

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '2035,2055p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  2035	        let (col_idx, _) = meta
  2036	            .lookup_column(old_col)
  2037	            .expect("verified at top before any mutation");
  2038	        meta.columns[col_idx as usize].name = Arc::from(new_lower.as_str());
  2039	        meta.columns[col_idx as usize].display = Arc::clone(&new_display_arc);
  2040	        // `get_mut` doesn't bump generation; do it explicitly so plan
  2041	        // caches keyed on `TableTable::generation` invalidate.
  2042	        self.workbook.tables_mut().bump_generation();
  2043	        // Invalidate plan cache by clearing — every formula that
  2044	        // referenced the OLD column now has new text, so cache lookups
  2045	        // miss anyway; bare invalidation prevents stale entries.
  2046	        self.plan_cache.clear();
  2047	        Ok(rewritten)
  2048	    }
  2049	
  2050	    /// **W5-122 (Phase 4.8.J):** resize a table's footprint per
  2051	    /// design § 12.3. Three scenarios are supported by a single op:
  2052	    ///
  2053	    /// - **Grow / shrink rows:** common path. Pass `added_columns =
  2054	    ///   removed_columns = []` and the new row count.
  2055	    /// - **Append column(s) at the END:** list each new display name

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '2268,2288p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  2268	            meta.columns.push(TableColumn {
  2269	                id,
  2270	                name: Arc::from(cn.to_ascii_lowercase().as_str()),
  2271	                display: Arc::from(cn.as_str()),
  2272	                totals_function: None,
  2273	            });
  2274	        }
  2275	        self.workbook.tables_mut().bump_generation();
  2276	        // Plan cache clear so formulas re-bind against the new range
  2277	        // / column roster on next eval. Targeted invalidation via
  2278	        // `table_to_formulas` is design § 4.5 / 4.8.G.3 (deferred).
  2279	        self.plan_cache.clear();
  2280	        Ok(())
  2281	    }
  2282	
  2283	    /// **W5-92 (Phase 4.6.D):** create a new sheet,
  2284	    /// emitting `Op::AddSheet` into the attached op log (if any). Returns
  2285	    /// the new sheet's `SheetId`. Wraps `Workbook::add_sheet_with_chunk_rows`.
  2286	    ///
  2287	    /// `chunk_rows` is the per-sheet chunk size. Pass `16_384` (the engine
  2288	    /// default) unless you have a specific reason — the qbook loader uses

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '2428,2450p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  2428	        for (s, r, c, new_text) in rewritten_cells {
  2429	            self.workbook.put_formula(s, r, c, new_text.as_str());
  2430	        }
  2431	        // Swap the name. Cannot fail here — validation already passed.
  2432	        self.workbook
  2433	            .rename_sheet(id, new_name)
  2434	            .expect("rename_sheet validation already passed");
  2435	        // Wipe the PlanCache so cached bound plans (which baked in the
  2436	        // old SheetId via SheetRef::Name resolution) miss on next
  2437	        // recompute and rebind against the new sheet name. Sheet
  2438	        // rename is edit-rate, not recompute-rate; a full cache flush
  2439	        // is acceptable per design § 10.5 / Codex MEDIUM-7. (A
  2440	        // finer-grained `sheet_gen` counter could be wired in a follow-
  2441	        // up — filed as a future polish item.)
  2442	        self.plan_cache.clear();
  2443	        Ok(())
  2444	    }
  2445	
  2446	    /// Phase 2B.5 (2026-05-12): clear a cell's formula association through
  2447	    /// the runtime, emitting `Op::ClearFormula` into the attached op log (if
  2448	    /// any). Idempotent: clearing a cell with no formula is a no-op for the
  2449	    /// workbook AND for the op log (no entry emitted) — avoiding spurious
  2450	    /// "removed nothing" entries.

exec
/bin/zsh -lc "rg -n \"struct TableTable|generation\\(|bump_generation|TableTable\" crates/ql-storage/src crates/ql-exec/src/plan_cache.rs crates/ql-exec/src/workbook_runtime.rs" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
crates/ql-exec/src/plan_cache.rs:15://! - Caller registers/removes a name → `NameTable::generation()` advances
crates/ql-exec/src/plan_cache.rs:57:/// - `name_gen` — `NameTable::generation()` at the moment of bind. A
crates/ql-exec/src/workbook_runtime.rs:599:        let name_gen = self.workbook.names().generation();
crates/ql-exec/src/workbook_runtime.rs:1004:            let name_gen = self.workbook.names().generation();
crates/ql-exec/src/workbook_runtime.rs:1539:        //    workbook-scoped `NameTable::generation()` — a sheet-scoped
crates/ql-exec/src/workbook_runtime.rs:1561:    /// - Name is unique against both `TableTable` AND `NameTable`
crates/ql-exec/src/workbook_runtime.rs:1567:    /// `TableTable::allocate_column_id`. Header / totals rows are
crates/ql-exec/src/workbook_runtime.rs:1636:        // Name uniqueness (shared namespace: TableTable + NameTable).
crates/ql-exec/src/workbook_runtime.rs:1764:    /// 1. Validate: source exists; target name available (TableTable +
crates/ql-exec/src/workbook_runtime.rs:1770:    /// 4. Re-key the `TableTable` entry from old canonical to new
crates/ql-exec/src/workbook_runtime.rs:1895:        // Re-key the TableTable entry.
crates/ql-exec/src/workbook_runtime.rs:1928:    ///    `name` and case-preserving `display`; bump TableTable
crates/ql-exec/src/workbook_runtime.rs:2041:        // caches keyed on `TableTable::generation` invalidate.
crates/ql-exec/src/workbook_runtime.rs:2042:        self.workbook.tables_mut().bump_generation();
crates/ql-exec/src/workbook_runtime.rs:2086:    /// **Caller responsibility:** resize bumps `TableTable::generation`
crates/ql-exec/src/workbook_runtime.rs:2125:        // `TableTable::generation`, and clearing the plan cache when
crates/ql-exec/src/workbook_runtime.rs:2253:        // Allocate new column ids first; mutable borrows on TableTable
crates/ql-exec/src/workbook_runtime.rs:2275:        self.workbook.tables_mut().bump_generation();
crates/ql-exec/src/workbook_runtime.rs:3202:                                let name_gen = self.workbook.names().generation();
crates/ql-exec/src/workbook_runtime.rs:3358:        let name_gen = self.workbook.names().generation();
crates/ql-exec/src/workbook_runtime.rs:4581:        let old_gen = wb.names().generation();
crates/ql-exec/src/workbook_runtime.rs:4585:            wb.names().generation() > old_gen,
crates/ql-exec/src/workbook_runtime.rs:9579:    ///   resolution against TableTable.
crates/ql-storage/src/tables.rs:11://! [`TableTable`]'s mutation methods are reserved for the qbook loader
crates/ql-storage/src/tables.rs:25:/// from the workbook-level counter (`TableTable::next_column_id`, private).
crates/ql-storage/src/tables.rs:282:pub struct TableTable {
crates/ql-storage/src/tables.rs:294:impl TableTable {
crates/ql-storage/src/tables.rs:301:    pub fn generation(&self) -> u64 {
crates/ql-storage/src/tables.rs:389:    pub fn bump_generation(&mut self) {
crates/ql-storage/src/tables.rs:529:        let mut tt = TableTable::new();
crates/ql-storage/src/tables.rs:542:        let mut tt = TableTable::new();
crates/ql-storage/src/tables.rs:543:        let g0 = tt.generation();
crates/ql-storage/src/tables.rs:545:        let g1 = tt.generation();
crates/ql-storage/src/tables.rs:548:        let g2 = tt.generation();
crates/ql-storage/src/tables.rs:552:        assert_eq!(g2, tt.generation());
crates/ql-storage/src/tables.rs:557:        let mut tt = TableTable::new();
crates/ql-storage/src/tables.rs:570:        let mut tt = TableTable::new();
crates/ql-storage/src/tables.rs:598:        let mut tt = TableTable::new();
crates/ql-storage/src/tables.rs:638:        let mut tt = TableTable::new();
crates/ql-storage/src/tables.rs:664:        let mut tt = TableTable::new();
crates/ql-storage/src/workbook.rs:167:    pub fn generation(&self) -> u64 {
crates/ql-storage/src/workbook.rs:280:    tables: crate::TableTable,
crates/ql-storage/src/workbook.rs:503:    /// registry. See `crate::TableTable` and the design doc at
crates/ql-storage/src/workbook.rs:505:    pub fn tables(&self) -> &crate::TableTable {
crates/ql-storage/src/workbook.rs:514:    pub fn tables_mut(&mut self) -> &mut crate::TableTable {
crates/ql-storage/src/lib.rs:31:pub use tables::{TableColumn, TableMetadata, TableTable, TotalsFunction};

exec
/bin/zsh -lc 'git diff b83c19c7a7d..a01b40f5101 -- crates/ql-exec/src/workbook_runtime.rs' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
diff --git a/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs b/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs
index 1cd9e73b8fb..c4d5d1fbcd9 100644
--- a/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs
+++ b/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs
@@ -1717,6 +1717,24 @@ impl<'a> WorkbookRuntime<'a> {
     /// the table footprint are untouched. Formulas referencing the
     /// dropped table re-bind to `BindError::UnknownTable` on next
     /// recompute. Emits `Op::DropTable` to the attached op log (if any).
+    ///
+    /// **W5-155 (Phase 4.8.G.3):** also fires the calcgraph's
+    /// `on_table_drop` hook (W5-154) so formulas previously
+    /// referencing this table get BFS-dirty-fanned. Pre-W5-155 the
+    /// drop completed without notifying the calcgraph — formulas
+    /// with cached `ExprPlan::StructuredRef` plans would continue
+    /// reading the (now-untyped) cell range until something else
+    /// invalidated them. Post-W5-155 the hook fires:
+    /// 1. `table_to_formulas[name]` enumerates the readers.
+    /// 2. Each reader marked dirty + BFS-fanout (W5-91 H2 pattern).
+    /// 3. Next recompute re-binds → `BindError::UnknownTable` →
+    ///    `Value::Error(#NAME?)` per the existing 4.8.F binder.
+    ///
+    /// The hook fires AFTER the op-log append (atomic with the
+    /// mutation, per W5-103) and BEFORE `tables_mut().remove`
+    /// (graph fanout is read-only on workbook state; the order
+    /// is irrelevant for correctness but matches the pre-existing
+    /// rename/resize convention).
     pub fn drop_table(&mut self, name: &str) -> Result<(), RuntimeError> {
         let canonical = name.to_ascii_uppercase();
         if self.workbook.tables().lookup(&canonical).is_none() {
@@ -1727,6 +1745,13 @@ impl<'a> WorkbookRuntime<'a> {
                 name: canonical.clone(),
             })?;
         }
+        // **W5-155 (Phase 4.8.G.3):** dirty-fan readers BEFORE
+        // removing the metadata. The hook uses case-insensitive
+        // lookup so either case (canonical-upper or the user's
+        // input casing) reaches the index.
+        if let Some(g) = self.graph.as_deref_mut() {
+            g.on_table_drop(&canonical);
+        }
         let _ = self.workbook.tables_mut().remove(&canonical);
         Ok(())
     }
@@ -10048,6 +10073,56 @@ mod tests {
         }
     }
 
+    /// **W5-155 (Phase 4.8.G.3):** `WorkbookRuntime::drop_table` must
+    /// invoke the calcgraph `on_table_drop` hook so any formula
+    /// referencing the dropped table is BFS-dirtied. Pre-W5-155 the
+    /// runtime relied on plan-cache invalidation alone, which left the
+    /// dirty set untouched and missed cells got stale values until an
+    /// unrelated edit happened to touch them.
+    #[test]
+    fn drop_table_fires_on_table_drop_hook_and_dirties_readers() {
+        use crate::CalcgraphSession;
+        let mut wb = make_runtime_workbook();
+        let reg = default_registry();
+        let mut graph = CalcgraphSession::new();
+        // Build: Sales table at A1:A3 (header + 2 data rows) with
+        // column Qty, values 10/20, and B1 = SUM(Sales[Qty]).
+        {
+            let mut rt = WorkbookRuntime::with_graph(&mut wb, &reg, &mut graph);
+            rt.create_table("Sales", 0, 0, 0, 3, 1, true, false, vec!["Qty".into()])
+                .unwrap();
+            rt.set_value(0, 1, 0, Value::Number(10.0)).unwrap();
+            rt.set_value(0, 2, 0, Value::Number(20.0)).unwrap();
+            let v = rt.set_formula(0, 0, 1, "SUM(Sales[Qty])").unwrap();
+            assert_eq!(v, Value::Number(30.0));
+            // Drain dirty so the post-drop assertion is unambiguous.
+            let _ = rt.recompute_dirty().expect("graph attached");
+        }
+        let formula_node = graph
+            .cell_node_for(0, 0, 1)
+            .expect("B1 formula node registered");
+        assert!(
+            !graph.is_dirty(formula_node),
+            "after recompute_dirty B1 should be clean"
+        );
+        let before = graph.hook_counts().table_drop;
+
+        // Drop the table — the hook must fire and dirty B1.
+        {
+            let mut rt = WorkbookRuntime::with_graph(&mut wb, &reg, &mut graph);
+            rt.drop_table("Sales").unwrap();
+        }
+        assert_eq!(
+            graph.hook_counts().table_drop,
+            before + 1,
+            "drop_table must call on_table_drop exactly once"
+        );
+        assert!(
+            graph.is_dirty(formula_node),
+            "B1 = SUM(Sales[Qty]) must be dirty after Sales is dropped"
+        );
+    }
+
     // ===== W5-119 (Phase 4.8.I) — rename_table =====
 
     #[test]

exec
/bin/zsh -lc 'rg -n "drop_table.*plan_cache|plan_cache.*drop_table|on_table_drop.*follow-up|hook.*follow-up|still relies on `plan_cache.clear'"\\(\\)"'`|not.*call this hook|wiring lands" crates docs .github --glob '"'"'!target/**'"'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 exited 2 in 0ms:
zsh:1: command not found: plan_cache.clear()
rg: .github: No such file or directory (os error 2)
docs/MASTER-PLAN.md:241:   Shipped: `CalcgraphSession::{formula_deps, volatile_formulas, name_to_formulas}` side-tables; `walk_plan_for_deps` recursive walker; `RebuildResult` aggregates per-formula bind failures (parallel to `RecomputeResult`); `on_set_formula` takes `&ExprPlan` (no re-binding in the hook); `on_clear_formula` evicts dep state. 9 new tests; full ql-exec suite at 268 tests. Open follow-ups: GAP-R-07 (scalar `NameRef` name-tracking) and the 3.3 dirty-propagation wire-up that consumes this substrate.  
docs/phase2/exit-packet.md:104:- **Range/Formula NamedTarget resolution**: still surface as `UnsupportedVariant`. Aggregate-context wiring lands when the calcgraph integrates (Phase 4+).
crates/ql-exec/src/calcgraph_session.rs:944:    /// yet call this hook (still relies on `plan_cache.clear()`).
crates/quantbook-py/pyproject.toml:2:# Phase 0 skeleton only — actual PyO3 wiring lands in Phase 5 (spec Decision C).
docs/audits/2026-05-13-session-final-megaudit-codex.txt:1731:   Shipped: `CalcgraphSession::{formula_deps, volatile_formulas, name_to_formulas}` side-tables; `walk_plan_for_deps` recursive walker; `RebuildResult` aggregates per-formula bind failures (parallel to `RecomputeResult`); `on_set_formula` takes `&ExprPlan` (no re-binding in the hook); `on_clear_formula` evicts dep state. 9 new tests; full ql-exec suite at 268 tests. Open follow-ups: GAP-R-07 (scalar `NameRef` name-tracking) and the 3.3 dirty-propagation wire-up that consumes this substrate.  
docs/audits/2026-05-13-finalization-planning-codex.txt:427:   241	   Shipped: `CalcgraphSession::{formula_deps, volatile_formulas, name_to_formulas}` side-tables; `walk_plan_for_deps` recursive walker; `RebuildResult` aggregates per-formula bind failures (parallel to `RecomputeResult`); `on_set_formula` takes `&ExprPlan` (no re-binding in the hook); `on_clear_formula` evicts dep state. 9 new tests; full ql-exec suite at 268 tests. Open follow-ups: GAP-R-07 (scalar `NameRef` name-tracking) and the 3.3 dirty-propagation wire-up that consumes this substrate.  
docs/audits/2026-05-15-w5155-audit-codex.md:22:2. **Plan cache interaction.** `PlanCacheKey` is `(text, sheet, name_gen, cell_anchor)` (see `crates/ql-exec/src/plan_cache.rs`). Note `name_gen` is `NameTable` generation, NOT `TableTable` generation. `try_recompute_with_simd_profile` (around `workbook_runtime.rs:3350`) is the recompute path — does it correctly re-bind structured-ref formulas after a `drop_table`, or does it serve a stale cached `ExprPlan::StructuredRef { resolved: <pre-drop range> }`?
docs/audits/2026-05-15-w5155-audit-codex.md:24:3. **Symmetry with peers.** `rename_table`, `rename_column`, `resize_table` all call `self.plan_cache.clear()`. `drop_table` does NOT. Is this an accidental asymmetry or intentional?
docs/audits/2026-05-15-w5155-audit-codex.md:32:7. **Stale docstrings.** Search for any docstring or comment that claims drop_table relies on `plan_cache.clear()`, or that the hook isn't wired yet (W5-154 said "wiring lands in a follow-up commit"). Are they updated?
docs/audits/2026-05-15-w5155-audit-codex.md:39:/bin/zsh -lc "rg -n \"on_table_drop|drop_table|PlanCacheKey|StructuredRef|plan_cache\\.clear|rebuild_from_workbook|drop_table_fires\" crates/ql-exec/src crates/ql-oplog/src" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
docs/audits/2026-05-15-w5155-audit-codex.md:73:crates/ql-exec/src/calcgraph_session.rs:944:    /// yet call this hook (still relies on `plan_cache.clear()`).
docs/audits/2026-05-15-w5155-audit-codex.md:478:   944	    /// yet call this hook (still relies on `plan_cache.clear()`).

exec
/bin/zsh -lc "rg -n 'drop_table.*plan_cache|plan_cache.*drop_table|on_table_drop.*follow-up|hook.*follow-up|still relies on "'`plan_cache'"\\.clear\\(\\)"'`|not.*call this hook|wiring lands'"' crates docs AGENTS.md --glob '"'!target/**'"' --glob '"'!docs/audits/**'"'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 exited 2 in 0ms:
rg: AGENTS.md: No such file or directory (os error 2)
docs/MASTER-PLAN.md:241:   Shipped: `CalcgraphSession::{formula_deps, volatile_formulas, name_to_formulas}` side-tables; `walk_plan_for_deps` recursive walker; `RebuildResult` aggregates per-formula bind failures (parallel to `RecomputeResult`); `on_set_formula` takes `&ExprPlan` (no re-binding in the hook); `on_clear_formula` evicts dep state. 9 new tests; full ql-exec suite at 268 tests. Open follow-ups: GAP-R-07 (scalar `NameRef` name-tracking) and the 3.3 dirty-propagation wire-up that consumes this substrate.  
docs/phase2/exit-packet.md:104:- **Range/Formula NamedTarget resolution**: still surface as `UnsupportedVariant`. Aggregate-context wiring lands when the calcgraph integrates (Phase 4+).
crates/ql-exec/src/calcgraph_session.rs:944:    /// yet call this hook (still relies on `plan_cache.clear()`).
crates/quantbook-py/pyproject.toml:2:# Phase 0 skeleton only — actual PyO3 wiring lands in Phase 5 (spec Decision C).

codex
I’m going to run the new targeted test. It should pass even if the end-to-end contract is broken, which is itself part of the coverage finding.
exec
/bin/zsh -lc 'cargo test -p ql-exec drop_table_fires_on_table_drop_hook_and_dirties_readers' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 10244ms:
    Finished `test` profile [unoptimized + debuginfo] target(s) in 2.22s
     Running unittests src/lib.rs (target/debug/deps/ql_exec-b3f1062c75853a85)

running 1 test
test workbook_runtime::tests::drop_table_fires_on_table_drop_hook_and_dirties_readers ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 602 filtered out; finished in 0.00s

     Running tests/error_precedence.rs (target/debug/deps/error_precedence-e2befdcc2eba4b3f)

running 0 tests

test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 15 filtered out; finished in 0.00s

     Running tests/ide_simulation.rs (target/debug/deps/ide_simulation-72a7d3a1ebd3350a)

running 0 tests

test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 7 filtered out; finished in 0.00s

     Running tests/oplog_e2e.rs (target/debug/deps/oplog_e2e-97d2ec293211cbd2)

running 0 tests

test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 20 filtered out; finished in 0.00s

     Running tests/region_mul2_e2e.rs (target/debug/deps/region_mul2_e2e-295add40907d53df)

running 0 tests

test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 21 filtered out; finished in 0.00s


exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '130,185p' && nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '245,305p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   130	        return Err(RuntimeError::InvalidCell {
   131	            sheet,
   132	            row,
   133	            col,
   134	            why: "col exceeds MAX_COLUMN (16,383)",
   135	        });
   136	    }
   137	    Ok(())
   138	}
   139	
   140	/// Errors from the runtime pipeline. Each upstream stage's error wraps cleanly.
   141	/// Phase 2A.6 audit H1/L4 (2026-05-12) added `InvalidSheet` so callers that pass
   142	/// a sheet id outside the workbook's range get a clean error instead of a panic
   143	/// from `Workbook::put_at`. Phase 2A.6 audit H4 added `ConflictingOps` for
   144	/// the `WorkbookTransaction` mixed-kind-on-same-cell case.
   145	#[derive(Debug, thiserror::Error)]
   146	pub enum RuntimeError {
   147	    // Phase 2A.11 audit M16: switched from `{0:?}` debug formatters to `{0}`
   148	    // Display now that LexError + BindError implement thiserror::Error with
   149	    // user-facing strings. IDE error display reads cleanly: "lex error:
   150	    // unexpected character: '@'" instead of "lex error: UnexpectedChar('@')".
   151	    #[error("lex error: {0}")]
   152	    Lex(LexError),
   153	
   154	    #[error("parse error: {0}")]
   155	    Parse(#[from] ParseError),
   156	
   157	    /// **W5-147 (Phase 4.9.K):** the canonicalize step of `set_formula`
   158	    /// (per design § 4.4) failed. This happens when the parsed AST
   159	    /// contains an intermediate `Expr::R1C1Ref` with a relative axis
   160	    /// that the printer can't resolve, or a malformed R1C1 axis. In
   161	    /// practice the bind site always has a cell anchor, so the only
   162	    /// reachable case is a hand-built AST with `AxisSpec::Abs(0)` —
   163	    /// effectively impossible from `set_formula` since the lexer
   164	    /// already rejects it as `MalformedR1C1`. Kept as a typed error
   165	    /// for defense-in-depth.
   166	    #[error("print error: {0}")]
   167	    Print(#[from] PrintError),
   168	
   169	    #[error("bind error: {0}")]
   170	    Bind(BindError),
   171	
   172	    #[error("invalid sheet {sheet}: workbook has {sheet_count} sheets")]
   173	    InvalidSheet { sheet: SheetId, sheet_count: usize },
   174	
   175	    /// Phase 2A.7 audit H1: row/col outside the Excel grid (MAX_ROW = 1,048,575;
   176	    /// MAX_COLUMN = 16,383). Surfaces at buffer time so `Sheet::put` doesn't
   177	    /// panic mid-commit. `why` describes which bound was exceeded.
   178	    #[error("invalid cell (sheet={sheet}, row={row}, col={col}): {why}")]
   179	    InvalidCell {
   180	        sheet: SheetId,
   181	        row: RowId,
   182	        col: ColId,
   183	        why: &'static str,
   184	    },
   185	
   245	    /// fixed-point loop hit its MAX_ITERATIONS bound with cells still
   246	    /// dirty. Signals a runaway spill shape transition or workbook
   247	    /// misconfiguration; the recompute result is partial.
   248	    #[error("recompute_dirty hit iteration cap with cells still dirty")]
   249	    RecomputeIterationCap,
   250	
   251	    /// **W5-118 (Phase 4.8.H):** `create_table` was rejected by a
   252	    /// validation rule. `reason` is a static string describing which
   253	    /// invariant fired (uniqueness, overlap, empty column name, ...).
   254	    #[error("table {name:?} create rejected: {reason}")]
   255	    TableCreateRejected { name: String, reason: &'static str },
   256	
   257	    /// **W5-118 (Phase 4.8.H):** `drop_table` was called on a name
   258	    /// that doesn't exist.
   259	    #[error("table {0:?} not found")]
   260	    TableNotFound(String),
   261	
   262	    /// **W5-121 (Phase 4.8.I.2):** `rename_column` referenced a column
   263	    /// name (case-insensitive) that does not exist in the target table.
   264	    #[error("table {table:?} has no column {column:?}")]
   265	    TableColumnNotFound { table: String, column: String },
   266	
   267	    /// **W5-121 (Phase 4.8.I.2):** `rename_column` was rejected by a
   268	    /// validation rule (target column name already in the table, empty,
   269	    /// etc.). `reason` is a static string describing which invariant
   270	    /// fired.
   271	    #[error("table {table:?} column {column:?} rejected: {reason}")]
   272	    TableColumnRejected {
   273	        table: String,
   274	        column: String,
   275	        reason: &'static str,
   276	    },
   277	
   278	    /// **W5-122 (Phase 4.8.J):** `resize_table` was rejected by a
   279	    /// validation rule (zero dims, arithmetic mismatch, trailing-
   280	    /// columns mismatch, column-name collision, footprint overlap,
   281	    /// etc.). `reason` is a static string describing which invariant
   282	    /// fired.
   283	    #[error("table {name:?} resize rejected: {reason}")]
   284	    TableResizeRejected { name: String, reason: &'static str },
   285	}
   286	
   287	impl From<LexError> for RuntimeError {
   288	    fn from(e: LexError) -> Self {
   289	        Self::Lex(e)
   290	    }
   291	}
   292	
   293	impl From<BindError> for RuntimeError {
   294	    fn from(e: BindError) -> Self {
   295	        Self::Bind(e)
   296	    }
   297	}
   298	
   299	/// Outcome of a single failed cell during `WorkbookRuntime::recompute_all`.
   300	///
   301	/// Phase 2B.2 (2026-05-12): part of the [`RecomputeResult`] aggregation
   302	/// replacing the prior short-circuit `Result<usize, RuntimeError>` return
   303	/// shape. Captures everything a caller needs to display the failure
   304	/// without re-walking the workbook: the cell address, the formula text
   305	/// that was tried, and the structural error from lex/parse/bind.

exec
/bin/zsh -lc "nl -ba crates/ql-oplog/src/replay.rs | sed -n '1,90p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
     1	//! Replay an `OpLog` against a `Workbook`.
     2	//!
     3	//! Phase 2A.3.a (2026-05-12): single entry point `replay_into(&OpLog, &mut
     4	//! Workbook, &FunctionRegistry) -> Result<usize, ReplayError>`. Returns the
     5	//! count of ops successfully applied; on failure, returns
     6	//! `ReplayError::At { index, kind }` carrying the position of the failing
     7	//! op and the underlying failure. The workbook is left in a partial state
     8	//! on failure (callers decide whether to discard, recover, or recompute).
     9	//!
    10	//! ## Replay semantics
    11	//!
    12	//! Each `Op` variant maps to one `Workbook` mutation:
    13	//!
    14	//! - `PutValue` → `Workbook::put_at` (after row/col bounds check).
    15	//! - `PutFormula` → `Workbook::put_formula` (text only; replay does NOT
    16	//!   re-evaluate — that's a separate step the caller drives via
    17	//!   `WorkbookRuntime::recompute_all`).
    18	//! - `ClearFormula` → `Workbook::clear_formula`.
    19	//! - `SetName` → `Workbook::set_name`. Reserved names (e.g. `AI`) surface
    20	//!   as `ReplayError::NameRejected`.
    21	//! - `AddSheet` → `Workbook::add_sheet_with_chunk_rows`.
    22	//! - `BatchCommit` → recurse on each inner op. Index reporting flattens:
    23	//!   a failure inside a BatchCommit reports the inner op's overall
    24	//!   position (current outer index + offset).
    25	//!
    26	//! ## Idempotence
    27	//!
    28	//! `replay_into` is idempotent only in the trivial sense: replaying twice
    29	//! against the same starting workbook produces the same final state IFF
    30	//! the underlying mutation methods are themselves idempotent (and they
    31	//! are — `put_at` / `put_formula` / `clear_formula` / `set_name` /
    32	//! `add_sheet` all are). Callers wanting at-most-once semantics across
    33	//! sessions track replay state themselves (Phase 5+ work).
    34	//!
    35	//! ## `registry` parameter
    36	//!
    37	//! Passed for API symmetry with `WorkbookRuntime::new(&mut wb, &registry)`.
    38	//! 2A.3.a's replay doesn't use it (formula evaluation is deferred to a
    39	//! separate `recompute_all` call), but keeping the parameter stable here
    40	//! avoids a breaking signature change in 2A.3.b/c when the eval path
    41	//! could land inside replay.
    42	
    43	use ql_functions::FunctionRegistry;
    44	use ql_storage::Workbook;
    45	use ql_types::{ColId, RowId, SheetId, Value, MAX_COLUMN, MAX_ROW};
    46	use thiserror::Error;
    47	
    48	use crate::log::OpLog;
    49	use crate::op::Op;
    50	
    51	/// Errors emitted by `replay_into`.
    52	#[derive(Debug, Error)]
    53	pub enum ReplayError {
    54	    /// An op-log entry failed to deserialize (corrupted log data).
    55	    #[error("replay deserialize error at op index {0}: {1}")]
    56	    Deserialize(usize, #[source] crate::error::OpLogError),
    57	
    58	    /// The op referenced an out-of-range sheet id.
    59	    #[error(
    60	        "replay invalid sheet at op index {index}: sheet {sheet} (workbook has {sheet_count})"
    61	    )]
    62	    InvalidSheet {
    63	        index: usize,
    64	        sheet: SheetId,
    65	        sheet_count: usize,
    66	    },
    67	
    68	    /// The op carried out-of-range row/col coordinates.
    69	    #[error("replay invalid cell at op index {index}: row={row} col={col} ({why})")]
    70	    InvalidCell {
    71	        index: usize,
    72	        row: RowId,
    73	        col: ColId,
    74	        why: &'static str,
    75	    },
    76	
    77	    /// Decoding the on-wire `CellWireValue` failed (e.g., unknown error sigil).
    78	    #[error("replay value-decode error at op index {index}: {source}")]
    79	    ValueDecode {
    80	        index: usize,
    81	        #[source]
    82	        source: ql_io::QbookError,
    83	    },
    84	
    85	    /// Decoding the on-wire `NamedTargetWire` failed.
    86	    #[error("replay named-target decode error at op index {index}: {source}")]
    87	    NamedTargetDecode {
    88	        index: usize,
    89	        #[source]
    90	        source: ql_io::QbookError,

exec
/bin/zsh -lc 'rg -n "recompute_all|rebuild_from_workbook|CalcgraphSession" crates/ql-oplog/src crates/ql-exec/tests crates/ql-exec/src/workbook_runtime.rs | head -80' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
crates/ql-exec/src/workbook_runtime.rs:5://! formula. Plus a `recompute_all` method that re-evaluates every formula cell in
crates/ql-exec/src/workbook_runtime.rs:16://! - `recompute_all()` — legacy HashMap-order full pass; no graph awareness.
crates/ql-exec/src/workbook_runtime.rs:18://!   Runs Tarjan SCC over the attached `CalcgraphSession`'s dirty set; cycled
crates/ql-exec/src/workbook_runtime.rs:299:/// Outcome of a single failed cell during `WorkbookRuntime::recompute_all`.
crates/ql-exec/src/workbook_runtime.rs:325:/// Aggregate outcome of `WorkbookRuntime::recompute_all`.
crates/ql-exec/src/workbook_runtime.rs:360:/// `recompute_dirty`. The counter is `0` for `recompute_all` (which
crates/ql-exec/src/workbook_runtime.rs:377:    /// Always `0` for the legacy `recompute_all` path.
crates/ql-exec/src/workbook_runtime.rs:384:    /// of region-eligible formulas. Always `0` for `recompute_all`.
crates/ql-exec/src/workbook_runtime.rs:419:    /// successive `set_formula` and `recompute_all` calls hit the cache
crates/ql-exec/src/workbook_runtime.rs:429:    graph: Option<&'a mut crate::CalcgraphSession>,
crates/ql-exec/src/workbook_runtime.rs:453:    /// log against a fresh workbook (then calling `recompute_all` to
crates/ql-exec/src/workbook_runtime.rs:479:    /// edge updates. Today's recompute_all still walks formulas in
crates/ql-exec/src/workbook_runtime.rs:485:        graph: &'a mut crate::CalcgraphSession,
crates/ql-exec/src/workbook_runtime.rs:507:        graph: &'a mut crate::CalcgraphSession,
crates/ql-exec/src/workbook_runtime.rs:631:        // because replay re-derives it via `WorkbookRuntime::recompute_all`
crates/ql-exec/src/workbook_runtime.rs:2090:    /// re-triggers eval (typically via [`Self::recompute_all`]).
crates/ql-exec/src/workbook_runtime.rs:2862:    pub fn recompute_all(&mut self) -> RecomputeResult {
crates/ql-exec/src/workbook_runtime.rs:2897:            // Phase 3.8: `recompute_all` doesn't run the VEQ check —
crates/ql-exec/src/workbook_runtime.rs:2903:            // only. `recompute_all` is the legacy full-pass path.
crates/ql-exec/src/workbook_runtime.rs:2910:    /// [`CalcgraphSession`] has marked dirty since the last call.
crates/ql-exec/src/workbook_runtime.rs:2917:    /// Returns `None` when no `CalcgraphSession` is attached (the
crates/ql-exec/src/workbook_runtime.rs:2921:    /// `recompute_all` for a full HashMap-order pass instead.
crates/ql-exec/src/workbook_runtime.rs:2924:    /// aggregation shape of `recompute_all`: per-cell failures
crates/ql-exec/src/workbook_runtime.rs:2929:        // The session lives behind an `Option<&'a mut CalcgraphSession>`
crates/ql-exec/src/workbook_runtime.rs:3295:    /// Phase 2B.3 helper for `recompute_all`: consult the bind-plan cache
crates/ql-exec/src/workbook_runtime.rs:3421:        // followed by `recompute_all` would write #CALC! at the
crates/ql-exec/src/workbook_runtime.rs:3775:    // ===== recompute_all =====
crates/ql-exec/src/workbook_runtime.rs:3778:    fn recompute_all_on_empty_workbook_is_noop() {
crates/ql-exec/src/workbook_runtime.rs:3783:        let result = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:3790:    fn recompute_all_refreshes_formula_values() {
crates/ql-exec/src/workbook_runtime.rs:3808:        let result = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:3821:    fn recompute_all_handles_multiple_formulas() {
crates/ql-exec/src/workbook_runtime.rs:3835:        let result = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:3899:        let result = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:4069:    fn recompute_all_resolves_named_constant() {
crates/ql-exec/src/workbook_runtime.rs:4075:        // Seed a formula manually (skipping set_formula) so recompute_all does the work.
crates/ql-exec/src/workbook_runtime.rs:4081:        let result = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:4339:    fn recompute_all_does_not_emit_ops() {
crates/ql-exec/src/workbook_runtime.rs:4340:        // recompute_all is idempotent re-evaluation; it shouldn't show up in
crates/ql-exec/src/workbook_runtime.rs:4346:        // then recompute_all must not add more.
crates/ql-exec/src/workbook_runtime.rs:4356:            let result = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:4362:            "recompute_all must not append ops"
crates/ql-exec/src/workbook_runtime.rs:4385:    fn recompute_all_failure_carries_exact_cell_and_formula_text() {
crates/ql-exec/src/workbook_runtime.rs:4395:        let result = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:4415:    fn recompute_all_does_not_short_circuit_on_first_failure() {
crates/ql-exec/src/workbook_runtime.rs:4428:        let result = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:4450:    fn recompute_all_does_not_panic_on_invalid_persisted_formulas() {
crates/ql-exec/src/workbook_runtime.rs:4460:        let result = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:4472:    fn recompute_all_eval_time_error_values_count_as_succeeded() {
crates/ql-exec/src/workbook_runtime.rs:4479:        let result = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:4493:    fn recompute_all_failed_cells_preserve_prior_state() {
crates/ql-exec/src/workbook_runtime.rs:4500:        let result = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:4513:    /// BPC-01: repeated `recompute_all` against the same workbook does NOT
crates/ql-exec/src/workbook_runtime.rs:4517:    fn recompute_all_second_pass_is_all_cache_hits() {
crates/ql-exec/src/workbook_runtime.rs:4528:        let r1 = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:4537:        let r2 = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:4544:    /// BPC-02: a NameTable mutation between recompute_all calls invalidates
crates/ql-exec/src/workbook_runtime.rs:4546:    /// The next recompute_all is all misses.
crates/ql-exec/src/workbook_runtime.rs:4561:            let _ = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:4576:        let _ = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:4593:        let _ = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:4602:    /// BPC-03: cache keys are stable across recompute_all calls — same
crates/ql-exec/src/workbook_runtime.rs:4617:            let _ = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:4638:        let _ = rt.recompute_all(); // 1 miss
crates/ql-exec/src/workbook_runtime.rs:4639:        let _ = rt.recompute_all(); // 1 hit
crates/ql-exec/src/workbook_runtime.rs:4650:    /// `set_formula` pre-warms the cache. A subsequent `recompute_all`
crates/ql-exec/src/workbook_runtime.rs:4667:        let _ = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:5117:        let result = rt.recompute_all();
crates/ql-exec/src/workbook_runtime.rs:5359:            assert!(rt.recompute_all().is_complete());
crates/ql-exec/src/workbook_runtime.rs:5399:    /// corresponding calcgraph hook. Attach a `CalcgraphSession`, drive
crates/ql-exec/src/workbook_runtime.rs:5403:        use crate::CalcgraphSession;
crates/ql-exec/src/workbook_runtime.rs:5407:        let mut graph = CalcgraphSession::new();
crates/ql-exec/src/workbook_runtime.rs:5447:        use crate::CalcgraphSession;
crates/ql-exec/src/workbook_runtime.rs:5460:        let rebuild = CalcgraphSession::rebuild_from_workbook(&wb);
crates/ql-exec/src/workbook_runtime.rs:5500:        use crate::CalcgraphSession;
crates/ql-exec/src/workbook_runtime.rs:5505:        let mut graph = CalcgraphSession::new();
crates/ql-exec/src/workbook_runtime.rs:5544:        use crate::CalcgraphSession;
crates/ql-exec/src/workbook_runtime.rs:5547:        let mut graph = CalcgraphSession::new();
crates/ql-exec/src/workbook_runtime.rs:5594:        use crate::CalcgraphSession;
crates/ql-exec/src/workbook_runtime.rs:5601:        let rebuild = CalcgraphSession::rebuild_from_workbook(&wb);

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '4380,4510p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  4380	
  4381	    /// R2B-01: a structural failure surfaces with the exact cell address +
  4382	    /// formula text + underlying RuntimeError. No information is lost going
  4383	    /// from "failed cell" to "RecomputeFailure entry".
  4384	    #[test]
  4385	    fn recompute_all_failure_carries_exact_cell_and_formula_text() {
  4386	        let mut wb = make_runtime_workbook();
  4387	        // Seed an unparseable formula by hand-writing it into the workbook
  4388	        // (bypassing the runtime's set_formula which would reject it
  4389	        // up-front). This simulates the on-disk-corruption scenario the
  4390	        // loader handles.
  4391	        wb.put_formula(0, 3, 5, "(((");
  4392	
  4393	        let reg = default_registry();
  4394	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  4395	        let result = rt.recompute_all();
  4396	        assert!(!result.is_complete());
  4397	        assert_eq!(result.failed_count(), 1);
  4398	        let failure = &result.failures[0];
  4399	        assert_eq!(failure.sheet, 0);
  4400	        assert_eq!(failure.row, 3);
  4401	        assert_eq!(failure.col, 5);
  4402	        assert_eq!(failure.formula_text.as_ref(), "(((");
  4403	        // Underlying error is a parse error (unclosed paren).
  4404	        assert!(
  4405	            matches!(failure.error, RuntimeError::Parse(_)),
  4406	            "expected Parse error, got {:?}",
  4407	            failure.error
  4408	        );
  4409	    }
  4410	
  4411	    /// R2B-02: a failure does not short-circuit. Cells whose formulas DO
  4412	    /// parse cleanly get re-evaluated and counted as succeeded, regardless
  4413	    /// of iteration order.
  4414	    #[test]
  4415	    fn recompute_all_does_not_short_circuit_on_first_failure() {
  4416	        let mut wb = make_runtime_workbook();
  4417	        // 3 good formulas + 2 bad ones. We don't know iteration order, but
  4418	        // we know exactly 3 should succeed and exactly 2 should fail.
  4419	        wb.put_at(0, 0, 0, Value::Number(10.0));
  4420	        wb.put_formula(0, 1, 0, "A1 + 1"); // good
  4421	        wb.put_formula(0, 2, 0, "A1 * 2"); // good
  4422	        wb.put_formula(0, 3, 0, "A1 - 5"); // good
  4423	        wb.put_formula(0, 4, 0, "((("); // parse error
  4424	        wb.put_formula(0, 5, 0, "@bogus"); // lex error
  4425	
  4426	        let reg = default_registry();
  4427	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  4428	        let result = rt.recompute_all();
  4429	        assert_eq!(result.attempted, 5);
  4430	        assert_eq!(result.succeeded, 3);
  4431	        assert_eq!(result.failed_count(), 2);
  4432	        assert!(!result.is_complete());
  4433	
  4434	        // Good cells were updated regardless of order.
  4435	        assert_eq!(
  4436	            wb.read(ql_types::Address::new(0, 1, 0)),
  4437	            Value::Number(11.0)
  4438	        );
  4439	        assert_eq!(
  4440	            wb.read(ql_types::Address::new(0, 2, 0)),
  4441	            Value::Number(20.0)
  4442	        );
  4443	        assert_eq!(wb.read(ql_types::Address::new(0, 3, 0)), Value::Number(5.0));
  4444	    }
  4445	
  4446	    /// R2B-03: invalid persisted formulas do NOT panic the runtime. Every
  4447	    /// kind of structural failure (lex / parse / bind) surfaces as a
  4448	    /// `RecomputeFailure` entry; the runtime stays alive.
  4449	    #[test]
  4450	    fn recompute_all_does_not_panic_on_invalid_persisted_formulas() {
  4451	        let mut wb = make_runtime_workbook();
  4452	        wb.put_formula(0, 0, 0, "@@@"); // lex error
  4453	        wb.put_formula(0, 0, 1, "1 +"); // parse error (trailing operator)
  4454	        wb.put_formula(0, 0, 2, "UnknownName + 1"); // bind error (UnresolvedName)
  4455	
  4456	        let reg = default_registry();
  4457	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  4458	        // Just calling this must not panic — the assertion is the absence
  4459	        // of a panic, plus the structural-failure invariant.
  4460	        let result = rt.recompute_all();
  4461	        assert_eq!(result.attempted, 3);
  4462	        assert_eq!(result.succeeded, 0);
  4463	        assert_eq!(result.failed_count(), 3);
  4464	        assert!(!result.is_complete());
  4465	    }
  4466	
  4467	    /// Evaluation-time errors (Value::Error variants like #DIV/0!) count as
  4468	    /// SUCCEEDED, not failed. Recompute writes the error value to the cell
  4469	    /// per Excel canon. Only structural failures (lex/parse/bind) populate
  4470	    /// `RecomputeResult::failures`.
  4471	    #[test]
  4472	    fn recompute_all_eval_time_error_values_count_as_succeeded() {
  4473	        let mut wb = make_runtime_workbook();
  4474	        wb.put_at(0, 0, 0, Value::Number(10.0));
  4475	        wb.put_formula(0, 1, 0, "A1 / 0"); // evaluates to #DIV/0!
  4476	
  4477	        let reg = default_registry();
  4478	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  4479	        let result = rt.recompute_all();
  4480	        assert_eq!(result.succeeded, 1);
  4481	        assert_eq!(result.failed_count(), 0);
  4482	        assert!(result.is_complete());
  4483	        // The cell value is the error sentinel, written by put_at.
  4484	        assert_eq!(
  4485	            wb.read(ql_types::Address::new(0, 1, 0)),
  4486	            Value::Error(ErrorValue::DivZero)
  4487	        );
  4488	    }
  4489	
  4490	    /// Failed cells keep their pre-recompute values; their formula text is
  4491	    /// preserved (recompute never clears formula on failure).
  4492	    #[test]
  4493	    fn recompute_all_failed_cells_preserve_prior_state() {
  4494	        let mut wb = make_runtime_workbook();
  4495	        wb.put_at(0, 0, 0, Value::Number(999.0)); // pre-recompute value
  4496	        wb.put_formula(0, 0, 0, "(((");
  4497	
  4498	        let reg = default_registry();
  4499	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
  4500	        let result = rt.recompute_all();
  4501	        assert_eq!(result.failed_count(), 1);
  4502	        // Cell value untouched by failed recompute.
  4503	        assert_eq!(
  4504	            wb.read(ql_types::Address::new(0, 0, 0)),
  4505	            Value::Number(999.0)
  4506	        );
  4507	        // Formula text still on disk.
  4508	        assert_eq!(wb.formula_at(0, 0, 0).map(|s| s.as_ref()), Some("((("));
  4509	    }
  4510	

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '1600,1715p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1600	        // upper bound fits within MAX_ROW / MAX_COLUMN. Closes the gap
  1601	        // where `for r in top_row..top_row + rows` either ran beyond
  1602	        // the addressable grid or u32-overflowed silently.
  1603	        if let Err(reason) =
  1604	            ql_storage::TableMetadata::validate_footprint_bounds(top_row, top_col, rows, cols)
  1605	        {
  1606	            return Err(RuntimeError::TableCreateRejected {
  1607	                name: name.to_owned(),
  1608	                reason,
  1609	            });
  1610	        }
  1611	        if column_names.len() != cols as usize {
  1612	            return Err(RuntimeError::TableCreateRejected {
  1613	                name: name.to_owned(),
  1614	                reason: "column_names length does not match cols",
  1615	            });
  1616	        }
  1617	        if column_names.iter().any(|s| s.is_empty()) {
  1618	            return Err(RuntimeError::TableCreateRejected {
  1619	                name: name.to_owned(),
  1620	                reason: "table column names cannot be empty",
  1621	            });
  1622	        }
  1623	        // Column-name uniqueness (case-insensitive).
  1624	        {
  1625	            use std::collections::HashSet;
  1626	            let mut seen: HashSet<String> = HashSet::new();
  1627	            for cn in &column_names {
  1628	                if !seen.insert(cn.to_ascii_lowercase()) {
  1629	                    return Err(RuntimeError::TableCreateRejected {
  1630	                        name: name.to_owned(),
  1631	                        reason: "table column names must be unique (case-insensitive)",
  1632	                    });
  1633	                }
  1634	            }
  1635	        }
  1636	        // Name uniqueness (shared namespace: TableTable + NameTable).
  1637	        if self.workbook.tables().lookup(&canonical).is_some() {
  1638	            return Err(RuntimeError::TableCreateRejected {
  1639	                name: name.to_owned(),
  1640	                reason: "table with this canonical name already exists",
  1641	            });
  1642	        }
  1643	        if self.workbook.names().lookup_ci(&canonical).is_some() {
  1644	            return Err(RuntimeError::TableCreateRejected {
  1645	                name: name.to_owned(),
  1646	                reason: "defined-name with this canonical name already exists (shared namespace)",
  1647	            });
  1648	        }
  1649	        // Non-overlap + no spill anchor inside the footprint.
  1650	        // **W5-124 (Phase 4.8.J.2):** spill-anchor invariant § 4.3 #5 —
  1651	        // Excel canon: array formulas can't anchor inside a table.
  1652	        // Previously enforced only at `write_spill` time (the write-side
  1653	        // check consults `Workbook::table_at(anchor)`); creating a
  1654	        // table over an existing anchor was silently allowed, leaving
  1655	        // the table claiming a cell whose value is computed-from-spill.
  1656	        // Single fused loop avoids walking the proposed footprint twice.
  1657	        for r in top_row..top_row + rows {
  1658	            for c in top_col..top_col + cols {
  1659	                if self.workbook.table_at(sheet, r, c).is_some() {
  1660	                    return Err(RuntimeError::TableCreateRejected {
  1661	                        name: name.to_owned(),
  1662	                        reason: "table footprint overlaps an existing table",
  1663	                    });
  1664	                }
  1665	                if self.workbook.spill_anchor_at(sheet, r, c).is_some() {
  1666	                    return Err(RuntimeError::TableCreateRejected {
  1667	                        name: name.to_owned(),
  1668	                        reason: "table footprint contains a spill anchor",
  1669	                    });
  1670	                }
  1671	            }
  1672	        }
  1673	        // ----- Op-log append (BEFORE mutation per W5-103 atomicity) -----
  1674	        if let Some(oplog) = self.oplog.as_deref_mut() {
  1675	            oplog.append(Op::CreateTable {
  1676	                name: canonical.clone(),
  1677	                sheet,
  1678	                top_row,
  1679	                top_col,
  1680	                rows,
  1681	                cols,
  1682	                has_header,
  1683	                has_totals,
  1684	                column_names: column_names.clone(),
  1685	            })?;
  1686	        }
  1687	        // ----- Mutation -----
  1688	        use ql_storage::{TableColumn, TableMetadata};
  1689	        let columns: Vec<TableColumn> = column_names
  1690	            .iter()
  1691	            .map(|cn| TableColumn {
  1692	                id: self.workbook.tables_mut().allocate_column_id(),
  1693	                name: Arc::from(cn.to_ascii_lowercase().as_str()),
  1694	                display: Arc::from(cn.as_str()),
  1695	                totals_function: None,
  1696	            })
  1697	            .collect();
  1698	        let meta = TableMetadata {
  1699	            name: Arc::from(canonical.as_str()),
  1700	            display_name: Arc::from(name),
  1701	            sheet,
  1702	            top_row,
  1703	            top_col,
  1704	            rows,
  1705	            cols,
  1706	            has_header,
  1707	            has_totals,
  1708	            columns,
  1709	        };
  1710	        self.workbook
  1711	            .tables_mut()
  1712	            .insert(Arc::from(canonical.as_str()), meta);
  1713	        Ok(())
  1714	    }
  1715	

exec
/bin/zsh -lc "nl -ba crates/ql-storage/src/tables.rs | sed -n '282,410p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   282	pub struct TableTable {
   283	    tables: HashMap<Arc<str>, TableMetadata>,
   284	    /// Monotonic counter bumped on every successful mutation. Plan-cache
   285	    /// invalidation keys can include this so column renames / resizes
   286	    /// invalidate cached `ExprPlan::StructuredRef` entries. See design
   287	    /// § 7.5.
   288	    generation: u64,
   289	    /// Monotonic per-column-id allocator. Shared across all tables.
   290	    /// Allocated at column-create time; never reused.
   291	    next_column_id: u32,
   292	}
   293	
   294	impl TableTable {
   295	    pub fn new() -> Self {
   296	        Self::default()
   297	    }
   298	
   299	    /// Bind-plan cache invalidation counter (see [`NameTable::generation`]
   300	    /// for the pattern). Bumped on every successful mutation.
   301	    pub fn generation(&self) -> u64 {
   302	        self.generation
   303	    }
   304	
   305	    /// Case-insensitive lookup. Uppercases the query.
   306	    pub fn lookup(&self, name: &str) -> Option<&TableMetadata> {
   307	        let upper = name.to_ascii_uppercase();
   308	        self.tables.get(upper.as_str())
   309	    }
   310	
   311	    /// Reverse lookup: which table contains the given cell, if any?
   312	    /// O(num_tables) scan; tables-per-workbook is small (typically < 100).
   313	    ///
   314	    /// Used by:
   315	    /// - the binder for `[@Col]` resolution (which table is the formula in?).
   316	    /// - `create_table` / `resize_table` validation (does the proposed
   317	    ///   footprint overlap another table?).
   318	    pub fn table_at(&self, addr: Address) -> Option<&TableMetadata> {
   319	        self.tables.values().find(|t| t.contains(addr))
   320	    }
   321	
   322	    /// All table entries; HashMap-arbitrary order.
   323	    pub fn iter(&self) -> impl Iterator<Item = (&Arc<str>, &TableMetadata)> + '_ {
   324	        self.tables.iter()
   325	    }
   326	
   327	    pub fn len(&self) -> usize {
   328	        self.tables.len()
   329	    }
   330	
   331	    pub fn is_empty(&self) -> bool {
   332	        self.tables.is_empty()
   333	    }
   334	
   335	    /// **Loader / mutation-internal API.** Insert a table by canonical
   336	    /// uppercase name. The caller is responsible for:
   337	    /// - canonicalizing the key (uppercase) BEFORE calling.
   338	    /// - validating uniqueness, non-overlap, valid Excel-shaped name
   339	    ///   (design § 5.3) — `WorkbookRuntime::create_table` does this in
   340	    ///   4.8.H.
   341	    ///
   342	    /// Bumps the generation counter on success.
   343	    ///
   344	    /// **W5-123 (Phase 4.8.L):** also auto-bumps `next_column_id` past
   345	    /// the highest column id in `meta.columns`. The `.qbook` loader
   346	    /// (Phase 4.8.L persistence) preserves stable column ids verbatim;
   347	    /// without this self-maintenance the runtime allocator could
   348	    /// later hand out an id that collides with one already in storage.
   349	    pub fn insert(&mut self, canonical: Arc<str>, meta: TableMetadata) {
   350	        if let Some(max_id) = meta.columns.iter().map(|c| c.id).max() {
   351	            self.next_column_id = self.next_column_id.max(max_id + 1);
   352	        }
   353	        self.tables.insert(canonical, meta);
   354	        self.generation = self.generation.wrapping_add(1);
   355	    }
   356	
   357	    /// Remove a table by canonical name. Bumps generation iff a removal
   358	    /// occurred (idempotent no-op clears don't invalidate caches).
   359	    pub fn remove(&mut self, canonical: &str) -> Option<TableMetadata> {
   360	        let upper = canonical.to_ascii_uppercase();
   361	        let removed = self.tables.remove(upper.as_str());
   362	        if removed.is_some() {
   363	            self.generation = self.generation.wrapping_add(1);
   364	        }
   365	        removed
   366	    }
   367	
   368	    /// Allocate and return the next column id. Each call returns a fresh
   369	    /// id; the counter is monotonic across the workbook lifetime (per
   370	    /// design § 4.1). Wraps at u32::MAX via `wrapping_add`; in practice
   371	    /// 4 billion column allocations is unreachable.
   372	    pub fn allocate_column_id(&mut self) -> u32 {
   373	        let id = self.next_column_id;
   374	        self.next_column_id = self.next_column_id.wrapping_add(1);
   375	        id
   376	    }
   377	
   378	    /// Mutable access to a table by canonical name. Used by the loader
   379	    /// and by mutation methods in 4.8.H+. Bumps generation on mutation
   380	    /// is the CALLER's responsibility (since `&mut TableMetadata` lets
   381	    /// the caller see no-op mutations too).
   382	    pub fn get_mut(&mut self, canonical: &str) -> Option<&mut TableMetadata> {
   383	        let upper = canonical.to_ascii_uppercase();
   384	        self.tables.get_mut(upper.as_str())
   385	    }
   386	
   387	    /// Explicitly bump generation. For callers that mutated through
   388	    /// `get_mut` and need to invalidate caches.
   389	    pub fn bump_generation(&mut self) {
   390	        self.generation = self.generation.wrapping_add(1);
   391	    }
   392	}
   393	
   394	#[cfg(test)]
   395	mod tests {
   396	    use super::*;
   397	
   398	    fn col(id: u32, name: &str) -> TableColumn {
   399	        TableColumn {
   400	            id,
   401	            name: Arc::from(name.to_ascii_lowercase().as_str()),
   402	            display: Arc::from(name),
   403	            totals_function: None,
   404	        }
   405	    }
   406	
   407	    fn sales_table() -> TableMetadata {
   408	        // Sales: A1:D10 with header (row 0) + totals (row 9). 8 data rows.
   409	        TableMetadata {
   410	            name: Arc::from("SALES"),

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/workbook_runtime.rs | sed -n '11595,11645p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
 11595	    }
 11596	
 11597	    /// **Coverage matrix has no duplicate cells.** Every
 11598	    /// (mode, locale, input) triple is unique — sanity that the
 11599	    /// matrix builder didn't accidentally repeat a row.
 11600	    #[test]
 11601	    fn coverage_matrix_has_no_duplicates() {
 11602	        use std::collections::HashSet;
 11603	        let cells = coverage_matrix();
 11604	        let mut seen = HashSet::new();
 11605	        for cell in &cells {
 11606	            let key = format!("{:?}|{:?}|{}", cell.mode, cell.locale, cell.input);
 11607	            assert!(seen.insert(key.clone()), "duplicate matrix cell: {key}");
 11608	        }
 11609	    }
 11610	
 11611	    /// **4.9.O audit verification:** plan cache collision when
 11612	    /// `@<range>` is used at multiple cells in the same runtime
 11613	    /// session. The W5-144 binder narrows `@A:A` at bind time using
 11614	    /// site.cell, producing a cell-specific `ExprPlan::CellRef`.
 11615	    /// But `PlanCacheKey` is `(canonical_text, sheet, name_gen)` —
 11616	    /// no cell coords. Two cells in the same sheet with the same
 11617	    /// canonical text `@A:A` would hit the cache and get the FIRST
 11618	    /// call's narrowed plan, reading the wrong row.
 11619	    ///
 11620	    /// This test must PASS for the engine to be correct under the
 11621	    /// "same `@<range>` formula at multiple cells" workload.
 11622	    #[test]
 11623	    fn plan_cache_at_range_collision_across_cells_regression() {
 11624	        let mut wb = make_runtime_workbook();
 11625	        wb.put(
 11626	            ql_types::Address::new(0, 3, 0),
 11627	            ql_types::Value::Number(11.0),
 11628	        );
 11629	        wb.put(
 11630	            ql_types::Address::new(0, 5, 0),
 11631	            ql_types::Value::Number(99.0),
 11632	        );
 11633	        let reg = default_registry();
 11634	        let mut rt = WorkbookRuntime::new(&mut wb, &reg);
 11635	        let v1 = rt.set_formula(0, 3, 1, "@A:A").unwrap();
 11636	        assert_eq!(
 11637	            v1,
 11638	            ql_types::Value::Number(11.0),
 11639	            "first @A:A at row 3 must read A4"
 11640	        );
 11641	        let v2 = rt.set_formula(0, 5, 1, "@A:A").unwrap();
 11642	        assert_eq!(
 11643	            v2,
 11644	            ql_types::Value::Number(99.0),
 11645	            "second @A:A at row 5 must read A6, NOT the cached A4 plan"

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/plan.rs | sed -n '1268,1338p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
  1268	///   range when possible (e.g., `headers ∪ data` = `all - totals`).
  1269	/// - `ThisRowColumn(col)` / `ThisRowColumnRange(c1, c2)` → column's
  1270	///   full data range with `is_this_row: true`. Eval narrows.
  1271	fn resolve_structured_ref(
  1272	    table_name: &Arc<str>,
  1273	    spec: &ql_formula_syntax::TableSpecSubtree,
  1274	    tables: &dyn TableLookup,
  1275	) -> Result<ExprPlan, BindError> {
  1276	    use ql_formula_syntax::TableSpecSubtree;
  1277	    let table = tables
  1278	        .lookup_table(table_name.as_ref())
  1279	        .ok_or_else(|| BindError::UnknownTable(Arc::clone(table_name)))?;
  1280	    let canonical_name = Arc::clone(&table.name);
  1281	    // Helper closures for the common error paths.
  1282	    let unknown_column = |col: &Arc<str>| BindError::UnknownTableColumn {
  1283	        table: Arc::clone(&canonical_name),
  1284	        column: Arc::clone(col),
  1285	    };
  1286	
  1287	    let (resolved, is_this_row) = match spec {
  1288	        TableSpecSubtree::BareColumn(col) => {
  1289	            let (idx, _) = table
  1290	                .lookup_column(col)
  1291	                .ok_or_else(|| unknown_column(col))?;
  1292	            let range = table.column_data_range(idx).ok_or_else(|| {
  1293	                BindError::StructuredRefDegenerateRange(Arc::clone(&canonical_name))
  1294	            })?;
  1295	            (range, false)
  1296	        }
  1297	        TableSpecSubtree::ThisRowColumn(col) => {
  1298	            let (idx, _) = table
  1299	                .lookup_column(col)
  1300	                .ok_or_else(|| unknown_column(col))?;
  1301	            // Eval narrows to one cell; binder hands back the full data
  1302	            // column. If the column has no data rows, the eval narrow
  1303	            // returns degenerate → cell-boundary maps to #VALUE!
  1304	            // (handled at 4.8.G).
  1305	            let range = table.column_data_range(idx).ok_or_else(|| {
  1306	                BindError::StructuredRefDegenerateRange(Arc::clone(&canonical_name))
  1307	            })?;
  1308	            (range, true)
  1309	        }
  1310	        TableSpecSubtree::ThisRowColumnRange(c1, c2) => {
  1311	            let (i1, _) = table.lookup_column(c1).ok_or_else(|| unknown_column(c1))?;
  1312	            let (i2, _) = table.lookup_column(c2).ok_or_else(|| unknown_column(c2))?;
  1313	            let lo = i1.min(i2);
  1314	            let hi = i1.max(i2);
  1315	            let r1 = table.column_data_range(lo).ok_or_else(|| {
  1316	                BindError::StructuredRefDegenerateRange(Arc::clone(&canonical_name))
  1317	            })?;
  1318	            let r2 = table.column_data_range(hi).ok_or_else(|| {
  1319	                BindError::StructuredRefDegenerateRange(Arc::clone(&canonical_name))
  1320	            })?;
  1321	            (
  1322	                Range::new(r1.sheet, r1.start_row, r1.start_col, r2.end_row, r2.end_col),
  1323	                true,
  1324	            )
  1325	        }
  1326	        TableSpecSubtree::Combination(items) => {
  1327	            resolve_sref_combination(&canonical_name, table, items)?
  1328	        }
  1329	    };
  1330	
  1331	    Ok(ExprPlan::StructuredRef {
  1332	        table_name: canonical_name,
  1333	        source: Arc::new(spec.clone()),
  1334	        resolved,
  1335	        is_this_row,
  1336	    })
  1337	}
  1338	

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/calcgraph_session.rs | sed -n '246,318p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   246	        ExprPlan::Array(_) | ExprPlan::Error(_) => {
   247	            // No deps; leaf literals.
   248	        }
   249	        // **W5-115 (Phase 4.8.F):** structured-ref dep extraction.
   250	        // Treat the resolved range as a range dep (same path as
   251	        // `AggregateNameRef`); the calcgraph stripe index handles
   252	        // invalidation when any cell inside the table footprint
   253	        // changes.
   254	        //
   255	        // **W5-154 (Phase 4.8.G.3 foundation):** ALSO push the table
   256	        // name into `deps.tables` so the `table_to_formulas` reverse
   257	        // index can find this formula when `on_table_*` mutation
   258	        // hooks fire (table rename / drop / column rename / resize).
   259	        ExprPlan::StructuredRef {
   260	            table_name,
   261	            resolved,
   262	            ..
   263	        } => {
   264	            deps.named_ranges.push((Arc::clone(table_name), *resolved));
   265	            deps.tables.push(Arc::clone(table_name));
   266	        }
   267	    }
   268	}
   269	
   270	/// Phase 3.1 runtime ↔ calcgraph integration container. Owns the
   271	/// `Graph` plus an O(1) cell index. Construct via `new()` for empty,
   272	/// or `rebuild_from_workbook` to build from an existing workbook's
   273	/// formulas.
   274	///
   275	/// Single-writer ownership: the runtime borrows `&mut CalcgraphSession`
   276	/// for the lifetime of one edit (same pattern as `&mut OpLog`).
   277	#[derive(Debug, Default)]
   278	pub struct CalcgraphSession {
   279	    graph: Graph,
   280	    /// O(1) lookup from a cell address to its `NodeId`. The Phase 0
   281	    /// `Graph` itself doesn't carry this index — it's a runtime-side
   282	    /// concern (the bench / structural tests don't need it). 3.1 puts
   283	    /// it here so the runtime hooks can answer "do I already have a
   284	    /// node for this cell?" without scanning.
   285	    cell_index: HashMap<(SheetId, RowId, ColId), NodeId>,
   286	    /// Phase 3.2: per-formula live dependency view. Keyed by the
   287	    /// formula cell's `NodeId`; replaced wholesale on re-bind so
   288	    /// stale entries can't leak (the Phase 0 graph's edges are
   289	    /// append-only; this side-table sidesteps that until Phase 3.3
   290	    /// brings delta-edge storage in).
   291	    formula_deps: HashMap<NodeId, FormulaDeps>,
   292	    /// Phase 3.2: formulas marked volatile by `walk_plan_for_deps`.
   293	    /// Phase 3.7 (volatile invalidation) will read this set every
   294	    /// recompute cycle to schedule re-evaluation regardless of
   295	    /// upstream dependency state.
   296	    volatile_formulas: HashSet<NodeId>,
   297	    /// Phase 3.2: reverse index from canonical name → formulas that
   298	    /// reference it. Today populated only by `AggregateNameRef` (the
   299	    /// only plan variant that preserves the name after binding). Used
   300	    /// in Phase 3.3 to mark formulas dirty when a named range
   301	    /// changes.
   302	    name_to_formulas: HashMap<Arc<str>, HashSet<NodeId>>,
   303	    /// **W5-154 (Phase 4.8.G.3 foundation):** reverse index from
   304	    /// canonical (case-preserved as stored at extract time) table
   305	    /// name → formula nodes that reference it via
   306	    /// `ExprPlan::StructuredRef`. Used by `on_table_*` mutation
   307	    /// hooks (drop / rename / column rename / resize) to BFS-fan
   308	    /// dirty per the design § 4.5 invalidation contract.
   309	    /// Symmetric to `name_to_formulas` for the structured-ref path.
   310	    ///
   311	    /// Today (W5-154): only `on_table_drop` consumes the index. The
   312	    /// other 4 hooks (create / rename / column rename / resize) and
   313	    /// runtime-side wiring land in subsequent commits per the
   314	    /// design § 4.5 hook list.
   315	    table_to_formulas: HashMap<Arc<str>, HashSet<NodeId>>,
   316	    /// Phase 3.3 (2026-05-12): reverse index from a cell address to
   317	    /// the formula node-ids that hold a DIRECT cell reference to it
   318	    /// (i.e. `ExprPlan::CellRef`). Range-dep candidates go through

exec
/bin/zsh -lc "nl -ba crates/ql-exec/src/calcgraph_session.rs | sed -n '170,210p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
 succeeded in 0ms:
   170	/// `cells`, `named_ranges`, and `names` are deduplicated by the
   171	/// `extract_and_register_deps` orchestrator (the walker emits
   172	/// duplicates; the orchestrator collapses).
   173	#[derive(Clone, Debug, Default, PartialEq)]
   174	pub struct FormulaDeps {
   175	    /// Direct cell references (`ExprPlan::CellRef`). Resolved sheet
   176	    /// is included.
   177	    pub cells: Vec<(SheetId, RowId, ColId)>,
   178	    /// Named-range references (`ExprPlan::AggregateNameRef`). Each
   179	    /// entry pairs the canonical (uppercase) name with the resolved
   180	    /// `Range` payload.
   181	    pub named_ranges: Vec<(Arc<str>, Range)>,
   182	    /// Names referenced by the formula. Currently only names that
   183	    /// resolve to ranges populate this (via `AggregateNameRef`); see
   184	    /// module docs for the limitation around scalar-resolved names.
   185	    pub names: Vec<Arc<str>>,
   186	    /// **W5-154 (Phase 4.8.G.3 foundation):** table names referenced by
   187	    /// the formula (via `ExprPlan::StructuredRef.table_name`). Used by
   188	    /// the `table_to_formulas` reverse index for `on_table_*`
   189	    /// mutation hooks. Populated alongside `named_ranges` (the
   190	    /// resolved range still goes there for stripe registration).
   191	    pub tables: Vec<Arc<str>>,
   192	    /// `true` iff any volatile function appears anywhere in the
   193	    /// plan tree.
   194	    pub is_volatile: bool,
   195	}
   196	
   197	impl FormulaDeps {
   198	    /// Total dependency count (cells + named ranges). Useful for
   199	    /// summary metrics; Phase 3.10 megaudit / ql-profile will surface
   200	    /// per-formula dep counts in the graph-profile JSON.
   201	    pub fn len(&self) -> usize {
   202	        self.cells.len() + self.named_ranges.len()
   203	    }
   204	
   205	    pub fn is_empty(&self) -> bool {
   206	        self.cells.is_empty() && self.named_ranges.is_empty()
   207	    }
   208	}
   209	
   210	/// Phase 3.2 — recursive walker. Accumulates dependencies + volatile-

codex
I agree with your suspected cache/asymmetry issue. The hook is wired, but the W5-155 contract is not actually satisfied.

**HIGH**

- [crates/ql-exec/src/workbook_runtime.rs:1738](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:1738), [crates/ql-exec/src/workbook_runtime.rs:3358](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:3358), [crates/ql-exec/src/plan_cache.rs:49](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/plan_cache.rs:49)  
  Contract violated: “next recompute re-binds dropped-table structured refs.”  
  `drop_table` calls `g.on_table_drop(&canonical)`, but does not invalidate `PlanCache`. `try_recompute_with_simd_profile` keys only on `(text, sheet, NameTable generation, cell_anchor)`, so a pre-drop cached `ExprPlan::StructuredRef { resolved: <old range> }` can be reused. `schedule_dirty()` drains dirty before eval, so the dirty mark is consumed and the formula can keep reading the old cells.  
  Proposed fix: add `self.plan_cache.clear()` in `drop_table`, or better include `TableTable::generation()` in `PlanCacheKey` and rely on `TableTable::remove()` bumping it. The peer clear calls make this look accidental, not intentional.

- [crates/ql-exec/src/workbook_runtime.rs:3107](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:3107), [crates/ql-exec/src/workbook_runtime.rs:3253](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:3253), [crates/ql-exec/src/calcgraph_session.rs:936](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/calcgraph_session.rs:936)  
  Contract violated: “`BindError::UnknownTable` → `Value::Error(#NAME?)`.”  
  Even after cache invalidation, bind failure is returned as `RuntimeError::Bind`, collected into `RecomputeFailure`, and the cell value is not written. Existing tests document structural failures preserving prior state.  
  Proposed fix: if the intended spreadsheet behavior is `#NAME?`, map structured-ref bind failures such as `UnknownTable` / `UnknownTableColumn` during recompute to `Value::Error(ErrorValue::Name)` and write it. Otherwise downgrade the docstring to “recompute reports a bind failure and leaves prior value intact.”

**MEDIUM**

- [crates/ql-exec/src/workbook_runtime.rs:10083](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:10083)  
  Contract not tested: `drop_table_fires_on_table_drop_hook_and_dirties_readers` only proves hook count and dirty state. It does not call `recompute_dirty()` after the drop and does not assert B1 becomes `#NAME?`.  
  Proposed fix: extend the test to drop, recompute, assert B1 is `Value::Error(ErrorValue::Name)`, and assert no stale `30.0` remains. This test should be written with a warmed cache, which the current `set_formula` path already provides.

**LOW**

- [crates/ql-exec/src/calcgraph_session.rs:943](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/calcgraph_session.rs:943)  
  Stale docstring: says `WorkbookRuntime::drop_table` does not call the hook and “wiring lands in a follow-up commit.” W5-155 now wires it.  
  Proposed fix: update/remove those W5-154 lines.

**Non-Findings**

- [crates/ql-oplog/src/replay.rs:523](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-oplog/src/replay.rs:523): replay removing table metadata directly is correct. Replay has no live calcgraph/plan cache; post-replay graph state is rebuilt from workbook state.

- [crates/ql-exec/src/workbook_runtime.rs:1743](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:1743): hook ordering is fine. `&mut WorkbookRuntime` prevents concurrent interleaving; op-log append happens before mutation, and the hook is workbook-read-free.

Verification run: `cargo test -p ql-exec drop_table_fires_on_table_drop_hook_and_dirties_readers` passes, but it only verifies dirtying.
tokens used
142,929
I agree with your suspected cache/asymmetry issue. The hook is wired, but the W5-155 contract is not actually satisfied.

**HIGH**

- [crates/ql-exec/src/workbook_runtime.rs:1738](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:1738), [crates/ql-exec/src/workbook_runtime.rs:3358](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:3358), [crates/ql-exec/src/plan_cache.rs:49](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/plan_cache.rs:49)  
  Contract violated: “next recompute re-binds dropped-table structured refs.”  
  `drop_table` calls `g.on_table_drop(&canonical)`, but does not invalidate `PlanCache`. `try_recompute_with_simd_profile` keys only on `(text, sheet, NameTable generation, cell_anchor)`, so a pre-drop cached `ExprPlan::StructuredRef { resolved: <old range> }` can be reused. `schedule_dirty()` drains dirty before eval, so the dirty mark is consumed and the formula can keep reading the old cells.  
  Proposed fix: add `self.plan_cache.clear()` in `drop_table`, or better include `TableTable::generation()` in `PlanCacheKey` and rely on `TableTable::remove()` bumping it. The peer clear calls make this look accidental, not intentional.

- [crates/ql-exec/src/workbook_runtime.rs:3107](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:3107), [crates/ql-exec/src/workbook_runtime.rs:3253](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:3253), [crates/ql-exec/src/calcgraph_session.rs:936](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/calcgraph_session.rs:936)  
  Contract violated: “`BindError::UnknownTable` → `Value::Error(#NAME?)`.”  
  Even after cache invalidation, bind failure is returned as `RuntimeError::Bind`, collected into `RecomputeFailure`, and the cell value is not written. Existing tests document structural failures preserving prior state.  
  Proposed fix: if the intended spreadsheet behavior is `#NAME?`, map structured-ref bind failures such as `UnknownTable` / `UnknownTableColumn` during recompute to `Value::Error(ErrorValue::Name)` and write it. Otherwise downgrade the docstring to “recompute reports a bind failure and leaves prior value intact.”

**MEDIUM**

- [crates/ql-exec/src/workbook_runtime.rs:10083](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:10083)  
  Contract not tested: `drop_table_fires_on_table_drop_hook_and_dirties_readers` only proves hook count and dirty state. It does not call `recompute_dirty()` after the drop and does not assert B1 becomes `#NAME?`.  
  Proposed fix: extend the test to drop, recompute, assert B1 is `Value::Error(ErrorValue::Name)`, and assert no stale `30.0` remains. This test should be written with a warmed cache, which the current `set_formula` path already provides.

**LOW**

- [crates/ql-exec/src/calcgraph_session.rs:943](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/calcgraph_session.rs:943)  
  Stale docstring: says `WorkbookRuntime::drop_table` does not call the hook and “wiring lands in a follow-up commit.” W5-155 now wires it.  
  Proposed fix: update/remove those W5-154 lines.

**Non-Findings**

- [crates/ql-oplog/src/replay.rs:523](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-oplog/src/replay.rs:523): replay removing table metadata directly is correct. Replay has no live calcgraph/plan cache; post-replay graph state is rebuilt from workbook state.

- [crates/ql-exec/src/workbook_runtime.rs:1743](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-exec/src/workbook_runtime.rs:1743): hook ordering is fine. `&mut WorkbookRuntime` prevents concurrent interleaving; op-log append happens before mutation, and the hook is workbook-read-free.

Verification run: `cargo test -p ql-exec drop_table_fires_on_table_drop_hook_and_dirties_readers` passes, but it only verifies dirtying.
