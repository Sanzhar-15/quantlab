Reading prompt from stdin...
OpenAI Codex v0.130.0
--------
workdir: /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR, /Users/sanzhar/.codex/memories]
reasoning effort: xhigh
reasoning summaries: none
session id: 019e507e-dea2-73c1-9e74-4395c2f08159
--------
user

# Codex audit: Phase 5.7 V2.7 — structured Error.code discrimination

## Context

V2.7 closes V2.1+V2.2+V2.3 Opus MEDIUM-3 carryforwards: engine error
variant discriminants were lost on the napi boundary because
`Error::from_reason(format!("{e}"))` only carried the Display string.

State at HEAD:
- engine `2a3e2ebcbfe` (V2.7 ship) on branch `feat/quantbook-engine`
- IDE `2a5619f9162` (V2.7 ship) on branch `feat/visualise-v1`

## What V2.7 shipped

### Engine (`2a3e2ebcbfe`)

1. `crates/ql-collab/src/transport.rs`: `impl TransportError {
   pub fn kind(&self) -> &'static str }` returning:
   - `Io(_)` → `"transport_io"`
   - `Closed` → `"transport_closed"`
   Plus 2 Rust unit tests.

2. `crates/ql-collab-ws/src/lib.rs`: `impl WebSocketError {
   pub fn kind(&self) -> &'static str }` returning:
   - `InvalidUrl(_)` → `"websocket_invalid_url"`
   - `ConnectFailed(_)` → `"websocket_connect_failed"`
   - `HandshakeFailed(_)` → `"websocket_handshake_failed"`
   - `RuntimeError(_)` → `"websocket_runtime_error"`
   Plus 1 Rust unit test.

3. `crates/ql-collab/src/session.rs`: `impl CollabSessionError {
   pub fn kind(&self) -> &'static str }` returning:
   - `OpLog(_)` → `"session_oplog"`
   - `Presence(_)` → `"session_presence"`
   - `Undo(_)` → `"session_undo"`
   - `Replay(_)` → `"session_replay"`
   - **`Transport(inner)` → `inner.kind()` passthrough** (the IDE
     contract: branch on the actual transport state, not a wrapper).
   Plus 1 Rust unit test.

4. `crates/ql-bindings-node/src/lib.rs`: three private helper fns
   (`collab_session_error_to_napi`, `transport_error_to_napi`,
   `websocket_error_to_napi`) each prepending `[<kind>]` to the
   Display string. **10 `.map_err` call sites updated** to use
   these helpers (CollabSession::{new, fromSnapshot, appendPutValue,
   exportBytes, mergeBytes, flushToTransport, pollRemote,
   flushDeltaToTransport, pollRemoteWithLimit, flushPendingToTransport's
   wait_for_drain path, Transport::websocketConnect).

### IDE (`2a5619f9162`)

1. `extensions/quantlab/src/quantbook/types.ts`:
   - `QuantbookErrorCode`: union of 11 codes (all 10 engine codes +
     `'unknown'` fallback).
   - `QuantbookErrorInfo`: `{ code, message, cause }` structured view.

2. `extensions/quantlab/src/quantbook/session.ts`:
   - `parseQuantbookError(err: unknown): QuantbookErrorInfo` extracts
     the `[<code>] <message>` prefix via regex
     `/^\[([a-z][a-z_]*)\]\s*(.*)$/s`.
   - `isQuantbookErrorCode(value: unknown): value is QuantbookErrorCode`
     type guard against `KNOWN_QUANTBOOK_ERROR_CODES` const set.

3. `extensions/quantlab/test/quantbook-roundtrip.test.ts`:
   - 8 parser unit tests (no engine load).
   - 3 end-to-end tests (real engine errors): invalid-URL surfaces
     `websocket_invalid_url`, connection-refused surfaces
     `websocket_connect_failed`, V2.3 substring tests still pass.

## Test outcomes

- `cargo fmt --all` + `cargo clippy --workspace --all-features` clean.
- `cargo test -p ql-collab --release --features test-fixtures --lib`:
  74/74 (was 71; +3 kind tests).
- `cargo test -p ql-collab-ws --release --lib`: 4/4 (was 3; +1).
- `cargo build -p ql-bindings-node --release`: clean.
- IDE mocha: 88/88 (was 77 at V2.5+V2.6 close; +11 V2.7).
- The end-to-end mocha "V2.3 substring tests still pass" empirically
  pins both `/invalid WebSocket URL/` AND `/^\[websocket_invalid_url\]/`
  on the same error.

## What Codex should audit

### Protocol & correctness lane

1. **Source-walk the engine error kinds**. Read each `kind()` impl
   in `transport.rs`, `ql-collab-ws/lib.rs`, `session.rs`. Verify
   match coverage:
   - All TransportError variants (2: Io, Closed) covered? ✓
   - All WebSocketError variants (4: InvalidUrl, ConnectFailed,
     HandshakeFailed, RuntimeError) covered? ✓
   - All CollabSessionError variants (5: OpLog, Presence, Undo,
     Transport, Replay) covered? With Transport-passthrough? ✓
   - Each enum is `#[non_exhaustive]`; the match is INSIDE the
     defining crate, so the compiler enforces exhaustiveness for
     adds — verify a new variant added without updating `kind()`
     would fail to compile.

2. **Verify napi helper threading**. Source-walk
   `crates/ql-bindings-node/src/lib.rs`. Confirm every
   `.map_err(|e| Error::from_reason(format!("{e}")))` site that
   touches an engine error type is now routed through the helpers.
   `grep -n 'Error::from_reason(format!.*\{e\}'` should show only
   the spawn_blocking JoinError path remaining (line ~861).

3. **Verify the bracket-prefix convention matches what IDE expects**.
   The IDE regex is `/^\[([a-z][a-z_]*)\]\s*(.*)$/s`. Each helper
   produces `format!("[{}] {e}", e.kind())`. Confirm kind strings
   match the regex character class.

4. **Verify CollabSessionError::Transport passthrough**. The
   binding maps every `CollabSessionError` via
   `collab_session_error_to_napi`. For
   `CollabSessionError::Transport(TransportError::Closed)`, the
   produced message should be `"[transport_closed] transport error:
   transport closed"`. The IDE consumer can branch on
   `info.code === 'transport_closed'` without unwrapping a
   `"session_transport"` wrapper. Pin this.

### Adversarial / edge cases lane

5. **V2.3 substring-match regression risk**. The V2.3 mocha tests
   match on substrings like `/invalid WebSocket URL/`. V2.7 prepends
   `"[websocket_invalid_url] "` but Display body is preserved.
   Verify the V2.3 assertion regexes survive against the new
   prefixed messages. The V2.7 "V2.3 substring tests still pass"
   mocha test pins this; confirm it's not vacuous.

6. **Drift hazard**: KNOWN_QUANTBOOK_ERROR_CODES const set in
   `session.ts` is hand-maintained vs. the union type. If engine
   adds a new kind without IDE update, `parseQuantbookError` falls
   back to `'unknown'` BUT preserves the full message (the test
   "falls back to unknown for unrecognized prefix code" pins this).
   Is this the right behavior? Should the IDE binding `throw` on
   binding-drift signal instead?

7. **`parseQuantbookError` regex robustness**. The regex
   `/^\[([a-z][a-z_]*)\]\s*(.*)$/s` requires snake_case starting
   with lowercase letter. Could any engine kind violate this? Could
   a malicious or buggy engine produce a payload that evades
   parsing (e.g., embedded brackets in the Display string)?

8. **Audit-discipline Rule 4**: any negative trait/behavior claim
   in the new code? `parseQuantbookError` docstring says "falls
   back to 'unknown' if the value is not an Error instance, OR the
   prefix code is not recognized" — verify both paths exist.

### Documentation lane

9. **Module doc block consistency**. The engine binding's
   `crates/ql-bindings-node/src/lib.rs` has a new V2.7 doc block
   explaining the prefix convention. Read it for drift with the
   actual code.

10. **JSDoc accuracy**. The IDE `parseQuantbookError` JSDoc claims
    "Falls back to 'unknown'" in 3 cases. Verify each case is
    actually a separate branch.

### Report format

- **VERDICT**: PASS / PASS-WITH-FINDINGS / FAIL
- **Strongest concern**: one sentence
- **Findings** (H/M/L) with file:line citations
- **Match coverage verdicts** (Q1-Q4): VERIFIED / UNVERIFIED / FAILED
- **Recommended changes** (if any) before closure commit

Engine root: `/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/`
IDE root: `/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/`

codex
I’ll audit this as a code review: first I’m loading the repo instructions and the exact files named in the request, then I’ll verify the error mapping paths and tests against the stated contract.
exec
/bin/zsh -lc "sed -n '1,260p' .github/copilot-instructions.md" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
exec
/bin/zsh -lc "sed -n '1,220p' AGENTS.md" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
exec
/bin/zsh -lc 'pwd && ls' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
2026-05-11-222312-this-session-is-being-continued-from-a-previous-c.txt
2026-05-13-003338-this-session-is-being-continued-from-a-previous-c.txt
AGENTS.md
CONTRIBUTING.md
Charts
CodeQL.yml
DESIGN_SYSTEM.md
LICENSE.txt
Logo
PATCHES.md
QIC_UI_IMPLEMENTATION.txt
QIC_UI_SPEC
Quantlab_On
README.md
Resources_bar_build
SECURITY.md
Server_side_implementation_plan
Switchconvo.txt
Switchsummary.md
ThirdPartyNotices.txt
Working_on_QIC.txt
build
cglicenses.json
cgmanifest.json
cli
crates
docs
engine
eslint.config.js
extensions
gulpfile.mjs
image.png
node_modules
package-lock.json
package.json
product.json
quantbook-engine
remote
resources
schemas
scripts
src
test
tsfmt.json

 succeeded in 0ms:
# VS Code Agents Instructions

This file provides instructions for AI coding agents working with the VS Code codebase.

For detailed project overview, architecture, coding guidelines, and validation steps, see the [Copilot Instructions](.github/copilot-instructions.md).

 succeeded in 0ms:
# VS Code Copilot Instructions

## Project Overview

Visual Studio Code is built with a layered architecture using TypeScript, web APIs and Electron, combining web technologies with native app capabilities. The codebase is organized into key architectural layers:

### Root Folders
- `src/`: Main TypeScript source code with unit tests in `src/vs/*/test/` folders
- `build/`: Build scripts and CI/CD tools
- `extensions/`: Built-in extensions that ship with VS Code
- `test/`: Integration tests and test infrastructure
- `scripts/`: Development and build scripts
- `resources/`: Static resources (icons, themes, etc.)
- `out/`: Compiled JavaScript output (generated during build)

### Core Architecture (`src/` folder)
- `src/vs/base/` - Foundation utilities and cross-platform abstractions
- `src/vs/platform/` - Platform services and dependency injection infrastructure
- `src/vs/editor/` - Text editor implementation with language services, syntax highlighting, and editing features
- `src/vs/workbench/` - Main application workbench for web and desktop
  - `workbench/browser/` - Core workbench UI components (parts, layout, actions)
  - `workbench/services/` - Service implementations
  - `workbench/contrib/` - Feature contributions (git, debug, search, terminal, etc.)
  - `workbench/api/` - Extension host and VS Code API implementation
- `src/vs/code/` - Electron main process specific implementation
- `src/vs/server/` - Server specific implementation

The core architecture follows these principles:
- **Layered architecture** - from `base`, `platform`, `editor`, to `workbench`
- **Dependency injection** - Services are injected through constructor parameters
- **Contribution model** - Features contribute to registries and extension points
- **Cross-platform compatibility** - Abstractions separate platform-specific code

### Built-in Extensions (`extensions/` folder)
The `extensions/` directory contains first-party extensions that ship with VS Code:
- **Language support** - `typescript-language-features/`, `html-language-features/`, `css-language-features/`, etc.
- **Core features** - `git/`, `debug-auto-launch/`, `emmet/`, `markdown-language-features/`
- **Themes** - `theme-*` folders for default color themes
- **Development tools** - `extension-editing/`, `vscode-api-tests/`

Each extension follows the standard VS Code extension structure with `package.json`, TypeScript sources, and contribution points to extend the workbench through the Extension API.

### Finding Related Code
1. **Semantic search first**: Use file search for general concepts
2. **Grep for exact strings**: Use grep for error messages or specific function names
3. **Follow imports**: Check what files import the problematic module
4. **Check test files**: Often reveal usage patterns and expected behavior

## Validating TypeScript changes

MANDATORY: Always check the `VS Code - Build` watch task output via #runTasks/getTaskOutput for compilation errors before running ANY script or declaring work complete, then fix all compilation errors before moving forward.

- NEVER run tests if there are compilation errors
- NEVER use `npm run compile` to compile TypeScript files but call #runTasks/getTaskOutput instead

### TypeScript compilation steps
- Monitor the `VS Code - Build` task outputs for real-time compilation errors as you make changes
- This task runs `Core - Build` and `Ext - Build` to incrementally compile VS Code TypeScript sources and built-in extensions
- Start the task if it's not already running in the background

### TypeScript validation steps
- Use the run test tool if you need to run tests. If that tool is not available, then you can use `scripts/test.sh` (or `scripts\test.bat` on Windows) for unit tests (add `--grep <pattern>` to filter tests) or `scripts/test-integration.sh` (or `scripts\test-integration.bat` on Windows) for integration tests (integration tests end with .integrationTest.ts or are in /extensions/).
- Use `npm run valid-layers-check` to check for layering issues

## Coding Guidelines

### Indentation

We use tabs, not spaces.

### Naming Conventions

- Use PascalCase for `type` names
- Use PascalCase for `enum` values
- Use camelCase for `function` and `method` names
- Use camelCase for `property` names and `local variables`
- Use whole words in names when possible

### Types

- Do not export `types` or `functions` unless you need to share it across multiple components
- Do not introduce new `types` or `values` to the global namespace

### Comments

- Use JSDoc style comments for `functions`, `interfaces`, `enums`, and `classes`

### Strings

- Use "double quotes" for strings shown to the user that need to be externalized (localized)
- Use 'single quotes' otherwise
- All strings visible to the user need to be externalized using the `vs/nls` module
- Externalized strings must not use string concatenation. Use placeholders instead (`{0}`).

### UI labels
- Use title-style capitalization for command labels, buttons and menu items (each word is capitalized).
- Don't capitalize prepositions of four or fewer letters unless it's the first or last word (e.g. "in", "with", "for").

### Style

- Use arrow functions `=>` over anonymous function expressions
- Only surround arrow function parameters when necessary. For example, `(x) => x + x` is wrong but the following are correct:

```typescript
x => x + x
(x, y) => x + y
<T>(x: T, y: T) => x === y
```

- Always surround loop and conditional bodies with curly braces
- Open curly braces always go on the same line as whatever necessitates them
- Parenthesized constructs should have no surrounding whitespace. A single space follows commas, colons, and semicolons in those constructs. For example:

```typescript
for (let i = 0, n = str.length; i < 10; i++) {
    if (x < 10) {
        foo();
    }
}
function f(x: number, y: string): void { }
```

- Whenever possible, use in top-level scopes `export function x(…) {…}` instead of `export const x = (…) => {…}`. One advantage of using the `function` keyword is that the stack-trace shows a good name when debugging.

### Code Quality

- All files must include Microsoft copyright header
- Prefer `async` and `await` over `Promise` and `then` calls
- All user facing messages must be localized using the applicable localization framework (for example `nls.localize()` method)
- Don't add tests to the wrong test suite (e.g., adding to end of file instead of inside relevant suite)
- Look for existing test patterns before creating new structures
- Use `describe` and `test` consistently with existing patterns
- Prefer regex capture groups with names over numbered capture groups.
- If you create any temporary new files, scripts, or helper files for iteration, clean up these files by removing them at the end of the task
- Never duplicate imports. Always reuse existing imports if they are present.
- Do not use `any` or `unknown` as the type for variables, parameters, or return values unless absolutely necessary. If they need type annotations, they should have proper types or interfaces defined.
- When adding file watching, prefer correlated file watchers (via fileService.createWatcher) to shared ones.
- When adding tooltips to UI elements, prefer the use of IHoverService service.

codex
The root instructions are light and defer to the VS Code coding rules. I’m checking for any more specific `AGENTS.md` files under the engine and extension roots before treating the named source files as authoritative.
exec
/bin/zsh -lc 'find quantbook-engine extensions/quantlab -name AGENTS.md -print' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
2026-05-22T16:22:59.911579Z ERROR codex_core::tools::router: error=write_stdin failed: stdin is closed for this session; rerun exec_command with tty=true to keep stdin open
exec
/bin/zsh -lc "pkill -f 'find quantbook-engine extensions/quantlab -name AGENTS.md -print' || true" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
sysmon request failed with error: sysmond service not found
pkill: Cannot get process list

 succeeded in 21815ms:
exec
/bin/zsh -lc 'rg --files -g AGENTS.md quantbook-engine extensions/quantlab' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 exited 1 in 0ms:
exec
/bin/zsh -lc 'ls -ld /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantbook-engine /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/extensions/quantlab' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 exited 1 in 0ms:
ls: /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantbook-engine: No such file or directory
drwxr-xr-x@ 14 sanzhar  staff  448 May 11 17:42 /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/extensions/quantlab
drwxr-xr-x@ 18 sanzhar  staff  576 May 22 14:20 /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine
drwxr-xr-x  21 sanzhar  staff  672 May 22 01:08 /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab

exec
/bin/zsh -lc 'git -C quantbook-engine rev-parse --show-toplevel && git -C quantbook-engine rev-parse HEAD && git -C quantbook-engine status --short' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
exec
/bin/zsh -lc 'git -C /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab rev-parse --show-toplevel && git -C /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab rev-parse HEAD && git -C /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab status --short' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 193ms:
?? ../.codex-phase-5-5-v2-v3-step-3-audit-prompt.md
?? ../.codex-phase-5-5-v2-v3-step-3-audit.out
?? ../.codex-phase-5-5-v2-v3-step-4-audit-prompt.md
?? ../.codex-phase-5-5-v2-v3-step-4-audit.out
?? ../.codex-phase-5-5-v2-v3-step-5-megaudit-prompt.md
?? ../.codex-phase-5-5-v2-v3-step-5-megaudit.out
?? ../.codex-phase-5-5-v2-v4-v1-step-1-audit-prompt.md
?? ../.codex-phase-5-5-v2-v4-v1-step-1-audit.out
?? ../.codex-phase-5-5-v2-v4-v1-step-2-audit-prompt.md
?? ../.codex-phase-5-5-v2-v4-v1-step-2-audit.out
?? ../.codex-phase-5-5-v2-v4-v1-step-3-audit-prompt.md
?? ../.codex-phase-5-5-v2-v4-v1-step-3-audit.out
?? ../.codex-phase-5-5-v2-v4-v1-step-4-audit-prompt.md
?? ../.codex-phase-5-5-v2-v4-v1-step-4-audit.out
?? ../.codex-phase-5-5-v2-v4-v1-step-5-audit-prompt.md
?? ../.codex-phase-5-5-v2-v4-v1-step-5-audit.out
?? ../.codex-phase-5-7-v1-audit-prompt.md
?? ../.codex-phase-5-7-v1-audit.out
?? ../.codex-phase-5-7-v1-megaudit-prompt.md
?? ../.codex-phase-5-7-v1-megaudit.out
?? ../.codex-phase-5-7-v2-1-audit-prompt.md
?? ../.codex-phase-5-7-v2-1-audit.out
?? ../.codex-phase-5-7-v2-2-audit-prompt.md
?? ../.codex-phase-5-7-v2-2-audit.out
?? ../.codex-phase-5-7-v2-3-audit-prompt.md
?? ../.codex-phase-5-7-v2-3-audit.out
?? ../.codex-phase-5-7-v2-4-audit-prompt.md
?? ../.codex-phase-5-7-v2-4-audit.out
?? ../.codex-phase-5-7-v2-5-audit-prompt.md
?? ../.codex-phase-5-7-v2-5-audit.out
?? ../.codex-phase-5-7-v2-7-audit-prompt.md
?? ../.codex-phase-5-7-v2-7-audit.out
?? ../.codex-v2-5-plan-review-prompt.md
?? ../.codex-v2-5-plan-review.out
?? ../2026-05-11-222312-this-session-is-being-continued-from-a-previous-c.txt
?? ../2026-05-13-003338-this-session-is-being-continued-from-a-previous-c.txt
?? ../node_modules
?? .codex/prompts/2026-05-14-AA-design-review.md
?? .codex/prompts/2026-05-14-J-127-verify.md
?? .codex/prompts/2026-05-14-J-128-verify.md
?? .codex/prompts/2026-05-14-J-129-verify.md
?? .codex/prompts/2026-05-14-J-megaudit-132-133followup-verify.md
?? .codex/prompts/2026-05-14-J-megaudit-HIGH3-verify.md
?? .codex/prompts/2026-05-14-J-megaudit-LOW-verify.md
?? .codex/prompts/2026-05-14-J-megaudit-MEDIUM4-verify.md
?? .codex/prompts/2026-05-14-J-megaudit-MEDIUM5-verify.md
?? .codex/prompts/2026-05-14-K-verify.md
?? .codex/prompts/2026-05-14-L-verify.md
?? .codex/prompts/2026-05-14-M-verify.md
?? .codex/prompts/2026-05-14-N-verify.md
?? .codex/prompts/2026-05-14-O-closure-verify.md
?? .codex/prompts/2026-05-14-O-megaudit.md
?? .codex/prompts/2026-05-14-phase-4.7.I-review.md
?? .codex/prompts/2026-05-14-phase-4.7.J-megaudit-pass2.md
?? .codex/prompts/2026-05-14-phase-4.7.J-review.md
?? .codex/prompts/2026-05-15-phase-4.8-closing-megaudit.md
?? .codex/prompts/2026-05-15-phase-4.8-closure-verify.md
?? .codex/prompts/2026-05-15-phase-4.8-final-reverify.md
?? .codex/prompts/2026-05-15-phase-4.9-design-review.md
?? .codex/prompts/2026-05-15-phase-4.9-mid-arc-audit.md
?? .plans/_archive/2026-05-13_w5-77-phase-4.5d-format-parser.md
?? .plans/_archive/2026-05-13_w5-78-phase-4.5d-renderer.md
?? .plans/_archive/2026-05-14_w5-121-phase-4.8.i.2-rename-column.md
?? .plans/_archive/2026-05-14_w5-122-phase-4.8.j-resize-table.md
?? .plans/_archive/2026-05-14_w5-123-phase-4.8.l-persistence-v6.md
?? .plans/_archive/2026-05-15_phase-4.8.O-findings.md
?? .plans/_archive/2026-05-17_phase-4-10-v1-260-closeout.md
?? .plans/_archive/2026-05-17_rt-v1-01-reference-tier-mini-phase.md
?? .plans/_archive/2026-05-18_phase-4-11-xlsx-import-export.md
?? .plans/_archive/2026-05-18_tier-a-api-stability.md
?? .plans/_archive/2026-05-21_phase-5-5-v2-v2-auto-flush.md
?? .plans/_archive/2026-05-21_phase-5-5-v2-v3-step-1-version-vector.md
?? .plans/_archive/2026-05-21_phase-5-5-v2-v3-step-2-poll-remote-auto-flush.md
?? .plans/_archive/2026-05-21_phase-5-5-v2-v3-step-3-offline-write-queue.md
?? .plans/_archive/2026-05-21_phase-5-5-v2-v3-step-4-websocket-transport.md
?? .plans/_archive/2026-05-21_phase-5-5-v2-v3-step-5-full-arc-megaudit.md
?? .plans/_archive/2026-05-21_phase-5-5-v2-v3-step-6-exit-packet.md
?? .plans/_archive/2026-05-21_phase-5-5-v2-v4-v1-step-1-ack-channel.md
?? .plans/_archive/2026-05-21_phase-5-5-v2-v4-v1-step-2-pending-op-count.md
?? .plans/_archive/2026-05-21_phase-5-5-v2-v4-v1-step-3-tier-j-bundle.md
?? .plans/_archive/2026-05-21_phase-5-5-v2-v4-v1-step-4-small-bundle.md
?? .plans/_archive/2026-05-21_phase-5-5-v2-v4-v1-step-5-discard-pending-ops.md
?? .plans/_archive/2026-05-22_phase-5-7-v1-vertical-slice.md
?? 2026-05-14-222539-this-session-is-being-continued-from-a-previous-c.txt
?? 2026-05-16-175547-this-session-is-being-continued-from-a-previous-c.txt

 succeeded in 210ms:
?? quantbook-engine/

exec
/bin/zsh -lc "sed -n '1,220p' /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/AGENTS.md" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
# VS Code Agents Instructions

This file provides instructions for AI coding agents working with the VS Code codebase.

For detailed project overview, architecture, coding guidelines, and validation steps, see the [Copilot Instructions](.github/copilot-instructions.md).

exec
/bin/zsh -lc "sed -n '1,260p' /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/.github/copilot-instructions.md" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
# VS Code Copilot Instructions

## Project Overview

Visual Studio Code is built with a layered architecture using TypeScript, web APIs and Electron, combining web technologies with native app capabilities. The codebase is organized into key architectural layers:

### Root Folders
- `src/`: Main TypeScript source code with unit tests in `src/vs/*/test/` folders
- `build/`: Build scripts and CI/CD tools
- `extensions/`: Built-in extensions that ship with VS Code
- `test/`: Integration tests and test infrastructure
- `scripts/`: Development and build scripts
- `resources/`: Static resources (icons, themes, etc.)
- `out/`: Compiled JavaScript output (generated during build)

### Core Architecture (`src/` folder)
- `src/vs/base/` - Foundation utilities and cross-platform abstractions
- `src/vs/platform/` - Platform services and dependency injection infrastructure
- `src/vs/editor/` - Text editor implementation with language services, syntax highlighting, and editing features
- `src/vs/workbench/` - Main application workbench for web and desktop
  - `workbench/browser/` - Core workbench UI components (parts, layout, actions)
  - `workbench/services/` - Service implementations
  - `workbench/contrib/` - Feature contributions (git, debug, search, terminal, etc.)
  - `workbench/api/` - Extension host and VS Code API implementation
- `src/vs/code/` - Electron main process specific implementation
- `src/vs/server/` - Server specific implementation

The core architecture follows these principles:
- **Layered architecture** - from `base`, `platform`, `editor`, to `workbench`
- **Dependency injection** - Services are injected through constructor parameters
- **Contribution model** - Features contribute to registries and extension points
- **Cross-platform compatibility** - Abstractions separate platform-specific code

### Built-in Extensions (`extensions/` folder)
The `extensions/` directory contains first-party extensions that ship with VS Code:
- **Language support** - `typescript-language-features/`, `html-language-features/`, `css-language-features/`, etc.
- **Core features** - `git/`, `debug-auto-launch/`, `emmet/`, `markdown-language-features/`
- **Themes** - `theme-*` folders for default color themes
- **Development tools** - `extension-editing/`, `vscode-api-tests/`

Each extension follows the standard VS Code extension structure with `package.json`, TypeScript sources, and contribution points to extend the workbench through the Extension API.

### Finding Related Code
1. **Semantic search first**: Use file search for general concepts
2. **Grep for exact strings**: Use grep for error messages or specific function names
3. **Follow imports**: Check what files import the problematic module
4. **Check test files**: Often reveal usage patterns and expected behavior

## Validating TypeScript changes

MANDATORY: Always check the `VS Code - Build` watch task output via #runTasks/getTaskOutput for compilation errors before running ANY script or declaring work complete, then fix all compilation errors before moving forward.

- NEVER run tests if there are compilation errors
- NEVER use `npm run compile` to compile TypeScript files but call #runTasks/getTaskOutput instead

### TypeScript compilation steps
- Monitor the `VS Code - Build` task outputs for real-time compilation errors as you make changes
- This task runs `Core - Build` and `Ext - Build` to incrementally compile VS Code TypeScript sources and built-in extensions
- Start the task if it's not already running in the background

### TypeScript validation steps
- Use the run test tool if you need to run tests. If that tool is not available, then you can use `scripts/test.sh` (or `scripts\test.bat` on Windows) for unit tests (add `--grep <pattern>` to filter tests) or `scripts/test-integration.sh` (or `scripts\test-integration.bat` on Windows) for integration tests (integration tests end with .integrationTest.ts or are in /extensions/).
- Use `npm run valid-layers-check` to check for layering issues

## Coding Guidelines

### Indentation

We use tabs, not spaces.

### Naming Conventions

- Use PascalCase for `type` names
- Use PascalCase for `enum` values
- Use camelCase for `function` and `method` names
- Use camelCase for `property` names and `local variables`
- Use whole words in names when possible

### Types

- Do not export `types` or `functions` unless you need to share it across multiple components
- Do not introduce new `types` or `values` to the global namespace

### Comments

- Use JSDoc style comments for `functions`, `interfaces`, `enums`, and `classes`

### Strings

- Use "double quotes" for strings shown to the user that need to be externalized (localized)
- Use 'single quotes' otherwise
- All strings visible to the user need to be externalized using the `vs/nls` module
- Externalized strings must not use string concatenation. Use placeholders instead (`{0}`).

### UI labels
- Use title-style capitalization for command labels, buttons and menu items (each word is capitalized).
- Don't capitalize prepositions of four or fewer letters unless it's the first or last word (e.g. "in", "with", "for").

### Style

- Use arrow functions `=>` over anonymous function expressions
- Only surround arrow function parameters when necessary. For example, `(x) => x + x` is wrong but the following are correct:

```typescript
x => x + x
(x, y) => x + y
<T>(x: T, y: T) => x === y
```

- Always surround loop and conditional bodies with curly braces
- Open curly braces always go on the same line as whatever necessitates them
- Parenthesized constructs should have no surrounding whitespace. A single space follows commas, colons, and semicolons in those constructs. For example:

```typescript
for (let i = 0, n = str.length; i < 10; i++) {
    if (x < 10) {
        foo();
    }
}
function f(x: number, y: string): void { }
```

- Whenever possible, use in top-level scopes `export function x(…) {…}` instead of `export const x = (…) => {…}`. One advantage of using the `function` keyword is that the stack-trace shows a good name when debugging.

### Code Quality

- All files must include Microsoft copyright header
- Prefer `async` and `await` over `Promise` and `then` calls
- All user facing messages must be localized using the applicable localization framework (for example `nls.localize()` method)
- Don't add tests to the wrong test suite (e.g., adding to end of file instead of inside relevant suite)
- Look for existing test patterns before creating new structures
- Use `describe` and `test` consistently with existing patterns
- Prefer regex capture groups with names over numbered capture groups.
- If you create any temporary new files, scripts, or helper files for iteration, clean up these files by removing them at the end of the task
- Never duplicate imports. Always reuse existing imports if they are present.
- Do not use `any` or `unknown` as the type for variables, parameters, or return values unless absolutely necessary. If they need type annotations, they should have proper types or interfaces defined.
- When adding file watching, prefer correlated file watchers (via fileService.createWatcher) to shared ones.
- When adding tooltips to UI elements, prefer the use of IHoverService service.

exec
/bin/zsh -lc 'rg --files -g AGENTS.md /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab | head -20' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/AGENTS.md

codex
The engine root at `quantbook-engine/` is on the requested engine commit, and the IDE root is the sibling VS Code checkout on the requested IDE commit. I’ll use those two exact roots for citations.
exec
/bin/zsh -lc "nl -ba quantbook-engine/crates/ql-collab/src/transport.rs | sed -n '1,240p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
exec
/bin/zsh -lc "nl -ba quantbook-engine/crates/ql-collab-ws/src/lib.rs | sed -n '1,260p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
     1	//! `Transport` trait — wire-byte channel for Phase 5 collaboration.
     2	//!
     3	//! **Phase 5.2.a (2026-05-19, scaffold):** trait shape defined.
     4	//! **Phase 5.5 V1 (2026-05-19, `924750819bc`):** `LoopbackTransport`
     5	//! shipped — in-process paired endpoints for 2-peer tests.
     6	//! **Phase 5.5 V2 V1 (2026-05-19, `ffd8f6e5f05` + audit closure):**
     7	//! `CollabSession` exposes 5 typed transport methods
     8	//! (`attach_transport` / `detach_transport` / `has_transport` /
     9	//! `flush_to_transport` / `poll_remote` (+ `poll_remote_with_limit`)).
    10	//! V2 V1 is "explicit drive" — caller invokes flush + poll on a
    11	//! tick.
    12	//! **Phase 5.5 V2 V2 (2026-05-21):** `AutoFlushPolicy` enum on
    13	//! `CollabSession`. Opt-in `OnAppend` invokes flush automatically
    14	//! after every mutator. Default is `Disabled` (V2 V1 behavior
    15	//! preserved). See [`crate::AutoFlushPolicy`] +
    16	//! [`crate::CollabSession::set_auto_flush_policy`].
    17	//! **Phase 5.5 V2 V3 step 1 (2026-05-21):** version-vector tracking
    18	//! for the **currently attached** transport baseline + delta flushes.
    19	//! `CollabSession::flush_delta_to_transport` sends only the delta
    20	//! since the last successful flush via `LoroDoc::ExportMode::Updates`.
    21	//! Auto-flush routes through this delta path — wire payload is
    22	//! O(per-op delta) instead of O(full state). Idempotency
    23	//! short-circuit (no state change → no send) closes the V2 V2
    24	//! echo-loop concern. The checkpoint is
    25	//! per-session-currently-attached-transport, NOT per-transport-
    26	//! identity (`attach_transport` resets the baseline).
    27	//! **Phase 5.5 V2 V3 step 2 (2026-05-21):** wires `poll_remote*`
    28	//! into auto-flush. After a successful drain (`merged > 0`), one
    29	//! auto-flush fires per call (not per-blob — bandwidth-efficient).
    30	//! V2 V3 step 1's idempotency guard prevents echo loops: drained
    31	//! bytes that Loro dedupes leave the VV unchanged → flush
    32	//! short-circuits to `Ok(false)`. 3-peer hub-fanout pattern now
    33	//! works automatically under `OnAppend`.
    34	//! **Phase 5.5 V2 V3 step 3 (2026-05-21):** offline-write story.
    35	//! Investigation showed no explicit queue is needed: Loro's CRDT op
    36	//! log IS the implicit offline queue. Append while no transport
    37	//! attached → `maybe_auto_flush` no-ops; op committed locally. On
    38	//! reattach: `attach_transport` resets `last_flushed_vv = None`; the
    39	//! next mutator (or explicit `flush_delta_to_transport`) sends from
    40	//! empty VV — delivers ALL accumulated ops including offline ones.
    41	//! Adds `has_pending_flush() -> bool` ergonomic helper (compares
    42	//! current VV vs last-flushed VV). 7 new integration tests pin the
    43	//! offline-write contract.
    44	//!
    45	//! **Phase 5.5 V2 V3 step 4 (2026-05-21, this ship):** first
    46	//! production-grade `Transport` impl ships as a separate crate,
    47	//! `ql-collab-ws::WebSocketTransport`. Bridges async
    48	//! tokio-tungstenite to the sync `Transport` trait via
    49	//! `tokio::sync::mpsc` channels and two spawned background tasks
    50	//! (reader and writer). Kept in a sibling crate so `ql-collab`
    51	//! core stays runtime-agnostic; embedders needing only
    52	//! `LoopbackTransport` or a custom impl don't pay for tokio. MVP
    53	//! scope: client-only, plain `ws://`, NO TLS, NO auto-reconnect
    54	//! (caller drives via detach and re-attach; V2 V3 step 1
    55	//! baseline-reset contract delivers offline ops on reconnect).
    56	//! 13 integration tests including 3 that verify the V2 V2 and
    57	//! V2 V3 step 1-3 contracts hold over a real WebSocket. See
    58	//! `ql-collab-ws` module docs for V1 limitations deferred to V2 V4
    59	//! (TLS, auto-reconnect, bounded queue, server side, inbound
    60	//! text/ping/pong frame dropping).
    61	//!
    62	//! **Phase 5.5 V2 V3 step 5 megaudit (2026-05-21, `bce64a5c1ce`):**
    63	//! 3-way Codex+Opus-A+Opus-B. Lifted `last_error()` to the trait
    64	//! (see method below), added `TaskExitGuard` RAII panic detection +
    65	//! Close-frame reason capture + poisoned-mutex no-fallback fix.
    66	//!
    67	//! **Phase 5.5 V2 V3 step 6 exit packet (2026-05-21, `8a8236840f2`):**
    68	//! V1 exit packet at `docs/phase5/v2-v3-exit-packet.md` + consumer
    69	//! doc rewrite at `docs/architecture/ide-consumer-contract.md`
    70	//! § 4.1.1-3 (3 worked-example subsections).
    71	//!
    72	//! **Phase 5.5 V2 V4 V1 (2026-05-21):** 12/13 Tier items shipped —
    73	//! ack channel (`flush_pending`), `pending_op_count`,
    74	//! `discard_pending_ops`, defensive hardening. K4 chunking deferred
    75	//! to V2 V4 V2. See `docs/phase5/v2-v4-v1-exit-packet.md`.
    76	//!
    77	//! **Phase 5.7 V1 (2026-05-22):** FIRST IDE binding via napi-rs.
    78	//! `crates/ql-bindings-node/` exposes `CollabSession` (no Transport
    79	//! yet — V2). See `docs/phase5/5-7-v1-exit-packet.md`.
    80	//!
    81	//! Tests can also use [`NoopTransport`] which discards traffic.
    82	//!
    83	//! The contract is intentionally minimal:
    84	//!
    85	//! - `send(&mut self, bytes: &[u8])` — push bytes to the channel.
    86	//!   Bytes are an opaque blob (a Loro export). The transport
    87	//!   doesn't interpret them.
    88	//! - `try_recv(&mut self) -> Option<Vec<u8>>` — non-blocking
    89	//!   receive. Returns `None` if no bytes are queued.
    90	//!
    91	//! That's enough to plumb a `CollabSession::export_bytes` →
    92	//! transport → remote `CollabSession::merge_bytes` flow. The
    93	//! transport handles framing / reliability / reconnect / auth as
    94	//! its implementation details.
    95	
    96	use std::collections::VecDeque;
    97	use std::sync::atomic::{AtomicBool, Ordering};
    98	use std::sync::{Arc, Mutex};
    99	
   100	use thiserror::Error;
   101	
   102	/// Errors emitted by `Transport` impls.
   103	#[derive(Debug, Error)]
   104	#[non_exhaustive]
   105	pub enum TransportError {
   106	    /// The underlying I/O failed (socket dead, network partition,
   107	    /// etc.). Implementations should distinguish recoverable
   108	    /// (reconnect) from permanent (auth rejected) failures via
   109	    /// variant structure in their own error type, then map to this
   110	    /// variant at the trait boundary if they don't want to expose
   111	    /// implementation details.
   112	    #[error("transport I/O error: {0}")]
   113	    Io(String),
   114	
   115	    /// The transport is closed and no further sends are accepted.
   116	    /// Receivers should still drain any already-queued bytes via
   117	    /// `try_recv` before considering the channel done.
   118	    #[error("transport closed")]
   119	    Closed,
   120	}
   121	
   122	impl TransportError {
   123	    /// **Phase 5.7 V2.7 (2026-05-22) — error-code discrimination.**
   124	    ///
   125	    /// Returns a stable `&'static str` identifier for this variant.
   126	    /// Intended for the napi binding to prepend `[<kind>]` to JS
   127	    /// error messages so IDE-side reconnect logic can branch on
   128	    /// `error.code` (after parsing via `parseQuantbookError`)
   129	    /// without substring-matching the human-readable `Display`
   130	    /// text.
   131	    ///
   132	    /// **Stability**: variant kinds are SemVer-stable strings.
   133	    /// Adding a new variant requires adding its kind here. Removing
   134	    /// or renaming a kind is a breaking change for IDE consumers
   135	    /// (their `code === 'transport_closed'` branches stop matching).
   136	    /// Closes V2.1+V2.2+V2.3 Opus MEDIUM-3 carryforwards (lossy
   137	    /// `Display` projection of the underlying enum discriminant).
   138	    pub fn kind(&self) -> &'static str {
   139	        match self {
   140	            TransportError::Io(_) => "transport_io",
   141	            TransportError::Closed => "transport_closed",
   142	        }
   143	    }
   144	}
   145	
   146	/// Wire-byte channel for Phase 5 collaboration.
   147	///
   148	/// Implementations push and pull opaque byte blobs. The blobs ARE
   149	/// Loro `LoroDoc::export(...)` snapshots / updates; the
   150	/// transport doesn't interpret them. Framing (where one blob ends
   151	/// and the next starts) is the implementation's responsibility.
   152	pub trait Transport {
   153	    /// Push `bytes` to the channel. Implementations may buffer
   154	    /// internally; the call returns when the bytes are
   155	    /// **queued for send**, not when the remote has received them.
   156	    ///
   157	    /// **V2 V3 step 5 megaudit closure (Codex M1 + Opus-B M1,
   158	    /// 2026-05-21):** for buffered impls (`WebSocketTransport` and
   159	    /// future async transports), there is a window between "send
   160	    /// returned Ok" and "bytes on the wire" where the bytes can be
   161	    /// silently lost (transport dropped, peer disconnect mid-flush,
   162	    /// allocator failure during the writer task). The session's
   163	    /// [`crate::CollabSession::flush_delta_to_transport`] advances
   164	    /// `last_flushed_vv` immediately on `send`'s Ok return, so
   165	    /// `has_pending_flush() == false` alone means "queued to the
   166	    /// currently-attached transport," NOT "the peer has received the
   167	    /// ops." **V2 V4 V1 step 1 (2026-05-21) Tier K1 closure**: call
   168	    /// [`Transport::flush_pending`] after `flush_delta_to_transport`
   169	    /// to block until the writer task has completed `ws_sink.send`
   170	    /// for every queued blob — that provides level-1 (local writer)
   171	    /// ack. For stronger guarantees (peer-application ack, TCP-level
   172	    /// ack), keep the transport attached until you've verified peer
   173	    /// reception out-of-band (e.g., via a custom ack-op layered on
   174	    /// `merge_bytes`). On transport drop or `Err(Closed)`, detach +
   175	    /// reattach a new transport — the V2 V3 step 1 baseline-reset
   176	    /// contract re-sends from empty VV.
   177	    ///
   178	    /// On error, the caller should treat the byte blob as unsent
   179	    /// and re-queue (or surface the error to the user). The
   180	    /// transport will not retry automatically — that policy lives
   181	    /// at the layer above (Phase 5.5 design will pick reconnect
   182	    /// semantics).
   183	    fn send(&mut self, bytes: &[u8]) -> Result<(), TransportError>;
   184	
   185	    /// Non-blocking receive. Returns `Some(bytes)` if a blob is
   186	    /// queued, `None` otherwise. Implementations choose how often
   187	    /// they poll the underlying channel; the contract is "if you
   188	    /// have something, give it to me, else `None`."
   189	    ///
   190	    /// Returns `Err(TransportError::Closed)` only when the channel
   191	    /// is permanently closed AND its internal queue is empty. A
   192	    /// transient I/O failure during poll surfaces as
   193	    /// `Err(TransportError::Io)`.
   194	    fn try_recv(&mut self) -> Result<Option<Vec<u8>>, TransportError>;
   195	
   196	    /// Read the most recent transport-internal error, if any. Default
   197	    /// returns `None` for impls without a runtime-error concept
   198	    /// (`NoopTransport`, `LoopbackTransport`). Buffered async impls
   199	    /// (`WebSocketTransport`) override to expose the underlying cause
   200	    /// of the most recent task-observed failure.
   201	    ///
   202	    /// **V2 V3 step 5 megaudit closure (Opus-A H1, 2026-05-21):** the
   203	    /// V2 V3 step 4 closure added `WebSocketTransport::last_error()`
   204	    /// on the concrete type for IDE consumers driving reconnect
   205	    /// handshakes — but `CollabSession::attach_transport` moves the
   206	    /// concrete type into `Box<dyn Transport + Send>`, making it
   207	    /// unreachable. Lifting the accessor to the trait + adding
   208	    /// [`crate::CollabSession::transport_last_error`] proxy is the
   209	    /// minimum-viable fix: IDE callers can now distinguish "peer
   210	    /// reset" from "auth rejected" from "capacity exceeded" without
   211	    /// downcasting or holding a parallel handle.
   212	    ///
   213	    /// Returns `Option<String>` (lossy) rather than a structured
   214	    /// error type to keep the trait minimal and avoid leaking
   215	    /// impl-specific types (`WebSocketError`, etc.). Consumers
   216	    /// pattern-match on substring or just display the message.
   217	    ///
   218	    /// Read AFTER observing [`TransportError::Closed`] from `send`
   219	    /// or `try_recv`. Returns `None` for clean shutdowns (caller
   220	    /// `close()`, graceful peer Close frame, transport never failed).
   221	    fn last_error(&self) -> Option<String> {
   222	        None
   223	    }
   224	
   225	    /// Block until every byte blob previously queued via `send` has
   226	    /// been written to the underlying transport's wire (e.g. WebSocket
   227	    /// sink). Default `Ok(())` for synchronous transports
   228	    /// (`LoopbackTransport`, `NoopTransport`) where `send` is already
   229	    /// wire-delivery — there is no buffer to drain.
   230	    ///
   231	    /// **Phase 5.5 V2 V4 V1 step 1 (2026-05-21) — Tier K1 ack channel.**
   232	    /// Closes the V2 V3 step 5 megaudit's convergent finding (Codex M1
   233	    /// and Opus-B M1): buffered async transports
   234	    /// (`WebSocketTransport`) return `Ok` from `send` once bytes are
   235	    /// queued in an mpsc channel, NOT when bytes reach the wire.
   236	    /// Without `flush_pending`, `CollabSession::has_pending_flush()
   237	    /// == false` would mean "queued to currently-attached transport,"
   238	    /// not "delivered." For IDE consumers building "safe to close
   239	    /// window?" workflows, this is a real UX hole. `flush_pending`
   240	    /// lets callers block until the local writer has caught up,

 succeeded in 0ms:
     1	//! `ql-collab-ws` — WebSocket `Transport` impl for Phase 5
     2	//! collaboration.
     3	//!
     4	//! **Phase 5.5 V2 V3 step 4 (2026-05-21, this ship):** first
     5	//! production-grade [`Transport`](ql_collab::Transport) impl. Bridges
     6	//! tokio-tungstenite's async WebSocket client to the sync `Transport`
     7	//! trait via `tokio::sync::mpsc` channels and two spawned background
     8	//! tasks.
     9	//!
    10	//! ## Crate boundary rationale
    11	//!
    12	//! `ql-collab` core is purely sync and embeddable in non-tokio
    13	//! runtimes (e.g., an IDE main thread, future WASM target). Pulling
    14	//! tokio + tokio-tungstenite into the core would force that
    15	//! constraint on every consumer. Keeping the WebSocket impl in a
    16	//! separate crate means:
    17	//! - Consumers who only need `LoopbackTransport` (tests) or a custom
    18	//!   transport (in-process IPC, gRPC streaming) don't pay for tokio.
    19	//! - The async runtime lives at the application boundary, not in the
    20	//!   collaboration layer.
    21	//!
    22	//! ## Architecture
    23	//!
    24	//! ```text
    25	//!  caller (sync, e.g. inside CollabSession::append_op)
    26	//!    │
    27	//!    │  send(bytes)        try_recv() -> Option<bytes>
    28	//!    ▼                            ▲
    29	//!  WebSocketTransport             │
    30	//!    │ outbound_tx              inbound_rx
    31	//!    │  (mpsc::UnboundedSender)  (mpsc::UnboundedReceiver)
    32	//!    ▼                            ▲
    33	//!  writer_task (tokio)          reader_task (tokio)
    34	//!    │  awaits outbound_rx        │  awaits ws_stream.next()
    35	//!    ▼                            ▲
    36	//!    │  ws_sink.send(Binary)      │  inbound_tx.send(bytes)
    37	//!    ▼                            │
    38	//!  ===================== TCP socket =====================
    39	//! ```
    40	//!
    41	//! The two background tasks own the WebSocket halves
    42	//! (`SplitSink` + `SplitStream` via `futures_util::StreamExt::split`).
    43	//! `Drop` aborts both tasks (RAII close).
    44	//!
    45	//! ## V1 limitations (deferred to V2 V4)
    46	//!
    47	//! - **No TLS (`ws://` only).** Use a TLS-terminating reverse proxy
    48	//!   in front, or wait for V2 V4 `rustls` feature.
    49	//! - **No auto-reconnect.** Caller drives via `detach_transport` +
    50	//!   new `connect` + `attach_transport`. The Phase 5.5 V2 V3 step 1
    51	//!   `last_flushed_vv = None` reset on attach delivers all accumulated
    52	//!   ops on reconnect (offline-write story).
    53	//! - **Unbounded outbound mpsc queue.** Memory growth is bounded in
    54	//!   practice for the common disconnect case: when the peer closes
    55	//!   the TCP socket, the reader task observes EOF / `Err` and sets
    56	//!   the closed flag; subsequent `send` calls fast-path to
    57	//!   `Err(Closed)` before reaching the unbounded mpsc. The genuine
    58	//!   growth window is the **dead-peer-but-not-closed** scenario
    59	//!   (server stops draining but doesn't tear down the TCP socket):
    60	//!   `ws_sink.send` stalls on TCP backpressure, `outbound_rx` grows
    61	//!   unboundedly, and the reader task may still be receiving heartbeat
    62	//!   frames so `closed` stays false. V2 V4 will switch to bounded
    63	//!   with caller-configurable backpressure policy (clarified in
    64	//!   V2 V3 step 4 audit closure, Opus M5).
    65	//! - **Client-only.** Server-side WebSocket impls use other libraries
    66	//!   (`axum-tungstenite`, `warp::ws`, etc.).
    67	//! - **Drops text/ping/pong/close frames as inbound data.** Loro
    68	//!   payloads are binary blobs; non-binary frames don't carry our
    69	//!   protocol. Close frames trigger reader task exit. Inbound Ping
    70	//!   frames: tokio-tungstenite queues a Pong response on the sink
    71	//!   for the next write — but with no app traffic the Pong is not
    72	//!   flushed until the next app send (clarified in V2 V3 step 4
    73	//!   audit closure, Opus L3). Server-side idle-timeout would then
    74	//!   close the connection. Text frames are silently dropped (a
    75	//!   future protocol extension could add a callback hook).
    76	//!
    77	//! ## Example
    78	//!
    79	//! ```ignore
    80	//! use ql_collab::{CollabSession, PeerId, AutoFlushPolicy};
    81	//! use ql_collab_ws::WebSocketTransport;
    82	//!
    83	//! let rt = tokio::runtime::Runtime::new()?;
    84	//! let ws = rt.block_on(WebSocketTransport::connect("ws://localhost:8080"))?;
    85	//! let mut session = CollabSession::new(PeerId::new(1))?;
    86	//! session.attach_transport(ws);
    87	//! session.set_auto_flush_policy(AutoFlushPolicy::OnAppend);
    88	//! // ... appends now propagate via WebSocket automatically ...
    89	//! ```
    90	
    91	use std::sync::atomic::{AtomicBool, AtomicU64, Ordering};
    92	use std::sync::{Arc, Condvar, Mutex};
    93	use std::time::Duration;
    94	
    95	use futures_util::{SinkExt, StreamExt};
    96	use ql_collab::{FlushAck, Transport, TransportError};
    97	use thiserror::Error;
    98	use tokio::sync::mpsc;
    99	use tokio::task::JoinHandle;
   100	use tokio_tungstenite::tungstenite::Message;
   101	
   102	/// **V2 V3 step 5 megaudit closure (Opus-B M2, 2026-05-21):**
   103	/// panic-safe task-exit guard. Held by the writer/reader tasks; its
   104	/// `Drop` impl runs even on panic-unwinding. If the task exits
   105	/// without calling [`Self::mark_clean_exit`], the guard treats it as
   106	/// an unexpected exit (panic) and sets `closed = true` + records a
   107	/// `RuntimeError` describing the panic.
   108	///
   109	/// Without this guard, a panic in `ws_sink.send`, `bytes.into()`, or
   110	/// any future inner code would leave the transport in `closed=false`
   111	/// with the task gone — callers would poll `try_recv` forever
   112	/// returning `Ok(None)` and `send` would queue bytes into an
   113	/// orphaned mpsc.
   114	struct TaskExitGuard {
   115	    task_name: &'static str,
   116	    closed: Arc<AtomicBool>,
   117	    last_error: Arc<Mutex<Option<WebSocketError>>>,
   118	    /// **V2 V4 V1 step 1 audit closure (Codex L1 / Opus M2, 2026-05-21):**
   119	    /// notify the `flush_pending` Condvar on panic exit so blocked
   120	    /// callers wake up immediately rather than waiting for the
   121	    /// `wait_timeout` tick. Same Arc as `WebSocketTransport::progress`.
   122	    progress: Arc<(Mutex<u64>, Condvar)>,
   123	    clean: bool,
   124	}
   125	
   126	impl TaskExitGuard {
   127	    fn new(
   128	        task_name: &'static str,
   129	        closed: Arc<AtomicBool>,
   130	        last_error: Arc<Mutex<Option<WebSocketError>>>,
   131	        progress: Arc<(Mutex<u64>, Condvar)>,
   132	    ) -> Self {
   133	        Self {
   134	            task_name,
   135	            closed,
   136	            last_error,
   137	            progress,
   138	            clean: false,
   139	        }
   140	    }
   141	
   142	    /// Call before the task body exits via any normal path
   143	    /// (Err arm, stream-end, clean recv-None). Skipping this
   144	    /// is the signal that the task panicked.
   145	    fn mark_clean_exit(mut self) {
   146	        self.clean = true;
   147	    }
   148	}
   149	
   150	impl Drop for TaskExitGuard {
   151	    fn drop(&mut self) {
   152	        if !self.clean {
   153	            // Panic path: stash the panic signal in last_error
   154	            // BEFORE setting closed (so a concurrent reader sees
   155	            // the error first). Per CLAUDE.md no-fallback rule,
   156	            // surface the panic loudly rather than swallowing.
   157	            record_runtime_error(
   158	                &self.last_error,
   159	                format!("ql-collab-ws {} task panicked", self.task_name),
   160	            );
   161	            self.closed.store(true, Ordering::Relaxed);
   162	            // V2 V4 V1 step 1 audit closure (Opus M2): wake any
   163	            // flush_pending waiter so they observe the closed flag
   164	            // without a 100ms timeout delay.
   165	            self.progress.1.notify_all();
   166	        }
   167	    }
   168	}
   169	
   170	/// **V2 V3 step 5 megaudit closure (Opus-B M3, 2026-05-21):** surface
   171	/// poisoned-mutex condition as a `RuntimeError` rather than the
   172	/// previous `.lock().ok()` swallow. A poisoned mutex means a holder
   173	/// panicked while populating the slot — the caller deserves to know
   174	/// the slot's most-recent error was lost.
   175	fn record_runtime_error(slot: &Arc<Mutex<Option<WebSocketError>>>, message: String) {
   176	    match slot.lock() {
   177	        Ok(mut guard) => {
   178	            *guard = Some(WebSocketError::RuntimeError(message));
   179	        }
   180	        Err(poisoned) => {
   181	            // Recover the inner mutex despite poison; overwrite with
   182	            // a message that explicitly notes the poisoning so the
   183	            // caller can detect a prior panic-during-error-stash.
   184	            let mut guard = poisoned.into_inner();
   185	            *guard = Some(WebSocketError::RuntimeError(format!(
   186	                "{message} (note: error slot mutex was poisoned by prior panic)"
   187	            )));
   188	        }
   189	    }
   190	}
   191	
   192	/// Errors emitted by [`WebSocketTransport`].
   193	///
   194	/// Distinguishes connect-time errors (`InvalidUrl`, `ConnectFailed`,
   195	/// `HandshakeFailed`) from runtime errors observed by the background
   196	/// reader/writer tasks (`RuntimeError`). Connect-time errors surface
   197	/// as the `Result::Err` from [`WebSocketTransport::connect`]; runtime
   198	/// errors are stashed on the transport and readable via
   199	/// [`WebSocketTransport::last_error`] AFTER the trait `send`/`try_recv`
   200	/// surface `TransportError::Closed`.
   201	///
   202	/// Maps tungstenite errors opaquely — we don't want to leak
   203	/// tungstenite types into the public API.
   204	///
   205	/// **V2 V3 step 4 audit closure (Opus M1, 2026-05-21):** the prior
   206	/// ship discarded background-task errors (`if let Err(_e)`), flattening
   207	/// 8 distinct tungstenite::Error variants into a uniform `Closed` at
   208	/// the trait boundary. This violated the CLAUDE.md no-fallbacks rule
   209	/// (errors must be visible) and made it impossible for IDE consumers
   210	/// driving reconnect handshakes to distinguish "connection lost" from
   211	/// "auth failed". Adds `RuntimeError(String)` variant + `last_error()`
   212	/// accessor; both tasks now populate the shared `last_error` slot on
   213	/// non-graceful exit.
   214	#[derive(Debug, Clone, Error)]
   215	#[non_exhaustive]
   216	pub enum WebSocketError {
   217	    /// The URL string failed to parse or is missing required parts.
   218	    /// Per CLAUDE.md no-fallback rule: surface clearly, don't silently
   219	    /// retry with a "fixed up" URL.
   220	    #[error("invalid WebSocket URL: {0}")]
   221	    InvalidUrl(String),
   222	
   223	    /// TCP connect failed (refused, timeout, DNS, etc.).
   224	    #[error("WebSocket connection failed: {0}")]
   225	    ConnectFailed(String),
   226	
   227	    /// WebSocket protocol handshake failed (HTTP upgrade rejected,
   228	    /// invalid response, etc.).
   229	    #[error("WebSocket handshake failed: {0}")]
   230	    HandshakeFailed(String),
   231	
   232	    /// Background reader/writer task observed a runtime failure
   233	    /// (peer reset, IO error, protocol violation, capacity exceeded,
   234	    /// etc.) AFTER a successful connect. Surfaces via
   235	    /// [`WebSocketTransport::last_error`] once the trait-level
   236	    /// `Closed` has been observed.
   237	    #[error("WebSocket runtime error: {0}")]
   238	    RuntimeError(String),
   239	}
   240	
   241	impl WebSocketError {
   242	    /// **Phase 5.7 V2.7 (2026-05-22) — error-code discrimination.**
   243	    ///
   244	    /// Returns a stable `&'static str` identifier for this variant.
   245	    /// Mirrors [`ql_collab::TransportError::kind`]'s contract;
   246	    /// closes V2.3 Opus MEDIUM-3 (lossy `Display` projection of
   247	    /// `WebSocketError` variants on the napi `Transport.websocketConnect`
   248	    /// rejection path).
   249	    ///
   250	    /// **Stability**: kinds are SemVer-stable. IDE reconnect logic
   251	    /// can branch on these strings after `parseQuantbookError`.
   252	    pub fn kind(&self) -> &'static str {
   253	        match self {
   254	            WebSocketError::InvalidUrl(_) => "websocket_invalid_url",
   255	            WebSocketError::ConnectFailed(_) => "websocket_connect_failed",
   256	            WebSocketError::HandshakeFailed(_) => "websocket_handshake_failed",
   257	            WebSocketError::RuntimeError(_) => "websocket_runtime_error",
   258	        }
   259	    }
   260	}

exec
/bin/zsh -lc "nl -ba quantbook-engine/crates/ql-collab/src/session.rs | sed -n '1,260p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
     1	//! `CollabSession` — per-peer collaboration state holder.
     2	//!
     3	//! **Phase 5.2.a (2026-05-19, scaffold ship):** wraps `ql_oplog::OpLog`
     4	//! with peer-id-aware operations + transport plumbing. Subsequent
     5	//! phases populate richer behavior:
     6	//!
     7	//! - ~~**Phase 5.4**~~ ✅ V1 + V2 V1 + V2 V1.1 shipped — V1
     8	//!   `89c02b9d83e`: 7 undo/redo methods. V2 V1 `6138a7203f6` +
     9	//!   `e199a5fda5a`: `start_undo_group` / `end_undo_group` +
    10	//!   `set_undo_merge_interval`. V2 V1.1: `start_undo_group_scoped`
    11	//!   returns RAII `UndoGroupGuard` for panic/Err-safe grouping.
    12	//!   Presence-origin commits auto-excluded.
    13	//! - ~~**Phase 5.5**~~ ✅ V1 + V2 V1 + V2 V2 + V2 V3 V1 + V2 V4 V1 SHIPPED
    14	//!   2026-05-21. V1 (`924750819bc`) -- `Transport` trait, `LoopbackTransport`,
    15	//!   `NoopTransport`. V2 V1 (`ffd8f6e5f05`) -- attach/detach/has,
    16	//!   `flush_to_transport`, `poll_remote*` typed methods. V2 V2
    17	//!   (`51748b02944`, `b7aa4cb7bb9`) -- `AutoFlushPolicy::OnAppend`.
    18	//!   V2 V3 steps 1-6 (`603bdc9aa6c` to `8a8236840f2`) -- delta flush,
    19	//!   poll-side auto-flush, offline-write contract, WebSocket impl
    20	//!   (`ql-collab-ws`), 3-way megaudit, exit packet. V2 V4 V1
    21	//!   (`3342e21b964` to `397676899fb`) -- ack channel
    22	//!   (`flush_pending_to_transport`), `pending_op_count`,
    23	//!   `discard_pending_ops`, defensive hardening. V2 V4 V2 (K4 chunking)
    24	//!   deferred. See `docs/phase5/v2-v3-exit-packet.md` and
    25	//!   `docs/phase5/v2-v4-v1-exit-packet.md`.
    26	//! - ~~**Phase 5.6**~~ ✅ V1 + V2 shipped — V1 `c677e244704`:
    27	//!   `presence` module + 4 typed methods. V2: `sweep_presence`
    28	//!   (caller-opt-in clean-slate on rejoin; closes V1 known
    29	//!   persistence limitation).
    30	//! - **Phase 5.7** ✅ V1 SHIPPED 2026-05-22 (cross-repo at engine
    31	//!   `677ee03ee8b` → `6003db4ce2c` + IDE `1a7fc8bbe3f` → `a517d7c5f71`).
    32	//!   New `crates/ql-bindings-node/` (napi-rs cdylib) binds
    33	//!   `CollabSession` for the quantlab VS Code fork's extension
    34	//!   host. V1 surface = constructor / `fromSnapshot` /
    35	//!   `appendPutValue` / `exportBytes` / `mergeBytes` / observability
    36	//!   accessors. V2 will bind Transport (3-5d, recommended next).
    37	//!   V3 will bind `rebuild_workbook` + full Op enum + undo/redo +
    38	//!   presence + persistence (cell-grid UI; 1-2wk). See
    39	//!   `docs/phase5/5-7-v1-exit-packet.md`.
    40	//!
    41	//! What ships in 5.2.a (5.2.b update 2026-05-19):
    42	//!
    43	//! - `CollabSession::new(peer_id)` — fresh session with an empty
    44	//!   `OpLog` and the peer-id wired through to
    45	//!   `LoroDoc::set_peer_id` (Phase 5.2.b — was a label-only stamp
    46	//!   in 5.2.a). Returns `Result` because Loro's `set_peer_id` is
    47	//!   fallible.
    48	//! - `CollabSession::from_snapshot(peer_id, bytes)` — fork from a
    49	//!   shared base snapshot (used by both peers in the D-4 probe
    50	//!   pattern). The reborn session's peer id is `peer_id`; existing
    51	//!   ops in the imported snapshot retain their original
    52	//!   attribution.
    53	//! - `append_op(&mut self, Op)` — local append; delegates to
    54	//!   `OpLog::append`.
    55	//! - `merge_bytes(&mut self, &[u8])` — pull a remote peer's
    56	//!   snapshot via Loro's CRDT merge; delegates to
    57	//!   `OpLog::merge_bytes`.
    58	//! - `export_bytes(&self)` — produce a snapshot for transport.
    59	//! - `op_log(&self)` — read-only view of the underlying log
    60	//!   (for replay against a workbook).
    61	//! - `peer_id(&self)` — the session's stable peer id.
    62	
    63	use thiserror::Error;
    64	
    65	use ql_functions::FunctionRegistry;
    66	use ql_oplog::{replay_into, Op, OpLog, OpLogError, PeerId, ReplayError, PRESENCE_COMMIT_ORIGIN};
    67	use ql_storage::Workbook;
    68	
    69	use crate::presence::{self, PresenceError, PresenceState};
    70	use crate::repair::{
    71	    repair_column_rename_chain, repair_sheet_rename_chain, repair_table_rename_chain,
    72	    ColumnRepairReport, SheetRepairReport, TableRepairReport,
    73	};
    74	use crate::transport::{Transport, TransportError};
    75	
    76	/// **Phase 5.5 V2 V1 audit closure (2026-05-19):** default cap on
    77	/// the number of blobs `CollabSession::poll_remote` drains per
    78	/// call. Prevents one poll from starving the calling thread when
    79	/// an externally-fed transport produces faster than we merge.
    80	/// Callers wanting a different bound use `poll_remote_with_limit`.
    81	pub const DEFAULT_POLL_REMOTE_LIMIT: usize = 64;
    82	
    83	// Phase 5.5 V2 V1 audit closure: pin the Send contract the
    84	// docstring promises ("callers can move sessions between threads
    85	// with attached transports"). If a future Loro dep bump or field
    86	// addition breaks Send, this stops compiling.
    87	const _ASSERT_COLLAB_SESSION_SEND: fn() = || {
    88	    fn assert_send<T: Send>() {}
    89	    assert_send::<CollabSession>();
    90	};
    91	
    92	// **V2 V4 V1 step 3 audit closure (Codex L1 + Opus M1, 2026-05-21):**
    93	// CollabSession is Send but NOT Sync — the
    94	// `Option<Box<dyn Transport + Send>>` field carries no Sync bound on
    95	// the trait object, so the field is `?Sync` and the struct inherits
    96	// `!Sync`. This contrasts with `WebSocketTransport` (the concrete
    97	// type), which IS Sync since all its concrete fields happen to be
    98	// Sync. Compile-asserting `!Sync` is awkward in stable Rust;
    99	// documented here via a probe-then-commented-out pattern so a future
   100	// reader can verify the bound by un-commenting and watching the
   101	// build error message naming the !Sync field.
   102	//
   103	// fn assert_collab_session_not_sync() {
   104	//     fn assert_sync<T: Sync>() {}
   105	//     assert_sync::<CollabSession>();  // EXPECTED COMPILE ERROR
   106	// }
   107	
   108	/// Errors emitted by `CollabSession` operations.
   109	#[derive(Debug, Error)]
   110	#[non_exhaustive]
   111	pub enum CollabSessionError {
   112	    /// The underlying `OpLog` failed (Loro encode/decode, serde,
   113	    /// etc.). Surfaces the wrapped `OpLogError` for diagnosis.
   114	    #[error("op log error: {0}")]
   115	    OpLog(#[from] OpLogError),
   116	
   117	    /// A presence-layer call failed (serde, peer-key parse,
   118	    /// underlying OpLog).
   119	    #[error("presence error: {0}")]
   120	    Presence(#[from] PresenceError),
   121	
   122	    /// **Phase 5.4 V1:** a Loro `UndoManager` call (`undo` /
   123	    /// `redo`) returned an error. Reachable when the underlying
   124	    /// document is in an invalid state for the requested
   125	    /// operation (e.g. mid-transaction).
   126	    #[error("undo/redo error: {0}")]
   127	    Undo(#[from] loro::LoroError),
   128	
   129	    /// **Phase 5.5 V2 (2026-05-19):** the attached `Transport`
   130	    /// returned an error.
   131	    ///
   132	    /// For `flush_to_transport`: the local `OpLog` is unaffected
   133	    /// (the snapshot export happened first; the failed step was
   134	    /// `Transport::send`). Caller can retry the flush.
   135	    ///
   136	    /// For `poll_remote`: any bytes successfully drained BEFORE
   137	    /// the error were already `merge_bytes`'d into the local
   138	    /// `OpLog` — those merges persist. The error indicates the
   139	    /// transport itself is misbehaving (`Io`) — `Closed` is
   140	    /// handled gracefully by `poll_remote` itself (returns
   141	    /// `Ok(merged)`, see Codex+Opus 5.5 V2 V1 audit closure).
   142	    /// Caller should detach + reattach a new transport instead
   143	    /// of retrying poll.
   144	    #[error("transport error: {0}")]
   145	    Transport(#[from] TransportError),
   146	
   147	    /// **Phase 5.3 step 5b (2026-05-20) — production wiring closure
   148	    /// (Opus-A Scenario E HIGH).** `rebuild_workbook` invoked
   149	    /// `replay_into` on the session's `OpLog` and replay failed at
   150	    /// some op index. The fresh workbook constructed internally was
   151	    /// in a HALF-MERGED state when the error fired and was DROPPED
   152	    /// before the error was returned (the wrapper owns it). The
   153	    /// session's `OpLog` is unaffected (replay reads only).
   154	    ///
   155	    /// Carries the underlying [`ReplayError`] via `#[source]` so
   156	    /// callers can `match err.source()` for the original op index +
   157	    /// diagnostic.
   158	    #[error("replay error: {0}")]
   159	    Replay(#[from] ReplayError),
   160	}
   161	
   162	impl CollabSessionError {
   163	    /// **Phase 5.7 V2.7 (2026-05-22) — error-code discrimination.**
   164	    ///
   165	    /// Returns a stable `&'static str` identifier for this variant.
   166	    /// For `Transport(inner)`, returns the inner `TransportError`'s
   167	    /// kind (e.g. `"transport_closed"`) so JS callers can branch on
   168	    /// the underlying transport state without unwrapping a wrapper
   169	    /// kind. For all other variants, returns the wrapper kind
   170	    /// directly (e.g. `"session_undo"`).
   171	    ///
   172	    /// **Stability**: see [`crate::TransportError::kind`].
   173	    pub fn kind(&self) -> &'static str {
   174	        match self {
   175	            CollabSessionError::OpLog(_) => "session_oplog",
   176	            CollabSessionError::Presence(_) => "session_presence",
   177	            CollabSessionError::Undo(_) => "session_undo",
   178	            CollabSessionError::Transport(inner) => inner.kind(),
   179	            CollabSessionError::Replay(_) => "session_replay",
   180	        }
   181	    }
   182	}
   183	
   184	/// **Phase 5.3 step 5b (2026-05-20) — production wiring closure
   185	/// (Opus-A Scenario E HIGH):** result of
   186	/// [`CollabSession::rebuild_workbook`]. Combines the replay op count
   187	/// with the two repair reports so callers can log a single
   188	/// diagnostic surface.
   189	///
   190	/// `#[non_exhaustive]` per Codex step 5b audit LOW closure: column
   191	/// repair (Phase 5.3 step 5c) is expected to add another field
   192	/// (`column_repair: ColumnRepairReport`) — external struct literal
   193	/// construction is forbidden so future field additions are
   194	/// non-breaking.
   195	#[derive(Debug, Clone, Default)]
   196	#[non_exhaustive]
   197	pub struct SyncReport {
   198	    /// Total ops applied by `replay_into`. Same as the success
   199	    /// value of `replay_into` itself.
   200	    pub ops_replayed: usize,
   201	    /// Sheet rename-repair pass diagnostics (formulas rewritten,
   202	    /// ambiguous-skipped rules).
   203	    pub sheet_repair: SheetRepairReport,
   204	    /// Table rename-repair pass diagnostics.
   205	    pub table_repair: TableRepairReport,
   206	    /// Column rename-repair pass diagnostics (Phase 5.3 step 5c —
   207	    /// closes Opus-A megaudit V1 LIM #3 HIGH).
   208	    pub column_repair: ColumnRepairReport,
   209	}
   210	
   211	/// Phase 5.3 step 5b audit closure (Opus MEDIUM-2) + step 5c update:
   212	/// one-line `Display` impl for log-line diagnostic use. Format:
   213	/// `"sync: ops=N sheet_rewrites=N(skip=N) table_rewrites=N(skip=N) column_rewrites=N(skip=N)"`.
   214	impl std::fmt::Display for SyncReport {
   215	    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
   216	        write!(
   217	            f,
   218	            "sync: ops={} sheet_rewrites={}(skip={}) table_rewrites={}(skip={}) column_rewrites={}(skip={})",
   219	            self.ops_replayed,
   220	            self.sheet_repair.formulas_rewritten,
   221	            self.sheet_repair.ambiguous_rules_skipped.len(),
   222	            self.table_repair.formulas_rewritten,
   223	            self.table_repair.ambiguous_rules_skipped.len(),
   224	            self.column_repair.formulas_rewritten,
   225	            self.column_repair.ambiguous_rules_skipped.len(),
   226	        )
   227	    }
   228	}
   229	
   230	/// **Phase 5.5 V2 V2 (2026-05-21):** auto-flush policy for
   231	/// [`CollabSession`]. Determines when (if ever) the session pushes
   232	/// its current snapshot through the attached transport without an
   233	/// explicit [`CollabSession::flush_to_transport`] call.
   234	///
   235	/// V2 V2 (this ship) adds two variants. V2 V3 will add a `Threshold`
   236	/// variant for byte-budget-based batching.
   237	///
   238	/// `#[non_exhaustive]` so adding a `Threshold(usize)` variant later
   239	/// is non-breaking — callers MUST handle the `_` arm in `match`.
   240	#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
   241	#[non_exhaustive]
   242	pub enum AutoFlushPolicy {
   243	    /// Default. V2 V1 behavior preserved: caller drives flush + poll
   244	    /// explicitly via [`CollabSession::flush_to_transport`] +
   245	    /// [`CollabSession::poll_remote`]. Auto-flush never fires.
   246	    #[default]
   247	    Disabled,
   248	    /// Flush automatically after every public method that mutates the
   249	    /// underlying `LoroDoc`. Triggers on: `append_op`, `merge_bytes`,
   250	    /// `update_presence`, `clear_presence`, `sweep_presence`,
   251	    /// `undo`, `redo`, `poll_remote`, `poll_remote_with_limit`.
   252	    ///
   253	    /// **Phase 5.5 V2 V3 step 1 (2026-05-21):** auto-flush routes
   254	    /// through [`flush_delta_to_transport`] (delta path) rather than
   255	    /// [`flush_to_transport`] (full-snapshot path). Wire payload is
   256	    /// O(per-op delta) instead of O(full state). The idempotency
   257	    /// short-circuit means a flush after no state change is a true
   258	    /// no-op (no transport.send invocation) — this closes the V2 V2
   259	    /// audit echo-loop class.
   260	    ///

exec
/bin/zsh -lc "rg -n \"Phase 5\\.7 V2\\.7|collab_session_error_to_napi|transport_error_to_napi|websocket_error_to_napi|Error::from_reason|map_err\" quantbook-engine/crates/ql-bindings-node/src/lib.rs" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
exec
/bin/zsh -lc "rg -n \"Error::from_reason\\(format"'!.*'"\\{e\\}\" quantbook-engine/crates/ql-bindings-node/src/lib.rs" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
259:// because `Error::from_reason(format!("{e}"))` only carries the
295:    Error::from_reason(format!("[{}] {e}", e.kind()))
302:    Error::from_reason(format!("[{}] {e}", e.kind()))
309:    Error::from_reason(format!("[{}] {e}", e.kind()))
915:            .map_err(|e| Error::from_reason(format!("flushPendingToTransport task: {e}")))?
1329:        .map_err(|e| Error::from_reason(format!("waitUntilBlocked task: {e}")))?

 succeeded in 0ms:
168:        return Err(Error::from_reason(format!(
173:        return Err(Error::from_reason(format!(
178:        return Err(Error::from_reason(format!(
183:        return Err(Error::from_reason(format!(
230:        return Err(Error::from_reason(
235:        return Err(Error::from_reason(
240:        return Err(Error::from_reason(
245:        return Err(Error::from_reason(
254:// Phase 5.7 V2.7 (2026-05-22) — error-code discrimination helpers
259:// because `Error::from_reason(format!("{e}"))` only carries the
294:fn collab_session_error_to_napi(e: CollabSessionError) -> Error {
295:    Error::from_reason(format!("[{}] {e}", e.kind()))
301:fn transport_error_to_napi(e: TransportError) -> Error {
302:    Error::from_reason(format!("[{}] {e}", e.kind()))
308:fn websocket_error_to_napi(e: WebSocketError) -> Error {
309:    Error::from_reason(format!("[{}] {e}", e.kind()))
348:        let session = CoreCollabSession::new(pid).map_err(collab_session_error_to_napi)?;
360:            .map_err(collab_session_error_to_napi)?;
411:            return Err(Error::from_reason(format!(
422:        inner.append_op(op).map_err(collab_session_error_to_napi)?;
431:        let bytes = inner.export_bytes().map_err(collab_session_error_to_napi)?;
473:            .map_err(collab_session_error_to_napi)?;
559:            Error::from_reason("Transport has already been consumed by attachTransport".to_string())
610:            .map_err(collab_session_error_to_napi)
643:        let blobs_drained = inner.poll_remote().map_err(collab_session_error_to_napi)?;
688:            .map_err(collab_session_error_to_napi)
719:            .map_err(collab_session_error_to_napi)?;
915:            .map_err(|e| Error::from_reason(format!("flushPendingToTransport task: {e}")))?
916:            .map_err(transport_error_to_napi)
927:        other => Err(Error::from_reason(format!(
957:        other => Err(Error::from_reason(format!(
1045:            .map_err(websocket_error_to_napi)?;
1128:            Error::from_reason("LoopbackPair.takeA already called on this pair".to_string())
1139:            Error::from_reason("LoopbackPair.takeB already called on this pair".to_string())
1238:            return Err(Error::from_reason(
1272:            Error::from_reason(
1320:                .map_err(|e| format!("blocked lock poisoned: {e}"))?;
1324:                    .map_err(|e| format!("blocked wait poisoned: {e}"))?;
1329:        .map_err(|e| Error::from_reason(format!("waitUntilBlocked task: {e}")))?
1330:        .map_err(Error::from_reason)

exec
/bin/zsh -lc "nl -ba quantbook-engine/crates/ql-bindings-node/src/lib.rs | sed -n '1,240p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
     1	//! `ql-bindings-node` — VS Code extension binding (Phase 5.7 V1).
     2	//!
     3	//! **Phase 5.7 V1 (2026-05-22, this ship):** binds `CollabSession` for the
     4	//! quantlab VS Code fork's extension host. Replaces the Phase-6+ reserved
     5	//! placeholder this crate used to be.
     6	//!
     7	//! ## Architecture
     8	//!
     9	//! cdylib + napi-rs 3.x. The `#[napi]` derive macros generate the
    10	//! JS-facing class shape from the Rust struct. napi-rs handles:
    11	//!   - JS class lifecycle (`new`, finalize)
    12	//!   - `BigInt <-> u64` conversion for `PeerId`
    13	//!   - `Uint8Array <-> &[u8]`: `as_ref()` exposes a slice INTO the
    14	//!     JS-owned ArrayBuffer (verified via napi-rs 3.9.0
    15	//!     `arraybuffer.rs:653`'s `slice::from_raw_parts` on the SAB-or-AB
    16	//!     backing store). On export we hand ownership of a `Vec<u8>` to
    17	//!     JS via `Uint8Array::from`, which transfers the buffer rather
    18	//!     than copying.
    19	//!   - panic safety: **napi-rs 3.x does NOT wrap entry points in
    20	//!     `catch_unwind` by default** (Phase 5.7 V1 audit Codex H1 +
    21	//!     Opus H3, 2026-05-22 — verified by reading napi-derive-backend
    22	//!     5.0.4 `src/codegen/fn.rs:217-240`: the wrap is gated on
    23	//!     `#[napi(catch_unwind)]` opt-in). The V1 binding does NOT use
    24	//!     `catch_unwind` — instead, every `#[napi]` method
    25	//!     pre-validates inputs that could trigger engine-side `assert_*!`,
    26	//!     `panic!`, `unwrap`, or `expect`. Smoke caught one such hazard
    27	//!     (PeerId(0) → `CollabSession::new`'s `assert_ne!(peer, 0)` →
    28	//!     Node abort "failed to initiate panic, error 5"); the
    29	//!     `peer_id_from_bigint` helper now pre-rejects 0. V2+ each new
    30	//!     `#[napi]` method MUST sweep the engine for FFI-reachable
    31	//!     assertions and pre-validate the same way (see V2 backlog).
    32	//!
    33	//! ## V1 surface
    34	//!
    35	//! Single class `CollabSession` exposing the minimum methods that let
    36	//! TS code drive a round-trip:
    37	//!   - `new(peerId: bigint)`: wraps `ql_collab::CollabSession::new`
    38	//!   - static `fromSnapshot(peerId: bigint, bytes: Uint8Array)`:
    39	//!     wraps `from_snapshot`
    40	//!   - `appendPutValue(sheet, row, col, value)`: convenience for V1
    41	//!     (single Op variant; full Op enum binding deferred to V2)
    42	//!   - `exportBytes() -> Uint8Array`: snapshot
    43	//!   - `mergeBytes(bytes: Uint8Array) -> number`: returns merged op count
    44	//!   - `opCount() -> number`
    45	//!   - `pendingOpCount() -> number` (V2 V4 V1 step 2 helper)
    46	//!   - `hasPendingFlush() -> boolean` (V2 V3 step 3 helper)
    47	//!   - `peerId() -> bigint`
    48	//!
    49	//! ## V1 deferred (see `docs/phase5/5-7-v1-exit-packet.md`)
    50	//!
    51	//! Full V1-deferred-to-V2/V3 list lives in the V1 exit packet's
    52	//! "V1 deferred to V2" table. Summary: Transport binding
    53	//! (LoopbackTransport, WebSocketTransport, `attach_transport` /
    54	//! `detach_transport` / `has_transport` / `flush_to_transport` /
    55	//! `flush_delta_to_transport` / `flush_pending_to_transport` /
    56	//! `poll_remote` / `poll_remote_with_limit` /
    57	//! `transport_last_error` / `set_auto_flush_policy`), multi-window
    58	//! demo, undo/redo (UndoGroupGuard RAII translation), presence
    59	//! (PresenceState shape), full Op enum (beyond PutValue),
    60	//! Format/D-1 (FormatId enum), `rebuild_workbook` (D-3
    61	//! production-visible closure — Phase 5.7 V3), `discard_pending_ops`,
    62	//! `.qbook` persistence import/export, `#[napi(catch_unwind)]` opt-in
    63	//! for defense in depth, SharedArrayBuffer defensive copy, BigInt
    64	//! return for `opCount`/`pendingOpCount`/`mergeBytes`,
    65	//! `mergeBytesDelta` companion, `@napi-rs/cli` publish pipeline,
    66	//! Windows-specific `.dll` naming, CI `QUANTBOOK_REQUIRE_ENGINE=1`
    67	//! enforcement. V2 picks up Transport; V3 picks up cell-grid UI +
    68	//! persistence + `rebuild_workbook` wiring.
    69	//!
    70	//! ## Send + Sync
    71	//!
    72	//! **V2.4 (2026-05-22) UPDATE**: `CollabSession` is now `Send + Sync`
    73	//! (was `Send + !Sync` in V1+V2.1+V2.2+V2.3). The shape changed in V2.4
    74	//! from `inner: CoreCollabSession` to `inner: Arc<parking_lot::Mutex<CoreCollabSession>>`
    75	//! as the closure for V2.3 audit's HIGH-1 (Rust UB via napi `&mut self`
    76	//! async re-entry). The Arc<Mutex> composition adds Sync via interior
    77	//! mutability — multiple shared `&CollabSession` references are sound;
    78	//! mutation flows through the Mutex.
    79	//!
    80	//! **Compile-asserted proof** (lib.rs near end of file): both Send AND
    81	//! Sync are pinned via `assert_send + assert_sync` const-fn pattern.
    82	//! Per Rule 4: positive proof is required (this is the V2.4
    83	//! re-pinning of what was V1's `!Sync` claim).
    84	//!
    85	//! **Underlying composition** (Send + Sync verified by source-walks):
    86	//!   - `CoreCollabSession: Send + !Sync` (engine claim at
    87	//!     `ql-collab/src/session.rs:87`, unchanged).
    88	//!   - `parking_lot::Mutex<T>: Send + Sync` when `T: Send`
    89	//!     (verified per `lock_api-0.4.14/src/mutex.rs:144`'s
    90	//!     `impl<R: RawMutex + Sync, T: Send> Sync for Mutex<R, T>`).
    91	//!   - `Arc<T>: Send + Sync` when `T: Send + Sync`.
    92	//!   - Therefore the V2.4 binding wrapper composition is `Send + Sync`.
    93	//!
    94	//! **V2.5 (2026-05-22): V8-block hazard CLOSED** (Opus V2.4 HIGH-1).
    95	//!
    96	//! The V2.4 binding pattern held `self.inner.lock()` across the
    97	//! `spawn_blocking` Condvar wait — concurrent JS sync method calls
    98	//! blocked the V8 event loop on lock acquisition. V2.5 refactored
    99	//! `flushPendingToTransport` to extract a detached `FlushAck`
   100	//! handle (via `ql_collab::CollabSession::flush_pending_handle`)
   101	//! under a brief lock acquisition, drop the lock, then wait on the
   102	//! handle's Arc-cloned progress state inside `spawn_blocking`. The
   103	//! handle owns no reference to the session mutex; concurrent JS
   104	//! sync methods acquire the lock immediately while the wait runs.
   105	//!
   106	//! See `flushPendingToTransport`'s docstring below + the V2.5 audit
   107	//! transcripts at `docs/audits/2026-05-22-phase-5-7-v2-5-{codex,opus}.md`
   108	//! for the full closure rationale + post-implementation
   109	//! source-walked verification (V8-block VERIFIED CLOSED in both
   110	//! lanes via the `let inner = self.inner.lock(); … }` scope drop +
   111	//! Arc-only handle-handoff to `spawn_blocking`).
   112	//!
   113	//! V2.5 caller note: there is NO V8-block-related caller discipline
   114	//! required today. Concurrent session method calls during a pending
   115	//! `flushPendingToTransport` are sound + non-blocking. The V2.6
   116	//! contract test (`flushPendingToTransport does NOT block sync
   117	//! methods on the same session`) empirically pins this — opCount
   118	//! during a 1000ms-blocked flush returned in <50ms on local dev.
   119	
   120	#![deny(clippy::all)]
   121	#![allow(clippy::missing_safety_doc)] // napi-rs generated wrappers
   122	
   123	use std::sync::Arc;
   124	
   125	use napi::bindgen_prelude::*;
   126	use napi_derive::napi;
   127	use parking_lot::Mutex;
   128	
   129	use ql_collab::AutoFlushPolicy as CoreAutoFlushPolicy;
   130	use ql_collab::CollabSession as CoreCollabSession;
   131	use ql_collab::CollabSessionError;
   132	use ql_collab::LoopbackTransport;
   133	use ql_collab::Transport as CoreTransport;
   134	use ql_collab::TransportError;
   135	use ql_collab_ws::WebSocketError;
   136	use ql_collab_ws::WebSocketTransport;
   137	use ql_oplog::CellWireValue;
   138	use ql_oplog::Op;
   139	use ql_types::PeerId;
   140	
   141	/// **Phase 5.7 V1 (2026-05-22):** smoke method exposing the binding's
   142	/// own version. The IDE's loader uses this to verify the loaded
   143	/// `.node` file matches the engine version it expects. V2 will add a
   144	/// proper version-mismatch error path.
   145	#[napi]
   146	pub fn version() -> String {
   147	    env!("CARGO_PKG_VERSION").to_string()
   148	}
   149	
   150	/// **Phase 5.7 V1 megaudit closure (Codex HIGH, 2026-05-22):**
   151	/// Validate a JS Number argument that represents a u32-domain value.
   152	/// Rejects non-finite, negative, fractional, and out-of-u32 values
   153	/// with a distinct error message naming the calling method + parameter.
   154	///
   155	/// Why this exists: napi-rs's `napi_get_value_uint32` (used for `u32`
   156	/// params) applies ECMAScript `ToUint32`, silently coercing `-1` to
   157	/// `u32::MAX`, `NaN`/`Infinity` to `0`, fractions to floor-toward-zero.
   158	/// By taking the param as `f64` (raw `napi_get_value_double`) and
   159	/// validating here, we surface bad inputs as explicit JS Errors
   160	/// instead of corrupting workbook state.
   161	///
   162	/// **V2.2 audit closure (Codex LOW-1, 2026-05-22)**: added `method`
   163	/// context parameter so the error message names the actual calling
   164	/// method. Prior version hard-coded `"appendPutValue"` which produced
   165	/// confusing diagnostics when called from `pollRemoteWithLimit`.
   166	fn validate_u32_index(method: &str, name: &str, value: f64) -> Result<u32> {
   167	    if !value.is_finite() {
   168	        return Err(Error::from_reason(format!(
   169	            "{method}: {name} must be a finite non-negative integer, got {value}"
   170	        )));
   171	    }
   172	    if value < 0.0 {
   173	        return Err(Error::from_reason(format!(
   174	            "{method}: {name} must be a non-negative integer, got {value}"
   175	        )));
   176	    }
   177	    if value.fract() != 0.0 {
   178	        return Err(Error::from_reason(format!(
   179	            "{method}: {name} must be an integer, got {value}"
   180	        )));
   181	    }
   182	    if value > u32::MAX as f64 {
   183	        return Err(Error::from_reason(format!(
   184	            "{method}: {name} must be in [0, 4294967295] (u32::MAX), got {value}"
   185	        )));
   186	    }
   187	    Ok(value as u32)
   188	}
   189	
   190	/// Helper: convert a JS `BigInt` to `PeerId` with explicit rejection of
   191	/// FIVE failure modes:
   192	///   - negative BigInt (signed bit set) — PeerId is u64-domain
   193	///   - BigInt doesn't fit in u64 (lossless = false)
   194	///   - `peer_id == 0` — Loro's LEGACY_PEER sentinel; rejected with a
   195	///     PROACTIVE check here (the engine's `CollabSession::new` would
   196	///     hit `assert_ne!` and panic, which napi-rs 3.x doesn't reliably
   197	///     catch into a JS exception — observed during V1 smoke test:
   198	///     "failed to initiate panic, error 5, aborting"). Pre-validate
   199	///     in the FFI boundary to keep Node alive.
   200	///   - `peer_id == u64::MAX` — Loro's `PeerID::MAX` sentinel (verified
   201	///     via Loro 1.12 `loro-internal-1.12.0/src/loro.rs:184`:
   202	///     `if peer == PeerID::MAX { return Err(...) }`). Loro returns a
   203	///     CLEAN `Err` here (not a panic), so the FFI boundary is intact
   204	///     either way — but pre-rejecting at the binding gives a faster
   205	///     failure and a consistent error message paired with the
   206	///     `peer_id == 0` case.
   207	///
   208	/// napi-rs 3.x `BigInt::get_u64()` returns `(sign_bit: bool, value: u64,
   209	/// lossless: bool)`. `sign_bit == true` indicates a negative BigInt.
   210	/// Per CLAUDE.md no-fallback rule: each failure mode surfaces a distinct
   211	/// error message so IDE consumers can show the right user-facing copy.
   212	///
   213	/// **Phase 5.7 V1 smoke-test finding (2026-05-22)**: the original
   214	/// version of this helper allowed `peer_id == 0` through, expecting
   215	/// `CollabSession::new`'s `assert_ne!(0, 0)` to surface a clean error.
   216	/// In practice the panic ABORTED the Node process. Lesson:
   217	/// engine-level `assert_*!` is NOT a safe boundary for FFI. Every
   218	/// engine method that uses `assert_*!` for input validation needs a
   219	/// proactive pre-check at the FFI layer.
   220	///
   221	/// **Phase 5.7 V1 audit closure (Rule 4 / Opus boundary test,
   222	/// 2026-05-22)**: the closure test `BigInt boundary -- u64::MAX
   223	/// accepted, 2^64 rejected` revealed that Loro also reserves
   224	/// `PeerID::MAX` (in addition to 0). Added pre-rejection here for
   225	/// symmetry. V2 will sweep the engine + Loro for any other reserved
   226	/// sentinel values as the binding surface grows.
   227	fn peer_id_from_bigint(peer_id: &BigInt) -> Result<PeerId> {
   228	    let (sign_bit, peer_u64, lossless) = peer_id.get_u64();
   229	    if sign_bit {
   230	        return Err(Error::from_reason(
   231	            "peerId BigInt must be non-negative (PeerId is u64-domain)".to_string(),
   232	        ));
   233	    }
   234	    if !lossless {
   235	        return Err(Error::from_reason(
   236	            "peerId BigInt does not fit in u64 (lossy conversion)".to_string(),
   237	        ));
   238	    }
   239	    if peer_u64 == 0 {
   240	        return Err(Error::from_reason(

exec
/bin/zsh -lc "nl -ba quantbook-engine/crates/ql-bindings-node/src/lib.rs | sed -n '240,560p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
   240	        return Err(Error::from_reason(
   241	            "peerId must be non-zero (PeerId(0) is LEGACY_PEER, reserved for pre-collab single-writer + qbook migration)".to_string(),
   242	        ));
   243	    }
   244	    if peer_u64 == u64::MAX {
   245	        return Err(Error::from_reason(
   246	            "peerId must not be u64::MAX (Loro reserves PeerID::MAX as an internal sentinel)"
   247	                .to_string(),
   248	        ));
   249	    }
   250	    Ok(PeerId::new(peer_u64))
   251	}
   252	
   253	// ============================================================
   254	// Phase 5.7 V2.7 (2026-05-22) — error-code discrimination helpers
   255	// ============================================================
   256	//
   257	// Closes V2.1+V2.2+V2.3 Opus MEDIUM-3 carryforwards: every engine
   258	// error type's variant discriminant is lost on the napi boundary
   259	// because `Error::from_reason(format!("{e}"))` only carries the
   260	// Display string.
   261	//
   262	// V2.7 design (Codex M3 + Opus V2.1/V2.2/V2.3 M3 convergent):
   263	//
   264	//   1. Engine: each error enum gets a `kind() -> &'static str`
   265	//      accessor (`crates/ql-collab/src/transport.rs::TransportError`,
   266	//      `crates/ql-collab-ws/src/lib.rs::WebSocketError`,
   267	//      `crates/ql-collab/src/session.rs::CollabSessionError`).
   268	//
   269	//   2. Binding: the helpers below prepend `[<kind>]` to the napi
   270	//      error message, producing strings like
   271	//      `"[transport_closed] transport closed"`.
   272	//
   273	//   3. IDE: `parseQuantbookError(err)` in
   274	//      `extensions/quantlab/src/quantbook/session.ts` extracts the
   275	//      bracketed code into a typed `QuantbookErrorCode` so reconnect
   276	//      logic can branch on `err.code === 'transport_closed'` etc.
   277	//
   278	// **V2.3 mocha test compatibility**: the existing Display strings
   279	// remain intact AFTER the prefix, so substring-match tests like
   280	// `/invalid WebSocket URL/` still match `"[websocket_invalid_url]
   281	// invalid WebSocket URL: ..."`. No test breakage expected.
   282	//
   283	// **Why not napi-rs `Error::with_code`?**: napi-rs 3.9.0's `Error`
   284	// uses `Status: enum` (no `Status::Custom(String)`). The standard
   285	// status codes don't map to our error variants. Prefix-encoding
   286	// in the message is the canonical workaround documented across the
   287	// napi-rs ecosystem until a future custom-code API lands.
   288	
   289	/// Map a [`CollabSessionError`] to a napi [`Error`] with a kind-
   290	/// prefixed message. The kind comes from
   291	/// [`CollabSessionError::kind`]; for `Transport(inner)` it
   292	/// transparently passes through the inner kind (e.g.
   293	/// `"transport_closed"` instead of `"session_transport"`).
   294	fn collab_session_error_to_napi(e: CollabSessionError) -> Error {
   295	    Error::from_reason(format!("[{}] {e}", e.kind()))
   296	}
   297	
   298	/// Map a [`TransportError`] to a napi [`Error`] with a kind-
   299	/// prefixed message. Used by `flushPendingToTransport`'s async
   300	/// `wait_for_drain` path.
   301	fn transport_error_to_napi(e: TransportError) -> Error {
   302	    Error::from_reason(format!("[{}] {e}", e.kind()))
   303	}
   304	
   305	/// Map a [`WebSocketError`] to a napi [`Error`] with a kind-
   306	/// prefixed message. Used by `Transport.websocketConnect`'s
   307	/// rejection path.
   308	fn websocket_error_to_napi(e: WebSocketError) -> Error {
   309	    Error::from_reason(format!("[{}] {e}", e.kind()))
   310	}
   311	
   312	/// JS-facing wrapper for `ql_collab::CollabSession`.
   313	///
   314	/// **V2.4 refactor (2026-05-22)**: holds `Arc<Mutex<CoreCollabSession>>`
   315	/// instead of `CoreCollabSession` directly. Closes V2.3 audit Codex+Opus
   316	/// HIGH-1 (Rust UB via napi `&mut self` async re-entry) by:
   317	///   - All napi methods take `&self` (not `&mut self`). napi-rs's
   318	///     generated wrappers never produce two `&'static mut` to the same
   319	///     CollabSession; the Arc + Mutex absorbs the mutation.
   320	///   - Sync methods: `self.inner.lock()` acquires + holds the mutex
   321	///     within the method body. parking_lot's Mutex is uncontended-fast
   322	///     and never poisons.
   323	///   - Async methods: `Arc::clone(&self.inner)` + `tokio::task::spawn_blocking`.
   324	///     The clone-and-move pattern means the spawned task owns its own
   325	///     `Arc` reference; the blocking lock acquisition happens off the
   326	///     tokio runtime's worker pool (closes V2.3 HIGH-2 tokio starvation).
   327	///
   328	/// **Send + Sync (V2.4 update)**: the new shape is `Send + Sync` (was
   329	/// `Send + !Sync` in V1+V2.1+V2.2). Sync now holds because
   330	/// `Arc<Mutex<T>>: Sync` when `T: Send`, regardless of T: Sync. JS
   331	/// single-threadedness still holds in practice; the Send + Sync bound
   332	/// is a strict improvement (Rule 4 pin updated below).
   333	#[napi]
   334	pub struct CollabSession {
   335	    inner: Arc<Mutex<CoreCollabSession>>,
   336	}
   337	
   338	#[napi]
   339	impl CollabSession {
   340	    /// Construct a fresh session for the given peer.
   341	    ///
   342	    /// **Failure modes**: `peerId == 0` is rejected by the underlying
   343	    /// engine (PeerId sentinel — Loro reserves the 0 value). napi-rs
   344	    /// surfaces engine errors as JS `Error` exceptions.
   345	    #[napi(constructor)]
   346	    pub fn new(peer_id: BigInt) -> Result<Self> {
   347	        let pid = peer_id_from_bigint(&peer_id)?;
   348	        let session = CoreCollabSession::new(pid).map_err(collab_session_error_to_napi)?;
   349	        Ok(Self {
   350	            inner: Arc::new(Mutex::new(session)),
   351	        })
   352	    }
   353	
   354	    /// Reconstruct a session from a previously-exported snapshot.
   355	    /// Mirrors `ql_collab::CollabSession::from_snapshot`.
   356	    #[napi(factory, js_name = "fromSnapshot")]
   357	    pub fn from_snapshot(peer_id: BigInt, bytes: Uint8Array) -> Result<Self> {
   358	        let pid = peer_id_from_bigint(&peer_id)?;
   359	        let session = CoreCollabSession::from_snapshot(pid, bytes.as_ref())
   360	            .map_err(collab_session_error_to_napi)?;
   361	        Ok(Self {
   362	            inner: Arc::new(Mutex::new(session)),
   363	        })
   364	    }
   365	
   366	    /// V1 convenience: append a `PutValue` op with a number value.
   367	    /// The full Op enum binding is V2 work; V1 only exposes this
   368	    /// single variant because it's the minimum that demonstrates
   369	    /// the round-trip.
   370	    ///
   371	    /// # Input validation (Phase 5.7 V1 megaudit closure, 2026-05-22)
   372	    ///
   373	    /// The original V1 closure tried to push validation into the TS
   374	    /// wrapper (`session.ts::appendPutValueValidated`), but that left
   375	    /// this `#[napi]` method as a public unchecked surface that direct
   376	    /// callers could bypass. The Phase 5.7 V1 megaudit (Codex HIGH,
   377	    /// 2026-05-22) flagged this as a real hazard: any consumer skipping
   378	    /// the wrapper hit ECMAScript `ToUint32` for `u32` row/col args:
   379	    ///   - `-1` → `0xFFFFFFFF` (silent wrap to u32::MAX)
   380	    ///   - `NaN` → `0` (silent coercion)
   381	    ///   - `Infinity` → `0` (silent coercion)
   382	    ///   - `2.5` → `2` (silent floor-toward-zero)
   383	    ///
   384	    /// **Closure**: the `row` and `col` parameters are now `f64`
   385	    /// (raw JS Number, NOT ToUint32-coerced via napi-rs's
   386	    /// `napi_get_value_double`). Inside the method we validate:
   387	    ///   - finite (not NaN / Infinity)
   388	    ///   - non-negative
   389	    ///   - integer (`fract() == 0.0`)
   390	    ///   - in u32 range (≤ `u32::MAX`)
   391	    /// then cast to `u32`. Any failure surfaces a precise JS Error.
   392	    ///
   393	    /// `sheet: u16` stays as-is — u16's `try_into::<u16>()` correctly
   394	    /// rejects out-of-range values from ToUint32, so the asymmetric
   395	    /// safety (Opus audit H2 finding) doesn't apply at u16.
   396	    ///
   397	    /// `value: f64` is already raw (Number → double, no coercion);
   398	    /// validate finiteness here too (NaN/Infinity rejection).
   399	    ///
   400	    /// The TS-side `appendPutValueValidated` wrapper becomes
   401	    /// defense-in-depth — it fails earlier with friendlier messages
   402	    /// but the engine-side validation is the load-bearing contract
   403	    /// for direct callers.
   404	    #[napi(js_name = "appendPutValue")]
   405	    pub fn append_put_value(&self, sheet: u16, row: f64, col: f64, value: f64) -> Result<()> {
   406	        // Validate row + col: finite, non-negative, integer, in u32 range.
   407	        let row_u32 = validate_u32_index("appendPutValue", "row", row)?;
   408	        let col_u32 = validate_u32_index("appendPutValue", "col", col)?;
   409	        // Validate value: finite (NaN/Infinity rejected).
   410	        if !value.is_finite() {
   411	            return Err(Error::from_reason(format!(
   412	                "appendPutValue value must be finite, got {value}"
   413	            )));
   414	        }
   415	        let op = Op::PutValue {
   416	            sheet,
   417	            row: row_u32,
   418	            col: col_u32,
   419	            value: CellWireValue::Number(value),
   420	        };
   421	        let mut inner = self.inner.lock();
   422	        inner.append_op(op).map_err(collab_session_error_to_napi)?;
   423	        Ok(())
   424	    }
   425	
   426	    /// Export a full snapshot of this session's op log.
   427	    /// Mirrors `CollabSession::export_bytes`.
   428	    #[napi(js_name = "exportBytes")]
   429	    pub fn export_bytes(&self) -> Result<Uint8Array> {
   430	        let inner = self.inner.lock();
   431	        let bytes = inner.export_bytes().map_err(collab_session_error_to_napi)?;
   432	        Ok(Uint8Array::from(bytes))
   433	    }
   434	
   435	    /// Merge a snapshot (or delta) from another peer.
   436	    ///
   437	    /// # Returns
   438	    ///
   439	    /// The session's `op_count` AFTER the merge (matches the
   440	    /// underlying `CollabSession::merge_bytes` signature). NOT the
   441	    /// number of newly merged ops -- duplicates are deduped by Loro
   442	    /// but the return value is the post-merge total, not the delta.
   443	    /// Per Codex M2 / Opus correction (Phase 5.7 V1 audit closure,
   444	    /// 2026-05-22): the original docstring said "merged count"
   445	    /// implying a delta; empirically, calling `mergeBytes` twice on
   446	    /// the same snapshot returns the same `op_count` both times.
   447	    ///
   448	    /// # SharedArrayBuffer hazard (V1 docstring; V2 defensive copy)
   449	    ///
   450	    /// `bytes.as_ref()` returns a slice INTO the JS-owned ArrayBuffer.
   451	    /// If the underlying buffer is a `SharedArrayBuffer`, another
   452	    /// Worker thread could mutate the bytes WHILE Loro's deserializer
   453	    /// is reading them. V1 assumption: callers pass non-SAB
   454	    /// `Uint8Array`s (which the V1 IDE demo does — `exportBytes`
   455	    /// returns a plain `Vec<u8>`-backed Uint8Array). V2 will add a
   456	    /// defensive `to_vec()` copy at the FFI boundary OR a SAB-detect
   457	    /// path via `napi_get_arraybuffer_info` (Codex H2 + Opus M3
   458	    /// convergent finding).
   459	    ///
   460	    /// # u32 clamp on overflow
   461	    ///
   462	    /// Return type is `u32`. If the post-merge `op_count` exceeds
   463	    /// `u32::MAX` (≈4 billion ops), the value is silently CLAMPED to
   464	    /// `u32::MAX`. Practically unreachable in V1 (4 billion ops would
   465	    /// consume terabytes of Loro storage) but flagged here per Opus M5
   466	    /// "verify before claiming". V2 will switch to `BigInt` return
   467	    /// type for unbounded sessions, matching `peerId()`'s precedent.
   468	    #[napi(js_name = "mergeBytes")]
   469	    pub fn merge_bytes(&self, bytes: Uint8Array) -> Result<u32> {
   470	        let mut inner = self.inner.lock();
   471	        let count = inner
   472	            .merge_bytes(bytes.as_ref())
   473	            .map_err(collab_session_error_to_napi)?;
   474	        Ok(u32::try_from(count).unwrap_or(u32::MAX))
   475	    }
   476	
   477	    /// Local op log length (visible LoroList). See V2 V4 V1 step 2
   478	    /// audit closure for the visible-list-vs-VV distinction; this
   479	    /// method returns the LIST length (matches IDE "how many ops have
   480	    /// I ever logged?" UX). For "how many pending vs last flush,"
   481	    /// use `pendingOpCount()`.
   482	    ///
   483	    /// **u32 clamp (Opus M5 closure):** silent clamp to `u32::MAX` if
   484	    /// the count exceeds u32. Same trade-off as `mergeBytes`'s
   485	    /// return; V2 will switch to BigInt.
   486	    #[napi(js_name = "opCount")]
   487	    pub fn op_count(&self) -> u32 {
   488	        let inner = self.inner.lock();
   489	        u32::try_from(inner.op_count()).unwrap_or(u32::MAX)
   490	    }
   491	
   492	    /// V2 V4 V1 step 2 helper: count of ops added since the last
   493	    /// successful flush to the currently-attached transport. Uses
   494	    /// VV math (monotonic under undo). Sibling invariant:
   495	    /// `pendingOpCount() > 0` iff `hasPendingFlush() === true`.
   496	    ///
   497	    /// V1 has no transport binding so this returns the count vs
   498	    /// the empty baseline (= same as opCount for a never-flushed
   499	    /// session). V2 will be where this gets interesting once the
   500	    /// Transport surface is bound.
   501	    ///
   502	    /// **u32 clamp (Opus M5 closure):** silent clamp to `u32::MAX` if
   503	    /// the count exceeds u32. Same trade-off as `opCount`; V2 will
   504	    /// switch to BigInt.
   505	    #[napi(js_name = "pendingOpCount")]
   506	    pub fn pending_op_count(&self) -> u32 {
   507	        let inner = self.inner.lock();
   508	        u32::try_from(inner.pending_op_count()).unwrap_or(u32::MAX)
   509	    }
   510	
   511	    /// V2 V3 step 3 helper: `true` when the local op log has ops
   512	    /// that haven't been flushed to the currently-attached transport.
   513	    #[napi(js_name = "hasPendingFlush")]
   514	    pub fn has_pending_flush(&self) -> bool {
   515	        let inner = self.inner.lock();
   516	        inner.has_pending_flush()
   517	    }
   518	
   519	    /// Peer ID of this session, as a `BigInt` (u64-domain).
   520	    #[napi(js_name = "peerId")]
   521	    pub fn peer_id(&self) -> BigInt {
   522	        let inner = self.inner.lock();
   523	        BigInt::from(inner.peer_id().as_u64())
   524	    }
   525	
   526	    // ==========================================================
   527	    // Phase 5.7 V2.1 (2026-05-22) — Transport surface
   528	    // ==========================================================
   529	
   530	    /// Attach a Transport to this session. Moves the inner boxed trait
   531	    /// object out of the `transport` wrapper (consuming it from JS's
   532	    /// perspective — subsequent calls fail).
   533	    ///
   534	    /// Mirrors `CollabSession::attach_transport_boxed` on the Rust side
   535	    /// which delegates to the V2 V3 step 1 baseline-reset path: the
   536	    /// next flush sends from empty VV (i.e., ALL local ops including
   537	    /// any appended while no transport was attached — Loro's CRDT op
   538	    /// log IS the implicit offline queue).
   539	    ///
   540	    /// V2.1 does NOT return the prior transport (if any) to JS. The
   541	    /// Rust side drops the returned `Box` immediately. Rationale: the
   542	    /// IDE has no use for the opaque prior transport, and exposing it
   543	    /// would require either creating another `Transport` wrapper
   544	    /// (defeating "this transport is now invalid" semantics) or a
   545	    /// special-cased "detached prior" type. V2.3+ may revisit if a use
   546	    /// case emerges.
   547	    ///
   548	    /// **Failure modes**:
   549	    /// - `transport` has already been used (its `inner` is `None`) →
   550	    ///   JS Error "Transport has already been consumed".
   551	    #[napi(js_name = "attachTransport")]
   552	    pub fn attach_transport(&self, transport: &mut Transport) -> Result<()> {
   553	        let boxed = transport.take_inner().ok_or_else(|| {
   554	            // V2.1 audit closure (Codex LOW-1, 2026-05-22): error wording
   555	            // standardized. A "spent LoopbackPair" throws at takeA/takeB
   556	            // BEFORE producing a Transport — so a consumed Transport
   557	            // wrapper must have come from attachTransport (or a future
   558	            // consumer added in V2.3+).
   559	            Error::from_reason("Transport has already been consumed by attachTransport".to_string())
   560	        })?;

exec
/bin/zsh -lc "nl -ba quantbook-engine/crates/ql-bindings-node/src/lib.rs | sed -n '560,980p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
   560	        })?;
   561	        // attach_transport_boxed handles baseline-reset + replacement.
   562	        // The Option<Box> it returns is the PRIOR transport; we drop
   563	        // it here intentionally (see method docstring).
   564	        let mut inner = self.inner.lock();
   565	        let _prior = inner.attach_transport_boxed(boxed);
   566	        Ok(())
   567	    }
   568	
   569	    /// Detach the currently-attached transport. Returns `true` if one
   570	    /// was attached (now released), `false` if there was nothing to
   571	    /// detach.
   572	    ///
   573	    /// **Background-task lifecycle (V2 V3 step 5 closure)**: detaching
   574	    /// a `WebSocketTransport` (V2.3+) drops its reader + writer tasks.
   575	    /// V2.1's LoopbackTransport has no background tasks; detach is
   576	    /// pure memory release.
   577	    ///
   578	    /// The returned `Box<dyn Transport>` is dropped Rust-side. JS does
   579	    /// NOT receive the prior transport — same rationale as
   580	    /// `attachTransport`.
   581	    #[napi(js_name = "detachTransport")]
   582	    pub fn detach_transport(&self) -> bool {
   583	        let mut inner = self.inner.lock();
   584	        inner.detach_transport().is_some()
   585	    }
   586	
   587	    /// `true` iff a transport is currently attached.
   588	    #[napi(js_name = "hasTransport")]
   589	    pub fn has_transport(&self) -> bool {
   590	        let inner = self.inner.lock();
   591	        inner.has_transport()
   592	    }
   593	
   594	    /// Full-snapshot flush to the attached transport. Sends the entire
   595	    /// op log as bytes. Returns `true` if bytes were actually sent.
   596	    ///
   597	    /// **Use `flushDeltaToTransport` instead in production** —
   598	    /// full-snapshot flushes get expensive as the op log grows. V2.1
   599	    /// exposes this method primarily for tests and "initial sync"
   600	    /// scenarios; V2.2 ships the delta path.
   601	    ///
   602	    /// **Failure modes**:
   603	    /// - No transport attached → returns `Ok(false)` (NOT an error).
   604	    /// - Transport's `send` returns `Err(TransportError::*)` → JS Error.
   605	    #[napi(js_name = "flushToTransport")]
   606	    pub fn flush_to_transport(&self) -> Result<bool> {
   607	        let mut inner = self.inner.lock();
   608	        inner
   609	            .flush_to_transport()
   610	            .map_err(collab_session_error_to_napi)
   611	    }
   612	
   613	    /// Drain inbound BLOBS from the attached transport (single-pass).
   614	    /// Returns the number of BLOBS drained (NOT the number of ops),
   615	    /// capped by the engine's default poll limit (`DEFAULT_POLL_REMOTE_LIMIT
   616	    /// = 64`). Each blob is one snapshot/delta that may contain many ops;
   617	    /// to count ops, compare `opCount()` before vs after.
   618	    ///
   619	    /// **V2.1 audit closure (Codex MEDIUM-1, 2026-05-22)**: the prior
   620	    /// docstring claimed "number of ops merged" which was wrong — the
   621	    /// engine's `poll_remote_with_limit` returns blob count (`merged <=
   622	    /// max_blobs` per its own doc). Mocha test "three-mutation chain
   623	    /// across LoopbackPair" found this empirically (asserting `>= 3`
   624	    /// failed against the actual blob-count semantics). The variable
   625	    /// names + JSDoc here now use "blobs" consistently.
   626	    ///
   627	    /// V2.1 ships the un-limited variant (engine default cap = 64).
   628	    /// `pollRemoteWithLimit(limit)` lands in V2.2.
   629	    ///
   630	    /// **Auto-flush integration (V2 V3 step 2)**: when AutoFlushPolicy
   631	    /// is `OnAppend` and `blobs_drained > 0`, the underlying call also
   632	    /// fires one delta flush back through the transport (idempotency
   633	    /// guard prevents echo loops). V2.1 doesn't bind AutoFlushPolicy yet
   634	    /// — V2.2 does.
   635	    ///
   636	    /// **Failure modes**:
   637	    /// - No transport attached → returns `Ok(0)` (NOT an error).
   638	    /// - Transport's `try_recv` returns `Err` → JS Error.
   639	    /// - Merging the bytes returns Err → JS Error.
   640	    #[napi(js_name = "pollRemote")]
   641	    pub fn poll_remote(&self) -> Result<u32> {
   642	        let mut inner = self.inner.lock();
   643	        let blobs_drained = inner.poll_remote().map_err(collab_session_error_to_napi)?;
   644	        Ok(u32::try_from(blobs_drained).unwrap_or(u32::MAX))
   645	    }
   646	
   647	    // ==========================================================
   648	    // Phase 5.7 V2.2 (2026-05-22) — full sync Transport surface
   649	    // ==========================================================
   650	
   651	    /// Delta flush to the attached transport. Sends ONLY the ops added
   652	    /// since the last successful flush (via Loro's `ExportMode::Updates`
   653	    /// from this session's `last_flushed_vv`). Returns `true` if bytes
   654	    /// were actually sent.
   655	    ///
   656	    /// **This is the production default**; prefer it over
   657	    /// `flushToTransport` (full-snapshot) which is O(full state). Delta
   658	    /// flushes are O(per-op delta).
   659	    ///
   660	    /// **Idempotency short-circuit**: if `last_flushed_vv == Some(current)`
   661	    /// — i.e., a previous flush already advanced the baseline to the
   662	    /// current state — returns `Ok(false)` without invoking
   663	    /// `transport.send`. Closes the V2 V2 audit echo-loop concern.
   664	    ///
   665	    /// **First flush after attach ALWAYS sends** even on an empty op
   666	    /// log: attach resets `last_flushed_vv = None`, so the idempotency
   667	    /// guard (which checks `Some(last_vv) == current_vv`) is bypassed.
   668	    /// The first call encodes from the empty VV — for an empty op log
   669	    /// this is a small baseline blob; for a non-empty log it's the
   670	    /// full state. V2.2 mocha test
   671	    /// `flushDeltaToTransport second call with no state change
   672	    /// short-circuits to false` pins this exact contract.
   673	    ///
   674	    /// **Per Phase 5.5 V2 V3 step 1 contract**: attach_transport resets
   675	    /// the per-session-per-transport `last_flushed_vv` to None. The
   676	    /// next call to `flushDeltaToTransport` after an attach sends from
   677	    /// the empty VV — delivering ALL local ops including any appended
   678	    /// while offline (Loro's CRDT op log IS the implicit offline queue).
   679	    ///
   680	    /// **Failure modes**:
   681	    /// - No transport attached → returns `Ok(false)` (NOT an error).
   682	    /// - Transport's `send` returns `Err(TransportError::*)` → JS Error.
   683	    #[napi(js_name = "flushDeltaToTransport")]
   684	    pub fn flush_delta_to_transport(&self) -> Result<bool> {
   685	        let mut inner = self.inner.lock();
   686	        inner
   687	            .flush_delta_to_transport()
   688	            .map_err(collab_session_error_to_napi)
   689	    }
   690	
   691	    /// Like `pollRemote` but with an explicit cap on the number of
   692	    /// blobs to drain per call. Returns the number of BLOBS drained
   693	    /// (NOT ops), `<= limit`.
   694	    ///
   695	    /// **Limit semantics** (per engine docstring):
   696	    /// - `limit == 0` → no-op, returns `Ok(0)` even if blobs queued.
   697	    /// - If the returned count equals `limit`, more blobs may still
   698	    ///   be queued — call again.
   699	    /// - If less, the queue drained (either empty or transport
   700	    ///   reported `Closed`).
   701	    ///
   702	    /// **Input validation** (per V1 megaudit closure pattern):
   703	    /// `limit` takes `f64` to avoid napi-rs's `napi_get_value_uint32`
   704	    /// ECMAScript ToUint32 silent coercion (`-1 → u32::MAX`, etc.).
   705	    /// Validated finite + non-negative + integer + in `usize` range
   706	    /// (on 64-bit systems usize = u64; we cap at u32::MAX for cross-
   707	    /// platform safety).
   708	    ///
   709	    /// **Failure modes**:
   710	    /// - No transport attached → returns `Ok(0)`.
   711	    /// - `limit` not a finite non-negative integer in u32 range → JS Error.
   712	    /// - Transport's `try_recv` returns `Err` → JS Error.
   713	    #[napi(js_name = "pollRemoteWithLimit")]
   714	    pub fn poll_remote_with_limit(&self, limit: f64) -> Result<u32> {
   715	        let limit_u32 = validate_u32_index("pollRemoteWithLimit", "limit", limit)?;
   716	        let mut inner = self.inner.lock();
   717	        let blobs_drained = inner
   718	            .poll_remote_with_limit(limit_u32 as usize)
   719	            .map_err(collab_session_error_to_napi)?;
   720	        Ok(u32::try_from(blobs_drained).unwrap_or(u32::MAX))
   721	    }
   722	
   723	    /// Returns the attached transport's most recent error message, or
   724	    /// `null` if either no transport is attached OR the transport
   725	    /// reports no error.
   726	    ///
   727	    /// **Use case**: IDE reconnect handshakes — after a mutator or
   728	    /// `flush_*_to_transport` returns a JS Error containing
   729	    /// "transport closed" (or similar), the IDE can call this to
   730	    /// distinguish underlying causes (`"peer reset"` vs
   731	    /// `"capacity exceeded"` for `WebSocketTransport`, etc.) and pick
   732	    /// the right retry strategy.
   733	    ///
   734	    /// **V2.2 audit-deferred caveat (Opus MEDIUM-3, 2026-05-22)**:
   735	    /// the engine surfaces errors as `Display` strings (lossy
   736	    /// projection of the underlying enum discriminant). IDE callers
   737	    /// today must substring-match to distinguish error categories.
   738	    /// V2.3+ will add structured discrimination via napi `Error::with_code`
   739	    /// (`#[napi(constructor)]` on a `TransportClosedError` etc.).
   740	    /// For V2.2 the string is the contract.
   741	    #[napi(js_name = "transportLastError")]
   742	    pub fn transport_last_error(&self) -> Option<String> {
   743	        let inner = self.inner.lock();
   744	        inner.transport_last_error()
   745	    }
   746	
   747	    /// Set the auto-flush policy. Returns the prior policy.
   748	    ///
   749	    /// **Accepted strings**: `"disabled"` (default) or `"onAppend"`.
   750	    /// Any other string returns a JS Error listing the accepted values.
   751	    ///
   752	    /// **`"onAppend"` semantics** (Phase 5.5 V2 V2 + V2 V3 steps 1+2):
   753	    /// every public mutator (`append_op`, `merge_bytes`,
   754	    /// `update_presence`, `clear_presence`, `sweep_presence`, `undo`,
   755	    /// `redo`, `poll_remote`, `poll_remote_with_limit`) auto-fires a
   756	    /// delta flush after the mutation. Idempotency short-circuits
   757	    /// no-op state changes; `poll_remote*` fires the flush once after
   758	    /// the batch (not per-blob).
   759	    ///
   760	    /// **Why string union over enum**: napi-rs binds Rust enums as
   761	    /// JS objects with a discriminant field, which is verbose for
   762	    /// 2-variant enums. The string-union approach is JS-idiomatic and
   763	    /// easy to test. Forward-compat: the engine's `AutoFlushPolicy`
   764	    /// is `#[non_exhaustive]` so future variants won't break this
   765	    /// binding — they'll just be unparseable by this method until V2.3+
   766	    /// adds string mappings.
   767	    ///
   768	    /// **Accepted alias forms (engine-side leniency)**: the engine
   769	    /// parser also accepts `"Disabled"`, `"OnAppend"`, `"on-append"`
   770	    /// as aliases for the canonical camelCase. The return value is
   771	    /// always canonical camelCase. JS callers SHOULD pass the
   772	    /// canonical forms (`"disabled"` / `"onAppend"`) only; the IDE-side
   773	    /// `isAutoFlushPolicy` type guard enforces this on the TS side
   774	    /// (rejects aliases). The lenient parser exists for engine-
   775	    /// internal callers and config-file backward-compat — not as a
   776	    /// public IDE contract. V2.2 audit closure (Opus HIGH-2): the
   777	    /// alias-acceptance is NOT pinned by the IDE mocha tests; engine-
   778	    /// side tests cover the parser.
   779	    ///
   780	    /// **Partial-state error contract (Codex MEDIUM-3 closure)**:
   781	    /// under `onAppend`, a mutator call sequence is "1. commit local
   782	    /// op → 2. flush to transport". If step 2 throws
   783	    /// `Error("transport closed")`, the local op is ALREADY committed
   784	    /// (step 1 succeeded). IDE retry logic must:
   785	    /// - NOT retry the original mutation (would duplicate the op).
   786	    /// - Reconnect the transport + call `flushDeltaToTransport`
   787	    ///   explicitly to flush the pending op.
   788	    /// V2.3+ will add structured `Error.code` discrimination so this
   789	    /// retry-shape is machine-readable; until then, the IDE must
   790	    /// substring-match `"transport"` in the error message.
   791	    #[napi(js_name = "setAutoFlushPolicy")]
   792	    pub fn set_auto_flush_policy(&self, policy: String) -> Result<String> {
   793	        let policy_enum = parse_auto_flush_policy(&policy)?;
   794	        let mut inner = self.inner.lock();
   795	        let prior = inner.set_auto_flush_policy(policy_enum);
   796	        // V2.2 audit closure (Opus HIGH-1): auto_flush_policy_to_string
   797	        // now returns Result and throws on unknown variants. Propagate
   798	        // the Result.
   799	        auto_flush_policy_to_string(prior)
   800	    }
   801	
   802	    /// Read the current auto-flush policy. Returns one of
   803	    /// `"disabled"` or `"onAppend"` (see `setAutoFlushPolicy`).
   804	    ///
   805	    /// **V2.2 audit closure (Opus HIGH-1, 2026-05-22)**: throws JS Error
   806	    /// instead of returning a `'unknown'` sentinel when the engine
   807	    /// reports a variant unknown to this binding (forward-compat skew
   808	    /// between engine and binding crate versions). Per CLAUDE.md
   809	    /// no-fallback rule: silent fall-through to `'unknown'` would
   810	    /// cause JS code `policy === 'onAppend'` to silently take the
   811	    /// `disabled` branch — corrupting reconnect logic. Throwing
   812	    /// surfaces the skew loudly and forces a binding upgrade.
   813	    #[napi(js_name = "autoFlushPolicy")]
   814	    pub fn auto_flush_policy(&self) -> Result<String> {
   815	        let inner = self.inner.lock();
   816	        auto_flush_policy_to_string(inner.auto_flush_policy())
   817	    }
   818	
   819	    // ==========================================================
   820	    // Phase 5.7 V2.5 (2026-05-22) — async Transport surface, V8-BLOCK CLOSED
   821	    // ==========================================================
   822	    //
   823	    // **V2.5 closure of Opus V2.4 HIGH-1 (V8-block UX hazard)**:
   824	    //
   825	    // V2.4 reintroduced `flushPendingToTransport` soundly (closed
   826	    // V2.3's UB + tokio-starvation HIGHs via the `Arc<Mutex<...>>`
   827	    // refactor + `spawn_blocking`). BUT it held `self.inner.lock()`
   828	    // during the Condvar wait — concurrent JS sync method calls on
   829	    // the SAME session blocked the V8 event loop on lock acquisition.
   830	    // Opus V2.4 HIGH-1 (`docs/audits/2026-05-22-phase-5-7-v2-4-opus.md:112-302`)
   831	    // documented this as a UX hazard (not a soundness hazard) and
   832	    // recommended Option A: split the lock acquisition from the
   833	    // Condvar wait via a detached ack handle.
   834	    //
   835	    // V2.5 implements Option A via:
   836	    //   1. Engine: `Transport::ack_handle(&self) -> Option<Box<dyn FlushAck + Send>>`
   837	    //      default-None trait method (`crates/ql-collab/src/transport.rs`).
   838	    //   2. Engine: `WebSocketTransport::ack_handle` override returning
   839	    //      a `WebSocketProgressAckHandle` cloning the transport's
   840	    //      Arc<(Mutex, Condvar)> progress state + capturing the
   841	    //      drain target at THIS call (Codex M1 contract).
   842	    //   3. Engine: `CollabSession::flush_pending_handle(&self)` proxy
   843	    //      (`crates/ql-collab/src/session.rs`).
   844	    //   4. Binding (this method): extract the handle under the session
   845	    //      lock, DROP the lock, then perform the wait without it held.
   846	    //
   847	    // Concurrent JS sync methods now acquire `self.inner.lock()`
   848	    // immediately — the V8 event loop stays responsive while the
   849	    // Condvar wait runs in the spawn_blocking task on cloned Arcs.
   850	
   851	    /// Async flush-pending — waits for the attached transport's writer
   852	    /// task to drain (level-1 local ack per V2 V4 V1 Tier K1 contract).
   853	    ///
   854	    /// Returns a JS `Promise<void>` that resolves when:
   855	    /// - the writer has completed `send` for every blob queued AT THIS
   856	    ///   CALL (Codex M1: target captured at handle extraction, NOT at
   857	    ///   wait-start), OR
   858	    /// - the transport has been detached / dropped / closed.
   859	    ///
   860	    /// **V2.5 V8-block CLOSURE** (Opus V2.4 HIGH-1, 2026-05-22):
   861	    ///
   862	    /// - Step 1: acquire session lock briefly, call
   863	    ///   `inner.flush_pending_handle()` which proxies to
   864	    ///   `Transport::ack_handle(&self)`. For `WebSocketTransport`,
   865	    ///   this captures `queued_count` as the drain target + clones
   866	    ///   the progress + closed Arcs.
   867	    /// - Step 2: DROP the session lock (`parking_lot::MutexGuard::drop`).
   868	    /// - Step 3: `tokio::task::spawn_blocking(move || handle.wait_for_drain())`.
   869	    ///   The wait runs on a tokio blocking thread, holding ONLY the
   870	    ///   handle's internal Arcs (NOT the session lock).
   871	    ///
   872	    /// Concurrent JS sync method calls on the same session (e.g.,
   873	    /// `opCount`, `appendPutValue`, `pollRemote`) acquire
   874	    /// `self.inner.lock()` immediately while this method's wait
   875	    /// runs — the V8 event loop stays responsive.
   876	    ///
   877	    /// **V2.3+V2.4 soundness retained** (UB + tokio-starvation HIGHs
   878	    /// remain closed):
   879	    /// - `&self`, not `&mut self`. napi-rs codegen produces shared
   880	    ///   `&'static CollabSession`; no aliasing UB possible.
   881	    /// - `spawn_blocking` runs on tokio's blocking pool (default
   882	    ///   512 threads), NOT the worker pool. Condvar wait doesn't
   883	    ///   occupy a worker.
   884	    ///
   885	    /// **Failure modes**:
   886	    /// - No transport attached, OR transport's `ack_handle` returns
   887	    ///   `None` (e.g., Loopback/Noop) → returns `Ok(())` immediately.
   888	    /// - Transport error during the wait → JS Error with the
   889	    ///   `TransportError` `Display` string.
   890	    /// - The `spawn_blocking` task panics → JS Error.
   891	    #[napi(js_name = "flushPendingToTransport")]
   892	    pub async fn flush_pending_to_transport(&self) -> Result<()> {
   893	        // **V2.5 lock-release pattern (Opus V2.4 HIGH-1 closure)**:
   894	        // extract the ack handle while holding the session lock,
   895	        // then RELEASE the lock before awaiting the wait. The
   896	        // scoped lock guard drops on the closing brace per
   897	        // parking_lot's standard MutexGuard semantics
   898	        // (`lock_api-0.4.14/src/mutex.rs::MutexGuard::drop`).
   899	        let handle_opt: Option<Box<dyn ql_collab::FlushAck + Send>> = {
   900	            let inner = self.inner.lock();
   901	            inner.flush_pending_handle()
   902	        };
   903	
   904	        // No transport attached (or transport has no ack semantics —
   905	        // Loopback/Noop default impl returns None). The contract
   906	        // matches the V2.4 method: `Ok(())`, NOT a rejection.
   907	        let Some(handle) = handle_opt else {
   908	            return Ok(());
   909	        };
   910	
   911	        // Wait on the detached handle. The Condvar wait runs on
   912	        // tokio's blocking pool; the session lock is NOT held.
   913	        tokio::task::spawn_blocking(move || handle.wait_for_drain())
   914	            .await
   915	            .map_err(|e| Error::from_reason(format!("flushPendingToTransport task: {e}")))?
   916	            .map_err(transport_error_to_napi)
   917	    }
   918	}
   919	
   920	/// Phase 5.7 V2.2 (2026-05-22) — parse a JS string into
   921	/// [`CoreAutoFlushPolicy`]. JS-idiomatic camelCase + lowercase aliases
   922	/// accepted. Rejects unknown strings with a precise error.
   923	fn parse_auto_flush_policy(s: &str) -> Result<CoreAutoFlushPolicy> {
   924	    match s {
   925	        "disabled" | "Disabled" => Ok(CoreAutoFlushPolicy::Disabled),
   926	        "onAppend" | "on-append" | "OnAppend" => Ok(CoreAutoFlushPolicy::OnAppend),
   927	        other => Err(Error::from_reason(format!(
   928	            "AutoFlushPolicy must be 'disabled' or 'onAppend', got {other:?}"
   929	        ))),
   930	    }
   931	}
   932	
   933	/// Phase 5.7 V2.2 (2026-05-22) — render [`CoreAutoFlushPolicy`] as a JS
   934	/// string. Uses canonical camelCase form ("disabled", "onAppend").
   935	///
   936	/// **V2.2 audit closure (Opus HIGH-1, 2026-05-22)**: returns `Result`
   937	/// and throws a JS Error when the engine reports a variant unknown to
   938	/// this binding (forward-compat skew). The prior version returned a
   939	/// `'unknown'` sentinel string — a silent fall-through that violated
   940	/// CLAUDE.md's no-fallback rule. JS code doing `policy === 'onAppend'`
   941	/// would silently miss the new variant and fall through to the
   942	/// `disabled` branch, corrupting reconnect / sync logic. Throwing is
   943	/// the correct fail-loudly path: an engine that ships a new variant
   944	/// ahead of the binding being upgraded surfaces the skew on first
   945	/// `autoFlushPolicy()` call.
   946	///
   947	/// The engine's `AutoFlushPolicy` is `#[non_exhaustive]` so V2.3+ must
   948	/// add new variants here when the engine ships them. The compile-time
   949	/// match-exhaustiveness check helps but doesn't catch new variants
   950	/// (because `#[non_exhaustive]` forces the catch-all arm).
   951	fn auto_flush_policy_to_string(p: CoreAutoFlushPolicy) -> Result<String> {
   952	    match p {
   953	        CoreAutoFlushPolicy::Disabled => Ok("disabled".to_string()),
   954	        CoreAutoFlushPolicy::OnAppend => Ok("onAppend".to_string()),
   955	        // Throw on unknown variant rather than silently returning a
   956	        // sentinel. See V2.2 audit closure note above for rationale.
   957	        other => Err(Error::from_reason(format!(
   958	            "autoFlushPolicy: engine reported unknown variant {other:?} — \
   959	             this binding crate ({}) is older than the engine; upgrade \
   960	             ql-bindings-node to add the new variant's JS string mapping",
   961	            env!("CARGO_PKG_VERSION"),
   962	        ))),
   963	    }
   964	}
   965	
   966	// =============================================================
   967	// Phase 5.7 V2.1 (2026-05-22) — Transport binding
   968	// =============================================================
   969	
   970	/// JS-facing opaque wrapper for a `Box<dyn ql_collab::Transport + Send>`.
   971	///
   972	/// **Design rationale (Phase 5.7 V1 megaudit Opus-B HIGH-2 closure,
   973	/// 2026-05-22)**: napi-rs cannot bind generic functions. The engine's
   974	/// `CollabSession::attach_transport<T>` accepts any `T: Transport + Send +
   975	/// 'static`. To make this reachable from JS we:
   976	///   1. Added `CollabSession::attach_transport_boxed(Box<dyn Transport + Send>)`
   977	///      on the Rust side as a sibling entry point (V2.1 cycle).
   978	///   2. Expose this opaque `Transport` class wrapping the boxed trait
   979	///      object. JS code obtains instances from static factories
   980	///      (`Transport.loopbackPair()` here; `Transport.websocketConnect(url)`

exec
/bin/zsh -lc "nl -ba quantbook-engine/crates/ql-bindings-node/src/lib.rs | sed -n '980,1160p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
   980	///      (`Transport.loopbackPair()` here; `Transport.websocketConnect(url)`
   981	///      in V2.3) and passes them to `CollabSession.attachTransport(t)`.
   982	///
   983	/// **Single-use semantics**: an instance owns its boxed trait object.
   984	/// `CollabSession.attachTransport` MOVES the box out of this wrapper,
   985	/// leaving it consumed. Subsequent attach calls with the same wrapper
   986	/// return a JS Error ("Transport has already been consumed by attachTransport").
   987	/// This mirrors Rust ownership semantics within the constraint that JS
   988	/// doesn't have a move primitive.
   989	///
   990	/// **Why opaque (no introspection methods)**: the inner `Box<dyn Transport>`
   991	/// is a fat pointer to one of several impls (Loopback, WebSocket, ...).
   992	/// V2.1 doesn't expose impl-specific accessors; future versions can add
   993	/// `kind() -> string` if a use case emerges.
   994	#[napi]
   995	pub struct Transport {
   996	    // `Option` so we can move the inner Box out at attach time without
   997	    // dropping the wrapper. napi-rs's class instance has `&mut self`
   998	    // discipline (per V1 module docs Send+!Sync rationale) — we cannot
   999	    // consume `self` from a `#[napi]` method, only take from an Option.
  1000	    inner: Option<Box<dyn CoreTransport + Send>>,
  1001	}
  1002	
  1003	#[napi]
  1004	impl Transport {
  1005	    /// `true` while this wrapper still owns its inner transport (not yet
  1006	    /// passed to `attachTransport`). Useful for IDE code that wants to
  1007	    /// branch on whether a Transport instance is still usable without
  1008	    /// catching an exception.
  1009	    #[napi(js_name = "isAttachable")]
  1010	    pub fn is_attachable(&self) -> bool {
  1011	        self.inner.is_some()
  1012	    }
  1013	
  1014	    // ==========================================================
  1015	    // Phase 5.7 V2.3 (2026-05-22) — async WebSocket factory
  1016	    // ==========================================================
  1017	
  1018	    /// Connect to a WebSocket peer and return a Transport wrapping the
  1019	    /// resulting `WebSocketTransport`. Returns a JS `Promise<Transport>`.
  1020	    ///
  1021	    /// **URL format**: standard `ws://host:port` (no TLS in V2.3 — V2.4+
  1022	    /// will add `wss://` once the engine's `ql-collab-ws` exposes TLS).
  1023	    ///
  1024	    /// **Failure modes** (all surface as JS Promise rejections):
  1025	    /// - `WebSocketError::InvalidUrl` — URL parse failed.
  1026	    /// - `WebSocketError::ConnectFailed` — TCP/DNS error.
  1027	    /// - `WebSocketError::HandshakeFailed` — WS upgrade rejected
  1028	    ///   (e.g., HTTP 401, protocol mismatch).
  1029	    ///
  1030	    /// **Lifecycle**: the returned Transport instance owns a
  1031	    /// `WebSocketTransport` with two background tokio tasks
  1032	    /// (reader + writer). On detach, the tasks are aborted via the
  1033	    /// engine's `WebSocketTransport::Drop`. Don't hold the Transport
  1034	    /// past detach (V2 V3 step 5 megaudit closure pattern).
  1035	    ///
  1036	    /// **napi-rs async pattern**: `#[napi]` on `async fn` runs the
  1037	    /// body on a tokio task spawned from napi-rs's built-in runtime
  1038	    /// (gated by the `async` feature in the workspace `Cargo.toml`).
  1039	    /// The Promise resolves on the V8 thread once the async fn
  1040	    /// completes — no manual ThreadsafeFunction plumbing needed.
  1041	    #[napi(js_name = "websocketConnect")]
  1042	    pub async fn websocket_connect(url: String) -> Result<Transport> {
  1043	        let ws = WebSocketTransport::connect(&url)
  1044	            .await
  1045	            .map_err(websocket_error_to_napi)?;
  1046	        Ok(Transport {
  1047	            inner: Some(Box::new(ws)),
  1048	        })
  1049	    }
  1050	}
  1051	
  1052	impl Transport {
  1053	    /// **`pub(crate)` — only the napi `CollabSession::attach_transport`
  1054	    /// method should call this.** Move the inner Box out for attach.
  1055	    /// Called by the napi method, NOT the Rust generic
  1056	    /// `ql_collab::CollabSession::attach_transport`. After this returns
  1057	    /// `Some`, the wrapper is consumed; subsequent `is_attachable`
  1058	    /// returns `false` and the inner box has been handed off.
  1059	    ///
  1060	    /// **V2.1 audit closure (Opus LOW-4, 2026-05-22)**: the prior
  1061	    /// docstring said "Rust-only" which could be misread as private to
  1062	    /// this `impl` block. `pub(crate)` IS the visibility — but calling
  1063	    /// from any site other than `CollabSession::attach_transport`
  1064	    /// silently consumes the wrapper without an `attach`-paired side
  1065	    /// effect. Future contributors: do NOT call this from anywhere
  1066	    /// else.
  1067	    pub(crate) fn take_inner(&mut self) -> Option<Box<dyn CoreTransport + Send>> {
  1068	        self.inner.take()
  1069	    }
  1070	}
  1071	
  1072	/// Two-ended in-process Transport pair (LoopbackTransport).
  1073	///
  1074	/// JS callers use this as a stepping-stone to obtain the two `Transport`
  1075	/// instances that share a Loopback queue:
  1076	///
  1077	/// ```ignore
  1078	/// const pair = new LoopbackPair();
  1079	/// const transportA = pair.takeA();
  1080	/// const transportB = pair.takeB();
  1081	/// sessionA.attachTransport(transportA);
  1082	/// sessionB.attachTransport(transportB);
  1083	/// ```
  1084	///
  1085	/// **Why this intermediate class** (Phase 5.7 V2.1 design note,
  1086	/// 2026-05-22): napi-rs's `#[napi(factory, ...)]` can only return a
  1087	/// single `Self`-typed class instance. Returning a 2-tuple or
  1088	/// `Vec<Transport>` fails the trait bound `ObjectFinalize` napi-rs
  1089	/// imposes on factory returns. The cleanest pattern that keeps each
  1090	/// `Transport` end as a separately-attachable class is to expose the
  1091	/// pair-creation step as its own short-lived class with two takers.
  1092	///
  1093	/// **Single-use takers**: each of `takeA` / `takeB` can be called once
  1094	/// per `LoopbackPair` instance. Calling a second time on the same end
  1095	/// returns a JS Error. (Calling a non-yet-taken end after the other has
  1096	/// been taken still works — the two ends are independent.)
  1097	#[napi]
  1098	pub struct LoopbackPair {
  1099	    // Both ends live here until taken. The pair is constructed eagerly
  1100	    // in `new()` so the shared queue exists before either end is
  1101	    // distributed.
  1102	    a: Option<LoopbackTransport>,
  1103	    b: Option<LoopbackTransport>,
  1104	}
  1105	
  1106	impl Default for LoopbackPair {
  1107	    fn default() -> Self {
  1108	        Self::new()
  1109	    }
  1110	}
  1111	
  1112	#[napi]
  1113	impl LoopbackPair {
  1114	    /// Construct a fresh Loopback pair. Both ends start un-taken.
  1115	    #[napi(constructor)]
  1116	    pub fn new() -> Self {
  1117	        let (a, b) = LoopbackTransport::pair();
  1118	        Self {
  1119	            a: Some(a),
  1120	            b: Some(b),
  1121	        }
  1122	    }
  1123	
  1124	    /// Take ownership of end A. Errors if already taken.
  1125	    #[napi(js_name = "takeA")]
  1126	    pub fn take_a(&mut self) -> Result<Transport> {
  1127	        let end = self.a.take().ok_or_else(|| {
  1128	            Error::from_reason("LoopbackPair.takeA already called on this pair".to_string())
  1129	        })?;
  1130	        Ok(Transport {
  1131	            inner: Some(Box::new(end)),
  1132	        })
  1133	    }
  1134	
  1135	    /// Take ownership of end B. Errors if already taken.
  1136	    #[napi(js_name = "takeB")]
  1137	    pub fn take_b(&mut self) -> Result<Transport> {
  1138	        let end = self.b.take().ok_or_else(|| {
  1139	            Error::from_reason("LoopbackPair.takeB already called on this pair".to_string())
  1140	        })?;
  1141	        Ok(Transport {
  1142	            inner: Some(Box::new(end)),
  1143	        })
  1144	    }
  1145	}
  1146	
  1147	// =============================================================
  1148	// Phase 5.7 V2.6 (2026-05-22) — BlockingTransportFixture
  1149	// =============================================================
  1150	//
  1151	// Test fixture for V2.5 contract testing (Opus V2.4 MEDIUM-2
  1152	// closure). JS-side helper that constructs a `BlockingTransport`
  1153	// (engine-side, feature-gated) and exposes:
  1154	//   - `takeTransport(): Transport` — moves the fixture's inner
  1155	//     BlockingTransport into a Transport wrapper that can be passed
  1156	//     to `attachTransport(t)`. Mirrors V2.1 `LoopbackPair`'s
  1157	//     `takeA`/`takeB` single-use pattern.
  1158	//   - `release(): void` — flips the engine-side release Condvar
  1159	//     so any in-progress `flush_pending` / `wait_for_drain` exits.
  1160	//   - `waitUntilBlocked(): Promise<void>` — async wait until the

exec
/bin/zsh -lc "nl -ba quantbook-engine/crates/ql-bindings-node/src/lib.rs | sed -n '1160,1370p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
  1160	//   - `waitUntilBlocked(): Promise<void>` — async wait until the
  1161	//     fixture's wait routine has actually entered the Condvar
  1162	//     wait. Closes Codex M3 (deterministic synchronization for the
  1163	//     V2.5 contract test — without this, the test's `opCount()`
  1164	//     call could race ahead of the spawn_blocking task and pass
  1165	//     vacuously).
  1166	//
  1167	// Codex M2 fix: this is NOT exposed as `new BlockingTransport(...)`.
  1168	// The napi `attachTransport(t: &mut Transport)` only accepts the
  1169	// opaque `Transport` wrapper class; a separately-constructed
  1170	// `BlockingTransport` napi class would not be attachable. The
  1171	// fixture-controller pattern is the closest analog to V2.1's
  1172	// `LoopbackPair`.
  1173	
  1174	/// **Phase 5.7 V2.6 (2026-05-22) — V2.5 contract-test fixture.**
  1175	///
  1176	/// JS class for constructing a `BlockingTransport` (engine-side
  1177	/// `ql_collab::BlockingTransport`, gated behind the `test-fixtures`
  1178	/// feature) and controlling its `release` + `blocked` Condvars from
  1179	/// JS. Used by IDE mocha contention tests to verify the V2.5
  1180	/// V8-block closure: a sync session method called concurrently with
  1181	/// a pending `flushPendingToTransport` MUST return immediately
  1182	/// (does NOT block on the session lock).
  1183	///
  1184	/// **Not for production use** — the underlying `BlockingTransport`
  1185	/// blocks `flush_pending` indefinitely until `release()` is called
  1186	/// (or the constructor's `block_ms` upper bound elapses).
  1187	#[napi]
  1188	pub struct BlockingTransportFixture {
  1189	    /// The fixture's inner `BlockingTransport`, taken once via
  1190	    /// `takeTransport()`. After taking, the fixture controller
  1191	    /// still owns the `release` + `blocked` Arcs so it can drive
  1192	    /// JS-side `release()` + `waitUntilBlocked()` against the
  1193	    /// transport that now lives inside a `CollabSession`.
  1194	    inner: Option<ql_collab::BlockingTransport>,
  1195	    /// Shared with the inner `BlockingTransport`. Flipped to `true`
  1196	    /// + notified by `release()`.
  1197	    release: std::sync::Arc<(std::sync::Mutex<bool>, std::sync::Condvar)>,
  1198	    /// Shared with the inner `BlockingTransport`. Set + notified by
  1199	    /// the transport when it enters the wait. `waitUntilBlocked()`
  1200	    /// awaits this signal.
  1201	    blocked: std::sync::Arc<(std::sync::Mutex<bool>, std::sync::Condvar)>,
  1202	}
  1203	
  1204	#[napi]
  1205	impl BlockingTransportFixture {
  1206	    /// JS: `new BlockingTransportFixture(blockMs: number)`.
  1207	    ///
  1208	    /// `blockMs` is the upper-bound wait duration. Must be in the
  1209	    /// range `[1, u32::MAX]` — a strictly positive finite integer.
  1210	    ///
  1211	    /// **V2.5 audit closure (Codex MEDIUM-1, 2026-05-22)**: `0` is
  1212	    /// REJECTED at the napi boundary even though the engine's
  1213	    /// `BlockingTransport` accepts `0` as "wait indefinitely" for
  1214	    /// Rust unit tests. Rationale: this napi class ships in every
  1215	    /// production cdylib (gated only by the `test-fixtures` Cargo
  1216	    /// feature on `ql-collab`, enabled unconditionally by
  1217	    /// `ql-bindings-node`). Allowing JS callers to construct an
  1218	    /// indefinite-block fixture would let in-process IDE code park
  1219	    /// `flushPendingToTransport` blocking-pool tasks until process
  1220	    /// termination — bounded self-DoS but unnecessary footgun. The
  1221	    /// engine `BlockingTransport::new(0, ..., ...)` constructor
  1222	    /// remains for Rust tests that explicitly want the `0` path.
  1223	    ///
  1224	    /// `blockMs` is taken as `f64` (per the V1 megaudit Codex H1
  1225	    /// ToUint32-hygiene pattern) and validated via
  1226	    /// `validate_u32_index` to reject non-finite, negative,
  1227	    /// fractional, or out-of-u32 values; then additionally checked
  1228	    /// `> 0` per the V2.5 closure above.
  1229	    ///
  1230	    /// V2 backlog (Opus V2.5 LOW-2): future production hardening
  1231	    /// will gate this entire napi class behind a `ql-bindings-node`-side
  1232	    /// feature, stripping it from production cdylib builds. The
  1233	    /// `block_ms > 0` check here is a defense-in-depth complement.
  1234	    #[napi(constructor)]
  1235	    pub fn new(block_ms: f64) -> Result<Self> {
  1236	        let block_ms_u32 = validate_u32_index("BlockingTransportFixture", "blockMs", block_ms)?;
  1237	        if block_ms_u32 == 0 {
  1238	            return Err(Error::from_reason(
  1239	                "BlockingTransportFixture: blockMs must be > 0 (strictly positive). \
  1240	                 Zero would allow indefinite blocking from JS — V2.5 audit closure \
  1241	                 (Codex MEDIUM-1) rejects this at the napi boundary."
  1242	                    .to_string(),
  1243	            ));
  1244	        }
  1245	        let release =
  1246	            std::sync::Arc::new((std::sync::Mutex::new(false), std::sync::Condvar::new()));
  1247	        let blocked =
  1248	            std::sync::Arc::new((std::sync::Mutex::new(false), std::sync::Condvar::new()));
  1249	        let transport = ql_collab::BlockingTransport::new(
  1250	            block_ms_u32 as u64,
  1251	            std::sync::Arc::clone(&release),
  1252	            std::sync::Arc::clone(&blocked),
  1253	        );
  1254	        Ok(Self {
  1255	            inner: Some(transport),
  1256	            release,
  1257	            blocked,
  1258	        })
  1259	    }
  1260	
  1261	    /// Take ownership of the inner `BlockingTransport`, wrapped in
  1262	    /// a `Transport` for attaching to a session. Single-use:
  1263	    /// errors on the second call.
  1264	    ///
  1265	    /// After taking, the fixture controller retains its Arc clones
  1266	    /// of `release` + `blocked`, so JS-side `release()` +
  1267	    /// `waitUntilBlocked()` still drive the transport that now
  1268	    /// lives inside the session.
  1269	    #[napi(js_name = "takeTransport")]
  1270	    pub fn take_transport(&mut self) -> Result<Transport> {
  1271	        let t = self.inner.take().ok_or_else(|| {
  1272	            Error::from_reason(
  1273	                "BlockingTransportFixture.takeTransport already called on this fixture".to_string(),
  1274	            )
  1275	        })?;
  1276	        Ok(Transport {
  1277	            inner: Some(Box::new(t)),
  1278	        })
  1279	    }
  1280	
  1281	    /// Flip the release Condvar so any in-progress `flush_pending`
  1282	    /// or `wait_for_drain` exits. Idempotent (calling twice is
  1283	    /// safe; the flag is monotonic).
  1284	    #[napi]
  1285	    pub fn release(&self) {
  1286	        let (lock, cv) = &*self.release;
  1287	        // The std::sync::Mutex API; recover from poison via
  1288	        // PoisonError::into_inner so a panicked test thread doesn't
  1289	        // leave the fixture permanently broken.
  1290	        let mut released = match lock.lock() {
  1291	            Ok(g) => g,
  1292	            Err(poisoned) => poisoned.into_inner(),
  1293	        };
  1294	        *released = true;
  1295	        cv.notify_all();
  1296	    }
  1297	
  1298	    /// Async wait until the fixture's wait routine has actually
  1299	    /// entered the Condvar wait. Resolves when the engine-side
  1300	    /// transport's `wait_blocked` has set the `blocked` flag +
  1301	    /// notified.
  1302	    ///
  1303	    /// **Codex M3 fix (2026-05-22)**: this is the deterministic
  1304	    /// synchronization point that V2.5 contract tests need. Without
  1305	    /// it, an `opCount()` call could race ahead of the
  1306	    /// `flushPendingToTransport` task and pass vacuously
  1307	    /// (returning before the spawn_blocking task has even acquired
  1308	    /// the session lock + extracted the ack handle).
  1309	    ///
  1310	    /// Uses `spawn_blocking` because the inner wait is a sync
  1311	    /// `Condvar::wait` on `std::sync` primitives. Tokio-friendly:
  1312	    /// the wait runs on a blocking-pool thread, not a worker.
  1313	    #[napi(js_name = "waitUntilBlocked")]
  1314	    pub async fn wait_until_blocked(&self) -> Result<()> {
  1315	        let blocked = std::sync::Arc::clone(&self.blocked);
  1316	        tokio::task::spawn_blocking(move || -> std::result::Result<(), String> {
  1317	            let (lock, cv) = &*blocked;
  1318	            let mut flag = lock
  1319	                .lock()
  1320	                .map_err(|e| format!("blocked lock poisoned: {e}"))?;
  1321	            while !*flag {
  1322	                flag = cv
  1323	                    .wait(flag)
  1324	                    .map_err(|e| format!("blocked wait poisoned: {e}"))?;
  1325	            }
  1326	            Ok(())
  1327	        })
  1328	        .await
  1329	        .map_err(|e| Error::from_reason(format!("waitUntilBlocked task: {e}")))?
  1330	        .map_err(Error::from_reason)
  1331	    }
  1332	}
  1333	
  1334	// =============================================================
  1335	// Send/Sync compile assertions (Rule 4 application)
  1336	// =============================================================
  1337	
  1338	// **Phase 5.7 V2.4 (2026-05-22) — Send + Sync UPDATE.**
  1339	//
  1340	// V1 + V2.1 + V2.2 + V2.3 claimed `Send + !Sync` (per V1 megaudit
  1341	// Opus-A MEDIUM-1 closure). V2.4 changes the underlying shape from
  1342	// `inner: CoreCollabSession` to `inner: Arc<Mutex<CoreCollabSession>>`
  1343	// to close V2.3 audit's `&mut self` async aliasing UB hazard.
  1344	//
  1345	// **New Send + Sync result** (positive compile proof below):
  1346	// - `CoreCollabSession: Send + !Sync` (engine's claim, unchanged).
  1347	// - `Mutex<T>: Send + Sync` when `T: Send`. parking_lot's Mutex is
  1348	//   `Send + Sync` (no poison; pure atomic-CAS path uncontended).
  1349	// - `Arc<T>: Send + Sync` when `T: Send + Sync`.
  1350	// - Therefore `CollabSession { inner: Arc<Mutex<CoreCollabSession>> }`
  1351	//   is `Send + Sync`.
  1352	//
  1353	// This is a strict improvement over V1's `Send + !Sync`: V2.4 can
  1354	// be shared across tokio tasks (load-bearing for V2.4's
  1355	// `flushPendingToTransport` spawn_blocking pattern).
  1356	//
  1357	// Per audit-discipline Rule 4: positive Send + Sync proof below.
  1358	const _ASSERT_BINDING_COLLAB_SESSION_SEND: fn() = || {
  1359	    fn assert_send<T: Send>() {}
  1360	    fn assert_sync<T: Sync>() {}
  1361	    assert_send::<CollabSession>();
  1362	    // V2.4: CollabSession is now Sync (was !Sync pre-V2.4).
  1363	    assert_sync::<CollabSession>();
  1364	};
  1365	
  1366	// **Phase 5.7 V2.1 (2026-05-22) Rule 4 application for `Transport`**:
  1367	// The wrapper holds `Option<Box<dyn CoreTransport + Send>>`. The trait
  1368	// object itself is `Send` (we declare it `dyn CoreTransport + Send`), and
  1369	// `Box<_>` of a `Send` trait object is `Send`, and `Option<T>` is `Send`
  1370	// when `T: Send`. So the composition is `Send`. Pin it.

codex
The three `kind()` impls cover their current variants and the `CollabSessionError::Transport(inner)` arm is a direct passthrough. I’m now checking whether every napi engine-error path actually uses those helpers, and whether any stale docs still describe the old lossy string contract.
exec
/bin/zsh -lc "rg -n \"kind\\(\\)|transport_closed|websocket_invalid_url|session_undo|session_replay|session_presence|session_oplog|websocket_connect_failed|websocket_handshake_failed|websocket_runtime_error\" quantbook-engine/crates/ql-collab quantbook-engine/crates/ql-collab-ws quantbook-engine/crates/ql-bindings-node -g '*.rs'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
quantbook-engine/crates/ql-collab-ws/src/lib.rs:254:            WebSocketError::InvalidUrl(_) => "websocket_invalid_url",
quantbook-engine/crates/ql-collab-ws/src/lib.rs:255:            WebSocketError::ConnectFailed(_) => "websocket_connect_failed",
quantbook-engine/crates/ql-collab-ws/src/lib.rs:256:            WebSocketError::HandshakeFailed(_) => "websocket_handshake_failed",
quantbook-engine/crates/ql-collab-ws/src/lib.rs:257:            WebSocketError::RuntimeError(_) => "websocket_runtime_error",
quantbook-engine/crates/ql-collab-ws/src/lib.rs:950:            WebSocketError::InvalidUrl("x".into()).kind(),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:951:            "websocket_invalid_url"
quantbook-engine/crates/ql-collab-ws/src/lib.rs:954:            WebSocketError::ConnectFailed("x".into()).kind(),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:955:            "websocket_connect_failed"
quantbook-engine/crates/ql-collab-ws/src/lib.rs:958:            WebSocketError::HandshakeFailed("x".into()).kind(),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:959:            "websocket_handshake_failed"
quantbook-engine/crates/ql-collab-ws/src/lib.rs:962:            WebSocketError::RuntimeError("x".into()).kind(),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:963:            "websocket_runtime_error"
quantbook-engine/crates/ql-bindings-node/src/lib.rs:264://   1. Engine: each error enum gets a `kind() -> &'static str`
quantbook-engine/crates/ql-bindings-node/src/lib.rs:271://      `"[transport_closed] transport closed"`.
quantbook-engine/crates/ql-bindings-node/src/lib.rs:276://      logic can branch on `err.code === 'transport_closed'` etc.
quantbook-engine/crates/ql-bindings-node/src/lib.rs:280:// `/invalid WebSocket URL/` still match `"[websocket_invalid_url]
quantbook-engine/crates/ql-bindings-node/src/lib.rs:293:/// `"transport_closed"` instead of `"session_transport"`).
quantbook-engine/crates/ql-bindings-node/src/lib.rs:295:    Error::from_reason(format!("[{}] {e}", e.kind()))
quantbook-engine/crates/ql-bindings-node/src/lib.rs:302:    Error::from_reason(format!("[{}] {e}", e.kind()))
quantbook-engine/crates/ql-bindings-node/src/lib.rs:309:    Error::from_reason(format!("[{}] {e}", e.kind()))
quantbook-engine/crates/ql-bindings-node/src/lib.rs:993:/// `kind() -> string` if a use case emerges.
quantbook-engine/crates/ql-collab/src/transport.rs:135:    /// (their `code === 'transport_closed'` branches stop matching).
quantbook-engine/crates/ql-collab/src/transport.rs:141:            TransportError::Closed => "transport_closed",
quantbook-engine/crates/ql-collab/src/transport.rs:1109:        assert_eq!(e.kind(), "transport_io");
quantbook-engine/crates/ql-collab/src/transport.rs:1115:        assert_eq!(e.kind(), "transport_closed");
quantbook-engine/crates/ql-collab/src/session.rs:167:    /// kind (e.g. `"transport_closed"`) so JS callers can branch on
quantbook-engine/crates/ql-collab/src/session.rs:170:    /// directly (e.g. `"session_undo"`).
quantbook-engine/crates/ql-collab/src/session.rs:175:            CollabSessionError::OpLog(_) => "session_oplog",
quantbook-engine/crates/ql-collab/src/session.rs:176:            CollabSessionError::Presence(_) => "session_presence",
quantbook-engine/crates/ql-collab/src/session.rs:177:            CollabSessionError::Undo(_) => "session_undo",
quantbook-engine/crates/ql-collab/src/session.rs:178:            CollabSessionError::Transport(inner) => inner.kind(),
quantbook-engine/crates/ql-collab/src/session.rs:179:            CollabSessionError::Replay(_) => "session_replay",
quantbook-engine/crates/ql-collab/src/session.rs:2786:    fn flush_to_transport_errors_when_transport_closed() {
quantbook-engine/crates/ql-collab/src/session.rs:3088:        assert_eq!(e.kind(), "transport_closed");
quantbook-engine/crates/ql-collab/src/session.rs:3091:        assert_eq!(e.kind(), "transport_io");

exec
/bin/zsh -lc "rg -n \"substring|Error::with_code|Display string|structured discrimination|transportLastError|V2\\.3\\+\" quantbook-engine/crates/ql-bindings-node/src/lib.rs" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
260:// Display string.
278:// **V2.3 mocha test compatibility**: the existing Display strings
279:// remain intact AFTER the prefix, so substring-match tests like
283:// **Why not napi-rs `Error::with_code`?**: napi-rs 3.9.0's `Error`
545:    /// special-cased "detached prior" type. V2.3+ may revisit if a use
558:            // consumer added in V2.3+).
574:    /// a `WebSocketTransport` (V2.3+) drops its reader + writer tasks.
737:    /// today must substring-match to distinguish error categories.
738:    /// V2.3+ will add structured discrimination via napi `Error::with_code`
741:    #[napi(js_name = "transportLastError")]
765:    /// binding — they'll just be unparseable by this method until V2.3+
788:    /// V2.3+ will add structured `Error.code` discrimination so this
790:    /// substring-match `"transport"` in the error message.
877:    /// **V2.3+V2.4 soundness retained** (UB + tokio-starvation HIGHs
947:/// The engine's `AutoFlushPolicy` is `#[non_exhaustive]` so V2.3+ must
1384:// it would break the V2.3+ Transport.websocketConnect async pattern

exec
/bin/zsh -lc "rg -n \"Error::from_reason\\(format"'!'"\\(\\\"\\{e\\}\\\"\\)|\\.map_err\\(\\|e\\| Error::from_reason\\(format"'!'"\\(\\\"\\{e\\}\\\"\\)\\)\" quantbook-engine/crates/ql-bindings-node/src/lib.rs" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
259:// because `Error::from_reason(format!("{e}"))` only carries the

exec
/bin/zsh -lc "rg -n \"map_err\\(.*collab_session_error_to_napi|map_err\\(.*transport_error_to_napi|map_err\\(.*websocket_error_to_napi|CollabSessionError|TransportError|WebSocketError\" quantbook-engine/crates/ql-bindings-node/src/lib.rs" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
131:use ql_collab::CollabSessionError;
134:use ql_collab::TransportError;
135:use ql_collab_ws::WebSocketError;
265://      accessor (`crates/ql-collab/src/transport.rs::TransportError`,
266://      `crates/ql-collab-ws/src/lib.rs::WebSocketError`,
267://      `crates/ql-collab/src/session.rs::CollabSessionError`).
289:/// Map a [`CollabSessionError`] to a napi [`Error`] with a kind-
291:/// [`CollabSessionError::kind`]; for `Transport(inner)` it
294:fn collab_session_error_to_napi(e: CollabSessionError) -> Error {
298:/// Map a [`TransportError`] to a napi [`Error`] with a kind-
301:fn transport_error_to_napi(e: TransportError) -> Error {
305:/// Map a [`WebSocketError`] to a napi [`Error`] with a kind-
308:fn websocket_error_to_napi(e: WebSocketError) -> Error {
348:        let session = CoreCollabSession::new(pid).map_err(collab_session_error_to_napi)?;
360:            .map_err(collab_session_error_to_napi)?;
422:        inner.append_op(op).map_err(collab_session_error_to_napi)?;
431:        let bytes = inner.export_bytes().map_err(collab_session_error_to_napi)?;
473:            .map_err(collab_session_error_to_napi)?;
604:    /// - Transport's `send` returns `Err(TransportError::*)` → JS Error.
610:            .map_err(collab_session_error_to_napi)
643:        let blobs_drained = inner.poll_remote().map_err(collab_session_error_to_napi)?;
682:    /// - Transport's `send` returns `Err(TransportError::*)` → JS Error.
688:            .map_err(collab_session_error_to_napi)
719:            .map_err(collab_session_error_to_napi)?;
889:    ///   `TransportError` `Display` string.
916:            .map_err(transport_error_to_napi)
1025:    /// - `WebSocketError::InvalidUrl` — URL parse failed.
1026:    /// - `WebSocketError::ConnectFailed` — TCP/DNS error.
1027:    /// - `WebSocketError::HandshakeFailed` — WS upgrade rejected
1045:            .map_err(websocket_error_to_napi)?;

exec
/bin/zsh -lc "nl -ba /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts | sed -n '1,280p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
exec
/bin/zsh -lc "nl -ba /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts | sed -n '1,220p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
     1	/*---------------------------------------------------------------------------------------------
     2	 *  Copyright (c) Microsoft Corporation. All rights reserved.
     3	 *  Licensed under the MIT License. See License.txt in the project root for license information.
     4	 *--------------------------------------------------------------------------------------------*/
     5	
     6	/**
     7	 * Phase 5.7 V1 (2026-05-22) -- thin TypeScript wrapper over the napi
     8	 * `CollabSession` class.
     9	 *
    10	 * The wrapper exists so:
    11	 * 1. Consumer code imports a STABLE TS-side identifier
    12	 *    (`QuantbookSession`) instead of reaching into a dynamically-loaded
    13	 *    module. The loader is hot-pathed once at startup; the wrapper
    14	 *    holds the resolved native class.
    15	 * 2. We can attach JSDoc + IDE-side validation that the napi layer
    16	 *    doesn't (e.g., reject negative `sheet` integers before the BigInt
    17	 *    boundary makes the error less actionable).
    18	 * 3. V2 / V3 can grow the wrapper without changing the napi surface
    19	 *    (e.g., add async retry wrappers around `mergeBytes`).
    20	 *
    21	 * **Per V1 scope:** no Transport, no presence, no undo, no formula
    22	 * support. See engine repo's `docs/phase5/5-7-v1-exit-packet.md`
    23	 * section "V1 deferred to V2" for the full V1 deferred list
    24	 * (engine + IDE).
    25	 */
    26	
    27	import { loadQuantbookEngine } from './loader';
    28	import type {
    29		AutoFlushPolicy,
    30		CollabSessionInstance,
    31		LoopbackPairInstance,
    32		QuantbookErrorCode,
    33		QuantbookErrorInfo,
    34		TransportInstance,
    35	} from './types';
    36	
    37	/**
    38	 * Construct a fresh `CollabSession` for the given peer.
    39	 *
    40	 * @param peerId Non-zero u64-domain peer identifier. Throws if `0n`,
    41	 *               negative, or doesn't fit in u64.
    42	 */
    43	export function createSession(peerId: bigint): CollabSessionInstance {
    44		const engine = loadQuantbookEngine();
    45		return new engine.CollabSession(peerId);
    46	}
    47	
    48	/**
    49	 * Append a `PutValue` op with JS-side input validation that the napi
    50	 * layer cannot perform (due to ECMAScript ToUint32 coercion happening
    51	 * BEFORE the Rust code sees the value).
    52	 *
    53	 * **V1 audit closure (Opus H2 + Codex H3, 2026-05-22)**: napi-rs's
    54	 * `napi_get_value_uint32` applies ECMAScript ToUint32 to JS Number
    55	 * arguments. That converts:
    56	 *   - `-1` to `0xFFFFFFFF` (silent wrap)
    57	 *   - `NaN` to `0` (silent coercion)
    58	 *   - `Infinity` to `0` (silent coercion)
    59	 *   - `2.5` to `2` (silent floor-toward-zero)
    60	 *
    61	 * The Rust binding sees the post-ToUint32 value and has no way to
    62	 * detect the original JS-side intent. Validate at the TS layer
    63	 * BEFORE the call so each failure surfaces a precise error.
    64	 *
    65	 * @throws Error if sheet/row/col aren't non-negative integers in range,
    66	 *               or if value isn't a finite number.
    67	 */
    68	export function appendPutValueValidated(
    69		session: CollabSessionInstance,
    70		sheet: number,
    71		row: number,
    72		col: number,
    73		value: number,
    74	): void {
    75		if (!Number.isInteger(sheet) || sheet < 0 || sheet > 0xFFFF) {
    76			throw new Error(`appendPutValue: sheet must be an integer in [0, 65535], got ${sheet}`);
    77		}
    78		if (!Number.isInteger(row) || row < 0 || row > 0xFFFFFFFF) {
    79			throw new Error(`appendPutValue: row must be an integer in [0, 4294967295], got ${row}`);
    80		}
    81		if (!Number.isInteger(col) || col < 0 || col > 0xFFFFFFFF) {
    82			throw new Error(`appendPutValue: col must be an integer in [0, 4294967295], got ${col}`);
    83		}
    84		if (typeof value !== 'number' || !Number.isFinite(value)) {
    85			throw new Error(`appendPutValue: value must be a finite number, got ${value}`);
    86		}
    87		session.appendPutValue(sheet, row, col, value);
    88	}
    89	
    90	/**
    91	 * Reconstruct a `CollabSession` from a previously-exported snapshot.
    92	 *
    93	 * @param peerId Non-zero u64-domain peer identifier for THIS session
    94	 *               (need not match the snapshot's origin peer; CRDT will
    95	 *               author new ops under this id).
    96	 * @param bytes  Snapshot bytes from another session's `exportBytes()`.
    97	 */
    98	export function sessionFromSnapshot(peerId: bigint, bytes: Uint8Array): CollabSessionInstance {
    99		const engine = loadQuantbookEngine();
   100		return engine.CollabSession.fromSnapshot(peerId, bytes);
   101	}
   102	
   103	/**
   104	 * Return the engine binding crate's version string. For diagnostics
   105	 * + the V2 version-mismatch error path.
   106	 */
   107	export function quantbookEngineVersion(): string {
   108		return loadQuantbookEngine().version();
   109	}
   110	
   111	// =====================================================================
   112	// Phase 5.7 V2.1 (2026-05-22) -- Transport binding wrappers
   113	// =====================================================================
   114	
   115	/**
   116	 * Construct a fresh `LoopbackPair` and immediately take both ends.
   117	 * Returns the two Transport instances ready to attach to a session
   118	 * each.
   119	 *
   120	 * Combines `new LoopbackPair()` + `takeA()` + `takeB()` into a single
   121	 * call because the typical use case wants both ends. For cases where
   122	 * one end is created independently (e.g., delayed peer connection),
   123	 * use `createLoopbackPair()` instead and call `takeA` / `takeB`
   124	 * separately.
   125	 *
   126	 * @returns `[transportA, transportB]` where bytes sent on A arrive
   127	 *           at B's `pollRemote` and vice versa.
   128	 *
   129	 * @example
   130	 * ```ts
   131	 * const [tA, tB] = loopbackTransportPair();
   132	 * sessionA.attachTransport(tA);
   133	 * sessionB.attachTransport(tB);
   134	 * // mutate sessionA, flushToTransport, pollRemote on sessionB
   135	 * ```
   136	 */
   137	export function loopbackTransportPair(): [TransportInstance, TransportInstance] {
   138		const engine = loadQuantbookEngine();
   139		const pair = new engine.LoopbackPair();
   140		return [pair.takeA(), pair.takeB()];
   141	}
   142	
   143	/**
   144	 * Construct an empty `LoopbackPair` without taking either end. Use
   145	 * when the two takes need to happen at different times.
   146	 */
   147	export function createLoopbackPair(): LoopbackPairInstance {
   148		const engine = loadQuantbookEngine();
   149		return new engine.LoopbackPair();
   150	}
   151	
   152	// =====================================================================
   153	// Phase 5.7 V2.2 (2026-05-22) -- AutoFlushPolicy helpers
   154	// =====================================================================
   155	
   156	/**
   157	 * Type guard for the camelCase string union `AutoFlushPolicy`. Returns
   158	 * `true` for `'disabled'` or `'onAppend'`. Note: the engine's
   159	 * `setAutoFlushPolicy` also accepts variant aliases (`'Disabled'`,
   160	 * `'OnAppend'`, `'on-append'`) -- this guard is the STRICT camelCase
   161	 * check for IDE-side validation; the engine is the loose parser.
   162	 */
   163	export function isAutoFlushPolicy(value: unknown): value is AutoFlushPolicy {
   164		return value === 'disabled' || value === 'onAppend';
   165	}
   166	
   167	// =====================================================================
   168	// Phase 5.7 V2.7 (2026-05-22) -- structured error-code discrimination
   169	// =====================================================================
   170	
   171	/**
   172	 * Set of `QuantbookErrorCode` values, used by the `parseQuantbookError`
   173	 * runtime validator to reject unknown codes (which would otherwise
   174	 * mask an engine-binding drift).
   175	 *
   176	 * **Codex M3 closure note**: keeping this set in sync with the
   177	 * union type {@link QuantbookErrorCode} is a manual discipline.
   178	 * V2 backlog: codegen this set from the union (or use a const enum)
   179	 * to eliminate the drift hazard.
   180	 */
   181	const KNOWN_QUANTBOOK_ERROR_CODES: ReadonlySet<QuantbookErrorCode> = new Set<QuantbookErrorCode>([
   182		'transport_io',
   183		'transport_closed',
   184		'websocket_invalid_url',
   185		'websocket_connect_failed',
   186		'websocket_handshake_failed',
   187		'websocket_runtime_error',
   188		'session_oplog',
   189		'session_presence',
   190		'session_undo',
   191		'session_replay',
   192		'unknown',
   193	]);
   194	
   195	/**
   196	 * Bracket-prefix regex: `[<code>] <message>` where `<code>` is
   197	 * snake_case alphabetics + underscores. Matches the napi binding's
   198	 * `[{kind}] {Display}` convention from
   199	 * `crates/ql-bindings-node/src/lib.rs::{collab_session,transport,websocket}_error_to_napi`.
   200	 */
   201	const QUANTBOOK_ERROR_PREFIX_RE = /^\[([a-z][a-z_]*)\]\s*(.*)$/s;
   202	
   203	/**
   204	 * Extract the structured code + message from a caught error.
   205	 *
   206	 * **Usage**:
   207	 * ```ts
   208	 * try {
   209	 *   session.flushPendingToTransport();
   210	 * } catch (err) {
   211	 *   const info = parseQuantbookError(err);
   212	 *   if (info.code === 'transport_closed') {
   213	 *     // reconnect logic
   214	 *   }
   215	 * }
   216	 * ```
   217	 *
   218	 * **Returns**: a `QuantbookErrorInfo` with `code = 'unknown'` if:
   219	 * - the value is not an `Error` instance,
   220	 * - the `Error.message` has no `[<code>] ` prefix, OR
   221	 * - the prefix code is not a recognized `QuantbookErrorCode` (which
   222	 *   means the IDE binding is older than the engine; a build refresh
   223	 *   is in order).
   224	 *
   225	 * Closes V2.1+V2.2+V2.3 Opus MEDIUM-3 carryforwards.
   226	 */
   227	export function parseQuantbookError(err: unknown): QuantbookErrorInfo {
   228		if (!(err instanceof Error)) {
   229			return {
   230				code: 'unknown',
   231				message: typeof err === 'string' ? err : String(err),
   232				cause: err,
   233			};
   234		}
   235		const match = QUANTBOOK_ERROR_PREFIX_RE.exec(err.message);
   236		if (!match) {
   237			return { code: 'unknown', message: err.message, cause: err };
   238		}
   239		const rawCode = match[1];
   240		const rest = match[2];
   241		if (KNOWN_QUANTBOOK_ERROR_CODES.has(rawCode as QuantbookErrorCode)) {
   242			return { code: rawCode as QuantbookErrorCode, message: rest, cause: err };
   243		}
   244		// Unknown code: preserve the full original message (so the
   245		// user can see the actual prefix) and return 'unknown'.
   246		// Caller can then escalate this as a binding-drift signal.
   247		return { code: 'unknown', message: err.message, cause: err };
   248	}
   249	
   250	/**
   251	 * Type guard for `QuantbookErrorCode`. Mirrors `isAutoFlushPolicy`'s
   252	 * defensive shape -- use when reading codes from external config or
   253	 * persisted error logs.
   254	 */
   255	export function isQuantbookErrorCode(value: unknown): value is QuantbookErrorCode {
   256		return typeof value === 'string' && KNOWN_QUANTBOOK_ERROR_CODES.has(value as QuantbookErrorCode);
   257	}

 succeeded in 0ms:
     1	/*---------------------------------------------------------------------------------------------
     2	 *  Copyright (c) Microsoft Corporation. All rights reserved.
     3	 *  Licensed under the MIT License. See License.txt in the project root for license information.
     4	 *--------------------------------------------------------------------------------------------*/
     5	
     6	/**
     7	 * Phase 5.7 V1 (2026-05-22) -- TypeScript declarations mirroring the
     8	 * `ql-bindings-node` napi-rs surface (Rust crate at
     9	 * `quantlab-quantbook/quantbook-engine/crates/ql-bindings-node`).
    10	 *
    11	 * **Source of truth**: the Rust crate. If you edit this file, also
    12	 * update `crates/ql-bindings-node/src/lib.rs` (and vice versa). V2
    13	 * will add a generator (`@napi-rs/cli typegen`) to keep them in sync
    14	 * automatically.
    15	 *
    16	 * **V1 surface**: a single class `CollabSession` exposing the minimum
    17	 * round-trip methods. See the Rust crate's module docs for V1 surface
    18	 * rationale + V2 deferred list.
    19	 */
    20	
    21	/**
    22	 * A peer-to-peer collaboration session. Wraps the engine's
    23	 * `ql_collab::CollabSession`.
    24	 *
    25	 * **Failure modes** (constructors throw JS `Error`):
    26	 * - `peerId == 0n` -- PeerId reserves 0 (Loro sentinel)
    27	 * - `peerId < 0n` -- PeerId is u64-domain
    28	 * - `peerId` exceeds u64 -- lossy BigInt conversion
    29	 *
    30	 * Method failures (engine-level errors, e.g., merge of malformed
    31	 * bytes) surface as JS `Error` exceptions with the engine's error
    32	 * message string.
    33	 */
    34	export interface CollabSessionInstance {
    35		/**
    36		 * V1 convenience: append a `PutValue` op with a numeric value.
    37		 * Full Op enum binding is V2 work; V1 exposes this single variant
    38		 * because it's the minimum that demonstrates the round-trip.
    39		 */
    40		appendPutValue(sheet: number, row: number, col: number, value: number): void;
    41	
    42		/** Full snapshot export. Use for initial sync / handshake. */
    43		exportBytes(): Uint8Array;
    44	
    45		/**
    46		 * Merge a snapshot (or delta) from another peer. Returns the
    47		 * session's `opCount` AFTER the merge (NOT the number of newly
    48		 * merged ops -- duplicates are deduped by Loro but the return
    49		 * value is the post-merge total).
    50		 *
    51		 * **V1 audit closure (Codex M2, 2026-05-22)**: the original
    52		 * docstring claimed "count of ops actually merged" implying a
    53		 * delta. Verified empirically by calling `mergeBytes` twice with
    54		 * the same snapshot: both calls return the same `opCount` value.
    55		 * V2 may add a `mergeBytesDelta` companion that returns just the
    56		 * newly merged count, once the use case (e.g., "warn if peer
    57		 * sent us X new ops") materializes.
    58		 */
    59		mergeBytes(bytes: Uint8Array): number;
    60	
    61		/** Local op log length (visible LoroList). */
    62		opCount(): number;
    63	
    64		/**
    65		 * V2 V4 V1 step 2 helper: count of ops added since the last
    66		 * successful flush. Uses VV math (monotonic under undo). Sibling
    67		 * invariant: `pendingOpCount() > 0` iff `hasPendingFlush() === true`.
    68		 *
    69		 * V1 has no Transport binding, so this returns the count vs the
    70		 * empty baseline (same as `opCount()` for a never-flushed session).
    71		 * V2 makes this interesting once Transport surface is bound.
    72		 */
    73		pendingOpCount(): number;
    74	
    75		/** V2 V3 step 3 helper: `true` when local ops haven't been flushed. */
    76		hasPendingFlush(): boolean;
    77	
    78		/** Peer ID of this session, as a BigInt (u64-domain). */
    79		peerId(): bigint;
    80	
    81		// =====================================================================
    82		// Phase 5.7 V2.1 (2026-05-22) -- Transport surface (sync portion)
    83		// =====================================================================
    84	
    85		/**
    86		 * Attach a Transport to this session. The `transport` instance is
    87		 * CONSUMED -- subsequent calls with the same wrapper throw.
    88		 *
    89		 * Resets the session's per-transport VV baseline (Phase 5.5 V2 V3
    90		 * step 1 contract): the next flush sends from empty VV, delivering
    91		 * all local ops including any appended while no transport was
    92		 * attached. Loro's CRDT op log IS the implicit offline queue.
    93		 *
    94		 * @throws Error if `transport` has already been consumed.
    95		 */
    96		attachTransport(transport: TransportInstance): void;
    97	
    98		/**
    99		 * Detach the currently-attached transport. Returns `true` if one
   100		 * was attached (now released, its background tasks dropped),
   101		 * `false` if there was nothing to detach.
   102		 *
   103		 * V2.1 drops the returned `Box<dyn Transport>` Rust-side -- JS
   104		 * does not receive the prior transport.
   105		 */
   106		detachTransport(): boolean;
   107	
   108		/** `true` iff a transport is currently attached. */
   109		hasTransport(): boolean;
   110	
   111		/**
   112		 * Full-snapshot flush to the attached transport. Returns `true` if
   113		 * bytes were sent, `false` if no transport is attached.
   114		 *
   115		 * **Use the upcoming `flushDeltaToTransport` instead in production**
   116		 * once V2.2 ships -- delta flushes are O(per-op delta) vs O(full
   117		 * state).
   118		 *
   119		 * @throws Error if the transport's `send` returns an error.
   120		 */
   121		flushToTransport(): boolean;
   122	
   123		/**
   124		 * Drain inbound BLOBS from the attached transport (single-pass).
   125		 * Returns the count of BLOBS drained (NOT the count of ops),
   126		 * capped by the engine's default poll limit (`DEFAULT_POLL_REMOTE_LIMIT
   127		 * = 64`). Each blob is one snapshot/delta that may contain many ops;
   128		 * to count ops, compare `opCount()` before vs after.
   129		 *
   130		 * `0` if no transport is attached or no bytes were queued.
   131		 *
   132		 * **V2.1 audit closure (Codex MEDIUM-1, 2026-05-22)**: the prior
   133		 * docstring said "count of ops merged" which contradicted the
   134		 * engine's semantics (`poll_remote_with_limit` returns `merged <=
   135		 * max_blobs`). Mocha test "three-mutation chain across LoopbackPair"
   136		 * empirically discovered this and now pins the blob-count contract.
   137		 *
   138		 * @throws Error if the transport's `try_recv` or the merge step
   139		 *               returns an error.
   140		 */
   141		pollRemote(): number;
   142	
   143		// =====================================================================
   144		// Phase 5.7 V2.2 (2026-05-22) -- full sync Transport surface
   145		// =====================================================================
   146	
   147		/**
   148		 * Delta flush to the attached transport. Sends ONLY the ops added
   149		 * since the last successful flush. Returns `true` if bytes were
   150		 * actually sent.
   151		 *
   152		 * **Production default**. Prefer this over `flushToTransport`
   153		 * (full-snapshot) which is O(full state). Delta flushes are
   154		 * O(per-op delta).
   155		 *
   156		 * Idempotency: no state changed since last flush -> returns
   157		 * `false` without invoking `transport.send` (closes the V2 V2
   158		 * audit echo-loop concern).
   159		 *
   160		 * Per V2 V3 step 1: `attachTransport` resets the per-transport
   161		 * VV baseline, so the next `flushDeltaToTransport` after an attach
   162		 * sends from empty -- delivering ALL ops including any appended
   163		 * while offline (Loro's op log IS the implicit offline queue).
   164		 *
   165		 * @throws Error if the transport's `send` returns an error.
   166		 */
   167		flushDeltaToTransport(): boolean;
   168	
   169		/**
   170		 * Like `pollRemote` but with an explicit per-call cap on the
   171		 * number of blobs to drain. Returns the blob count, `<= limit`.
   172		 *
   173		 * Returned-count semantics:
   174		 * - `limit === 0` -> always returns 0 (no-op even if blobs queued).
   175		 * - Returned count `=== limit` -> more blobs may be queued; call again.
   176		 * - Returned count `< limit` -> queue drained.
   177		 *
   178		 * @param limit Non-negative integer in `[0, u32::MAX]`. NaN /
   179		 *              Infinity / fractional / negative throw.
   180		 *
   181		 * @throws Error if `limit` is invalid OR transport `try_recv` errors.
   182		 */
   183		pollRemoteWithLimit(limit: number): number;
   184	
   185		/**
   186		 * The attached transport's most recent error message, or `null` if
   187		 * no transport is attached OR the transport reports no error.
   188		 *
   189		 * **Use case**: after a mutator throws `Error("transport closed")`
   190		 * (or similar), call this to distinguish underlying causes (peer
   191		 * reset vs auth rejection vs capacity exceeded for V2.3+
   192		 * WebSocketTransport) and pick the reconnect strategy.
   193		 *
   194		 * **V2.2 audit-deferred caveat (Opus MEDIUM-3)**: error strings are
   195		 * lossy `Display` projections of the underlying enum. IDE callers
   196		 * today must substring-match to distinguish categories. V2.3+ will
   197		 * add structured discrimination via napi `Error.code`.
   198		 */
   199		transportLastError(): string | null;
   200	
   201		/**
   202		 * Set the auto-flush policy. Returns the prior policy as a string.
   203		 *
   204		 * Accepted: `'disabled'` (default) or `'onAppend'`. Other strings
   205		 * throw with a precise error.
   206		 *
   207		 * `'onAppend'` semantics (V2 V2 + V2 V3 steps 1+2): every public
   208		 * mutator auto-fires a delta flush after the mutation. Idempotency
   209		 * short-circuits no-op state changes; `pollRemote*` fires once
   210		 * after the batch (not per-blob).
   211		 *
   212		 * @throws Error on unknown policy string.
   213		 */
   214		setAutoFlushPolicy(policy: AutoFlushPolicy): AutoFlushPolicy;
   215	
   216		/**
   217		 * Read the current auto-flush policy. `'disabled'` is the default.
   218		 *
   219		 * **V2.2 audit closure (Opus HIGH-1, 2026-05-22)**: throws if the
   220		 * engine reports a variant unknown to this binding (forward-compat

exec
/bin/zsh -lc "rg -n \"parseQuantbookError|KNOWN_QUANTBOOK_ERROR_CODES|QuantbookErrorCode|websocket_invalid_url|invalid WebSocket URL|connection refused|connect_failed|falls back|unknown|V2\\.3 substring\" /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:212:	 * @throws Error on unknown policy string.
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:220:	 * engine reports a variant unknown to this binding (forward-compat
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:222:	 * being upgraded). Prior version returned `'unknown'` as a silent
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:228:	 * @throws Error if the engine variant is unknown to this binding.
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:458:		 *          `WebSocket handshake failed`, `invalid WebSocket URL`).
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:501:export type QuantbookErrorCode =
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:507:	| 'websocket_invalid_url'
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:508:	| 'websocket_connect_failed'
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:521:	| 'unknown';
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:527: * Use [`parseQuantbookError`] to construct from a caught `unknown`.
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:531:	 * Stable code identifier; one of the {@link QuantbookErrorCode}
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:532:	 * values. `'unknown'` if the error message had no recognizable
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:535:	readonly code: QuantbookErrorCode;
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:538:	 * `[<code>] ` prefix stripped. For `'unknown'` errors, the full
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:547:	readonly cause: unknown;
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:32:	QuantbookErrorCode,
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:163:export function isAutoFlushPolicy(value: unknown): value is AutoFlushPolicy {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:172: * Set of `QuantbookErrorCode` values, used by the `parseQuantbookError`
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:173: * runtime validator to reject unknown codes (which would otherwise
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:177: * union type {@link QuantbookErrorCode} is a manual discipline.
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:181:const KNOWN_QUANTBOOK_ERROR_CODES: ReadonlySet<QuantbookErrorCode> = new Set<QuantbookErrorCode>([
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:184:	'websocket_invalid_url',
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:185:	'websocket_connect_failed',
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:192:	'unknown',
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:211: *   const info = parseQuantbookError(err);
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:218: * **Returns**: a `QuantbookErrorInfo` with `code = 'unknown'` if:
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:221: * - the prefix code is not a recognized `QuantbookErrorCode` (which
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:227:export function parseQuantbookError(err: unknown): QuantbookErrorInfo {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:230:			code: 'unknown',
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:237:		return { code: 'unknown', message: err.message, cause: err };
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:241:	if (KNOWN_QUANTBOOK_ERROR_CODES.has(rawCode as QuantbookErrorCode)) {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:242:		return { code: rawCode as QuantbookErrorCode, message: rest, cause: err };
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:245:	// user can see the actual prefix) and return 'unknown'.
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:247:	return { code: 'unknown', message: err.message, cause: err };
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:251: * Type guard for `QuantbookErrorCode`. Mirrors `isAutoFlushPolicy`'s
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:255:export function isQuantbookErrorCode(value: unknown): value is QuantbookErrorCode {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:256:	return typeof value === 'string' && KNOWN_QUANTBOOK_ERROR_CODES.has(value as QuantbookErrorCode);
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:41:	isQuantbookErrorCode,
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:43:	parseQuantbookError,
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:62:		const seen = global as unknown as { __quantbookEngineSkipLogged?: boolean };
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:203:		(process as unknown as { dlopen: typeof process.dlopen }).dlopen =
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:205:				(mod as unknown as { exports: Record<string, unknown> }).exports = { foo: 1 };
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:232:		(process as unknown as { dlopen: typeof process.dlopen }).dlopen =
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:234:				(mod as unknown as { exports: Record<string, unknown> }).exports = {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:267:		(process as unknown as { dlopen: typeof process.dlopen }).dlopen =
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:269:				(mod as unknown as { exports: Record<string, unknown> }).exports = {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:850:	test('setAutoFlushPolicy rejects unknown string', () => {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1066:			/invalid WebSocket URL/,
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1071:	test('Transport.websocketConnect rejects on connection refused with ConnectFailed category', async function () {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1398:			(fakeCollabSession.prototype as Record<string, unknown>)[m] = function () { /* */ };
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1400:		const fakeTransport = function () { /* fake */ } as unknown as { websocketConnect?: unknown };
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1402:		(process as unknown as { dlopen: typeof process.dlopen }).dlopen =
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1404:				(mod as unknown as { exports: Record<string, unknown> }).exports = {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1439:			(fakeCollabSession.prototype as Record<string, unknown>)[m] = function () { /* */ };
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1441:		const fakeTransport = function () { /* fake */ } as unknown as { websocketConnect?: unknown };
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1443:		(process as unknown as { dlopen: typeof process.dlopen }).dlopen =
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1445:				(mod as unknown as { exports: Record<string, unknown> }).exports = {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1473://   (A) parseQuantbookError unit tests (no engine load needed):
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1478://       JS via parseQuantbookError. Pins the bracket-prefix
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1481:suite('quantbook V2.7 -- structured error-code discrimination (parseQuantbookError unit)', function () {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1485:	test('parseQuantbookError extracts known transport_closed code', () => {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1487:		const info = parseQuantbookError(err);
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1493:	test('parseQuantbookError extracts websocket_invalid_url code with message body', () => {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1494:		const err = new Error('[websocket_invalid_url] invalid WebSocket URL: not-a-url');
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1495:		const info = parseQuantbookError(err);
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1496:		assert.strictEqual(info.code, 'websocket_invalid_url');
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1497:		assert.strictEqual(info.message, 'invalid WebSocket URL: not-a-url');
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1500:	test('parseQuantbookError falls back to unknown for missing prefix', () => {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1502:		const info = parseQuantbookError(err);
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1503:		assert.strictEqual(info.code, 'unknown');
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1507:	test('parseQuantbookError falls back to unknown for unrecognized prefix code', () => {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1509:		// updates the union. Treated as 'unknown' + full message preserved.
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1511:		const info = parseQuantbookError(err);
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1512:		assert.strictEqual(info.code, 'unknown');
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1514:			'unknown-code path preserves full message including bracket prefix');
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1517:	test('parseQuantbookError handles non-Error throwables', () => {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1518:		const info1 = parseQuantbookError('string thrown');
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1519:		assert.strictEqual(info1.code, 'unknown');
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1522:		const info2 = parseQuantbookError(42);
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1523:		assert.strictEqual(info2.code, 'unknown');
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1526:		const info3 = parseQuantbookError(undefined);
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1527:		assert.strictEqual(info3.code, 'unknown');
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1530:	test('parseQuantbookError preserves cause for re-throw / inspection', () => {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1532:		const info = parseQuantbookError(err);
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1536:	test('isQuantbookErrorCode accepts known codes', () => {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1537:		assert.ok(isQuantbookErrorCode('transport_closed'));
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1538:		assert.ok(isQuantbookErrorCode('websocket_invalid_url'));
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1539:		assert.ok(isQuantbookErrorCode('session_oplog'));
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1540:		assert.ok(isQuantbookErrorCode('unknown'));
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1543:	test('isQuantbookErrorCode rejects non-strings and unknown codes', () => {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1544:		assert.ok(!isQuantbookErrorCode(undefined));
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1545:		assert.ok(!isQuantbookErrorCode(null));
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1546:		assert.ok(!isQuantbookErrorCode(42));
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1547:		assert.ok(!isQuantbookErrorCode('future_new_kind'));
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1548:		assert.ok(!isQuantbookErrorCode('Transport_Closed'),
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1553:suite('quantbook V2.7 -- structured error-code end-to-end (engine → napi → parseQuantbookError)', function () {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1564:	test('end-to-end: invalid WebSocket URL surfaces websocket_invalid_url code', async () => {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1570:			const info = parseQuantbookError(err);
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1571:			assert.strictEqual(info.code, 'websocket_invalid_url',
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1572:				`expected websocket_invalid_url, got code=${info.code}, msg=${info.message}`);
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1574:			assert.match(info.message, /invalid WebSocket URL/);
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1578:	test('end-to-end: connection refused surfaces websocket_connect_failed code', async function () {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1585:			const info = parseQuantbookError(err);
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1586:			assert.strictEqual(info.code, 'websocket_connect_failed',
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1587:				`expected websocket_connect_failed, got code=${info.code}, msg=${info.message}`);
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1591:	test('end-to-end: V2.3 substring tests still pass (Display preserved after [code] prefix)', async () => {
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1592:		// V2.3 mocha asserts /invalid WebSocket URL/ on the raw
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1593:		// Error.message. V2.7 prepends "[websocket_invalid_url] " --
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1601:			assert.match(err.message, /invalid WebSocket URL/,
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1602:				'V2.3 substring-match assertion preserved post-V2.7');
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1603:			assert.match(err.message, /^\[websocket_invalid_url\]/,

exec
/bin/zsh -lc "nl -ba /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts | sed -n '430,560p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
   430		/** Smoke method exposing the binding crate's own version. */
   431		version(): string;
   432	
   433		/** Session class -- see {@link CollabSessionInstance}. */
   434		readonly CollabSession: CollabSessionConstructor;
   435	
   436		/**
   437		 * V2.1: opaque Transport wrapper. JS-side this is mostly used
   438		 * as a parameter type to `CollabSession.attachTransport`.
   439		 * The constructor is NOT directly exposed -- obtain instances
   440		 * via factories like `LoopbackPair`.
   441		 *
   442		 * **V2.3 (2026-05-22)**: added `websocketConnect(url)` static
   443		 * async factory that resolves with a Transport wrapping a
   444		 * `WebSocketTransport`. See `flushPendingToTransport` for the
   445		 * matching async drain helper.
   446		 */
   447		readonly Transport: {
   448			prototype: TransportInstance;
   449			/**
   450			 * Async factory: connect to a WebSocket peer and return a
   451			 * Transport wrapping the live connection.
   452			 *
   453			 * @param url `ws://host:port` URL. No TLS in V2.3.
   454			 * @returns Promise resolving with the Transport (single-use;
   455			 *          pass to `attachTransport` once).
   456			 * @throws  Error with WebSocket failure category in the
   457			 *          message (`WebSocket connection failed`,
   458			 *          `WebSocket handshake failed`, `invalid WebSocket URL`).
   459			 */
   460			websocketConnect(url: string): Promise<TransportInstance>;
   461		};
   462	
   463		/**
   464		 * V2.1: LoopbackPair class -- factory for paired in-process
   465		 * Transport ends.
   466		 */
   467		readonly LoopbackPair: LoopbackPairConstructor;
   468	
   469		/**
   470		 * V2.6 (2026-05-22): BlockingTransportFixture class -- test
   471		 * fixture for V2.5 V8-block contract testing. See
   472		 * {@link BlockingTransportFixtureInstance}.
   473		 *
   474		 * **NOT for production code** -- the underlying transport blocks
   475		 * `flush_pending` indefinitely until released.
   476		 */
   477		readonly BlockingTransportFixture: BlockingTransportFixtureConstructor;
   478	}
   479	
   480	// ============================================================================
   481	// Phase 5.7 V2.7 (2026-05-22) -- structured error-code discrimination
   482	// ============================================================================
   483	
   484	/**
   485	 * Stable string identifier for an engine error variant, extracted from
   486	 * the bracketed prefix that the napi binding prepends to thrown
   487	 * `Error.message`. IDE reconnect logic can switch on these codes
   488	 * without substring-matching the human-readable `Display` text.
   489	 *
   490	 * **Codes are SemVer-stable.** Engine-side
   491	 * `crates/ql-collab/src/transport.rs::TransportError::kind`,
   492	 * `crates/ql-collab-ws/src/lib.rs::WebSocketError::kind`, and
   493	 * `crates/ql-collab/src/session.rs::CollabSessionError::kind` are
   494	 * the authoritative sources; this union mirrors them.
   495	 *
   496	 * **Closes V2.1+V2.2+V2.3 Opus MEDIUM-3 carryforwards**: prior to
   497	 * V2.7, IDE callers had to substring-match error messages to
   498	 * distinguish transport-closed from transport-io etc. This was
   499	 * lossy and fragile across Display-string edits.
   500	 */
   501	export type QuantbookErrorCode =
   502		// Transport-layer errors (`TransportError::kind`)
   503		| 'transport_io'
   504		| 'transport_closed'
   505		// WebSocket-layer errors (`WebSocketError::kind`).
   506		// Surfaced by `Transport.websocketConnect` on rejection.
   507		| 'websocket_invalid_url'
   508		| 'websocket_connect_failed'
   509		| 'websocket_handshake_failed'
   510		| 'websocket_runtime_error'
   511		// Session-layer errors (`CollabSessionError::kind`).
   512		// Note: `Transport(_)` passes through to the inner transport
   513		// kind (e.g. `transport_closed`), not a wrapper string.
   514		| 'session_oplog'
   515		| 'session_presence'
   516		| 'session_undo'
   517		| 'session_replay'
   518		// Fallback when the message has no recognizable code prefix.
   519		// Typically means the error came from non-engine code (napi
   520		// task panic, validation guard, JS-side throw).
   521		| 'unknown';
   522	
   523	/**
   524	 * Structured view of a Quantbook engine error, extracted from the
   525	 * `[<code>] <message>` convention that the napi binding uses.
   526	 *
   527	 * Use [`parseQuantbookError`] to construct from a caught `unknown`.
   528	 */
   529	export interface QuantbookErrorInfo {
   530		/**
   531		 * Stable code identifier; one of the {@link QuantbookErrorCode}
   532		 * values. `'unknown'` if the error message had no recognizable
   533		 * code prefix.
   534		 */
   535		readonly code: QuantbookErrorCode;
   536		/**
   537		 * Human-readable message; the original `Error.message` with the
   538		 * `[<code>] ` prefix stripped. For `'unknown'` errors, the full
   539		 * original message.
   540		 */
   541		readonly message: string;
   542		/**
   543		 * The original caught value, for callers that need to re-throw
   544		 * or inspect non-Error throwables (strings, numbers, custom
   545		 * classes, etc.).
   546		 */
   547		readonly cause: unknown;
   548	}

exec
/bin/zsh -lc "nl -ba /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts | sed -n '1020,1095p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
  1020				const addr = wss.address() as AddressInfo;
  1021				resolve({
  1022					url: `ws://127.0.0.1:${addr.port}`,
  1023					close: () => new Promise<void>((res) => {
  1024						// **V2.3 audit closure (Opus MEDIUM-4, 2026-05-22)**:
  1025						// the prior version wrapped `s.terminate()` in
  1026						// `try { ... } catch { /* ignore */ }` which
  1027						// silently swallowed errors (CLAUDE.md no-fallback
  1028						// violation, in test code too). `ws.terminate()` is
  1029						// documented to never throw -- it forcibly closes
  1030						// the socket without sending a Close frame. The
  1031						// only way it could "error" is if the socket is
  1032						// already destroyed, which is fine (idempotent).
  1033						// Removing the try/catch surfaces any real future
  1034						// error class loudly.
  1035						for (const s of sockets) {
  1036							s.terminate();
  1037						}
  1038						wss.close(() => res());
  1039					}),
  1040				});
  1041			});
  1042		});
  1043	}
  1044	
  1045	suite('quantbook V2.3 -- async Transport surface (WebSocketTransport + flushPending)', function () {
  1046		suiteSetup(function () {
  1047			const r = shouldSkip();
  1048			if (r.skip) {
  1049				this.skip();
  1050			}
  1051			_resetQuantbookEngineCacheForTests();
  1052			this.timeout(60000);
  1053			loadQuantbookEngine();
  1054		});
  1055	
  1056		test('Transport.websocketConnect rejects on invalid URL with InvalidUrl category', async () => {
  1057			// **V2.3 audit closure (Codex LOW-1, 2026-05-22)**: prior version
  1058			// of this test accepted any of 3 categories. tokio-tungstenite's
  1059			// URL parser categorizes `not-a-ws-url` as InvalidUrl (the
  1060			// classifier hits the `tokio_tungstenite::tungstenite::Error::Url(_)`
  1061			// arm in `ql-collab-ws/src/lib.rs::WebSocketTransport::connect`).
  1062			// Pin the exact category prefix.
  1063			const engine = loadQuantbookEngine();
  1064			await assert.rejects(
  1065				engine.Transport.websocketConnect('not-a-ws-url'),
  1066				/invalid WebSocket URL/,
  1067				'rejection must carry InvalidUrl prefix (engine WebSocketError::InvalidUrl)',
  1068			);
  1069		});
  1070	
  1071		test('Transport.websocketConnect rejects on connection refused with ConnectFailed category', async function () {
  1072			// **V2.3 audit closure (Codex LOW-1, 2026-05-22)**: the prior
  1073			// version accepted ConnectFailed OR HandshakeFailed; tokio-
  1074			// tungstenite categorizes TCP refusals as the `Error::Io(_)`
  1075			// arm → ConnectFailed at the binding. Pin it.
  1076			this.timeout(10000);
  1077			const engine = loadQuantbookEngine();
  1078			await assert.rejects(
  1079				// Port 1 is privileged + almost certainly not listening.
  1080				engine.Transport.websocketConnect('ws://127.0.0.1:1'),
  1081				/WebSocket connection failed/,
  1082				'TCP refusal must surface as ConnectFailed (engine WebSocketError::ConnectFailed)',
  1083			);
  1084		});
  1085	
  1086		test('Transport.websocketConnect succeeds against a localhost relay + returns attachable Transport', async function () {
  1087			this.timeout(15000);
  1088			const relay = await spawnWsRelay();
  1089			try {
  1090				const engine = loadQuantbookEngine();
  1091				const t = await engine.Transport.websocketConnect(relay.url);
  1092				assert.ok(t.isAttachable(),
  1093					'returned Transport is attachable (single-use, fresh)');
  1094			} finally {
  1095				await relay.close();

exec
/bin/zsh -lc "nl -ba /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts | sed -n '1460,1615p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
  1460				process.dlopen = originalDlopen;
  1461				_resetQuantbookEngineCacheForTests();
  1462			}
  1463		});
  1464	});
  1465	
  1466	// =============================================================
  1467	// Phase 5.7 V2.7 (2026-05-22) -- structured error-code discrimination
  1468	// =============================================================
  1469	//
  1470	// Closes V2.1+V2.2+V2.3 Opus MEDIUM-3 carryforwards. Two test
  1471	// groups:
  1472	//
  1473	//   (A) parseQuantbookError unit tests (no engine load needed):
  1474	//       prefix parsing, fallback semantics, type guards.
  1475	//
  1476	//   (B) End-to-end integration: drive a real engine error through
  1477	//       the napi binding + verify the code is recoverable from
  1478	//       JS via parseQuantbookError. Pins the bracket-prefix
  1479	//       convention as a contract, not coincidence.
  1480	
  1481	suite('quantbook V2.7 -- structured error-code discrimination (parseQuantbookError unit)', function () {
  1482		// These tests do NOT require the engine to load -- they exercise
  1483		// the pure-TS parser. Run regardless of QUANTBOOK_ENGINE_PATH.
  1484	
  1485		test('parseQuantbookError extracts known transport_closed code', () => {
  1486			const err = new Error('[transport_closed] transport closed');
  1487			const info = parseQuantbookError(err);
  1488			assert.strictEqual(info.code, 'transport_closed');
  1489			assert.strictEqual(info.message, 'transport closed');
  1490			assert.strictEqual(info.cause, err);
  1491		});
  1492	
  1493		test('parseQuantbookError extracts websocket_invalid_url code with message body', () => {
  1494			const err = new Error('[websocket_invalid_url] invalid WebSocket URL: not-a-url');
  1495			const info = parseQuantbookError(err);
  1496			assert.strictEqual(info.code, 'websocket_invalid_url');
  1497			assert.strictEqual(info.message, 'invalid WebSocket URL: not-a-url');
  1498		});
  1499	
  1500		test('parseQuantbookError falls back to unknown for missing prefix', () => {
  1501			const err = new Error('plain old error with no prefix');
  1502			const info = parseQuantbookError(err);
  1503			assert.strictEqual(info.code, 'unknown');
  1504			assert.strictEqual(info.message, 'plain old error with no prefix');
  1505		});
  1506	
  1507		test('parseQuantbookError falls back to unknown for unrecognized prefix code', () => {
  1508			// Unknown code = engine introduces new variant before IDE
  1509			// updates the union. Treated as 'unknown' + full message preserved.
  1510			const err = new Error('[future_new_kind] some new error');
  1511			const info = parseQuantbookError(err);
  1512			assert.strictEqual(info.code, 'unknown');
  1513			assert.strictEqual(info.message, '[future_new_kind] some new error',
  1514				'unknown-code path preserves full message including bracket prefix');
  1515		});
  1516	
  1517		test('parseQuantbookError handles non-Error throwables', () => {
  1518			const info1 = parseQuantbookError('string thrown');
  1519			assert.strictEqual(info1.code, 'unknown');
  1520			assert.strictEqual(info1.message, 'string thrown');
  1521	
  1522			const info2 = parseQuantbookError(42);
  1523			assert.strictEqual(info2.code, 'unknown');
  1524			assert.strictEqual(info2.message, '42');
  1525	
  1526			const info3 = parseQuantbookError(undefined);
  1527			assert.strictEqual(info3.code, 'unknown');
  1528		});
  1529	
  1530		test('parseQuantbookError preserves cause for re-throw / inspection', () => {
  1531			const err = new Error('[transport_closed] transport closed');
  1532			const info = parseQuantbookError(err);
  1533			assert.strictEqual(info.cause, err, 'cause is the original caught value');
  1534		});
  1535	
  1536		test('isQuantbookErrorCode accepts known codes', () => {
  1537			assert.ok(isQuantbookErrorCode('transport_closed'));
  1538			assert.ok(isQuantbookErrorCode('websocket_invalid_url'));
  1539			assert.ok(isQuantbookErrorCode('session_oplog'));
  1540			assert.ok(isQuantbookErrorCode('unknown'));
  1541		});
  1542	
  1543		test('isQuantbookErrorCode rejects non-strings and unknown codes', () => {
  1544			assert.ok(!isQuantbookErrorCode(undefined));
  1545			assert.ok(!isQuantbookErrorCode(null));
  1546			assert.ok(!isQuantbookErrorCode(42));
  1547			assert.ok(!isQuantbookErrorCode('future_new_kind'));
  1548			assert.ok(!isQuantbookErrorCode('Transport_Closed'),
  1549				'case-sensitive: TitleCase rejected');
  1550		});
  1551	});
  1552	
  1553	suite('quantbook V2.7 -- structured error-code end-to-end (engine → napi → parseQuantbookError)', function () {
  1554		suiteSetup(function () {
  1555			const r = shouldSkip();
  1556			if (r.skip) {
  1557				this.skip();
  1558			}
  1559			_resetQuantbookEngineCacheForTests();
  1560			this.timeout(60000);
  1561			loadQuantbookEngine();
  1562		});
  1563	
  1564		test('end-to-end: invalid WebSocket URL surfaces websocket_invalid_url code', async () => {
  1565			const engine = loadQuantbookEngine();
  1566			try {
  1567				await engine.Transport.websocketConnect('not-a-ws-url');
  1568				assert.fail('expected rejection');
  1569			} catch (err) {
  1570				const info = parseQuantbookError(err);
  1571				assert.strictEqual(info.code, 'websocket_invalid_url',
  1572					`expected websocket_invalid_url, got code=${info.code}, msg=${info.message}`);
  1573				// Display string preserved after the code prefix.
  1574				assert.match(info.message, /invalid WebSocket URL/);
  1575			}
  1576		});
  1577	
  1578		test('end-to-end: connection refused surfaces websocket_connect_failed code', async function () {
  1579			this.timeout(10000);
  1580			const engine = loadQuantbookEngine();
  1581			try {
  1582				await engine.Transport.websocketConnect('ws://127.0.0.1:1');
  1583				assert.fail('expected rejection');
  1584			} catch (err) {
  1585				const info = parseQuantbookError(err);
  1586				assert.strictEqual(info.code, 'websocket_connect_failed',
  1587					`expected websocket_connect_failed, got code=${info.code}, msg=${info.message}`);
  1588			}
  1589		});
  1590	
  1591		test('end-to-end: V2.3 substring tests still pass (Display preserved after [code] prefix)', async () => {
  1592			// V2.3 mocha asserts /invalid WebSocket URL/ on the raw
  1593			// Error.message. V2.7 prepends "[websocket_invalid_url] " --
  1594			// the substring is still present. Pin this explicitly.
  1595			const engine = loadQuantbookEngine();
  1596			try {
  1597				await engine.Transport.websocketConnect('not-a-ws-url');
  1598				assert.fail('expected rejection');
  1599			} catch (err) {
  1600				assert.ok(err instanceof Error);
  1601				assert.match(err.message, /invalid WebSocket URL/,
  1602					'V2.3 substring-match assertion preserved post-V2.7');
  1603				assert.match(err.message, /^\[websocket_invalid_url\]/,
  1604					'V2.7 prefix is present');
  1605			}
  1606		});
  1607	});

exec
/bin/zsh -lc "rg -n \"\\.map_err\\(\\|e\\|\" quantbook-engine/crates/ql-bindings-node/src/lib.rs" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
915:            .map_err(|e| Error::from_reason(format!("flushPendingToTransport task: {e}")))?
1320:                .map_err(|e| format!("blocked lock poisoned: {e}"))?;
1324:                    .map_err(|e| format!("blocked wait poisoned: {e}"))?;
1329:        .map_err(|e| Error::from_reason(format!("waitUntilBlocked task: {e}")))?

exec
/bin/zsh -lc "nl -ba quantbook-engine/crates/ql-collab-ws/src/lib.rs | sed -n '936,970p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
exec
/bin/zsh -lc "nl -ba quantbook-engine/crates/ql-collab/src/transport.rs | sed -n '1088,1122p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
   936	    fn runtime_error_display_includes_cause() {
   937	        // V2 V3 step 4 audit closure (Opus M1, 2026-05-21): pin the
   938	        // Display impl for the new variant so its surface is stable.
   939	        let e = WebSocketError::RuntimeError("peer reset".into());
   940	        assert!(e.to_string().contains("peer reset"));
   941	    }
   942	
   943	    // ============================================================
   944	    // Phase 5.7 V2.7 (2026-05-22) — error-code discrimination
   945	    // ============================================================
   946	
   947	    #[test]
   948	    fn websocket_error_kind_all_variants() {
   949	        assert_eq!(
   950	            WebSocketError::InvalidUrl("x".into()).kind(),
   951	            "websocket_invalid_url"
   952	        );
   953	        assert_eq!(
   954	            WebSocketError::ConnectFailed("x".into()).kind(),
   955	            "websocket_connect_failed"
   956	        );
   957	        assert_eq!(
   958	            WebSocketError::HandshakeFailed("x".into()).kind(),
   959	            "websocket_handshake_failed"
   960	        );
   961	        assert_eq!(
   962	            WebSocketError::RuntimeError("x".into()).kind(),
   963	            "websocket_runtime_error"
   964	        );
   965	    }
   966	}

 succeeded in 0ms:
  1088	        std::thread::spawn(move || {
  1089	            std::thread::sleep(std::time::Duration::from_millis(50));
  1090	            *release_clone.0.lock().unwrap() = true;
  1091	            release_clone.1.notify_all();
  1092	        });
  1093	
  1094	        let start = Instant::now();
  1095	        handle.wait_for_drain().expect("wait_for_drain ok");
  1096	        assert!(
  1097	            start.elapsed() < std::time::Duration::from_millis(500),
  1098	            "handle wait should unblock independently of transport"
  1099	        );
  1100	    }
  1101	
  1102	    // ============================================================
  1103	    // Phase 5.7 V2.7 (2026-05-22) — error-code discrimination
  1104	    // ============================================================
  1105	
  1106	    #[test]
  1107	    fn transport_error_kind_io() {
  1108	        let e = super::TransportError::Io("socket dead".into());
  1109	        assert_eq!(e.kind(), "transport_io");
  1110	    }
  1111	
  1112	    #[test]
  1113	    fn transport_error_kind_closed() {
  1114	        let e = super::TransportError::Closed;
  1115	        assert_eq!(e.kind(), "transport_closed");
  1116	    }
  1117	}

exec
/bin/zsh -lc "nl -ba quantbook-engine/crates/ql-collab/src/session.rs | sed -n '3068,3100p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
  3068	        let origin = CollabSession::new(PeerId::new(1)).unwrap();
  3069	        let bytes = origin.export_bytes().unwrap();
  3070	        let result = CollabSession::from_snapshot(PeerId::new(u64::MAX), &bytes);
  3071	        assert!(
  3072	            matches!(result, Err(CollabSessionError::OpLog(_))),
  3073	            "from_snapshot(u64::MAX) must fail; got {result:?}"
  3074	        );
  3075	    }
  3076	
  3077	    // ============================================================
  3078	    // Phase 5.7 V2.7 (2026-05-22) — error-code discrimination
  3079	    // ============================================================
  3080	
  3081	    #[test]
  3082	    fn collab_session_error_kind_transport_inner_passthrough() {
  3083	        // Transport(inner) should return the inner's kind, NOT a
  3084	        // generic "session_transport" wrapper string. This is the
  3085	        // contract IDE callers depend on for reconnect logic.
  3086	        use crate::transport::TransportError;
  3087	        let e = super::CollabSessionError::Transport(TransportError::Closed);
  3088	        assert_eq!(e.kind(), "transport_closed");
  3089	
  3090	        let e = super::CollabSessionError::Transport(TransportError::Io("dead".into()));
  3091	        assert_eq!(e.kind(), "transport_io");
  3092	    }
  3093	}

exec
/bin/zsh -lc 'rg -n "InvalidUrl|ConnectFailed|HandshakeFailed|RuntimeError|TransportError::Closed|TransportError::Io" quantbook-engine/crates/ql-collab-ws/src/lib.rs quantbook-engine/crates/ql-collab/src/session.rs quantbook-engine/crates/ql-collab/src/transport.rs' in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
quantbook-engine/crates/ql-collab/src/transport.rs:140:            TransportError::Io(_) => "transport_io",
quantbook-engine/crates/ql-collab/src/transport.rs:141:            TransportError::Closed => "transport_closed",
quantbook-engine/crates/ql-collab/src/transport.rs:190:    /// Returns `Err(TransportError::Closed)` only when the channel
quantbook-engine/crates/ql-collab/src/transport.rs:193:    /// `Err(TransportError::Io)`.
quantbook-engine/crates/ql-collab/src/transport.rs:218:    /// Read AFTER observing [`TransportError::Closed`] from `send`
quantbook-engine/crates/ql-collab/src/transport.rs:248:    /// - `Err(TransportError::Closed)` if the transport is closed
quantbook-engine/crates/ql-collab/src/transport.rs:252:    /// - `Err(TransportError::Io)` for internal synchronization
quantbook-engine/crates/ql-collab/src/transport.rs:366:    /// `Err(TransportError::Closed)` if the transport was closed on
quantbook-engine/crates/ql-collab/src/transport.rs:367:    /// entry or mid-wait; `Err(TransportError::Io(...))` for
quantbook-engine/crates/ql-collab/src/transport.rs:398:    /// `TransportError::Closed`; `try_recv` returns `Closed` since
quantbook-engine/crates/ql-collab/src/transport.rs:408:            return Err(TransportError::Closed);
quantbook-engine/crates/ql-collab/src/transport.rs:416:            return Err(TransportError::Closed);
quantbook-engine/crates/ql-collab/src/transport.rs:461:/// - A's `send` returns `TransportError::Closed` immediately.
quantbook-engine/crates/ql-collab/src/transport.rs:526:    /// calls return `TransportError::Closed`. The peer endpoint
quantbook-engine/crates/ql-collab/src/transport.rs:550:            return Err(TransportError::Closed);
quantbook-engine/crates/ql-collab/src/transport.rs:554:            .map_err(|e| TransportError::Io(format!("loopback outbox lock poisoned: {e}")))?
quantbook-engine/crates/ql-collab/src/transport.rs:570:            .map_err(|e| TransportError::Io(format!("loopback inbox lock poisoned: {e}")))?;
quantbook-engine/crates/ql-collab/src/transport.rs:576:            Err(TransportError::Closed)
quantbook-engine/crates/ql-collab/src/transport.rs:684:                TransportError::Io(format!("BlockingTransport blocked lock poisoned: {e}"))
quantbook-engine/crates/ql-collab/src/transport.rs:693:            TransportError::Io(format!("BlockingTransport release lock poisoned: {e}"))
quantbook-engine/crates/ql-collab/src/transport.rs:702:                    TransportError::Io(format!("BlockingTransport release wait poisoned: {e}"))
quantbook-engine/crates/ql-collab/src/transport.rs:718:                    TransportError::Io(format!("BlockingTransport release wait poisoned: {e}"))
quantbook-engine/crates/ql-collab/src/transport.rs:818:        assert!(matches!(t.send(b"second"), Err(TransportError::Closed)));
quantbook-engine/crates/ql-collab/src/transport.rs:827:        assert!(matches!(t.try_recv(), Err(TransportError::Closed)));
quantbook-engine/crates/ql-collab/src/transport.rs:895:        assert!(matches!(a.send(b"x"), Err(TransportError::Closed)));
quantbook-engine/crates/ql-collab/src/transport.rs:900:        assert!(matches!(a.try_recv(), Err(TransportError::Closed)));
quantbook-engine/crates/ql-collab/src/transport.rs:919:        assert!(matches!(a.try_recv(), Err(TransportError::Closed)));
quantbook-engine/crates/ql-collab/src/transport.rs:921:        assert!(matches!(a.send(b"nope"), Err(TransportError::Closed)));
quantbook-engine/crates/ql-collab/src/transport.rs:1108:        let e = super::TransportError::Io("socket dead".into());
quantbook-engine/crates/ql-collab/src/transport.rs:1114:        let e = super::TransportError::Closed;
quantbook-engine/crates/ql-collab/src/session.rs:831:    /// `Err(CollabSessionError::Transport(TransportError::Closed))`
quantbook-engine/crates/ql-collab/src/session.rs:836:    /// transient I/O vs auth-prompt for `HandshakeFailed("401")`.
quantbook-engine/crates/ql-collab/src/session.rs:1442:    /// `TransportError::Closed` is handled gracefully — the
quantbook-engine/crates/ql-collab/src/session.rs:1499:    /// (c) inner match returns `Err(TransportError::Closed)` →
quantbook-engine/crates/ql-collab/src/session.rs:1502:    /// or `try_recv` errors with `Err(TransportError::Io(_))` (the
quantbook-engine/crates/ql-collab/src/session.rs:1541:                Err(TransportError::Closed) => {
quantbook-engine/crates/ql-collab/src/session.rs:2802:                Err(CollabSessionError::Transport(TransportError::Closed))
quantbook-engine/crates/ql-collab/src/session.rs:3087:        let e = super::CollabSessionError::Transport(TransportError::Closed);
quantbook-engine/crates/ql-collab/src/session.rs:3090:        let e = super::CollabSessionError::Transport(TransportError::Io("dead".into()));
quantbook-engine/crates/ql-collab-ws/src/lib.rs:107:/// `RuntimeError` describing the panic.
quantbook-engine/crates/ql-collab-ws/src/lib.rs:171:/// poisoned-mutex condition as a `RuntimeError` rather than the
quantbook-engine/crates/ql-collab-ws/src/lib.rs:178:            *guard = Some(WebSocketError::RuntimeError(message));
quantbook-engine/crates/ql-collab-ws/src/lib.rs:185:            *guard = Some(WebSocketError::RuntimeError(format!(
quantbook-engine/crates/ql-collab-ws/src/lib.rs:194:/// Distinguishes connect-time errors (`InvalidUrl`, `ConnectFailed`,
quantbook-engine/crates/ql-collab-ws/src/lib.rs:195:/// `HandshakeFailed`) from runtime errors observed by the background
quantbook-engine/crates/ql-collab-ws/src/lib.rs:196:/// reader/writer tasks (`RuntimeError`). Connect-time errors surface
quantbook-engine/crates/ql-collab-ws/src/lib.rs:200:/// surface `TransportError::Closed`.
quantbook-engine/crates/ql-collab-ws/src/lib.rs:211:/// "auth failed". Adds `RuntimeError(String)` variant + `last_error()`
quantbook-engine/crates/ql-collab-ws/src/lib.rs:221:    InvalidUrl(String),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:225:    ConnectFailed(String),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:230:    HandshakeFailed(String),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:238:    RuntimeError(String),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:254:            WebSocketError::InvalidUrl(_) => "websocket_invalid_url",
quantbook-engine/crates/ql-collab-ws/src/lib.rs:255:            WebSocketError::ConnectFailed(_) => "websocket_connect_failed",
quantbook-engine/crates/ql-collab-ws/src/lib.rs:256:            WebSocketError::HandshakeFailed(_) => "websocket_handshake_failed",
quantbook-engine/crates/ql-collab-ws/src/lib.rs:257:            WebSocketError::RuntimeError(_) => "websocket_runtime_error",
quantbook-engine/crates/ql-collab-ws/src/lib.rs:385:                tokio_tungstenite::tungstenite::Error::Url(_) => WebSocketError::InvalidUrl(msg),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:386:                tokio_tungstenite::tungstenite::Error::Io(_) => WebSocketError::ConnectFailed(msg),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:389:                    WebSocketError::HandshakeFailed(msg)
quantbook-engine/crates/ql-collab-ws/src/lib.rs:391:                _ => WebSocketError::HandshakeFailed(msg),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:444:                    // via last_error() after seeing TransportError::Closed.
quantbook-engine/crates/ql-collab-ws/src/lib.rs:590:    /// graceful Close frame from peer); returns `Some(RuntimeError(_))`
quantbook-engine/crates/ql-collab-ws/src/lib.rs:594:    /// Read AFTER observing [`TransportError::Closed`] from
quantbook-engine/crates/ql-collab-ws/src/lib.rs:673:            return Err(TransportError::Closed);
quantbook-engine/crates/ql-collab-ws/src/lib.rs:680:            .map_err(|_| TransportError::Closed)?;
quantbook-engine/crates/ql-collab-ws/src/lib.rs:698:                    Err(TransportError::Closed)
quantbook-engine/crates/ql-collab-ws/src/lib.rs:709:                Err(TransportError::Closed)
quantbook-engine/crates/ql-collab-ws/src/lib.rs:743:    /// - `TransportError::Closed` if the transport was already closed
quantbook-engine/crates/ql-collab-ws/src/lib.rs:746:    /// - `TransportError::Io` if the progress mutex is poisoned by a
quantbook-engine/crates/ql-collab-ws/src/lib.rs:764:            return Err(TransportError::Closed);
quantbook-engine/crates/ql-collab-ws/src/lib.rs:770:            .map_err(|e| TransportError::Io(format!("flush_pending lock poisoned: {e}")))?;
quantbook-engine/crates/ql-collab-ws/src/lib.rs:773:                return Err(TransportError::Closed);
quantbook-engine/crates/ql-collab-ws/src/lib.rs:777:                .map_err(|e| TransportError::Io(format!("flush_pending wait poisoned: {e}")))?;
quantbook-engine/crates/ql-collab-ws/src/lib.rs:819:/// `Err(TransportError::Closed)` if the transport closes mid-wait;
quantbook-engine/crates/ql-collab-ws/src/lib.rs:820:/// `Err(TransportError::Io)` on poisoned mutex (writer task
quantbook-engine/crates/ql-collab-ws/src/lib.rs:837:            return Err(TransportError::Closed);
quantbook-engine/crates/ql-collab-ws/src/lib.rs:842:            .map_err(|e| TransportError::Io(format!("wait_for_drain lock poisoned: {e}")))?;
quantbook-engine/crates/ql-collab-ws/src/lib.rs:845:                return Err(TransportError::Closed);
quantbook-engine/crates/ql-collab-ws/src/lib.rs:849:                .map_err(|e| TransportError::Io(format!("wait_for_drain wait poisoned: {e}")))?;
quantbook-engine/crates/ql-collab-ws/src/lib.rs:906:        let e = WebSocketError::InvalidUrl("malformed".into());
quantbook-engine/crates/ql-collab-ws/src/lib.rs:908:        let e = WebSocketError::ConnectFailed("refused".into());
quantbook-engine/crates/ql-collab-ws/src/lib.rs:910:        let e = WebSocketError::HandshakeFailed("401".into());
quantbook-engine/crates/ql-collab-ws/src/lib.rs:921:            WebSocketError::InvalidUrl("x".into()),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:922:            WebSocketError::ConnectFailed("y".into()),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:923:            WebSocketError::HandshakeFailed("z".into()),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:924:            WebSocketError::RuntimeError("w".into()),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:927:                WebSocketError::InvalidUrl(_) => (),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:928:                WebSocketError::ConnectFailed(_) => (),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:929:                WebSocketError::HandshakeFailed(_) => (),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:930:                WebSocketError::RuntimeError(_) => (),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:939:        let e = WebSocketError::RuntimeError("peer reset".into());
quantbook-engine/crates/ql-collab-ws/src/lib.rs:950:            WebSocketError::InvalidUrl("x".into()).kind(),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:954:            WebSocketError::ConnectFailed("x".into()).kind(),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:958:            WebSocketError::HandshakeFailed("x".into()).kind(),
quantbook-engine/crates/ql-collab-ws/src/lib.rs:962:            WebSocketError::RuntimeError("x".into()).kind(),

exec
/bin/zsh -lc "nl -ba quantbook-engine/crates/ql-collab-ws/src/lib.rs | sed -n '348,398p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
   348	        // **V2 V4 V1 step 4 (Tier K6, 2026-05-21):** surface
   349	        // last_error presence in Debug output. When a transport is
   350	        // closed and both tasks have exited, knowing whether the
   351	        // error slot is populated matters more than knowing the
   352	        // booleans alone — it distinguishes "clean caller-close"
   353	        // (last_error=None) from "task observed a runtime failure"
   354	        // (last_error=Some). Per V2 V3 step 5 megaudit Opus-B L2.
   355	        f.debug_struct("WebSocketTransport")
   356	            .field("closed", &self.closed.load(Ordering::Relaxed))
   357	            .field("writer_finished", &self.writer_task.is_finished())
   358	            .field("reader_finished", &self.reader_task.is_finished())
   359	            .field("last_error_present", &self.last_error().is_some())
   360	            .finish()
   361	    }
   362	}
   363	
   364	impl WebSocketTransport {
   365	    /// Connect to a WebSocket server at `url` (e.g. `ws://host:port/path`).
   366	    ///
   367	    /// Must be called from within a tokio runtime context (the two
   368	    /// background tasks are spawned via `tokio::spawn`). The typical
   369	    /// pattern from sync code is `runtime.block_on(connect(...))`.
   370	    ///
   371	    /// On success, returns a fully-wired transport: send/recv channels
   372	    /// connected, background tasks running. On failure, returns one
   373	    /// of the [`WebSocketError`] variants with the underlying cause.
   374	    pub async fn connect(url: &str) -> Result<Self, WebSocketError> {
   375	        // tokio-tungstenite::connect_async handles URL parse + TCP
   376	        // connect + WS handshake in one call. Distinguish the failure
   377	        // modes via the returned error string match — opaque to keep
   378	        // tungstenite types out of our public API.
   379	        let (ws_stream, _response) = tokio_tungstenite::connect_async(url).await.map_err(|e| {
   380	            let msg = e.to_string();
   381	            // Coarse classification — tungstenite::Error doesn't have a
   382	            // clean discriminant for "URL parse" vs "TCP connect" vs
   383	            // "handshake". Use the error type to route.
   384	            match e {
   385	                tokio_tungstenite::tungstenite::Error::Url(_) => WebSocketError::InvalidUrl(msg),
   386	                tokio_tungstenite::tungstenite::Error::Io(_) => WebSocketError::ConnectFailed(msg),
   387	                tokio_tungstenite::tungstenite::Error::Http(_)
   388	                | tokio_tungstenite::tungstenite::Error::HttpFormat(_) => {
   389	                    WebSocketError::HandshakeFailed(msg)
   390	                }
   391	                _ => WebSocketError::HandshakeFailed(msg),
   392	            }
   393	        })?;
   394	
   395	        let (mut ws_sink, mut ws_stream) = ws_stream.split();
   396	
   397	        let (outbound_tx, mut outbound_rx) = mpsc::unbounded_channel::<Vec<u8>>();
   398	        let (inbound_tx, inbound_rx) = mpsc::unbounded_channel::<Vec<u8>>();

exec
/bin/zsh -lc "nl -ba quantbook-engine/crates/ql-collab-ws/src/lib.rs | sed -n '580,735p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
   580	    /// [`close`](Self::close), via Drop, or by either background task
   581	    /// observing a permanent failure (peer disconnect, WS Close frame,
   582	    /// IO error).
   583	    pub fn is_closed(&self) -> bool {
   584	        self.closed.load(Ordering::Relaxed)
   585	    }
   586	
   587	    /// **V2 V3 step 4 audit closure (Opus M1, 2026-05-21):** read the
   588	    /// most recent task-observed runtime error, if any. Returns `None`
   589	    /// for clean shutdowns (caller [`close`](Self::close), Drop, or
   590	    /// graceful Close frame from peer); returns `Some(RuntimeError(_))`
   591	    /// when the writer's `ws_sink.send` or the reader's `ws_stream.next`
   592	    /// surfaced an error before exiting.
   593	    ///
   594	    /// Read AFTER observing [`TransportError::Closed`] from
   595	    /// [`Transport::send`] or [`Transport::try_recv`] to distinguish
   596	    /// "peer reset" vs "auth rejected" vs "capacity exceeded" etc.
   597	    /// IDE consumers driving reconnect handshakes use this to choose
   598	    /// the right user-facing message and backoff strategy.
   599	    ///
   600	    /// Returns a clone (cheap — `WebSocketError` is small + `Clone`).
   601	    ///
   602	    /// **V2 V3 step 5 megaudit closure (Opus-B M3, 2026-05-21):**
   603	    /// poisoned mutex no longer silently returns `None`. Recovers
   604	    /// the inner via `PoisonError::into_inner()` so the slot's
   605	    /// most-recent contents stay readable across the poison boundary.
   606	    pub fn last_error(&self) -> Option<WebSocketError> {
   607	        match self.last_error.lock() {
   608	            Ok(guard) => guard.clone(),
   609	            Err(poisoned) => poisoned.into_inner().clone(),
   610	        }
   611	    }
   612	
   613	    /// Explicitly mark this transport closed. Subsequent
   614	    /// [`Transport::send`] calls return `Err(Closed)`; subsequent
   615	    /// [`Transport::try_recv`] calls drain the inbound queue first,
   616	    /// then return `Err(Closed)` (per trait contract).
   617	    ///
   618	    /// Does NOT abort the background tasks — only Drop does that. If
   619	    /// the caller wants the tasks gone, drop the transport.
   620	    ///
   621	    /// **V2 V4 V1 step 1 (Tier K1):** also notifies the
   622	    /// flush_pending Condvar so any concurrent `flush_pending` waiter
   623	    /// wakes up and observes the new closed state.
   624	    pub fn close(&self) {
   625	        self.closed.store(true, Ordering::Relaxed);
   626	        self.progress.1.notify_all();
   627	    }
   628	}
   629	
   630	impl Drop for WebSocketTransport {
   631	    fn drop(&mut self) {
   632	        // **V2 V3 step 4 audit closure (Opus M2, 2026-05-21):** the
   633	        // sole shutdown path under current design is `abort()` on both
   634	        // task handles. The graceful `outbound_rx.recv() -> None ->
   635	        // ws_sink.close()` branch in the writer task is UNREACHABLE
   636	        // FROM DROP because `Drop::drop` runs before the struct's
   637	        // fields drop (Rust drop order: body first, then fields in
   638	        // decl order). So `outbound_tx` is still alive when we call
   639	        // `writer_task.abort()`. The graceful arm only fires if a
   640	        // future "soft close" API explicitly detaches `outbound_tx`
   641	        // before drop — not part of the current MVP.
   642	        //
   643	        // `closed = true` ordering: set first so any concurrent
   644	        // sync `send`/`try_recv` observes Closed via the flag check
   645	        // before the task abort propagates.
   646	        //
   647	        // **V2 V4 V1 step 1 (Tier K1):** also notify the
   648	        // flush_pending Condvar — a caller awaiting a long-running
   649	        // flush would otherwise wait until the next wait_timeout
   650	        // tick to observe the closed flag. (Unreachable in practice
   651	        // because Drop is called on a unique `&mut self` so no
   652	        // concurrent flush_pending can be in flight, but kept for
   653	        // safety + future shared-reference APIs.)
   654	        self.closed.store(true, Ordering::Relaxed);
   655	        self.progress.1.notify_all();
   656	        self.writer_task.abort();
   657	        self.reader_task.abort();
   658	    }
   659	}
   660	
   661	impl Transport for WebSocketTransport {
   662	    fn send(&mut self, bytes: &[u8]) -> Result<(), TransportError> {
   663	        // **LOAD-BEARING** (V2 V3 step 4 audit closure, Opus M4): this
   664	        // closed-flag check MUST precede `outbound_tx.send`. The
   665	        // `collab_session_websocket_send_after_close_surfaces_closed`
   666	        // integration test depends on observing `Closed` here (the
   667	        // reader task sets `closed = true` on peer disconnect; the
   668	        // writer task may still be parked on `outbound_rx.recv` and
   669	        // hasn't yet seen the broken sink). If a future refactor
   670	        // reorders this check (e.g., to enable an outbox-queuing-
   671	        // while-closed feature), that test will flake.
   672	        if self.closed.load(Ordering::Relaxed) {
   673	            return Err(TransportError::Closed);
   674	        }
   675	        // UnboundedSender::send returns Err iff the receiver dropped.
   676	        // The receiver lives in the writer task; if it dropped, the
   677	        // task exited (panic, abort, or graceful). Treat as Closed.
   678	        self.outbound_tx
   679	            .send(bytes.to_vec())
   680	            .map_err(|_| TransportError::Closed)?;
   681	        // **V2 V4 V1 step 1 (2026-05-21) — Tier K1 ack channel.**
   682	        // Bump the queued-count counter AFTER successful mpsc enqueue.
   683	        // flush_pending() reads this as the target the writer task
   684	        // must catch up to.
   685	        self.queued_count.fetch_add(1, Ordering::SeqCst);
   686	        Ok(())
   687	    }
   688	
   689	    fn try_recv(&mut self) -> Result<Option<Vec<u8>>, TransportError> {
   690	        // Drain-before-close per `Transport` trait contract
   691	        // (transport.rs:112-115): "Closed only when channel
   692	        // permanently closed AND internal queue is empty."
   693	        use mpsc::error::TryRecvError;
   694	        match self.inbound_rx.try_recv() {
   695	            Ok(bytes) => Ok(Some(bytes)),
   696	            Err(TryRecvError::Empty) => {
   697	                if self.closed.load(Ordering::Relaxed) {
   698	                    Err(TransportError::Closed)
   699	                } else {
   700	                    Ok(None)
   701	                }
   702	            }
   703	            Err(TryRecvError::Disconnected) => {
   704	                // Reader task exited and dropped inbound_tx. Mark
   705	                // closed so subsequent calls fast-path; treat as
   706	                // Closed for this call (the queue is empty AND the
   707	                // upstream is permanently gone).
   708	                self.closed.store(true, Ordering::Relaxed);
   709	                Err(TransportError::Closed)
   710	            }
   711	        }
   712	    }
   713	
   714	    /// **V2 V3 step 5 megaudit closure (Opus-A H1, 2026-05-21):**
   715	    /// override the default `None` to expose the runtime-error cause
   716	    /// through the trait so [`ql_collab::CollabSession::transport_last_error`]
   717	    /// can reach it after `attach_transport` consumes the concrete
   718	    /// type into `Box<dyn Transport + Send>`.
   719	    ///
   720	    /// Returns the underlying [`WebSocketError`]'s `Display` string.
   721	    /// Consumers wanting to discriminate handshake/connect/runtime
   722	    /// reasons can pattern-match on substrings or use the prefix
   723	    /// (`"WebSocket runtime error: ..."`) to detect the variant —
   724	    /// for stronger typing keep a parallel reference to the concrete
   725	    /// transport before attach.
   726	    fn last_error(&self) -> Option<String> {
   727	        Self::last_error(self).map(|e| e.to_string())
   728	    }
   729	
   730	    /// **V2 V4 V1 step 1 (2026-05-21) — Tier K1 ack channel.**
   731	    /// Block until every byte blob previously queued via `send` has
   732	    /// been written to the WebSocket sink. Closes the V2 V3 step 5
   733	    /// megaudit's convergent finding (queued-vs-acked semantics).
   734	    ///
   735	    /// Mechanism: reads `queued_count` as the target; locks the

exec
/bin/zsh -lc "rg -n \"websocket_runtime_error|transportLastError\\(|transport_last_error\\(\" quantbook-engine/crates/ql-bindings-node/src/lib.rs quantbook-engine/crates/ql-collab-ws/src/lib.rs quantbook-engine/crates/ql-collab/src/session.rs /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:821:		assert.strictEqual(session.transportLastError(), null);
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:829:		assert.strictEqual(session.transportLastError(), null);
quantbook-engine/crates/ql-collab/src/session.rs:841:    pub fn transport_last_error(&self) -> Option<String> {
quantbook-engine/crates/ql-collab/src/session.rs:1140:    ///   reachable via `transport_last_error()`).
quantbook-engine/crates/ql-collab-ws/src/lib.rs:257:            WebSocketError::RuntimeError(_) => "websocket_runtime_error",
quantbook-engine/crates/ql-collab-ws/src/lib.rs:963:            "websocket_runtime_error"
quantbook-engine/crates/ql-bindings-node/src/lib.rs:742:    pub fn transport_last_error(&self) -> Option<String> {
quantbook-engine/crates/ql-bindings-node/src/lib.rs:744:        inner.transport_last_error()
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:187:	'websocket_runtime_error',
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:199:	transportLastError(): string | null;
/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:510:	| 'websocket_runtime_error'

exec
/bin/zsh -lc "nl -ba quantbook-engine/crates/ql-collab/src/session.rs | sed -n '820,846p'" in /Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook
 succeeded in 0ms:
   820	    /// currently attached.
   821	    pub fn has_transport(&self) -> bool {
   822	        self.transport.is_some()
   823	    }
   824	
   825	    /// **Phase 5.5 V2 V3 step 5 megaudit closure (Opus-A H1,
   826	    /// 2026-05-21):** proxy through to the attached transport's
   827	    /// [`Transport::last_error`]. Returns `None` if no transport is
   828	    /// attached OR the transport reports no error.
   829	    ///
   830	    /// Use for IDE reconnect handshakes: after observing
   831	    /// `Err(CollabSessionError::Transport(TransportError::Closed))`
   832	    /// from any mutator or `flush_*_to_transport` call, query this
   833	    /// to distinguish the underlying cause (e.g., `WebSocketTransport`
   834	    /// surfaces `"peer reset"` vs `"capacity exceeded"`). Choose the
   835	    /// retry strategy accordingly — e.g., immediate reconnect for
   836	    /// transient I/O vs auth-prompt for `HandshakeFailed("401")`.
   837	    ///
   838	    /// Returns `Option<String>` (lossy) to keep `Transport` trait-
   839	    /// dyn-compatible without leaking impl-specific error types
   840	    /// across the public API.
   841	    pub fn transport_last_error(&self) -> Option<String> {
   842	        self.transport.as_ref().and_then(|t| t.last_error())
   843	    }
   844	
   845	    /// **Phase 5.5 V2 V4 V1 step 1 (2026-05-21) — Tier K1 ack channel.**
   846	    /// Block until every byte blob previously queued via the attached

codex
**VERDICT**: PASS-WITH-FINDINGS

**Strongest concern**: `websocket_runtime_error` is defined as a structured code, but current runtime WebSocket failures still cross the attached-transport path as raw `transportLastError()` strings, not prefixed parseable `Error.message`s.

**Findings**
- **L**: `websocket_runtime_error` is in the Rust kind set and IDE union, but I found no current napi rejection path that emits `[websocket_runtime_error]`. `websocketConnect` uses the helper, but connect errors classify into invalid URL / connect failed / handshake failed; runtime task failures are exposed through `Transport::last_error()` as `e.to_string()`, and the binding returns that raw string. See [ql-collab-ws/src/lib.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-collab-ws/src/lib.rs:257), [ql-collab-ws/src/lib.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-collab-ws/src/lib.rs:720), [ql-bindings-node/src/lib.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-bindings-node/src/lib.rs:741), and [types.ts](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:510). If that code is intentionally future-facing, document the reachability boundary.

**Match Coverage Verdicts**
- **Q1**: VERIFIED. `TransportError`, `WebSocketError`, and `CollabSessionError` matches cover all current variants with no wildcard arms; because each match is inside the defining crate, adding a variant without updating `kind()` will fail Rust exhaustiveness checking. See [transport.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-collab/src/transport.rs:138), [ql-collab-ws/src/lib.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-collab-ws/src/lib.rs:252), [session.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-collab/src/session.rs:173).
- **Q2**: VERIFIED. Engine-error `.map_err` call sites route through `collab_session_error_to_napi`, `transport_error_to_napi`, or `websocket_error_to_napi`; remaining closure-style `Error::from_reason(format!(... {e}))` sites are join/fixture paths, not engine error discriminants. Helpers are at [lib.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-bindings-node/src/lib.rs:294).
- **Q3**: VERIFIED. All kind strings are lowercase snake_case with letters/underscores only, matching `/^\[([a-z][a-z_]*)\]\s*(.*)$/s`; parser is at [session.ts](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:201).
- **Q4**: VERIFIED. `CollabSessionError::Transport(inner)` returns `inner.kind()`, so `TransportError::Closed` maps to code `transport_closed`; combined with Display and helper formatting, the message is `[transport_closed] transport error: transport closed`. See [session.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-collab/src/session.rs:144), [session.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-collab/src/session.rs:178), [lib.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-bindings-node/src/lib.rs:294).

**Other Audit Notes**
- V2.3 substring compatibility is not vacuous: old tests still assert `/invalid WebSocket URL/` and `/WebSocket connection failed/`, and V2.7 additionally asserts the raw invalid-URL message has both substring and prefix. See [quantbook-roundtrip.test.ts](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1056) and [quantbook-roundtrip.test.ts](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1591).
- The drift fallback behavior is appropriate for `parseQuantbookError`: it preserves the original message and avoids throwing while already handling an error. Treat `code === 'unknown'` as an escalation signal at call sites rather than making the parser throw.
- Regex robustness is acceptable for current engine kinds. Embedded brackets in the Display body cannot evade parsing because the helper-owned prefix is anchored at message start.
- The parse JSDoc’s three fallback cases all exist as separate branches: non-`Error`, no prefix, unrecognized prefix. See [session.ts](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:218) and [session.ts](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:227).

**Recommended Changes**
- Before closure, either document that `websocket_runtime_error` is currently parseable but not emitted by the attached-transport `transportLastError()` path, or add a structured last-error accessor if IDE logic is expected to branch on that code.
- Non-blocking cleanup: derive `KNOWN_QUANTBOOK_ERROR_CODES` from a const tuple and derive the union from it, removing the manual set/union drift hazard.
tokens used
133,695
**VERDICT**: PASS-WITH-FINDINGS

**Strongest concern**: `websocket_runtime_error` is defined as a structured code, but current runtime WebSocket failures still cross the attached-transport path as raw `transportLastError()` strings, not prefixed parseable `Error.message`s.

**Findings**
- **L**: `websocket_runtime_error` is in the Rust kind set and IDE union, but I found no current napi rejection path that emits `[websocket_runtime_error]`. `websocketConnect` uses the helper, but connect errors classify into invalid URL / connect failed / handshake failed; runtime task failures are exposed through `Transport::last_error()` as `e.to_string()`, and the binding returns that raw string. See [ql-collab-ws/src/lib.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-collab-ws/src/lib.rs:257), [ql-collab-ws/src/lib.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-collab-ws/src/lib.rs:720), [ql-bindings-node/src/lib.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-bindings-node/src/lib.rs:741), and [types.ts](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/types.ts:510). If that code is intentionally future-facing, document the reachability boundary.

**Match Coverage Verdicts**
- **Q1**: VERIFIED. `TransportError`, `WebSocketError`, and `CollabSessionError` matches cover all current variants with no wildcard arms; because each match is inside the defining crate, adding a variant without updating `kind()` will fail Rust exhaustiveness checking. See [transport.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-collab/src/transport.rs:138), [ql-collab-ws/src/lib.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-collab-ws/src/lib.rs:252), [session.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-collab/src/session.rs:173).
- **Q2**: VERIFIED. Engine-error `.map_err` call sites route through `collab_session_error_to_napi`, `transport_error_to_napi`, or `websocket_error_to_napi`; remaining closure-style `Error::from_reason(format!(... {e}))` sites are join/fixture paths, not engine error discriminants. Helpers are at [lib.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-bindings-node/src/lib.rs:294).
- **Q3**: VERIFIED. All kind strings are lowercase snake_case with letters/underscores only, matching `/^\[([a-z][a-z_]*)\]\s*(.*)$/s`; parser is at [session.ts](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:201).
- **Q4**: VERIFIED. `CollabSessionError::Transport(inner)` returns `inner.kind()`, so `TransportError::Closed` maps to code `transport_closed`; combined with Display and helper formatting, the message is `[transport_closed] transport error: transport closed`. See [session.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-collab/src/session.rs:144), [session.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-collab/src/session.rs:178), [lib.rs](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab-quantbook/quantbook-engine/crates/ql-bindings-node/src/lib.rs:294).

**Other Audit Notes**
- V2.3 substring compatibility is not vacuous: old tests still assert `/invalid WebSocket URL/` and `/WebSocket connection failed/`, and V2.7 additionally asserts the raw invalid-URL message has both substring and prefix. See [quantbook-roundtrip.test.ts](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1056) and [quantbook-roundtrip.test.ts](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/test/quantbook-roundtrip.test.ts:1591).
- The drift fallback behavior is appropriate for `parseQuantbookError`: it preserves the original message and avoids throwing while already handling an error. Treat `code === 'unknown'` as an escalation signal at call sites rather than making the parser throw.
- Regex robustness is acceptable for current engine kinds. Embedded brackets in the Display body cannot evade parsing because the helper-owned prefix is anchored at message start.
- The parse JSDoc’s three fallback cases all exist as separate branches: non-`Error`, no prefix, unrecognized prefix. See [session.ts](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:218) and [session.ts](/Users/sanzhar/Documents/Sanzhar/Sanzhar/quantlab/quantlab/extensions/quantlab/src/quantbook/session.ts:227).

**Recommended Changes**
- Before closure, either document that `websocket_runtime_error` is currently parseable but not emitted by the attached-transport `transportLastError()` path, or add a structured last-error accessor if IDE logic is expected to branch on that code.
- Non-blocking cleanup: derive `KNOWN_QUANTBOOK_ERROR_CODES` from a const tuple and derive the union from it, removing the manual set/union drift hazard.
